{
  "version": 1,
  "spec": "spec/0.37.0.md",
  "created_at": "0001-01-01T00:00:00Z",
  "updated_at": "2026-09-02T04:10:54.296749Z",
  "workflow": {},
  "coverage": {
    "total_sections": 9,
    "mapped_sections": 7,
    "context_only": [
      "# tp v0.37.0 — Audit convergence",
      "## 5. Non-Goals"
    ],
    "unmapped": []
  },
  "tasks": [
    {
      "id": "converge-field",
      "title": "audit_converge_on on the workflow types with built-in all",
      "status": "done",
      "tags": [
        "field"
      ],
      "depends_on": [],
      "estimate_minutes": 8,
      "acceptance": "model.Workflow and model.WorkflowOverride carry AuditConvergeOn; engine/projectconfig.go parses workflow.audit_converge_on with the same string-type warning as its twin; ResolveWorkflowLayers merges task override over project config over the built-in `all`; tp config --resolved reports it with its source on a repo with no configuration. Section 7 row 1 passes.",
      "source_sections": [
        "## 2. `audit_converge_on`"
      ],
      "started_at": "2026-09-02T00:37:41.341321Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T00:45:47.166325Z",
      "closed_reason": "audit_converge_on now exists on the workflow types and resolves to the built-in all, the deliberate asymmetry with review_converge_on's blocking that section 2.1 measures.\n- model.Workflow gains AuditConvergeOn string and model.WorkflowOverride the *string twin, counted by IsEmpty so a config setting only this field is not read as empty; verified by the package building and by TestResolveWorkflowLayers_AuditConvergeOn constructing both.\n- engine/projectconfig.go admits audit_converge_on to knownWorkflowKeys and parses it with review_converge_on's exact string-type warning; verified by an assertion over parseWorkflowOverride({\"audit_converge_on\":7}) requiring the field nil and the warning 'workflow.audit_converge_on: expected a string, ignored'.\n- ResolveWorkflowLayers merges task override over project config over the built-in via pickString; verified in TestResolveWorkflowLayers_AuditConvergeOn that the task override wins over project, that project applies with no override, and that an invalid stored value resolves raw rather than clamped.\n- tp config --resolved reports the field with its source; verified by two CLI tests run through the freshly built binary: TestAuditConvergeOnDefaultAll gets value all / source default on a repo with no configuration, and TestAuditConvergeOnResolvedNamesItsSource gets project then override as the layers are added.\n- Section 7 row 1 passes, and its mutant was constructed rather than reasoned about: the built-in was first shipped as AuditConvergeOnBlocking and both default assertions were observed failing with 'expected all, actual blocking' (engine configresolve_test.go:100, cli auditconvergeon_test.go:37) while every other assertion in the same tests stayed green, which is what isolates the default from the precedence; switching the built-in to AuditConvergeOnAll turned both green. Dogfooded on tp's own repo: config --resolved reports audit_converge_on all/default beside review_converge_on blocking/default. Gate green end to end, exit 0.\nOut of scope, each owned by a later task in this cycle: the write sinks (set --workflow still rejects audit_converge_on as an unknown workflow field, which is what keeps TestAuditGate_NoEscapeHatchFromTheGate green until reverse-gate-guard reverses it), the consuming-sink validation, ValidAuditConvergeOn and its hint, config --extract hoisting and validate --project, the unattended fence, the severity predicate, by_severity, and the documentation.",
      "gate_passed_at": "2026-09-02T00:45:47.166325Z",
      "commit_sha": "da9e04d",
      "commit_shas": [
        "da9e04d",
        "c30004c",
        "05a8df0",
        "311dfcc",
        "8c1d4e7",
        "33d4b05",
        "090de4c",
        "5cafb7a",
        "9f00bac"
      ],
      "commit_files": [
        "internal/cli/auditconvergeon_test.go",
        "internal/cli/config.go",
        "internal/engine/auditconvergeon.go",
        "internal/engine/configresolve.go",
        "internal/engine/configresolve_test.go",
        "internal/engine/projectconfig.go",
        "internal/model/projectconfig.go",
        "internal/model/workflow.go"
      ]
    },
    {
      "id": "converge-resolution-layers",
      "title": "Assert the resolver consults exactly two layers",
      "status": "done",
      "tags": [
        "field"
      ],
      "depends_on": [
        "converge-field"
      ],
      "estimate_minutes": 6,
      "acceptance": "A test asserts task override beats project config beats built-in, and that neither a TP_AUDIT_CONVERGE_ON environment variable nor an --audit-converge-on flag changes what resolves — the mutant is adding either layer to engine.ResolveWorkflowLayers. Section 7 row 2 passes.",
      "source_sections": [
        "## 2. `audit_converge_on`"
      ],
      "started_at": "2026-09-02T01:00:10.432165Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T01:02:36.583073Z",
      "closed_reason": "v0.37.0 §7 row 2: three tests assert audit_converge_on resolves through exactly two layers, and each was proved discriminating by running its named mutant.\n- Precedence: TestResolveWorkflowLayers_AuditConvergeOnConsultsExactlyTwoLayers/'the two layers rank' asserts task override beats project config beats built-in (all/blocking/all) in internal/engine/configresolve_test.go.\n- No environment layer: the sibling subtest sets TP_AUDIT_CONVERGE_ON=blocking — the value neither real layer names — and asserts all resolves in all three configurations, so the assertion is not the tautology an unset variable would give; TestAuditConvergeOnHasNoEnvironmentLayer repeats it end to end through a tp config --resolved child, asserting value=all and source=default.\n- No flag layer: TestAuditConvergeOnHasNoCommandFlag asserts tp audit/config/set/run/review each refuse --audit-converge-on with exit 2 and 'unknown flag: --audit-converge-on', so silent acceptance is what fails.\n- Mutant A (env layer at the TOP rank of engine.ResolveWorkflowLayers): all three environment assertions red (got blocking, wanted all), 'the two layers rank' green, TestAuditConvergeOnHasNoEnvironmentLayer red, the flag subtests and the shipped row-1 tests green.\n- Mutant A2 (same layer at the LOWEST rank, below project config): only the built-in case red, the two rank-pinning cases and 'the two layers rank' green — so the row is caught at any rank, not just the top.\n- Mutant B (a real --audit-converge-on flag registered on tp audit): only the audit subtest red, config/set/run/review green, both environment tests green — per-command discrimination.\n- Reverted both mutants (git diff on internal/engine/configresolve.go and internal/cli/audit.go empty) and the quality gate passed on the final tree: suite-state wrapper ok, golangci-lint 0 issues, deadcode and complexity clean, exit 0.",
      "gate_passed_at": "2026-09-02T01:02:36.583073Z",
      "commit_sha": "34ced11",
      "commit_shas": [
        "34ced11",
        "91e33b6",
        "b879360"
      ],
      "commit_files": [
        "internal/cli/auditconvergeon_test.go",
        "internal/engine/configresolve_test.go"
      ]
    },
    {
      "id": "converge-write-sinks",
      "title": "Reject an illegal literal at both set sinks with ExitUsage",
      "status": "done",
      "tags": [
        "validation"
      ],
      "depends_on": [
        "converge-field"
      ],
      "estimate_minutes": 8,
      "acceptance": "tp set --workflow audit_converge_on=nope and its --project form exit ExitUsage with a hint naming the field and its two literals; a legal value persists. Section 7 row 3's write half passes.",
      "source_sections": [
        "## 2. `audit_converge_on`"
      ],
      "started_at": "2026-09-02T01:24:18.350658Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T01:49:05.357984Z",
      "closed_reason": "Both tp set write sinks accept audit_converge_on and refuse an illegal literal with ExitUsage, verified by running the freshly built binary rather than by reading the code.\n- The task sink (internal/cli/set.go) and the project sink (internal/cli/set_project.go) each refuse the literal nope with exit 2, the envelope error `invalid audit_converge_on value \"nope\"` and the hint `must be one of: blocking, all`. Asserted on the exit status and on the decoded envelope in TestAuditConvergeOn_WriteSinksRefuseIllegalLiteral, and re-run here against a scratch project on the dev binary: both forms printed that envelope and exited 2.\n- A refused write leaves the tree untouched, which is the half a refusal test can miss: the task file is byte-identical before and after (shasum compared across both refusals), and the refused project write creates no .tp/config.json.\n- A legal value persists at both sinks and resolves back with its layer named: after the project write, tp config --resolved reports value blocking source project; after a task override is added on top, value all source override. Pinned by TestAuditConvergeOn_LegalLiteralPersistsAtProjectSink and _AtTaskSink.\n- Both sinks call engine.ValidAuditConvergeOn and engine.AuditConvergeOnHint rather than re-deriving the legal set or the wording (set.go:399-400, set_project.go:82-83), so a future change to the enum reaches both paths at once.\n- Five mutants were built and run in an rsync copy by the unit that wrote this code: four killed, and M5 - reusing the review twin's predicate and hint at these sinks - survives and is undetectable by design. That is confirmed from source here rather than taken on trust: AuditConvergeOnHint is byte-identical to ReviewConvergeOnHint by intent (auditconvergeon.go documents why), and ValidAuditConvergeOn accepts exactly the two literals its twin does, so no input can separate the two implementations today. Inventing a divergence to make it testable was declined.\n- The persistence half of those tests was shown to discriminate by an independent sixth mutant built while closing reverse-gate-guard: a project sink that accepts audit_converge_on and persists nothing was killed by TestAuditConvergeOn_LegalLiteralPersistsAtProjectSink.\n- 20556e0 is a refactor this sink forced rather than an unrelated cleanup: golangci-lint's unparam rejects a resolved-field helper whose parameter only ever receives one literal.\n- Quality gate green on the tree this closes: check-suite-state.sh, golangci-lint run reporting 0 issues, check-deadcode.sh and check-complexity.sh, exit 0.\nOut of scope, and reported rather than hidden: these six commits ALONE leave the suite red. Making the field settable reddens the two assertions in TestAuditGate_NoEscapeHatchFromTheGate that assert it is unknown, which is reverse-gate-guard's work, so the two changes are one atomic edit and the task graph has the dependency backwards. The reversal was therefore made first and the gate above was run over both; the commit lists are kept separate.",
      "gate_passed_at": "2026-09-02T01:49:05.357984Z",
      "commit_sha": "4362778",
      "commit_shas": [
        "4362778",
        "0fcd69b",
        "20556e0",
        "5692e79",
        "1cfc580",
        "cc14f64"
      ],
      "commit_files": [
        "internal/cli/auditconvergeon_test.go",
        "internal/cli/auditconvergeon_write_test.go",
        "internal/cli/set.go",
        "internal/cli/set_project.go"
      ]
    },
    {
      "id": "converge-consume-sinks",
      "title": "Reject an illegal stored value at the audit sinks with ExitValidation",
      "status": "done",
      "tags": [
        "validation"
      ],
      "depends_on": [
        "converge-resolution-layers"
      ],
      "estimate_minutes": 8,
      "acceptance": "An illegal value stored in .tp/config.json or in a task file makes tp audit --record and tp audit --status exit ExitValidation, a code distinct from the write sinks'. Section 7 row 3's consume half passes.",
      "source_sections": [
        "## 2. `audit_converge_on`"
      ],
      "started_at": "2026-09-02T01:03:45.229088Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T01:11:14.7935Z",
      "closed_reason": "Both audit consuming sinks now refuse an illegal stored audit_converge_on with ExitValidation, and the refusal precedes every write.\n\n- An illegal value stored in .tp/config.json makes both `tp audit \u003cspec\u003e --record` and `tp audit \u003cspec\u003e --status` exit 1 (ExitValidation): TestAuditConvergeOn_InvalidStoredInProjectConfigExitsValidation asserts the exit code, the quoted value and the hint through the parsed error envelope.\n- An illegal value stored in a task file's workflow block does the same on both sinks: TestAuditConvergeOn_InvalidStoredInTaskFileExitsValidation. The two layers are separate tests, not subtests of one fixture, because they are reached differently — dropping the project layer from resolution leaves the task-file test green and reddens the project-config one (mutant M5, run).\n- The code is ExitValidation (1) and is distinct from the write sinks' ExitUsage (2): the guard is `engine.ValidAuditConvergeOn` with `engine.AuditConvergeOnHint`, byte-identical to ReviewConvergeOnHint as section 2 requires (\"the same hint as review_converge_on\"). Mutant M1, mapping both consume sinks to ExitUsage, reddens four assertions across the two tests.\n- Section 7 row 3's consume half passes; the write half stays with converge-write-sinks. Row 3's named mutant (\"map both to one code\") is M1 above. Two further half-graded mutants were built and run: deleting only the --status guard (M3) and deleting only the --record guard (M4) each redden their own half and leave the other green.\n- The ordering hazard did NOT require reordering, and the reason was measured rather than assumed. runAuditRecord already resolves the workflow once into wfPre at the top, before EnsureReviewState and recordAuditRoundEntry, so the guard was placed there; nothing above that line writes. Mutant M2 — the same guard moved onto the later `wf` resolve, after the round is stored — was built and run: every exit-code assertion stayed GREEN while only \"a refused --record wrote no audit-round-1.ndjson\" and \"... no state.json\" reddened, which is the defect an exit-code-only check cannot see.\n- Verified by running, not by reading: on the shipped binary before the change, `tp audit spec.md --record` over a tree storing \"bogus\" exited 0 and wrote .tp-review/spec/audit-round-1.ndjson plus state.json; the new tests were watched failing on exactly those five assertions before the guards were added. After the change the same tree exits 1 and creates no .tp-review directory at all, while a legal `blocking` still records round 1 — dogfooded with a freshly built /tmp/tp-dev/tp.\n- Quality gate green: `./scripts/check-suite-state.sh \u0026\u0026 golangci-lint run \u0026\u0026 ./scripts/check-deadcode.sh \u0026\u0026 ./scripts/check-complexity.sh` exited 0 (cli 47.7s, engine 30.4s, all packages ok; golangci-lint \"0 issues.\"; deadcode and complexity silent-clean).",
      "gate_passed_at": "2026-09-02T01:11:14.7935Z",
      "commit_sha": "ec47da6",
      "commit_shas": [
        "ec47da6",
        "fb19231",
        "8aece9d",
        "cc04c71"
      ],
      "commit_files": [
        "internal/cli/audit_record.go",
        "internal/cli/auditconvergeon_test.go",
        "internal/engine/auditconvergeon.go"
      ]
    },
    {
      "id": "clean-predicate",
      "title": "The severity predicate that grades a round under the resolved policy",
      "status": "done",
      "tags": [
        "engine"
      ],
      "depends_on": [
        "converge-field"
      ],
      "estimate_minutes": 10,
      "acceptance": "One engine predicate over (rows, policy): under `all` clean iff no non-PASS row; under `blocking` clean iff no non-PASS row carries a blocking severity, where `error` blocks and a severity outside the enum, JSON null, an absent key or a non-string value all block. Four fixtures, one per unrecognised shape; the mutant that must fail is grading a row with no usable severity as advisory. Section 7 rows 5, 6, 7 and 8 pass.",
      "source_sections": [
        "## 2. `audit_converge_on`"
      ],
      "started_at": "2026-09-02T00:47:06.791525Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T00:53:44.686687Z",
      "closed_reason": "- One engine predicate over (rows, policy): engine.AuditRowsClean(rows []map[string]any, convergeOn string) bool in internal/engine/auditclean.go, a pure function of its two arguments because section 2 stamps clean at record time rather than recomputing it live.\n- Under all, clean iff the round holds no non-PASS row: the loop returns false on the first row AuditRowIsPass rejects, and any policy literal other than the exact string blocking takes that arm, so an unrefused value grades under the stricter default.\n- Under blocking, clean iff no non-PASS row carries a blocking severity: auditSeverityBlocking names the advisory half of the vocabulary (warning, info) and blocks by exclusion, so error blocks and a string outside the enum, JSON null, an absent key and a non-string value all block. The comparison is byte-for-byte like AuditRowIsPass, so WARNING is outside the enum and blocks, which keeps section 4 identity error plus unrecognised equals the blocking rows true. It never consults engine.SeverityRank, which is built for the review words and ranks every audit severity unknown.\n- Four fixtures, one per unrecognised shape: TestAuditRowsClean_UnusableSeverityBlocks subtests a string outside the enum, JSON null, an absent key, and a value that is not a string, each unclean under blocking and under all.\n- The mutant that must fail is grading a row with no usable severity as advisory: built as blocking iff the severity type assertion equals error, and run. It reddened exactly the four row-8 subtests at auditclean_test.go:77, the blocking assertion, while rows 5, 6 and 7 stayed green. Three further named mutants were run for discrimination: grade on status alone reddened rows 5 and 6 only; return clean whenever the policy is blocking reddened rows 7 and 8 only; special-case info only reddened row 6 alone, which is why section 7 asserts it apart from row 5. The real implementation then turned all six functions and four subtests green.\n- Section 7 rows 5, 6, 7 and 8 pass: TestAuditRowsClean_InfoIsAdvisory, _WarningIsAdvisory, _ErrorBlocksUnderBoth and _UnusableSeverityBlocks all PASS, plus _PassRowsAreNotGraded (PASS rows carry severity null, so a predicate over rows of any severity would report every all-PASS round unclean) and _UnknownPolicyGradesAsAll.\n- Quality gate green: check-suite-state.sh, golangci-lint run with 0 issues, check-deadcode.sh and check-complexity.sh all passed, exit 0.",
      "gate_passed_at": "2026-09-02T00:53:44.686687Z",
      "commit_sha": "fe34e8e",
      "commit_shas": [
        "fe34e8e",
        "09980fd",
        "43d8e12",
        "1dad5f3",
        "8485d9a",
        "4bf40e9",
        "e3ae4fd"
      ],
      "commit_files": [
        "internal/engine/auditclean.go",
        "internal/engine/auditclean_test.go"
      ]
    },
    {
      "id": "record-time-stamping",
      "title": "Stamp clean at record time, both directions, and never re-grade on read",
      "status": "done",
      "tags": [
        "engine"
      ],
      "depends_on": [
        "clean-predicate",
        "converge-consume-sinks"
      ],
      "estimate_minutes": 12,
      "acceptance": "tp audit --record stamps the round's clean from the predicate under the policy resolved at record time; recording under all then setting blocking without re-recording leaves every round's clean unchanged in state.json AND as tp audit --status reports it; a round recorded under blocking keeps clean:true after the knob returns to all. The mutant is re-grading each stored round from its rows in the reporting path. Section 7 rows 9 and 9b pass.",
      "source_sections": [
        "## 2. `audit_converge_on`"
      ],
      "started_at": "2026-09-02T01:12:27.098349Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T01:22:50.570819Z",
      "closed_reason": "tp audit --record now stamps clean from engine.AuditRowsClean under the resolved audit_converge_on; the predicate had no production caller before this.\n\n- clean is stamped once at record time from the predicate under the resolved policy: internal/cli/audit_record.go replaces `clean := findings == 0` (the `all` policy written as an equality) with engine.AuditRowsClean(rows, wfPre.AuditConvergeOn); countAuditFindings becomes parseAuditRows and returns the rows, because the blocking predicate grades each non-PASS row's severity and a tally has discarded it\n- the policy is taken from wfPre, the resolve that precedes every state write, not from the wf resolved after recordAuditRoundEntry: the round is stored before that second resolution, so a stamp from it would grade a round already in a store section 2 declares immutable\n- recording under all then setting blocking without re-recording leaves every round's clean unchanged in state.json AND as tp audit --status reports it: TestAuditRecord_StampSurvivesTighteningTheKnob asserts the reported audit_rounds[].clean, consecutive_clean, converged and the verbatim state.json bytes over two rounds; row 9's named mutant (re-grade each stored round from its rows in the reporting path) reddens the first three and leaves the bytes green, exactly as the spec predicts\n- a round recorded under blocking keeps clean:true after the knob returns to all: TestAuditRecord_StampSurvivesRelaxingTheKnob was red before the fix for the expected reason, and asserts findings:1 beside clean:true so the verdict is visibly not the count; run at both stored layers because a stamp reading only one of them is green on the other\n- five mutants built and run in an rsync copy, red/green matrix recorded: row 9's named mutant, row 9b's read-side floor (leaves row 9 wholly green), re-grade only the derived signals (leaves audit_rounds[].clean honest), store the old equality while reporting the new stamp (leaves the --record payload honest), and drop the project layer (reddens only the project subtest)\n- gate green: ./scripts/check-suite-state.sh \u0026\u0026 golangci-lint run \u0026\u0026 ./scripts/check-deadcode.sh \u0026\u0026 ./scripts/check-complexity.sh, exit 0; dogfooded on the freshly built /tmp/tp-dev/tp — recorded under blocking gives findings 1 / clean true, and clean survives the knob returning to all in both state.json and --status",
      "gate_passed_at": "2026-09-02T01:22:50.570819Z",
      "commit_sha": "d0cc236",
      "commit_shas": [
        "d0cc236",
        "c7ced2e",
        "e7ca611"
      ],
      "commit_files": [
        "internal/cli/audit_category_sink_test.go",
        "internal/cli/audit_record.go",
        "internal/cli/audit_stamping_test.go",
        "internal/cli/record_idempotent_test.go"
      ]
    },
    {
      "id": "next-action-count",
      "title": "next_action names the accepted count on both changed branches",
      "status": "done",
      "tags": [
        "engine"
      ],
      "depends_on": [
        "record-time-stamping"
      ],
      "estimate_minutes": 12,
      "acceptance": "AuditNextAction gains the count of the latest recorded round's non-PASS rows; on both audit sinks, a round holding accepted rows renders that numeral in the recommendation on the converged branch AND on the clean-but-not-yet-converged branch, textually different from an empty round's. The boolean latestRoundHasFindings is not kept beside a count that subsumes it — this repo enables unparam. Section 7 rows 10 and 10b pass.",
      "source_sections": [
        "## 2. `audit_converge_on`"
      ],
      "started_at": "2026-09-02T03:24:22.379405Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T03:36:32.593655Z",
      "closed_reason": "AuditNextAction's third argument is now the latest recorded round's stamped clean verdict and its fourth the count of that round's non-PASS rows; the boolean latestRoundHasFindings, which folded the two questions audit_converge_on separates, is gone from the signature and from both sinks.\n- Both audit sinks pass the pair from the same round: internal/cli/audit_record.go's --record path passes (clean, findings) and its --status path reads Clean and Findings off the latest stored round, so neither surface names rows the other is silent about.\n- On the converged branch a round holding accepted rows renders 'converged over 3 accepted rows — implementation verified, proceed to release' against an empty round's unchanged 'converged — implementation verified, proceed to release'; on the clean-but-not-yet-converged branch it renders '3 accepted rows carried forward — run the next audit round: tp audit spec.md --record \u003cfile\u003e' against the unchanged 'run the next audit round: tp audit spec.md --record \u003cfile\u003e'. Both verified by dogfooding the freshly built binary on a blocking-configured project, on both sinks.\n- Section 7 rows 10 and 10b pass as internal/cli/audit_nextaction_count_test.go, each covering both sinks as subtests and asserting textual difference against an empty round rather than mere presence; internal/engine/nextaction_test.go adds the unit-level pair plus the noun-agreement boundary at 1, 2 and 0 and a pin that the fix-and-re-audit branch stays silent about the count.\n- The tests were written first and watched fail on all four sink/branch combinations for the expected reason before any code changed.\n- Four mutants were built in an rsync copy with the baseline from the unmutated tree and all four were killed: row 10's named mutant (converged arm ignores the count) reddened the converged tests only, row 10b's named mutant (count in the converged arm only) reddened the clean-round tests only, an unnamed variant leaving the --status sink at the pre-release call reddened exactly the --status subtests while every --record subtest stayed green, and a second unnamed variant branching on findings \u003e 0 instead of !clean reddened the clean-round tests by routing a blocking-clean round to the fix directive.\n- Quality gate green on the final tree: check-suite-state.sh, golangci-lint run (0 issues, unparam included), check-deadcode.sh, check-complexity.sh.",
      "gate_passed_at": "2026-09-02T03:36:32.593655Z",
      "commit_sha": "3acef8f",
      "commit_shas": [
        "3acef8f",
        "6b7a740",
        "a811138",
        "1b92eb4",
        "52c301a",
        "adbcf15"
      ],
      "commit_files": [
        "internal/cli/audit_nextaction_count_test.go",
        "internal/cli/audit_record.go",
        "internal/cli/audit_signal_test.go",
        "internal/engine/nextaction.go",
        "internal/engine/nextaction_test.go"
      ]
    },
    {
      "id": "config-surfaces",
      "title": "Hoist the field in config --extract and check it in validate --project",
      "status": "done",
      "tags": [
        "config"
      ],
      "depends_on": [
        "converge-resolution-layers"
      ],
      "estimate_minutes": 8,
      "acceptance": "hoistedFields includes audit_converge_on so tp config --extract hoists it when every scanned task file carries it; tp validate --project accepts a legal value and rejects an illegal one; tp config --extract emits the field. Section 7 row 4 passes.",
      "source_sections": [
        "## 2. `audit_converge_on`"
      ],
      "started_at": "2026-09-02T02:36:00.49163Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T02:48:29.50886Z",
      "closed_reason": "v0.37.0 §7 row 4: tp config --extract hoists audit_converge_on and tp validate --project reports it, each half asserted separately because the row's named mutant reaches only one of them.\n- Extract half: computeCommonPolicy, hoistedFields and mergeCommon all carry the field (internal/cli/config_extract.go). TestConfigExtract_HoistsAuditConvergeOn asserts the payload emits it, .tp/config.json receives it and the task file loses it — the three together, since a hoist that strips without writing destroys the value and either assertion alone passes that. TestConfigExtract_AuditConvergeOnStillResolvesAfterTheHoist reads the same fact through tp config --resolved: blocking/override before, blocking/project after.\n- Validate half: workflowDeviations compares the two layers (internal/cli/validate_project.go). TestValidateProject_AuditConvergeOn_AcceptsLegalRejectsIllegal runs the command: a legal value the project also holds is accepted (exit 0, nothing reported), an illegal one against a legal project policy is reported and refused by --strict with exit 1, and a legal value that contradicts the policy is reported on the same terms. It does not grade the literal, because §2 places that refusal at the write sinks (exit 2) and the consuming audit sinks (exit 1).\n- Red first: all six new tests were run before the implementation and failed for the expected reasons — hoisted was [review_max_rounds], .tp/config.json held only review_max_rounds, a.tasks.json still held the field, and the deviations array was empty. TestComputeCommonPolicy_AuditConvergeOnNotHoistedWhenDivergentOrAbsent passed pre-implementation and is reported as the non-discriminating negative guard it is.\n- Mutant matrix, built in an rsync copy with the baseline from the unmutated tree, five mutants, no survivors. M1, the named mutant (drop the field from hoistedFields): kills the two extract e2e tests, the computeCommonPolicy test and the fence test; leaves both validate tests GREEN, which is why the halves are asserted separately. M2 (variant, drop from mergeCommon only — hoisted and stripped but never written, so the value is destroyed): kills TestMergeCommon_WritesAuditConvergeOn and both extract e2e tests. M3 (variant, drop from computeCommonPolicy only): kills the same extract set. M4 (variant, drop the comparison from workflowDeviations): kills both validate tests and leaves every extract test green — the complement of M1. M5 (variant, invert the comparator to ==): kills all three validate subtests, including the accepts subtest, which M4 alone leaves green — so the accepts half is discriminating rather than decoration.\n- The measurement fence-extract-scope needs, run on the live tree after this change: TP_UNATTENDED=1 tp config --extract over two task files both carrying blocking exits 0 and now hoists audit_converge_on, while a third spec with no task file at all moves from {source: default, value: all} to {source: project, value: blocking}. The refusing input the fence cannot see is therefore live now.\n- TestAuditConvergeOnFence_ExtractPassesWhatPreservesResolution went red on this landing exactly as its own comment predicted; its assertion moved from the task file to .tp/config.json and its comment records that the first of its two reasons is gone.\n- Quality gate on the final tree: check-suite-state.sh ok (cli, engine, fakerunner, model, output all ok), golangci-lint 0 issues, check-deadcode.sh exit 0, check-complexity.sh exit 0.",
      "gate_passed_at": "2026-09-02T02:48:29.50886Z",
      "commit_sha": "274ea0c",
      "commit_shas": [
        "274ea0c",
        "dd89d25",
        "5091668",
        "a1348c2",
        "5733160",
        "e6433f6"
      ],
      "commit_files": [
        "internal/cli/auditconvergeon_config_test.go",
        "internal/cli/auditconvergeon_fence_test.go",
        "internal/cli/config_extract.go",
        "internal/cli/config_extract_test.go",
        "internal/cli/validate_project.go",
        "internal/cli/validate_project_test.go"
      ]
    },
    {
      "id": "fence-change-rule",
      "title": "The unattended change-rule fence at every write path",
      "status": "done",
      "tags": [
        "fence"
      ],
      "depends_on": [
        "converge-write-sinks"
      ],
      "estimate_minutes": 15,
      "acceptance": "Under TP_UNATTENDED=1 a write that changes the resolved value to blocking exits 2 at tp set --workflow, its --project form, tp import and tp config --extract; a write of all, a --project write of blocking beneath a task override of all, and an import carrying an already-resolved blocking forward all pass. The fence is string-shaped and placed before the type dispatch, not a widening of the numeric fence. Every test sets the variable it names rather than relying on the ambient value. Section 7 rows 12, 13, 13b and 13c pass.",
      "source_sections": [
        "## 3. The fence"
      ],
      "started_at": "2026-09-02T01:52:28.466452Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T02:13:05.78332Z",
      "closed_reason": "Section 3's change rule is implemented at all four write paths and proved by running.\n\n- tp set --workflow audit_converge_on=blocking exits 2 under TP_UNATTENDED=1 when it changes what resolves, leaving the task file byte-identical.\n- Its --project form exits 2 on the same terms and creates no .tp/config.json.\n- tp import exits 2, placed after the exists-guard and after the workflow-preservation step, so it compares the block that will actually be written.\n- tp config --extract calls the rule over every scanned base before the merge; the refusing input does not exist yet and that was measured, not reasoned (see the note below), so only the passing half is asserted.\n- A write of all passes at both set sinks, including over an already-resolved blocking, which tightens the gate.\n- A --project write of blocking beneath a task override of all passes: the value reaches the project layer and all still resolves.\n- An import carrying an already-resolved blocking forward passes, which is the case a field- or value-scoped fence would deadlock for ever.\n- The fence is string-shaped and its call sites sit in the string-enum arm before the integer and float dispatch; the numeric fenceWorkflowWrite is untouched, since its float64 signature runs after the literal is parsed as a number.\n- Every test decides TP_UNATTENDED for its own child through a runner that filters the variable out of os.Environ() and re-adds it only when the arm needs it; row 13c removes it rather than omitting it.\n- Rows 12, 13, 13b and 13c pass, and seven mutants were built and run in rsync copies against a baseline binary from the unmutated tree: all seven fail. The two the spec names no row for -- an import fence reading the active pointer instead of its own target, and a rule refusing any change rather than a change to blocking -- both survived the first matrix and are now killed by two added tests.\n\nGate green: check-suite-state.sh, golangci-lint, check-deadcode.sh, check-complexity.sh, exit 0. Under TP_UNATTENDED=1 the cli package's failing set is byte-identical to HEAD's 21 pre-existing failures (TestImport_WorkflowPreserved's three subtests among them); three tests this fence broke under the ambient variable now state their own environment. Out of scope and reported: --extract's widened scope, the refusal's three exits, and the escalate enum value, so the hint still says --decision other.",
      "gate_passed_at": "2026-09-02T02:13:05.78332Z",
      "commit_sha": "a10588b",
      "commit_shas": [
        "a10588b",
        "8fe8f9d",
        "7d0e95d",
        "5875b52",
        "9f36c56",
        "95cc839",
        "81328e8",
        "d957e24",
        "6660e9b",
        "e9ee9ec",
        "90a2e6d",
        "b4f881c",
        "d6bc03c"
      ],
      "commit_files": [
        "internal/cli/audit_signal_test.go",
        "internal/cli/auditconvergeon_fence_test.go",
        "internal/cli/auditconvergeon_write_test.go",
        "internal/cli/config_extract.go",
        "internal/cli/importcmd.go",
        "internal/cli/set.go",
        "internal/cli/set_project.go",
        "internal/cli/unattended.go",
        "internal/engine/auditconvergeon.go"
      ]
    },
    {
      "id": "fence-extract-scope",
      "title": "The extract fence evaluates every base the repository resolves",
      "status": "done",
      "tags": [
        "fence"
      ],
      "depends_on": [
        "fence-change-rule",
        "config-surfaces"
      ],
      "estimate_minutes": 12,
      "acceptance": "tp config --extract compares resolved-before against resolved-after for every base the repository resolves, not only the task files it scanned, and --force does not exempt it. A test builds the measured counterexample — two task files carrying the field, one spec with none — and asserts the third spec's resolution is unchanged or the command exits 2. Written after running the pre-fix behaviour and observing it pass.",
      "source_sections": [
        "## 3. The fence"
      ],
      "started_at": "2026-09-02T02:49:56.4936Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T03:06:03.687725Z",
      "closed_reason": "The extract fence now evaluates every base the repository resolves, not only the scanned task files.\n\n- tp config --extract compares resolved-before against resolved-after over the scanned task-file overrides PLUS one empty override, which is every base with no task-level override; it is one element rather than one per spec because engine.ResolveWorkflowLayers is a pure function of the two layers it is handed, so every such base resolves identically.\n- --force does not exempt it, asserted against the exists-guard's exit 4 so the row cannot pass green on the wrong guard, and paired with a --force hoist that does pass so the assertion cannot be satisfied by a --force that is always refused.\n- The measured counterexample is a test: two task files carrying blocking, one spec with none; exit 2, the third spec still resolves all, and neither the project config nor a task file was written.\n- Written after running the pre-fix behaviour and observing it pass: on the shipped binary the same tree exited 0, hoisted [review_max_rounds, audit_converge_on], and moved base c from default/all to project/blocking. The new tests were then run red against the unfixed fence for that same reason before a line of the fix was written.\n- Mutant matrix, baseline green: M1 population without the empty override, M2 --force exempts the fence, M3 the fence moved after the write, all killed. The variant the spec names nowhere, M4 the fence moved ahead of the --dry-run return, SURVIVED the first matrix and turns a read-only preview into an exit 2; the killing test was added and the whole matrix re-run with all four killed.\n- The empty override is appended unconditionally, which covers the prospective case (the next tp init or tp import resolving blocking from the hoisted project layer) at the stated cost of a deliberate over-refusal where every base already has a task file; both are asserted rather than implied.",
      "gate_passed_at": "2026-09-02T03:06:03.687725Z",
      "commit_sha": "9ee1a76",
      "commit_shas": [
        "9ee1a76",
        "2208df1"
      ],
      "commit_files": [
        "internal/cli/auditconvergeon_fence_test.go",
        "internal/cli/config_extract.go"
      ]
    },
    {
      "id": "fence-import-remedies",
      "title": "The import refusal and the two documents that must pass",
      "status": "done",
      "tags": [
        "fence"
      ],
      "depends_on": [
        "fence-change-rule"
      ],
      "estimate_minutes": 12,
      "acceptance": "Into a zero-task shell under TP_UNATTENDED=1 over a task-level all and a project-level blocking: a document carrying a workflow key without the field exits 2 at the fence, and two documents import cleanly — one omitting the top-level workflow key, one carrying audit_converge_on: all. The precondition is asserted, not assumed: over a tasks-bearing target tp import exits 3 at the exists-guard before the fence is reached. Section 7 rows 13b and 13d pass.",
      "source_sections": [
        "## 3. The fence"
      ],
      "started_at": "2026-09-02T02:14:36.812472Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T02:23:54.440119Z",
      "closed_reason": "Rows 13b and 13d are now asserted rather than assumed, over row 13b's own shell (task-level all, project-level blocking) with TP_UNATTENDED set per child.\n\n- The refusal: a document carrying a workflow key without the field exits 2 at the fence. TestAuditConvergeOnFence_ImportUncoversProjectBlocking already covered the review_max_rounds document; TestAuditConvergeOnFence_TheBlockTPWritesTakesTheRefusedPath adds the block tp itself writes, read out of the file tp init emits rather than restated as a literal.\n- The precondition, asserted not assumed: over a tasks-bearing target tp import exits 3 with 'task file already exists' and no mention of audit_converge_on, and the same document over the zero-task shell exits 2 at the fence. 3 and 2 are different answers and both are pinned in one test.\n- The two remedies pass: omitting the top-level workflow key (preservation carries the task-level all forward) and carrying audit_converge_on: all explicitly, each exit 0 with the written file and tp config --resolved both still reporting all.\n- tp init's emitted shape verified: model.TaskFile gives workflow no omitempty, the emitted file carries \"workflow\": {}, and that block takes 13b's refused path, so the refusing input is the common shape.\n- Mutant matrix, baseline built from the unmutated tree in an rsync copy, per test: M1 (row 13b's mutant, fence on the literal written) killed by ImportUncoversProjectBlocking, WritesThatChangeNothingPass, ImportExistsGuardPrecedesTheFence and TheBlockTPWritesTakesTheRefusedPath; M2 (row 13d's mutant, refuse when the imported block omits the field) killed by ImportRemediesPass and WritesThatChangeNothingPass with 13b surviving as the row predicts; M3, a variant section 7 does not name (the fence moved above the exists-guard), killed only by ImportExistsGuardPrecedesTheFence while all six pre-existing fence tests stay green. No variant survived.\n- Quality gate green: check-suite-state, golangci-lint (0 issues), check-deadcode, check-complexity all exit 0. 21 pre-existing failures under a suite-wide TP_UNATTENDED=1 were measured in the copy and left alone; none is a fence test.",
      "gate_passed_at": "2026-09-02T02:23:54.440119Z",
      "commit_sha": "23c28d9ec2d43b731851e9f57b2f726006c71561",
      "commit_shas": [
        "23c28d9ec2d43b731851e9f57b2f726006c71561",
        "907fc425fa0dc18da48c7be2ac347c0940c0ef98",
        "b71ae5c32b37f75ac8fafbb3c143187fc13eccbe"
      ],
      "commit_files": [
        "internal/cli/auditconvergeon_fence_test.go"
      ]
    },
    {
      "id": "fence-message",
      "title": "The refusal message names all three exits",
      "status": "done",
      "tags": [
        "fence"
      ],
      "depends_on": [
        "fence-import-remedies",
        "escalate-decision"
      ],
      "estimate_minutes": 8,
      "acceptance": "The refusal names the condition selecting each exit — omit the workflow key, carry the resolved value explicitly, or escalate — and does not contain the command-field refusal's wording `names a command the driver executes`. Section 7 row 14 passes.",
      "source_sections": [
        "## 3. The fence"
      ],
      "started_at": "2026-09-02T04:01:24.125991Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T04:10:54.281295Z",
      "closed_reason": "v0.37.0 §7 row 14: the refusal names all three exits and the condition selecting each, and the exits are the sink's own.\n- The message text at all four sinks, run on the freshly built binary in a scratch tree, exit 2 each: the error is '\u003csink\u003e changes the resolved audit_converge_on from all to blocking, which relaxes the audit gate and is a user-approved decision refused under TP_UNATTENDED' and the hint is 'if you did not mean to change it, \u003csink exit\u003e; if you did, escalate: tp escalate --decision audit-converge-on --evidence \u003cwhat you found\u003e'. The sink exit reads 'the document is the fix: omit its top-level \"workflow\" key to carry the current block forward, or write \"audit_converge_on\": \"all\" into it' at tp import, 'do not make this write: it names the value itself, so no form of it lands while audit_converge_on still resolves all' at tp set --workflow and its --project form, and 'do not run this hoist: --extract moves every field the task files share in one write, so no form of it lands while audit_converge_on still resolves all' at tp config --extract.\n- The remedies are sink-specific rather than a shared list, which is the acceptance's substance and not a simplification of it: only tp import has an input the unit can re-author, so §3's two documents are exits there and nowhere else; tp set names the literal and --extract moves every shared field in one write, so at neither is there a form of the command that lands while leaving the value alone. An over-general message that offered all four exits everywhere would satisfy every positive assertion and send a unit at tp set to edit a document that is not what refused.\n- Neither the message nor the hint contains the command-field refusal's wording 'names a command the driver executes', asserted at every sink on both strings; the shipped helper checked the message alone.\n- The test was written first and watched failing against unchanged code at all four sinks for the expected reasons: the hint carried neither condition clause and no exit at all, and the message named no resolved value. The negative half was already green then, so it was proved discriminating by mutation rather than by construction.\n- Three mutants built and run in an rsync copy with the baseline taken from the unmutated tree; no survivors, and internal/cli/unattended.go restored byte-identical by sha256 after each. M1 is §7 row 14's named mutant, delegating to refuseUnattendedCommandField: red at all four sinks in both the new test and the shipped assertFenceRefused helper. V1 is the variant row 14 names no mutant for and the one the reviewer's second reading is about, an over-general message offering all four exits at every sink: red at all four sinks in the new test alone, with the entire pre-existing fence suite green, so the negative half is the only thing standing between the release and that defect. V2 drops the two condition clauses while keeping the remedies: red at all four sinks, again only in the new test. V3 swaps the set and extract remedies, keeping both conditions and both texts sink-specific: red at set --workflow, its --project form and config --extract, and correctly green at import, whose text it did not touch.\n- Full project gate green on the final tree: ./scripts/check-suite-state.sh \u0026\u0026 golangci-lint run \u0026\u0026 ./scripts/check-deadcode.sh \u0026\u0026 ./scripts/check-complexity.sh exited 0, with 0 lint issues and every package ok under -race -count=1.\nOut of scope, left for the documentation tasks: skills/tp/REFERENCE.md lines 321 and 502 quote the command-field refusal and neither mentions this field's message, and no shipped document carries the new three-exit wording.",
      "gate_passed_at": "2026-09-02T04:10:54.281295Z",
      "commit_sha": "24f25ba",
      "commit_shas": [
        "24f25ba",
        "6c825fb"
      ],
      "commit_files": [
        "internal/cli/auditconvergeon_message_test.go",
        "internal/cli/config_extract.go",
        "internal/cli/unattended.go"
      ]
    },
    {
      "id": "escalate-decision",
      "title": "audit-converge-on joins the --decision enum",
      "status": "done",
      "tags": [
        "fence"
      ],
      "depends_on": [],
      "estimate_minutes": 6,
      "acceptance": "engine's escalation decision enum carries audit-converge-on, the field maps to it, and an unknown decision still exits usage.",
      "source_sections": [
        "## 3. The fence"
      ],
      "started_at": "2026-09-02T02:25:08.729244Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T02:34:35.035443Z",
      "closed_reason": "- engine's escalation decision enum carries audit-converge-on: EscalateAuditConvergeOn joins the const block and escalationDecisions between import-force and other, so EscalationDecisions(), IsEscalationDecision() and Escalation.Validate() all take it without a second list. Verified by running the freshly built binary on a scratch run directory: tp escalate --decision audit-converge-on writes the record and exits 2, and the refusal hint now enumerates six values because it is built from the slice.\n- The field maps to it: cli's escalationDecision(\"audit_converge_on\") returns audit-converge-on instead of falling through to other, so the section 3 fence's hint at all four sinks reads 'tp escalate --decision audit-converge-on'. Asserted in the shared assertFenceRefused helper, positively and negatively (the old text --decision other must be absent), which is section 7 row 14.\n- An unknown decision still exits usage: the rejected table gains audit_converge_on, audit-converge and AUDIT-CONVERGE-ON on top of the four near-misses it carried; each exits 2 and writes no record. TestEscalateAcceptsTheDocumentedDecisions also asserts the literal list equals engine.EscalationDecisions() rather than merely iterating it, so a value added to the enum, dropped from it, or reordered fails here.\n- Every test was written first and watched failing against unchanged code for the expected reason: 'other' != 'audit-converge-on' on the mapping, the hint containing --decision other at three sinks, and the set equality reporting len=5 against len=6.\n- Five mutants built and run in an rsync copy with the baseline taken from the unmutated tree, each restored byte-identical by sha256; no survivors. Removing the switch case reddened the mapping test and the fence hint at every sink but neither enum test; opening IsEscalationDecision to return true reddened the reject half and engine's record validator only; mapping the field to the plausible non-member audit-converge reddened three tests including the drift guard; landing the mapping without the enum entry (the half-landing the spec names no mutant for) reddened only the set equality and the drift guard while the fence hint stayed green, which is why both of those tests exist; and reordering the slice reddened the equality alone.\n- Full project gate green after the change: check-suite-state.sh 0, golangci-lint 0 issues, check-deadcode.sh 0, check-complexity.sh 0.\nOut of scope: the refusal message's three exits are fence-message's. Three shipped enumerations of the decision set are documentation and were left for document-the-field / reversal-sweep: skills/tp/REFERENCE.md line 347, skills/tp/SKILL.md line 247 and skills/tp/SKILL.md line 372 all list five values and omit audit-converge-on (SKILL.md 372 also calls them 'the four decisions'). CLAUDE.md lines 89 and 180 name the same four user-approved decisions and are equally one short.",
      "gate_passed_at": "2026-09-02T02:34:35.035443Z",
      "commit_sha": "a9da512",
      "commit_shas": [
        "a9da512",
        "e57ce56",
        "f4e15d4",
        "5d9c427",
        "2831fb6",
        "fbc80da",
        "7433771"
      ],
      "commit_files": [
        "internal/cli/auditconvergeon_fence_test.go",
        "internal/cli/escalate.go",
        "internal/cli/escalate_test.go",
        "internal/cli/escalation_decision_internal_test.go",
        "internal/cli/unattended.go",
        "internal/engine/escalation.go",
        "internal/engine/escalation_test.go",
        "internal/fakerunner/cmd/tp-fake-runner/main.go"
      ]
    },
    {
      "id": "merge-by-severity",
      "title": "by_severity on tp audit --merge",
      "status": "done",
      "tags": [
        "merge"
      ],
      "depends_on": [
        "clean-predicate"
      ],
      "estimate_minutes": 12,
      "acceptance": "The existing by_status/by_role loop in runAuditMerge gains a by_severity counter over the round's non-PASS rows with buckets error, warning, info and unrecognised and no empty-string bucket; the key is present on any round holding a non-PASS row under both policies, absent on an all-PASS round, and survives --compact. Over one fixture carrying all four unrecognised shapes plus an error, a warning and an info, error+unrecognised equals the blocking-row count and warning+info the rest. Section 7 rows 15, 16 and 17 pass.",
      "source_sections": [
        "## 4. `by_severity` on `tp audit --merge`"
      ],
      "started_at": "2026-09-02T03:07:26.298729Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T03:22:58.439186Z",
      "closed_reason": "by_severity ships on tp audit --merge (v0.37.0 section 4), verified by running rather than reading.\n\n- The counter is built in the existing by_status/by_role loop over the round's non-PASS rows, with buckets error, warning, info and unrecognised and no empty-string bucket; the four unrecognised shapes are one bucket.\n- The classifier was LIFTED, not re-derived: engine.auditSeverityBlocking's inline vocabulary became engine.AuditSeverityBucket and the predicate is now phrased over it, so one derivation serves both sections and a second copy cannot drift.\n- Present on any round holding a non-PASS row under both policies: asserted over an advisory-only fixture (clean under blocking, unclean under all) with .tp/config.json set each way, which is the only input separating a rows-keyed emission from a cleanliness-keyed one.\n- Absent on an all-PASS round, and present on both output forms, including the -o form Workflow D actually calls.\n- Survives --compact, with overlap_report's disappearance as the control that --compact took effect on that payload.\n- Over the seven-non-PASS-row fixture, error + unrecognised equals the round's blocking-row count and warning + info the rest, both sums derived by running each row through engine.AuditRowsClean rather than written beside the fixture.\n- Section 7 rows 15, 16 and 17 pass; six mutants were built and run in an rsync copy against an unmutated baseline and all six were killed, including two variants section 7 does not name (a breakdown over the pre-dedup input rows, and one emitted on the stdout branch only) that survived the entire suite until their killing tests were added.\n- Dogfooded on tp's own corpus with a freshly built binary: v0.35.0 audit round 4 reports {error 1, warning 2, info 1}, reproducing section 2.1's independently measured mix, and v0.36.0 round 9 reports {warning 2, info 2} - the advisory-only round the section exists for.",
      "gate_passed_at": "2026-09-02T03:22:58.439186Z",
      "commit_sha": "e707907",
      "commit_shas": [
        "e707907",
        "74d3d3b",
        "0a02b3d",
        "d0f2a71",
        "b5d00e3",
        "5e0ee07"
      ],
      "commit_files": [
        "internal/cli/audit_merge.go",
        "internal/cli/audit_merge_severity_test.go",
        "internal/engine/auditclean.go",
        "internal/engine/auditclean_test.go"
      ]
    },
    {
      "id": "replay-fixture",
      "title": "Commit the replay projection of the recorded audit rounds",
      "status": "done",
      "tags": [
        "fixture"
      ],
      "depends_on": [],
      "estimate_minutes": 12,
      "acceptance": "testdata carries a projection of v0.35.0's nine and v0.36.0's thirteen recorded audit rounds — status, severity, role, item_id and the round ordinal — 3,046 rows taken at a named commit, with the generator committed beside it. The replay test does not read spec/.tp-review/, which is live state for cycles in flight.",
      "source_sections": [
        "## 7. Tests"
      ],
      "started_at": "2026-09-02T03:37:58.8309Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T03:47:27.035905Z",
      "closed_reason": "- testdata/auditreplay carries the projection of v0.35.0's 9 and v0.36.0's 13 recorded audit rounds: status, severity, role, item_id and the round ordinal, 3,046 rows over 22 audit-round-\u003cn\u003e.ndjson files, counted by the generator rather than taken from the spec (v0.35.0 1,800 rows / 9 rounds, v0.36.0 1,246 / 13). The spec's 3,046 and 1.4 MiB both hold: sources are 1,453,664 bytes; the projection is 311,400 bytes (304.1 KiB) against the spec's 'about 295 KiB', 5.6% larger.\n- Provenance is manifest.json beside the data: source_commit 6f7e3bfa73d7e8d1b8d9aa9bd38428bbc75b3686, sources_clean_at_commit true (checked with git status over both source directories, not assumed), the sha256 and row count of every one of the 22 sources, the sha256 of each output, and a syntactic census (status literals, severity key shapes) that embeds no grading rule.\n- The generator is committed beside the data at internal/engine/testdata/auditreplay/gen.go, run as 'go run internal/engine/testdata/auditreplay/gen.go'. It reads only files named exactly audit-round-\u003cn\u003e.ndjson, so 0.36.0's per-role audit-r9..r13 files are not double-counted, copies each projected key as raw JSON so absent stays absent and null stays null, and is byte-deterministic: two runs produced identical sha256 for both outputs.\n- The replay test does not read spec/.tp-review/: the loader added here, loadAuditReplayRounds in internal/engine/auditreplay_test.go, reads testdata/auditreplay only and returns rows grouped by round, index i holding round i+1, in the map[string]any shape AuditRowsClean consumes. The loader ships here rather than with replay-test so the fixture arrives with its own consumer.\n- TestAuditReplayFixtureMatchesItsManifest was watched failing before it was trusted: dropping a severity key from one row reddened both the sha256 assertion and the null-severity census assertion (1758 vs 1757), and moving one row's ordinal from 1 to 3 reddened the contiguity check ('round 3 follows round 0'). Regeneration restored both sha256 values exactly.\n- Independently verified that the projection reproduces spec 0.37.0 section 2.1 and section 7 row 11 before handover: at audit_clean_rounds 2, v0.35.0 converges at round 3 under blocking and round 9 under all; v0.36.0 at round 3 under blocking and never under all; at 1, 2/8 and 2/never. Rounds 7, 12 and 13 of v0.36.0 and round 4 of v0.35.0 are unclean under blocking, matching the error rows section 2.1 names.\n- Quality gate green: ./scripts/check-suite-state.sh \u0026\u0026 golangci-lint run \u0026\u0026 ./scripts/check-deadcode.sh \u0026\u0026 ./scripts/check-complexity.sh - all six packages ok, 0 issues, no deadcode or complexity regression.",
      "gate_passed_at": "2026-09-02T03:47:27.035905Z",
      "commit_sha": "789358e",
      "commit_shas": [
        "789358e",
        "a0460e5",
        "2c7ca0d"
      ],
      "commit_files": [
        "internal/engine/auditreplay_test.go",
        "internal/engine/testdata/auditreplay/0.35.0.ndjson",
        "internal/engine/testdata/auditreplay/0.36.0.ndjson",
        "internal/engine/testdata/auditreplay/gen.go",
        "internal/engine/testdata/auditreplay/manifest.json"
      ]
    },
    {
      "id": "replay-test",
      "title": "The replay reproduces the table that decides the default",
      "status": "done",
      "tags": [
        "fixture"
      ],
      "depends_on": [
        "replay-fixture",
        "clean-predicate"
      ],
      "estimate_minutes": 10,
      "acceptance": "Replaying the projection at audit_clean_rounds 2 gives blocking 3 / all 9 for v0.35.0 and blocking 3 / all never for v0.36.0, and at 1 gives 2 / 8 and 2 / never, with the field resolving before round 1. The named mutant — grading error as advisory — moves every blocking cell to 2 and 1, and is observed failing before the code is written. Section 7 row 11 passes.",
      "source_sections": [
        "### 2.1 Why the default is `all`",
        "## 7. Tests"
      ],
      "started_at": "2026-09-02T03:48:46.941313Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T03:59:09.443811Z",
      "closed_reason": "Replayed tp's own recorded audit cycles through engine.AuditRowsClean in internal/engine/auditreplay_table_test.go; the fixture reproduces all eight of §2.1's cells and §7 row 11 passes.\n\n- At audit_clean_rounds 2 the replay gives v0.35.0 blocking 3 / all 9 and v0.36.0 blocking 3 / all never, and at 1 it gives 2 / 8 and 2 / never - the eight cells §2.1 states, asserted by TestAuditReplayReproducesTheTableThatDecidesTheDefault over the committed projection, never over spec/.tp-review/.\n- The field resolving before round 1 is asserted rather than assumed: auditReplayConvergence takes the first round graded under the policy as a parameter, the cells are its resolvesAt 1 column, and TestAuditReplayCellsNeedTheFieldResolvedBeforeRoundOne requires that delaying resolution never converges earlier and somewhere converges later. §2.1's own arithmetic form of that claim - no earlier than k + audit_clean_rounds - is false on this corpus under either reading of k, because v0.35.0 rounds 8 and 9 are clean under all as well, so the true property is asserted instead and the measurement recorded in the test's comment.\n- The named mutant, grading error as advisory, was built in an rsync copy and observed failing: it moves every blocking cell and only those, 3 to 2 and 2 to 1 in both cycles, leaving the four all cells untouched.\n- Row 11's rejected mutant was re-verified rather than trusted: counting the streak as a running total instead of a trailing run leaves the suite green, so it still cannot fail.\n- Two unnamed variants were built. An inverted comparator making all grade like blocking is killed and moves exactly the four all cells the named mutant never reaches; grading PASS rows too is killed and makes every cycle never converge. A third, row 8's unrecognised-as-advisory mutant, is unobservable here - green on the replay, killed by its own test - which confirms the fixture census and that row 11 does not overlap row 8.\n- Quality gate green, exit 0: ./scripts/check-suite-state.sh \u0026\u0026 golangci-lint run \u0026\u0026 ./scripts/check-deadcode.sh \u0026\u0026 ./scripts/check-complexity.sh.",
      "gate_passed_at": "2026-09-02T03:59:09.443811Z",
      "commit_sha": "dac48a6e065884d78ee3c2532ed4e62335db53e2",
      "commit_shas": [
        "dac48a6e065884d78ee3c2532ed4e62335db53e2",
        "fbc49276132f75eaf5eefe89c3791c0ffabdd446",
        "43be9b59901d25859074bc2a3e582af1b340717b"
      ],
      "commit_files": [
        "internal/engine/auditreplay_table_test.go"
      ]
    },
    {
      "id": "reverse-gate-guard",
      "title": "Reverse the gate guard's two assertions",
      "status": "done",
      "tags": [
        "reversal"
      ],
      "depends_on": [
        "converge-write-sinks",
        "clean-predicate"
      ],
      "estimate_minutes": 8,
      "acceptance": "In TestAuditGate_NoEscapeHatchFromTheGate the two assertions naming audit_converge_on assert it is a known workflow field; every other assertion in that test is unchanged and passes. The test's fixture rows carry no severity key, so the fail-closed rule is what keeps them green once the guard's project-level blocking write takes effect — asserted, not assumed. Section 7 row 18 passes.",
      "source_sections": [
        "## 6. Five shipped assertions are reversed"
      ],
      "started_at": "2026-09-02T01:49:10.796369Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-02T01:50:32.336829Z",
      "closed_reason": "The two assertions naming audit_converge_on in TestAuditGate_NoEscapeHatchFromTheGate now assert it IS a known workflow field, and the fail-closed interaction that keeps the round unclean under blocking is asserted rather than assumed. Section 7 row 18 passes.\n- The red state was observed first, not taken from the brief: on the tree as inherited, go test -run TestAuditGate_NoEscapeHatchFromTheGate reported exactly two failures, at audit_signal_test.go:473 and :474, and nothing else. Both are assert rather than require, so the test ran on, and the require at :481 and every assertion after it were green - which is the independent confirmation that only these two needed reversing.\n- Reversed, not deleted: `assert.NotEqual(t, 0, code)` became `require.Equal(t, 0, code)` and `assert.Contains(..., \"unknown workflow field: audit_converge_on\")` became `assert.NotContains(...)`. require, because everything below the write now runs under the policy that write installs. The header comment moved with them: it stated that there is no audit_converge_on knob to flip, which is the opposite of what the code below now asserts.\n- Row 18's named mutant was built and run, and it SURVIVES: deleting the two assertions instead of reversing them (keeping the runTP call, discarding stdout) leaves TestAuditGate_NoEscapeHatchFromTheGate green and the whole internal/cli package green, 41s, exit 0. So no bare count or suite run discriminates a deletion from a reversal - only row 18's positively stated requirement does. Reporting it rather than inventing a guard the release did not ask for.\n- The reversal itself is load-bearing, shown by the inverse mutant: restoring the two pre-v0.37.0 assertions reddens exactly those two lines and nothing else.\n- The fail-closed interaction is now asserted in three places: that the project write actually resolves (config --resolved reports blocking, not merely exit 0), that no fixture row carries a severity key, and that engine.AuditRowsClean still grades those rows unclean under blocking; plus the round tp itself stamped, read from state.json rather than from a payload.\n- Those assertions were proven able to fail. Inverting the fail-closed rule (auditSeverityBlocking returning false for an ungradeable severity) reddens exactly the AuditRowsClean assertion and the stored-clean assertion. A mutant the spec does not name - the project sink accepting audit_converge_on and persisting nothing - reddens exactly the resolved-value assertion, and is not caught by the two reversed assertions, which pass on a write that reports success and stores nothing.\n- One claim in this task's own acceptance was measured and does NOT hold as worded, so it is corrected rather than repeated. \"The fail-closed rule is what keeps them green\" is false of the pre-existing assertions: under the fail-open mutant, go-safety's streak entry is still {consecutive_clean 0, open 1} because a per-role streak counts non-PASS rows and not severities, and --status --check still exits 1 because one clean round leaves consecutive_clean at 1 against a required 2. Both pre-existing assertions survive the inverted rule by that one-round margin. Measured on binaries built from the mutated and unmutated trees: baseline stores clean flags [true, false, false] with consecutive_clean 0, the fail-open mutant [true, false, true] with consecutive_clean 1, and both exit 1 on --check. What the rule actually governs at this guard is the stored clean flag and the streak margin, and nothing here asserted either until this task - which is precisely why row 18 asks for it to be asserted.\n- A measurement error was caught and corrected mid-task rather than reported as a result: the first probe built its baseline and its mutant binary from the same tree while the mutation was still applied, so the two ran identically and appeared to show the shipped code grading an ungradeable row clean. Rebuilding the baseline from the unmutated tree reversed that reading. The mutation copy was an rsync copy throughout; git status over the working tree confirms only the intended test file changed, and every mutated file was restored byte-identical to the working tree afterwards.\n- Every other assertion in the test is unchanged: the --resolve invocation, the role_streaks membership assertion, the --status --check exit code and the streak-entry shape loop are untouched, and all pass.\n- Quality gate green over the whole tree: ./scripts/check-suite-state.sh \u0026\u0026 golangci-lint run \u0026\u0026 ./scripts/check-deadcode.sh \u0026\u0026 ./scripts/check-complexity.sh, with golangci-lint reporting 0 issues and steps 3 and 4 silent, exit 0.\nOut of scope and left to reversal-sweep: only this test's header comment was corrected, because it sat directly above the assertions being reversed and contradicted them. The rest of section 6's sweep across skills/, README.md and the other comments and constants in internal/engine and internal/cli is untouched here.",
      "gate_passed_at": "2026-09-02T01:50:32.336829Z",
      "commit_sha": "bf174fb",
      "commit_shas": [
        "bf174fb",
        "39ef522",
        "cfe6419",
        "616cb67"
      ],
      "commit_files": [
        "internal/cli/audit_signal_test.go"
      ]
    },
    {
      "id": "reverse-docs-and-constant",
      "title": "SKILL.md's sentence and engine.DivergenceHint, coordinated",
      "status": "open",
      "tags": [
        "reversal"
      ],
      "depends_on": [
        "record-time-stamping"
      ],
      "estimate_minutes": 10,
      "acceptance": "skills/tp/SKILL.md's own prose and engine.DivergenceHint each say convergence counts every non-PASS row the resolved policy does not accept — true under both values — and internal/cli/docs_contract_test.go's two independent checks both pass. Editing the constant alone must leave SKILL.md failing. Section 7 row 19 passes.",
      "source_sections": [
        "## 6. Five shipped assertions are reversed"
      ],
      "started_at": null,
      "closed_at": null,
      "closed_reason": null,
      "gate_passed_at": null,
      "commit_sha": null
    },
    {
      "id": "reverse-reference-paragraph",
      "title": "REFERENCE.md's Honest Convergence Signals paragraph, with a guard",
      "status": "open",
      "tags": [
        "reversal"
      ],
      "depends_on": [],
      "estimate_minutes": 8,
      "acceptance": "internal/cli/docs_contract_test.go gains an assertion over that paragraph — nothing reads it today — and the paragraph no longer lists the stored per-round clean flag among what behaves exactly as in v0.32.0. Re-adding the flag must redden the new assertion. Section 7 row 20 passes.",
      "source_sections": [
        "## 6. Five shipped assertions are reversed"
      ],
      "started_at": null,
      "closed_at": null,
      "closed_reason": null,
      "gate_passed_at": null,
      "commit_sha": null
    },
    {
      "id": "reverse-divergence-comment",
      "title": "ComputeAuditDivergence's condition-4 comment, and the suppression pinned",
      "status": "open",
      "tags": [
        "reversal"
      ],
      "depends_on": [
        "record-time-stamping"
      ],
      "estimate_minutes": 10,
      "acceptance": "The doc comment no longer calls condition 4 redundant; no line of the function's logic changes. Two fixtures satisfying conditions 1 and 5 assert that a round clean and converged under blocking emits no object — once with roled open rows, once with roleless ones, the second pinning the unattributed_open cost the release accepts. Section 7 rows 21 and 22 pass.",
      "source_sections": [
        "## 6. Five shipped assertions are reversed"
      ],
      "started_at": null,
      "closed_at": null,
      "closed_reason": null,
      "gate_passed_at": null,
      "commit_sha": null
    },
    {
      "id": "reversal-sweep",
      "title": "Run the reversal sweep over every comment, constant and assertion",
      "status": "open",
      "tags": [
        "reversal"
      ],
      "depends_on": [
        "reverse-docs-and-constant",
        "reverse-reference-paragraph",
        "reverse-divergence-comment"
      ],
      "estimate_minutes": 15,
      "acceptance": "Every shipped assertion matching the four criteria in section 6 — what convergence counts; the stored clean flag, next_action or the --check exit code being unchanged; tp having no audit-side equivalent of review_converge_on; and any enumeration of the settable workflow fields — across skills/, README.md and every comment, constant and test assertion in internal/engine/ and internal/cli/, states a rule true under both values. A sentence already covered by another item is re-read for a second false clause, and a field table is checked for falsifying by omission.",
      "source_sections": [
        "## 6. Five shipped assertions are reversed"
      ],
      "started_at": null,
      "closed_at": null,
      "closed_reason": null,
      "gate_passed_at": null,
      "commit_sha": null
    },
    {
      "id": "document-the-field",
      "title": "Document the field, its fence and its decision input",
      "status": "open",
      "tags": [
        "docs"
      ],
      "depends_on": [
        "converge-field",
        "escalate-decision",
        "fence-change-rule"
      ],
      "estimate_minutes": 12,
      "acceptance": "skills/tp/SKILL.md and REFERENCE.md document audit_converge_on, its default, its fence, its escalation name and the four write paths, and both field tables list it; SKILL.md carries beside it the one-line command that counts severity across a cycle's audit-round files, which is the input the default decision needs. README.md reflects the new flag surface.",
      "source_sections": [
        "## 1. Overview",
        "## 2. `audit_converge_on`",
        "### 2.1 Why the default is `all`"
      ],
      "started_at": null,
      "closed_at": null,
      "closed_reason": null,
      "gate_passed_at": null,
      "commit_sha": null
    }
  ]
}
