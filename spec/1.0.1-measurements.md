# 1.0.1 — measurements

Supplemental material for `spec/1.0.1.md`. **Not a spec; `tp ground` does not grade it.**

## 1. The floor budget was designed and then refuted

The release was going to carry a `spec-floor-budget` warning: a threshold on `floor_size`, above which
`tp lint` would tell the author the spec is too expensive. Deriving the threshold refuted the premise.

Round-1 floor for every converged shipped spec, against that cycle's recorded rounds. Each spec's
round-1 review snapshot is extracted to a scratch directory and ground's own splitter is run over it:

```
for v in <shipped versions>; do
  git show "v$v:spec/.tp-review/$v/snapshot-round-1.md" > "$d/s.md"
  (cd "$d" && tp ground s.md --units | grep -c '^u[0-9]')
done
```

with rounds counted as `review-round-*.ndjson` plus `audit-round-*.ndjson` under each spec's state
directory. Correlating the two:

- **r(round-1 floor, total rounds) ≈ +0.06.** Effectively zero.
- **r(round-1 lines, total rounds) ≈ +0.22** — the figure `CLAUDE.md` already records as too weak to
  decide on.

So floor size predicts cycle length worse than the measure already rejected for that purpose. The
extremes run the wrong way: the largest floor in the corpus belongs to a **mid-length** cycle, and the
two specs with almost identical floors — one `tool`, one `loop` — differ in cost by roughly sevenfold.
That pair is the whole argument: **the driver is the class, not the size.**

**A target of "about sixty units" had been agreed in conversation before any of this was run.** The
measured median over converged specs is half again that, and the distribution is wide. The number was
invented, it was never written into a spec, and the only reason it cost nothing is that the rule
"prototype before shipping" was applied to it. That is this section's second finding.

### 1.1 The pooled coefficient was the wrong statistic, and this file quoted it

Round 1's refutation unit stratified the same seventeen cycles by class and the two within-class
coefficients came out with **opposite signs**, the pooled figure being their average. So "effectively
zero" is an artifact of pooling across the stratum this file names as the driver — right conclusion,
wrong statistic. The decision survives on the surviving half: within the class most of the backlog
belongs to, line count predicts the outcome far better than floor does.

Two attacks on the method failed and are recorded because a failed attack is a result. Counting
*emitted* rounds rather than recorded ones changes nothing: across the corpus exactly one round was
emitted and never recorded, and the totals run the other way because early cycles predate snapshots.

### 1.2 What round 1 measured, and the five defects that were mine

Round 1 graded 46 units: **12 FAIL, 18 PARTIAL, 13 NOT-A-CLAIM, 10 PASS, 1 UNVERIFIABLE**, at about
1.2 tool calls per unit against the previous spec's 1.5. **That comparison is confounded** — the brief
and the floor both changed — so it is a first observation, not the brief's effect. The single-variable
measurement is the round the previous release still owes, where the spec is fixed and only the brief
moves.

Five of the defects were repeats of error classes this repository had already written down, which is
why they are listed rather than summarised:

| defect | the class it repeats |
|---|---|
| a trigger word asserted into a list it is not in | asserting a membership without counting it |
| expected line numbers written down, one off by one | pinning a derivation to a line rather than to content |
| the header claiming figures were absent and commands present, when the reverse held | the rule this release ships, violated in the file that states it |
| a field named `floor_size` while `tp ground` already emits that name for a different population | naming without reading the tool's own output |
| an entry added to `spec/undecided.md` mid-round, leaving the spec's smell-rule Non-Goal contradicting it | editing what a running round cites |

The last two were found by the grader rather than by the author.

**The sharpest finding sat in a cut unit, again.** §1's decision sentence was cut, and cut *mid-emphasis*:
the splitter guards backticked spans and nothing guards `**`. The sentence the release exists to state
was the one no round graded. The repair rewrote the three affected sentences so that emphasis does not
straddle a split; the guard itself belongs to `spec/backlog/04a-ground-command-friction.md`.

**Repair effect on the floor**, both measured with the same command: before, `floor_size` 46 with 20
cut; after, 44 with 24. The document grew and the graded population shrank — which is the cut-share
warning in §2 happening to this file.

### 1.3 Round 1's record was destroyed and partly recovered

**The order was wrong and the tool allowed it.** The sequence must be *grade → record → repair →
emit*; this round went *grade → repair → emit*, the grader's findings file was removed in a pre-commit
tidy, and re-emitting round 1 against the repaired text **silently overwrote the unrecorded round's
floor**. `--status` then reported a round with zero dispositions.

Recovery was possible only by accident: an `rsync` copy taken for unrelated work, **while the grader
was still writing**, held the pre-repair spec, the pre-repair floor and 50 of the grader's 54 rows.
Restoring those three into a scratch directory and recording there was accepted — `--record` matches
rows against the floor file rather than against the spec's current hash, which is what made the
rescue possible and is itself worth knowing.

**The loss is bounded and visible in the record.** Coverage after recording: 46 emitted, 44
dispositioned, `u65` and `u66` absent. Both are rows the repair dropped anyway — the two whose named
mutants could not make them red. The remaining lost rows were off-floor. Nothing was reconstructed
from the grader's prose report: a row rebuilt by the operator is not a row the grader wrote, and the
two units are simply asked again.

**What the rescue bought, measured:** round 2 asks **38 of 44** floor units, the other 6 carrying a
disposition forward. Without the record all 44 would have been re-asked — a full round of grading
spent re-deriving judgements that already existed, which is the complaint this release exists to
address.

**The defect underneath is tp's, not only the operator's.** Review and audit expose an
`in_flight_round` signal for exactly this state; ground has no counterpart, so an emission over an
unrecorded round's floor is silent. `spec/backlog/04a-ground-command-friction.md` takes it: emitting
when a floor exists for an unrecorded round whose new floor differs should refuse and name the round,
with a flag to override.

## 2. What the floor does measure

Grading one round of the release now numbered `spec/1.0.2.md` — 149 floor units — took three parallel
units and roughly seven hundred thousand tokens between them, at a per-unit cost that was consistent
across the three. That is a *per-round* cost, and total cost is rounds × cost-per-round.

**One spec is not a conversion factor.** The three points come from one document; a second cycle's
measurement is what would earn a tokens-per-unit rule, and it would belong in `skills/tp/SKILL.md`, not
in tp. the spec's *What `tp lint` adds* reports units and says nothing about tokens for exactly this reason.

## 3. Where the sidecar rule came from

Grading `spec/1.0.2.md` counted its rows by `kind`. The rate ordering was: design and universal claims
highest, re-derived corpus figures next, structural claims lowest. The corpus findings were correct and
close to worthless — *a date interval off by two days, a census that did not reach its stated total* —
defects in sentences that exist only to justify other sentences.

The cut that followed moved forensics out of the body. Its effect, measured on that spec: the forensic
sections lost about half their units while the decision sections **grew**, because the round had found
real defects and stating them took room. Net floor reduction was modest. **The rule works and is not a
lever you can pull twice** — that is worth knowing before anyone applies it to the backlog expecting
the first number.

## 4. Prior art this agrees with

- **ADR practice** independently arrives at the same split: keep the record short, focus on decision
  and rationale rather than implementation, and *link* to supporting research — with the constraint
  that the decision must stand without the linked material. the spec's *What ships in `skills/tp/SKILL.md`* rule is that sentence.
- **EARS** (Easy Approach to Requirements Syntax) constrains a requirement to
  `WHILE <precondition>, WHEN <trigger>, the <system> SHALL <response>`. The argument for adopting it
  in the test table is local and measured: grading found three test rows whose named mutant could not
  fail them, and in each case the reason was that the trigger or precondition lived in the surrounding
  prose rather than in the row.
- **Requirements smells** are a named family in the requirements-engineering literature and in
  ISO/IEC/IEEE 29148 — subjective, ambiguous, non-verifiable, vague, superlative, comparative,
  loophole, vague pronoun. tp's `vague-language` is one member; the spec's Non-Goals record the rest as refuted rather than as
  candidates rather than as a set, because this repository's prototyping record says most candidates
  die.

Sources: [adr.github.io](https://adr.github.io/) ·
[Microsoft Well-Architected — ADR](https://learn.microsoft.com/en-us/azure/well-architected/architect-role/architecture-decision-record) ·
[Fowler, ArchitectureDecisionRecord](https://www.martinfowler.com/bliki/ArchitectureDecisionRecord.html) ·
[EARS](https://alistairmavin.com/ears/) ·
[spec-kit issue #1356](https://github.com/github/spec-kit/issues/1356) ·
[Characterizing Requirements Smells](https://arxiv.org/pdf/2404.11106)

## 5. Field reports behind the rules that ship in the skill

Two cycles in other repositories, reported by their operators:

- A review cycle recorded hundreds of findings and **zero dispositions** over eight rounds, its spec
  nearly doubling in length, with the heaviest findings of the last round sitting inside the previous
  round's own repairs. Three causes were identified with committed evidence behind two of them; the
  emitted wording is only one. The operator has since confirmed the structural one: `--record` rejects
  a findings file carrying `fixed` rows, so a cycle disposing everything `fixed` records zero
  dispositions by construction.
- An audit cycle wrote its dispositions into the merged input file rather than the recorded round, so
  none of them reached `--status`. `skills/tp/SKILL.md` documented the wrong file until shortly before
  this release.

Both are repaired by `spec/1.0.2.md`; they are here because the rules that ship in the skill are what stop the spec growing
in the first place, and the reports are their provenance.

**Two further reports arrived while this file was written and are routed rather than fixed here:** a
quality gate that cannot run is accepted at `tp import` and only fails at the first `tp done`; and
closure verification splits an acceptance criterion on `;`, including where the semicolon introduces a
list inside a single criterion, so a five-item acceptance became nine without the author being told at
write time. Both belong to the same family as this release — catch it where it is cheapest — and both
need their own measurement first.

## 6. Round 2, and the figures this file got wrong

**Round 2: 47 rows over 38 asked units — 23 PASS, 11 FAIL, 8 PARTIAL, 4 NOT-A-CLAIM, 1 UNVERIFIABLE**,
plus nine rows against cut units. Two units worked it: a grader on coverage and a refuter scoped to
seven design claims, neither seeing the other. **Six of the seven claims were refuted**, all by
construction. Counts come from the record:

```
python3 -c "import json,collections;rows=[json.loads(l) for l in open('spec/.tp-review/1.0.1/ground-round-2.ndjson') if l.strip()];print(len(rows),collections.Counter(r['verdict'] for r in rows))"
```

**Round 1's breakdown in §1.2 was wrong and is corrected here.** That paragraph quoted the grading
unit's report, which counted 54 rows written before four were lost; the record holds 50, with `FAIL`
10 and `PARTIAL` 16. The quoted set exists nowhere. **The same wrong figures were copied into round
2's brief**, and the round-2 grader reported them back — the error travelled upward, into the artifact
the next decision reads. That is why `skills/tp/SKILL.md` now says a brief names the record and
repeats no figure from it.

### 6.1 What round 2 found that round 1's repair created

- **A second name collision, worse than the first.** `review_roles` and `audit_roles` already exist in
  tp as the frontmatter role-override maps; §2 had reused both as integer counts. The first collision
  (`floor_size`) was one of the five repeated error classes §1.2 lists — this is the same class, one
  round later, and §2's own cut unit warned against its inverse.
- **The role rationale was false in both directions.** *"They must be separate because deactivating a
  reviewer moves one and not the other"* fails sufficiency — same-size corpora give 4 and 3, because
  `spec-coverage` is skipped for having no checklist items — and fails necessity: deactivating a fifth
  reviewer gives corpora of 5 and 4 with counts of 4 and 4. Both fixtures were built. The field is now
  a **list**, because membership discriminates where a count does not.
- **§1's derivation command emitted nothing.** `git tag --list 'v*'` yields `v0.1.0`, and the body then
  asked for `v$v`; `|| continue` swallowed every failure. §3's command emitted nothing for the opposite
  reason — the repair had removed the findings it displayed. Both are now written to run, and the rule
  that a fenced command must print something is in `SKILL.md`.
- **§3's justification was the wrong one.** The section argued from symmetry with sibling rules; the
  refuter showed `duplicate-line` firing on a fully backticked line, so an inline-span exclusion
  matches no sibling. The repair keeps the exclusion and replaces the argument: **each rule excludes
  what its own predicate cannot judge.** `vague-language` measures prose; a code span is a literal
  token. The siblings measure structure, for which a backticked line is still a duplicate — so their
  *not* excluding spans is correct, and `duplicate-line`'s behaviour is the principle's evidence rather
  than a counter-example.

### 6.2 The inline measurement, which did not hold as assumed

Measured at `9297dd2b`, the commit before the fix, over 66 corpus files:

```
28 vague-language findings — 3 inside fenced blocks, 1 inside an inline code span, 24 in plain prose
```

Not 27, and not zero inline. All four are the same class — a document quoting the rule's own output or
its trigger list — and the fence exclusion closes three of them, the span exclusion the fourth. That
fourth is `spec/undecided.md`'s table naming the loophole family, which is the natural way to document
a lint rule's triggers; this release's own sidecar, `undecided.md` and README all use that form.

### 6.3 Repair locality is undefined in the ground phase

The round-2 grader was asked to tag each finding as sitting in repair-written or older text. The answer
came back **47 of 47**, and the grader correctly refused to read it as a signal: ground's carry rule
keeps exactly the byte-identical units, so *carried ⟺ unchanged text* and an asked unit can never be
older text. The metric is degenerate here by construction and meaningful only in review, where there is
no carry.

The substantive split it gave instead: **43 of 47 are new propositions, 4 restate round-1 propositions
in reworded sentences.** That second ratio — *restated* rather than *new* — may be the signal
`spec/backlog/12-repair-locality.md` actually wants, and it is recorded here for that release rather
than claimed by this one.

## 7. The fenced commands, run before round 3

The rule *every fenced command in a spec runs and prints something* was applied to this file's own two
commands before a grading unit saw them, because the class had already cost two rounds.

| block | output | verdict |
|---|---|---|
| §1's floor-vs-rounds derivation | 17 lines | runs |
| §3's before/after lint count | `9297dd2b: 2` / `65406160: 0` | runs, **after repair** |

**§3's command was worse than silent — it corrupted the checkout.** The version that went into round 2
used `git stash` and `git checkout -`. On a clean tree the stash is empty, so `git stash pop` errors on
every iteration; and `checkout -` returns to the *previous* ref rather than the starting one, so after
the second iteration the repository sat detached at the older commit. Measured in a throwaway clone,
which is why nothing was lost. The shipped version runs each commit in a temporary `git worktree` and
never touches the checkout.

Neither defect is visible by reading. The first version printed the right two numbers.

## 8. Round-over-round

| | round 1 | round 2 | round 3 |
|---|---|---|---|
| floor index | 66 | 68 | 82 |
| cut | 20 | 24 | 28 |
| `floor_size` | 46 | 44 | 54 |
| carried | 0 | 6 | 21 |
| **asked** | **46** | **38** | **33** |
| rows recorded | 50 | 53 | 56 |
| non-`PASS` share | 54% | 45% | 36% |

Counting rule for the share: rows whose verdict is neither `PASS` nor `NOT-A-CLAIM`, over all rows in
that round's recorded file — carried rows included, because they are in the file. Every figure in
this table is `len(floor-ground-round-N.txt)`'s `u`-lines, `tp ground --status`'s `cut`, or a
`collections.Counter` over `ground-round-N.ndjson`; none is transcribed from a round's own prose.


## 9. Round 3, and the cut it forced

Round 3 ran two arms that did not see each other: a grader over the 33 asked units, and a refuter
scoped to §2's four design claims. **Both independently refuted the same two things** — §2's audit
derivation and §6's `review_panel` row — which is the strongest evidence in this cycle that the
defect was in the section's design rather than in one reader.

The stopping rule for round 3 was pre-registered before either arm ran: *if the rewritten §2 still
carries `FAIL`, the problem is the field design, not the text — do not repair, escalate.* It fired.
No round 4 was emitted.

### 9.1 What the arms returned

| | grader | refuter |
|---|---|---|
| scope | 33 asked units, all of §2 and §6 marked | §2's four design claims |
| result | 20 `PASS`, 8 `PARTIAL`, 7 `FAIL` | 3 of 4 claims refuted by construction |
| §2 | 3 `FAIL`, 3 `PARTIAL` over 11 asked units | name collision, panel rationale, audit derivation |
| §6 | 1 `FAIL`, 3 `PARTIAL` over 10 rows | row 2's mutant survives a conforming fixture |

Derive: `python3 -c 'import json,collections;print(collections.Counter(json.loads(l)["verdict"] for l in open("spec/.tp-review/1.0.1/ground-round-3.ndjson") if l.strip()))'`

**The `core` field is not in the record, and that is tp working.** The grader was asked to mark each
row `core: true` when it fell in §2 or §6, so the stopping rule could be evaluated without reading
prose. `tp ground --record` rejected the file: *"field `core`: is not a field §7.2's table names"*.
Correct behaviour — the round schema is closed — but it means a brief-level instrument has no
channel into the record, and its counts survive only here: 23 `core` rows, 13 `PASS` / 5 `PARTIAL` /
5 `FAIL`.

### 9.2 The four claims, and what each cost

1. **No name collides.** *Refuted.* `floor_size` is emitted from two places with two referents —
   `internal/cli/ground.go` (the live text's floor) and `internal/engine/groundadvisory.go` (the
   latest *emitted* round's frozen index, carried in `tp review`'s `ungrounded` envelope). They
   disagree on the same file at the same instant whenever the spec has changed since emission.
   `cut` inherits the split. This is the section's **third** name collision in three rounds, and the
   first two were also uniqueness claims over an unenumerated set.
2. **`review_panel` is the round-1 panel after frontmatter.** *Membership survived; the rationale
   did not.* A spec with no frontmatter at all still loses a role to `domain-mismatch`;
   `internal/engine/skipped.go` defines five reasons and three can fire at round 1. §2 named only
   deactivation.
3. **No audit counterpart, because no tasks → no checklist → no panel.** *Refuted by one run.* On a
   repository with no task file, `tp audit` resolved a four-role panel and a non-empty checklist:
   `routeChecklist` takes table rows and numbered list items from the spec's own markdown, and every
   non-`spec-coverage` role is fed from `git diff`. The Non-Goal's conclusion survives; its
   derivation did not, and it now names the reason that holds.
4. **The anchor rule as stated.** *Sum survived, description refuted.* Neither arm could build a
   document where `floor_by_section` fails to sum to `floor_size`. Both built documents the prose
   does not describe. The fix is to delete the prose, not to extend it.

**One grader claim did not reproduce, and is recorded rather than acted on.** The round-3 grader
wrote that `spec/undecided.md` carries the same sibling over-count §3 did. Searched at `HEAD`: that
file states no count of `vague.go`'s rules at all. The row stands as the grader recorded it — the
record is the reader's — and the repair was not made. Naming a file that does not contain the
defect is the same class as naming one that does not contain the question, which `spec/0.36.0.md`
warns about and then committed twice.

### 9.3 The two dropped fields

Both measurements are reproduced in `spec/undecided.md`, which is where a refuted candidate lives.
The commands:

```
# spec_bytes: the product against the real prompt bytes
tp review <spec> --no-state | python3 -c \
  'import json,sys;d=json.load(sys.stdin);print(sum(len(p["prompt"]) for p in d["prompts"]), len(d["prompts"]))'
wc -c <spec>

# floor_figure_share: two-arm (shipped predicate) against one-arm (digit only)
tp ground <spec> --units | python3 -c \
  'import sys,re;u=[l.split(chr(9),2)[2] for l in sys.stdin if re.match(r"^u\d+\t",l)];\
   b=lambda s:re.sub(r"`[^`]*`"," ",s);\
   print(len(u), sum(1 for x in u if re.search(r"\d",b(x)))/len(u), sum(1 for x in u if "`" in x)/len(u))'
```

Run both in an `rsync` copy. `tp ground --units` emits a round when the spec has changed since the
last one, so running it against the repository to take a measurement advances a round the
measurement does not own. The share is over each spec's latest emitted floor, not over its round-1
floor — a population caveat that matters if these are ever compared across cycles.

### 9.4 The rule this round changed

From here to the tag, **§2 takes no further prose repair.** When a field is disputed the answer is a
test and the corpus, and the spec is fitted to the tested behaviour rather than the other way round.
A review finding of the form *"the text should say X"* is a `wontfix` naming the test; a finding of
the form *"the behaviour is wrong"* becomes a test written red first.

The reason is arithmetic. Three grounding rounds and no line of implementation: each round refuted
the previous round's *restatement* of behaviour that was never itself in question. Round 3's repair
is therefore a subtraction — six fields to four, and three prose restatements (the anchor rule, the
panel rationale, the universal that every field reads a floor predicate) to zero — with every
remaining field naming the function that computes it.

**It did not make the section shorter, and this file claimed it had.** Measured over the round
snapshots and the file today: §2 ran 37 lines at round 1, 38 at rounds 2 and 3, and 47 after the
cut. The claim went in unverified, was caught by counting, and is left here rather than deleted
because it is the release's own error class committed inside the release's own forensics — a
comparison asserted without being counted. The section grew because *disambiguating* `floor_size`'s
two live referents is new content that the earlier drafts were shorter for omitting. Naming a
function is cheaper than describing it; saying which of two functions you mean is not free.

Derive: `python3 -c 'import sys;\
ls=open(sys.argv[1]).read().split(chr(10));\
i=[n for n,l in enumerate(ls) if l.startswith("## ")];\
print([b-a for a,b in zip(i,i[1:]+[len(ls)]) if ls[a].startswith("## 2.")])' <file>`
The carry is doing the work the record exists for: 21 of round 2's judgements travel into round 3
rather than being re-derived. The floor grew each time because each repair added text; the asked
count fell anyway.

**Round 3's column was wrong in this file before it was right, and the error is the one this release
ships a rule against.** An earlier draft carried `79 / 26 / 53 / 21 / 32`, taken from the round-3
emission that existed when the table was written — before §1's and §3's fenced commands were
repaired, which added text and re-emitted the floor. The figures were copied at one tree state and
read at another. That is why §4's rule says a spec names the artifact rather than quoting the
figure, and why this table now names the command that derives each column.

**The same class hit the round-3 grading brief.** It said *"round 2 returned 12 FAIL over 38 units"*.
The 12 is right and the denominator is not: 12 is the `FAIL` count over all 53 recorded rows, while
38 is the number of units round 2 *asked*. The two are different populations, and the sentence
silently multiplies them into a rate that was never measured. The grading unit reported the same
pairing back. **A figure copied into a brief travels with its errors** — the `SKILL.md` rule that
ships in §4 is this instance generalised, and this is its second occurrence in three rounds.

## 10. The fenced command that is green under one shell and red under another

`spec/1.0.1.md` §1's block was verified before round 3 and again by three reviewers in review round
1. Two of them measured it printing **nothing**. Both were right, and so was the verification.

```
$ bash -c 'cd <clone> && <§1 block>' | wc -l
17
$ zsh  -c 'cd <clone> && <§1 block>' | wc -l
0        # exit 0
```

The cause is `"$tag:spec/.tp-review/$v/snapshot-round-1.md"`. `zsh` parses `$tag:s…` as the `:s`
history modifier, so the expansion becomes `v0.36.0shot-round-1.md`, every `git show` exits 128,
`|| continue` skips every iteration, and the loop ends having printed nothing at exit 0. `bash` has
no such modifier in parameter expansion. `${tag}` closes it, and the spec's block now uses braces
throughout.

**What this costs the rule it violates.** §4 shipped *"every fenced command in a spec runs at `HEAD`
and prints something"* without naming an interpreter, which makes the rule's verdict a property of
the operator's login shell rather than of the command. The pre-round mechanical step that caught two
other command defects in this cycle ran under `bash` and reported this one green; a reviewer whose
tool shell is `zsh` reported it red on the same tree at the same commit. The rule now names `bash`,
and the mechanical step invokes `bash -c` rather than inheriting a shell.

**The generalisable half:** a mechanical pre-round check is only as good as the environment it fixes.
This one was worth its cost twice over — it caught a command that printed nothing and a command that
detached the repository, both before a grading token was spent — and it still passed a defect through
because it left one variable free.

## 11. What `review_panel` is worth, in bytes

The case for this release rests on one field, and the number is here rather than in the spec.
Measured at `HEAD` in an `rsync` copy, for `spec/1.0.1.md`:

| way to learn a round-1 panel | bytes the agent reads | writes a round? |
|---|---|---|
| `tp review <spec> --no-state` | 22,181 | no |
| `tp review <spec>` | 22,181 plus the round | **yes** |
| `tp lint <spec>` (this release) | ~96 | no |

Roughly two to three orders of magnitude, on the only one of the fields with no cheap alternative.
Derive it on **a spec with review rounds left** — `spec/1.0.1.md` itself no longer works, which is
worth stating because it is this release's own rule breaking on its own forensics:

```
tp review spec/1.0.2.md --no-state | wc -c
tp review spec/1.0.2.md --no-state | python3 -c \
  'import json,sys;d=json.load(sys.stdin);print(len(json.dumps([p["role"] for p in d["prompts"]])))'
```

**The original block named `spec/1.0.1.md` and stopped printing part-way through this cycle.**
`tp review` refuses with exit 4 once a spec has recorded its cap — `1.0.1` recorded two rounds
against a cap of two — so the block returns nothing on the very file it measures. The magnitude is
unaffected: re-run on `spec/1.0.2.md` it returns 22,752 bytes against the 22,181 recorded above, a
difference that is the other spec's length rather than any drift in the measurement. §4's rule
*"every fenced command runs and prints something"* does not formally reach this file, which `tp
ground` does not grade — but a reader following the recipe got an empty result, and the rule exists
because that is indistinguishable from a measurement of zero.

For contrast, the argument §2 *did* make — that a list beats a count because it names the missing
role — is worth the difference between a four-element JSON array and an integer. The release's
strongest measurement was absent from the spec for three rounds while its weakest one was argued at
length.

**And a stale warning, corrected.** Several briefs in this cycle, including ones this orchestrator
wrote, told units that `tp ground --units` writes state and must be run in a copy. Measured twice
and independently: in an empty scratch directory it creates no file, and in a full copy with the
spec edited past its last round all eleven state files stay md5-identical. `--units` computes a live
floor and persists nothing. `spec/1.0.1.md` Non-Goal 2's *"writes nothing"* is correct; the ambient
warning was not.

## 12. What review round 1 measured, and the two rows it killed by running them

Round 1 returned 34 findings after dedup across four roles, one critical and nine high, with §2
carrying fifteen of them — against a section that had just been cut to four fields and stripped of
three prose restatements. That is the measurement that ended the argument about how §2 should be
written.

**Three sentences the round-3 cut introduced were refuted by running:**

| the cut's sentence | what a run returned |
|---|---|
| "`cut` carries the same split" | `tp review`'s `ungrounded` envelope is `{round, undispositioned, floor_size}`. There is no `cut` in it, and `GroundAdvisory` has no such field. |
| "every reason a role can be absent at round 1 is already reflected in it" | false for `no-spec-change`: a round-1 `--diff-from` emission drops a role through a filter that runs *after* the resolver |
| "a malformed role file exits 3 in both" | true for a reviewer file; a malformed **auditor** file gives lint 3 and review 0 |

And Non-Goal 8's *second* derivation died the way its first did: on a pristine clone with an empty
`git status --porcelain`, `tp audit spec/1.0.1.md` exits **0** and reports auditing the first fifty
of several hundred changed files. It needs no changed-file set and does not exit 4. The Non-Goal now
gives no reason at all, which is the correct shape for a scope decision.

**Two test rows were killed by building their mutants**, which is the first time in this cycle a row
died to an execution rather than to an argument:

- **The `floor_by_section` row.** Its assertion — the values sum to `floor_size` — is invariant under
  *every* anchor misassignment, because each uncut unit still receives exactly one anchor. Correct
  versus mutant on `spec/1.0.1.md`: seven keys against six, both summing to 62. The round-3 repair
  had moved the row's fixture; the defect was in the assertion, and no fixture could have exposed it.
  This is why the field was dropped rather than re-pinned.
- **The corpus-parity row.** No reviewer role in `.tp/reviewers/` declares `domains`, so the domain
  filter the mutant removes is inert. Sixty-five real emissions over `spec/*.md` and
  `spec/backlog/*.md` produced **zero** `domain-mismatch` skips. The row's own criterion — that only
  a spec the corpus actually filters separates mutant from original — is unmet by the corpus it
  names. Its universal was also unevaluable: seventeen specs are past round 1, `spec/0.28.0.md` exits
  4 with no prompts at all, and thirteen of the emissions include `regression`, which a round-1 panel
  excludes. It is replaced by two bare fixtures that construct the discriminating condition instead
  of hoping the repository contains one.

**A third row survived by having its sibling corrected.** Row 8 asserted that a structural sibling
still reports where the vague rule does not, and named `duplicate-line`. `blankInlineCode` is
rune-preserving, not byte-preserving, and the substitution is a deterministic function of the line,
so equal lines stay equal and every duplicate rule is provably invariant under the mutant —
measured 1→1 for `duplicate-line`, `duplicate-paragraph` and `orphan-list-item`, while
`numbering-gap` takes `[]*Heading` and never sees a line. Only `broken-cross-ref` discriminates
(1→0), and the row now names it.

**What three roles found that no single role would have.** The `§4` bullet count was wrong — the
section said four new rules over a list of six plus a precedent — and `architect`, `implementer` and
`ax-economist` each counted it independently in the same round. A count stated beside the artifact
that determines it is the defect class this release ships a rule against, committed in the section
that ships the rule.

**And one finding came from neither the roles nor the orchestrator.** `tp lint`'s `code-citation-drift`
mechanical check failed on a `spec/backlog/` filename that does not exist — invented during the
round-3 repair. All three roles that noticed it declined to file it, correctly, because the class is
mechanised; the check alone caught it. That is the mechanised-class contract working as designed, and
worth recording because the class was registered long before this cycle and had not paid out until
now.

## 13. The cap stop, and why the release goes to implementation unconverged

Review round 2 was a delta pass over the round-1 repair, five roles including `regression`. It
returned **52 findings against round 1's 34**, and `review_max_rounds` was 2, so the cycle stopped
at its cap without converging. The operator was given the numbers and chose implementation.

| | round 1 | round 2 |
|---|---|---|
| findings after dedup | 34 | **52** |
| critical / high | 1 / 9 | **3 / 19** |
| §2 | 15 | **19** |
| §6 | 8 | 11 |
| §7 (added by the round-1 repair) | — | **10** |
| roles | 4 | 5 |

For contrast, the three grounding rounds were converging slowly: non-clean rows 27, 24, 20.

Derive: `python3 -c 'import json,collections;print([(n,collections.Counter(json.loads(l)["severity"] for l in open(f"spec/.tp-review/1.0.1/review-round-{n}.ndjson") if l.strip())) for n in (1,2)])'`

**The count rose because the repair wrote new text, not because the release got worse.** §7 did not
exist at round 1 and drew ten findings on first exposure — the same treatment every new paragraph in
this cycle received. §2 rose from fifteen to nineteen after being cut to three fields and stripped of
every prose restatement, because the repair replaced the prose with a **table**, and the table named
two functions that compute different quantities: `engine.FloorUnits` returns candidate units
including the arms' cuts, `engine.FloorIndexRows` emits one row per unit unconditionally, so the
difference the table called `cut` is identically zero — measured across five specs whose real cut
counts are 33, 15, 97, 53 and 6.

**That is the finding the whole cycle was walking toward.** The prose was replaced with a table on
the theory that a table cannot make a false claim about behaviour. It can. What the spec author
cannot do — in prose, in a table, or in a test name — is state a fact about code that has not been
written. Five rounds established this by exhaustion: three grounding rounds, one review round, one
delta pass, five different sets of behaviour sentences, each written by the repair for the round
before, each refuted by running.

**So the columns are now empty by design.** §2's `derived by` and `pinned by` cells carry *"task N
names it here"*, each field's `value` is an **observable** — a command whose output a reader can
compare against lint's — and filling the cells is acceptance rather than authorship. §7 does the same
for its tests. The spec follows tested behaviour instead of predicting it.

### 13.1 Six classes tp flagged for mechanisation

`--record` named six `mechanize_candidates`, each seen in both rounds:

| class | rounds | total |
|---|---|---|
| `rule-not-self-applied` | 2 | 4 |
| `uncounted-quantifier` | 2 | 4 |
| `mutant-survives-its-row` | 2 | 2 |
| `panel-completeness-claim` | 2 | 2 |
| `redundant-test-row` | 2 | 2 |
| `refuted-derivation` | 2 | 2 |

The repository's rule is to write the check on the **third** round, not the second, so none is
registered here. The first two are its two oldest defect classes and are the plausible ones: whether
a `tp lint` rule can catch a spec asserting a count of its own list, or a spec violating a rule it
states, is the next lint release's question and is recorded in `spec/undecided.md` rather than
attempted now. Registering an untested check suppresses its whole class for every reviewer, which
this repository has already paid for once.

### 13.2 `--merge`'s `found_by` settled a disagreement between two roles

`architect` counted all 193 rows under `spec/.tp-review/1.0.1/` and reported that §4's claim —
*"three reviewers independently counted it wrong in the same round"* — is false, with exactly one row
carrying that miscount. `regression` read the merged round-1 row and found `found_by: 3`,
`found_by_roles: [architect, ax-economist, implementer]`. **The claim is true and the count is
wrong**, because `--merge` deduplicates by `(location, class)` and records the attribution on the
surviving row; counting raw per-role files after a merge loses it.

This is the first measured payoff of that field in this repository, and it is worth stating as a
reading rule: **after a merge, `found_by` is the attribution, not the row count.** It also cost
nothing — one role's careful count would have driven a repair that made the spec less true.

### 13.3 What the two `wontfix` rounds deferred, recorded so it is not lost

Round 1 deferred mechanising the fenced-command rule, on the ground that a check must be tested
before it is registered. Round 2 asked for that deferral to be *recorded*; this is the record. Also
deferred to task acceptance rather than to spec prose: the difference between a row's quoted counts
and the counts of the existing test it closes against; pinning the *binary* as well as the shell in
the fenced-command rule; and measuring `orphan-list-item`'s invariance under row 8's mutant rather
than arguing it.

And one disposition from round 1 was **corrected rather than defended**. Its evidence claimed each of
rows 4–7's mutants "fails exactly one" of the four existing tests. Built and run: under row 4's
mutant, rows 4, 5 and 7 all go 0 to 1 — three, not one. Rows 5's and 6's mutants do fail exactly one
each, as claimed. The disposition stands on its surviving half — each row pins a distinct `SHALL` —
and the false half is deleted rather than left in the record.

### 13.4 The counts §13.3 deferred, now run

**Two of §13.3's three deferrals are measured here**, at row 8's implementing task; pinning the
*binary* as well as the shell in the fenced-command rule is not, and stays deferred. Every number
below was produced in a full `rsync -a` copy of the tree, one copy per mutant, with each fixture run
through the rule directly rather than through a pass/fail assertion — so the copy reports a count
instead of a verdict, and a mutant that changes nothing is distinguishable from one that is not
built.

| existing test | rule | `HEAD` | mutant | that row's mutant, as built |
|---|---|---|---|---|
| `TestVagueLanguageSkipsFencedBlocks` | `vague-language` | 0 | 1 | row 4 — drop the fence skip |
| `TestVagueLanguageSkipsInlineCodeSpans` | `vague-language` | 0 | **2** | row 5 — drop the inline blanking |
| `TestVagueLanguageStillReportsProse` | `vague-language` | 2 | 1 | row 6 — break at the first fence |
| `TestVagueLanguageTreatsUnterminatedFenceLikeItsSiblings` | `vague-language` | **1** | **2** | row 7 — ignore fences on an odd fence count |

Rows 4 and 6 quote their test's counts exactly; **rows 5 and 7 do not**, and neither side is wrong.
A §6 row's fixture column counts the one trigger word the row is about, while the test's fixture
carries two triggers on a single line (row 5) and one already-reported trigger *before* the fence
(row 7). The rows are left as written and the tests' counts are recorded here, which is what the
deferral asked for.

**And §13.3's closing paragraph is corrected by the same run.** Under row 4's mutant as built here —
the fence skip removed, the inline blanking kept, which is the minimal mutant for row 4's `SHALL` —
the tests that fail are rows **4, 6 and 7**, not rows 4, 5 and 7, and only row 4's goes 0 to 1
(row 6's goes 2 to 3, row 7's 1 to 2). Row 5's fixture contains no fence at all, so a fence-only
mutant cannot move it: measured 0 under that mutant. Rows 5's, 6's **and 7's** mutants each fail
exactly one test. The claim *"three, not one"* survives; *which* three does not.

`orphan-list-item`'s invariance under row 8's mutant is now measured rather than argued, and the two
duplicate rules were re-measured beside it rather than carried forward from the round that first
claimed them. Each row inserts `blankInlineCode` into that rule alone, and every fixture carries
inline spans, so the mutant does alter each line the rule compares:

| rule | fixture | `HEAD` | mutant |
|---|---|---|---|
| `orphan-list-item` | a numbered list starting at 2 whose items carry inline spans | 1 | 1 |
| `orphan-list-item` | the same, with a span whose contents are a list marker | 1 | 1 |
| `duplicate-line` | two identical lines, each carrying an inline span | 1 | 1 |
| `duplicate-paragraph` | two identical two-line paragraphs, each carrying two spans | 1 | 1 |
| `broken-cross-ref` | row 8's fixture | 1 | **0** |

`numbering-gap` is invariant by signature rather than by measurement, and that is the honest
statement of it: `CheckNumberingGaps` takes `[]*Heading` and is never handed a line to blank.

## 14. What implementation did that five review rounds could not

This is the measurement the whole cycle was for, and it is a count rather than an argument.

**Five grading rounds** — three grounding, one review, one delta pass — produced five successive sets
of sentences about `tp lint`'s new fields, each written by the repair for the round before, each
refuted by running. None of them survived. The last set was a *table*, written on the theory that a
table cannot make a false claim about behaviour; its `derived by` column named two functions whose
difference is identically zero.

**Then the columns were emptied**, each field was given an observable instead of a function name, and
filling the cells became the implementing task's acceptance. Under that arrangement, **every one of
the first seven tasks corrected something a previous unit or the spec had asserted** — and each
correction came from a run, not a reading.

| task | what it corrected | how |
|---|---|---|
| `lint-floor-fields` | the cell `floor-count-export` had just filled named a test that **passes under the mutant it was meant to catch** — it pinned `engine.FloorSize` and never invoked `tp lint` | ran row 1's named mutant against it |
| `lint-floor-fields` | this file's `--compact`/`--quiet` byte figure was stale | re-measured rather than citing |
| `lint-floor-fields` | §6 row 1's *"a six-line frontmatter moves the floor by one unit"* is content-dependent, not a fixed offset | built two frontmatter blocks, one with a digit and one without |
| `resolver-split` | §7 weighed two alternatives; a third existed — `engine.roleUnits` was already the pure resolution — so the split put **two copies into one** instead of adding a third | read the package rather than the section |
| `lint-review-panel` | its own first mutant reddened a **fixture precondition**, so the message read *"the fixture is broken"* rather than *"the requirement is violated"* | reordered so the row's own assertion decides |
| `review-panel-edges` | a doc comment declaring a test's lint half vacuous was **stale** — the half had become discriminating one task earlier | ran two mutants against it |
| `review-panel-edges` | found a **pre-existing data race** that makes the gate green on one run and red on the next | ran the gate twice |
| `sibling-rule-test` | §13.3 of this file says row 4's mutant moves rows 4, 5 and 7; the tests that move are **4, 6 and 7**, and only one of them 0→1 | built the mutant and read which tests failed |
| `skill-and-reference-rules` | its own first draft shipped **8** rules against §4's derived **7**, which is the exact miscount §4 exists to warn about | ran `grep -c` instead of counting by eye |
| `reference-floor-size` | its brief said two payloads spell `floor_size`; there are **three** keys carrying two quantities, the third being `tp ground`'s own emission envelope where they coincide by construction | counted `json:"floor_size"` under `internal/` |
| `reference-floor-size` | its brief said `--status` would suffice; `ungrounded` is *absent* for a fully dispositioned spec, so observing it needed an emission on a spec with an open unit | ran `--status` and read what was missing |
| `readme-and-release-notes` | §11's derivation command had stopped printing on the spec it measures, because that spec exhausted its review cap mid-cycle | ran the block |
| `readme-and-release-notes` | this section's own closing sentence counted tasks it had not counted — see immediately below | counted the table's `task` column |

**Every correction above came from an execution**, set against five grading rounds in which the same
section was rewritten four times and was wrong after each.

**The count of them is derived from this table, not asserted beside it**, because the first draft of
this sentence said *"nine corrections across seven tasks"* and the table it sits under carried nine
rows over **six** distinct task ids — `floor-count-export` appears only as a thing corrected, never
as a corrector. That is an uncounted quantifier of exactly the class §4 ships a rule against,
committed in the paragraph celebrating the rule, and caught by the last task in the cycle running
`cut -d'|' -f2 | sort -u` over the table rather than reading it. The rows above are the record; count
them there.

**What separates the two regimes is not care and not effort.** The grading rounds were careful — they
built mutants, ran commands, and refuted real claims; two of them killed test rows by executing them.
What they could not do is check a sentence about code that does not exist. Every claim §2 made was a
prediction, and a prediction is refutable only by writing the thing. The implementing tasks were not
better readers; they had a compiler.

**The operational form of this, which is what ships in `SKILL.md`:** a sentence describing behaviour
the release does not change is a test name or it is not in the spec, and a sentence describing
behaviour the release *will create* is a requirement whose verification belongs to the task that
creates it. The spec follows tested behaviour rather than predicting it. Everything else in this file
is the evidence for that one line.

## 15. How the audit's file set was chosen, and what it could not see

A finding about `tp audit` rather than about this release, recorded here because it decided what
round 1 was able to read.

**`tp audit` auto-detects the changed files and caps the list.** Re-run on the tree after round 1's
repairs, `tp audit spec/1.0.1.md --json`'s `file_summary` reports `total_changed: 339`,
`total_files: 50` and `truncated: true`. The cap is the first 50 paths alphabetically, and 50 of 339
reaches neither `internal/engine/rolepanel.go` nor anything under `skills/`. All four roles saw the
skill documents only because the orchestrator named them in `--affected-files`.

**That hand-built list came from the tasks' own commits, and task commits cannot see a release's
pre-task work.** The ten tasks in `spec/1.0.1.tasks.json` carry 33 `commit_shas` between them; the
production Go files those commits touch number **six**. `git diff v1.0.0..HEAD` names **eight**. The
two it does not reach are `internal/engine/configresolve.go` and — the costly one —
`internal/engine/vague.go`, §3's own production file and where round 1's code FAIL lived.

The cause is checkable rather than inferred: §3's repair landed at `65406160`, timestamped
`2026-09-07 14:48`, while the first task commit `999ae1cc` is `17:50`. The repair predates the task
cycle, so no task's `commit_shas` can contain it, and any derivation keyed on them is blind to it by
construction.

**The correct derivation is the diff from the release base**, which sees pre-task work and post-task
work alike:

```bash
git diff --name-only v1.0.0..HEAD -- '*.go' | grep -v '_test.go$'
```

Recorded because the cap is silent about *which* files it dropped — `file_summary` gives the counts
and the `truncated` flag, not the names — and a role that never receives a file cannot report on it.

## 16. Why §6 row 2's fixture changed polarity after the audit

Round 1's repair moved three test fixtures' frontmatter under the `tp:` mapping, because tp reads
only that mapping and a top-level `domain:` key was inert. The repair landed and every mutant the
row names still reddened. Then one more mutant was built: make `engine.ResolveRolePanel` call
`ResolveActiveCorpus(dir, "software", phase)` and never read the spec's domain at all. **All four
repaired tests stayed green under it**, `TestLintReviewPanelOmitsADomainMismatchedRole` — §6 row 2's
test, and the pin on this release's headline field — included.

The cause is that the fixture's declared domain *was* the default. `ParseFrontmatter` returns
`software` for a spec with no frontmatter, so a fixture declaring `software` has a frontmatter block
that is live but not load-bearing: a mutant that ignores frontmatter entirely resolves the same
panel. The key was read; nothing turned on its value.

The fixture is now polarised — spec `tp.domain: prose`, role `domains: ["software"]` — and every
clause was run rather than predicted:

| | `review_panel` | test |
|---|---|---|
| `HEAD` | `["universal"]`, lint stderr empty | PASS |
| skip the domain filter (`roleAppliesToDomain` → `true`) | `["software-only", "universal"]` | FAIL on the row's own `NotContains` |
| ignore the frontmatter (`ResolveActiveCorpus(dir, "software", …)`) | `["software-only", "universal"]` | FAIL on the row's own `NotContains` |

`prose` rather than an invented domain because tp ships an embedded corpus for it, so the mismatch
draws no unknown-domain notice and the row's `assert.Empty(t, lintErr)` still measures the absence of
the wrapper's advisory channel rather than the absence of a warning about the fixture. The polarity
is asserted in the fixture against `engine.DomainSoftware` rather than restated in a comment, so it
cannot drift back onto the default.

The general shape is a mechanisation candidate in `spec/undecided.md`, `frontmatter-key-namespace`.
Note which predicate the two halves of this need: "a frontmatter key outside the `tp:` mapping"
catches the three inert-key instances round 1 found, and misses this one entirely — the key here is
inside `tp:`. Only "a frontmatter value equal to the parser's default" reaches it.
