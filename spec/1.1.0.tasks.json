{
  "version": 1,
  "spec": "spec/1.1.0.md",
  "created_at": "0001-01-01T00:00:00Z",
  "updated_at": "2026-09-08T10:04:36.034146Z",
  "workflow": {
    "review_max_rounds": 3
  },
  "coverage": {
    "total_sections": 6,
    "mapped_sections": 4,
    "context_only": [
      "# tp v1.1.0 — The spec follows tested behaviour",
      "## 4. Non-Goals"
    ],
    "unmapped": []
  },
  "tasks": [
    {
      "id": "fixture-migration",
      "title": "Migrate review finding fixtures to four keys",
      "status": "done",
      "tags": [
        "test",
        "fixtures"
      ],
      "depends_on": [],
      "estimate_minutes": 15,
      "acceptance": "Every review-finding fixture the suite feeds to `tp review --merge` or to `tp review \u003cspec\u003e --record` carries a non-empty `severity`, `finding`, `location` and `evidence`, with no production file touched by this task, and fixtures built by helpers count as much as single-line literals (`classRow` in `report_class_test.go` feeds nine record tests through `review_suppression_test.go:58`). The acceptance is outcome-shaped rather than census-shaped: the migration is complete when `go test -count=1 ./...` is green with §1 decision 5's parity change applied on top — applied locally, measured, then reverted — rather than when some number of rows has been edited. A deliberately incomplete fixture stays incomplete, measured: a mechanical sweep that repaired the row omitting `location` in `merge_inputs_test.go` turned `TestReviewMerge_InputsReportPerFileCounts` red at 3/1 against an expected 2/2, and this task closes green under the full project gate on its own with the four step exit codes read separately.",
      "source_sections": [
        "## 1. The decision"
      ],
      "source_lines": "44-48",
      "started_at": "2026-09-08T07:16:00.738796Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-08T07:32:41.705091Z",
      "closed_reason": "Every review-finding fixture the suite feeds to tp review --merge or tp review \u003cspec\u003e --record now carries a non-empty severity, finding, location and evidence: 145 row literals swept across 25 files plus 10 hand-edited rows in merge_inputs, record_status_named, review_record, output_flag_scope, spec_hash_hint and audit_signal.\n- Helper-built fixtures were migrated at the helper, not the call site: classRow (report_class_test.go, reached by nine record tests through review_suppression_test.go:58) and longFindingLine (ndjson_read_contract_test.go); rows the census rule cannot see were migrated too - rows carrying a resolved block and rows shaped for both phases at once.\n- Verified outcome-shaped rather than census-shaped: with §1 decision 5's parity change applied on top (evidence appended to missingFindingFields in review_merge.go, plus a strings.TrimSpace check on the top-level evidence in parseRecordRows), go test -count=1 ./... went from 68 failing test functions and 0 build failures to 0 and 0; the probe was then reverted and both production files are byte-identical to HEAD (git checkout confirmed, git status shows no non-test change).\n- Deliberately incomplete fixtures were left incomplete, each still omitting exactly the key it tests: merge_inputs_test.go:60 (no location, so TestReviewMerge_InputsReportPerFileCounts still measures 2 parsed / 2 skipped), review_merge_test.go's empty-severity and no_severity rows, and review_test.go:595 (TestReviewMissingSeverityDefaultsToUnknown). Audit rows and the --findings-only fixtures were left alone as out of the gate scope.\n- The project gate was run with the four step exit codes read separately and all four are 0: check-suite-state.sh 0 (race suite green, spec/.tp-review and .tp/rounds digests unchanged), golangci-lint run 0 with '0 issues.', check-deadcode.sh 0, check-complexity.sh 0.",
      "gate_passed_at": "2026-09-08T07:32:41.705091Z",
      "commit_sha": "6d774973",
      "commit_shas": [
        "6d774973",
        "277704e4"
      ],
      "commit_files": [
        "internal/cli/audit_signal_test.go",
        "internal/cli/budget_test.go",
        "internal/cli/finding_class_test.go",
        "internal/cli/ground_state_untouched_test.go",
        "internal/cli/harness_note_test.go",
        "internal/cli/integration_test.go",
        "internal/cli/location_clusters_test.go",
        "internal/cli/merge_inputs_test.go",
        "internal/cli/ndjson_read_contract_test.go",
        "internal/cli/nextaction_test.go",
        "internal/cli/output_flag_scope_test.go",
        "internal/cli/record_idempotent_test.go",
        "internal/cli/record_status_named_test.go",
        "internal/cli/regression_autoinclude_test.go",
        "internal/cli/report_class_test.go",
        "internal/cli/review_converge_live_test.go",
        "internal/cli/review_exclusion_test.go",
        "internal/cli/review_merge_test.go",
        "internal/cli/review_record_advisory_test.go",
        "internal/cli/review_record_test.go",
        "internal/cli/review_regression_test.go",
        "internal/cli/review_report_test.go",
        "internal/cli/review_resolve_positionals_test.go",
        "internal/cli/review_state_rounds_test.go",
        "internal/cli/review_test.go",
        "internal/cli/review_verify_test.go",
        "internal/cli/reviewconvergeon_clean_test.go",
        "internal/cli/role_attribution_transparency_test.go",
        "internal/cli/role_mode_test.go",
        "internal/cli/spec_hash_hint_test.go"
      ]
    },
    {
      "id": "record-required-fields",
      "title": "Record refuses a row missing a required field",
      "status": "done",
      "tags": [
        "cli",
        "review"
      ],
      "depends_on": [
        "fixture-migration"
      ],
      "estimate_minutes": 14,
      "acceptance": "§1 decisions 1 and 2 at the record gate, plus §2's table row: `evidence` is a mandatory non-empty free-text key beside `severity`, `finding` and `location`, `tp review \u003cspec\u003e --record` refuses a file holding a row missing any of the four, it names every offending line in one exit rather than stopping at the first, and tp does not look at what the evidence text says. §5 row 1 — WHEN a four-row file, one row per required key with each row legal on the other three, is recorded against a spec that already holds one recorded round → exit 1 naming lines 1, 2, 3 and 4, no round file written, `review_rounds` unchanged, the discriminating assertion being the SET of lines named because the stated mutant (a record-side check testing only `evidence`) also exits 1 and also names line 4 — and §5 row 2, WHEN the file's only row is `{}` → exit 1, no round file, `review_rounds` unchanged. Every test this change turns red is repaired here and named in the closure, measured on the tree `fixture-migration` left (the four named at decomposition time are green there, that measurement having used a narrower migration), and the ordering constraint — the new check pre-empting neither the pre-resolved-`fixed` abort's message nor the unreadable-spec exit 3 — is held by a dedicated assertion written in this task, since `spec_hash_hint_test.go` stopped discriminating it once its fixture became legal on all four keys, with the closure stating which ordering was chosen and naming that assertion.",
      "source_sections": [
        "## 2. The evidence field on a review finding",
        "## 5. Tests"
      ],
      "started_at": "2026-09-08T07:37:08.2659Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-08T07:48:56.84348Z",
      "closed_reason": "- tp review \u003cspec\u003e --record now refuses a file holding a row whose severity, finding, location or evidence is missing or empty once strings.TrimSpace has run, naming every offending line in one exit and reading nothing of what the evidence text says: missingRequiredFields plus missingFieldsClause in internal/cli/review_record.go accumulate the offenders, and runReviewRecord raises them with recordRequiredFieldsHint. Verified by running the freshly built binary on a six-row file in a temp dir outside the repo: one exit 1 naming lines 1 through 6, covering the empty object and an evidence of three spaces, with no round file written.\n- Section 5 row 1 is TestReviewRecord_RefusesEveryRowMissingARequiredField and row 2 is TestReviewRecord_RefusesAnEmptyObjectRow, both recorded against a spec that already holds one recorded round; each asserts exit 1, that review-round-2.ndjson does not exist, and that review_rounds still holds one entry. Row 1 rests on the SET of line numbers extracted from the message (ElementsMatch over 1, 2, 3, 4) rather than on the exit code: built and run, its stated mutant, requiredFindingFields reduced to evidence alone, turned it red with the actual set being 4 alone, and row 2 mutant, a check reading only the keys a row actually has, turned row 2 red at exit 0. Both mutants were reverted.\n- The ordering chosen is spec-file errors and every aborting row rule first, required-field refusal last: it is raised after engine.SpecHash and before EnsureReviewState, so an unreadable spec still exits 3 and a pre-resolved fixed row still returns its own message. TestReviewRecord_RequiredFieldRefusalIsRaisedLast is the dedicated assertion that holds both halves, and it was observed red under two further built mutants, one raising the check before SpecHash (exit 1 instead of 3) and one returning on the first offender (line 1 instead of the pre-resolved fixed row line 2), while TestSpecHashFailureNamesTheRealCause stayed green under both, confirming it no longer discriminates the constraint. No existing test went red: go test -count=1 ./... reports 0 failing test functions and 0 build failures both before and after, so the four functions named at decomposition time are green on the tree fixture-migration left, and the four project gate steps each exited 0 read separately.",
      "gate_passed_at": "2026-09-08T07:48:56.84348Z",
      "commit_sha": "436a9f5f",
      "commit_shas": [
        "436a9f5f",
        "e9117fe0"
      ],
      "commit_files": [
        "internal/cli/record_required_fields_test.go",
        "internal/cli/review_record.go"
      ]
    },
    {
      "id": "record-legal-file-records",
      "title": "Record a file legal on four keys",
      "status": "done",
      "tags": [
        "test",
        "review"
      ],
      "depends_on": [
        "record-required-fields"
      ],
      "estimate_minutes": 7,
      "acceptance": "§5 row 3 — WHEN a three-row file legal on all four keys, carrying no `resolved` block, its three severities `high`, `medium` and `low`, is recorded against a spec with no prior round → the round records with `findings: 3` and `consecutive_clean: 0`, no row's `evidence` length being load-bearing and any non-empty text serving. The test is written first and observed red against row 3's stated mutant — `--record` testing `resolved.evidence`, the key the record path already reads, in place of the top-level `evidence` — which exits 1 on that legal file while the implementation exits 0.",
      "source_sections": [
        "## 5. Tests"
      ],
      "source_lines": "146-146",
      "started_at": "2026-09-08T08:13:49.581067Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-08T08:18:23.132101Z",
      "closed_reason": "- §5 row 3, the positive half of §2's required set: a three-row file legal on all four keys and carrying no resolved block, its severities high, medium and low, records against a spec with no prior round at exit 0 with round 1, findings 3 and consecutive_clean 0. internal/cli/record_legal_file_test.go is a guard only — the row was green at HEAD, since rows 1 and 2 pin what --record refuses and nothing pinned what it must still accept; what it now catches is a future check that over-refuses a legal row.\n- Observed red under the row's stated mutant before it was trusted: parseRecordRows was made to test resolved.evidence — the key the record path already reads for a pre-resolved wontfix row — in place of the top-level evidence, and the legal file was then refused on all three lines at exit 1 with 'missing required field evidence'; the mutant was reverted and git diff --no-ext-diff confirms internal/cli/review_record.go unchanged.\n- Kept non-vacuous rather than resting on the exit code: findings 3 is read back against review-round-1.ndjson holding three rows, so the count cannot pass with a subset; consecutive_clean 0 is pinned by a fixture that is dirty under either converge_on policy (the high severity under blocking, the three survivors under all); and the three evidence texts run one character, a phrase and a sentence, so a check keyed on evidence length rather than emptiness is red — the row's 'no row's evidence length is load-bearing' clause.\n- Gate green, four exit codes read separately: check-suite-state.sh 0 (0 '--- FAIL:' lines and 0 build failures), golangci-lint run 0, check-deadcode.sh 0, check-complexity.sh 0.",
      "gate_passed_at": "2026-09-08T08:18:23.132101Z",
      "commit_sha": "15bbab2c",
      "commit_shas": [
        "15bbab2c"
      ],
      "commit_files": [
        "internal/cli/record_legal_file_test.go"
      ]
    },
    {
      "id": "record-preresolved-boundary",
      "title": "Pin the pre-resolved fixed boundary",
      "status": "done",
      "tags": [
        "test",
        "review"
      ],
      "depends_on": [
        "record-legal-file-records"
      ],
      "estimate_minutes": 7,
      "acceptance": "§5 row 6 — WHEN one file holds one row legal on all four required keys whose `resolved` carries `status: fixed` and its own evidence → `--merge` still emits it at exit 0 and `--record` still exits 1 naming its line, because §2's parity is over the required-field set and does not reach `--record`'s pre-resolved rules. The test is red against the stated mutant, which unifies the two gates by routing `--record` through `missingFindingFields` alone and so drops the pre-resolved `fixed` abort, taking `--record` to exit 0.",
      "source_sections": [
        "## 5. Tests"
      ],
      "source_lines": "149-149",
      "started_at": "2026-09-08T08:19:50.725662Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-08T08:26:21.341266Z",
      "closed_reason": "- §5 row 6, the boundary of §2's parity: a row pre-resolved resolved.status fixed and legal on all four required keys is still emitted by tp review --merge at exit 0 and still refused by tp review \u003cspec\u003e --record at exit 1 naming line 1, because the parity is over the required-field set and does not reach --record's pre-resolved rules. internal/cli/record_preresolved_boundary_test.go asserts it end to end: --merge writes the row to -o with merged_count 1, the merged row is read back for resolved.status fixed and its own top-level evidence, and --record then runs against those exact bytes.\n- Red against the row's stated mutant, built and run: deleting the case \"fixed\" abort from parseRecordRows so --record routes through missingFindingFields alone took --record to exit 0 and turned two assertions red — the exit code and os.IsNotExist on review-round-1.ndjson, the read-back ordered before the exit-code require so both are reached. Reverted and confirmed byte-identical with git -c diff.external= diff --no-ext-diff.\n- Green at HEAD before the change, so the guard's value is its non-vacuity, and three properties are pinned deliberately: a control run records the SAME row with the resolved block removed and nothing else changed (exit 0, findings 1), which proves the four keys legal and rules out the trap of passing for a required-field refusal; both rows are built from one shared base literal so a second difference cannot creep in; the row sits alone in its input file, so a --merge that skipped it would drop the input and refuse at exit 1 rather than pass unnoticed.\n- Gate green, four exit codes read separately: check-suite-state.sh 0, golangci-lint run 0, check-deadcode.sh 0, check-complexity.sh 0; 0 lines matching '^--- FAIL: ' and 0 build failures.\nOut of scope: the same mutant also turns TestReviewRecord_CleanAndDirty, TestReviewRecord_RowRules and TestReviewRecord_RequiredFieldRefusalIsRaisedLast red, so the pre-resolved fixed abort was not unguarded before this row; what none of them held is the boundary against §2's required-field set.",
      "gate_passed_at": "2026-09-08T08:26:21.341266Z",
      "commit_sha": "2f0858e9",
      "commit_shas": [
        "2f0858e9"
      ],
      "commit_files": [
        "internal/cli/record_preresolved_boundary_test.go"
      ]
    },
    {
      "id": "status-reads-pre-release-rounds",
      "title": "Status reads rounds recorded before this release",
      "status": "done",
      "tags": [
        "test",
        "review"
      ],
      "depends_on": [
        "record-required-fields"
      ],
      "estimate_minutes": 8,
      "acceptance": "§5 row 7, which is §1 decision 3 — WHEN `tp review \u003cspec\u003e --status` reads a round recorded before this release, whose rows carry no `evidence` → exit 0 with `consecutive_clean: 0`, and exit 1 under `--check` as before, because the requirement is enforced when a round is recorded and never when one is loaded. The fixture is a pre-`1.1.0` round file copied under `testdata` holding one undispositioned `high` row and no `evidence` key, rather than the live corpus, which stops being uniformly pre-`1.1.0` at this release's own first recorded round, and the test is red against row 7's mutant, a status path applying the record-time check on load.",
      "source_sections": [
        "## 1. The decision",
        "## 5. Tests"
      ],
      "started_at": "2026-09-08T08:34:33.300027Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-08T08:43:48.316656Z",
      "closed_reason": "- §5 row 7, which is §1 decision 3: tp review \u003cspec\u003e --status reads a round recorded before this release, whose one undispositioned high row carries no evidence, at exit 0 with consecutive_clean 0, and still exits 1 under --check. internal/cli/status_pre_release_round_test.go asserts it against internal/cli/testdata/pre-v1.1.0-review-round.ndjson, one row copied byte for byte out of spec/.tp-review/1.0.0/review-round-4.ndjson (sha256 f9c9212c…, verified identical) rather than a read of the live corpus.\n- What I built for the fixture, because a round file alone is not a readable round: the temp dir holds spec.md, .tp-review/spec/review-round-1.ndjson (the copied row) and a hand-written .tp-review/spec/state.json indexing round 1 with the spec's real hash — without the index, --status aborts at exit 3 on a round file beside a missing state.json.\n- Red under row 7's stated mutant, built and run: fourteen lines in runReviewStatus applying missingFindingFields to every row engine.LoadRoundRows returns. The test failed at the plain --status run with exit 1 and {\"error\":\"missing required field evidence\"}. Reverted, and the revert confirmed by git -c diff.external= diff --no-ext-diff (review_status.go byte-identical, codedbpro revision back to e2dc650f).\n- What this row holds that the tree did not: the whole suite under that mutant produced exactly one --- FAIL line, this test, and zero build failures — so nothing else in 240 test files reaches the load path with an evidence-less row. The two paths are different packages: parseRecordRows has one caller, the --record sink, while --status reads through engine.LoadRoundRows, which validates nothing.\n- Four properties pinned so no assertion is vacuous, each a require on the fixture or a read-back: the fixture is one row, severity high, undispositioned and legal on finding/location/severity so evidence is the only required key it lacks; state.json stores clean:true and the payload reports clean:false, which only opening and grading the round file can do; overlap_report credits the row's own role tester with unique 1, a second independent parse of the same bytes; stale is false, so the --check exit 1 is attributable to convergence rather than to a hash that never matched; and the same bytes handed to --record in a separate temp dir are required to exit 1 naming evidence, which pins the fixture on the wrong side of the record-time gate — without it the test would pass against a fixture carrying evidence and prove nothing.\n- Project gate green, four exit codes read separately: check-suite-state.sh 0 (0 --- FAIL lines, 0 build failures), golangci-lint run 0 (0 issues), check-deadcode.sh 0, check-complexity.sh 0.",
      "gate_passed_at": "2026-09-08T08:43:48.316656Z",
      "commit_sha": "ed8960c0",
      "commit_shas": [
        "ed8960c0"
      ],
      "commit_files": [
        "internal/cli/status_pre_release_round_test.go",
        "internal/cli/testdata/pre-v1.1.0-review-round.ndjson"
      ]
    },
    {
      "id": "merge-required-fields",
      "title": "Merge skips a row missing a required field",
      "status": "done",
      "tags": [
        "cli",
        "review"
      ],
      "depends_on": [
        "fixture-migration",
        "record-required-fields"
      ],
      "estimate_minutes": 12,
      "acceptance": "§1 decision 2 at the merge gate: `evidence` joins `location`, `severity` and `finding` in `missingFindingFields`, so `--merge` skips a row missing it, names the missing key in the stderr warning it already writes for the other three, and counts it as skipped. §5 row 5 — WHEN the row omitting `evidence` sits alone in its own input file, beside a second file holding one row legal on all four keys → `--merge` skips it and exits 1, that input having parsed no row under the existing dropped-input rule which the legal second file does not lift, while `--record` exits 1 naming its line. The offending key must be `evidence` rather than one of the other three, because with `location` omitted instead `--record` accepts the row and the mutant becomes indistinguishable from the shipped rule, and the one test function this change adds to the red set is repaired here — measured in a clone, `TestRecordIdempotent_RetryAfterPartialFailureConverges` is green under the record change alone and red only once both gates carry the rule.",
      "source_sections": [
        "## 2. The evidence field on a review finding",
        "## 5. Tests"
      ],
      "started_at": "2026-09-08T07:50:35.110222Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-08T07:56:53.406405Z",
      "closed_reason": "- §1 decision 2 at the merge gate: evidence joins location, severity and finding in missingFindingFields (internal/cli/review_merge.go), so --merge skips a row missing it, names the key in the stderr warning it already writes for the other three, and counts it as skipped. Verified with a freshly built binary in a temp dir outside the repo: a two-file merge printed 'warning: skipping incomplete line (missing evidence) in f1.ndjson' and reported that input as parsed 0 / skipped 1.\n- §5 row 5: TestReviewMerge_SkipsRowMissingEvidence (internal/cli/merge_required_fields_test.go) puts the row omitting evidence alone in its own input file beside a second file legal on all four keys, and asserts merge exit 1, the stderr warning naming evidence, per-input counts 0/1 and 1/0, and --record exit 1 naming line 1. Written first and observed red against row 5's stated mutant, which is exactly the pre-task tree: merge exited 0 because it parsed the offending row.\n- The offending key is evidence rather than one of the other three, as the acceptance requires; --record's rules were not touched and missingFindingFields keeps its equality-to-empty test, since unifying the two gates on strings.TrimSpace is shared-trim-predicate's acceptance, not this one.\n- Nothing entered the red set. go test -count=1 ./... is green with 0 '--- FAIL:' lines and 0 build failures, and the four gate steps each exit 0.\nOut of scope: this task's acceptance and the brief both state that TestRecordIdempotent_RetryAfterPartialFailureConverges is red once both gates carry the rule and must be repaired here. Measured on the tree fixture-migration left, it is green before and after this change: its 'disposed' fixture row at record_idempotent_test.go:126 already carries a non-empty top-level evidence, and it never invokes --merge (recordUnitRound calls only 'review spec.md --record'). The decomposition-time measurement was taken against a narrower migration than the one that shipped.",
      "gate_passed_at": "2026-09-08T07:56:53.406405Z",
      "commit_sha": "7af4a740",
      "commit_shas": [
        "7af4a740",
        "49b7da73"
      ],
      "commit_files": [
        "internal/cli/merge_required_fields_test.go",
        "internal/cli/review_merge.go"
      ]
    },
    {
      "id": "shared-trim-predicate",
      "title": "Trim decides emptiness for all four keys",
      "status": "done",
      "tags": [
        "cli",
        "review"
      ],
      "depends_on": [
        "merge-required-fields"
      ],
      "estimate_minutes": 12,
      "acceptance": "§2's one-predicate paragraph: both gates decide missing-or-empty by the same rule, and a value that is empty once `strings.TrimSpace` has run counts as empty for `severity`, `location` and `finding` as much as for `evidence`. The predicate is pinned over all four keys rather than two, because a build trimming only `evidence` and `location` while leaving `severity` and `finding` on an equality-to-empty test was measured green across every §5 row, so one fixture row carries a single space in each of `severity`, `location` and `finding` and the test goes red when the trim reaches fewer than four keys. §5 row 9 — WHEN one file holds two rows, the first carrying an `evidence` of one space and legal on the other three, the second legal on all four → `--merge` exits 0, skips the first, and warns naming `evidence`, while `--record` exits 1 naming its line — and §5 row 11, the same two-row shape with a `location` of one space → `--merge` exits 0, skips it, warns naming `location`, exactly as for a missing `location`, both rows asserting merge exit 0 because the file still parses its second row, which is what separates them from row 5, with `HEAD`'s equality-to-empty test standing as row 11's mutant.",
      "source_sections": [
        "## 2. The evidence field on a review finding",
        "## 5. Tests"
      ],
      "started_at": "2026-09-08T07:58:32.555962Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-08T08:05:05.845137Z",
      "closed_reason": "- One predicate, called from both gates (§2): missingFindingFields now lives beside requiredFindingFields in internal/cli/review_record.go, iterates that one four-key list, and decides emptiness after strings.TrimSpace; review_merge.go's own copy — which tested equality to the empty string over its own key list — is deleted and its call site now reaches the shared one. Verified with a freshly built binary in a temp dir outside the repo: one two-row file whose first row carries a single space in severity, location and finding made --merge warn 'skipping incomplete line (missing severity, finding, location)' at exit 0 with that input parsed 1 / skipped 1, and --record refuse at exit 1 with 'line 1: missing required fields severity, finding, location' — the same three keys in the same order at both gates.\n- §5 row 9 (evidence of one space, judged missing at both gates) and row 11 (location of one space, skipped by --merge exactly as an absent location is) are pinned in internal/cli/shared_trim_predicate_test.go, each on a two-row file so merge exits 0 and the skip shows as a warning rather than a dropped input. Both assert the exact set of keys the warning names and the per-input parsed/skipped counts, because row 11's stated mutant — HEAD's equality-to-empty test — gives exit 0 either way.\n- The predicate is pinned over all four keys rather than two: a third test feeds a row carrying a single space in severity, location and finding at once and asserts the warning names all three. Both mutants were built and run. Row 11's mutant is the pre-task tree, and all three new tests were red against it before the production change (measured, not predicted). The second mutant — trimming only evidence and location, leaving severity and finding on equality-to-empty — was applied and the whole internal/cli package run: exactly one test red, TestReviewMerge_TrimDecidesEmptinessForEveryRequiredKey, confirming the acceptance's claim that such a build is green across every other §5 row and that this fixture is its only discriminator. The mutant was then reverted.\n- Project gate green, four exit codes read separately: check-suite-state.sh 0, golangci-lint run 0 (0 issues), check-deadcode.sh 0, check-complexity.sh 0. No existing test turned red; no package failed to build. Deadcode is the step that would have caught a leftover missingRequiredFields, which is why one of the two functions was deleted rather than left as an alias.\n- Out of scope: internal/cli/audit_merge.go:184 writes the same 'skipping incomplete line (missing %s)' warning from its own audit-side missing-fields predicate, which is unrelated to §2's review-finding set and was left untouched. Also: the brief's corpus figure is now 331 canonical round files / 20,159 rows rather than 330 / 20,130 — the conclusion is unchanged, zero rows carry a whitespace-only severity, location, finding or evidence, so this change alters no recorded data.",
      "gate_passed_at": "2026-09-08T08:05:05.845137Z",
      "commit_sha": "29183d46",
      "commit_shas": [
        "29183d46",
        "948ccbc1"
      ],
      "commit_files": [
        "internal/cli/review_merge.go",
        "internal/cli/review_record.go",
        "internal/cli/shared_trim_predicate_test.go"
      ]
    },
    {
      "id": "merge-representative-evidence",
      "title": "Merge representative carries its own evidence",
      "status": "done",
      "tags": [
        "test",
        "review"
      ],
      "depends_on": [
        "merge-required-fields"
      ],
      "estimate_minutes": 7,
      "acceptance": "§5 row 4 — WHEN `tp review --merge` clusters two rows sharing `(location, class)` whose `evidence` texts differ → the representative row carries its own `evidence` and the merged output is itself recordable, asserted by an end-to-end merge-then-record run rather than by a field read. The fixture is two input files with identical `location`, `class` and `severity`, different finding text and different `evidence` text, the roles sorting `implementer` before `tester` so the representative is fixed by the fixture, and the test is red against the stated mutant, a merge emitting the representative with `evidence` dropped, after which `--record` refuses the merged file.",
      "source_sections": [
        "## 5. Tests"
      ],
      "source_lines": "147-147",
      "started_at": "2026-09-08T08:06:41.889741Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-08T08:11:46.921692Z",
      "closed_reason": "- §5 row 4, asserted end to end rather than by a field read: internal/cli/merge_representative_evidence_test.go merges two rows sharing (location, class) and severity but differing in finding text and in evidence, then runs tp review spec.md --record against the file --merge wrote. The merge exits 0 with merged_count 1 and duplicates_removed 1, the one emitted row carries role implementer, found_by 2 and the implementer's own evidence text, and --record accepts that file unchanged at exit 0 with round 1 and findings 1.\n- The row was already green at HEAD in both halves, so the test ships as a regression guard rather than as an acceptance: clusterMergeFindings emits allFindings[c.Representative(cfs)] verbatim and never projects the row onto a field list, so evidence survives by construction. What the guard now catches is any future merge path that rebuilds the representative instead of passing it through, which would silently make --merge's own output unrecordable.\n- Red under the row's stated mutant, built and run: delete(row, \"evidence\") inserted after the representative lookup in clusterMergeFindings made the field assertion fail on a nil evidence AND --record refuse the merged file with 'line 1: missing required field evidence' at exit 1; the mutant was reverted (git diff clean on review_merge.go) before the gate ran.\n- The fixture pins the properties the verdict rests on rather than assuming them: merged_count 1 and found_by 2 prove the two rows really clustered, since two unclustered rows would each carry their own evidence under the mutant too; the two evidence texts are asserted unequal; and the finding texts are ordered so that role, not finding text, is the key §8.4 uses to choose the representative.\n- Quality gate green, four exit codes read separately: check-suite-state.sh 0, golangci-lint run 0, check-deadcode.sh 0, check-complexity.sh 0; zero '--- FAIL:' lines and zero 'build failed' lines in the suite output. No production file changed.",
      "gate_passed_at": "2026-09-08T08:11:46.921692Z",
      "commit_sha": "3e710bd4",
      "commit_shas": [
        "3e710bd4"
      ],
      "commit_files": [
        "internal/cli/merge_representative_evidence_test.go"
      ]
    },
    {
      "id": "merge-refusal-leaves-output-untouched",
      "title": "Refuse the merge before writing its output",
      "status": "done",
      "tags": [
        "cli",
        "review"
      ],
      "depends_on": [
        "shared-trim-predicate"
      ],
      "estimate_minutes": 13,
      "acceptance": "§5 row 10 — WHEN `tp review --merge` exits non-zero → it creates no file at the `-o` path and modifies no file already there, `HEAD` being the mutant because it writes the surviving row before it refuses. The fixture is two input files, the first's only row omitting `location` so that input parses no row and the merge refuses at exit 1, the second legal on all four keys, run three times against one `-o` path: once with the path absent, once with it already holding a seed line whose bytes are pinned before the run, and once with a missing input file named in place of the first, which refuses at exit 3 against the same seeded path so that non-zero is pinned by more than exit 1 — while a clean round stays untouched, two zero-byte inputs and two blank-line-only inputs still merging at exit 0 and still writing the zero-byte `-o` the loop depends on. The `-o` write leaves no partial file behind when the write itself fails, by writing to a temporary path and renaming on success, `internal/cli/merge_inputs.go`'s `finishMerge` doc comment is rewritten to state the new rule and to say what supersedes v0.35.0 §8a.4's stated rationale, and `TestMerge_DroppedInputStillWritesOutput` — measured in a clone as the single test this change turns red — is rewritten to the new rule rather than deleted.",
      "source_sections": [
        "## 5. Tests"
      ],
      "source_lines": "153-153",
      "started_at": "2026-09-08T09:40:19.503547Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-08T09:54:16.924333Z",
      "closed_reason": "- §5 row 10: `tp review --merge` now creates no file at the `-o` path and modifies no file already there when it exits non-zero. writeMergeOutput (internal/cli/merge_inputs.go) declines the write when an input was dropped and reports whether it wrote, so review_merge.go names `output_path` in the summary only when a file is there to name; the accounting still reaches stdout. Verified with a freshly built binary in a temp dir outside the repo, three runs against one -o path: absent -\u003e exit 1 and `ls` reports no such file; seeded -\u003e exit 1 and sha256 245edd73... unchanged; missing input named in place of the dropped one -\u003e exit 3 and the same sha unchanged. At HEAD the same three runs left a 149-byte file, replaced the seed, and left it unchanged respectively.\n- The rule does not reach a clean round: the refusal is keyed on the dropped input, never on the merged row count. Measured, two zero-byte inputs and two blank-line-only inputs each still exit 0 and still write the zero-byte -o the loop reads as nothing found; TestMerge_CleanRoundStillWritesOutput pins both shapes and goes red under a mutant keying the refusal on an empty payload.\n- The -o write goes to os.CreateTemp in the destination's own directory and is renamed on success, with one failure path removing the temporary however the write ends. TestMerge_UnwritableOutputLeavesNoPartialFile pins the no-leftover half (red when the os.Remove is dropped) and TestMerge_OutputGoesThroughATemporaryFile pins the mechanism itself, since the first cannot tell temp-and-rename from a plain os.WriteFile: a destination directory at mode 0500 holding a still-writable seeded file makes the merge exit 3 with the seed intact, and is red under an os.WriteFile mutant.\n- finishMerge's doc comment is rewritten: it states the new rule for the review merge, quotes v0.35.0 §8a.4's superseded rationale and says what supersedes it (the accounting reaches stdout either way; it was the file, not the accounting, that let a refused merge be chained into --record as a clean round), and records that the rationale still stands for `tp audit --merge`. The stale copy of the old comment that survived above the rewritten function was deleted, checked by counting the phrase in the file.\n- TestMerge_DroppedInputStillWritesOutput was rewritten, not deleted, as TestMerge_RefusedMergeLeavesOutputPathUntouched, with the three runs the row's fixture names. Measured red set of this change: that one test plus TestAuditMerge_OnlyMalformedFails when the change was first applied to both merges — the acceptance predicted one. Dogfooded on spec/.tp-review/0.37.0/review-round-1.ndjson, 65 rows, none carrying evidence: the merge refuses at exit 1 and writes nothing, so REFERENCE.md:182's `[ -f ]`-guarded `;` chain now stops instead of recording a zero-byte file as a converged round.\n- Quality gate green on the final tree, four exit codes read separately: check-suite-state.sh 0, golangci-lint 0, check-deadcode.sh 0, check-complexity.sh 0; 0 '--- FAIL:' lines and 0 'build failed' packages.\nOut of scope: `tp audit --merge` still writes -o before it refuses, identically to the defect fixed here — measured, a seeded outC.ndjson was replaced at exit 1. It is left alone deliberately because spec/1.1.0.md §4 says 'No change to the audit phase'; the audit side of the acceptance channel is spec/backlog/00-a-finding-can-leave-a-round.md, which is where this belongs.",
      "gate_passed_at": "2026-09-08T09:54:16.924333Z",
      "commit_sha": "bcf5bf3b",
      "commit_shas": [
        "bcf5bf3b",
        "4705c124"
      ],
      "commit_files": [
        "internal/cli/audit_merge.go",
        "internal/cli/merge_inputs.go",
        "internal/cli/merge_inputs_test.go",
        "internal/cli/review_merge.go"
      ]
    },
    {
      "id": "verify-prompt-output-contract",
      "title": "Give the verify prompt an output contract",
      "status": "done",
      "tags": [
        "cli",
        "review"
      ],
      "depends_on": [
        "shared-trim-predicate"
      ],
      "estimate_minutes": 11,
      "acceptance": "`tp review \u003cspec\u003e --verify` emits an output-format JSON object line, which its prompt carries none of at `HEAD` (`review_verify.go:267`), and that line names the four required keys `severity`, `finding`, `location` and `evidence`, so a row copied from it survives both gates. The whole contract is authored rather than one key, because a verifier findings file reaches `--merge` and `--record` like any other, so once the required set lands a verifier file whose rows lack `evidence` parses zero rows and trips the dropped-input exit 1. The `role` stamp with the value `verifier` stays the separate stamp the builder already writes and is not added to the output format's key list, and the assertion is made by emitting the prompt in a `git clone --no-hardlinks` outside the repository and parsing that object line, never by a substring search.",
      "source_sections": [
        "## 5. Tests"
      ],
      "source_lines": "151-151",
      "started_at": "2026-09-08T09:16:50.623121Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-08T09:23:29.579327Z",
      "closed_reason": "- tp review \u003cspec\u003e --verify now ends with an output format whose JSON object line names exactly the four keys both gates require — severity, finding, location, evidence — the set requiredFindingFields declares in internal/cli/review_record.go; verifyOutputFormat in internal/cli/review_verify.go replaces the prose-only 'Respond with one JSON finding per line' tail that carried no object line at all.\n- Verified as §5 row 8 asks, by emitting and parsing rather than searching: a git clone --no-hardlinks outside the repository, built with this change, ran tp review spec.md --verify --findings findings.ndjson and its prompt held exactly one line parsing as a JSON object, with keys ['severity','finding','location','evidence'].\n- A row copied from that key list survives both gates, run with the same clone binary outside the repository: --merge reported that input parsed 1 / skipped 0 at merged_count 1, and --record exited 0 recording it as findings 1 (round 1).\n- The role: \"verifier\" stamp stays the separate instruction the builder already wrote and is absent from the key list; the emitted prompt still carries that instruction, and the parsed key set does not carry role.\n- internal/cli/verify_prompt_contract_test.go asserts on the parsed set, never a substring: it collects every prompt line that parses as a JSON object, requires exactly one, checks the four keys and role's absence, and pins the reason for parsing by asserting the word evidence still occurs in the prompt with the object line removed — its fixture's fixed finding carries resolution evidence, so a substring search is green with no contract present. Red under three mutants, each at its own assertion: evidence dropped from the key list (line 64), HEAD's prose-only tail restored so no object line is emitted (line 57), role added to the key list (line 66); all three reverted and the revert confirmed with git -c diff.external= diff --no-ext-diff.\n- Gate green, four exit codes read separately: check-suite-state.sh 0 (0 lines matching '^--- FAIL: ' and 0 packages reporting 'build failed'), golangci-lint run 0, check-deadcode.sh 0, check-complexity.sh 0.\nOut of scope: findingFormat at internal/cli/review.go:804 — the panel prompts' object line — still names severity, category, location, finding, suggestion and no evidence, and codeAuditOutputFormat at :1766 likewise; that is the open task prompt-formats-name-required-keys, not this fence.",
      "gate_passed_at": "2026-09-08T09:23:29.579327Z",
      "commit_sha": "9abea9d6",
      "commit_shas": [
        "9abea9d6",
        "17ebb12d"
      ],
      "commit_files": [
        "internal/cli/review_verify.go",
        "internal/cli/verify_prompt_contract_test.go"
      ]
    },
    {
      "id": "prompt-formats-name-required-keys",
      "title": "Prompt output formats name the required keys",
      "status": "done",
      "tags": [
        "cli",
        "review"
      ],
      "depends_on": [
        "verify-prompt-output-contract"
      ],
      "estimate_minutes": 12,
      "acceptance": "§5 row 8, which is §1 decision 1's last clause — WHEN a review role prompt is emitted → the key list in the prompt's output format, the JSON object line a reviewer copies, names `evidence`. The fixture is the five commands the row names rather than a count — `tp review \u003cspec\u003e`, `tp review \u003cspec\u003e --role \u003cr\u003e`, `tp review \u003cspec\u003e --perspective code-audit`, `tp review \u003cspec\u003e --perspective regression` and `tp review \u003cspec\u003e --verify` — each emitted and its output-format object line parsed, with a whole-prompt substring search ruled out because the word already occurs in a panel prompt's instruction text and in whatever an `--affected-files` excerpt contains, and with a listed path carrying no object line failing the check rather than being skipped. Every listed format carries all four required keys rather than `evidence` alone, since `codeAuditOutputFormat`'s eleven keys carry no `location` and a reviewer copying that line verbatim with only `evidence` added still produces a row `--merge` skips and `--record` refuses — and, measured in a clone, this change turns no existing test red, so the closure names what it ran rather than what it repaired.",
      "source_sections": [
        "## 1. The decision",
        "## 5. Tests"
      ],
      "started_at": "2026-09-08T09:26:06.549076Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-08T09:38:22.480986Z",
      "closed_reason": "- §5 row 8: the object line a reviewer copies now names all four required keys on every one of the five commands the row lists. findingFormat (internal/cli/review.go), carried by every panel role prompt and by the regression prompt, gains `evidence`; codeAuditOutputFormat gains `location` as well, since its eleven keys named file and line but no spec anchor, so `evidence` alone would still have produced a row --merge skips and --record refuses. Both formats now state beneath the line that severity, finding, location and evidence are required and non-empty.\n- Verified by parsing, not by searching: internal/cli/prompt_required_keys_test.go emits `tp review \u003cspec\u003e`, `--role tester`, `--perspective code-audit`, `--perspective regression` and `--verify`, requires exactly one JSON object line per emitted prompt — so a listed path carrying none fails rather than being skipped, and the keys asserted are provably that line's — and asserts the four keys on the parsed key set. Seven prompts are graded across the five commands and the count is pinned: the default panel must emit more than one prompt and --role exactly one.\n- The substring search is ruled out by measurement rather than by assertion. With the object line excised the word `evidence` still reaches every listed prompt, by a different route each: the panel prompts from tp's own read-only instruction ('report it with its evidence instead of making the edit'), code-audit from its --affected-files excerpt, regression from the changed-section body, verify from the rendered resolution evidence of the fixed finding. Each case names its sentence and requires it to survive excision, so an edit that removes a decoy fails loudly instead of leaving the rebuttal vacuous.\n- Six mutants built and each watched go red, then reverted (confirmed with git -c diff.external= diff --no-ext-diff): evidence dropped from findingFormat reddened the default panel, --role and regression rows; location dropped from codeAuditOutputFormat reddened code-audit; evidence dropped from verifyOutputFormat reddened verify, so this test grades that path too and not only its sibling; a second object line added to findingFormat reddened the exactly-one assertion; codeAuditOutputFormat's object line replaced by prose that still names all thirteen keys reddened the same assertion, which a key-name search would have passed; and removing the excerpt decoy from the fixture reddened the substring rebuttal.\n- The acceptance's prediction that this turns no existing test red was measured rather than assumed and held: the full suite under ./scripts/check-suite-state.sh exits 0 with zero '--- FAIL: ' lines and zero 'build failed' packages, counted separately. All four gate steps exit 0 — check-suite-state.sh 0, golangci-lint run 0 (0 issues), check-deadcode.sh 0, check-complexity.sh 0.\n- Honest limit: the bare-word rebuttal no longer discriminates, and the test says so where it would otherwise mislead. Since this change both formats name the four keys in prose beneath the object line, a require.Contains(rest, \"evidence\") could not fail whatever the fixture does, so that assertion was removed in favour of the per-case named sentences.\n- Out of scope: outputContractInstruction is shared with all four auditor prompts and §4 forbids changing the audit phase, so it was not touched. It still tells every review role to stamp role, location, class and severity and names no evidence, which means a reviewer reading the stamp block rather than the object line is told a four-key contract that omits the key both gates now require. docOutputFormat and testOutputFormat emit plan items rather than findings and reach neither gate, so they are untouched by design.",
      "gate_passed_at": "2026-09-08T09:38:22.480986Z",
      "commit_sha": "3eedb2cd",
      "commit_shas": [
        "3eedb2cd",
        "08933080"
      ],
      "commit_files": [
        "internal/cli/prompt_required_keys_test.go",
        "internal/cli/review.go"
      ]
    },
    {
      "id": "reference-recipe-scope",
      "title": "Scope the overlap recipe to post-release rounds",
      "status": "done",
      "tags": [
        "docs"
      ],
      "depends_on": [
        "shared-trim-predicate"
      ],
      "estimate_minutes": 8,
      "acceptance": "The *Measuring whether a second model earns its cost* recipe in `skills/tp/REFERENCE.md` has its required-field set corrected to the four keys of §2, the sentence at line 685 today stating that `--merge` requires only `location`, `severity` and `finding`. The recipe as a whole gains a caveat that it applies to rounds recorded at or after v1.1.0, because a pre-`1.1.0` pair fed to `--merge` skips every row and yields no `overlap_report` at all, so the tag would otherwise ship a procedure that cannot be run on the corpus it was written for.",
      "source_sections": [
        "## 3. What ships in the skill documents"
      ],
      "started_at": "2026-09-08T08:28:27.183121Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-08T08:32:57.398498Z",
      "closed_reason": "- The *Measuring whether a second model earns its cost* recipe in skills/tp/REFERENCE.md now names the four required keys: its step 2 said `--merge` requires only location, severity and finding, and reads \"requires severity, finding, location and evidence\" — the set requiredFindingFields declares at internal/cli/review_record.go:321 and the shared predicate at :329 applies, which review_merge.go:166 calls, so the sentence and the gate now agree. The rest of that step, that role is attribution --merge never checks against the corpus, is retained and re-checked: no corpus resolution appears anywhere in review_merge.go.\n- The recipe gains a scope caveat, placed before its numbered steps because step 1 is prospective advice while the caveat governs whether the corpus already on disk can be used at all: it applies to rounds recorded at or after v1.1.0. Measured with a freshly built binary in a temp dir outside the repo, merging this repository's own spec/.tp-review/0.37.0 rounds 1 and 2 — every content line of both skipped (parsed 0 / skipped 65 and 0 / skipped 56, each warned 'missing evidence'), and exit 1 with 'no line parsed in a.ndjson, b.ndjson'.\n- Two corrections to the acceptance's own prediction, both measured. A pre-1.1.0 pair does not yield 'no overlap_report at all': the key is present and empty, overlap_report: []. And the decisive failure is earlier than the read — v0.35.0's zero-parsed rule makes the merge exit 1 naming both files, so step 3 never gets to be read. The caveat states both facts rather than the predicted one.\n- No test was added and none is warranted: the change is prose, and a Contains/NotContains guard over prose cannot pin what a document claims — both polarities pass while a neighbouring paragraph re-adds the promise. What the tree gains is that the tag no longer ships a reference contradicting its own release, and a reader no longer follows a procedure that exits 1 on every round this repository has recorded. Corpus check, for the caveat's 'as a rule carries none': 90 of 4,059 recorded review rows carry a non-empty evidence, and all 90 are v1.1.0's own rounds 1-3 — the figure is deliberately not written into the document, only the mechanism that produces it.\nOut of scope: the same probe showed `--merge` writing its `-o` file even on the exit-1 refusal (merged.ndjson existed, merged_count 0) — that is §5 row 10, and REFERENCE.md:722's own required/attribution key list is reference-required-versus-attribution's task, not this one; neither was touched.",
      "gate_passed_at": "2026-09-08T08:32:57.398498Z",
      "commit_sha": "16468d11",
      "commit_shas": [
        "16468d11"
      ],
      "commit_files": [
        "skills/tp/REFERENCE.md"
      ]
    },
    {
      "id": "reference-required-versus-attribution",
      "title": "Separate required keys from attribution keys",
      "status": "done",
      "tags": [
        "docs"
      ],
      "depends_on": [
        "reference-recipe-scope"
      ],
      "estimate_minutes": 9,
      "acceptance": "The *Finding `class` and Report* output contract at `skills/tp/REFERENCE.md:722` is repaired on its own grounds rather than as a consequence of §2: it names which keys the gates require — `severity`, `finding`, `location` and `evidence` — and which are attribution and cluster keys no gate requires, namely `role` and `class`. That site is not falsified by §2 and the repair says so, the shipped sentence listing `role`, `location`, `class` and `severity` while omitting `finding`, so it disagrees with `missingFindingFields` in two directions at `HEAD`. The reference also states this release's meaning of whitespace, which §2's predicate has and no acceptance row can pin — the whitespace `strings.TrimSpace` recognises, and nothing outside that set — while `skills/tp/SKILL.md`'s four *What a spec is about* rules, in the tree since `4914215f`, are verified present unchanged rather than rewritten, which is §1 decision 4.",
      "source_sections": [
        "## 1. The decision",
        "## 3. What ships in the skill documents"
      ],
      "started_at": "2026-09-08T08:45:56.639409Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-08T08:52:10.687455Z",
      "closed_reason": "- The *Finding `class` and Report* contract at skills/tp/REFERENCE.md now separates the four keys both gates require — severity, finding, location, evidence — from role and class, which no gate requires; the required half is bound to what I ran, not to the code text: --record refused a one-row file missing evidence (exit 1, 'line 1: missing required field evidence'), --merge skipped a row missing evidence with the stderr warning and reported that input parsed 0 / skipped 1, and a row carrying neither role nor class recorded at exit 0 with one stderr advisory naming the absent role.\n- Repaired on its own grounds and the paragraph says so: the shipped sentence named role, location, class and severity, so it disagreed with the gates in two directions before evidence joined the set — it named two keys neither gate checked, and omitted finding, which --merge required at v1.0.1 (git show v1.0.1:internal/cli/review_merge.go, missingFindingFields over {location, severity, finding} at line 192). The doc anchors that claim to that ref and line rather than asserting it of all history.\n- The reference now states this release's meaning of whitespace, enumerated from unicode.IsSpace by a Go probe outside the repo (25 runes: tab, newline, vertical tab, form feed, carriage return, space, U+0085, U+00A0, U+1680, U+2000-U+200A, U+2028, U+2029, U+202F, U+205F, U+3000) and pinned by a boundary a reader can re-run: a one-row file whose evidence is a single U+00A0 makes --record exit 1 naming line 1, and the same file with U+200B records at exit 0 — measured with the freshly built binary in /tmp, not in the repo.\n- skills/tp/SKILL.md's four *What a spec is about (v1.1.0)* rules are verified present unchanged, not rewritten: the section from '#### What a spec is about' to '### Step 1: Init' hashes to sha256 023d7a6731716c6e86408b80e8c827e80a3b3434caf05f1a0396e31d69db0d59 at both 4914215f and HEAD, and holds exactly four bulleted rules. The one hunk SKILL.md gained since 4914215f is in Step 1.5, outside that range.\n- No test was added, deliberately. The subject is a paragraph of unbounded prose, where a Contains/NotContains guard is a local assertion whose complement is free — this cycle measured both polarities passing while the deleted promise was re-added in a neighbouring paragraph — and no bounded-artifact or read-back shape is available here. What the change buys is narrow and stated as such: the tag no longer ships a reference whose output contract contradicts the gates it documents, in either direction.\n- Quality gate green, four exit codes read separately: check-suite-state.sh 0 (0 '--- FAIL:' lines and 0 'build failed' lines), golangci-lint run 0, check-deadcode.sh 0, check-complexity.sh 0.\nOut of scope: skills/tp/SKILL.md's merge step still states §5 row 10's '-o writes only after the merge succeeds' as shipped fact; that belongs to review-record-unit-command. Separately, tp review --merge exits 1 with 'no line parsed in \u003cfile\u003e' when every line of one input was skipped, so an all-incomplete input fails the merge rather than being skipped quietly — observed, not a defect I was asked to judge.",
      "gate_passed_at": "2026-09-08T08:52:10.687455Z",
      "commit_sha": "fb729c17",
      "commit_shas": [
        "fb729c17"
      ],
      "commit_files": [
        "skills/tp/REFERENCE.md"
      ]
    },
    {
      "id": "review-record-unit-command",
      "title": "Rewrite the review-record unit command chain",
      "status": "done",
      "tags": [
        "docs"
      ],
      "depends_on": [
        "merge-refusal-leaves-output-untouched",
        "reference-required-versus-attribution"
      ],
      "estimate_minutes": 9,
      "acceptance": "The `review-record` unit `brief_command` at `skills/tp/REFERENCE.md:182` chains merge into record with `\u0026\u0026` rather than with `;` behind an `[ -f ]` guard, so a refused merge stops the chain and the merge's own stderr diagnosis is the unit's last output instead of tp's missing-findings-file error, which also brings the command into line with the rule stated at `SKILL.md:260` that it breaks today. That rule — `-o` writes only after the merge succeeds, so a failed merge leaves nothing and `--record` exits 3 on the missing file — is settled by running it against the tree this task lands on rather than by reading it, and the sentence is edited only where the run shows it false. At the tagged tree no superseded sentence survives in either document, neither `REFERENCE.md` statement nor the `;` chain behind its `[ -f ]` guard nor `SKILL.md`'s merge-step sentence, a task editing `SKILL.md` alone not satisfying this, and the closure names each site with the run that settled it.",
      "source_sections": [
        "## 3. What ships in the skill documents",
        "## 5. Tests"
      ],
      "started_at": "2026-09-08T09:56:05.228645Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-08T10:04:36.019327Z",
      "closed_reason": "- skills/tp/REFERENCE.md:182's review-record brief_command now chains merge into record with \u0026\u0026 behind the same [ -f ] guard: `[ -f X ] || tp review --merge ... -o X \u0026\u0026 tp review \u003cspec\u003e --record X`. Settled by running all three branches under /bin/sh with the freshly built binary in a temp dir outside the repo: guard miss + refused merge stops at exit 1 with the merge's own 'no line parsed in ...' diagnosis last and no round recorded, where the shipped ';' form ran on and ended at exit 3 on 'cannot read findings file'; guard miss + clean merge reached --record at exit 0 and wrote review-round-1.ndjson; guard hit (X already present, merge skipped) also reached --record at exit 0 and recorded the round, so the (A || B) \u0026\u0026 C precedence keeps the retry path that protects the dispositions review-resolve accumulates in merged.ndjson.\n- SKILL.md:272's merge-step rule was settled by running it on this tree, not read: a refused review merge created no file at an absent -o path (ls: no such file) and left a seeded one byte-identical (sha256 33b32dce... before and after), and --record on the missing file exited 3. Both outcome halves hold, so only the stale mechanism was edited — '(the write is the last step)' is replaced by what writeMergeOutput actually does, decline the write when an input parsed nothing, which is also why the promise was false before this release. The clean-round path was re-run to be sure the edit does not overclaim: two zero-byte inputs still merge at exit 0 and still write a zero-byte -o.\n- REFERENCE.md's exit-code statement at the old line 1084, 'The exit-1 path still emits the full payload and still writes -o', covered both phases and is now only half true, so it is split rather than made uniform: review declines the write, audit still writes it. Measured for the audit half on the same tree — tp audit --merge with a dropped input created a 51-byte results file at an absent -o path and replaced a seeded one (33b32dce... -\u003e f6d91df0...) while exiting 1 — so the sentence names tp audit --merge as the phase v1.1.0 §4 fences out and tells an audit chain to branch on the exit code, not on the file's presence.\n- No superseded sentence survives in either document: a repo-wide regex for 'merged.ndjson;|results.ndjson;|writes `-o`|still writes' and a literal search for 'the write is the last step' now return, in skills/, only the two rewritten phase-split sentences and SKILL.md's deliberately-quoted field-cycle failure (the '\u003e' redirect example, which must keep its ';').\n- No test was added. Both changes are prose, and a Contains/NotContains guard over prose cannot pin what a document claims: two rounds of this cycle measured both polarities passing while the promise was re-added in a neighbouring paragraph. The claims above are pinned by the runs, not by a guard.\n- Quality gate green, four exit codes read separately: ./scripts/check-suite-state.sh 0, golangci-lint run 0 (0 issues), ./scripts/check-deadcode.sh 0, ./scripts/check-complexity.sh 0; 0 lines matching '^--- FAIL: ' and 0 packages printing 'build failed'.\nOut of scope: the emitted brief_command still uses ';'. engine.recordBriefCommand (internal/engine/briefcommand.go:71) renders ONE template for both record kinds, so switching it to \u0026\u0026 would change the audit brief too, which §4's 'No change to the audit phase' forbids and which §5 gives no test row; the task is tagged docs and its acceptance names document sites. Rather than let REFERENCE.md's brief_command column silently diverge from tp's output, the paragraph under the table states the divergence, its cost (a tp run child gets tp's exit-3 missing-file error instead of the merge's exit-1 diagnosis; no round is recorded either way, because the declined -o write already prevents that) and where the single source is. Three tests pin the ';' string byte-for-byte: internal/engine/briefcommand_test.go:27,30, internal/engine/nextunits_test.go:421, internal/cli/resume_next_units_test.go:318.",
      "gate_passed_at": "2026-09-08T10:04:36.019327Z",
      "commit_sha": "746edf26",
      "commit_shas": [
        "746edf26",
        "6b78bd02"
      ],
      "commit_files": [
        "skills/tp/REFERENCE.md",
        "skills/tp/SKILL.md"
      ]
    },
    {
      "id": "release-note",
      "title": "Write the v1.1.0 release note",
      "status": "open",
      "tags": [
        "docs",
        "release"
      ],
      "depends_on": [
        "record-preresolved-boundary",
        "status-reads-pre-release-rounds",
        "merge-representative-evidence",
        "prompt-formats-name-required-keys",
        "review-record-unit-command",
        "skill-pre-review-build"
      ],
      "estimate_minutes": 6,
      "acceptance": "The release note is in English and states what a user gets at the tag: the four required keys enforced at both gates, `--merge` skipping and `--record` refusing with every offending line named, the `-o` path untouched by a refused merge, the `--verify` prompt's new output contract, and that rounds recorded before this release still load, clean and converge. It names the upgrade danger this release creates rather than inherits — a findings file written before v1.1.0 fails the merge gate on every row, and chaining a refused merge into `--record` with `;` records a zero-byte file as a clean round — and says the remedy is to re-emit the round's findings with `evidence`. §3's four `skills/tp/SKILL.md` rules reach a user at this tag rather than at their commit so the note names them, and with `.claude-plugin/plugin.json` already carrying `1.1.0` from `4914215f`, the note records that the bump precedes the tag rather than repeating it.",
      "source_sections": [
        "## 3. What ships in the skill documents"
      ],
      "started_at": null,
      "closed_at": null,
      "closed_reason": null,
      "gate_passed_at": null,
      "commit_sha": null
    },
    {
      "id": "skill-pre-review-build",
      "title": "Ship the pre-review build rule",
      "status": "done",
      "tags": [
        "docs",
        "skill"
      ],
      "depends_on": [],
      "estimate_minutes": 9,
      "acceptance": "`skills/tp/SKILL.md` gains one rule, as a single item at the end of Step 1.5 or the head of Step 2: a spec that changes a validation rule, a gate or a record schema has its minimal change built in a clone and the full suite run before it enters review, with packages that fail to build counted separately from `--- FAIL:` lines, and the count of tests that break is the release's real size — entering §1 as a task of its own rather than as another task's acceptance criterion. The rule points at the suite-build section of `spec/1.1.0-measurements.md` for the measurement instead of restating a figure, per this repository's anchored-figure rule, and it is written as guidance for the next cycle rather than as a report on this one. `tp lint skills/tp/SKILL.md` reports no more warnings after the edit than before it, and `go test ./internal/cli -run TestPluginVersion -count=1` stays green.",
      "source_sections": [
        "## 3. What ships in the skill documents"
      ],
      "started_at": "2026-09-08T09:09:44.039547Z",
      "duration_source": "claimed",
      "closed_at": "2026-09-08T09:15:05.530463Z",
      "closed_reason": "- skills/tp/SKILL.md gains one item at the head of Step 2: a spec that changes a validation rule, a gate or a record schema has its minimal change built in a git clone --no-hardlinks outside the repository and the full suite run there before the first counted round, with packages printing 'FAIL … [build failed]' counted separately from '^--- FAIL: ' lines because they emit no such line, and that total named as the release's real size belonging in §1 as a decision that decomposition gives the fixture migration a task of its own with the rule-changing task depending on it — written as guidance for a next cycle, naming no figure from this one.\n- The item cites the measurement instead of restating it, per the anchored-figure rule: the cited string was parsed out of the shipped SKILL.md with whitespace normalized and compared byte for byte against a heading of spec/1.1.0-measurements.md — it equals '### The release's size is the suite, and nobody had run it' exactly; a scan of the inserted item for standalone digits returns only the '1' of '§1', so no measured figure was copied.\n- tp lint skills/tp/SKILL.md reports 7 warnings after the edit and 7 before it (5 vague-language, 2 section-size, 0 errors, 33 info both times); the new item sits in Step 2, which was not among the two section-size warnings and is not now. The comparison was proved able to discriminate rather than assumed: a scratch copy of the edited file with the word 'some' inserted into the new paragraph lints at 8 warnings with a sixth vague-language row on the inserted line, so a lint-raising edit would have been caught.\n- go test ./internal/cli -run TestPluginVersion -count=1 is green (ok, 0.655s), and the whole project gate is green read step by step: check-suite-state.sh 0, golangci-lint run 0 (0 issues), check-deadcode.sh 0, check-complexity.sh 0. The suite log holds 0 lines matching '^--- FAIL: ' and 0 '[build failed]' packages, counted separately.\n- No test turned red and none was added: the change is prose in a skill document. The three test files that read SKILL.md (docs_contract_test.go, docs_flag_inventory_test.go, plugin_test.go) ran green inside step 1.\nOut of scope: skills/tp/SKILL.md Step 1.5 is 92 lines and already carries a section-size warning; it was left untouched.",
      "gate_passed_at": "2026-09-08T09:15:05.530463Z",
      "commit_sha": "4aa3e12d",
      "commit_shas": [
        "4aa3e12d"
      ],
      "commit_files": [
        "skills/tp/SKILL.md"
      ]
    }
  ]
}
