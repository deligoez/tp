# 1.1.0 — measurements and sources

Supplemental material for `spec/1.1.0.md`. The spec stands without this file; this file holds the
figures, the derivations and the survey sources the spec refers to by name. Nothing here is graded by
`tp ground`.

## The corpus carries no evidence key

Every recorded review row in this repository, under both `.tp-review` roots, counted at the tag of
`v1.0.1`:

```bash
bash -c 'python3 -c "
import json,glob
files=sorted(glob.glob(\"spec/.tp-review/*/review-round-*.ndjson\")+glob.glob(\"spec/backlog/.tp-review/*/review-round-*.ndjson\"))
rows=[json.loads(l) for f in files for l in open(f) if l.strip()]
print(\"files\",len(files),\"rows\",len(rows),\"with_evidence_key\",sum(1 for r in rows if \"evidence\" in r))
"'
```

Run at `4602f081` (the `v1.0.1` tag): 174 files, 3,969 rows, 0 with an `evidence` key. That zero is
why §1's third decision and §5 row 7 exist: the record-time rule must not be applied on load, or
every recorded cycle becomes unreadable.

The ground phase already records a tier per row; its vocabulary at the same commit, over both roots:

```bash
bash -c 'python3 -c "
import json,glob,collections
files=sorted(glob.glob(\"spec/.tp-review/*/ground-round-*.ndjson\")+glob.glob(\"spec/backlog/.tp-review/*/ground-round-*.ndjson\"))
rows=[json.loads(l) for f in files for l in open(f) if l.strip()]
print(collections.Counter(r.get(\"tier\") for r in rows).most_common())
"'
```

That is the vocabulary `spec/1.1.0.md` §2 takes whole, rather than a pair drawn from it.

## What the `v1.0.1` cycle measured

All of it is in `spec/1.0.1-measurements.md`; the sections this release rests on, by title:

- *What implementation did that five review rounds could not* — the table of corrections made by
  implementing tasks, each from a run, against five grading rounds that rewrote one section four
  times. This is the measurement behind the skill's rules on what a spec is about.
- *The cap stop, and why the release goes to implementation unconverged* — review findings rose from
  round 1 to round 2 after the repair replaced prose with a table; the table was wrong too. Behind
  the cut disposition: the findings that rose were about behaviour no one had built.
- *The audit arc, and the round that subtracted* — three audit repairs replaced a claim with a claim
  and each replacement was refuted by one more command; the first subtracting repair ended it. Behind
  the SKILL.md rule *a repair subtracts before it adds*.
- *What review round 1 measured, and the two rows it killed by running them* — every finding that
  changed a decision in this cycle came with a run or a read; no finding that rested on a reading of
  prose alone survived the next round. Behind the evidence requirement at record.

The two field cycles named in the spec are the ones `spec/backlog/README.md` cites under *What decides
the order*, with the same counting rules.

## The survey

Run on 2026-09-07 with a fixed budget of searches; each source is listed with the concrete rule taken
from it. Nothing below is quoted in the spec.

**Spec-driven development tools.**

- GitHub spec-kit, `spec-driven.md` and the spec template — the reviewed spec must "avoid HOW to
  implement (no tech stack, APIs, code structure)"; code-level decisions live in a separate
  `implementation-details/` artifact that is not the reviewed document; ambiguities are marked inline
  and the gate is that none remain, not a round count.
  https://github.com/github/spec-kit/blob/main/spec-driven.md
- AWS Kiro feature specs and best practices — three files (`requirements.md` in EARS, `design.md`,
  `tasks.md`), one human approval after each, and a task-level *Sync Files* step after
  implementation; the design file does describe structure before code exists, which the field report
  below names as the over-specification that made specs rigid.
  https://kiro.dev/docs/specs/feature-specs/ · https://kiro.dev/docs/specs/best-practices/ ·
  https://dev.to/aws-builders/what-i-learned-using-specification-driven-development-with-kiro-pdj
- Tessl — one spec per code file, an API section in code, a `[@test]` link per requirement; the only
  surveyed tool that keeps spec and code in sync, and it does so by linking tests, not by prose.
  https://docs.tessl.io/use/spec-driven-development-with-tessl
- Martin Fowler's team on the three tools — observed over-specification, duplication with existing
  code, and authors "frequently confused when to stay on the functional level".
  https://www.martinfowler.com/articles/exploring-gen-ai/sdd-3-tools.html

**Executable specifications.**

- Cucumber, *Writing better Gherkin* — "describe the intended behaviour of the system, not the
  implementation"; the per-sentence test *will this wording need to change if the implementation
  does?* https://cucumber.io/docs/bdd/better-gherkin/
- Adzic, *Specification by Example* (InfoQ excerpt) — "automating validation without changing
  specifications"; "watch out for descriptions of how the system should work".
  https://www.infoq.com/articles/specification-by-example-book
- Python doctest and rustdoc documentation tests — a document's examples are executed to keep it
  "up to date", not reviewed. https://docs.python.org/3/library/doctest.html ·
  https://doc.rust-lang.org/rustdoc/write-documentation/documentation-tests.html

**Design-document and RFC cultures.**

- Rust RFC template and the libs-changes guide — unresolved questions are split into those resolved
  before merge and those "expected to resolve through the implementation"; "nor must an
  implementation PR perfectly match the RFC text"; RFCs favour immutability.
  https://rust-lang.github.io/rfcs/libs_changes.html
- PEP 1 — an accepted PEP "is considered a historical document rather than a living specification";
  expected behaviour is maintained elsewhere. https://peps.python.org/pep-0001/
- Oxide RFD 1 — states from prediscussion to committed; a days-long discussion window, author decides
  when to merge. https://rfd.shared.oxide.computer/rfd/0001
- Google design docs (Ubl) — context and scope, goals and non-goals, the design with trade-offs,
  alternatives; not "an implementation manual".
  https://www.industrialempathy.com/posts/design-docs-at-google/

**Requirements engineering.**

- INCOSE Guide to Writing Requirements v4 (rules summarised at reqi.io) — R31 *solution free*; every
  requirement verifiable, with a stated method. https://reqi.io/articles/incose-requirements-quality-42-rule-guide
- ISO/IEC/IEEE 29148:2018 — the individual-requirement characteristics include *appropriate*
  ("avoids implementation details") and *verifiable*; the standard's own definition of *requirements
  verification* is a check of form, distinct from verifying the system.
  https://www.reqview.com/doc/iso-iec-ieee-29148-templates/

**Review loops of language-model agents.**

- *Adversarial Review* (arXiv 2608.18167) — a reviewer–critic loop underperformed a single reviewer
  until each finding had to cite code that confirms or refutes it; "agents can optimize for agreement
  rather than correctness". https://arxiv.org/html/2608.18167
- *Refute-or-Promote* (arXiv 2604.19049) and *Benchmarking Agentic Review Systems* (arXiv
  2606.19749) — refute-first staging for precision; more peer agents can yield diminishing or
  negative returns. https://arxiv.org/html/2604.19049 · https://arxiv.org/html/2606.19749v1

Nothing was found on multi-round review of *prose* specifications by agents converging or not; the
direct evidence for that is this repository's own recorded rounds.

## What the survey did not change

The backlog's order. Each surveyed practice that maps to a tp mechanism already has a backlog file —
the panel record, the checklist cap, the emit-time hash, the forced brief — and the survey supports
their subjects without changing their priority. The one practice with no file is the subject of this
release.

## Pre-round verification, before ground round 1

Run at `0bc4f3a9`, 2026-09-07T22:03:12Z, by a second party rather than by the author — the step that
caught two broken commands in `v1.0.1`'s cycle before a grading token was spent.

- **`tp lint spec/1.1.0.md`** — 0 errors, 0 warnings, 8 informational `structured-elements`;
  `floor_size` 57, `cut` 30, `review_panel` all four reviewers. The author's figures reproduce.
- **Fenced blocks**: none in `spec/1.1.0.md`; two in this file, both already invoking `bash -c` and
  both already globbing `spec/.tp-review/` **and** `spec/backlog/.tp-review/`. Those are `v1.0.1`'s
  two rules — name the interpreter, read both round directories — applied at authoring time rather
  than repaired after a round.
- **Both blocks run and print.** The first returns `files 174 rows 3969 with_evidence_key 0`,
  reproducing the author's count exactly.

The second block's output is worth keeping here rather than only running, because §2's design rests
on it — the tier vocabulary the corpus actually uses, and which §2 takes whole:

| `tier` | rows |
|---|---|
| `read` | 975 |
| `run` | 746 |
| `query` | 462 |
| `probe` | 234 |
| *(absent)* | 231 |
| `break-and-control` | 123 |
| `red-green` | 19 |

## After ground round 1's repair

Measured on the tree at `5058fc99` plus the repair itself, working tree otherwise clean.

- **Floor.** `tp ground spec/1.1.0.md --units` returns **35** unit lines, and `tp lint` reports
  `floor_size` 35 and `cut` 15 — against 57 and 30 before the repair. `--units` writes no state: the
  digest of every file under `spec/.tp-review/1.1.0/` is identical before and after, checked in an
  `rsync` copy rather than in the repository.
- **The manifest leads the newest tag, and that is the documented window.**
  `.claude-plugin/plugin.json` says `1.1.0` while the newest tag is `v1.0.1`; commit `4914215f` made
  the bump. `CLAUDE.md`'s pre-release item 4b requires the order bump → commit → tag, so a manifest
  ahead of the tag is legal inside that window. `hooks/session-start.sh` reads the same field as the
  **minimum tp version**, which is what makes the window matter: it is safe only while the bump is
  unpushed. It is: `4914215f` is still listed by `git log origin/main..HEAD`, so the bump has never
  reached the remote and no user out there resolves a floor above the newest released binary. That
  reference is the claim, not a commit count — the count moved with this repair's own commits. The
  window closes at the tag, which has to ship a binary at or above `1.1.0`.

## After ground round 2's repair

Measured on the tree at `125670cc` plus the repair itself, with the dogfood binary built from that
tree. The probes below run in a `git clone --no-hardlinks` outside the repository, because
`tp review` writes a round snapshot even without `--record`.

### `--record` accepts `{}` at exit 0 — the defect §2 closes

```bash
bash -c 'rm -rf /tmp/tp-probe && git clone --no-hardlinks -q . /tmp/tp-probe && cd /tmp/tp-probe
mkdir -p /tmp/tp-dev && go build -o /tmp/tp-dev/tp ./cmd/tp
printf "{}\n" > empty-row.ndjson
/tmp/tp-dev/tp review spec/1.1.0.md >/dev/null 2>&1
/tmp/tp-dev/tp review spec/1.1.0.md --record empty-row.ndjson >rec.json 2>rec.err; echo "record exit=$?"
python3 -c "import json;d=json.load(open(\"rec.json\"));print(\"findings\",d[\"findings\"],\"clean\",d[\"clean\"])"
echo "round file holds: $(cat spec/.tp-review/1.1.0/review-round-1.ndjson)"
/tmp/tp-dev/tp review --merge empty-row.ndjson >/dev/null 2>merge.err; echo "merge exit=$?"
cat merge.err'
```

Prints `record exit=0`, `findings 1 clean False`, `round file holds: {}` and `merge exit=1`. So a row
with no keys at all records as a finding and advances `review_rounds`; `--record`'s only complaint is
a stderr warning about the missing `role` field. The same file at `--merge` is skipped with
`warning: skipping incomplete line (missing location, severity, finding)`.

That asymmetry is the subject of §1's second decision: `missingFindingFields`
(`internal/cli/review_merge.go:190-198`) requires `location`, `severity` and `finding`, while
`parseRecordRows` (`internal/cli/review_record.go:249-282`) validates only JSON well-formedness and
the pre-resolved rules.

### The parity does not reach the pre-resolved rules

A row legal on all four required keys and carrying `resolved: {"status": "fixed", "evidence": "done"}`:
`--merge` emits it at exit **0**, `--record` exits **1** with `row arrives pre-resolved fixed — a fix
means the spec changed`. So *a row both accept is accepted by both* is false for reasons that have
nothing to do with required fields — `parseRecordRows` aborts on a pre-resolved `fixed` row and
`--merge` has no counterpart rule. That is why §2 scopes the parity to the field set and §5 row 6
pins the boundary rather than leaving it to be discovered by the implementing task.

### The tier vocabulary does not cross the phase boundary

The counts §1's *Alternatives considered* refers to, over both `.tp-review` roots at `125670cc`:

```bash
bash -c 'python3 -c "
import json,glob,collections
for phase in [\"ground\",\"review\"]:
    c=collections.Counter(); n=0
    for root in [\"spec/.tp-review/\",\"spec/backlog/.tp-review/\"]:
        for f in glob.glob(root+\"*/\"+phase+\"-round-*.ndjson\"):
            for l in open(f):
                if l.strip():
                    n+=1
                    for k in json.loads(l): c[k]+=1
    print(phase,\"rows\",n,\"kind\",c[\"kind\"],\"tier\",c[\"tier\"],\"evidence\",c[\"evidence\"])
"'
```

| phase | rows | `kind` | `tier` | `evidence` |
|---|---|---|---|---|
| ground | 2,889 | 2,656 | 2,656 | 2,656 |
| review | 3,969 | 0 | 0 | 0 |

The three ground keys arrive together, and none of them reaches a review row. A `tier` is read
against the `kind` cell beside it; a review finding has no such cell, which is the design reason the
enum was cut rather than reworded a third time.

### Two failure modes this round recorded, for the files that own them

- **A repair replaced a true anchor with a stronger and false one.** Round 1 wrote that the skill
  rules shipped in `4914215f`, *before this spec's first grading round*; the repair that closed the
  finding rewrote it as *before this file was written*, which git refutes.
  `git merge-base --is-ancestor 81cb5749 4914215f` exits 0 and
  `git log --diff-filter=A -- spec/1.1.0.md` names `81cb5749` as the commit that added the file, so
  the skill commit is a descendant, one second later. The round-1 form is true:
  `git merge-base --is-ancestor 4914215f 5058fc99` exits 0, and `5058fc99` is the commit that
  recorded round 1 — this repair restores it. The class is the one `CLAUDE.md` records under *a
  repair that REPLACES a claim is unverified by construction*, in its sharpest form: the replacement
  was strictly stronger than the claim it closed, and nobody ran it.
- **A carried PASS row brought forward a note about text that had been cut.** u35 was graded PASS in
  round 1 with a note describing a disposition this cycle later removed, and carried into round 2
  unchanged — so rounds 1 and 2 both hold the stale note in their recorded files. This repair planned
  to leave it for round 3 to re-ask, and that plan could not run: unit ids are re-assigned at every
  emission while carries key on `text_sha`, so round 3 asks the sentence under neither id. What round
  3 did instead is recorded under *After ground round 3's repair*. This is a second instance of the
  pattern `spec/backlog/02b-what-a-rounds-rows-say.md` owns — a row's verdict and the note beside it
  can disagree, and only the verdict is counted.

## After ground round 3's repair

Measured on the tree at `1ca54e25`, working tree otherwise clean. Every command that runs tp does so
against a binary built from a `git clone --no-hardlinks` of that tree outside the repository, because
`tp review` writes a round snapshot even without `--record`. The corpus counts are read-only and were
taken in the same clone.

### The enum was cut for a reason that had nothing to do with grading it

Until this repair §1 carried *rejected after two grading rounds refuted two different enums*. The
record does not support it, in either half:

- Every round-2 row whose subject is the tier-vocabulary enum — `u24`, `u25`, `u26`, `u28`, `u39` —
  is **PASS**. What round 2 recorded against the enum is a reservation inside a PASS note, which is
  the shape `spec/backlog/02b-what-a-rounds-rows-say.md` owns: a row's verdict and the note beside it
  can disagree, and only the verdict is counted.
- No non-PASS row in round 2 has the enum as its subject. Round 2's four FAILs are `u3` (the class
  claim), `u29` (the skill section) and `u9`/`u11` (both about `--merge`); its twelve PARTIALs sit on
  §5's rows, the skill's reach, the Non-Goals and the survey attribution.
- `git log -1 --format=%s 7e3fd758` returns *docs(spec): 1.1.0 §2 cuts the evidence enum for one
  required-field set both gates enforce*, and `git show --stat` gives one file, 13 insertions, 10
  deletions. The commit that cut the enum names the required-field-set parity — the subject of round
  2's `--merge` findings — as its reason, not any refutation of the enum.

Round 3's `u25` reached the same place from the other side: *one round refuted one enum*.

The deleted sentence came from the design authority's brief for the round-2 repair, in its own name,
and the repair wrote it without reading the record. The decision is untouched: the enum stays cut on
its design reason — a review finding has no `kind` cell to read a tier against — which round 3 graded
PASS twice, at `u26` and `u27`.

### Two contradictory predicates for "non-empty", in the two functions this release edits

`missingFindingFields` (`internal/cli/review_merge.go:190-198`) tests `s == ""`; `parseRecordRows`
(`internal/cli/review_record.go:271`) tests `strings.TrimSpace(evidence) == ""`. Four one-row
fixtures through the `HEAD` binary, `--merge` then `--record`:

| the row | `--merge` | `--record` |
|---|---|---|
| `"location": " "` | exit 0, kept — 1 unique finding, no warning | exit 0 |
| `"location": ""` | exit 1, `skipping incomplete line (missing location)` | exit 0 |
| `"resolved": {"status":"wontfix","evidence":" "}` | exit 0, kept | exit 1, `pre-resolved wontfix row requires non-empty resolved.evidence` |
| top-level `"evidence": " "` | exit 0, kept | exit 0 |

The merge gate treats a whitespace-only value as **present**; the record gate treats it as **empty**.

**The brief that ordered this repair said `evidence: " "` is "kept by `--merge` and refused by
`--record`" at `HEAD`. The last row refutes that**: at `HEAD` no gate looks at a top-level `evidence`
at all, so both accept it. The claim holds of `resolved.evidence`, a different key under a different
rule. What is true, and is what §2 now settles, is that the two predicates sit side by side in the
two functions this release edits, so an implementation that adds `evidence` to each gate using the
predicate that gate already carries reproduces the divergence without writing one line of new logic.

That mutant was built and run rather than reasoned about. Fixture: two rows in one file, the first
carrying `"evidence": " "` and legal on the other three keys, the second legal on all four.

| binary | `--merge` | `--record` |
|---|---|---|
| mutant — merge `== ""`, record its own `TrimSpace` check | exit 0, **2 unique findings, no warning** | exit 1, `line 1: evidence is missing or empty` |
| one predicate — both gates through `missingFindingFields`, trimming | exit 0, **1 unique finding**, `skipping incomplete line (missing evidence)` | exit 1, `line 1: missing or empty evidence` |

So §5 row 9's discriminating half is the merge side; both gates refuse the row at record either way.

§2's fence on the far side of that predicate was measured too, with a six-case `strings.TrimSpace`
probe: `" "`, `"\t\n"`, U+00A0 no-break space and U+2002 en space all trim to empty; **U+200B
zero-width space and U+2060 word joiner do not**. So the zero-width space §2 names as *not judged* is
a real case and not a hypothetical one, and the fence is where the sentence says it is.

### `--merge` exits 1 when an input parses no row — §5 row 5's exit code

Row 5 picks the offending row *alone in its own input file* so the skip is observable, and that choice
carries an exit code with it. `droppedInputs` names any input with at least one content line and no
parsed row, and `finishMerge` exits `ExitValidation`, which is 1
(`internal/cli/merge_inputs.go:32-60`, `internal/cli/exitcodes.go:6`). Run at `HEAD` on exactly row
5's fixture — one file holding a single row that omits `location`, plus a second file holding one
legal row — `--merge` exits **1** and prints `no line parsed in …: every content line was skipped, so
that input contributed nothing to the merge`. The second file does not lift the exit; it only keeps
the merged payload non-empty. Row 5's SHALL now says so, because a test written from the row alone
would otherwise assert a clean merge and go red for a reason the row never names. The same run with
the offending row moved *beside* the legal row in one file exits 0 — which is why §5 row 9 uses that
second shape.

### The whole corpus fails the parity rule at `--merge`

Counting rule, restated so it is re-run rather than copied: both globs
(`spec/.tp-review/*/*.ndjson` and `spec/backlog/.tp-review/*/*.ndjson`); basenames matching
`^review-round-\d+\.ndjson$` for the canonical set and `^review-r\d+-[a-z-]+\.ndjson$` for the
per-role set; every non-blank line must parse to a JSON object; a key counts as present when it
exists and is a non-empty string; rows carrying `resolved` still count.

| set | files | non-empty | rows | missing `severity` / `finding` / `location` | missing `evidence` |
|---|---|---|---|---|---|
| canonical `review-round-N` | 174 | 163 | 3,969 | 0 / 0 / 0 | 3,969 |
| per-role `review-rN-<role>` | 59 | 59 | 669 | 0 / 0 / 0 | 669 |

All 174 canonical files sit under `spec/.tp-review/`; `spec/backlog/.tp-review/` holds **zero** review
rounds. That was counted per root rather than assumed, and it is why one glob happens to agree here.
The 3,969 is the cost §1's third decision exempts: under the four-field rule every committed review
row would be refused at record, and every one of them for `evidence` alone.

The exit codes were then measured rather than computed, by running `tp review --merge` once per file
under two binaries:

| binary | canonical non-empty (163) | per-role (59) |
|---|---|---|
| `HEAD` | 163 × exit 0 | 59 × exit 0 |
| `evidence` added to `missingFindingFields` | 163 × exit 1 | 59 × exit 1 |

That pair of rows is what replaced §2's *nothing else about `--merge` changes*. The quantifier was
true of the code and false of the observable behaviour.

### The emitted prompt names `evidence` three times and lists finding keys three times

`tp review spec/1.1.0.md --role implementer` in the clone, counted on the emitted
`prompts[0].prompt` (4,269 characters): `evidence` occurs **3** times — twice as an echo of this
spec's own `## 2. The evidence field on a review finding` heading, once in the *Focus your review on*
outline and once in the structured-table checklist, and a third time in *report it with its evidence
instead of making the edit*. **None of the three is in a key list.** So `Contains(prompt, "evidence")`
is green against `HEAD` with nothing implemented, which is why §5 row 8 now names the key list and
rules the substring check out.

The same prompt enumerates finding keys at three sites, and they already disagree with each other:

| site | keys it names |
|---|---|
| `findingFormat`'s JSON example (`internal/cli/review.go:804`) | `severity`, `category`, `location`, `finding`, `suggestion` |
| the optional-`class` sentence beside it (`review.go:806`) | `class` |
| `outputContractInstruction` (`review.go:1128-1152`) | `role`, `location`, `class`, `severity` |

`role` appears only in the third; `finding`, `category` and `suggestion` only in the first. Making the
three derive from one source is acceptance for the task that adds `evidence` to the emission — it is
not a claim in the spec body, because the spec cannot assert the shape of code that does not exist
yet.

### A carry is overridden by writing a fresh payload row for the same unit

Round 3's grader found the carried `u35` holding a note about text this cycle had already cut, and
filed a fresh payload row for that sentence instead of letting the carry stand; `tp ground --record`
accepted it at exit 0. In the recorded round the sentence is `u46`, with no `carried_from` and round
2's `u35` `text_sha` (`29ee5c28eeb4`); round 3 records 10 carried rows rather than the 11 it would
have carried. No flag was needed and nothing further is to be filed.

The same measurement fixes the shape of any plan written for a later round: **a plan expressed in unit
ids cannot survive an emission.** Ids are re-assigned each time the floor is emitted, while carries
are keyed on `text_sha` — between rounds 2 and 3, 11 sentences share a `text_sha` and **9 of them
changed unit id** (`u33`→`u44`, `u37`→`u48`, `u8`→`u10`, `u21`→`u31`, `u38`→`u49`, `u17`→`u24`, …).
Name the anchor and the sentence, never `u35`.

## After review round 1's repair

Measured on the tree at `c407bb7e` plus the repair itself. Every command that runs tp does so against
a binary built from a `git clone --no-hardlinks` of that tree outside the repository, because
`tp review` writes a round snapshot even without `--record` and this spec's recorded round 1 must not
move. Where a figure disagrees with the brief that ordered the repair, the disagreement is stated
rather than smoothed over.

### The release's size is the suite, and nobody had run it

Until this round every probe in this cycle read corpus files or emitted prompt text; none built the
change. Built here: `evidence` appended to `missingFindingFields` (`internal/cli/review_merge.go`)
and a `strings.TrimSpace` check on the top-level `evidence` in `parseRecordRows`
(`internal/cli/review_record.go`) — nothing else, and no test touched.

Counting rule, stated before the count: a **failing test function** is one line matching `^--- FAIL: `
in the output of `go test ./internal/cli/ -count=1`, top level only, since `go test` indents a
failing subtest.

| tree | result | failing test functions |
|---|---|---|
| `HEAD` | `ok … 14.869s` | 0 |
| `evidence` in `missingFindingFields` only | `FAIL … 15.482s` | **18** |
| both gates | `FAIL … 15.937s` | **68** |

The merge-only figure reproduces the brief's exactly. The baseline wall clock does not — the brief
reported ~15.7s against this machine's 14.9s — and neither wall clock is load-bearing.

Counting rule for fixtures: a **review-finding fixture row** is a line of Go source in a `_test.go`
file holding a single-line JSON object literal that names both `"severity"` and `"finding"` and
neither `"item_id"` nor `"status"`, the two keys that would make it an audit row.

| scope | rows | files |
|---|---|---|
| `internal/cli` | 147 | 28 |
| repository-wide | **169** | **30** |

No fixture is built as a multi-line Go struct — `grep -rn 'reviewFinding{' --include='*_test.go'`
returns nothing — so the single-line rule misses none. **The brief's 164 rows across 33 files did not
reproduce under any of the three scopes tried**; the brief stated no counting rule, so the two figures
are not comparable rather than one of them being wrong. §1's sixth decision rests on the 68 rather
than on the 169: the 68 is what an implementing unit meets when it runs the gate.

### Row 8's second SHALL was unsatisfiable inside §4's fence

The cut half required every key-naming site in the prompt to name the same set. Three facts, each run
rather than read:

- `outputContractInstruction` is called from `review.go`, `review_regression.go` **and**
  `audit_roles.go`. Emitting `tp audit` finds the block in all four auditor prompts —
  `ax-contract`, `go-safety`, `maintainability-conventions`, `spec-coverage`. Equalising the sites
  therefore edits the audit prompt, which §4 forbids.
- `findingFormat`'s JSON example names `category`, and `tp review --report` ships a `by_category` key
  (`internal/cli/review_report.go`) fed from it. Dropping `category` to equalise the sites would empty
  a shipped payload key.
- The same equalisation forces `class`, labelled *Optional* in the sentence beside the example, to
  become mandatory.

The surviving SHALL's fixture is the three review emission paths, whose key lists were counted rather
than assumed:

| path | keys in the output-format object | names `evidence` |
|---|---|---|
| `tp review <spec>`, each panel role | 5 — `severity`, `category`, `location`, `finding`, `suggestion` | no |
| `tp review <spec> --role implementer` | 5, byte-identical to the panel entry | no |
| `tp review <spec> --perspective code-audit` | **11** — `role`, `id`, `file`, `line`, `pattern`, `current_behavior`, `spec_coverage`, `finding`, `suggestion`, `severity`, `category` | no |

That eleven-key `code-auditor` format is row 8's mutant standing at `HEAD`. Cross-site key agreement
is recorded under *Undecided* in `spec/undecided.md` with this measurement.

### `evidence` is write-only, and the row that pins it

`reviewFinding` (`internal/cli/review.go`) carries `severity`, `category`, `class`, `location`,
`finding`, `suggestion` and `resolved`, and no `evidence`. `findingFromRow` copies each of those out
of a recorded row; a top-level `evidence` has no destination, so the previous-round injection drops
it. Run: one row recorded with `"evidence": "QUUXFRAB7 …"` and `"finding": "ZORBLAX …"`, then the
next round emitted.

| binary | `QUUXFRAB7` (evidence) | `ZORBLAX` (finding) |
|---|---|---|
| `HEAD` | **0** | 3 |
| the same tree plus an `Evidence` field carried through `findingFromRow` and rendered | **3** | 3 |

The round file on disk holds the evidence text either way: it is written and never read back. The
second line is the control — without it the new §5 row would be an assertion no implementation could
turn green, which is the table's own disqualifier.

### Row 5: `location` does not discriminate, `evidence` does

Row 5's WHEN admitted any of the four keys, and its SHALL is false for three of them. Two binaries,
two fixtures, the offending row alone in its file beside a second legal file:

| the offending row omits | binary | `--merge` | `--record` |
|---|---|---|---|
| `location` | both gates | 1 | **0** |
| `location` | `--record` only, which is row 5's mutant | 1 | **0** |
| `evidence` | both gates | 1 | 1 |
| `evidence` | `--record` only, which is row 5's mutant | **0** | 1 |

With `location` the mutant is indistinguishable from the shipped rule, so the row cannot be red. The
brief that ordered this repair said the mutant survives there because both halves of the SHALL are
green under it; **that is wrong** — `--record` exits 0, so the SHALL's second half is false under the
mutant *and* under the correct implementation. The conclusion holds and the stated reason does not,
which is why the row now names `evidence` in its WHEN as well as in its fixture.

### Row 3's stated mutant cannot make row 3 red

The mutant was *the check also rejects an `evidence` under 20 characters*. Whether it is red depends
entirely on text the fixture cell never named:

| fixture, three legal rows | `evidence` lengths | both gates | plus the length mutant |
|---|---|---|---|
| long | 70 / 65 / 32 | exit 0, findings 3 | **exit 0, findings 3** |
| short | 11 / 7 / 13 | exit 0, findings 3 | exit 1 |

The first line reproduces the tester's measurement: against that fixture the mutant survives. Row 3 is
kept rather than deleted, because it is the table's only row asserting that a legal file *records* —
rows 1, 2, 5, 6 and 9 each assert a refusal, and an implementation that refused everything would pass
them all. Its mutant is replaced by one that was built and watched go red: `--record` tests
`resolved.evidence`, the key `parseRecordRows` already reads a few lines further down, in place of the
top-level `evidence`. That mutant exits 1 on **both** fixtures while the implementation exits 0 on
both, so the row's redness no longer turns on a value the fixture cell does not state.

### This cycle's own round 1, under this release's own rule

Round 1 is the first review round in this repository recorded with evidence on every row. Derived from
`spec/.tp-review/1.1.0/review-round-1.ndjson`:

| quantity | value |
|---|---|
| merged rows recorded | 33 |
| rows carrying a non-empty `evidence` | **33 / 33** |
| rows before `--merge`, as the sum of `found_by` | **34** |
| clusters larger than one | one, of size 2 — `implementer` + `tester`, §5, `unsatisfiable-shall` |
| by representative role | `implementer` 10, `architect` 8, `tester` 8, `ax-economist` 7 |
| severities | 9 high, 20 medium, 4 low |

**The brief's figures for this measurement did not reproduce, and the difference is now settled by
construction.** The brief reported 35 pre-merge rows, a tester total of 10 and one cluster of three;
the recorded artifact gives 34, 9 and one cluster of two. Both are right, and they count different
things: **`found_by` counts distinct roles, not rows.** Measured in a clone — two rows from the *same*
role sharing `(location, class)` merge to one row carrying `found_by: 1`, and adding a third row from
a different role takes that same representative to `found_by: 2`. So `sum(found_by)` is not the input
row count whenever a role duplicates itself, and the round's 35 parsed rows reconcile with 34 as one
cross-role cluster (−1) plus one within-role cluster (−1). The hypothesis this paragraph carried
before — that one row was skipped at `--merge` for a missing field — is refuted: the merge envelope
reported `skipped: 0` on all four inputs. The table above is what the recorded round supports; the
brief's 35 is what the four role files held.

### The floor, and the one warning this repair accepts

| | `floor_size` | `cut` | errors | warnings |
|---|---|---|---|---|
| before the repair | 47 | 21 | 0 | 0 |
| after | **54** | **23** | 0 | **1** |

`tp ground spec/1.1.0.md --units | grep -c '^u'` returns 54, agreeing with `floor_size`; the emitted
index runs to `u77`, and 77 − 54 is the `cut` of 23. Both runs used one binary in one directory with
this repository's own role corpus, because a lint run outside it falls back to the embedded corpus and
reports a three-role panel instead of this repository's four — which is a way to compare two figures
that are not comparable.

**No sentence left the floor.** Diffing the two `--units` listings, three units are absent afterwards
— §5's rows 3, 5 and 8 — and each reappears under its new text, because the repair rewrote them. Ten
units are new: those three, row 10, and six sentences from §1's decisions 5 and 6 and §3's second
paragraph.

Two sentences this repair wrote are dropped by the arms rather than floored: §5's new preamble
sentence and §3's closing sentence. The first is a property of the region rather than of the wording —
§5's two *existing* preamble sentences are dropped at `HEAD` too, so no phrasing of a preamble
sentence would be graded there.

**The one warning is accepted, not overlooked.** `section-size` fires above 50 lines
(`internal/cli/lint.go` passes 50) and §1 was exactly 50 before this repair, so the two decisions the
round asked for put it at 60 and the warning is the unavoidable price of the addition. The
alternative — splitting §1 — renumbers every section and would falsify the `§1`/`§2`/`§5` references
in this file and in the spec, which is the failure `CLAUDE.md` records as 47 stale citations across
two rename sweeps.

### The lesson, and where it belongs

**A spec that changes a validation rule has its minimal change built in a clone and the suite run
before it enters review; the number of broken tests is the release's size.** Nobody did that here.
Every repair probe in this cycle measured corpus files or prompt text, and the one unit that built the
code found the release an order of magnitude larger than its test table — ten rows against 68 failing
test functions.

That rule belongs in `skills/tp/SKILL.md` rather than in this spec: it governs every future cycle and
not this release's behaviour. It is this release's own skill task, beside §3's.

## After review round 2's repair

Measured on the tree at `2833f81e` plus the repair itself. Anything that runs `tp review` ran against
a binary built from a `git clone --no-hardlinks` of that tree outside the repository, because
`tp review` writes a round snapshot even without `--record` and rounds 1 and 2 must not move.
`tp lint` was run in the repository, guarded by a before/after digest of `spec/.tp-review/` and
`.tp/rounds/`, which was unchanged.

This repair was pre-registered as **subtraction with two named additions**, because the round-1
repair closed a defect in one place by writing a sentence that reproduced it in another and only its
two subtracting repairs survived round 2. What follows separates the two.

Decision numbers moved: §1's decision 6 is decision 5 after this round's cut, so the *After review
round 1's repair* sections above naming a *sixth* decision mean the same clause.

### The corpus does not fail uniformly, and §2 said it did

§2 closed with *"every review round file and every per-role findings file this repository has
recorded exits 1 at `--merge`, because no row in any of them carries an `evidence` key"*, and
decision 3 carried the same shape — *"under it every recorded review row would be refused"*. Both are
false on the tree that shipped them, and the second half of each is what makes them false.

Counting rule, stated before the count: a **canonical round file** matches
`spec/.tp-review/*/<phase>-round-<N>.ndjson` or the same path under `spec/backlog/`, with `<phase>`
one of `review`, `audit`, `ground`; a **row** is a JSON object on a non-blank line of one.

```
python3 -c 'import json,glob,re
pat=re.compile(r"/review-round-\d+\.ndjson$")
fs=[f for g in ("spec/.tp-review/*/*.ndjson","spec/backlog/.tp-review/*/*.ndjson") for f in glob.glob(g) if pat.search(f)]
rs=[[json.loads(l) for l in open(f) if l.strip()] for f in fs]
print(len(fs), sum(len(r) for r in rs),
      sum(1 for r in rs for x in r if isinstance(x.get("evidence"),str) and x["evidence"].strip()),
      sum(1 for r in rs if not r))'
```

| | |
|---|---|
| review round files, both globs | 176 |
| rows in them | 4,030 |
| rows carrying a non-empty top-level `evidence` | **61** — this cycle's own round 1 (33/33) and round 2 (28/28) |
| review round files holding zero rows | **11** |

So *no row carries an `evidence` key* was falsified by the two rounds this very spec had already
recorded, and *every review round file exits 1* is falsified by eleven empty ones, which
`--merge` exits **0** on by its own documented all-empty rule. Neither sentence was a measurement
that had gone stale: each was a quantifier over a set nobody enumerated, which is the class round 2
reported three times. Both are deleted. Decision 2's *"as it already does for the first three"* went
with them, for the reason under *`--merge` keeps a whitespace-only key* below: it asserts the release
changes nothing for three keys that §2's own shared predicate does change.

### The review emission sites, derived

**§5 row 8's fixture cites this list.** It is derived rather than restated, because the same
enumeration has now been wrong twice — round 1's disposition claimed completeness at three paths and
round 2 found a fourth and a fifth.

The derivation is the set of call sites in the review command that write an output contract into an
emitted prompt. Run at `2833f81e`:

```
git grep -nE 'findingFormat|codeAuditOutputFormat|JSON finding per line' -- \
  'internal/cli/review.go' 'internal/cli/review_regression.go' 'internal/cli/review_verify.go'
```

Eight lines, of which three are the `const` definitions (`review.go:802`, `review.go:1412`,
`review.go:1764`) and **four are call sites**: `review.go:1108`, `review.go:1178`,
`review_regression.go:195`, `review_verify.go:267`. Those four sites are reached by five commands.
Each was emitted and its output-format object line parsed:

| command | site | keys in the output-format object | names `evidence` |
|---|---|---|---|
| `tp review <spec>` — each of the four panel roles | `review.go:1108` | 5 — `severity`, `category`, `location`, `finding`, `suggestion` | no |
| `tp review <spec> --role <r>` | `review.go:1108` | 5, byte-identical | no |
| `tp review <spec> --perspective code-audit` | `review.go:1178` | 11 — `role`, `id`, `file`, `line`, `pattern`, `current_behavior`, `spec_coverage`, `finding`, `suggestion`, `severity`, `category` | no |
| `tp review <spec> --perspective regression` | `review_regression.go:195` | 5, the panel format | no |
| `tp review <spec> --verify` | `review_verify.go:267` | **none — the prompt carries no JSON object line at all** | no |

`--verify` is row 8's mutant standing at `HEAD`: a listed path whose format lacks the key, in the
strongest form available, because there is no format. Measured on the emitted prompt, the string
`evidence` occurs **0** times in the `--verify` prompt and **0** times in the code-audit prompt, and
once in each panel prompt — which is why the row's ban on a whole-prompt substring search stays and
the justification that used to accompany it was deleted: it asserted the word occurs in *the* prompts
generally, and it does not occur in two of the five.

The two `--perspective` prompts that emit **plan items** rather than findings — `documentation` and
`testing`, `docOutputFormat` and `testOutputFormat` — are not in the list and the derivation does not
return them, because they write neither `findingFormat` nor a finding contract. Their rows carry
`id`/`action`/`priority` and no `severity`, so they never reach `--record` as findings.

### `--verify` is a mode, not a perspective

Round 1's disposition on the same enumeration claimed completeness from reading `reviewPerspectives`
(`internal/cli/review.go:512`, four entries: `documentation`, `testing`, `code-audit`, `regression`).
`--verify` is a **flag on `tp review`, not a value of `--perspective`**, so that check could not have
reached it however carefully it was run. The error class is worth naming on its own: *a completeness
check run against the wrong enumeration returns a clean answer to a question it never asked.* It is
the same shape as the fenced-command rule `CLAUDE.md` records — the command ran and printed something,
and what it printed answered a different question.

### `evidence` is write-only: the three injection sites, and where the reading half went

§1's decision 5 — *a recorded finding's evidence is read back* — and §5 row 10 are **cut**. The
measurements that motivated them are kept here.

`reviewFinding` (`internal/cli/review.go:131-139`) carries `severity`, `category`, `class`,
`location`, `finding`, `suggestion` and `resolved`, and no `evidence`; the round-1 sentinel run above
measured 0 occurrences of the evidence text against 3 of the finding text in the next round's
prompts. The field is written and never read back.

Three sites inject previous-round findings into an emitted prompt, and the decision named none of
them:

```
git grep -nE 'UNRESOLVED findings from previous rounds|Previously fixed findings|Unresolved findings \(still open\)' -- 'internal/cli/*.go'
```

| site | block |
|---|---|
| `internal/cli/review.go:1345` | the panel prompt's `UNRESOLVED findings from previous rounds — DO NOT re-report:` |
| `internal/cli/review_regression.go:178` | `## Previously fixed findings`, which already renders `resolved.evidence` |
| `internal/cli/review_verify.go:250` | `Unresolved findings (still open):` |

Why the decision could not be repaired in place: the panel block is capped at 50 detailed rows and
truncates its three free-text renders at 80, 60 and 40 characters, so *each finding* is unbuildable
past row 50; round 2 measured the block growing **+245%** on the rows that reach it and **+386%** over
all 33 of round 1's rows under a full render; and row 10's sentinel length was never pinned — the same
correct implementation is green against a 21-character sentinel and red against a 110-character one.
Narrowing the decision to one named channel would have made it an implementation sentence, which is
the shape this cycle keeps refuting.

**Where the reading half goes.** Not to `spec/backlog/05-forced-commitment-in-the-brief.md`: that
file's §2 is the **audit** prior-round injection (`loadAuditPriorRound`,
`renderPriorRoundSection`) and it does not hold the review-side question. No backlog file does — the
twenty-one entries in `spec/backlog/README.md` were read for it. It belongs to *The evidence
contract* under **Undecided** in `spec/undecided.md`, which §1's *Consequences* and §4's first
non-goal already name as the open question, and which now carries it explicitly.

### `--merge -o` writes its output and then refuses — §5 row 10

`runReviewMerge` (`internal/cli/review_merge.go:93-101`) writes the `-o` file and *then* calls
`finishMerge(..., dropped)`, so the refusal comes after the write. Run in the clone, with one input
holding a single content line that parses as no finding:

| | |
|---|---|
| `tp review --merge roleA.ndjson roleB.ndjson -o merged.ndjson` | exit **1** |
| stderr | `warning: skipping incomplete line (missing location)` then `no line parsed in roleA.ndjson …` |
| `merged.ndjson` | **present, 143 bytes**, holding only role B's row |

The byte count is a property of the fixture text, not of tp; the brief that ordered this repair said
149 and used a different fixture, so the two are not comparable and neither is load-bearing. What is
load-bearing is *present*.

**`skills/tp/SKILL.md:260` promises the opposite**, verbatim: *"`-o` writes only after the merge
succeeds (the write is the last step), so a failed merge leaves nothing and `--record` exits 3 on the
missing file."* That sentence is why this is in scope: `HEAD` contradicts tp's own shipped
documentation, and §2 turns the path from rare into the expected one by adding a fourth required key
no emitted prompt has ever asked for.

The consumer is tp's own driver. `skills/tp/REFERENCE.md:182` gives the `review-record` unit command
as `[ -f $TP_ROUND_DIR/merged.ndjson ] || tp review --merge … -o $TP_ROUND_DIR/merged.ndjson; tp
review <spec> --record $TP_ROUND_DIR/merged.ndjson` — `;`, not `&&`, behind an `[ -f ]` guard. Run
end to end in the clone with the same two inputs:

| | |
|---|---|
| the recipe's `--record` | exit **0** |
| recorded | `round 1, findings 1, clean true` |
| role A's row | absent, with nothing in the payload saying so |

and on a retry the `[ -f ]` guard sees the truncated file and skips the merge, so the round can never
be repaired by re-running the unit. That is why §3 puts the recipe in the docs task beside its two
prose sites.

### `--merge` keeps a whitespace-only key, and the corpus holds none — §5 row 11

`missingFindingFields` (`internal/cli/review_merge.go:190-198`) tests `s == ""` over `location`,
`severity` and `finding`. §2's shared predicate is `strings.TrimSpace`, and §2 states it for those
three keys as much as for `evidence` — so the release *does* change what `--merge` accepts for keys
decision 2 used to say it left alone. Not pinning that would make §5 false by omission. Run in the
clone against one file whose first row carries `"location": "   "` and whose second is legal:

| binary | `--merge` exit | `parsed` / `skipped` | stderr warning |
|---|---|---|---|
| `HEAD` | 0 | **2 / 0** | none |
| what §2 requires | 0 | 1 / 1 | names `location` |

`HEAD` is the mutant, so the row is red against the shipped tree without anything being built.

**How often the corpus holds one: never.** Counting rule, stated before the count: over the canonical
round files of both globs, a key is whitespace-only when its value is a string, is non-empty, and
`str.strip()` is empty.

```
python3 -c 'import json,glob,re
pat=re.compile(r"/(review|audit|ground)-round-\d+\.ndjson$")
fs=[f for g in ("spec/.tp-review/*/*.ndjson","spec/backlog/.tp-review/*/*.ndjson") for f in glob.glob(g) if pat.search(f)]
rs=[json.loads(l) for f in fs for l in open(f) if l.strip()]
print(len(fs), len(rs), sum(1 for r in rs if any(isinstance(r.get(k),str) and r[k] and not r[k].strip() for k in ("severity","location","finding"))))'
```

330 canonical round files, 20,130 rows, **0** carrying a whitespace-only `severity`, `location` or
`finding`. So the row closes a hole in a predicate, not an observed failure — which is the honest
reason to keep it small.

### Rows 3 and 7's numbers, run rather than referred to

Both rows said *"the same … the previous binary reports"*, which no Go test can hold. Run in the
clone against `HEAD`, one fixture each:

| row | fixture | result |
|---|---|---|
| 3 | three rows legal on all four keys, severities `high` / `medium` / `low`, no prior round | `findings: 3`, `consecutive_clean: 0` |
| 3, control | the same three rows at `low` / `medium` / `low` | `findings: 3`, `consecutive_clean: **1**` |
| 7 | one undispositioned `high` row with no `evidence` key, recorded, then `--status` | exit 0, `consecutive_clean: 0`; `--status --check` exit 1 |

The control is why row 3's fixture cell now states the severities: under the default
`review_converge_on=blocking` the same three-row file gives a different `consecutive_clean`
depending on a value the cell did not state, which is exactly what §5's preamble forbids.

### `--skip-gate` is a fence in the binary, not a rule in a spec

Decision 6's closing sentence has been deleted. Run in the clone:

```
TP_UNATTENDED=1 tp done t1 "…" --skip-gate --commit <sha>
{"error":"--skip-gate is a user-approved decision and is refused under TP_UNATTENDED",
 "code":2,"hint":"escalate instead: tp escalate --decision skip-gate --evidence <what you found>"}
```

The sentence restated, in weaker instructional form, a rule the binary enforces mechanically — and a
disposition two commits earlier had refused a reviewer's request for exactly that reason.

### Round 2's own merge: 28 rows, no cluster

| | round 1 | round 2 |
|---|---|---|
| merged rows recorded | 33 | **28** |
| rows carrying a non-empty `evidence` | 33 / 33 | **28 / 28** |
| `sum(found_by)` | 34 | **28** |
| clusters larger than one | one, of size 2 | **zero** |
| distinct `location` values | 4 | 5 |
| distinct `class` values | 33 | 28 |

Round 1's four role files held 35 rows and merged to 33; round 2 is reported as 28 in and 28 out. Two
collapses out of 63 role-written rows across two rounds. **The pre-merge count is not derivable from a
round file** — `found_by` counts distinct roles, not rows, which is the correction recorded above — so
round 2's 28-in figure is the merge envelope's, not this table's.

The mechanism: `location` takes only 4–5 distinct values in a round while `class` is free-form per
role, and `--merge` clusters on `(location, class)` byte-for-byte. Two roles agree on `location`
constantly and on a slug essentially never, so dedup fires only on exact slug agreement.

**Note what the distinct-class count is not.** *Every recorded row has a distinct class* is
tautological on a recorded round: `--merge` has already collapsed whatever agreed. The informative
facts are the two inputs to that — `location`'s coarseness and `class`'s freedom — and not the
distinctness of the pairs that survived.

**And the conclusion three roles drew from it is half wrong, because the signal they say does not
exist shipped in `v0.35.0`.** Round 2's architect, regression and ax-economist each argued from the
dedup key that no round can ever see one defect reported across sections — **in their reports to the
orchestrator, not in any recorded row.** Searched: `location, class`, `dedup` and `cluster` return
zero matches across all 28 rows' `finding`, `suggestion` and `evidence`. The argument reached a human
and never reached the record, which is the carrier gap those same reports were describing, arriving
as an instance of itself. `spec/0.35.0.md` §8a.1
already owns that class — *"roles compose their class slugs independently, so two roles reporting one
defect almost never collide"* — and it shipped `location_clusters` in the same release: `tp review
--merge` and `--status` emit one entry per `location` carrying findings from more than one role.
Verified in a clone: two rows, same `location`, different `class`, one from each of two roles →
`[{"location":"§1","roles":["a","b"],"severities":["high"],"count":2}]`. Recomputed for this cycle:

| | rows | distinct locations | `location_clusters` | the hot ones |
|---|---|---|---|---|
| round 1 | 33 | 4 | **4** | §5 all 4 roles (15 findings), §2 all 4 (10), §1 three (6), §3 two (2) |
| round 2 | 28 | 5 | **3** | §5 **all 5 roles** (14), §1 four (9), §3 two (3) |

**What that refutes, and what it does not.** It refutes *no round sees that a section is hot across
every role* — that is emitted on every merge. It does not touch *these three findings are one defect*
or *the release's per-round cost spans three sections*, because in both of those the locations differ,
which is precisely what `location_clusters` keys on. So the roles' premise about the dedup key stands
and their conclusion overreaches by exactly one step.

**The orchestrator ran the command and did not read the output.** `location_clusters` was present in
both merge envelopes and was discarded twice, because the orchestrator piped each envelope through a
filter printing only the keys it expected (`merged_count`, `inputs`) and reported the absence of
`found_by` clusters as *zero clusters*. This is the inverse of this repository's *prove by running*
rule: the command ran, the answer was in the payload, and the reading threw it away. The rule that
follows — **when filtering a JSON envelope for a report, print the names of the keys being dropped** —
belongs in `skills/tp/SKILL.md` and ships with this release's skill task, not here.

**Two figures spelled `63` appear in this cycle and they are different quantities.** `0.35.0` §8a.1's
is *blocking findings that shared a `location` with another role's finding, over nine rounds on an
external spec* (its companion figure: merge removed 7 duplicates). This file's is *role-written rows
across this spec's two rounds*, of which 2 collapsed. Different populations, different denominators,
different questions; neither is evidence for the other.

### The implementer's build, and a census that did not reproduce

Reported by the round-2 implementer unit and **not re-run here**: a fixture migration of 149 rows
across 29 files, carrying no production change, closes under the full project gate on its own; the
parity change applied on top of it then leaves **6 failing test functions, none of them a census
row**. The consequence is the one decision 5 (formerly 6) needs: the census is a **size**, not a task
boundary. The decision stays and the numbers stay out of the spec.

The census figures do not reproduce under this file's own counting rule, re-run at `2833f81e`: a
review-finding fixture row is a line of Go source in a `_test.go` file holding a single-line JSON
object literal naming both `"severity"` and `"finding"` and neither `"item_id"` nor `"status"`. That
rule gives **169 rows across 30 files** repository-wide and **147 across 28** in `internal/cli` — the
same figures as the round-1 section, unchanged. So 149/29 is a different census under an unstated
rule, and the two are not comparable. The 6 is the number that matters and it does not depend on
either.

### The floor after this repair

| | `floor_size` | `cut` | errors | warnings |
|---|---|---|---|---|
| before the repair | 54 | 23 | 0 | 1 |
| after | **51** | 23 | 0 | 1 |

Diffing the two `tp ground --units` listings: **13 units left the floor and 10 entered.** Seven of the
thirteen reappear under new text, because the repair rewrote them — §1's class sentence, decision 2's
and decision 3's, and §5 rows 3, 5, 7 and 8. **Six left for good**, and they are the subtraction:
decision 5's two sentences, decision 6's `--skip-gate` sentence, §2's two-sentence corpus paragraph,
and §5 row 10. Three of the ten are new: §3's `review-record` sentence and §5's two added rows.

**No sentence this repair kept was dropped from the floor by narrowing it** — the failure the round-1
repair recorded twice on this document. Every narrowed sentence (decision 2's, decision 3's) is still
a graded unit under its new text; that was checked by hash against the two listings, not assumed.

The one `section-size` warning stays and is the same one: §1 is now **54** lines against the rule's
50, down from 60 before the repair. The repair could not close it without splitting §1, which would
renumber every section and falsify the `§1`/`§2`/`§5` references in both files.

## After review round 3's repair

Round 3 recorded **29 findings, 15 of them `high`** — 8 `regression`, 6 `architect`, 6 `tester`, 5
`ax-economist`, 4 `implementer`. This section records what the final repair ran. Everything invoking
`tp review --merge`, `tp review --record` or a mutated binary ran in a `git clone --no-hardlinks` of
`9ba917e1` outside the repository; `tp lint`, `tp ground --units` and the two check scripts ran in the
repository, which `git status` shows unchanged apart from this repair's own files.

### There is no round 4, and the reason is a property of the table

Round 3's `tester` wrote the sentence that ended the loop: **rounds 1 and 2 found rows that were
wrong; round 3 found rows individually fine and collectively insufficient.** The cap is spent and the
design authority decided to decompose after this repair, so nothing written here will be graded.

The brief that ordered this repair said *four of its six findings* are of the form "no row can see X".
Re-read against the recorded rows, that count is not supported: **two** of the six span more than one
cell — `mutant-survives-every-row` (green on all eleven rows) and `unpinned-discriminator` (rows 1 and
2 sharing one blind spot) — while the other four are cell-level, three of them on row 8 alone. The
sentence stands; the count does not, and it is corrected here rather than repeated.

### A danger this release creates at upgrade: the 0.37.0 zero-byte run

This is why §5 row 10 is **repaired rather than cut**, and it is a danger the release *creates*
rather than one it inherits: §2 adds a fourth required key that no emitted prompt has ever asked for,
so every findings file written before this release fails the merge gate at once.

Run in the clone against a binary carrying only decision 2's change — `evidence` added to
`missingFindingFields` — with one real pre-`1.1.0` input, `spec/.tp-review/0.37.0/review-round-1.ndjson`
(65 rows, **0** of them carrying a non-empty `evidence`):

| | |
|---|---|
| `tp review --merge role-a.ndjson -o merged.ndjson` | exit **1**, 65 × `warning: skipping incomplete line (missing evidence)` |
| `merged.ndjson` | **present, 0 bytes** |
| the recipe's first `tp review <spec> --record merged.ndjson` | exit 0 — `round 1, findings 0, clean true, consecutive_clean 1` |
| the second | exit 0 — `round 2, findings 0, clean true, converged **true**` |
| `tp review <spec> --status --check` | exit **0** |

A zero-byte merged file is exactly what `skills/tp/SKILL.md` documents as how a *converged* round
reports itself, and `skills/tp/REFERENCE.md`'s `review-record` unit command chains merge into record
with `;` behind an `[ -f ]` guard. So **an in-flight round silently converges at upgrade** — and the
`[ -f ]` guard then makes it unrepairable by re-running the unit, because the guard sees the truncated
file and skips the merge. Both documents are §3's docs task, which already named the recipe and now
names `SKILL.md`'s sentence beside it.

### Row 10's SHALL was false, and the two runs that refuted it

The row said *leave no file at the `-o` path*. Two runs in the clone, seeding the `-o` path with a
31-byte line and comparing digests before and after:

| run | exit | `-o` afterwards |
|---|---|---|
| a missing input file named on the command line | **3** | **untouched** — same digest, same 31 bytes |
| the dropped-input fixture against the seeded `-o` | **1** | **replaced** — different digest, holding the merged row |

The first refutes the SHALL directly: a non-zero exit left a file at the `-o` path, because the file
was already there and `--merge` never reached its write. The second is the one worth having, and the
old row could not see it — `HEAD` does not merely *create* the file, it *overwrites* what is there.
The repaired SHALL — *create no file at the `-o` path, and modify no file already there* — covers
both, is already satisfied by the exit-3 run, and has `HEAD` as its mutant on the other two.

**What the SHALL deliberately does not reach: a clean round.** Measured before the row was written,
because a SHALL quantified over "non-zero exit" would be a defect if a converged merge exited
non-zero. Two zero-byte inputs merge at **exit 0** and write the zero-byte `-o` the loop depends on;
so do two inputs holding only a blank line. The refusal path and the convergence path are separated by
the exit code, which is what makes the row safe to phrase this way.

### Row 1's fixture was green under a mutant no row could see

Round 3's `tester` built two mutants the table did not name. The one that matters is **a `--record`
check that tests only `evidence`**: green on all eleven rows, recording `{"evidence": "…"}` at exit 0.

Built in the clone as a pair — a correct implementation (the four keys through one predicate,
offenders accumulated and every one named) and the mutant (a record-side check reading only
`evidence`) — and run first against the **old** row-1 fixture, whose two offending rows both offend on
`evidence`:

```
correct : 2 rows are missing a required field: line 2 (missing evidence); line 4 (missing evidence)
mutant  : 2 rows are missing a required field: line 2 (missing evidence); line 4 (missing evidence)
```

Byte-identical, both exit 1. That fixture could not have made the row red. Against the **new**
fixture — four rows, one per required key, each legal on the other three:

| | exit | lines named | `review_rounds` |
|---|---|---|---|
| correct | 1 | 1, 2, 3, 4 | 1 → 1 |
| mutant | 1 | 4 | 1 → 1 |

So the row is **red under its own stated mutant**, and the discriminating assertion is the *set* of
lines named rather than the exit code — which the repaired cell now says, because both exit 1. The
correct implementation also wrote no round file: against a spec with no prior round the state
directory was never created at all.

### Rows 1 and 2 now pin a prior round, because round 3 showed the clause was free

`unpinned-discriminator`, `high`, undispositioned when this repair ran: both rows SHALL *leave
`review_rounds` unchanged* while neither fixture named a prior round, so against an empty
`review_rounds` the clause discriminated nothing. Re-run here rather than taken on the finding's word.
The mutant is the refusal path destroying the recorded history — `st.ReviewRounds = []` plus a save,
in `runReviewRecord`'s parse-error branch:

| fixture as the row stated it | result |
|---|---|
| no prior round (rows 1 and 2 as written) | exit 1, and **no `state.json` exists at all** — the mutant survives, there being nothing to be unchanged |
| one round recorded first | exit 1, `review_rounds` **1 → 0**, `state.json` digest changed — the mutant dies |

Both fixture cells now say *recorded against a spec that already holds one recorded round*. This is
the third instance of the class §5's preamble was added to close, which is why it was worth spending
the run on rather than leaving to a round that will not happen.

### Row 8 stops depending on this file, and the derivation's arithmetic was wrong

`method-only-in-ungraded-sidecar` came back from `tp review --record` carrying `rounds_seen: 3,
total: 3`. `CLAUDE.md`'s rule is that a class surviving three prose repairs gets a check rather than a
fourth prose fix, so row 8's fixture now names the five commands itself and the derivation stays here.

Re-running the grep under *The review emission sites, derived* at `9ba917e1` returns **eight** lines,
and that section's split of them does not add up: it says three `const` definitions and *four* call
sites, which is seven. There are **five** call sites. The unaccounted one is
`internal/cli/review.go:1110`, which writes `findingFormatRound2` — a one-sentence *"only report NEW
issues"* reminder appended to a panel prompt from round 2 onward, carrying no output contract and no
keys. So: 3 consts + 5 call sites = 8, of which **four** write a finding contract, and those four are
reached by the five commands the section's table already lists correctly. The table was right; the
sentence under it was not.

### The `evidence` occurrence counts were stated without naming their input

That section says the string `evidence` occurs *0 times in the `--verify` prompt and 0 times in the
code-audit prompt, and once in each panel prompt*. Re-measured by emitting every prompt in the clone,
the figure turns out not to be a property of tp at all — it is a property of the spec under review and
of whatever `--affected-files` names:

| command | neutral spec, neutral affected file | `spec/1.1.0.md` itself, `--affected-files internal/cli/review.go` |
|---|---|---|
| `tp review <spec>` (each panel role) | 1 | 3 |
| `tp review <spec> --role <r>` | 1 | 3 |
| `--perspective code-audit` | **0** | 2 |
| `--perspective regression` | **0** | 0 |
| `--verify` | 0 | 1 |

The one scaffolding occurrence in a panel prompt is a single sentence — *"report it with its evidence
instead of making the edit"*. The rest come from the spec's own §2 heading and, in the code-audit
column, from `resolvedStatus`'s `json:"evidence"` struct tag inside the excerpted file. Two
corrections follow. The code-audit figure of 0 holds only when the affected file happens not to
contain the word, which the original measurement did not say. And the conclusion *"it does not occur
in two of the five"* is wrong in both directions: under scaffolding-only counting it occurs in
neither the code-audit, the regression nor the `--verify` prompt — **three** of five — because
regression was never counted; under the spec's-own-text counting it occurs in four of the five.

The brief that ordered this repair reported these counts as *"stated 0/0/1, measured 1/1/24"*. The
1/1 reproduces; **24 reproduces nowhere** and is not a figure any run here returned. What survives all
of it is the row's ban on a whole-prompt substring search, and the re-measurement strengthens the
reason for it: the count moves with the document under review.

### The check that replaces the fourth prose fix

`scripts/check-test-rows-cite-sidecar.py`, registered in `.tp/config.json`'s `workflow.checks` as
class `method-only-in-ungraded-sidecar`. Its subject — a spec's Tests table and its sidecar — already
exists in the review phase, which is `CLAUDE.md`'s condition for registering a check rather than
suppressing a finding class for nothing.

The counting rule lives in the script's own docstring so it cannot drift from the code: a scanned
section is a heading whose text is a number followed by `Tests`; a scanned row is a table line that is
neither the header nor the `|---|` separator; a scanned cell is a column whose header **contains** the
word `fixture` or `mutant`; a cell cites the sidecar when it holds `measurements file` or
`-measurements.md`; fenced blocks are skipped.

```
python3 scripts/check-test-rows-cite-sidecar.py --stats spec/*.md spec/backlog/*.md
# 1.1.0.md:144: fixture cell cites the measurements sidecar ('-measurements.md')
# scanned 290 Tests-table row(s) across 26 file(s) with a Tests table
```

**290 rows, 26 files, exactly one flag — the true positive, and zero false positives.** Both
narrowings were measured rather than assumed. Containment on the column header is load-bearing: this
corpus writes the column as *the mutant that must fail it* and *the fixture that separates them* far
more often than as a bare word, so an equality match scans **one** file of twenty-six. On the citation
pattern, the brief predicted that the bare word `measurements` produces a false positive in
`spec/backlog/README.md`; **it does not.** That file holds no Tests table, so under a row-scoped rule
it can produce nothing, and the bare plural returns the same single true positive over the whole
corpus. It is the **singular** `measurement` that costs two false positives, and both of those are in
`spec/1.0.1.md`. The shipped pattern is the narrow one regardless, because it says what it means.

**`spec/1.0.1.md` is not caught**, which was the specific thing to check. It does name
`spec/1.0.1-measurements.md` inside its own §6 Tests section, at line 260 — but in the section's prose
preamble, not in a row's fixture or mutant cell. That is exactly the distinction the rule rests on: a
Tests section may say where its forensics live; a *row* may not put its fixture or its mutant there.

The check was tested against built inputs rather than trusted. A fabricated spec carrying the citation
in a *the mutant that must fail it* cell and in a *the fixture that separates them* cell is flagged
twice; the same file with those two cells reworded exits 0; a `fixture`/`mutant` table under a
non-Tests heading is not scanned; and a sample table inside a fenced block under a `## 7. Tests`
heading contributes nothing.

### Round 3's withdrawals

Three findings were withdrawn by their own authors rather than dispositioned. They are recorded here
because a withdrawal is evidence and a round's rows carry no place to put one.

- `ax-economist` withdrew **`unbounded-injection-payload` in full**: decision 5 was cut in round 2's
  repair, so the channel that finding measured no longer exists in this release.
- The same role withdrew the *suggestion* attached to `unfenced-reader-set` while keeping the
  observation, because taking the suggestion would have written into §4 the code-shaped sentence
  decision 4 forbids.
- `tester` withdrew *"passes at `HEAD` with nothing built"* for rows 10 and 11: the round-2 repair had
  already converted that property into an honest mutant, so the observation was true of the draft it
  was written against and not of the text it was filed on.

### A requirement can fall below the cut, and one did here

The first draft of §3's addition wrote its requirement as a short sentence of its own — *"The chain
must not be able to do that."* Measured immediately after the edit: `cut` went 23 → 25, and
`tp ground --units` listed neither that sentence nor the paragraph's closing one. The requirement had
left the graded floor by being short. It was folded into the sentence before it, and the closing
sentence was deleted rather than rewritten; `cut` returned to 23 and the requirement is a floor unit
again. This is the third measured instance of the pattern on this one document.

### The floor after this repair

| | `floor_size` | `cut` | errors | warnings |
|---|---|---|---|---|
| before the repair | 51 | 23 | 0 | 1 |
| after | **52** | 23 | 0 | 1 |

Diffing the two `tp ground --units` listings by unit hash: **5 units left the floor and 6 entered.**
The five that left are §3's `review-record` sentence and §5 rows 1, 2, 8 and 10; five of the six that
entered are those same five under new text. The one net addition is §3's new sentence about
`skills/tp/SKILL.md`. **No sentence this repair kept was dropped from the floor by narrowing it** —
checked by hash against the two listings, not assumed; the one sentence that was is the section
above, and it was repaired before this table was measured.

The one `section-size` warning stays and is the same §1 one.

## The floor this spec ships with

Run against the repaired text, on the tree this section is committed in:

```
tp lint spec/1.1.0.md                            # floor_size 52, cut 23, 0 errors, 1 warning
tp ground spec/1.1.0.md --units | grep -c '^u'   # 52
```

`tp lint` also reports the round-1 panel without emitting a round: `architect`, `ax-economist`,
`implementer`, `tester`. Both figures move with every edit to the spec, so the commands are the
authority and the numbers here are only what they printed. `floor_size` names different quantities
depending on which payload carries it — `skills/tp/REFERENCE.md`'s *`floor_size` — one name, two
quantities* says which — so a figure from `tp lint` is not interchangeable with one from a ground
emission envelope, and the two commands above are quoted together for that reason.

The `--units` listing itself is deliberately not pasted here. It is the spec's own sentences, so a
copy would duplicate the document and go stale at the first edit — the failure this file's own
anchored-figure rule exists to prevent.

**And the last edit of round 2's repair was a second instance of the cut pattern above.**
§2's structural sentence was narrowed from *"by calling the same function, `missingFindingFields`;
the record path grows no second copy of the rule"* to *"by the same rule"*, because no §5 row could
make the function-name form red. The shortened sentence fell below the arms' cut threshold: measured
immediately after the edit, `floor_size` went 48 → 47 and `cut` 20 → 21, and `tp ground --units` no
longer lists it. So the narrowing removed from the floor the very sentence the decision was about —
the same shape recorded under *After ground round 2's repair*, in the same file, within the hour.
What makes it harmless here rather than a defect is that the two consequence sentences are still
floor units (`A value that is empty once strings.TrimSpace has run…` and `Whitespace TrimSpace does
not recognise…`) and §5 row 9 pins the observable behaviour, which was the stated reason for
narrowing. It is recorded because the pattern has now been measured **three** times on one document —
round 3's repair hit it again, under *A requirement can fall below the cut, and one did here* above —
and `spec/backlog/04a-ground-command-friction.md` §11.1 carries the open proposal.

### What the shipped document still carries, named rather than left to be found

**One lint warning, deliberately.** `tp lint spec/1.1.0.md` reports `section-size` on §1 at 54 lines
and nothing else — 0 errors, `floor_size` 52, `cut` 23. Splitting §1 would renumber every section and
every cross-reference in both files; the warning is cheaper than the renumbering, and naming it here is
what keeps it from reading as an oversight to the next round that opens this file.

**One sentence was cut on the last pass, and its rule moved rather than died.** §2 carried *"Whitespace
`TrimSpace` does not recognise — a zero-width space, say — is not whitespace for this purpose and is
not judged."* Rows 9 and 11 pin a regular space; nothing pins a zero-width one, and nothing can — the
boundary is a property of the predicate's own definition, not an observable of the release. So the
sentence left the spec and its content is now a docs obligation in §3: the reference states that
whitespace means the set `strings.TrimSpace` recognises and nothing outside it. Being wrong there is a
documentation defect; being wrong in a spec is a requirement defect, and only one of those has a test
row that could have caught it.

**An error class this cycle produced twice, both times from the same place.** The design authority's
round-3 brief named four review emission paths; there are **five** — `--perspective regression`
(`review_regression.go:195`) writes the panel's `findingFormat` and was not counted. Earlier in the
same cycle its brief carried an `evidence` occurrence figure of `1/1/24` whose third term reproduces
nowhere. Both are `CLAUDE.md`'s *never assert a quantifier you did not count*, and both arrived in an
instruction rather than in the document — which is the harder place to catch them, because a brief is
not graded by any round. The remedy that worked both times was the same: the unit re-derived the list
and said which claim was wrong.


### A mechanical sweep repairs the fixture whose subject is the missing field

Decomposition measured the fixture migration by building it, and the sweep that looked correct was not.
`internal/cli/merge_inputs_test.go` holds `{"severity":"high","finding":"no location at all"}` — a row
whose **absent `location` is what the test is about**. A census-driven sweep added the key, and
`TestReviewMerge_InputsReportPerFileCounts` went red at 3/1 against 2/2 **with no production change at
all**. The failure is indistinguishable, from the gate's output, from a real regression in the change
the migration was preparing for.

This is a new member of the family this repository already records under *a fixture's incidental
property can carry the verdict* — the earlier instances were a directory name that sorted after every
`*.tasks.json`, and a fixture whose row order decided which representative a merge kept. The shape is
the same and the direction is the same: **the property that makes the fixture work is invisible in the
fixture's own text**, so any rule applied to it uniformly destroys it. The defence is also the same,
and it is not "read more carefully": assert the property. A fixture that depends on a key being absent
should say so where the sweep can see it.

Two figures from the same measurement, because they bound how much of this work a census can plan.
The sidecar's own census rule (169 rows / 30 files, which reproduces exactly) is **not a superset of
the migration**: it excludes rows naming `"status"` — every row carrying a `resolved` block — and rows
naming `"item_id"`, the rows fed to both merges, and it cannot see rows a helper builds at all.
Migrating the full census still left **18** failures, of which **nine** came through one helper,
`classRow` in `internal/cli/report_class_test.go`, reached from `internal/cli/review_suppression_test.go`.
So the census is a size, and a lower bound on the size.
