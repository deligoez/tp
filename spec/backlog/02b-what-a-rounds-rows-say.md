# 02b — what a round's rows say (absorbed)

This file is a forwarding stub; it stays because shipped artifacts cite this path. Its accepted-finding half now lives in `a-finding-can-leave-an-audit-round.md`.
Its resolve fence — `--resolve`/`--resolve-all` refused under `TP_UNATTENDED=1`, `resolved.evidence` non-empty at write and at record — is `a-finding-can-leave-an-audit-round.md` §3, which took it from `a-findings-exits-agree.md` §4 so that the fence and the change it fences ship in one release.
Its role-scoped convergence (`audit_converge_roles`) is `round-knows-its-panel.md` §4a.
Its PASS-note counter was DROPPED because it measures a constant — every role-carrying PASS row carries a note; the derivation moved to `round-knows-its-panel-measurements.md` under *PASS rows carrying a note (dropped counter)*.
Its four open questions (§5a) moved to `spec/undecided.md` under *From the rows spec: four questions*.
