{"category":"consistency","class":"id-anchor-path-space","evidence":"Probe in a fresh git init: repo/sub/.tp/config.json, repo/sub/spec.md, repo/sub/app/calc.go committed, task closed with --commit \u003csha\u003e. `cd repo/sub \u0026\u0026 tp audit spec.md --affected-from-tasks` -\u003e files ['sub/app/calc.go'], spec-coverage affected_files [{path: sub/app/calc.go, tasks: [calc]}]. `cd \u003cscratch\u003e/elsewhere \u0026\u0026 tp audit \u003cabs\u003e/repo/sub/spec.md --file \u003cabs\u003e/spec.tasks.json --affected-from-tasks` -\u003e identical. Read: internal/engine/workflow_resolve.go:17-20 (project config discovered from the working directory), internal/engine/projectconfig.go:473-491 (ProjectRoot: .tp/ dir, else git boundary, else start dir; 'never re-anchored to a --file located in another directory'), internal/engine/auditfiles.go:209-251 (git show --name-only joined on inUniverse), internal/cli/audit.go:325 (mapping resolved in filepath.Dir(specPath)), internal/cli/audit.go:597-605 (changed_since via git log --name-only). Round 3's finding 5 suggested the spec's repository; the repair chose .tp/ discovery instead, which round 3's finding 22 noted differs in a monorepo.","finding":"Decision 1's anchor, 'the project root — the directory tp resolves .tp/ from', is a different path space from the one every audit git derivation joins on, and it moves with the working directory, so the decision contradicts both §3 and its own 'not which directory tp runs from'. (1) tp discovers .tp/ from the working directory (engine.ResolveWorkflow: 'the project config (discovered once from the working directory)'; ProjectRoot is never re-anchored to a file elsewhere). Today the audited file list is anchored on the spec's repository instead and does not move: measured, the same audit run from inside the project and from an unrelated directory both emit files ['sub/app/calc.go']. Under decision 1, the run from the unrelated directory has a project root holding neither .tp/ nor .git (the cwd itself), every audited file is 'outside that root' and is taken by its absolute path, so each file gets a second id — §2's defect, caused by where tp runs from. (2) When .tp/ sits below the git top level (a monorepo with one tp project per service, which DiscoverTPDir supports), 'the emitted file list names each file once, by that path' renames the list to project-relative 'app/calc.go', while GitTaskFileMapping joins the list against `git show --name-only`, which prints repo-relative 'sub/app/calc.go' (inUniverse[f]). No task maps, spec-coverage takes its fallback list with empty task lists — exactly the silent loss §3 exists to remove — and changed_since (git log --name-only) and audit-round-prep.py's diff-based carrying key on repo-relative paths too. No §6 row runs tp from outside the project or with a nested .tp/; row 1b's subdirectory case resolves to the same root and cannot reach either.","found_by":1,"found_by_roles":["architect"],"location":"§2.1 d1","role":"architect","severity":"high","suggestion":"Anchor decision 1 on the root the audit's git calls already use: the git top level of the repository holding the spec (and, outside any repository, the spec's directory), so the id, the emitted list, the task mapping and changed_since share one path space and none depends on the working directory. Add §6 rows: the same spec audited from inside its project and from an unrelated directory gives one id per file (mutant: anchor on the working directory's .tp/ discovery); and a project whose .tp/ is below the git top level still maps its task's file to spec-coverage (mutant: express the list relative to the .tp/ directory)."}
{"category":"feasibility","class":"unit-key-not-unique-under-group","evidence":"Read internal/engine/nextunits.go:181-198 (dispositionUnit: first undisposed non-PASS row -\u003e newNextUnit(UnitAuditFix, auditRowKey(row))), internal/engine/unitkind.go:174-175 and :271-290 (rowIsDisposed returns rowDisposed of the first row matching role and item_id), internal/cli/audit_resolve.go:17-19 (the role:item_id selector is the key the oracle hands an audit-fix unit), internal/engine/driver.go:370 (1 + run_max_unit_retries attempts of the same unit), and skills/tp/REFERENCE.md:187 (the audit-fix id is 'the finding's role:item_id', written as 'that row carries a disposition'). Probe: I rsync'd the repo to scratchpad/review/r4/ax.d96P and added a scratch test calling dispositionUnit and UnitAuditFix.DurableWrite over merged.ndjson. Case A, [PASS f1, FAIL f1]: unit=security:f1, durable=false; after the FAIL is disposed, durable(security:f1)=false and there is no next unit. Case B, [FAIL/warning f1 disposed, FAIL f1]: next unit=security:f1, its durable write already=true. No §6 row sets TP_ROUND with a group in merged.ndjson (rows 10b, 10c, 11, 11b and 12g are all about next_step and matching_rounds).","finding":"Decision 2 keeps every row of a disagreeing (role, item_id) group in the merge output, and it makes the role:item_id selector refuse. Its Why then claims 'Nothing is refused, so the driver's record chain records as before', but it checks only the record unit. The driver's audit-fix unit is keyed by exactly that pair: the oracle emits one unit per undisposed non-PASS row, with ID auditRowKey(row) = role:item_id, and its durable write rowIsDisposed(key) reads the FIRST row carrying that key. Both break on the group decision 2 now produces. (a) Row 5's own shape is PASS first, FAIL second under one key. The unit disposes the FAIL, the only finding, but its predicate reads the undisposed PASS and stays false. Both attempts fail and the run stops with unit-failure. Satisfying it would take a disposition on a PASS row, which §4 d4 says is not a finding. (b) Row 5b's shape is two FAILs. Once row 0 is disposed, the oracle emits a unit for row 1 whose durable write is already true, which breaks the oracle's stated invariant that it 'can never hand the driver a unit that is already complete'. A child that exits 0 passes, and the same unit comes back each iteration until run_max_units. (c) The unit's natural first command, `tp audit merged.ndjson --resolve role:item_id`, is the selector the resolve code documents as 'the key the oracle hands an audit-fix unit', and decision 2 makes it exit 2. So a round holding a group, the case decision 2 exists for ('a role that ... writes two rows for one item'), ends a tp run in unit-failure or spends it in a loop.","found_by":1,"found_by_roles":["ax-economist"],"location":"§2.2","role":"ax-economist","severity":"high","suggestion":"Say in §2.2 how a driver audit-fix unit addresses a row of a group. One option: the oracle emits one unit per (role, item_id) group, its durable write is 'every non-PASS row under the key carries a disposition', and `--resolve role:item_id` disposes every undisposed non-PASS row under the key and names their indexes, instead of refusing. That is one call per group, not a refusal plus N index calls. Another option: key the unit by index. Add a §6 row inside a driver round, merged.ndjson = [PASS k, FAIL k], where the unit's resolve satisfies its own durable write, with the mutant 'predicate reads the first row under the key'."}
{"category":"regression","class":"merge-duplicates-break-audit-fix-unit-key","evidence":"Read the current spec §2.2 lines 78-89 and the round-3 snapshot's decision 2 (worse verdict kept, one row per key). Read internal/engine/nextunits.go:178-197 (audit-fix is one unit per row, id auditRowKey(row)); internal/engine/unitkind.go:255-290 (auditRowKey; rowIsDisposed returns rowDisposed of the first role/item_id match); internal/cli/audit_resolve.go:14-20 and 164-175 (the selector exists for the unit; at HEAD it resolves the first match); skills/tp/REFERENCE.md:187 (audit-fix id = 'the finding's role:item_id', predicate 'that row carries a disposition'). The sidecar's Review round 2 section records the refused-merge driver deadlock that round's repair removed.","finding":"The round-3 repair turned a same-(role, item_id) group from something --merge never emits into something it always keeps, and made the role:item_id selector refuse on a group. Both land on tp run's audit-fix unit, which the spec does not mention. That unit's identity is the row's role:item_id key: nextunits.go:186-191 emits one audit-fix unit per undisposed non-PASS row with TP_UNIT_ID = auditRowKey(row). Its durable-write predicate, rowIsDisposed (unitkind.go:271-289), reads the FIRST row carrying that key. And audit_resolve.go:17-19 says the role:item_id selector exists so 'a unit can name its own row without first locating an index'. Take the input the spec itself names as decision 2's case, a role that writes two rows for one item, e.g. PASS then FAIL (the row 5 and 5f order), inside a driver round. The audit-record chain records as the Why promises. Then the oracle emits audit-fix 'role:item' for the FAIL row. The unit's designed command, --resolve role:item, now exits 2. If it disposes the FAIL by index, the predicate still reads the undisposed PASS row first, so the attempt counts as failed and the run ends in unit-failure. With FAIL/FAIL (row 5b) the predicate is true after the first row is disposed, while the oracle keeps emitting the same unit id for the second row, so it hands the driver a unit that already reads complete. At HEAD, and under round 2's keep-one merge, merged.ndjson never holds a repeated key (the corpus check in the sidecar: 13,420 rows, 13,420 distinct keys), so none of this could arise. 'Nothing is refused, so the driver's record chain records as before' covers the record unit only. The unit after it deadlocks, which is the class round 2's fix (the refused merge deadlocking the driver) removed.","found_by":0,"location":"§2.2","role":"regression","severity":"high","suggestion":"Say what an audit-fix unit is keyed on once merged.ndjson can hold a group, e.g. the row index or role:item_id plus an ordinal, and make its predicate read the same row the unit disposes. Add a §6 row under TP_ROUND and TP_FILE: a merged file holding PASS then FAIL for one key, and the audit-fix unit's disposal satisfies its predicate. Or scope the selector refusal so the unit's own key still resolves to one row."}
{"category":"consistency","class":"driver-unit-key-uniqueness","evidence":"Read: internal/engine/nextunits.go:178-197 (dispositionUnit: one audit-fix unit per undisposed non-PASS row, ID auditRowKey(row); doc: 'the emission condition is the exact negation of the kind's own §3.3 durable write'), internal/engine/unitkind.go:174-175 and 255-290 (UnitAuditFix.DurableWrite = rowIsDisposed(merged, t.ID), which returns on the first row matching role and item_id), internal/cli/audit_resolve.go:14-20 and 164-175 (selector is 'the role:item_id key the oracle hands an audit-fix unit'; first match wins today). Spec §2.2 d2: 'A --resolve selector role:item_id that names more than one row refuses with exit 2'; §2.2 Why: 'Nothing is refused, so the driver's record chain records as before'. No §6 row runs a conflict group through the oracle or the audit-fix predicate.","finding":"Decision 2 lets tp run's merged.ndjson hold two undisposed non-PASS rows under one (role, item_id) and makes the `role:item_id` selector refuse on such a key, but that key is the audit-fix unit's identity and the whole of its durable-write predicate. The oracle emits one audit-fix unit per undisposed non-PASS row with ID auditRowKey(row) = role:item_id; the kind's predicate rowIsDisposed(merged, key) returns the disposition of the FIRST row carrying the key; resolve's usage text says the selector exists because it is 'the role:item_id key the oracle hands an audit-fix unit'. With a conflict group R0, R1 (both non-PASS, verdicts differ): the unit's natural `--resolve role:item_id` now exits 2; if it disposes R1 by index the predicate reads R0 and fails the unit, which is retried to exhaustion; if it disposes R0 only, the unit succeeds and the oracle then emits audit-fix role:item_id again for R1 — a unit whose predicate is already true, breaking dispositionUnit's documented invariant that the emission condition is the exact negation of the durable write. §2.2's Why checks only the record chain ('Nothing is refused, so the driver's record chain records as before'); §4 says the unit 'follows its own brief', which carries only the key. The corpus check (13,420 rows, 13,420 distinct keys) is why this never fired: uniqueness was the merge's guarantee, and decision 2 removes it.","found_by":1,"found_by_roles":["architect"],"location":"§2.2 d2","role":"architect","severity":"high","suggestion":"Make decision 2 say what the audit-fix unit is keyed on once keys can repeat: either one unit per conflict group whose predicate is 'every non-PASS row under the key carries a disposition' (emission: any such row lacks one), or a unit ID that names the row (e.g. key plus its index), with the oracle and predicate reading the same form. Add a §6 row: a merged.ndjson holding two non-PASS rows under one key, one disposed — the oracle emits no unit whose DurableWrite is already true, and disposing both completes it (mutant: first-match predicate, the shipped rowIsDisposed)."}
{"category":"consistency","class":"write-target-breaks-downstream-anchor","evidence":"Probe in a fresh git init: round file with one go-safety PASS on src/touched.go and one FAIL, committed ('record audit round 1'); src/touched.go repaired and committed; `tp audit \u003cround file\u003e --resolve 1 fixed …` (the §4 d1 target) and the change committed. `audit-round-prep.py spec/x.md --no-build` before the disposition commit -\u003e previous_record_sha aabb6ef9, changed ['src/touched.go'], go-safety carried 0; after it -\u003e previous_record_sha f7bd8240 (the disposition commit), changed [], carried 1. Read: scripts/audit-round-prep.py:23-30 (the 'untouched' rule) and :289-306 (sha = git log -1 -- round_rel), scripts/audit-round-prep-test.sh:108 and its header ('a delta re-grade that carried any of the last five would tell a role it need not re-measure something the repair round changed underneath it, which is the failure this script exists to make impossible').","finding":"§4 d1 moves the interactive loop's dispositions into the recorded round file, written in step 4 after the code is fixed, and nothing in the spec adjusts the one consumer that treats the last commit touching that file as the moment the round was recorded. scripts/audit-round-prep.py defaults previous_record_sha to `git log -1 -- \u003cround file\u003e` and carries a PASS row verbatim when its evidence_file is absent from `git diff --name-only \u003cthat sha\u003e..HEAD`. Under the new step 4 the round file is modified after the repair commits, and once that bookkeeping change is committed the 'record sha' becomes the disposition commit, which postdates the repairs: the diff window is empty and every PASS row over a file the repair changed is carried and briefed as 'do not re-measure' — the failure the script's own test calls the one it 'exists to make impossible', and the delta re-grade CLAUDE.md prescribes for this repo. §2.1's consequence edits the same script for decision 1 and is silent on this; no §6 row carries a round whose file was modified after it was recorded.","found_by":1,"found_by_roles":["architect"],"location":"§4 d1","role":"architect","severity":"high","suggestion":"Add a consequence to §4 (or §2.1's script consequence): audit-round-prep.py takes the record point from the round itself rather than from the last commit touching its file — e.g. the first commit that added the round file (`git log --diff-filter=A`), or the commit at or after state.json's recorded_at — so a disposition written into the recorded round after step 3 cannot narrow the diff window. Add a §6 row: record, repair, dispose into the recorded round file, commit; the prep re-measures the repaired file's PASS row (mutant: the shipped `git log -1 -- \u003cround file\u003e`)."}
{"category":"feasibility","class":"id-root-anchored-at-cwd","evidence":"Probe impl.u3g1: a git repo holding spec.md and a modified app/a.go. `tp audit spec.md` from the repo, `tp audit ../spec.md` from repo/app, and `tp audit repo/spec.md` from the parent directory (no .git, no .tp/) all emit files ['app/a.go'] and the same id, file-security-app-a-go-apply-the-security-role-rules-t. internal/engine/projectconfig.go:473-491 (ProjectRoot: .tp/ found upward from start, else the git boundary, else start). internal/cli/audit.go:714-715 (detectChangedFiles(specDir, …)). internal/engine/auditfiles.go:225-232 (git show with cmd.Dir = the spec's repository, 'not the process cwd').","finding":"Decision 1 roots the path at 'the directory tp resolves .tp/ from', and that directory is discovered from the process cwd. engine.ProjectRoot is documented as 'anchored once at start and never re-anchored to a --file located in another directory', and with no .tp/ and no git boundary it returns the cwd itself. The audit's paths come from somewhere else. Every git call in the audit path runs with cmd.Dir set to the spec's directory, so auto-detected files are relative to the spec's git top level. At HEAD that makes the auto-detected id independent of the cwd. Built as written, decision 1 breaks that. A run from the checkout's parent directory, which has no .tp/ and no .git, has to re-express app/a.go as repo/app/a.go and so gets a new id. In a monorepo with .tp/ under a package, a run from the git root and a run from the package directory resolve two different roots. The mechanism the decision names contradicts its own 'not ... which directory tp runs from' and regresses a case HEAD already handles. The implementer has to choose between the literal rule and the promise.","found_by":1,"found_by_roles":["implementer"],"location":"§2.1","role":"implementer","severity":"medium","suggestion":"Anchor decision 1's root at the audited spec, meaning .tp/ discovered upward from the spec's directory, else its git boundary. That is the same anchor the audit's git calls use. Add a §6 row: the auto-detected id of one file is identical when tp runs from the repository root and from its parent directory."}
{"category":"completeness","class":"path-normalization-gap","evidence":"From the probe repo entered as /tmp/claude-501/…/impl.u3g1/repo: `pwd` prints /tmp/claude-501/…/repo, and `git rev-parse --show-toplevel` prints /private/tmp/claude-501/…/repo. `ls -ld /tmp` shows /tmp -\u003e private/tmp. internal/engine/projectconfig.go:487 (filepath.Abs, no EvalSymlinks). internal/cli/audit.go:691-711 (--affected-files is only stat'ed, so both spellings are accepted).","finding":"Decision 1 says an absolute spelling does not make a second id, but it does not say whether the root and an absolute path are compared before or after resolving symlinks. §4 d3 says 'compared after resolving symlinks' for round files, and decision 1 says nothing. On macOS /tmp and /var are symlinks, and the two natural root sources spell them differently: the cwd, which filepath.Abs and ProjectRoot use, keeps the logical /tmp/… form, while `git rev-parse --show-toplevel` returns the physical /private/tmp/… form. Name a file by its physical path from a logically spelled cwd, or the reverse. filepath.Rel then yields ../../private/…, so the file counts as 'outside that root', gets an absolute-path id, and so gets a second id for the same file. t.TempDir() lives under /var, so whether row 1b's absolute spelling passes on a Mac depends on which root source the implementer picked, while Linux CI stays green. Resolving symlinks over the whole path is also wrong: an in-repository symlink and its target would become one checklist item, so two named files would get one id.","found_by":1,"found_by_roles":["implementer"],"location":"§2.1","role":"implementer","severity":"medium","suggestion":"State in decision 1 that the root and a named path are compared after resolving symlinks in the root prefix only. Symlinks below the root stay distinct files. Add a §6 row that names the file by its physically spelled absolute path from a logically spelled cwd and asserts one id."}
{"category":"regression","class":"id-anchor-depends-on-checkout-or-cwd","evidence":"Probe (regression.ClYo, fresh git init): services/a/.tp/, services/a/spec.md, libs/shared/x.go committed; from services/a, `/tmp/tp-dev/tp audit spec.md --affected-files ../../libs/shared/x.go --affected-files app/a.go --json` exit 0, files [\"../../libs/shared/x.go\", ...], ids file-security-libs-shared-x-go-… and file-maintainability-conventions-libs-shared-x-go-…, so HEAD accepts a file inside the repository and outside the .tp/ root. Code: internal/engine/workflow_resolve.go:17-22 ('The project config (discovered once from the working directory)', DiscoverTaskFile(\".\")); internal/engine/projectconfig.go:473-491 (ProjectRoot: .tp/ dir, else git boundary, else the start directory itself; 'never re-anchored to a --file located in another directory'). Diff against snapshot-round-3.md: '**relative to the repository root**' replaced by 'relative to the project root — the directory tp resolves .tp/ from … a file outside that root is taken by its absolute path'.","finding":"The repair of round 3's 'id-root-undefined' finding anchors decision 1 on 'the project root — the directory tp resolves .tp/ from'. For two inputs that anchor breaks decision 1's own clauses. (1) Monorepo, with .tp/ below the git top level: the case round 3's architect finding named. A file in the same repository but outside the .tp/ directory is 'outside that root' and 'taken by its absolute path', so its id changes with where the checkout lives. That includes every git worktree or clone, and this project builds one clone per audit round. Round 3's text took the path relative to the repository root, so this file had a stable libs/shared/x.go path. The repair makes a stable case checkout-dependent, against 'not where the checkout lives' and row 1c. (2) No .tp/ and no git, the fixed finding's case (1). tp resolves .tp/ from the working directory, and ProjectRoot then falls back to the start directory. So the root is wherever tp runs, and 'a spelling relative to a subdirectory run from there' (row 1b) gets a second id. Round 3's finding said that choosing the working directory 'breaks not which directory tp runs from … which is §2's defect'. The repair names that directory. Running tp from outside the project, with an absolute spec path, lands on the same fallback.","found_by":0,"location":"§2.1","role":"regression","severity":"medium","suggestion":"Anchor the path on something that moves with the file and not with the working directory: the git boundary of the file when there is one (so monorepo siblings stay repository-relative), and otherwise the spec's directory or the .tp/ root found from the spec, not from the working directory. Add a row with a file inside the repository but outside the .tp/ directory, named in two clones, that gets one id."}
{"category":"ambiguity","class":"symlink-spelling-unpinned","evidence":"ls -ld /tmp /var: both are symlinks to private/…. In the probe repo reached through /tmp/…: PWD=/tmp/claude-501/…/tester.O3Fp, toplevel=/private/tmp/claude-501/…/tester.O3Fp. engine.ProjectRoot, DiscoverTPDir and FindGitBoundary all start from filepath.Abs(start) and walk lexically. §4 d3: 'compared after resolving symlinks'.","finding":"Decision 1 does not say whether a path is judged against the project root before or after resolving symlinks, and 'cleaned' reads as lexical. §4 d3 resolves symlinks for its own path comparison. On macOS, /tmp and /var are symlinks, and every t.TempDir() is under /var. If tp's cwd is spelled /tmp/x, ProjectRoot is lexical and returns /tmp/x, while `git rev-parse --show-toplevel` returns /private/tmp/x. So an absolute spelling built from git, /private/tmp/x/app/a.go, is lexically outside the root. It is then 'taken by its absolute path' and gets a second id for the same file, which is the defect §2 removes. A build that resolves symlinks gives one id. Row 1b's 'by its absolute path' and row 1c do not say which absolute spelling to use, so two engineers write contradictory tests, and the macOS result differs from Linux.","found_by":1,"found_by_roles":["tester"],"location":"§2.1 d1","role":"tester","severity":"medium","suggestion":"Add to decision 1: 'a path is placed inside or outside the root after resolving symlinks in both'. Add a row 1b spelling through a symlinked ancestor (the /tmp versus /private/tmp form), with the mutant 'relativise lexically'."}
{"category":"consistency","class":"root-undefined-case","evidence":"internal/engine/projectconfig.go ProjectRoot: .tp/ dir, else FindGitBoundary, else filepath.Abs(start). The CLI callers pass '.'. Probe in a directory under the scratchpad with no .git and no .tp: `tp audit spec.md --role security --affected-files app/a.go` exited 0, and `(cd app \u0026\u0026 tp audit ../spec.md --role security --affected-files a.go)` also exited 0.","finding":"Decision 1 says the id does not depend on 'which directory tp runs from' and takes the path relative to 'the directory tp resolves .tp/ from'. When there is no .tp/ and no git boundary, that directory is the working directory, so the two clauses contradict each other on an input tp accepts today. Take a spec and app/a.go in a plain directory. Run from the top with app/a.go, the file gets the relative path 'app/a.go'. Run from app/ with a.go, it gets 'a.go', so the file has two ids, which is §2's defect. Row 1b's subdirectory spelling does not say that its fixture has a git repository or a .tp/. A test harness that builds the fixture in a bare t.TempDir() therefore sees a conforming build fail the row.","found_by":1,"found_by_roles":["tester"],"location":"§2.1 d1","role":"tester","severity":"medium","suggestion":"Name the root for that case in decision 1, for example 'the spec file's directory when neither .tp/ nor a git boundary is found', so that it no longer depends on the cwd. Alternatively, scope the 'which directory tp runs from' clause to a project with a .tp/ or a git boundary. Either way, state in row 1b that its fixture is a git repository."}
{"category":"consistency","class":"shape-test-not-disjoint","evidence":"Probe in a fresh git repo with /tmp/tp-dev/tp (bd4a18f1): tp audit spec.md --role security --affected-files public/build/assets/app-3f2a9b1c4d5e6f70.js,aaaaaaaaaaaaaaaaaaaaaaa/0123456789abcdef.go emitted file-security-public-build-assets-app-3f2a9b1c4d5e6f70 and file-security-aaaaaaaaaaaaaaaaaaaaaaa-0123456789abcdef. Each ends in '-' + 16 lowercase hex. internal/cli/audit_roles.go slugifySubject: lowercase, non-[a-z0-9] runs to '-', cut to 40.","finding":"Decision 1b classifies an earlier-derivation id by shape: it does not end in 'such a digest'. The Why claims the shape 'tells an earlier id apart row by row, whichever binary recorded the round'. That claim fails for ids HEAD already emits. HEAD's slug alphabet is [a-z0-9-], and its 40-character cut lands inside any long path. So a path whose cut slug ends in '-' plus a hex run of the digest's length already carries a digest-shaped tail at HEAD. Content-hashed build assets are the ordinary case, and they are auditable .js. With a 16-hex digest, the natural encoding of the 64-bit floor, such a HEAD-recorded row counts as current. It is listed with the rows that answer instead of under the earlier-derivation line. audit-round-prep.py does not exit, and it carries the untouched PASS row under an id no item carries, which is the outcome the prep consequence exists to prevent. No §6 row holds such a path.","found_by":1,"found_by_roles":["tester"],"location":"§2.1 d1b","role":"tester","severity":"medium","suggestion":"Make the shape disjoint from HEAD's alphabet. For example, require that the digest be set off by a character slugifySubject never emits ('_' or '.'), or be of a length or position no HEAD id can produce, and say so in decision 1. Then add a row: a HEAD-recorded FAIL on public/build/assets/app-3f2a9b1c4d5e6f70.js is listed under the earlier-derivation line, with the mutant 'classify by a trailing hex run'."}
{"category":"completeness","class":"conflict-group-carried-by-prep","evidence":"scripts/audit-round-prep.py:155-175 (split_rows: `carried = status == PASS and evidence_file untouched`, decided per row, with no look at other rows of the same item_id; every other row's item_id goes to remeasure). :225-243 (brief text 'do not re-verify them' / 'Re-measure exactly these N item(s)'). Spec §2.2 d2: 'rows whose verdicts differ are all kept … --record records every row'. §6 row 5f.","finding":"Decision 2 now guarantees that a group of rows under one (role, item_id) reaches the recorded round, for example a PASS and a PARTIAL (row 5f, spec-coverage across shards) or a PASS and a FAIL. The spec does not say what scripts/audit-round-prep.py does with such a group, and §2.1 already changes that script. The script partitions row by row. The PASS row, if its evidence_file is untouched, is carried verbatim, with the brief saying 'Re-record unchanged, and do not re-verify'. The same item_id also goes to 'Re-measure exactly these item(s)'. So the brief for that item contradicts itself. The role then re-records the carried PASS and adds a new verdict. If the re-measure is non-PASS again, the next round holds the same group, conflicts reports it again, and the stale PASS is carried forever. Before decision 2 a merge never delivered such a group, so this is new behaviour the spec creates, and the implementer has to invent the rule.","found_by":1,"found_by_roles":["implementer"],"location":"§2.2","role":"implementer","severity":"medium","suggestion":"Add to §2.2 Consequences: audit-round-prep.py carries no row of a (role, item_id) that also has a non-PASS row in the previous round, and that item is listed for re-measure only. Add a §6 row: a prior round holding a PASS and a PARTIAL under one id carries neither."}
{"category":"regression","class":"shard-advice-leaves-spec-coverage-partial","evidence":"Read internal/engine/auditfiles.go:123-168: selectSpecCoverage keeps only universe files with task ids, capped at AuditFileCap (20); the universe is the --affected-files list (line 55). Read the current spec §2.2 Consequences (lines 91-95) and round-3 row [14] (regression, shard-blind-spot-recorded-as-verdict, resolved fixed with 'the skill tells an operator sharding by hand to run spec-coverage in one shard'). Round-2's fixed finding quoted in the prompt says 'The truncation notice … does not say --role'.","finding":"The repair of round 3's 'shard-blind-spot-recorded-as-verdict' finding tells a hand-sharding operator 'to run spec-coverage in one shard'. That does not give spec-coverage the files it was missing. Its list is the named universe intersected with the task-mapped files (selectSpecCoverage), so spec-coverage running in one shard grades every spec-derived item against that shard's files only. An item implemented in another shard's files then comes back PARTIAL or FAIL every round, the non-clean-every-round outcome the fixed finding described. Or it comes back PASS on the grader's inference. Either way, now there is only one row, so there is no conflict: the round records a verdict on a partial file list and nothing says so, which is §1's defect shape. The advice closes the conflict and keeps the partial view. It works only if that one shard names every file, which is possible because spec-coverage's cap (20) is above the code roles' (10), but the consequence does not say that. The advice also lives in the skill, while what prompts sharding is tp's truncation notice ('name the rest with --affected-files'). Round 2's shard finding already noted that the notice does not mention --role.","found_by":0,"location":"§2.2","role":"regression","severity":"medium","suggestion":"Say what the one shard is: spec-coverage emitted alone (--role spec-coverage) with every file named. Put that in the truncation notice that recommends sharding, or say why the skill is enough."}
{"category":"consistency","class":"remedy-recreates-defect-shape","evidence":"Read: spec §2.2 Consequences ('the skill tells an operator who shards by hand to run spec-coverage in one shard'), §1 item 2 and opening sentence, §3 Context and row 9. internal/engine/auditfiles.go:123-168 (selectSpecCoverage ranks the task-mapped files OF the universe it is given; with no mapped file it takes the first 20 of that universe) and internal/cli/audit.go:321-327 (--affected-files replaces the universe for every role). The advice was introduced by round 3's repair of finding 14 (resolved evidence: 'the skill tells an operator sharding by hand to run spec-coverage in one shard').","finding":"§2.2's consequence tells an operator who shards by hand to run spec-coverage in one shard, which re-creates for every hand-sharded round the exact loss §3 removes and §1 names as this release's subject. An operator shards because the named files exceed the cap (§2 Context: the truncation notice's own 'name the rest with --affected-files'). spec-coverage's list is the task-mapped subset of its OWN emission's universe (selectSpecCoverage over the shard's --affected-files), so under the advice every task file named only in the other shards reaches no conformance role, and the recorded spec-coverage PASS reads as a verdict over the whole spec with nothing saying which files it never saw — 'the recorded round holds something other than what was graded, and nothing says so' (§1), 'the other commits' files reach no conformance role, and nothing says so' (§3). The spec already has the input that avoids it: §3 row 9 makes --affected-from-tasks and spec-coverage's mapping agree, so the shard spec-coverage runs in can be chosen by the task mapping rather than by whichever slice of the named list it happens to be.","found_by":1,"found_by_roles":["architect"],"location":"§2.2","role":"architect","severity":"medium","suggestion":"Make the consequence name which shard: run spec-coverage once, in an emission whose universe is the task mapping (`tp audit \u003cspec\u003e --affected-from-tasks --role spec-coverage`), and the code roles in the --affected-files shards — so conformance is graded over every task's files and §3's agreement is what makes the split sound. If the skill keeps 'one shard' unqualified, say in the same sentence that files outside that shard are not graded for conformance."}
{"category":"ambiguity","class":"comparison-rule-unpinned","evidence":"Probe with audit_converge_on=blocking set by `tp set --workflow`: recording one FAIL/warning gave round 1, findings 1, clean True; recording FAIL/warning plus FAIL/WARNING on one id gave round 2, findings 2, clean False; `tp audit --merge` of that pair at HEAD gave merged_count 1, duplicates_removed 1, by_severity {warning:1}. engine.AuditSeverityBucket: a string outside error, warning and info is unrecognised. engine.AuditRowIsPass: status == \"PASS\". Round-2 tester finding (§2.2 d2, 'three readings') was resolved as 'spec repaired at 48a8a187: pins the order', and that order no longer exists.","finding":"Decision 2 collapses rows 'only when their verdicts agree', but it no longer says how the three components are compared. Round 2's finding on severity comparison was resolved by the order that round 3 removed, so it is open again in the current text. A normalising comparison, which is a plausible 'robust' build, reproduces round 3's high through another input and passes every §6 row, because rows 5 to 5f use only canonical values. Case-folded, 'warning' and 'WARNING' agree, so the first row (warning, advisory) is kept and the WARNING row is dropped. That row lands in the unrecognised bucket and blocks under audit_converge_on=blocking, so the round records clean. 'PASS' and 'pass' behave the same way under `all`, because AuditRowIsPass is exact. In the other direction, a raw JSON comparison reports PASS/null against PASS/absent as a conflict. tp's corpus writes PASS rows with severity null, and every other reading merges that pair, so tests of 'agreeing' rows split on it.","found_by":1,"found_by_roles":["tester"],"location":"§2.2 d2","role":"tester","severity":"medium","suggestion":"Pin the comparison in decision 2, for example 'components are compared exactly as recorded, with a missing value equal to null'. Add a row: two FAIL rows on one id, the first `warning` and the second `WARNING`, under blocking; both are kept and reported and the round records not clean, with the mutant 'compare case-insensitively'."}
{"category":"completeness","class":"clause-without-row","evidence":"Probe at HEAD with a recorded audit round 1 of 2 rows. `tp audit empty.ndjson --resolve-all wontfix \"accepted: x\"` gave exit 0, resolved_count 0, next_step 'tp audit \u003cspec\u003e --record empty.ndjson', both with TP_ROUND unset and with TP_ROUND=1. Row 10c's own fixture: `TP_ROUND=1 tp audit onerole.ndjson --resolve-all …` offered the step, and `TP_ROUND=1 tp audit spec.md --record onerole.ndjson` rewrote audit-round-1.ndjson from 2 rows to 1. review-round-3.ndjson: the tester's §4 empty-file finding has resolved.evidence '…a file with no rows matches no round; row 12j'.","finding":"The clause 'a file with no rows matches no round' has no §6 row, although it covers the most damaging input on the re-record path. Round 3's empty-file finding was resolved by citing row 12j, but row 12j tests key order on a non-empty file. Row 10c's HEAD value describes the empty file ('recording an empty file under TP_ROUND=1 turns round 1 into a clean round of zero rows'), yet its fixture is one role's rows, so that quoted value is not the value its own fixture produces. The mutant 'containment is vacuous for an empty file' passes every row: it lists every round of every searched spec in matching_rounds. At HEAD this input is reachable, it offers the step, and following the step empties the round.","found_by":1,"found_by_roles":["tester"],"location":"§4","role":"tester","severity":"medium","suggestion":"Add a row 10d: `--resolve-all` into an empty file, inside driver round 1 and outside a run. The payload names no --record, and outside a run matching_rounds is []. At HEAD the step is offered. Mutant: treat an empty file as contained in every round. Restate row 10c's HEAD value from its own fixture: following the step rewrites round 1 to that role's rows, 2 → 1 in a two-row round."}
{"category":"consistency","class":"counter-meaning-diluted","evidence":"internal/cli/audit_resolve.go:132-155: skipped_count increments only on rows carrying `resolved`, and stderr prints 'resolved %d audit rows as %s (%d already resolved, skipped)'. Spec §4 d4 ('counts them under skipped_count') and §6 row 14 ('resolved_count 1, skipped_count 1'). I counted with python over both round globs, canonical audit-round-N files: 115 rounds, 13,420 rows, 12,800 PASS (95.4%). spec/.tp-review/1.1.0/audit-round-4.ndjson has 55 of 63 PASS, and 1.0.1 round 3 has 58 of 66. The resolved round-1 ax-economist row 'unearned-payload-key' (§4 d4) suggested 'skipped_count keeps its meaning of already disposed'.","finding":"§4 d4 counts skipped PASS rows under the existing `skipped_count`, and row 14 pins `skipped_count 1` for one PASS row. Today that key, and the stderr line printed beside it ('(%d already resolved, skipped)'), mean 'rows that already carried a disposition'. That is the only thing that tells an agent some findings kept an earlier disposition, and so the only input to deciding whether `--force` is wanted. Recorded audit rounds are about 95% PASS, so after d4 the key is dominated by PASS rows on every --resolve-all. It would read about 55 on a typical round where 0 to 8 rows were really pre-disposed, and the stderr line would claim those 55 were 'already resolved'. That is a false statement on the very command §4 exists to make honest. The key stops carrying its decision, and nothing new replaces it. Round 1's repair of the separate PASS count left 'skipped_count keeps its meaning' as the alternative. Folding the PASS rows in reverses that.","found_by":1,"found_by_roles":["ax-economist"],"location":"§4","role":"ax-economist","severity":"medium","suggestion":"Have d4 count PASS rows nowhere. They are not candidates, so `skipped_count` stays 'already carried a disposition' and the stderr line stays true. Change row 14 to `resolved_count 1, skipped_count 0`, with the mutant 'count skipped PASS rows as skipped'. If a PASS figure is wanted at all, put it on the stderr line only, worded as PASS rather than as already resolved."}
{"category":"feasibility","class":"unbounded-round-state-walk","evidence":"internal/engine/projectconfig.go:480-491: with no .tp/ and no git boundary, ProjectRoot returns filepath.Abs(start). internal/engine/reviewstate.go:101-105: ReviewStateDir = \u003cspec-dir\u003e/.tp-review/\u003cbase\u003e, so there is no registry to consult instead of a walk. This session's own scratch directory holds several probe repositories, each with its own .tp-review/. Row 12h's case is met from the working directory's root, not from the file's.","finding":"Decision 3 searches 'every spec whose round state lives under the project root of the working directory or of the file'. Round state is a \u003cspec-dir\u003e/.tp-review/\u003cbase\u003e/ directory wherever the spec sits, so the search has to walk the whole root. For a directory that holds neither .tp/ nor .git, engine.ProjectRoot returns that directory itself. A merge output kept in ~ or /tmp, or an orchestrator whose cwd is $HOME, therefore makes every --resolve walk the home directory or /tmp, and the field report's workflow made 75 resolve calls. On macOS, walking ~ reaches permission-protected directories whose EACCES is neither 'a round state it cannot read', which must go to stderr, nor stated to be skippable. A scratch directory also holds other probes' recorded rounds with copied rows, and those then show up in matching_rounds. The walk's bound and its error rule for unreadable directories are left for the implementer to invent, and either choice is visible to the user.","found_by":1,"found_by_roles":["implementer"],"location":"§4 d3","role":"implementer","severity":"medium","suggestion":"Say that a directory adds its root to the search only when that root holds .tp/ or is a git boundary. A bare directory contributes nothing, and row 12h still passes through the working directory's root. Say what an unreadable directory met during the walk does, for example named on stderr like an unreadable state."}
{"category":"regression","class":"mutant-cannot-fail-its-row","evidence":"Read the current spec §2.1 decision 1b (lines 59-62) and §6 row 4 (line 193): the assertion is 'the prior-round section lists the row under the earlier-derivation line', the mutant is 'keep the earlier id for a file whose id carried no suffix'. Read round-3 row [8] (implementer, two-readings-of-answers-no-item, resolved fixed at f6ad4fc2 as 'd1b lists earlier-derivation rows apart under a line saying so; row 4 asserts that line'). Its finding text says reading (b) leaves row 4 green under its own mutant.","finding":"Row 4's mutant can no longer fail row 4. Round 3's 'two-readings-of-answers-no-item' finding (marked fixed) warned about exactly this. The repair makes decision 1b a rule on the shape of the prior row's own id: 'a file_check id that does not end in such a digest is of the earlier derivation … listed apart'. The listing therefore depends only on the recorded id and never on what the current derivation gives the file. Row 4's mutant, 'keep the earlier id for a file whose id carried no suffix', changes only the current checklist. The prior row's HEAD id still ends in no digest, so the section still lists it under the earlier-derivation line and row 4 stays green. Round 3's finding said reading (b), the scheme or shape rule, 'is what Non-Goal 5 presupposes, but under it row 4 stays green under its own mutant, because the row is marked whatever id the new derivation gives'. The repair adopted reading (b) and kept the mutant.","found_by":0,"location":"§6","role":"regression","severity":"medium","suggestion":"Name a mutant that reaches the listing: 'list an id without a digest with the other rows' (the shipped section), or 'treat any id as of the current derivation'. Keep 'keep the earlier id …' on a row that asserts the current checklist, e.g. row 1 or 2."}
{"category":"completeness","class":"mutant-cannot-redden-row","evidence":"python3 hashlib: sha256('tests/Services/Campaigns/RevenueBonusCampaign/F42057Test.php')[:8] == sha256('…/F48508Test.php')[:8] == 37076acc (True). The same two filenames under 'tests/Services/Campaigns/RevenueBonusCampaign' (no trailing slash), '', 'tests/', 'app/', 'tests/Feature/' and 'tests/Unit/' do not collide. Sidecar 'A candidate derivation, not a decision': 'whether the digest is eight characters or more, and which hash, belong to the implementing task'. Sidecar 'Built before review' names the pair only as '…/F42057Test.php', '…/F48508Test.php'.","finding":"Row 1d is the only row on decision 1's width bound, and its named mutant ('a 32-bit digest') reddens it only for one of the derivations decision 1 permits. The sidecar's pair collides in its first 32 bits only under SHA-256 of exactly the root-relative path string. Decision 1 leaves the hash, its input (the path alone, or role plus path, which 'a function of the role and the path' allows) and the encoding open, and the sidecar says 'which hash belong[s] to the implementing task'. Under FNV-64, SHA-1, or SHA-256 over role+path, 'a 32-bit digest' of that function gives the pair two ids and row 1d stays green. Separately, no row separates 64 bits from 40, 48 or 56: a 48-bit digest passes all of §6, so the stated lower bound has no test. The row's fixture is also not fully written down, because the sidecar elides the directory ('…/F42057Test.php').","found_by":1,"found_by_roles":["tester"],"location":"§6 row 1d","role":"tester","severity":"medium","suggestion":"Either pin the digest in decision 1 as SHA-256 of the cleaned root-relative path, lowercase hex, at least 16 characters, and make row 1d assert that the id's suffix equals sha256(path)[:N] with N \u003e= 16 (which also tests the width), naming the full directory; or keep the hash open, rewrite row 1d's fixture as 'a pair the implementing task finds by birthday search to collide in the first 32 bits of its own digest', and add a row asserting the suffix encodes at least 64 bits."}
{"category":"redundancy","class":"readable-part-names-no-file","evidence":"internal/cli/audit_roles.go:98-129: slug = slugifySubject(path + ' ' + text), truncated to 40 characters from the front. internal/cli/audit_schema.go:21: 'evidence_file: ... null for FAIL'. Probe in a fresh git-init repo (scratchpad/review/r4/ax.d96P/repo): `tp audit spec.md --role security --affected-files tests/Unit/Domain/Billing/Invoices/Listeners/AlphaTest.php,.../BetaTest.php` emitted ids file-security-tests-unit-domain-billing-invoices-liste and file-security-tests-unit-domain-billing-invoices-liste-2, so the readable part holds no part of either file name. With a 43-character directory, ...Billing/Invoices/AlphaListenerTest.php and BetaListenerTest.php gave '-alpha' and '-betal', so the file name survives only while the directory stays short.","finding":"Decision 1 requires the id to keep 'a readable prefix derived from the path', without saying which part of the path. The shipped slug takes the first 40 characters of the path plus the item text. In the case this spec exists for, a directory deeper than about 40 characters, that prefix is the directory, identical for every file in it. So the up-to-40 readable characters of every such id name no file, and the only part that tells the files apart is the opaque digest. Outside the emitted prompt, the id is the only pointer a FAIL row has to its file, because the audit output schema tells a grader to leave evidence_file null on a FAIL. An audit-fix unit or operator reading a FAIL row in the merge output or the recorded round file gets `file-security-tests-unit-domain-billing-invoices-liste-\u003cdigest\u003e` for every file in the directory. Recovering the file then means re-emitting the prompt, which writes the round snapshot, or hashing candidate paths. This release pins the id's shape anyway, so choosing the readable part costs nothing now and costs a spec change later.","found_by":1,"found_by_roles":["ax-economist"],"location":"§2.1","role":"ax-economist","severity":"low","suggestion":"Say in decision 1 that the readable part ends in the file's base name, for example the slug of the path's tail cut from the left, or the slug of the base name. Alternatively drop the readable-prefix clause and leave the prefix to the task. Add to row 1 (or 1e) that two files in a directory deeper than the cut get readable parts that differ, with the mutant 'cut the slug from the front'."}
{"category":"regression","class":"script-copy-of-binary-shape-unguarded","evidence":"Diff vs snapshot-round-3.md §2.1: removed 'A test holds the script's copy of the current value equal to the binary's'. Current §2.1 Consequences (lines 71-74) and d1 (line 57). Round-1 row 23 (implementer, §2, fixed at 8dafa6ed) text: 'the script … has no source for the current scheme … a copied literal that drifts'. scripts/audit-round-prep.py:46-50 (exit 2 = nothing to carry, which is also the first-round exit).","finding":"Removing id_scheme also removed the guard that round 2 wrote to answer round 1's 'no source for the current scheme' finding: 'A test holds the script's copy of the current value equal to the binary's'. The need for that guard is back. scripts/audit-round-prep.py must now decide, row by row, whether a file_check id 'ends in such a digest'. The spec fixes the digest only as 'fixed-length … of at least 64 bits' and leaves its length and encoding to the Go implementation. So the Python script has to hard-code the same length and alphabet, which is the 'copied literal that drifts' round 1's finding named. Nothing in §2.1 or §6 ties the two. If they differ in one direction, the script classifies every current id as earlier-derivation and exits 2 on every round. That exit is the one a first round gets, so the delta re-grade turns off silently. If they differ in the other direction, it carries rows no item holds. Row 4c would catch a mismatch only if its fixture's ids come from the built binary rather than from a hand-written shell fixture, and the row does not say which.","found_by":0,"location":"§2.1","role":"regression","severity":"low","suggestion":"Either say where the script learns the digest's shape (a tp output, or a test that holds the script's pattern equal to the binary's), or have row 4c's fixture record its round with the built binary so a mismatch reddens it."}
{"category":"ambiguity","class":"partition-predicate-unpinned","evidence":"Spec §2.1 d1b: 'a prior row carrying one answers no current item, and the prior-round section lists it apart from the rows that do, under a line saying its id is of the earlier derivation'. Round 3's implementer finding (resolved) records that the HEAD section 'prints the role's prior non-PASS rows … and never sees the current checklist'.","finding":"Decision 1b has the prior-round section list an earlier-derivation row 'apart from the rows that do' answer a current item. That describes the main list as rows that answer, but a third kind of row exists: a current-derivation row whose file is not in this round's list. A shard, or a file dropped by the cap, gives one. It answers no item and is not of the earlier derivation. If the section is partitioned by shape, the row goes in the main list, which is what HEAD does, since it lists every prior non-PASS row without consulting the checklist. If the section is partitioned by answering, the row is dropped or listed apart. For a prior FAIL on a file not named this round, two engineers therefore assert opposite outcomes.","found_by":1,"found_by_roles":["tester"],"location":"§2.1 d1b","role":"tester","severity":"low","suggestion":"Say which: 'every other prior row is listed as today, whether or not an item carries its id'. Or add a row with a prior current-derivation FAIL on a file absent from this round's list."}
{"category":"ambiguity","class":"digest-shape-not-delimited","evidence":"internal/cli/audit_roles.go:98-149 (the id is file-\u003crole\u003e-\u003cslugifySubject(path + ' ' + text)\u003e; the slug is lowercased, non-alphanumeric runs become '-', cut at 40). A Python replica of slugifySubject on 'public/build/assets/app-0123456789abcdef.js' prints file-security-public-build-assets-app-0123456789abcdef. The sidecar's *A candidate derivation* gives `file-\u003crole\u003e-\u003cslug40\u003e-\u003chex\u003e`.","finding":"Decision 1b tells an id of the earlier derivation apart by whether it 'ends in such a digest', and the Why says that shape works 'row by row, whichever binary recorded the round'. That holds only if the digest's shape is one the earlier derivation cannot produce, and the spec does not require that. The earlier slug uses the alphabet [a-z0-9-] and is cut at 40 characters. Take public/build/assets/app-0123456789abcdef.js: its earlier-derivation id is file-security-public-build-assets-app-0123456789abcdef. That id ends in '-' plus 16 hex characters, which is exactly the shape a 64-bit hex digest takes under the sidecar's candidate `-\u003chex\u003e` form. A shape classifier reads that row as current-derivation. It then answers no item but is listed among the rows that do, and audit-round-prep.py does not treat it as earlier. Two classifiers, one in Go and one in the Python script, have to agree on the shape, and the spec does not say where it is defined.","found_by":1,"found_by_roles":["implementer"],"location":"§2.1 d1b","role":"implementer","severity":"low","suggestion":"Require the digest to be set off by a character the earlier slug never emits, meaning anything outside [a-z0-9-], or by a fixed marker. State that audit-round-prep.py reads that definition from the same source as the binary. Add a §6 row whose earlier-derivation id ends in 16 hex characters."}
{"category":"completeness","class":"group-located-two-ways","evidence":"Spec §2.2 d2: conflicts names 'every row's input, line and verdict'; the selector 'refuses with exit 2 and names their indexes'. SKILL.md:677: the --resolve selector is 'a 0-based index or the row's role:item_id key'. Sidecar Review round 2: the round file is a byte copy of --record's input. §6 row 5 asserts that conflicts 'names both input lines' and row 6 that the refusal 'names both indexes'. No row asserts an output index in conflicts or a verdict in the refusal.","finding":"Decision 2 describes one group of rows in two shapes, and neither lets the agent act on it directly. `--merge`'s `conflicts` names each row's input file, line and verdict. The rows are then disposed in the recorded round file, which is a byte copy of the merge output (-o), by 0-based index, and the conflicts entry does not give that index. The `--resolve role:item_id` refusal names the indexes but not the verdicts. An agent that reads conflicts and decides which row to dispose, say the shard-artefact PARTIAL beside a PASS (row 5f), has two routes. It can map input:line to an output index by reading the merged file. Or it can issue the selector, take the exit 2, and then read the file to learn which index holds which verdict. Either way it pays an extra read per group before its first valid --resolve. The facts that feed the one decision — which row to dispose — are split across two outputs, and the location schemes differ (input line and output index).","found_by":1,"found_by_roles":["ax-economist"],"location":"§2.2","role":"ax-economist","severity":"low","suggestion":"Give each conflicts member its index in the -o file too, since that is the index in the recorded round. Give the refusal each named row's verdict (status, severity, disposition status), in the same member shape as conflicts. Then either output alone is enough for the next --resolve \u003cindex\u003e. Extend rows 5 and 6 accordingly, with the mutants 'input line only' and 'indexes only'."}
{"category":"ambiguity","class":"report-channel-unpinned","evidence":"Probe at HEAD: `tp audit --merge merged.ndjson` (piped, no -o) printed the rows on stdout and 'merged: 2 rows from 1 files (0 duplicates removed, 1 non-PASS)' on stderr. With -o /dev/null, stdout was the JSON summary (by_role, by_status, merged_count, …). With --json and no -o, the JSON summary had output_path 'stdout'.","finding":"Decision 2 reports each conflict group 'under a conflicts key', but `tp audit --merge` prints a JSON summary only with -o or --json. Without them, and piped, stdout carries the merged rows and stderr a one-line human summary. The decision does not say whether that mode reports a group at all, for example on the stderr line. Row 5 does not pin -o, so one harness sees the key and another sees rows with no report. That is a silent group in the mode that most resembles 'merge, then pipe'.","found_by":1,"found_by_roles":["tester"],"location":"§2.2 d2","role":"tester","severity":"low","suggestion":"Say that the stderr summary line counts the conflict groups whenever no JSON summary is printed, or that conflicts is reported only in the JSON summary. Pin row 5 to `-o`."}
{"category":"ambiguity","class":"payload-key-presence-unstated","evidence":"Diff of spec vs snapshot-round-3.md, §4 d3: removed '… a resolve into one carries `recorded_round: true` and no `matching_rounds`'; added 'Outside a driver round, …' as the scope of the recorded_round: false sentence and 'Inside a driver round no search runs and the payload carries neither `matching_rounds` nor the statement'. Rows 12g and 13 (lines 218, 222). Round-3 row [20] (ax-economist) finding text.","finding":"Restructuring §4 decision 3 left two payload cells unstated. (1) A resolve into a recorded round file outside a driver round: round 3's text said it carries 'recorded_round: true and no matching_rounds'. The repair kept only 'carries recorded_round: true'. A containment search over a recorded round always matches that round, so one build will print matching_rounds naming the file itself and another will not. Row 13 does not decide. (2) Inside a driver round: the repair says the payload carries neither matching_rounds nor the statement, but says nothing about recorded_round. Every sentence that mentions recorded_round: false is scoped 'Outside a driver round'. Round 3's 'in-run-payload-feeds-no-decision' finding (fixed) objected to recorded_round: false in-run as well ('\"false plus a path\" reads as \"your write missed\"'), and row 12g asserts only the two absences.","found_by":0,"location":"§4","role":"regression","severity":"low","suggestion":"State both cells: a recorded round file carries recorded_round: true and no matching_rounds; inside a driver round the payload carries no recorded_round either, or carries it and say why. Let rows 12g and 13 assert the key's absence."}
{"category":"regression","class":"changed-payload-key-unpinned","evidence":"internal/cli/audit_resolve.go:149 `fmt.Fprintf(os.Stderr, \"resolved %d audit rows as %s (%d already resolved, skipped)\\n\", resolvedCount, status, skippedCount)` and :152 skipped_count: skippedCount. Current §4 d4 (lines 157-158) and row 14 (line 224). Round-3 row [37] (tester), resolved fixed as 'd4 counts skipped PASS rows under skipped_count; row 14 asserts it'.","finding":"Round 3's 'changed-payload-key-unpinned' finding, marked fixed, named two outcomes. If a build counts a skipped PASS row in skipped_count, the stderr text becomes false for it. If it leaves the row out, resolved_count + skipped_count no longer equals the row count. The repair picked the first build ('counts them under skipped_count') and left the stderr line alone. At HEAD that line prints skippedCount as '(N already resolved, skipped)'. Under decision 4, row 14's fixture (one FAIL, one undisposed PASS) prints 'resolved 1 audit rows as wontfix (1 already resolved, skipped)', and the PASS row it counts is not resolved. That is exactly the false text the finding described.","found_by":0,"location":"§4","role":"regression","severity":"low","suggestion":"Have d4 also say that the stderr line names PASS rows as skipped, not already resolved, or that it counts them apart from the rows that were already resolved."}
{"category":"ambiguity","class":"collection-equality-unpinned","evidence":"Spec §4 Decisions preamble ('compared as JSON values, dispositions aside, never as bytes') and d2/d3. Sidecar Review round 2: 'two identical FAIL rows record as two findings at HEAD'. §2.2 d2: '--record records every row, as today'.","finding":"§4 pins value-versus-bytes comparison for rows but does not say whether decision 2's 'the file's rows are exactly the rows of that spec's round' and decision 3's 'whose rows include every row of the file' compare sequences, sets or multisets. The readings split on inputs a unit can produce. First, a copy of round 1 with its rows reordered: sequence equality withholds the step and set equality offers it. Second, a copy of round 1 with one FAIL row duplicated, as a hand-concatenated file produces: set equality offers the step, and following it records the duplicate as an extra finding, because --record keeps every row. The decision's own reason, 'recording it would replace the round with that part', applies to that case too.","found_by":1,"found_by_roles":["tester"],"location":"§4 d2","role":"tester","severity":"low","suggestion":"State 'exactly the rows, as a multiset, in any order' for d2 and 'every row of the file, counted with multiplicity' (or plain set containment) for d3. Add a row for the duplicated-row copy inside driver round 1, with the mutant 'compare as sets'."}
{"category":"ambiguity","class":"payload-key-presence-unpinned","evidence":"Spec §4 d3: 'a resolve into one carries recorded_round: true. Inside a driver round no search runs and the payload carries neither matching_rounds nor the statement'. §6 row 12g asserts only that matching_rounds and the stderr statement are absent. Row 13 asserts recorded_round: true and silence on stderr.","finding":"Decision 3 leaves two cases of the resolve payload's key set open. (1) Inside a driver round it says only that 'no search runs and the payload carries neither matching_rounds nor the statement'. It does not say whether recorded_round is present. Computing it needs the recorded-round listing, which is exactly what TP_FILE's spec state provides in a run. (2) Outside a run, for a resolve into a recorded round, it says the payload carries recorded_round: true. It does not say whether matching_rounds is present, empty or absent. Rows 12g and 13 assert neither. Two conforming implementations therefore give an agent different key sets, and the key an agent branches on is recorded_round.","found_by":2,"found_by_roles":["implementer","tester"],"location":"§4 d3","role":"implementer","severity":"low","suggestion":"State both outright. For example: recorded_round is present on every resolve payload, including inside a driver round, and matching_rounds appears only when recorded_round is false outside a driver round. Extend rows 12g and 13 to assert it."}
{"category":"ambiguity","class":"symlink-spelling-unpinned","evidence":"ls -ld /var: symlink to private/var. Spec §4 d3: 'as an absolute path' and 'compared after resolving symlinks'. round-2 tester §4 d3 finding (resolved 'a path relative to its repository root'); round-3 resolution 'matching_rounds elements are absolute paths'.","finding":"Each matching_rounds element is 'a recorded round file, as an absolute path', and recorded rounds are 'compared after resolving symlinks'. The spec does not say whether the element is the symlink-resolved path or the path as the round state and root spell it. This is the part of round 2's form finding that survived: round 2 fixed it by making elements repo-relative, and round 3 replaced that with absolute paths and did not say which spelling. On macOS t.TempDir() is under /var, a symlink to /private/var. A Go test that asserts equality with filepath.Join(t.TempDir(), …) passes a lexical build and fails a resolving one. Both are conforming, so rows 12, 12c, 12f and 12h are written two ways.","found_by":1,"found_by_roles":["tester"],"location":"§4 d3","role":"tester","severity":"low","suggestion":"Pin the spelling: 'each element is the round file's path with symlinks resolved'. Make rows 12 and 13b compare against filepath.EvalSymlinks of the expected path."}
{"category":"consistency","class":"degraded-read-reported-as-fact","evidence":"Read: spec §4 d3 ('a round state it cannot read is named on stderr, never skipped in silence'; 'A recorded round is a file its spec's round state lists as a round's file'; 'a resolve into one carries recorded_round: true') and §6 row 12i (exit 0). skills/tp/REFERENCE.md:647 ('A directory holding round files with no state.json aborts state-reading commands with exit 3 and a repair hint — that index referenced recorded history, and tp never rebuilds over it').","finding":"§4 d3 makes --resolve a state-reading command that exits 0 over an unreadable round state (row 12i), where the documented .tp-review/ contract is that a directory holding round files with no state.json 'aborts state-reading commands with exit 3'. The spec neither says it is an exception to that contract nor says what the JSON carries when the unreadable state is the target's own. Resolve into .tp-review/\u003cbase\u003e/audit-round-1.ndjson whose state.json is missing: the file is a recorded round, but 'a recorded round is a file its spec's round state lists', the list cannot be read, so the payload says `recorded_round: false` and `matching_rounds: []` — a definite machine-readable claim that the write reached no round, with the only qualification on stderr. The agent reads the JSON (P1); the §4 Context's own defect is a success payload that reads normally while being wrong.","found_by":1,"found_by_roles":["architect"],"location":"§4 d3","role":"architect","severity":"low","suggestion":"State in d3 that the resolve is an exception to the exit-3 rule because the round state is read only to inform, and say what the payload carries when the file lies in a round directory whose state cannot be read — e.g. omit recorded_round (or set it null) and name the unreadable state in the JSON as well as on stderr — with a §6 row for the target's own state missing."}
{"category":"consistency","class":"payload-key-meaning-drift","evidence":"Read: internal/cli/audit_resolve.go:101-156 (skippedCount increments only for rows with an existing `resolved`; stderr 'resolved %d audit rows as %s (%d already resolved, skipped)'; doc comment 'tp review --resolve-all's behaviour on the audit artifact'). Spec §4 d4 ('counts them under skipped_count') and §6 row 14 (resolved_count 1, skipped_count 1 for one FAIL and one PASS).","finding":"§4 d4 counts skipped PASS rows under the existing `skipped_count`, whose meaning today is 'rows already carrying a disposition': the same command's stderr line reads 'resolved %d audit rows as %s (%d already resolved, skipped)', and runAuditResolveAll is documented as tp review --resolve-all's behaviour on the audit artifact, where skipped_count means only already-resolved. After d4 one counter carries two reasons, the audit and review siblings disagree on what it means, and under --force (which d4 says still skips PASS rows) a nonzero skipped_count no longer implies anything was already resolved. A file of 74 PASS and 1 FAIL resolved with --resolve-all reports 'resolved 1 … (74 already resolved, skipped)', which is false.","found_by":1,"found_by_roles":["architect"],"location":"§4 d4","role":"architect","severity":"low","suggestion":"Either count PASS rows under their own key (e.g. `pass_skipped_count`) and leave skipped_count meaning already-resolved, or say in d4 that skipped_count's meaning widens and that the stderr line names both reasons; row 14 then asserts whichever is chosen."}
{"category":"completeness","class":"mutant-depends-on-unpinned-fixture","evidence":"python3: os.path.commonprefix of the two paths is 48 characters; sha256(path)[:8] is 37076acc for both. Current §2.1 d1 (lines 50-57) and row 1d (line 189). Round-3 rows [4], [7], [12], resolved fixed as 'row 1d is the measured 32-bit pair'.","finding":"Row 1d, added for round 3's 'no row can fail the 8-hex mutant' findings, fails its own mutant ('a 32-bit digest') only when the id's readable prefix is identical for both paths. Decision 1 leaves the prefix unpinned ('keeps a readable prefix derived from the path'). The sidecar's pair, tests/Services/Campaigns/RevenueBonusCampaign/F42057Test.php and F48508Test.php, shares its first 48 characters and differs after that. With HEAD's 40-character cut the prefixes coincide, so the digest decides and the mutant reddens. With any prefix longer than about 48 slug characters, or an uncut one, the ids differ whatever the digest's width, so a build carrying a 32-bit digest passes row 1d and every other row. That is the build the fixed findings said §6 could not catch.","found_by":0,"location":"§6","role":"regression","severity":"low","suggestion":"Pin the prefix in row 1d's fixture (e.g. 'with a prefix the two paths share'), or require the prefix to be cut at a length the pair shares, or name the mutant as 'a 32-bit digest with the prefix cut at 40'."}
{"category":"completeness","class":"fixture-underspecified","evidence":"Sidecar #27 Run B: 'emitted as two shards of two with --role security --affected-files'. scripts/audit-round-prep.py docstring: 'Exit … 2 when there is nothing to carry', with fail(2, error=…) cases no-round-directory, no-audit-round and unrecorded-round. Spec §2.2 d2: 'refuses with exit 2 and names their indexes'.","finding":"Three rows leave out a fixture property on which their assertion depends. Row 3 expects 'four distinct ids' from two shards of two files but does not name the role panel. The sidecar's Run B used --role security, while the default panel gives three code roles × four files plus spec-coverage's shared items. Row 4b's 'previous round recorded at HEAD' must hold a file_check row. A HEAD round of spec-coverage rows only has no earlier-derivation row, so a conforming build carries it and fails the row, and 'exits as it does with nothing to carry' has three distinct error codes in the script (no-round-directory, no-audit-round, unrecorded-round). Row 6 asserts exit 2 and both indexes but not that the refused file is unchanged, so the mutant 'dispose every match, then exit 2' passes it.","found_by":1,"found_by_roles":["tester"],"location":"§6","role":"tester","severity":"low","suggestion":"Row 3: add '--role security'. Row 4b: 'a previous round recorded at HEAD holding a file_check PASS row on an untouched file: exit 2, stdout empty', and name the error code, or say that any is acceptable. Row 6: add 'and the file is byte-identical after the refusal'."}
