{"unit_id":"u3","anchor":"§1","text_sha":"dbfa39a3cb02","ordinal":1,"verdict":"PASS","kind":"corpus","tier":"query","evidence":"`git log -S 'Dogfood the in-progress binary' --format=%H -- CLAUDE.md | wc -l` returns 1, and the commit is 3e1a3f0a8e855e36e988bd9cb17c4ea7a90525c8 (`docs(claude): dogfood the in-progress binary during self-development`, 2026-07-21). `git describe --contains 3e1a3f0a` returns `v0.24.0~84`, so the first containing tag is v0.24.0. `git show v0.23.0:CLAUDE.md | grep -c 'Dogfood the in-progress binary'` returns 0; the same command at v0.24.0 returns 1; `grep -c` on CLAUDE.md at HEAD returns 1.","note":"Every one of the three commands the sentence quotes was run and every one returns what the sentence says. `git tag --contains 3e1a3f0a | head -1` is also v0.24.0, so 'first containing tag' holds under both the describe and the tag-list reading. Counting rule for 'has carried it since': -S counts commits that CHANGE the number of occurrences of the string, so exactly one such commit plus presence at HEAD means the line was added once and never removed — continuity, not merely first appearance."}
{"unit_id":"u12","anchor":"§2","text_sha":"b36276e4c194","ordinal":1,"verdict":"PARTIAL","partial_kind":"two-readings","kind":"behaviour","tier":"run","evidence":"Sourcing lines 1-79 of `hooks/session-start.sh`: `version_below 1.0.1 1.44.0` returns 0 (true, below). A clean clone outside the repository checked out at HEAD (941d1b66) and built with `go build -o tp ./cmd/tp` reports `tp version v1.0.1-0.20260904033311-941d1b66ef29`, so `version_number` reduces it to `1.0.1` and the refuted predicate fires. The 'only shipped comparator' half is contradicted by this spec's own §4: `grep -n 'comparePluginVersions' internal/cli/plugin_test.go` returns a second dotted-numeric comparator, which §4's own u63 calls one of 'two version comparators'.","note":"The operative conclusion — the refuted predicate fires on a binary built seconds earlier from HEAD — is fully reproduced. What does not survive is the quantifier 'only'. §4 (u56) describes `comparePluginVersions` as used by 'three SHIPPED tests', so within this document 'shipped' already covers test code; under that reading the repository holds two shipped comparators, not one. The claim is true only under an unstated narrowing of 'shipped' to production code paths. CLAUDE.md's own standing rule — never assert a quantifier you did not count — is what makes this worth a row rather than a note."}
{"unit_id":"u13","anchor":"§2","text_sha":"3288bca39543","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"Built c9f19088623a in a clone outside the repository: `tp version v1.0.1-0.20260904005015-c9f19088623a` and `go version -m` prints `build vcs.time=2026-09-04T00:50:15Z`. `git log -1 --format='%ci' c9f19088623a` returns `2026-09-04 03:50:15 +0300`, the same instant in UTC — so the embedded timestamp is that commit's commit time, not a build time.","note":"Reproduced independently of the transcript: the +dirty suffix in §2's transcript does not change vcs.time, which confirms the stronger form of the claim — a dirty build at that commit and a clean build at that commit carry the identical timestamp, so build recency is not recoverable from the version string. The pseudo-version's own `20260904005015` field equals the same instant, so the claim holds for both the version string and the BuildInfo setting."}
{"unit_id":"u15","anchor":"§2","text_sha":"0e7692faaf57","ordinal":1,"verdict":"NOT-A-CLAIM","note":"The header row of §2's table. It names three columns and asserts nothing about the world; the assertions are in u16 and u17, which are graded."}
{"unit_id":"u16","anchor":"§2","text_sha":"d7da7ebbe378","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"In a clone of this repository outside it: `git checkout 65d0b09f2800 && go build -o tp-a ./cmd/tp` then `./tp-a --version` prints `tp version v0.37.1-0.20260902095645-65d0b09f2800`, and `go version -m tp-a` prints `build vcs.revision=65d0b09f280084b9b6b4bb4588ef217c12701419`. `git rev-parse 65d0b09f2800` returns the same 40-character sha.","note":"All three cells reproduce byte for byte, including the pseudo-version's timestamp field."}
{"unit_id":"u17","anchor":"§2","text_sha":"f3e725f0fe4e","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"Same clone: `git checkout 10dee489714a && go build -o tp-b ./cmd/tp` then `./tp-b --version` prints `tp version v0.37.1-0.20260902102453-10dee489714a`, and `go version -m tp-b` prints `build vcs.revision=10dee489714a8522e6f3609c82c9dd76187c1501`. `git rev-parse 10dee489714a` returns the same 40-character sha.","note":"All three cells reproduce byte for byte."}
{"unit_id":"u18","anchor":"§2","text_sha":"ea0a337c9f46","ordinal":1,"verdict":"PASS","kind":"corpus","tier":"query","evidence":"`git rev-list --count 65d0b09f2800..10dee489714a` returns 11; `git rev-list --count --first-parent 65d0b09f2800..10dee489714a` returns 11.","note":"This is the figure the repair rewrote (the commit that carries the repair is titled '1.44.0's seven commits are eleven'), so it is exactly the sentence this round was asked to re-derive: both forms return 11. Counting rule: `rev-list --count A..B` counts commits reachable from B and not from A, which excludes the endpoint A and includes B — the distance between the two builds, not the number of commits between them exclusive of both. The two forms agreeing means the range is linear, which is why the qualifier adds nothing here but is not wrong."}
{"unit_id":"u19","anchor":"§2","text_sha":"99d0ed45c4bc","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"I ran precisely the stated recipe in a fresh `git clone` of this repository placed outside it — `git checkout <rev> && go build ./cmd/tp`, then `go version -m` on each result — for both revisions, and both rows of §2's table reproduce exactly (see u16 and u17). No step of the stated recipe is missing or superfluous.","note":"A methodology sentence graded by re-executing it rather than by reading it. The recipe as written is sufficient: nothing else (no ldflags, no GOFLAGS, no tag fetch) was needed to obtain the table's values."}
{"unit_id":"u20","anchor":"§2","text_sha":"23777a57467f","ordinal":1,"verdict":"FAIL","kind":"behaviour","tier":"run","evidence":"This is a CUT unit whose text the arms dropped; text_sha computed by me as the first 12 hex of sha256 over its own canonicalised text (algorithm confirmed against u3, whose text hashes to the index's dbfa39a3cb02). Its text: '**The two version strings differ, and they differ in exactly the revision.** A string comparison of the second column reports them different, and the third column can be read straight off the second.' Two measurements against §2's own table. (1) The two strings are `v0.37.1-0.20260902095645-65d0b09f2800` and `v0.37.1-0.20260902102453-10dee489714a`: they differ in the pseudo-version TIMESTAMP field (20260902095645 vs 20260902102453) as well as the revision field, so they do not 'differ in exactly the revision'. (2) The third column is the full 40-character sha `65d0b09f280084b9b6b4bb4588ef217c12701419`; the second column carries only the 12-character prefix `65d0b09f2800`. 40 characters cannot be read off 12, and §3's own u40 insists the value is 'the full 40-character sha ... not a short one'.","note":"Filed against its own unit_id because the arms produced this unit and cut it — this is the third grounded spec running in which the sharpest finding sat in a cut unit, and in 1.44.0's round 1 it was two cut units carrying the section's headline measurement. Neither half survives. Half (2) has no charitable reading available inside this document: the sentence says the third column, u40 says the third column is 40 characters, and the second column contains 12. Half (1) is defensible only if 'differ in exactly the revision' is read as 'differ only in fields determined by the revision' — but the spec never says that, and u13 (which does establish that the timestamp is the commit's) is a DIFFERENT sentence three paragraphs earlier that this one does not invoke. The conclusion the paragraph needs — the discriminator is present in the version string and the prefix comparison throws it away — survives and is stated correctly in u21 and u22, so the fix is to delete or restate this sentence, not to rescue it."}
{"unit_id":"u21","anchor":"§2","text_sha":"8f24a0d6e460","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"Sourcing the hook's functions: `version_number v0.37.1-0.20260902095645-65d0b09f2800` prints `0.37.1` and `version_number v0.37.1-0.20260902102453-10dee489714a` prints `0.37.1`. The implementation at `hooks/session-start.sh:51` is `sed -e 's/^[vV]//' -e 's/[-+].*$//'`.","note":"The claim reproduces exactly for the two strings it is about. Two imprecisions in the description of the mechanism, neither material to the conclusion: the sed also strips a leading `v`/`V`, and it strips from the first `-` OR the first `+`, whichever comes first — for a pseudo-version the `-` is always first, so the stated behaviour is what happens here."}
{"unit_id":"u24","anchor":"§2","text_sha":"74e3e6a9faea","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`internal/cli/root.go:15` declares `version = \"dev\"` as a package-level var, `:55` is `if version == \"dev\" {`, and `:56` is the `debug.ReadBuildInfo()` call — so a build that sets `version` through `-ldflags -X` skips the block entirely. For the second qualification I ran it: `go build -ldflags=\"-s -w\" -o tp-stripped ./cmd/tp` in a clone, and `go version -m tp-stripped | grep -c 'build.*vcs'` returns 4 — `vcs=git`, `vcs.revision`, `vcs.time`, `vcs.modified` all present.","note":"Both qualifications hold. One wording note, not a defect in substance: the four entries counted are `vcs`, `vcs.revision`, `vcs.time` and `vcs.modified`; only three of the four literally match the glob `vcs.*` written in the sentence, since the first has no dot. The count of four is right and the survival claim is right."}
{"unit_id":"u27","anchor":"§3","text_sha":"38d47354c508","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"The row's advisory text asserts that a binary with no `vcs.revision` and a real `Main.Version` is a `go install`ed binary and not a build of this tree. I built the tree five ways in copies outside the repository and read each with `go version -m`: `go build -buildvcs=false` at HEAD → `(devel)`; `go build` in an rsync copy with `.git` removed → `(devel)`; `go build -buildvcs=false` at the v1.0.0 TAG commit → `(devel)`; `go build` in a `.git`-less copy of the v1.0.0 tag checkout → `(devel)`; `GOBIN=… go install github.com/deligoez/tp/cmd/tp@v1.0.0` → `Main.Version` `v1.0.0` with no vcs settings at all.","note":"I deliberately built the sharpest edge the spec does not itself probe — a vcs-less build at the exact release tag, which is the one place a build of this tree might plausibly claim a real module version — and Go still reports `(devel)`. So the conjunction (absent revision AND real Main.Version) has no false-positive construction available from this tree, and the advisory's assertion is sound rather than merely plausible. Note that a `go build` AT the tag with vcs on does report `Main.Version` `v1.0.0`, but it also carries `vcs.revision=dbf7fdeb…`, so it never reaches this row."}
{"unit_id":"u28","anchor":"§3","text_sha":"d99567a9cfda","ordinal":1,"verdict":"NOT-A-CLAIM","note":"A row of the predicate table: it states a condition and the decision that the advisory says nothing in that state. It asserts nothing about the world beyond the condition's own terms; the factual support for why the row must exist is u31, which is graded."}
{"unit_id":"u29","anchor":"§3","text_sha":"889da938d606","ordinal":1,"verdict":"NOT-A-CLAIM","note":"A row of the predicate table: condition plus the decision about what the advisory names. No assertion about the world."}
{"unit_id":"u31","anchor":"§3","text_sha":"513a561532aa","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Four binaries with no `vcs.revision` were built or installed outside the repository: `go install …@v1.0.0` (`Main.Version` `v1.0.0`), `go build -buildvcs=false` inside a checkout (`(devel)`), `go build` in a `.git`-less copy (`(devel)`), and `go build -buildvcs=false` at the v1.0.0 tag (`(devel)`). Three of the four are builds of this tree, so a missing revision alone does not identify a release.","note":"Directly probed rather than reasoned. The claim is exactly right and the second row is load-bearing: without it the release advisory would fire on three of the four constructions I built."}
{"unit_id":"u32","anchor":"§3","text_sha":"c8c5b157cfe3","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"I reproduced all three probes outside the repository and read each with `go version -m <binary>`, obtaining §3's table exactly (see u34, u35, u36). The reading method the sentence names is the one that yields the `vcs.revision` and `Main.Version` columns.","note":"One imprecision worth recording without changing the verdict: the first of the three is `go install github.com/deligoez/tp/cmd/tp@v1.0.0`, which does not build the copy of the tree at all — it fetches the tagged module from the proxy. 'Probed three ways in a copy of the tree' is accurate for probes 2 and 3 and true of probe 1 only in the weaker sense that the command was run from a working directory outside the repository. The three results are what matters and all three reproduce."}
{"unit_id":"u33","anchor":"§3","text_sha":"d16a7ebf120d","ordinal":1,"verdict":"NOT-A-CLAIM","note":"The header row of §3's probe table. Four column names, no assertion."}
{"unit_id":"u34","anchor":"§3","text_sha":"3981714b2a02","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"`GOBIN=<scratch>/gobin go install github.com/deligoez/tp/cmd/tp@v1.0.0`, then `$GOBIN/tp --version` prints `tp version v1.0.0` and `go version -m $GOBIN/tp` prints `mod github.com/deligoez/tp v1.0.0 h1:bkv/…` with no `vcs` line of any kind.","note":"Run into a scratch GOBIN rather than trusting the tp already on PATH, so the row is verified by the command the row names rather than by an artifact that happened to be present. The same three cells also hold for the installed binary at /Users/deligoez/go/bin/tp, which is what §4's u51 rests on."}
{"unit_id":"u35","anchor":"§3","text_sha":"2bb80a1f50a0","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"In a clone checked out at HEAD: `go build -buildvcs=false -o tp-novcs ./cmd/tp`; `go version -m tp-novcs` prints `mod github.com/deligoez/tp (devel)` and no `vcs` settings; `./tp-novcs --version` prints `tp version dev`.","note":"All three cells reproduce."}
{"unit_id":"u36","anchor":"§3","text_sha":"92d0173b40c9","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"`rsync -a --exclude .git clone/ nogit/` then `go build -o tp-nogit ./cmd/tp` inside the copy: `go version -m tp-nogit` prints `mod github.com/deligoez/tp (devel)` and no `vcs` settings; `./tp-nogit --version` prints `tp version dev`.","note":"All three cells reproduce. Together with u35 this is the pair that makes the unattributable row necessary: two distinct build routes, identical on both signals."}
{"unit_id":"u37","anchor":"§3","text_sha":"115b64f9efcf","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"Both builds are of this tree by construction (u35 is a `go build` inside the checkout; u36 is an rsync copy of it). `internal/cli/root.go:56` reads `if info, ok := debug.ReadBuildInfo(); ok && info.Main.Version != \"\" && info.Main.Version != \"(devel)\"`, so `(devel)` is rejected and `version` stays `\"dev\"` — which is what both binaries print.","note":"Every clause checks out, including the causal one: the `(devel)` rejection at root.go:56 is literally why `--version` prints `dev` rather than a module version, and there is indeed no installed version available for a release advisory to name."}
{"unit_id":"u38","anchor":"§3","text_sha":"fe0ae688b507","ordinal":1,"verdict":"PARTIAL","partial_kind":"two-readings","kind":"behaviour","tier":"run","evidence":"`Main.Version` across §3's three probes: `v1.0.0`, `(devel)`, `(devel)`. It separates row 1 from rows 2 and 3; it does not separate row 2 from row 3, which are identical on both signals. The co-location claim is exact: `go version -m` prints the `mod` line (`debug.BuildInfo.Main`) and the `build vcs.*` lines (`debug.BuildInfo.Settings`) from the same `BuildInfo`, and `root.go:56` reaches `info.Main.Version` through the same `debug.ReadBuildInfo()` call.","note":"True under 'separates the three builds into the states the predicate needs' (two states, and rows 2 and 3 belong to the same one); false under the literal reading that the signal distinguishes three things. The literal reading is the one the document's own next sentence forecloses — §3 goes on to say tp 'cannot tell whether such a build is current or stale', i.e. rows 2 and 3 are NOT separable. The second half of the unit, that Main.Version sits in the same BuildInfo as the revision, is exactly right and is the part the design rests on."}
{"unit_id":"u40","anchor":"§3","text_sha":"3445de6fb3e6","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"`go version -m` on the binary built at 65d0b09f2800 prints `build vcs.revision=65d0b09f280084b9b6b4bb4588ef217c12701419` — 40 hex characters, quoted in the spec verbatim and matching `git rev-parse 65d0b09f2800`. The same holds for every VCS-stamped binary I built (HEAD: `941d1b66ef2978bf9e32ecdd6de95d8600c6b9d7`).","note":"The first clause ('truncated by tp for display') is a decision about unimplemented behaviour; the graded assertion is the second, and it reproduces exactly. This unit is also what makes u20's 'the third column can be read straight off the second' unrescuable: 40 characters against a 12-character prefix, in the same document."}
{"unit_id":"u42","anchor":"§3","text_sha":"0608137b67de","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"In a clone at HEAD I appended a comment to `cmd/tp/main.go` and rebuilt: `go version -m` prints `vcs.revision=941d1b66ef2978bf9e32ecdd6de95d8600c6b9d7` (equal to HEAD) together with `vcs.modified=true`, and `--version` prints `v1.0.1-0.20260904033311-941d1b66ef29+dirty`. The clean build at the same commit prints the identical revision with `vcs.modified=false`.","note":"Probed in the clone, never in the repository. The separability claim is demonstrated by the pair rather than argued: the two builds differ in `vcs.modified` alone while the revision half of the staleness test is identical, so triggering on the flag would fire on a binary the predicate itself calls current."}
{"unit_id":"u44","anchor":"§3","text_sha":"c0de71696c58","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"query","evidence":"`grep -rn 'exec.Command(\"git\"' --include='*.go' internal/ cmd/ | grep -v _test.go | grep -v testdata | wc -l` returns 16. Reading all 16: they are in config_extract.go (1), audit.go (3), commit.go (6), audit_roles.go (1), engine/auditfiles.go (1), engine/worktree.go (3) — every one an `os/exec` invocation of the git binary, so 'every one a subprocess' holds by inspection of all sixteen and not by sampling.","note":"The quantifier is one I actually enumerated rather than counted. The reasoning the figure supports also holds: since the predicate needs HEAD on every invocation and tp has no in-process git, a commit distance is a second subprocess, and the sentence is careful to call its own cost argument weak."}
{"unit_id":"u45","anchor":"§3","text_sha":"bcb03a3b7e39","ordinal":1,"verdict":"NOT-A-CLAIM","kind":"code-structure","tier":"read","evidence":"The unit states the design of an unimplemented gate. Its one checkable premise is that BuildInfo carries a module path, and it does: `go version -m` on any tp binary prints `path github.com/deligoez/tp/cmd/tp` and `mod github.com/deligoez/tp`, both fields of the `debug.BuildInfo` that `internal/cli/root.go:56` already reads.","note":"Graded as a decision because 'A repository that is not tp sees nothing' and 'the gate compares X against Y' describe what this release will do, not what the world contains. The premise it rests on is checked and holds; whether the gate is the right design is the review phase's question, not this round's."}
{"unit_id":"u46","anchor":"§3","text_sha":"def1f79a7675","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"query","evidence":"`grep -rn 'go\\.mod' --include='*.go' internal/ cmd/ | grep -v _test.go` returns nothing (exit 1). The other side is in hand: `root.go:56` already calls `debug.ReadBuildInfo()`, whose `Main.Path`/`Path` fields carry the module path.","note":"Counting rule stated because the grep is a literal-string search: it establishes that no non-test file under internal/ or cmd/ mentions the string `go.mod`. I checked the obvious way round it — a module-file parser reached without naming the file — and there is none: `faster_search` for `modfile` in internal/ returns no hits, and go.mod's own require list carries no golang.org/x/mod dependency. So the asymmetry the sentence claims (one side reused, one side new) holds."}
{"unit_id":"u48","anchor":"§4","text_sha":"c8568dff5b97","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`hooks/session-start.sh`: line 86 reads the manifest's `\"version\"` field out of `$plugin_root/.claude-plugin/plugin.json`, line 91 captures `tp --version`, line 97 compares the two through `version_below`, and line 98 calls `fail()`, defined at lines 27-30 as `printf … >&2; exit 2`. `hooks/hooks.json` registers the script under the `SessionStart` event with matcher `*`.","note":"All three clauses — what it compares, when it runs, and that it fails with exit 2 — verified from the artifacts rather than from the spec's paraphrase. The 'via fail()' attribution is exact: the version branch is one of six call sites of that function and the only one on the version comparison."}
{"unit_id":"u51","anchor":"§4","text_sha":"b021976d3cf6","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"Four facts, each run on this machine. `.claude-plugin/plugin.json:3` is `\"version\": \"1.0.0\"`. `tp --version` (at /Users/deligoez/go/bin/tp) prints `tp version v1.0.0`. Sourcing the hook's functions, `version_below 1.0.0 1.0.0` returns 1 (not below); running the whole hook with CLAUDE_PLUGIN_ROOT set to this repository exits 0 with empty stderr, so it is silent. `go version -m $(command -v tp)` prints `mod github.com/deligoez/tp v1.0.0 h1:…` and `build -buildmode=exe` and no `vcs` setting at all.","note":"The strongest sentence in §4 and it reproduces in every part, including the end-to-end silence rather than just the comparator's return value. The final clause — that this is §3's release state — follows from the table: revision absent, Main.Version real."}
{"unit_id":"u52","anchor":"§4","text_sha":"d3682e881e95","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"§3's four conditions (snapshot lines 84-87) are: revision absent + Main.Version real; revision absent + Main.Version `(devel)`; revision present and ≠ HEAD; revision == HEAD. Every one is a presence test or a string equality; none is an ordering.","note":"Checked against the table rather than the prose about the table. The 'simplification rather than a saving' half is a framing judgement, not a world-claim, and is not what carries the verdict."}
{"unit_id":"u54","anchor":"§4","text_sha":"04f459234d17","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`hooks/session-start.sh` line 1 is `#!/bin/sh`. The body is POSIX throughout (`set -u`, `printf`, `case`, `$((…))`, no arrays, no `[[`), and I executed it with `sh` rather than bash, which succeeded.","note":"Verified by running under sh as well as by reading the shebang, so the claim holds in the stronger sense that the script is sh-compatible and not merely sh-labelled."}
{"unit_id":"u55","anchor":"§4","text_sha":"9052c526cf00","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`hooks/session-start.sh:50-52` `version_number` is `sed -e 's/^[vV]//' -e 's/[-+].*$//'` — a normaliser with no comparison. `:56-62` `version_part` uses `cut -d. -f$2` and a `case` that coerces non-numeric to 0 — an extractor with no comparison. `:65-79` `version_below` is the only function containing `-lt`/`-gt`.","note":"The negative half ('neither compares') is the checkable one and it holds: the two `[` comparisons in the file are both inside version_below."}
{"unit_id":"u56","anchor":"§4","text_sha":"7219c09b9b6f","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`internal/cli/plugin_test.go:125` defines `comparePluginVersions`, `:140` defines `pluginVersionPart`, and `:129` shows the first calling the second. `grep -n 'comparePluginVersions' internal/cli/plugin_test.go` returns lines 106, 122, 125, 287, 348; resolving each to its enclosing `func Test…` gives three distinct shipped tests — TestPluginManifestDeclaresIdentity (106), TestPluginVersionIsNotBehindTheLatestTag (287), TestPluginVersionIsBumpedWhenPluginContentChanges (348).","note":"Two imprecisions, neither changing the verdict, both recorded because the sentence quotes an exact command. (1) The grep returns FIVE lines, not four: line 122 is the doc comment. (2) 'the same dotted-numeric algorithm' is not exact — version_below caps at three components and coerces a non-numeric component to 0, while comparePluginVersions iterates all components and fails the test on a non-numeric one, so they disagree on inputs like `1.0.0.5` vs `1.0.0.4`. The load-bearing claim §4 needs — that a second dotted-numeric comparator exists in Go with three shipped call sites — is exactly right."}
{"unit_id":"u61","anchor":"§5","text_sha":"66a59c28c96f","ordinal":1,"verdict":"PARTIAL","partial_kind":"reason-not-conclusion","kind":"corpus","tier":"query","evidence":"`grep -rn 'No merge with the binary check' spec/` returns six lines, and one of them is `spec/1.44.0.md:159` — this sentence itself, because it quotes the search string. Two more are the round-1 and round-2 snapshots of spec/1.45.0.md, and one more is this spec's own round-2 snapshot. Only `spec/1.45.0.md:122` is the sibling's actual non-goal clause.","note":"The conclusion holds and I verified it separately: spec/1.45.0.md:122 carries '**No merge with the binary check.**' as its own non-goal, and `grep -n -i 'merg' spec/1.44.0.md` returns only line 126 (the §4 heading) and lines 156-159 (this clause), so there is no reciprocal clause here and the refusal is stated from one direction only. What fails is the parenthetical derivation as written: the command it quotes finds a match 'here', in the very sentence that claims it does not — the search string and the searched corpus are the same file. This is the class the round was asked to watch for: a command written into a repair without being run in the state the repair created. It is fixable by naming the file (`grep -rn … spec/1.45.0.md`) or by dropping the parenthetical."}
{"unit_id":"u63","anchor":"§5","text_sha":"f926a889d1b6","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"§4 (snapshot lines 144-150) lists two implementations and both exist as described: `hooks/session-start.sh:65` `version_below`, and `internal/cli/plugin_test.go:125` `comparePluginVersions`. So the document does record what this unit says it records, and the record is itself true.","note":"Graded on both halves rather than on the cross-reference alone: the sentence would be a true statement about §4 even if §4 were wrong, so I checked that the two comparators exist. They do. See u56 for the one imprecision in §4's description of the second."}
{"unit_id":"u62","anchor":"§5","text_sha":"4bfae2eb87fa","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"Same read as u52: §3's four conditions (snapshot lines 84-87) are two presence tests and two string equalities, with no ordering. `grep -n -i 'newer\\|older\\|below\\|above' spec/1.44.0.md` returns eleven lines and not one of them falls inside §3's predicate table — the only §3 hit is line 115, 'the predicate above', which is positional rather than an ordering.","note":"The non-goal restates §4's sentence and rests on the same artifact; it is true of the specification as written. Whether the implementation honours it is a later phase's question. Counting rule for the grep: I read all eleven hits and classified each by section, rather than reading the hit count as the answer."}
{"unit_id":"u65","anchor":"§5","text_sha":"df6203086b9f","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"query","evidence":"`grep -rniE 'sessionID|session_id|TP_SESSION' --include='*.go' internal/ cmd/ | grep -v _test.go` returns nothing (exit 1).","note":"Counting rule: the grep is case-insensitive over three spellings in non-test Go under internal/ and cmd/, so it establishes the absence of those identifiers, not the absence of every conceivable session concept. I did not find a weaker form either — tp's own hooks receive a session id from Claude Code but nothing in the Go source reads one. The second half ('inventing one is a larger mechanism than the advisory') is a design judgement and is not what the verdict rests on."}
{"unit_id":"u68","anchor":"§6","text_sha":"b70b2a9d9113","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"The row's checkable half is that the named mutant fires on this configuration. A clone at HEAD builds a binary whose `vcs.revision` equals HEAD (941d1b66…) and which reports `v1.0.1-0.20260904033311-941d1b66ef29`; `spec/1.44.0.md` is under development and numbers above it; and `version_below 1.0.1 1.44.0` returns 0. So the version predicate does fire on the configuration this row says emits nothing.","note":"Graded on the mutant, which is the part that can be false: a mutant that cannot fail is the failure mode this repository has paid for twice. This one fails as advertised, and the instruction to watch it fail first is exactly the right form. The assertion half ('emits nothing') is about unimplemented behaviour and is a decision."}
{"unit_id":"u69","anchor":"§6","text_sha":"0f196475158b","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"Both halves run. The two fixture binaries exist and carry different revisions (u16, u17). The named mutant is the shipped comparator's prefix reduction: `version_number` on each of the two version strings returns `0.37.1`, so a numeric-prefix predicate cannot distinguish them and would give the same verdict for both — failing an assertion that one is silent and one advises.","note":"The claim inside the mutant column — that the revision is present in both strings and the prefix comparison discards it — is the one measured: the strings carry `65d0b09f2800` and `10dee489714a`, and the sed at hooks/session-start.sh:51 cuts from the first `-`, removing exactly that."}
{"unit_id":"u70","anchor":"§6","text_sha":"9fbb876f5b4e","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"All three fixtures are buildable and were built (u34, u35, u36): `go install …@v1.0.0` gives absent revision with `Main.Version` `v1.0.0`; the two local builds give absent revision with `(devel)`. The first mutant is therefore falsifiable — dropping the `Main.Version` condition makes the release advisory fire on the two `(devel)` builds, which are builds of this tree. The second mutant is falsifiable too: treating a missing revision as a match silences the go-installed release, and §4's case is reproduced (the hook exits 0 and prints nothing for that binary).","note":"The strongest row in the table, because the fixture set is what makes both mutants fail rather than one. I checked the sentence's own insistence that all three fixtures be present: with only the first, dropping the Main.Version condition changes nothing and the mutant survives — so the qualifier is load-bearing exactly as claimed."}
{"unit_id":"u71","anchor":"§6","text_sha":"d1bca022cdd6","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"Probed in a clone: appending a comment to `cmd/tp/main.go` and rebuilding yields `vcs.modified=true` beside `vcs.revision=941d1b66ef2978bf9e32ecdd6de95d8600c6b9d7`, which is HEAD. So the mutant's premise is measured, not assumed, and a dirty trigger would fire on a binary the predicate calls current.","note":"Same measurement as u42, applied here to the mutant rather than to the design sentence. The mutant can fail, which is the property this row needs."}
{"unit_id":"u72","anchor":"§6","text_sha":"ea4f5171a608","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"§3 (snapshot line 80) says 'four states, decided in order', and the table lists release, unattributable, stale, current — so release is decided before stale, as this row asserts. The mutant is well-formed against that: with presence checked second, a binary with no revision reaches the stale comparison first, `\"\" != HEAD` holds, and the advisory names an empty revision.","note":"The document claim is exact. The mutant's predicted symptom is a consequence of the ordering rather than a measurement, which is appropriate here because the code does not exist yet; nothing in the row asserts a fact about the tree that could be checked more deeply."}
{"unit_id":"u75","anchor":"§6","text_sha":"be2019872342","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"§2's first transcript (snapshot lines 28-37) is `go build -o tp ./cmd/tp && ./tp --version` inside this repository reporting `v1.0.1-…`, with `git rev-parse --short=12 HEAD` confirming the build is from HEAD; `spec/1.44.0.md` is under development and 1.44.0 numbers above 1.0.1. So the description of the fixture matches the transcript, and I reproduced the fixture itself at today's HEAD (`v1.0.1-0.20260904033311-941d1b66ef29`).","note":"One staleness worth recording without changing the verdict: the transcript's `git rev-parse --short=12 HEAD` prints `c9f19088623a`, which was HEAD when the spec was written and is now an ancestor of 941d1b66 — `git log -1 c9f19088623a` confirms it is a real commit of this repository. The transcript is a record of a past run, and the fixture as this unit DESCRIBES it (generically, 'a binary built from HEAD') is reproducible today, which is what the acceptance needs."}
{"unit_id":"u77","anchor":"§6","text_sha":"648d5d193c43","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"`git describe --tags --abbrev=0` returns v1.0.0 and `git tag -l v1.0.1 | wc -l` returns 0, so Go's incremented patch is 1.0.1 and a HEAD build reports it. Running the refuted predicate against four spec numbers: version_below(1.0.1, 1.0.0) false, version_below(1.0.1, 0.37.0) false, version_below(1.0.1, 1.0.1) false, version_below(1.0.1, 1.44.0) true. So a spec at or below the last release does not trip it, and only one above the incremented patch does.","note":"The 'load-bearing' judgement is verified by the boundary rather than asserted: I tested the last release (1.0.0), a number below it (0.37.0), the incremented patch itself (1.0.1, which also does not fire because version_below is strict) and a number above (1.44.0). The qualifier is doing real work — without it the acceptance would be satisfiable by a configuration the refuted predicate ignores."}
{"unit_id":"u78","anchor":"§6","text_sha":"88c737d655a0","ordinal":1,"verdict":"PARTIAL","partial_kind":"two-readings","kind":"behaviour","tier":"run","evidence":"For the fixture as built, the claim holds: the two binaries report `v0.37.1-0.20260902095645-65d0b09f2800` and `v0.37.1-0.20260902102453-10dee489714a`, which differ. But the universal form is falsified by a construction §3's own table enumerates: `git checkout 65d0b09f2800 && go build -buildvcs=false` and `git checkout 10dee489714a && go build -buildvcs=false`, both in a clone outside the repository, produce two binaries at two different commits that BOTH print `tp version dev`.","note":"Two readings, and the document supplies both. Restricted to VCS-stamped builds — which is the fixture's own condition — 'Go embeds the revision, so no two commits can' is exactly right. Stated unrestrictedly, as it is, §3's second and third probe rows are the counterexample: Go does not always embed the revision, and when it does not, every commit reports the same string. The row's operative point (row 2's fixture is buildable, its cheaper form is not) survives either way; the reason offered for it is stated more broadly than it holds. Fix is one clause: 'Go embeds the revision in a VCS-stamped build'."}
{"unit_id":"u1","anchor":"§0","text_sha":"870568c49d68","verdict":"PASS","kind":"document","tier":"read","evidence":"CLAUDE.md:327 still carries the roadmap row `| 1.44.0 | the binary check | The advisory when the running tp is older than the spec being developed | tool | ready |`, and `git show 41efe9cb^:CLAUDE.md | grep -n 'binary check'` returns the same row at line 315 under the old number 0.45.0 — so the release was indeed originally specified with the version predicate. The refutation itself is graded at u7 (PASS).","ordinal":1,"carried_from":1,"note":"The roadmap row is the artifact that carries the original predicate, and it has NOT been updated: `grep -n 'binary check' CLAUDE.md` still describes this release as 'the advisory when the running tp is older than the spec being developed'. The claim is true, and the tree still specifies the form this file refutes."}
{"unit_id":"u2","anchor":"§0","text_sha":"a642fe395fb1","verdict":"PASS","kind":"document","tier":"read","evidence":"Snapshot spec/.tp-review/1.44.0/snapshot-ground-round-1.md lines 17-46: §2 is titled 'The version comparison is the wrong predicate' and records both directions with transcripts.","ordinal":1,"carried_from":1}
{"unit_id":"u6","anchor":"§1","text_sha":"be02d2d9982b","verdict":"NOT-A-CLAIM","kind":"code-structure","tier":"read","evidence":"A decision about the unbuilt release. The channel it names exists: internal/cli/root.go registers `--quiet` ('suppress info-level output'), and internal/output/output.go:186-187 `func Notice(msg string) { if quiet { ... } }` returns early under quiet, so an advisory routed through output.Notice is silenced by `--quiet` and writes to stderr rather than to any exit code.","ordinal":1,"carried_from":1,"note":"Recorded as a decision with the mechanism checked, not as a claim about the world."}
{"unit_id":"u8","anchor":"§2","text_sha":"e0e068003abd","verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Cloned the repo to a scratch directory outside it, built at both commits the section pins, and ran the hook's own comparator over the results. False negative: `tp version v0.37.1-0.20260902095645-65d0b09f2800` and `tp version v0.37.1-0.20260902102453-10dee489714a` both reduce to 0.37.1 under hooks/session-start.sh's version_number, so version_below returns the same verdict for a current and a stale binary. False positive: a build at today's HEAD reports v1.0.1-0.20260904002842-2d7ef17bd9e7+dirty, and version_below '1.0.1' '1.53.0' succeeds, so the predicate fires on exactly the binary the rule asks for.","ordinal":1,"carried_from":1,"note":"The conclusion holds. The reason §2 gives for the false negative does not — see the reader-added rows on u11 and u16: the two binaries do not report indistinguishable versions, they report versions that differ in precisely the revision, and the predicate is blind because it compares only the numeric prefix."}
{"unit_id":"u23","anchor":"§2","text_sha":"a2b38b62b761","verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`ReadBuildInfo` occurs exactly twice in the repository (once in this spec): internal/cli/root.go:56 `if info, ok := debug.ReadBuildInfo(); ok \u0026\u0026 info.Main.Version != \"\" \u0026\u0026 info.Main.Version != \"(devel)\" {`, and it is used for info.Main.Version — the citation is exact. `go version -m` on a `go build` of the tree prints `build vcs=git`, `build vcs.revision=2d7ef17bd9e7ef40030f073b4a5f5bce86ec83ac`, `build vcs.time`, `build vcs.modified=true`, with no flag passed, so buildvcs is on by default and both settings are present.","ordinal":1,"carried_from":1,"note":"Two qualifications a reader should have. The root.go call is guarded by `if version == \"dev\"`, so a binary built with an ldflags-set version never reaches it — the advisory would need its own unguarded call rather than reusing this one. And the production build CLAUDE.md documents, `go build -ldflags=\"-s -w\"`, keeps the settings: probed, vcs.revision and vcs.modified survive the strip."}
{"unit_id":"u30","anchor":"§3","text_sha":"51a4355aea86","verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"`go build -o tp-git ./cmd/tp` in a copy of the working tree records `build vcs.revision=2d7ef17bd9e7ef40030f073b4a5f5bce86ec83ac`, which equals `git rev-parse HEAD`, and does so with `vcs.modified=true` — so the equality holds on a dirty tree, which is the case the current state has to survive.","ordinal":1,"carried_from":1}
{"unit_id":"u41","anchor":"§3","text_sha":"d3ed9bd36902","verdict":"NOT-A-CLAIM","kind":"mechanism","tier":"probe","evidence":"A decision, with the fact it rests on checked: a build from a dirty tree records `vcs.modified=true` alongside `vcs.revision` equal to HEAD (probed on tp-git above), so the modified flag is genuinely separable from staleness and dropping it as a trigger costs no discrimination.","ordinal":1,"carried_from":1,"note":"Recorded as a decision with the mechanism confirmed."}
{"unit_id":"u49","anchor":"§4","text_sha":"4866832a5064","verdict":"NOT-A-CLAIM","ordinal":1,"carried_from":1,"note":"A description of the unbuilt release's own behaviour — a decision plus a prediction, with nothing yet in the tree to contradict or confirm."}
{"unit_id":"u59","anchor":"§5","text_sha":"8e8ed7fdac00","verdict":"PASS","kind":"document","tier":"read","evidence":"§4 (snapshot lines 69-83) does state the reason: 'They disagree on the case that matters, and that is why both exist', with the freshly-installed-release case spelled out. `grep -n -i 'merg' spec/1.44.0.md` returns exactly two lines, 69 and 89 — the §4 heading and this non-goal.","ordinal":1,"carried_from":1,"note":"Confirms the asymmetry a neighbouring spec assumed away: this is the ONLY no-merge non-goal here and its subject is the plugin's version check. There is no reciprocal clause about the untracked-task-file advisory, while spec/1.45.0.md:122 carries '**No merge with the binary check.**' — the refusal is stated on one side only. The cadence half does check out: `grep -n 'per invocation' spec/1.44.0.md` returns lines 14 and 96."}
{"unit_id":"u73","anchor":"§6","text_sha":"dc24f19b91c9","verdict":"NOT-A-CLAIM","ordinal":1,"carried_from":1,"note":"A test row for an unwritten test. 'Asserted across a command set, not one command' is a decision about test breadth; the set itself is not named here, so there is nothing yet to check against the tree."}
{"unit_id":"u74","anchor":"§6","text_sha":"c732fc66fd14","verdict":"NOT-A-CLAIM","kind":"code-structure","tier":"read","evidence":"A test row for an unwritten test. The premise it cites is confirmed: `grep -rni 'sessionID|session_id|TP_SESSION' --include='*.go' internal/ cmd/` excluding tests returns zero hits, so tp genuinely holds no session identity, and §5.6 (snapshot lines 96-97) does decline to invent one.","ordinal":1,"carried_from":1,"note":"Recorded as a decision with its premise checked."}
