{"unit_id":"u2","anchor":"§0","text_sha":"c5de4634ed39","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"Both defects re-run by me at HEAD 196da662. §4 defect: in a fresh git repo outside the tree, `tp audit demo.md --role no-such-role --affected-files a.go` exits 2 and leaves `.tp-review/demo/snapshot-audit-round-1.md`; `tp review demo.md --role no-such-role` exits 2 and leaves `snapshot-round-1.md` + `state.json`. §3 defect: `./scripts/check-suite-state.sh ./probe -run TestProbeMutate` in an rsync copy, with the mutation done by the narrowed suite, exits 0 for add-dir and rm-dir on both watched arms and 1 for the file control. Tags: `git tag --list` carries v0.37.0 and v1.0.0; `gh release list` dates them 2026-09-02 and 2026-09-03, both after v0.36.0 (2026-09-01) and before this file's repair commit 659f5250 (2026-09-04).","note":"The half I cannot check is the process claim — that the AUTHOR re-ran them, and 'again at its ground round'. What I can check is the operative content: both are live at HEAD today, so the sentence is not carrying forward a stale handover. Author-held for the rest."}
{"unit_id":"u3","anchor":"§0","text_sha":"bedc40fcf49d","ordinal":1,"verdict":"PASS","kind":"corpus","tier":"query","evidence":"`grep -rl 'task-suite-state-assert' spec/.tp-review/0.36.0/` returns 18 audit round files, so the item is that cycle's. For the universal: I ran a wider regex than the spec's own — `review-round-|audit-round-|--record|round file|round findings` over every row of `spec/.tp-review/0.36.0/*.ndjson` — 91 rows, and read every class slug. All concern the per-ROLE findings file (the `--out-dir` prompt set, the append-as-you-go clause), the snapshot, mode matrices or the §7 replay gate. None concerns the atomicity or the write of `review-round-N.ndjson`/`audit-round-N.ndjson`. Counting rule: a row 'concerns the round findings file's write' if its finding/notes text names that file or the code path that writes it.","note":"Nearest miss is review-round-2's `partial-line-fails-completion-predicate`, which is about a truncated trailing line in a ROLE findings file — the same shape of defect on a different file. The distinction the sentence draws is real but thin."}
{"unit_id":"u4","anchor":"§0","text_sha":"18f7d0c813b0","ordinal":1,"verdict":"FAIL","kind":"corpus","tier":"query","evidence":"I ran the unit's own command verbatim. It returns 17 rows, of which THREE come from review files, not two: `review-round-1.ndjson atomicity-mechanism-unspecified`, `review-round-1.ndjson partial-write-policy-unstated`, and `review-round-10.ndjson cross-package-suite-races-shared-state`. The third is an implementer finding about `snapshot-round-N.md` written through `engine.WriteSnapshotAtomic` from concurrently-running test packages — not the `--out-dir` prompt set. So 'the two review rows that match ... are both about the --out-dir prompt set' is false of the output of the command in the same sentence.","note":"A count claim over a set the author's own command enumerates, and the count is wrong — the class CLAUDE.md names as 'never assert a quantifier you did not count'. The §0 conclusion it serves (no v0.36.0 row concerns the round findings file's write) survives: the third row is about the snapshot write. Two further reservations on the audit half: audit-round-8's `file-go-safety-internal-cli-audit-go-apply-the-go-safet` is squarely about the same atomicity fix (it records round 7's FAIL as fixed and re-measured), and audit-round-8's `.golangci.yml` row reasons explicitly about `writeFileAtomic`, so 'the only audit atomicity row' holds only if 'row' means 'distinct finding'."}
{"unit_id":"u6","anchor":"§1","text_sha":"d3d6789abda7","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`ls spec/.tp-review/1.0.0/` holds `state.json` (the round index), `snapshot-review-*`/`snapshot-audit-round-N.md`, `review-round-N.ndjson`/`audit-round-N.ndjson`; `ls spec/.tp-review/1.43.0/` holds `snapshot-ground-round-{1,2}.md` and `floor-ground-round-{1,2}.txt` alongside `ground-round-1.ndjson`. All five named artifact kinds are present.","note":null}
{"unit_id":"u9","anchor":"§1","text_sha":"5fb44cbf0aa7","ordinal":1,"verdict":"PASS","kind":"guard","tier":"break-and-control","evidence":"Break: in an rsync copy outside the repo, a narrowed suite (`./scripts/check-suite-state.sh ./probe -run TestProbeMutate`) that itself creates an empty directory under a watched path — so the wrapper brackets the mutation. add-dir-review exit 0, rm-dir-review exit 0, add-dir-rounds exit 0, rm-dir-rounds exit 0. Control on the same fixture: add-file-review exit 1, rm-file-review exit 1, empty-a-dir (remove the only file from a populated watched dir) exit 1. Documented-and-not-closed: `scripts/check-suite-state.sh` carries the comment block headed `KNOWN BLIND SPOT, measured in audit round 3` immediately above `state_digest`, and the shipped walk is still `find \"$dir\" -type f -print0`.","note":"The controls also confirm the narrowing the Overview added this round: emptying a directory IS caught (exit 1)."}
{"unit_id":"u12","anchor":"§1","text_sha":"8f9c3efa55e6","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"Not fixed: re-provoked at HEAD 196da662 (see u2's evidence; audit leaves snapshot-audit-round-1.md, review leaves snapshot-round-1.md + state.json, both on exit 2). Owned: `spec/0.37.1.md` §3 is titled 'A refused invocation writes no state' and its line 121 states the same defect. Not shipped: `git tag --list 'v0.37*'` returns `v0.37.0` alone; `gh release list` goes v0.37.0 (2026-09-02) -> v1.0.0 (2026-09-03); `spec/0.37.1.tasks.json` does not exist.","note":null}
{"unit_id":"u13","anchor":"§2","text_sha":"eadc77b80180","ordinal":1,"verdict":"FAIL","kind":"code-structure","tier":"query","evidence":"The grep is accurate — it returns exactly `internal/cli/audit_record.go:236` and `internal/cli/review_record.go:136`. But 'those two sites are the whole of it' is false: the round file has a THIRD non-atomic writer, `writeNDJSON` at `internal/cli/review_resolve.go:316` (`return os.WriteFile(path, []byte(buf.String()), 0o600)`), reached from four call sites — review_resolve.go:127 and :228, audit_resolve.go:79 and :141 — whose `filePath := args[0]` is a caller-supplied path. Provoked in an rsync copy: `tp review spec/.tp-review/1.0.0/review-round-4.ndjson --resolve 0 fixed 'probe evidence'` printed `resolved finding 0 as fixed` and moved the file's sha256 from 179db0f2feae2b6e to 9fd098c145e8827a. The corpus shows this is the normal path, not a misuse: 85 recorded `*-round-N.ndjson` files carry 1,566 rows with a `resolved` dict (fixed 1467, wontfix 99).","note":"This matters to §2's own fix, not just to the sentence. §2 changes only the two `--record` sites, and `ReviewRoundClean` reads the same file live on every call — so after the release a `--resolve` still rewrites the round file with a plain `os.WriteFile` while `--status`/`tp resume` read it under no lock, and §2.1's chain from a truncated file to a clean round is unbroken through that path. Counting rule for 'writer of the round file': a non-test call that writes bytes to a path that is or can be `<state-dir>/<review|audit>-round-N.ndjson`. The grep the sentence quotes cannot see the third site because it matches on the literal `engine.ReviewStateDir` argument."}
{"unit_id":"u14","anchor":"§2","text_sha":"30fe9979a3b9","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`git grep -n 'func WriteSnapshotAtomic'` -> internal/engine/reviewstate.go:397. Its body (reviewstate.go:397-410) is `MkdirAll` then `return writeFileAtomic(filepath.Join(dir, snapshotFilename(phase, round)), data)`. `writeFileAtomic` (internal/engine/runstate.go:255-271) is exactly `os.CreateTemp(filepath.Dir(path), filepath.Base(path)+\".*.tmp\")`, `tmp.Write(data)`, `tmp.Close()`, `os.Rename(tmpName, path)`. The temp file is created in the target directory (filepath.Dir(path)), as stated. And it is the same directory and round: `engine.ReviewStateDir(specPath)` is what both this and the two record sites join against.","note":null}
{"unit_id":"u15","anchor":"§2","text_sha":"04fe3d9e4f9d","ordinal":1,"verdict":"PARTIAL","partial_kind":"reason-not-conclusion","kind":"code-structure","tier":"read","evidence":"Three of the four assertions hold. An earlier draft did say `reviewstate.go:378` (`git show e73788ab:spec/1.43.0.md` line 23). It was exact at its own commit and at every commit that carried it: `git show <c>:internal/engine/reviewstate.go | sed -n '378p'` returns `func WriteSnapshotAtomic(...)` for 3a7118b3, 9614e907, c670b18e and e73788ab. It is line 397 at HEAD (grep above), a drift of nineteen lines. The fourth does NOT hold: at HEAD line 378 is a BLANK LINE. `hasAnyPrefixed` ends with `}` on 377 and `WriteSnapshotAtomic`'s doc comment opens on 379, so `:378` lands in no function at all — one line before the intended symbol's own comment block, not nineteen lines into an unrelated one.","note":"The nineteen is the distance from 378 to 397, not a depth into anything. The conclusion the sentence exists to support — cite by symbol because a drifted line still resolves — survives and is if anything better served by the truth: the citation now resolves to a blank line, which reads as harmless. This clause is new in the repair commit 659f5250; round 1's own row on the old unit recorded 'At HEAD line 378 is blank', so the repair added the characterisation the round-1 evidence contradicts."}
{"unit_id":"u18","anchor":"§2","text_sha":"2f896527db0c","ordinal":1,"verdict":"FAIL","kind":"code-structure","tier":"query","evidence":"`git grep -n LoadRoundRows -- internal/` filtered to non-test call sites gives EIGHT, not six: cli/audit.go:557, cli/review.go:1987, cli/review_record.go:164, cli/review_regression.go:143, cli/review_report.go:498, engine/resumepayload.go:100, engine/reviewclean.go:57, engine/reviewclean.go:101. The list is wrong in both directions. Too broad: `tp review --merge` is named and is NOT a caller — `internal/cli/review_merge.go` contains no `LoadRoundRows`, no `ReviewStateDir` and no `os.ReadFile`; `loadMergeFindings` (review_merge.go:134) opens the positional argument paths with `os.Open` + `bufio.Scanner`, i.e. the per-role files, never the recorded round file. Too narrow: it omits `review_record.go:164` (the mechanize-candidates walk over every recorded round inside `--record` itself) and `reviewclean.go:101` (`ReviewRoundNonBlockingOpen`).","note":"The sentence opens 'Which readers, counted rather than quantified', so the enumeration is the claim. The substantive point — that a truncated review round file moves review convergence — survives through reviewclean.go:57, and `--merge` being absent from the reader set arguably strengthens it. Counting rule: a non-test source line containing `LoadRoundRows(` that is a call rather than the declaration."}
{"unit_id":"u19","anchor":"§2","text_sha":"9455483e9a5c","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`internal/engine/rolestreaks.go:66` `ComputeAuditRoleStreaks` calls `ReadAuditRoundRows` at lines 76 and 93; `git grep -n 'func ReadAuditRoundRows'` puts that reader at `internal/engine/auditrounds.go:88`. auditrounds.go:84-87 states in its own comment that it 'reads rounds here rather than through LoadRoundRows'. Same files: both resolve through `ReviewStateDir(specPath)` and the round entry's `File`.","note":null}
{"unit_id":"u20","anchor":"§2","text_sha":"694b98a4a8e4","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`internal/engine/reviewstate.go:528` `Converged` is `ConsecutiveClean(rounds) >= requiredCleanRounds && !StateStale(...)`; `ConsecutiveClean` (reviewstate.go:448) walks `rounds[i].Clean` backwards and reads no file. Audit's callers are `internal/cli/audit_record.go:124` and `:386` (`engine.Converged(st.AuditRounds, wf.AuditCleanRounds, specHash)`), plus `audit.go:352` using `ConsecutiveClean` directly; `st.AuditRounds` is loaded from state.json. Corroborated by `engine.AuditRowsClean`'s own doc comment (auditclean.go:86-90): 'This is a pure function of the rows and the policy, not of any stored round: §2 stamps `clean` at record time rather than recomputing it live ... That is the one place it differs from its review twin, ReviewRoundClean, which reads the round's file on every call by design.'","note":null}
{"unit_id":"u23","anchor":"§2.1","text_sha":"fbd1e284612e","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"I built a THIRD independent implementation of the described probe (Go, outside the repository): 42,723-byte payload (the corpus median from §2.2's own command), 4,000 writes by one writer to one path, four goroutine readers taking no lock, arm A `os.WriteFile`, arm B a transcription of `writeFileAtomic` (`os.CreateTemp(filepath.Dir(path), base+\".*.tmp\")`, Write, Close, Rename). Three runs. Arm A: partial 8019/7978/8140 against whole 7566/7598/6833. Arm B: partial 0/0/0 against whole 77610/79251/77174. No fault injection, no process death, no signals — the difference is entirely the write shape, so 'live concurrency defect, not crash safety' is what the probe measures.","note":"The probe is faithful to the description as written; every parameter in the sentence was reproducible from the sentence alone, which is the point of stating a recipe rather than a number."}
{"unit_id":"u26","anchor":"§2.1","text_sha":"028e384a9726","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Arm A of my probe (see u23): partial reads 8019, 7978, 8140 across three runs — thousands. Whole reads on the same runs 7566, 7598, 6833. Ratios partial/whole 1.06, 1.05, 1.19, i.e. the same order of magnitude, as the cell claims.","note":null}
{"unit_id":"u27","anchor":"§2.1","text_sha":"90d71e984b64","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Arm B of my probe (see u23): partial reads 0 on run 1, 0 on run 2, 0 on run 3, over 77610 + 79251 + 77174 = 234,035 whole reads. I also counted a fourth bucket the spec does not ask for — reads of the right length with the wrong bytes — and it was 0 on every run of both arms, so nothing is hiding behind a length-only comparison.","note":null}
{"unit_id":"u28","anchor":"§2.1","text_sha":"dc6b06e59826","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"The operative claim — that the counts are a function of how many reads four goroutines complete during the writer's run, so they are load- and machine-dependent and not reproducible — is what my own three runs show: on ONE machine, in ONE implementation, back to back, the partial share of all reads was 51.5%, 51.2% and 54.4%, and the absolute counts moved 8019/7978/8140 partial and 7566/7598/6833 whole. So the integers are not stable even across consecutive runs, which is the reason the section gives for not quoting them. §2.1 quotes no integer for either arm ('thousands', 'zero').","note":"What I cannot check is the provenance half — that TWO earlier independent implementations returned 47.5% and 52.6-52.9%. Those are author-held runs. Worth recording that my third implementation lands at 51.2-54.4%, i.e. outside the 52.6-52.9% band on two of three runs, which is evidence FOR the sentence's thesis rather than against it. The sentence's own '47.5%' and '52.6-52.9%' are figures inside a paragraph headed 'No integers are quoted' — literally consistent (they are not integers, and they are not the counts) but the reader has to work for it."}
{"unit_id":"u29","anchor":"§2.1","text_sha":"cc34d9a57ad1","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Mechanism: `rename(2)` replaces the directory entry in one step, so an `os.ReadFile` that opens the name gets whichever inode the entry named at open time — the complete old file or the complete new one. Measured consequence in arm B of my probe: 234,035 reads across three runs, 0 partial and 0 same-length-wrong-bytes, while arm A on the identical fixture produced ~8,000 partial reads per run. The zero is a floor rather than a sample: the arm has no state in which a short read is reachable, which is what 'categorical rather than statistical' asserts.","note":"The categorical claim is about a single writer plus lock-free readers, which is tp's shape. rename's atomicity is per-directory-entry; it does not serialise two concurrent writers, and v0.36.0's own `cross-package-suite-races-shared-state` row says exactly that about the snapshot. §2 does not claim otherwise (Non-Goal 4 says no lock is added), so the scope is right, but the sentence's 'never a half-written one' is a statement about readers only."}
{"unit_id":"u31","anchor":"§2.1","text_sha":"898f7e0e2ab3","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"query","evidence":"`git grep -n WithReviewStateLock -- internal/` gives the declaration (engine/reviewstate.go:117), two non-test CALL sites — cli/audit_record.go:212 and cli/review_record.go:109 — one test call (engine/reviewstate_test.go:131) and three mentions in comments (cli/ground.go:671, engine/reviewstate.go:273, engine/nextunits.go:155). ground.go names it only to say its own lock keys on the spec path instead, and calls `engine.WithFileLock`. Lock-free readers: `internal/cli/review_status.go` contains no lock call and reaches the round file at line 64 via `engine.ReviewRoundClean`; `internal/engine/resumepayload.go` likewise calls `LoadRoundRows` at line 100 under no lock (`git grep -n 'WithFileLock\\|WithReviewStateLock' -- internal/cli/resume.go internal/engine/resumepayload.go internal/cli/review_status.go` returns nothing).","note":"Counting rule for 'caller': a non-test source line containing `WithReviewStateLock(` that is a call, not the declaration."}
{"unit_id":"u32","anchor":"§2.1","text_sha":"981ea99f83d3","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`internal/engine/reviewstate.go:547-550`: `var m map[string]any` / `if err := json.Unmarshal([]byte(line), &m); err != nil { continue }`. Verbatim as quoted, and the `continue` is the whole of the error handling — nothing is logged or returned. Its own doc comment (reviewstate.go:532-535) closes with 'Blank lines are skipped; unparseable lines are ignored.'","note":null}
{"unit_id":"u33","anchor":"§2.1","text_sha":"2322f4ca4728","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`ReviewRoundClean` (internal/engine/reviewclean.go:56-77) calls `LoadRoundRows` at line 57 on every invocation, drops rows resolved wontfix/duplicate, and returns `surviving == 0` or `survivingBlocking == 0` — recomputed from whatever rows survived, exactly as stated. `ReviewConsecutiveClean` (reviewclean.go:121-130) walks the rounds backwards calling `ReviewRoundClean` and counts the trailing run.","note":"One care point on the quotation. The exact capitalised string 'Read live from the round's recorded findings' sits at reviewclean.go:91, inside the doc comment of the SIBLING function `ReviewRoundNonBlockingOpen`, not of `ReviewRoundClean`. `ReviewRoundClean`'s own comment carries the same words lowercase and with a trailing noun — 'read live from the round's recorded findings file' (reviewclean.go:50-51) — so the attribution is substantively right and the quote marks are a shade tighter than the source. Not graded down, but a reader checking the exact string will land in the wrong function."}
{"unit_id":"u35","anchor":"§2.2","text_sha":"b1f883f84e69","ordinal":1,"verdict":"NOT-A-CLAIM","note":"'The severity is in the indistinguishability rather than the odds' is an evaluation of the defect, not an assertion about the world — it says which of two true facts the section will rest on. The rest of the unit is an instruction introducing the fenced command. The facts it is a judgement ABOUT are graded at u36, u41 and u43, all of which I ran."}
{"unit_id":"u36","anchor":"§2.2","text_sha":"578a81fdf071","ordinal":1,"verdict":"PASS","kind":"corpus","tier":"query","evidence":"Ran §2.2's first fenced command verbatim at HEAD 196da662: `files 280 median 42723.5 max 166911 over4k 251 zero 12`. Median 42,723.5 bytes = 41.7 KiB, 'tens of kilobytes'. over4k/files = 251/280 = 89.6%, 'roughly nine in ten'. Counting rule is the command's own: files matching `spec/.tp-review/*/*.ndjson` whose basename matches `(review|audit)-round-\\d+\\.ndjson`, sized with os.path.getsize, 'exceeds' meaning strictly greater than 4096.","note":"Both hedges survive drift: at e73788ab the same command gave 265 files, median 41 KB, 236 over 4 KB (89.1%), and the two English phrases were already true then."}
{"unit_id":"u37","anchor":"§2.2","text_sha":"4063a783dc6c","ordinal":1,"verdict":"PARTIAL","partial_kind":"two-readings","kind":"corpus","tier":"query","evidence":"The historical half is exactly right, checked commit by commit. `git show e73788ab:spec/1.43.0.md` quotes four measured figures in this section — median **41 KB**, max **112 KB**, **236 of 265 (89%)**, and **12 of the 265** zero-byte. Re-running the same derivation at HEAD gives median 42723.5 (~41.7 KiB, so 41 KB no longer rounds), max 166911 (~163 KB, not 112), 251 of 280 (not 236 of 265), and zero = 12 — so exactly three of the four are stale and the surviving one is the zero count. Dates: e73788ab is 2026-09-02, today is 2026-09-04, 'two days later'. Cause: 265 -> 280 is +15, and `git ls-files 'spec/.tp-review/1.0.0/*' | grep -c '(review|audit)-round-[0-9]+\\.ndjson'` is exactly 15, so all of the growth is v1.0.0's rounds.","note":"The reading that fails is the literal one. '**No figure is stated here**' is bolded and unqualified, and §2.2 as it now stands states 'roughly nine in ten' — a corpus-derived ratio, 89.6% today and 89.1% at e73788ab — plus 'a single 4 KB page', plus the 'four'/'three'/'two days' of this very sentence. Under the reading the release plainly intends (no exact measured integer, the rule §5 states as 'No row quotes an integer'), it holds. Under its own words it does not. The hedged ratio is the right engineering choice; the sentence claiming zero figures is the over-claim."}
{"unit_id":"u40","anchor":"§2.2","text_sha":"38e43cf5d971","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"Ran §2.2's second fenced command verbatim. It filters to zero-byte `(review|audit)-round-N.ndjson`, opens the sibling `state.json`, pulls `(findings, clean)` from the matching entry of `st[ph+'_rounds']`, and prints `last-of-phase` or `NOT-last (last is N)`. Output is 12 lines, one per zero-byte round file, each carrying both facts. So the sentence describes what the command below it does.","note":null}
{"unit_id":"u41","anchor":"§2.2","text_sha":"e453dc95df7e","ordinal":1,"verdict":"PASS","kind":"corpus","tier":"query","evidence":"All 12 lines of the command's output end `[(0, True)]` — 0.23.0 review 13 and 14, 0.24.0 review 5 and 6, 0.25.0 review 6 and 7, 0.26.0 review 5, 0.30.0 audit 3, 0.30.0 review 8 and 9, 0.31.1 review 2, 0.35.0 review 21. 12 of 12, matching the `zero 12` field of the first command, so the two commands agree on the denominator.","note":null}
{"unit_id":"u43","anchor":"§2.2","text_sha":"e79a62b653d5","ordinal":1,"verdict":"PASS","kind":"corpus","tier":"query","evidence":"Of the 12 zero-byte rounds the command reports, 4 print NOT-last: 0.23.0 review-round-13 (last is 14), 0.24.0 review-round-5 (last 6), 0.25.0 review-round-6 (last 7), 0.30.0 review-round-8 (last 9). 4 of 12 = 33%, a substantial minority. Each of the four is round N-1 where N is that phase's last, and in each case round N is itself zero-byte and (0, True) — so they are trailing clean rounds, and the four pairs are exactly the shape a two-clean-round convergence rule produces.","note":"Counting rule: NOT-last means the command printed 'NOT-last'; 'trailing' means every round from this one to the phase's last is clean, read off the same output. The pre-repair text's 'every one checked is the *last* round of its cycle' is refuted by the same run — 4 of 12 are not."}
{"unit_id":"u49","anchor":"§3","text_sha":"4ca6b704b39b","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`scripts/check-suite-state.sh`: `WATCHED=(spec/.tp-review .tp/rounds)`, `before=$(state_digest)`, then the suite (`go test -count=1 -race ./...` or the narrowed `\"$@\"` form), then `after=$(state_digest)` and a comparison that exits 1 on difference. `state_digest` hashes both entries of WATCHED. Confirmed by running it: the wrapper printed `before:`/`after:` digests when I made the narrowed suite write a file under a watched path.","note":null}
{"unit_id":"u50","anchor":"§3","text_sha":"5aa99c8ce675","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"The fenced block at snapshot lines 149-158 is byte-identical to `scripts/check-suite-state.sh`'s definition — same tab indentation, same `local dir`, same `for`/`if [ -d \"$dir\" ]`/`find ... -type f -print0 | LC_ALL=C sort -z | xargs -0 shasum -a 256`, same `done | shasum -a 256 | cut -d' ' -f1`. Nothing of the function is omitted. Both named parts are used below: the `[ -d ]` guard carries §3.1's 'in a fresh checkout it is absent rather than empty' (u77), and the outer pipe is what carries the inner pipeline's failure out of the function under `pipefail`, which is u71's measured abort — I reproduced that abort and it depends on both pipes.","note":null}
{"unit_id":"u51","anchor":"§3","text_sha":"464f7f7b0640","ordinal":1,"verdict":"PASS","kind":"guard","tier":"break-and-control","evidence":"Reproduced the described procedure. `rsync -a --exclude .git` of the repo to a directory outside it; a `probe` package whose single test performs one mutation named by an env var, so the mutation happens INSIDE the run the wrapper brackets; invoked as `./scripts/check-suite-state.sh ./probe -run TestProbeMutate`. Results: add an empty dir under spec/.tp-review -> exit 0; remove it -> exit 0; add an empty dir under .tp/rounds -> exit 0; remove it -> exit 0. Control 1, write a file under spec/.tp-review -> exit 1 with the FAIL banner and differing before/after digests. Control 2, remove that file -> exit 1. Control 3, remove the only file from a populated watched directory -> exit 1. So: both directions, both arms, exit 0; control exits 1. `-type f` is the reason — `find ... -type f` never emits the directory, so the sorted file list and its hashes are unchanged.","note":"Every clause of the sentence is separately reproduced, including the one that is easy to skip — 'with the mutation performed *by* the narrowed suite so the wrapper brackets it'. A mutation made before or after the wrapper runs would be invisible for a different reason and would prove nothing."}
{"unit_id":"u53","anchor":"§3","text_sha":"bd0eaa035569","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"`scripts/check-suite-state.sh` carries the comment block immediately above `state_digest`. Its heading line is `# KNOWN BLIND SPOT, measured in audit round 3:`. It contains verbatim `The blindness is to directory-only changes:` and `every file written under either path is still caught`, and its final sentence is `Routed to a later release rather than repaired here; what could not stand was the claim.` Both quoted fragments are exact.","note":"One shade of over-claim: 'closing with' names a clause that opens the last sentence rather than closes the comment — the comment's actual last words are 'what could not stand was the claim.' The substance is right and the fragments are exact."}
{"unit_id":"u56","anchor":"§3","text_sha":"028b991ff6d1","ordinal":1,"verdict":"PASS","kind":"corpus","tier":"query","evidence":"`find spec/.tp-review .tp/rounds -type d -empty` at HEAD 196da662 prints exactly one line, `spec/.tp-review/1.0.0/rounds`, and nothing else (piped to `wc -l` gives 1). Both watched roots exist, so the command searched both.","note":"It is untracked — an empty directory cannot be committed — so a fresh clone will not have it and the sentence's 'right now' is doing real work. The fixture is live in this working tree, which is what the sentence claims."}
{"unit_id":"u58","anchor":"§3.1","text_sha":"e3dd4214d3d3","ordinal":1,"verdict":"NOT-A-CLAIM","kind":"mechanism","tier":"probe","evidence":"This is the release's decision — what `state_digest` will emit — not an assertion about the tree. I nevertheless built the walk it describes (per watched dir: `find \"$dir\" -print0 | LC_ALL=C sort -z | xargs -0 -n1 echo`, then the existing `find \"$dir\" -type f -print0 | LC_ALL=C sort -z | xargs -0 shasum -a 256`, the whole piped to `shasum -a 256 | cut -d' ' -f1`) and it is constructible exactly as worded: 'directories included' and 'concatenated with the existing file-content hashes' both have a direct rendering, and 'hashed as today' is the unchanged outer pipe. Its measured behaviour is graded at u60.","note":"Recorded as a decision with the constructibility check attached, since a decision that could not be built would be worth reporting."}
{"unit_id":"u60","anchor":"§3.1","text_sha":"9875893049ac","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Built the proposed walk and digested one fixture (two watched dirs, three files, one nested subdir) in the four named states, rebuilding the fixture from scratch for each so the states are independent. Proposed column: baseline b509ee80b207, added 2d618479c006, removed b509ee80b207, content 0f0f1d90c1bb. So added differs, removed equals the baseline value byte for byte, content differs — all three directions, as stated. The shipped walk on the same four states gives 55b12b2dd683 / 55b12b2dd683 / 55b12b2dd683 / c9cf14e1de2c, i.e. it discriminates in one direction only.","note":"The exact-baseline half is a real property and not a coincidence of my fixture: the added path sorts inside a listing that `LC_ALL=C sort` orders, so removing it restores the byte sequence. An unsorted listing would not guarantee it."}
{"unit_id":"u62","anchor":"§3.1","text_sha":"a3b80a1cadf8","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Both variants built and run over the same four fixture states (see u60's harness). Neither works, and the failures are different in kind: `find \"$dir\" -type f -o -type d -print0` gives e3b0c44298fc in ALL FOUR states — the sha256 of the empty string (`printf '' | shasum -a 256` = e3b0c44298fc1c14...), so it measures nothing at all; `find \"$dir\" \\( -type f -o -type d \\) -print0` gives 55b12b2dd683 / 55b12b2dd683 / 55b12b2dd683 / c9cf14e1de2c, byte-identical to the shipped walk in every state, so it measures exactly what is measured today. Keeping them apart is load-bearing: a test that only asserted 'no change on the added state' would pass for both and could not tell 'silently disarms the gate' from 'changes nothing'.","note":null}
{"unit_id":"u64","anchor":"§3.1","text_sha":"5678fcc66a6b","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Row measured directly: shipped walk digests are baseline 55b12b2dd683, added 55b12b2dd683 (no change), removed 55b12b2dd683 (no change), content c9cf14e1de2c (detected). The same three verdicts appear at the level of the real gate script in u51/u9's break-and-control run: exit 0 on add and remove, exit 1 on a file write.","note":null}
{"unit_id":"u65","anchor":"§3.1","text_sha":"1ab8d18bd0c3","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Naive walk digests across the four states: e3b0c44298fc, e3b0c44298fc, e3b0c44298fc, e3b0c44298fc. No change in all three columns, including the file-content column, which is the cell that separates this row from the shipped one.","note":null}
{"unit_id":"u66","anchor":"§3.1","text_sha":"ae2603f66d20","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Parenthesised walk digests across the four states: 55b12b2dd683, 55b12b2dd683, 55b12b2dd683, c9cf14e1de2c. No change on added, no change on removed, detected on content — and every value equals the shipped walk's value for the same state, which is the stronger claim §3.1 makes about this row two paragraphs later.","note":null}
{"unit_id":"u68","anchor":"§3.1","text_sha":"4e4160f6f37d","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Every clause measured. Binding: `find <dir> -type f -o -type d -print0 | tr '\\0' '\\n'` on a fixture holding one file and one subdirectory prints the two DIRECTORIES and not the file, confirming `-print0` attaches to the `-type d` branch alone. Errors: running the naive pipeline emits one `shasum: <path>: Is a directory` per directory on stderr and nothing on stdout. Digest: the outer `shasum` therefore hashes empty input and returns e3b0c44298fc... = `printf '' | shasum -a 256`, in all four fixture states including the one where a.txt's contents changed (see u65). So the gate step's before and after are equal by construction and it can no longer report any mutation.","note":null}
{"unit_id":"u70","anchor":"§3.1","text_sha":"21886d2a6453","ordinal":1,"verdict":"PARTIAL","partial_kind":"two-readings","kind":"code-structure","tier":"read","evidence":"The operative half is measured and true: `before=$(state_digest)` captures stdout only, so running the naive pipeline printed `shasum: <path>: Is a directory` on the terminal, one line per directory, while the assignment took the empty stdout. And the quoted command is accurate — `git grep -c 'dev/null' -- scripts/check-suite-state.sh` prints nothing and exits 1, so there is no /dev/null anywhere. The half that fails is 'there is no redirect anywhere in the script': `grep -c '>&2' scripts/check-suite-state.sh` returns **12** — lines 83-94, the whole FAIL diagnostic block, including `git status --short -- \"${WATCHED[@]}\" >&2 || true`.","note":"Under the reading the argument needs — no redirect that DISCARDS output, which is what the /dev/null grep tests — the sentence holds and I reproduced its consequence. Under its own words it does not: the script redirects twelve times. Same shape as u37: the evidence in the parenthesis is narrower than the sentence around it, and the sentence is the one a reader carries away."}
{"unit_id":"u71","anchor":"§3.1","text_sha":"65c92ba0ac53","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Ran the assignment under the script's own options. `bash -c 'set -euo pipefail; ...; echo \"about to assign\"; before=$(state_digest); echo \"ASSIGNED before=$before\"; echo \"reached the line after\"'` with state_digest carrying the naive walk printed `about to assign`, then the two `shasum: ...: Is a directory` lines, then NOTHING — neither `ASSIGNED` nor `reached the line after` — and the shell exited 1. Each link separately: `find <dir> -type d -print0 | xargs -0 shasum -a 256 >/dev/null; echo $?` gives 1, so xargs does exit non-zero; `pipefail` is what carries that out of the inner pipeline and the outer one; and `-e` is what turns the failed assignment into an abort.","note":"This is why u68's 'the digest is sha256 of the empty string in every fixture state' has to be measured with -e off, as I did: under the real script the naive form never produces a digest at all. The two measurements are consistent because the spec states them as successive qualifications, but an implementer reproducing u68 under the script's own options will see an abort and may think the claim is wrong."}
{"unit_id":"u76","anchor":"§3.1","text_sha":"60da22e34501","ordinal":1,"verdict":"PASS","kind":"guard","tier":"break-and-control","evidence":"One array and one loop: `WATCHED=(spec/.tp-review .tp/rounds)` and `for dir in \"${WATCHED[@]}\"; do ... done` — there is no second walk in the script and no per-arm branch. Equally blind, measured on both arms in the same break-and-control run (u51/u9): add an empty dir under spec/.tp-review -> exit 0, under .tp/rounds -> exit 0; remove from either -> exit 0; the file control on spec/.tp-review -> exit 1.","note":"'Making them differ would leave the untested one to drift' is the decision half and is not graded."}
{"unit_id":"u77","anchor":"§3.1","text_sha":"65ab7d315fd9","ordinal":1,"verdict":"PARTIAL","partial_kind":"two-readings","kind":"code-structure","tier":"query","evidence":"For shipped tp the claim holds. `git grep -n 'MkdirAll' -- internal/ | grep -v _test.go` gives 23 sites; the only one whose path lies under `.tp/rounds` is `internal/engine/driver.go:598`, `os.MkdirAll(a.target.RoundDir, 0o750)` inside `clearUnitArtifacts` (driver.go:591), where `RoundDir` is `engine.RoundDir(root, taskFile, phase, round)` = `.tp/rounds/<base>/<phase>-r<round>` (childenv.go:131-133) — and that is reached only from the `tp run` driver. Absent in a fresh checkout: `.tp/.gitignore` line 5 is `rounds/` and `git check-ignore -v .tp/rounds` confirms it, so the directory is never committed. Present and populated here: `.tp/rounds/0.36.0` exists and `find .tp/rounds -type f | wc -l` is 69.","note":"The reading that fails is 'only creator' taken over the repository rather than over shipped tp. `internal/fakerunner/cmd/tp-fake-runner/main.go:204` also does `os.MkdirAll(roundDir, 0o750)` — a second creator, in the module, used by the suite. It is not on the `tp run` path and not in `cmd/tp`, so the sentence's intent is right; but the suite is exactly the process the gate wraps, which makes the exception the least harmless one available."}
{"unit_id":"u79","anchor":"§4","text_sha":"36308681d203","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"All four sub-claims run at HEAD 196da662. `spec/0.37.1.md` exists and its §3 is headed 'A refused invocation writes no state', line 121 stating '`tp audit <spec> --role <unknown>` refuses with exit 2 and creates a state directory anyway'. `git tag -l 'v0.37*'` returns `v0.37.0` and nothing else. `gh release list` returns v1.0.0 (2026-09-03) then v0.37.0 (2026-09-02) then v0.36.0 — nothing between. `ls spec/0.37.1.tasks.json` -> No such file or directory. And 'not because it is fixed' is confirmed by u80's re-run.","note":"This is the repair of the defect round 1 found — the pre-repair text at e73788ab said the item was 'shipped in v0.37.1'. The replacement does not swap one wrong provenance for another: every one of its four citations resolves at HEAD."}
{"unit_id":"u80","anchor":"§4","text_sha":"60c13390f883","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"Provoked at HEAD 196da662, in two throwaway directories outside the repository. Audit side, inside a fresh git repo: `tp audit demo.md --role no-such-role --affected-files a.go` -> `{\"error\":\"unknown role: no-such-role\",\"code\":2,...}`, exit 2, and `find . -type f` afterwards shows `./.tp-review/demo/snapshot-audit-round-1.md` where nothing existed before. Review side: `tp review demo.md --role no-such-role` -> same shape of error, exit 2, and afterwards `.tp-review/demo/snapshot-round-1.md` AND `.tp-review/demo/state.json` exist. Both file names and the review side's extra state.json are exactly as the sentence names them.","note":"The first audit attempt exited 4 ('not in a git repo') without writing anything, so the refusal has to be reached inside a git repo with --affected-files for the defect to appear — worth knowing for whoever writes 0.37.1's test."}
{"unit_id":"u81","anchor":"§4","text_sha":"ac4ef5be8cf0","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"`git show e73788ab:spec/1.43.0.md` (the pre-repair text, 2026-09-02) reads: 'A third, *a refused invocation still writes state*, was measured here and then **shipped in v0.37.1** with the rest of that patch, because its fix was already built and green.' So an earlier draft did drop it on that ground. The release it named does not exist: `git tag -l 'v0.37*'` -> v0.37.0 only, `gh release list` -> v1.0.0 then v0.37.0 with nothing between. And the defect is still live at HEAD (u80).","note":"'If 0.37.1 slips, this item is unowned' is a conditional about the future and is not graded; the two factual halves both hold. This is the sharpest thing the repair fixed and it fixed it without introducing a new provenance error."}
{"unit_id":"u83","anchor":"§4","text_sha":"65833808bb68","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`internal/engine/reviewstate.go:113-119`: `func WithReviewStateLock(specPath string, fn func() error) error { return WithFileLock(reviewStatePath(specPath), fn) }` — a one-line wrapper, and `reviewStatePath` is `filepath.Join(ReviewStateDir(specPath), stateFileName)`, so 'a thin WithFileLock on the state path' is exact. Both record paths are inside it: `internal/cli/review_record.go:109` opens the lock and the `os.WriteFile` at :136 plus `SaveReviewState` at :154 are inside the closure; `internal/cli/audit_record.go:212` does the same around :236.","note":"The decision half ('§2 changes how bytes land inside that critical section, not who may write') is not graded. One consequence worth carrying to u13: the third writer I found, `writeNDJSON` on the `--resolve` path, takes `engine.WithFileLock(filePath, ...)` on the round file itself rather than WithReviewStateLock on state.json — a different lock, so it is not serialised against a concurrent `--record`."}
{"unit_id":"u85","anchor":"§4","text_sha":"aaa6ff599349","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`scripts/check-suite-state.sh` line 41: `WATCHED=(spec/.tp-review .tp/rounds)`, and `state_digest` iterates exactly that array. The Non-Goal's checkable content — the watched set is those two paths and stays those two paths — matches the artifact it names.","note":"A wording tension worth flagging to the implementer rather than grading down: §3.1 does change what the gate hashes, since the sorted path listing becomes part of the hashed byte stream. The Non-Goal's own second sentence pins the intended sense ('the watched set stays ...'), i.e. which paths are in scope; read without it, 'No change to what the gate hashes' contradicts §3.1."}
{"unit_id":"u90","anchor":"§5","text_sha":"e2f47a6e9d85","ordinal":1,"verdict":"PASS","kind":"guard","tier":"break-and-control","evidence":"The row is a test specification, and I ran both its arms. Assertion arm (atomic write, four lock-free readers, 4,000 writes of a 42,723-byte payload): partial reads 0, 0, 0 across three runs over 234,035 whole reads. Mutant arm (plain `os.WriteFile`, the shipped shape): partial reads 8019, 7978, 8140 on the same three runs. So the assertion is satisfiable, the named mutant makes it fail on every run, and no fault injection, signal or process kill is involved anywhere — the only difference between the arms is the write shape.","note":"'thousands ... against the atomic arm's floor of zero, so the test discriminates without fault injection' is the discriminating claim and it is the one I measured. A test written from this row would be non-flaky in the direction that matters: the mutant arm's margin is ~8,000, not 1."}
{"unit_id":"u91","anchor":"§5","text_sha":"d0329d41b829","ordinal":1,"verdict":"PASS","kind":"guard","tier":"break-and-control","evidence":"Built the row's named mutant — rename a temp file of the payload's exact length holding the previous round's payload — and ran it against the row's own assertion, four readers, 2,000 writes. Result: same-length-wrong-bytes reads 38,359, wrong-length reads 0, so the assertion goes RED under `bytes.Equal` and stays GREEN under any length comparison. Control on the identical harness (honest CreateTemp/Write/Close/Rename): 36,821 whole reads, 0 wrong-length, 0 same-length-wrong-bytes, both assertions green. So the mutant fails the test, it fails it only through bytes.Equal, and the row's insistence on bytes.Equal over a length comparison is exactly what the mutant discriminates.","note":"This is the strongest row in §5: the mutant is realizable, it is caught, and it is caught by precisely the assertion form the row names and by no weaker one."}
{"unit_id":"u93","anchor":"§5","text_sha":"552b26000042","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"Provoked directly in a throwaway git repo outside the tree: `tp review demo.md` then `tp review demo.md --record empty.ndjson` with a zero-byte findings file. Output `{\"clean\": true, \"findings\": 0, \"consecutive_clean\": 1, \"round\": 1, ...}`; `ls -l .tp-review/demo/review-round-1.ndjson` shows 0 bytes; state.json's entry is `{'round': 1, 'findings': 0, 'clean': True, 'file': 'review-round-1.ndjson', ...}`. So the shipped behaviour is exactly what the row asserts must be preserved. Corpus corroboration for the mutant's cost: 12 recorded zero-byte round files exist and every one carries (0, True), so a fix that treated an empty write as a failure would reject 12 legitimate historical rounds.","note":null}
{"unit_id":"u94","anchor":"§5","text_sha":"123b88f37bbc","ordinal":1,"verdict":"PASS","kind":"guard","tier":"break-and-control","evidence":"Assertion arm: the proposed walk gives baseline b509ee80b207 and added-empty-dir 2d618479c006 — the digest differs, so the assertion is satisfiable. Mutant arm (`-type f`, the shipped walk): 55b12b2dd683 in both states, so the two states are reported identical and the assertion goes red. At the level of the real wrapper the same mutant shows as exit 0 for add-dir on both watched arms against exit 1 for the file control. And the script's comment does say it is missing: 'a run that creates a directory and leaves it empty is NOT caught, and engine.clearUnitArtifacts does exactly that MkdirAll.'","note":null}
{"unit_id":"u95","anchor":"§5","text_sha":"a33380f1ce24","ordinal":1,"verdict":"PARTIAL","partial_kind":"reason-not-conclusion","kind":"guard","tier":"break-and-control","evidence":"The row's substance is measured and holds. Naive `-type f -o -type d -print0`: e3b0c44298fc — sha256 of the empty string — in all four fixture states, so it digests empty input in every state. Parenthesised `\\( -type f -o -type d \\) -print0`: 55b12b2dd683 / 55b12b2dd683 / 55b12b2dd683 / c9cf14e1de2c, byte-identical to the shipped walk in every state. Two different failures, and a fixture asserting only 'no change on the added state' passes under both, so keeping them apart is necessary.","note":"The word that does not survive measurement is 'silently'. Running the naive pipeline prints `shasum: <path>: Is a directory` on stderr once per directory per invocation — §3.1's own u70 says so — and under the script's real `set -euo pipefail` the `before=$(state_digest)` assignment fails outright and the gate step exits 1 without running the suite, which I reproduced. So the naive variant is loud and fails closed, not silent; §3.1 says this two paragraphs earlier and §5 row 5 contradicts it. The conclusion (both variants must be separately falsified) is unaffected."}
{"unit_id":"u96","anchor":"§5","text_sha":"fbcef83003ef","ordinal":1,"verdict":"PARTIAL","partial_kind":"reason-not-conclusion","kind":"guard","tier":"break-and-control","evidence":"Both measured halves hold. Assertion arm: the proposed walk gives baseline b509ee80b207, added 2d618479c006, removed b509ee80b207 — differs on removal and equals the baseline byte for byte. Mutant arm: the shipped file-content-only digest is 55b12b2dd683 in the added state and 55b12b2dd683 in the removed state, the same value, so the removal direction is indeed untested under it.","note":"The stated REASON for sorting did not reproduce. I built the unsorted variant — `find \"$dir\" -print0 | xargs -0 -n1 echo` with no `sort` — on a fixture of eight files plus a subdirectory, added a directory sorting mid-way (`mmm`) and removed it: base 2bc4c80b1bac, added c0d13272e85b (changes), removed 2bc4c80b1bac — it DID return to baseline. One filesystem (APFS) and one attempt, so a 'can' claim is not refuted; but on the machine this spec is written on I could not build the case the sentence gives as its justification. Sorting has a stronger and unstated justification anyway — it is what makes the digest a function of the tree rather than of readdir order, so two identical trees agree."}
{"unit_id":"u98","anchor":"§5","text_sha":"8fffafe4fdd2","ordinal":1,"verdict":"PASS","kind":"guard","tier":"break-and-control","evidence":"Built and ran both readings of the withdrawn mutant against row 1b's own assertion (bytes.Equal on every whole read), four readers, 2,000 writes each, with the honest atomic writer as control. Reading 1, `os.CreateTemp` + Write + Rename with no Close: 33,149 whole reads, 0 wrong-length, 0 same-length-wrong-bytes — bytes.Equal green. Reading 2, an empty temp renamed in: 9 whole reads, 39,425 wrong-length, 0 same-length-wrong-bytes — bytes.Equal green again, because the assertion is over whole reads and no whole read carries wrong bytes. So under both readings row 1b's own assertion stays green, which is what the sentence claims.","note":"Both readings were built and run here too, independently of the author, which is the part of the sentence a later reader most needs replicated."}
{"unit_id":"u99","anchor":"§5","text_sha":"e298f42d609c","ordinal":1,"verdict":"PASS","kind":"guard","tier":"break-and-control","evidence":"Measured (see u98, reading 1): the no-Close writer produced 33,149 whole reads and zero reads of any other kind, so the file on disk is the full payload at every observation and bytes.Equal is true throughout. The mechanism is as stated: `os.File.Write` issues `write(2)` directly, so there is no user-space buffer that a missing `Close` could leave unflushed — `Close` on a plain `*os.File` releases the descriptor, it does not flush anything to the kernel.","note":null}
{"unit_id":"u100","anchor":"§5","text_sha":"fdd91fb746aa","ordinal":1,"verdict":"PASS","kind":"guard","tier":"break-and-control","evidence":"Measured (see u98, reading 2): renaming an empty temp file in gives 39,425 reads whose length differs from the payload's and 0 reads of the payload's length with wrong bytes. So a length comparison alone catches it on the first read, while bytes.Equal is never the assertion that fires — the row would establish nothing about bytes.Equal that a cheaper check does not already give.","note":null}
{"unit_id":"u103","anchor":"§5","text_sha":"3c6f41f17b99","ordinal":1,"verdict":"PASS","kind":"corpus","tier":"query","evidence":"'the corpus already holds legitimate ones, every one carrying findings: 0, clean: true' — §2's second command run at HEAD prints 12 zero-byte round files and all 12 end `[(0, True)]`; 12 also matches the `zero` field of §2.2's first command, so the two derivations agree. 'An empty round file is not a failed write' is confirmed live: a `--record` of an empty findings file writes a 0-byte round file and records findings 0, clean true (u93). And the sentence keeps its own rule — it states no count, it names the command.","note":null}
{"unit_id":"u107","anchor":"§5","text_sha":"881fe03d7489","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"`.tp/.gitignore` line 5 is `rounds/` and `git check-ignore -v .tp/rounds` returns `.tp/.gitignore:5:rounds/\\t.tp/rounds`. At HEAD, with 69 untracked files under `.tp/rounds` (`find .tp/rounds -type f | wc -l`), `git status --short -- .tp/rounds | wc -l` prints **0**, while the script's own combined form `git status --short -- spec/.tp-review .tp/rounds` prints only spec/.tp-review entries. So the diagnostic is blank for that arm however much has changed under it.","note":"The trap is real in the harder direction too: my own break-and-control run of the wrapper in a copy with no .git printed `fatal: not a git repository` in place of the diagnostic and still returned the correct exit code, which is a second reason the row's 'assert on the exit code, never on the diagnostic' is right."}
{"unit_id":"u34","anchor":"§2.1","text_sha":"1d65b6d3fb2a","ordinal":1,"verdict":"PARTIAL","partial_kind":"two-readings","kind":"behaviour","tier":"run","evidence":"A cut unit carrying §2's own conclusion, so I graded it. text_sha computed by me over the canonical text '**A truncated review round file therefore makes the round clean.** Not \"more often than it should\": the truncation removes the findings and the predicate reads what is left.' — recipe validated by reproducing three emitted hashes (u64 5678fcc66a6b, u27 90d71e984b64, u41 e453dc95df7e) from `--units` text. Measured live: a scratch repo outside the tree, three findings at severity high recorded into review-round-1.ndjson (1,032 bytes, rows ~344 bytes each), `tp review demo.md --status` reporting consecutive_clean. Intact: 0. Truncated to 0 bytes: **1**. To 100 bytes: 1. To 300 bytes: 1. To **600** bytes: **0** — because the prefix now holds one complete blocking row. Restored: 0.","note":"So 'therefore makes the round clean' holds for a prefix that stops before the first complete blocking row, and NOT for a longer prefix — which on a 42 KB corpus-median file is most of the possible truncation points. The sentence's own second half is what is universally true: the truncation removes findings and the predicate reads what is left, so the round is graded on a subset. Two refinements an implementer needs: (1) under `review_converge_on=blocking`, a prefix that drops every blocking row while keeping advisory ones is also clean, so 'clean' is reachable well past the first row; (2) state.json's recorded `findings` count does not move — it stayed 3 in every arm — so the JSON shows findings 3 beside a clean round, which is a signal a status reader could use and which §2.2's indistinguishability argument does not mention. Filed against the cut unit's own id rather than null, because the arms produced this unit and cutting the section's conclusion is what let the overstatement stand."}
{"unit_id":"u67","anchor":"§3.1","text_sha":"e72d763e5e0b","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"A cut unit — the fourth row of §3.1's table, and the only row asserting that the proposed walk works; the three rows asserting that candidates FAIL (u64, u65, u66) are all in the floor. text_sha computed by me over 'sorted path listing **+** file content hashes — **changes** — **returns to baseline** — detected', same validated recipe as u34. Measured on the proposed walk over one fixture in four independently rebuilt states: baseline b509ee80b207, empty dir added 2d618479c006 (changes), that dir removed b509ee80b207 (returns to baseline, byte for byte), file content changed 0f0f1d90c1bb (detected). All three cells hold.","note":"Round 1 filed the same complaint about the same table (its u31: 'the arms cut the one unit carrying §3's proposed mechanism'), and the repair answered it the right way — §3.1's prose now carries the measurement in u58 and u60, both in this round's floor, so the claim is no longer reachable only through a cut unit. The table row itself is still cut; that is now redundancy rather than a hole."}
{"unit_id":"u8","anchor":"§1","text_sha":"1977ead396fb","verdict":"PASS","kind":"code-structure","tier":"read","evidence":"internal/cli/review_record.go:136 and internal/cli/audit_record.go:236 are both `os.WriteFile(filepath.Join(engine.ReviewStateDir(specPath), fileName), data, 0o600)`. The snapshot in the same directory goes through engine.WriteSnapshotAtomic -\u003e writeFileAtomic (internal/engine/runstate.go:255), which is os.CreateTemp + Write + Close + os.Rename.","ordinal":1,"carried_from":1,"note":"Verified at HEAD (accc37d4) and at the spec's own commit 3a7118b3: both line numbers are exact at both."}
{"unit_id":"u75","anchor":"§3.1","text_sha":"621f9d0e0b46","verdict":"PASS","kind":"document","tier":"read","evidence":"CLAUDE.md does carry the general form, twice over: the gate paragraph reads 'the gate-sequence release's §1.1 carries ten built-and-run inputs showing that a gate step merely NAMED in executable text certifies itself, that `if: false` and `continue-on-error: true` disable a certified step without changing its text, and that the wrapper's own digest comparison can be deleted with the package green.' 'measured ten ways' is that ten.","ordinal":1,"carried_from":1,"note":"The first clause ('a comment naming a blind spot is not a mitigation') and the last ('this release deletes the comment by closing it') are decisions. The analogy they rest on is the checkable part and it checks out."}
{"unit_id":"u78","anchor":"§4","text_sha":"83e26f14b8b5","verdict":"PASS","kind":"document","tier":"read","evidence":"CLAUDE.md's roadmap row for the gate-sequence release reads '`quality_gate` as an ordered array of named entries, each with its own exit code. Takes the three v0.36.0 handovers 1.43.0 left: two CI guards narrower than their own claims, and a load-sensitive gate test.' The two documents agree on the routing and on the wording.","ordinal":1,"carried_from":1,"note":"CLAUDE.md counts THREE handovers left to that release and names two guards plus one test; this unit names the same two-plus-one. The arithmetic is consistent."}
{"unit_id":"u82","anchor":"§4","text_sha":"d0588b1bb193","verdict":"NOT-A-CLAIM","ordinal":1,"carried_from":1,"note":"A scope decision (no cleanup command, no --prune, no GC) with a justification that is a judgement about what tp can infer, not an assertion about the tree. Nothing here is checkable against the world."}
{"unit_id":"u92","anchor":"§5","text_sha":"15ab5d633e8d","verdict":"PASS","kind":"code-structure","tier":"read","evidence":"review_record.go writes the round file at 136 under the comment `// Round file first, index entry second` at 135, and reaches engine.SaveReviewState at 154 — nineteen lines later, same critical section. audit_record.go: write at 236, SaveReviewState at 255. Both orders are as the row states, and §2's change is inside the write, so the ordering is untouched by this release.","ordinal":1,"carried_from":1,"note":"engine/nextunits.go:155 records the same invariant from the reader's side ('writers put round files first (WithReviewStateLock), so a crash between the'), which is a second, independent statement of it in the tree."}
{"unit_id":"u97","anchor":"§5","text_sha":"f838b88ccd0b","verdict":"PASS","kind":"guard","tier":"break-and-control","evidence":"The two arms are already the same code — one WATCHED array, one loop (scripts/check-suite-state.sh:32, 47-54) — and they are equally blind today, which is what makes 'asserted on both' a meaningful requirement rather than a formality: real script, rsync copy, suite creates .tp/rounds/zzz-probe-empty -\u003e **exit 0**, same as the spec/.tp-review arm, against a file-write control at exit 1.","ordinal":1,"carried_from":1,"note":"The named mutant (widen one arm) is realizable and would be caught by an assertion on both. One practical wrinkle for whoever writes it: .tp/rounds is git-ignored, so the script's own `git status --short` diagnostic prints nothing for that arm, and a test reading the diagnostic rather than the exit code would see no difference between the arms."}
