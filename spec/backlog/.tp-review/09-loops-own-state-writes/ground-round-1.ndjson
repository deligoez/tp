{"unit_id":"u1","anchor":"§0","text_sha":"390e7033a85a","ordinal":1,"verdict":"PARTIAL","partial_kind":"two-readings","kind":"corpus","tier":"query","evidence":"Searched every recorded v0.36.0 round: python3 over spec/.tp-review/0.36.0/*.ndjson for /atomic|WriteFile/i. §3's blind spot IS a v0.36.0 audit handover — audit-round-4 and audit-round-5, role spec-coverage, item task-suite-state-assert, the round-5 note reading 'Its own comment now records the empty-directory blind spot rather than claiming the opposite; spec/0.38.0.md §7a.2 owns that' (0.38.0 is this file's former number). §2's defect is NOT: zero rows in any v0.36.0 round concern the round findings file's write. The only atomicity finding there is audit-round-7's class snapshot-tmp-race-under-parallel-role-units, about WriteSnapshotAtomic's FIXED temp name, and audit-round-8 records it fixed.","note":"Under the reading 'both defects below were handed over by v0.36.0's audit' the sentence is false for half the file. Under the weaker reading 'this file collects defects in the area v0.36.0's audit flagged' it holds. CLAUDE.md agrees with the weaker one: its 1.47.0 row says that release 'Takes the three v0.36.0 handovers 1.43.0 left', which makes 1.43.0 not the handover release, and the four-release sentence naming v0.36.0's undone work does not list 1.43.0 at all. The first clause, 'This file is decisions', is not a claim."}
{"unit_id":"u2","anchor":"§0","text_sha":"e18df90e7597","ordinal":1,"verdict":"PARTIAL","partial_kind":"two-readings","kind":"corpus","tier":"query","evidence":"The ancillary fact checks out: `gh release list` shows v0.37.0 released 2026-09-02T09:00:41Z and the spec's own commits (10dee489, 5ee7c869, 3a7118b3) are dated 2026-09-02, so v0.37.0 did ship in between. The process claim does not hold for the third defect: it is still reproducible at HEAD (see u3), so a re-run against HEAD could not have concluded it shipped.","note":"§2's half is corroborated — my independent probe reproduces its measurement to within run variance — and §3's core is corroborated on the real script. But u24's third enumerated direction and u30's last table cell are both refuted by measurement (see those rows), and u3/u36 assert a release that does not exist, so 'each was re-run against HEAD' is true of the defects and not of every figure written beside them."}
{"unit_id":"u3","anchor":"§0","text_sha":"9afaad4fde8d","ordinal":1,"verdict":"FAIL","kind":"behaviour","tier":"run","evidence":"There is no v0.37.1. `git tag -l 'v0.37*'` returns only v0.37.0; `gh release list` goes v0.37.0 (2026-09-02) then v1.0.0 (2026-09-03) with nothing between; spec/0.37.1.md exists but has no 0.37.1.tasks.json, so it was never decomposed or implemented. And the defect is live at HEAD: in a scratch repo outside the tree, `/tmp/tp-dev/tp audit spec/probe.md --affected-files x.go --role nosuchrole` exits 2 with {\"error\":\"unknown role: nosuchrole\"} and leaves spec/.tp-review/probe/snapshot-audit-round-1.md behind; the review sibling `tp review spec/probe.md --role nosuchrole` exits 2 and leaves snapshot-round-1.md AND state.json. Binary built from HEAD (accc37d4).","note":"This is the spec's only claim that something is already fixed, and it is the one claim that removes work from the release. §4 non-goal 1 (u36) repeats it. Nothing in the tree carries the fix; spec/0.37.1.md §3 is still an unimplemented specification of it, right down to test row 5, 'after a --role refusal in a repo with no state directory, none exists'."}
{"unit_id":"u4","anchor":"§1","text_sha":"41c22bb2c9f0","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`ls spec/.tp-review/0.36.0/` holds state.json (the round index — {spec, review_rounds[], audit_rounds[]}, written by EnsureReviewState/SaveReviewState, internal/engine/reviewstate.go), snapshot-round-N.md and snapshot-audit-round-N.md (WriteSnapshotAtomic), and review-round-N.ndjson / audit-round-N.ndjson (the findings). ReviewStateDir derives <spec-dir>/.tp-review/<base> with no flag or env override.","note":"Ground rounds now write into the same directory (snapshot-ground-round-N.md, floor-ground-round-N.txt), which the sentence's enumeration predates but does not contradict."}
{"unit_id":"u6","anchor":"§1","text_sha":"1977ead396fb","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"internal/cli/review_record.go:136 and internal/cli/audit_record.go:236 are both `os.WriteFile(filepath.Join(engine.ReviewStateDir(specPath), fileName), data, 0o600)`. The snapshot in the same directory goes through engine.WriteSnapshotAtomic -> writeFileAtomic (internal/engine/runstate.go:255), which is os.CreateTemp + Write + Close + os.Rename.","note":"Verified at HEAD (accc37d4) and at the spec's own commit 3a7118b3: both line numbers are exact at both."}
{"unit_id":"u7","anchor":"§1","text_sha":"a6ced528329d","ordinal":1,"verdict":"PASS","kind":"guard","tier":"break-and-control","evidence":"Ran the real scripts/check-suite-state.sh in an `rsync -a --exclude .git` copy outside the repository, narrowed with its own \"$@\" to a probe package whose single test mutates the watched paths. Four runs: no mutation -> exit 0; the suite creates an empty directory under spec/.tp-review -> **exit 0**, the mutation still on disk afterwards; the CONTROL, the suite writes a file under the same path -> **exit 1** with 'FAIL: the test suite mutated live review state' and differing before/after digests; the suite creates an empty directory under .tp/rounds -> **exit 0**. So the guard discriminates a file and is blind to a directory, on both arms.","note":"The blind spot is documented in the script's own comment at lines 38-46 ('KNOWN BLIND SPOT, measured in audit round 3 ... Routed to a later release rather than repaired here') and no test asserts it: `git grep -l check-suite-state -- '*_test.go'` returns only internal/cli/quality_gate_config_test.go, which checks the gate literal, not the walk. There is a live instance in the repository right now: `find spec/.tp-review .tp/rounds -type d -empty` returns spec/.tp-review/1.0.0/rounds."}
{"unit_id":"u9","anchor":"§2","text_sha":"fd5aef61e6f1","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`git show HEAD:internal/cli/review_record.go | sed -n 136p` and the same for audit_record.go:236 both yield `if writeErr := os.WriteFile(filepath.Join(engine.ReviewStateDir(specPath), fileName), data, 0o600); writeErr != nil {`. Identical at the spec's own commit 3a7118b3. Both sit inside engine.WithReviewStateLock, review_record.go:109 and audit_record.go:212.","note":"The only two round-file write sites in the tool; nothing else writes review-round-N.ndjson or audit-round-N.ndjson."}
{"unit_id":"u10","anchor":"§2","text_sha":"a2971142eacf","ordinal":1,"verdict":"PARTIAL","partial_kind":"true-when-written","held_at":"3a7118b3","kind":"code-structure","tier":"read","evidence":"At 3a7118b3, `git show 3a7118b3:internal/engine/reviewstate.go` puts `func WriteSnapshotAtomic` on line **378** exactly. At HEAD (accc37d4) line 378 is blank and the declaration has moved to **397** — v1.0.0 inserted hasAnyPrefixed above it. The substance still holds at HEAD: WriteSnapshotAtomic calls writeFileAtomic (internal/engine/runstate.go:255), which is os.CreateTemp in the same directory, Write, Close, os.Rename; and it is the writer for the snapshot in the same state directory during the same round (internal/cli/audit.go and review.go call it before emitting).","note":"A drifted pointer, not a wrong one: worth repointing before implementation, since a nineteen-line drift lands the reader in the middle of an unrelated function. The line 378 was itself inherited from v0.36.0 audit-round-7's finding, which cites 'reviewstate.go:378' for the same symbol."}
{"unit_id":"u11","anchor":"§2","text_sha":"1679f5b060ea","ordinal":1,"verdict":"PARTIAL","partial_kind":"reason-not-conclusion","kind":"code-structure","tier":"read","evidence":"'every convergence count' is a quantifier that does not hold. Review convergence does read the round file: ReviewConsecutiveClean -> ReviewRoundClean -> LoadRoundRows, live per round (internal/engine/reviewclean.go:121-129, and its own comment 'Read live from the round's recorded findings'). Audit convergence does NOT: Converged -> ConsecutiveClean(rounds) walks the stored `Clean` booleans in state.json (internal/engine/reviewstate.go:447-457, 529). So a truncated audit round file cannot move audit convergence, while a truncated review round file can. `tp review --merge` and the role streaks (ComputeAuditRoleStreaks, internal/engine/rolestreaks.go) do read it.","note":"The conclusion — that the round file is load-bearing and is the one written without the guarantee — holds and is if anything understated, because the count that DOES read it is the one that decides review convergence. This is the 'never assert a quantifier you did not count' class CLAUDE.md names; the fix is to name the readers rather than say 'every'."}
{"unit_id":"u12","anchor":"§2","text_sha":"e684f477461a","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Built an independent probe outside the repository (Go, mirroring internal/engine/runstate.go's writeFileAtomic for the atomic arm and review_record.go:136 for the shipped arm): 41,026-byte payload — the corpus median at the spec's own commit — 4,000 writes, 4 concurrent lock-free readers, three runs. os.WriteFile arm: whole 7,432 / 7,095 / 7,521, partial 8,236 / 7,955 / 8,374, zero ENOENT and zero read errors. So a partial read is produced by the write itself, not by a crash, and the readers never hit a missing file.","note":"'live concurrency defect, not crash safety' is exactly what the probe separates: os.WriteFile truncates in place and the window is the write, so no fault injection and no process death is needed. The 41 KB median is correct at the spec's commit — 41,026 bytes over the 265 round files then in the tree."}
{"unit_id":"u14","anchor":"§2","text_sha":"1ab8d081f3c9","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Independent probe, three runs at the row's own parameters (4,000 writes, 4 readers, 41,026-byte payload): whole 7,432 / 7,095 / 7,521 and partial 8,236 / 7,955 / 8,374. The row's 9,944 / 8,988 is not reproduced to the integer and cannot be — the counts are a function of how many reads four goroutines complete during the writer's run, which is load-dependent — but the partial count lands within 8% of the row's, and the ratio the section draws from it (roughly half) reproduces at 52.6% / 52.9% / 52.7% against the row's 47.5%.","note":"Reported as PASS because the row's load-bearing content is that partial reads are of the order of thousands and comparable to whole reads, which reproduces on a different machine and a different implementation of the probe. A reader should not expect the integers back."}
{"unit_id":"u15","anchor":"§2","text_sha":"610286099895","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Same probe, atomic arm (os.CreateTemp in the target directory, Write, Close, os.Rename — a byte-for-byte transcription of internal/engine/runstate.go:255): whole 79,780 / 77,514 / 80,298, partial **0** on every run, no ENOENT and no read error. The row's 80,696 / 0 is within 1.1% of my highest run.","note":"The zero is the claim that matters and it is categorical rather than statistical: rename(2) is atomic on this filesystem, so a reader observes the old inode or the new one and never a half-written one. The whole-read count being ten times the os.WriteFile arm's is also reproduced, and has the same cause — a reader that never blocks on a truncated file completes more reads."}
{"unit_id":"u16","anchor":"§2","text_sha":"b539a250c893","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"'roughly half': 8,236/(7,432+8,236) = 52.6%, reproduced three times. 'lock-free by design': engine.WithReviewStateLock has exactly two non-test callers, review_record.go:109 and audit_record.go:212, both record paths — internal/cli/review_status.go, audit_status.go and resume.go take no lock at all. 'a truncated NDJSON parses to fewer rows': engine.LoadRoundRows (internal/engine/reviewstate.go:536-554) does `if err := json.Unmarshal(...); err != nil { continue }` — an unparseable line is silently skipped, not an error, and its own doc comment says so.","note":"The causal chain is tighter than the sentence claims. ReviewRoundClean recomputes cleanliness live from those rows (internal/engine/reviewclean.go, 'Read live from the round's recorded findings'), so a truncated review round file yields fewer surviving findings and ReviewConsecutiveClean counts it clean — the round becomes clean more often, exactly as stated, through a path that is code and not inference."}
{"unit_id":"u17","anchor":"§2","text_sha":"07381af0520c","ordinal":1,"verdict":"PARTIAL","partial_kind":"true-when-written","held_at":"3a7118b3","kind":"corpus","tier":"query","evidence":"At the spec's own commit, `git ls-tree -r --long 3a7118b3 spec/.tp-review/` over review-round-*.ndjson and audit-round-*.ndjson gives exactly 265 files, median 41,026 B, max 112,488 B, and 236 of 265 (89.1%) above 4,096 B — every figure exact. At HEAD (accc37d4) the same query over the working tree gives **280** files, median 42,723 B, max **166,911** B, and **251 of 280 (90%)** above 4,096 — v1.0.0's rounds landed in between.","note":"Three of the four figures have moved in two days; only the ~41 KB median survives. The section's conclusion is untouched — a partial write is still a real shape — but any test or later claim keyed to '265' or '112 KB' is already stale. This is the class CLAUDE.md warns about: a measured constant in prose goes stale faster than a release. Bind it to the derivation, not the number."}
{"unit_id":"u18","anchor":"§2","text_sha":"931a6595d01c","ordinal":1,"verdict":"PARTIAL","partial_kind":"reason-not-conclusion","kind":"corpus","tier":"query","evidence":"'12 ... are already zero bytes' and 'every one recorded findings: 0, clean: true' both hold exactly — the twelve are 0.23.0 r13,r14; 0.24.0 r5,r6; 0.25.0 r6,r7; 0.26.0 r5; 0.30.0 audit-3, review-8, review-9; 0.31.1 review-2; 0.35.0 review-21, and reading each one's entry out of its state.json gives findings=0, clean=true for all twelve. 'every one checked is the *last* round of its cycle' does not: **4 of the 12 are the penultimate round**, not the last — 0.23.0 review-round-13 (last is 14), 0.24.0 review-round-5 (last is 6), 0.25.0 review-round-6 (last is 7), 0.30.0 review-round-8 (last is 9).","note":"The four are the first of each cycle's two trailing clean rounds, which is what a two-clean-round convergence rule produces, so the conclusion the section draws — these are legitimate empty writes and not truncations — is if anything better supported than the stated reason. But 'last round of its cycle' is false for a third of the set, and u51 restates it without u18's 'every one checked' hedge. The '265' has also moved to 280 since 3a7118b3."}
{"unit_id":"u23","anchor":"§3","text_sha":"999e3db61983","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"scripts/check-suite-state.sh:32 `WATCHED=(spec/.tp-review .tp/rounds)`; state_digest() at 47-54 loops over both; `before=$(state_digest)` at 56, the suite at 74-78, `after=$(state_digest)` at 80, and the comparison at 82. The fenced quotation in the spec (`find \"$dir\" -type f -print0 | LC_ALL=C sort -z | xargs -0 shasum -a 256`) is byte-identical to line 51.","note":null}
{"unit_id":"u24","anchor":"§3","text_sha":"709711f6789f","ordinal":1,"verdict":"PARTIAL","partial_kind":"two-readings","kind":"guard","tier":"break-and-control","evidence":"Ran the real script in an rsync copy outside the repository, one direction per run, each with the mutation performed BY the suite so the wrapper brackets it. Creating an empty directory under spec/.tp-review -> exit 0 (invisible, as claimed). Removing an already-existing empty directory -> exit 0 (invisible, as claimed). **Emptying an existing directory — the suite removes the one file inside it, the directory survives and is left with 0 entries — -> exit 1**, 'FAIL: the test suite mutated live review state', before/after digests differing. Control: writing a file -> exit 1. So two of the three enumerated directions are invisible and the third is detected.","note":"'emptying one' is invisible only under the reading where it means 'a directory ends up empty and its emptiness is not itself recorded'. Under the plain reading — the suite empties a directory — the file's disappearance changes the sorted file digest and the guard fires. The script's own comment already says the narrower thing correctly: 'The blindness is to directory-only changes: every file written under either path is still caught.' An implementer taking §3's enumeration literally would write a test asserting the wrapper misses a case it already catches."}
{"unit_id":"u25","anchor":"§3","text_sha":"797240ad9067","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"Line 38 of scripts/check-suite-state.sh, at HEAD and at 3a7118b3, is exactly `# KNOWN BLIND SPOT, measured in audit round 3: an EMPTY watched directory hashes`. Lines 45-46 close it with 'Routed to a later release rather than repaired here; what could not stand was the claim.' — documented and not closed.","note":"One incidental: the comment says 'measured in audit round 3', but the v0.36.0 audit rows that carry the blind spot are rounds 4 and 5 (role spec-coverage, item task-suite-state-assert). That is the script's claim, not the spec's, and the spec quotes it accurately."}
{"unit_id":"u26","anchor":"§3","text_sha":"d45d1cce3463","ordinal":1,"verdict":"PARTIAL","partial_kind":"reason-not-conclusion","kind":"mechanism","tier":"probe","evidence":"The refutation holds and is stronger than stated (see u30). But 'the error goes to /dev/null' is false against the tree: `grep -c 'dev/null' scripts/check-suite-state.sh` returns **0** — there is no redirect anywhere in the script. The digest is captured by `before=$(state_digest)`, which takes stdout only, so shasum's 'Is a directory' errors go to the operator's terminal, visibly. My probe printed two such lines per invocation. And under the script's own `set -euo pipefail` the variant does worse than fail silently: xargs exits 123, pipefail propagates it, and the `before=$(...)` assignment returns status 1 — measured in a copy of the assignment under the same shell options.","note":"'both walks return the digest of empty input' is true under the reading 'the before walk and the after walk', which is the reading the sentence is making: the naive variant's digest is e3b0c44298fc...b855, which is sha256 of the empty string, in every state I put the fixture in. It is NOT true of the shipped -type f walk, which returns a real digest. The first clause of the unit is a decision (the digest shall hash the sorted path listing as well as the contents) and my probe confirms that mechanism works — see u31."}
{"unit_id":"u27","anchor":"§3","text_sha":"d0a1c3322970","ordinal":1,"verdict":"NOT-A-CLAIM","note":"'Measured:' — a lead-in to the table that follows. It asserts nothing about the world on its own; the table rows u29, u30 and the cut u31 carry the assertions."}
{"unit_id":"u29","anchor":"§3","text_sha":"6a8b292bfe3d","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Probe of the shipped pipeline on a fixture outside the repository: empty directory added -> digest unchanged (64cc70cc... before and after); file content changed -> digest changes. Confirmed independently on the real script itself in an rsync copy (u7, u24): empty dir -> exit 0, file written -> exit 1.","note":"The two '—' cells are unmeasured rather than claimed, which is honest; I measured one of them anyway — removing an already-existing empty directory is also invisible under -type f (exit 0 on the real script)."}
{"unit_id":"u30","anchor":"§3","text_sha":"fb0d8576d91f","ordinal":1,"verdict":"PARTIAL","partial_kind":"two-readings","kind":"mechanism","tier":"probe","evidence":"Two writings of 'adding -type d to the existing pipeline' behave differently and the row is true of neither on its own. Naive, `find \"$dir\" -type f -o -type d -print0 | ...`: find's -print0 binds only to the second branch, so only directories are printed, shasum errors on every one of them, and the digest is sha256 of empty input (e3b0c442...b855) in EVERY fixture state — empty dir added: no change, and **file content changed: no change either**, so the row's last cell, 'detected', is FALSE. Parenthesised, `find \"$dir\" \\( -type f -o -type d \\) -print0 | ...`: directories go to stderr and only file hashes reach stdout, so the digest is byte-identical to the shipped -type f walk — empty dir added: no change, file content changed: **detected**, so that cell is true but 'both walks return the digest of empty input' (u26) is then false.","note":"The section cannot have both cells. Under the reading that makes u26's stated reason true, this row's 'detected' is wrong; under the reading that makes 'detected' right, u26's reason is wrong. The naive form is also materially worse than the row suggests: it does not merely fail to add a capability, it zeroes the whole gate step — and under the script's own `set -euo pipefail` it fails the assignment (status 1). Test row 5 (u48) inherits the same understatement."}
{"unit_id":"u31","anchor":"§3","text_sha":"f8e55cf3f201","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Cut unit, graded because it is the only row in §3 asserting that a fix works. Its text: 'sorted path listing **+** file content hashes — **changes** — changes, back to baseline — detected'. Probe outside the repository, hashing `find \\( -type f -o -type d \\) -print | sort` concatenated with the shipped `find -type f -print0 | sort -z | xargs shasum` output: empty directory added -> digest CHANGES; that directory removed -> digest returns to the exact baseline value; an existing directory emptied -> CHANGES; file content changed -> CHANGES. All four directions discriminate, which no other row in the table does.","note":"The arms cut the one unit carrying §3's proposed mechanism, so the floor asks for a disposition on two refuted variants and not on the one the release intends to ship. It is not a defect — the mechanism works as described, on all four directions including the removal case the sibling cut unit u32 insists on — but a round that graded only the floor would have graded neither of §3's load-bearing claims: this row and, in the other direction, u26's /dev/null reason."}
{"unit_id":"u33","anchor":"§3","text_sha":"621f9d0e0b46","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"CLAUDE.md does carry the general form, twice over: the gate paragraph reads 'the gate-sequence release's §1.1 carries ten built-and-run inputs showing that a gate step merely NAMED in executable text certifies itself, that `if: false` and `continue-on-error: true` disable a certified step without changing its text, and that the wrapper's own digest comparison can be deleted with the package green.' 'measured ten ways' is that ten.","note":"The first clause ('a comment naming a blind spot is not a mitigation') and the last ('this release deletes the comment by closing it') are decisions. The analogy they rest on is the checkable part and it checks out."}
{"unit_id":"u34","anchor":"§3","text_sha":"27efb2c47294","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"'gets the same walk': scripts/check-suite-state.sh:32 puts both paths in one WATCHED array and state_digest loops over it, so the two arms are the same code — and my break-and-control run confirms the same blindness on the second arm (the suite creating .tp/rounds/zzz-probe-empty -> gate exit 0). 'exists only after a tp run': the only creator is clearUnitArtifacts (internal/engine/driver.go:591, called from driver.go:460), which is the `tp run` driver; the path itself is built by engine.childenv.go:133 as .tp/rounds/<run base>/<unit>.","note":"'usually absent rather than empty' is a claim about other projects and is not checkable here, where .tp/rounds exists and holds 85 entries from v0.36.0's own run. Worth knowing for the test: .tp/rounds/ is git-ignored (.tp/.gitignore lists `rounds/`), so the script's failure diagnostic `git status --short -- \"${WATCHED[@]}\"` prints nothing for that arm even when the digest does change."}
{"unit_id":"u35","anchor":"§4","text_sha":"83e26f14b8b5","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"CLAUDE.md's roadmap row for the gate-sequence release reads '`quality_gate` as an ordered array of named entries, each with its own exit code. Takes the three v0.36.0 handovers 1.43.0 left: two CI guards narrower than their own claims, and a load-sensitive gate test.' The two documents agree on the routing and on the wording.","note":"CLAUDE.md counts THREE handovers left to that release and names two guards plus one test; this unit names the same two-plus-one. The arithmetic is consistent."}
{"unit_id":"u36","anchor":"§4","text_sha":"8e7b8a694ec7","ordinal":1,"verdict":"FAIL","kind":"behaviour","tier":"run","evidence":"There is no v0.37.1 tag (`git tag -l 'v0.37*'` -> v0.37.0 only) and no v0.37.1 release (`gh release list` -> v0.37.0 2026-09-02, then v1.0.0 2026-09-03). spec/0.37.1.md has no companion 0.37.1.tasks.json. The defect itself reproduces against a binary built from HEAD: `tp audit spec/probe.md --affected-files x.go --role nosuchrole` exits 2 and leaves snapshot-audit-round-1.md in the state directory; `tp review spec/probe.md --role nosuchrole` exits 2 and leaves snapshot-round-1.md and state.json.","note":"Same claim as u3, restated as a Non-Goal, so the release would drop the item on the strength of a release that has not happened. CLAUDE.md's own 1.43.0 row still lists three defects for this file, the refusal among them; its 0.37.1 row says 'ready', not shipped."}
{"unit_id":"u37","anchor":"§4","text_sha":"d0588b1bb193","ordinal":1,"verdict":"NOT-A-CLAIM","note":"A scope decision (no cleanup command, no --prune, no GC) with a justification that is a judgement about what tp can infer, not an assertion about the tree. Nothing here is checkable against the world."}
{"unit_id":"u38","anchor":"§4","text_sha":"2dd630562c71","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"engine.WithReviewStateLock (internal/engine/reviewstate.go:117) is `return WithFileLock(reviewStatePath(specPath), fn)`, and both round-file writes sit inside it — review_record.go:109 wrapping the write at 136, audit_record.go:212 wrapping the write at 236. So WithFileLock does guard the record path, one indirection down, and §2's change is confined to how the bytes land inside that critical section.","note":"The claim names WithFileLock where the call site names WithReviewStateLock; the indirection makes it true rather than approximate. Worth noting for §2's design that the lock does not make the write safe for READERS — reads take no lock at all (u16), which is the whole reason the atomic write is needed."}
{"unit_id":"u39","anchor":"§4","text_sha":"6a5b7125c021","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"The watched set today is exactly those two: scripts/check-suite-state.sh:32, `WATCHED=(spec/.tp-review .tp/rounds)`, unchanged at HEAD and at 3a7118b3. The presupposition the Non-Goal rests on is correct.","note":"The forward half ('stays') is a decision. The distinction it draws — what is hashed versus what the walk can see — is the right one and is exactly what u31's mechanism preserves: walk C adds directory paths to the listing without changing which trees are visited."}
{"unit_id":"u43","anchor":"§5","text_sha":"4df24e7f79b1","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"The assertion is achievable and the discrimination is real, measured on my own probe at the row's parameters: atomic arm 0 partial reads on each of three runs (whole 79,780 / 77,514 / 80,298); os.WriteFile arm 8,236 / 7,955 / 8,374 partial. Nothing was injected — the writer just writes, the readers just read.","note":"The row's '8,988 on the same probe' is not reproducible to the integer (mine gave 7,955-8,374) and should be stated as a derivation rather than a number; the margin claim survives either way, since the two arms differ by thousands against a floor of zero."}
{"unit_id":"u44","anchor":"§5","text_sha":"fda1d52a6cf1","ordinal":1,"verdict":"FAIL","kind":"mechanism","tier":"probe","evidence":"The named mutant does not do what the row says, in either form it can take. Built both and ran them at 2,000 writes / 4 readers / 41,026-byte payload. (a) os.CreateTemp, tmp.Write(data), os.Rename with NO Close — 'contents never flushed' read literally: **33,236 whole reads, 0 empty, 0 partial**, and the file on disk is the full 41,026 bytes. os.File.Write is a write(2) syscall; there is no user-space buffer to leave unflushed, so the mutant is a no-op and test 1b would stay GREEN under it. (b) os.CreateTemp, no Write at all, then Rename: **39,300 empty reads**, final on-disk length 0 — which a length-only assertion catches at once, since 0 != 41,026.","note":"So the row's justification is wrong twice: the realizable mutant is exactly the one a length-only assertion catches, and the mutant as described cannot fail the test. If 1b is worth keeping, its mutant has to be something that produces a whole-length file with wrong bytes — e.g. renaming a temp file padded to the right length, or writing the previous round's payload — and the assertion then has to be bytes.Equal, not len(). My probe counted 'wrong bytes at full length' separately and saw 0 of them in every arm, so nothing in the current design reaches that state by accident."}
{"unit_id":"u45","anchor":"§5","text_sha":"15ab5d633e8d","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"review_record.go writes the round file at 136 under the comment `// Round file first, index entry second` at 135, and reaches engine.SaveReviewState at 154 — nineteen lines later, same critical section. audit_record.go: write at 236, SaveReviewState at 255. Both orders are as the row states, and §2's change is inside the write, so the ordering is untouched by this release.","note":"engine/nextunits.go:155 records the same invariant from the reader's side ('writers put round files first (WithReviewStateLock), so a crash between the'), which is a second, independent statement of it in the tree."}
{"unit_id":"u46","anchor":"§5","text_sha":"fe6faefa76d2","ordinal":1,"verdict":"PASS","kind":"behaviour","tier":"run","evidence":"Ran it, outside the repository, with a binary built from HEAD: `: > empty.ndjson; /tmp/tp-dev/tp review spec/probe.md --record empty.ndjson` exits 0, prints {\"clean\": true, \"findings\": 0, \"consecutive_clean\": 1, ...}, writes state.json with `\"findings\": 0, \"clean\": true` for round 1, and leaves spec/.tp-review/probe/review-round-1.ndjson at **0 bytes**. The corpus agrees: all twelve zero-byte round files carry findings=0, clean=true in their index entries.","note":"The row is right about the behaviour and right that it is the one to get backwards. Note the sharpness of the hazard it names: the empty file the shipped code produces and the truncated-to-nothing file the defect produces are the same bytes, so no assertion on the file alone can separate them."}
{"unit_id":"u47","anchor":"§5","text_sha":"6a9f2b824c94","ordinal":1,"verdict":"PASS","kind":"guard","tier":"break-and-control","evidence":"The mutant named is the shipped walk, and it does report the two states identical: real script, rsync copy, suite creates an empty directory under spec/.tp-review -> exit 0, the directory present afterwards; control, suite writes a file under the same path -> exit 1 with differing digests; baseline, no mutation -> exit 0. So the assertion is currently false of the shipped code and would be true of a fixed one, which is what a test row needs. 'nobody has' it: `git grep -l check-suite-state -- '*_test.go'` returns only internal/cli/quality_gate_config_test.go, which asserts on the gate literal, not on the walk.","note":"The script's comment does not literally say an assertion is missing; it says the blind spot is documented and 'Routed to a later release rather than repaired here'. The substance holds. There is a live instance in the tree to build the fixture from: spec/.tp-review/1.0.0/rounds is empty right now."}
{"unit_id":"u48","anchor":"§5","text_sha":"4f9b6d56975b","ordinal":1,"verdict":"PARTIAL","partial_kind":"two-readings","kind":"mechanism","tier":"probe","evidence":"'not enough' is right in both writings — neither the naive `-type f -o -type d -print0` nor the parenthesised `\\( -type f -o -type d \\) -print0` changes the digest when an empty directory is added, so the fixture still fails under either. But 'measured no change in either direction' is true only of the naive form, and it understates what that form does: its digest is sha256 of empty input in EVERY state, so a file content change is not detected either and the gate step stops measuring anything at all — and under the script's own `set -euo pipefail` the `before=$(...)` assignment returns status 1. The parenthesised form does change on file content, exactly like the shipped walk.","note":"The two writings have to be separated or the mutant is ambiguous: 'ship that variant' names a variant that is silently inert in one writing and merely useless in the other. The stronger test is that the parenthesised form is digest-IDENTICAL to the shipped walk on every fixture state I built, which is a sharper thing to assert than 'no change'."}
{"unit_id":"u49","anchor":"§5","text_sha":"68c967f57b70","ordinal":1,"verdict":"PASS","kind":"mechanism","tier":"probe","evidence":"Measured on the walk §3 proposes (sorted path listing including directories, concatenated with the shipped file-content hashes): baseline d37cba50...; empty directory added -> digest CHANGES; that directory removed -> digest returns to d37cba50..., the exact baseline value. Both halves of the assertion hold, including the return-to-baseline that distinguishes a walk noticing a change from one noticing a direction.","note":"The mutant's description is loose — a file-set-only comparison misses an empty directory being created as well as being removed, so 'catches creation through a sibling file' only applies when a file happens to be created alongside. The assertion itself is sound and is the one CLAUDE.md's own rule asks for: the informative experiment is the one that could have failed, and a walk that hashes only additions passes an addition-only test."}
{"unit_id":"u50","anchor":"§5","text_sha":"f838b88ccd0b","ordinal":1,"verdict":"PASS","kind":"guard","tier":"break-and-control","evidence":"The two arms are already the same code — one WATCHED array, one loop (scripts/check-suite-state.sh:32, 47-54) — and they are equally blind today, which is what makes 'asserted on both' a meaningful requirement rather than a formality: real script, rsync copy, suite creates .tp/rounds/zzz-probe-empty -> **exit 0**, same as the spec/.tp-review arm, against a file-write control at exit 1.","note":"The named mutant (widen one arm) is realizable and would be caught by an assertion on both. One practical wrinkle for whoever writes it: .tp/rounds is git-ignored, so the script's own `git status --short` diagnostic prints nothing for that arm, and a test reading the diagnostic rather than the exit code would see no difference between the arms."}
{"unit_id":"u51","anchor":"§5","text_sha":"b912630d0150","ordinal":1,"verdict":"PARTIAL","partial_kind":"reason-not-conclusion","kind":"corpus","tier":"query","evidence":"The conclusion is right and the running evidence backs it (u46: an empty record writes a 0-byte file and records findings 0 / clean true, exit 0). The stated reason does not hold: 'every one a legitimate terminal clean round' is false for **4 of the 12** — 0.23.0 review-round-13 (the cycle's last is 14), 0.24.0 review-round-5 (last 6), 0.25.0 review-round-6 (last 7), 0.30.0 review-round-8 (last 9). All twelve do carry findings=0 and clean=true; a third of them are penultimate rounds, which is what a two-clean-round convergence rule produces. The 265 has also moved: 280 round files at HEAD, still 12 zero-byte.","note":"u18 hedges the same claim with 'every one checked'; this row drops the hedge and states it flatly, which is the version an implementer will read. The hedge was load-bearing and the code fence it rests on names exactly the three that ARE terminal (0.24.0 r6, 0.23.0 r14, 0.35.0 r21). 'legitimate' and 'clean' survive; 'terminal' does not, and nothing in the section's argument needs it."}
{"unit_id":null,"anchor":"§3","text_sha":"93ebdc9ff26e","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"A claim the index never carried: §3's fenced block presents `find \"$dir\" -type f -print0 | LC_ALL=C sort -z | xargs -0 shasum -a 256` as what the script does. It is byte-identical to scripts/check-suite-state.sh line 51, at HEAD (accc37d4) and at the spec's own commit 3a7118b3.","note":"Filed because the whole of §3 rests on the quotation being faithful, and a fenced block is not a unit, so no floor row asks anyone to check it. It is faithful — but the quotation omits the `if [ -d \"$dir\" ]` guard at line 50 and the outer `| shasum -a 256 | cut -d' ' -f1` at 53, and the outer pipe is where the empty-input digest comes from, so a reader working only from the fence cannot reconstruct u26's stated mechanism."}
