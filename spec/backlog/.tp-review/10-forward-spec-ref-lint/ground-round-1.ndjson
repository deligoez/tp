{"unit_id":"u3","anchor":"§0","text_sha":"9c5874db7c50","ordinal":1,"verdict":"PARTIAL","partial_kind":"true-when-written","held_at":"85a46682 (2026-09-04 03:05)","kind":"document","tier":"read","evidence":"`git show 85a46682:spec/candidates.md | wc -l` = 308 lines, with `## Refuted` at line 84 and four refuted-candidate subsections — so the pointer resolved when the spec was written. At committed HEAD 020262d9, `wc -l spec/candidates.md` = 44 and its second line reads **\"This file holds no content. It is a forwarding note, and it exists only so the references to it do not break.\"**; its own table routes 'the refuted candidates' to `spec/undecided.md`, `## Refuted` (verified: `grep -n '^##' spec/undecided.md` shows `## Refuted` at line 24 with six `###` entries). The measurements are no longer in the file this sentence names.","note":"The same stub's table routes 'the forward-spec-ref prototype, the one lint rule that survived' to `spec/1.58.0.md` — the cited file now points back at the citing spec. Separately, the count 'four' has no stated counting rule: `spec/undecided.md ## Refuted` carries six entries (unexecutable-split, contradictory-comparator, example-table, the identifier pass, the corpus-replay gate as a procedure, broken-cross-ref across files), of which four, five or six are 'lint-rule candidates' depending on whether the identifier pass and the replay gate count. Not graded FAIL on the number; graded on the pointer."}
{"unit_id":"u6","anchor":"§1","text_sha":"cf4f9ca6e215","ordinal":1,"verdict":"PARTIAL","partial_kind":"reason-not-conclusion","kind":"corpus","tier":"query","evidence":"The two figures reproduce against their source verbatim: `CLAUDE.md:473-476` reads 'it found **47 citations to renumbered specs**, of which ~35 were stale ... None was caught by checking that the target file exists — every one resolved, and every one had moved.' The ordinal does not. `CLAUDE.md:472` — the sentence immediately above the figure — says: '**The ordinal is not checkable and this file used to assert one** — v1.0.0's grounding of the patch release found `CLAUDE.md` saying *fourth* while `spec/candidates.md` said *fifth*, and a repair unit refused to pick a side. What git records is **one** renumbering commit, `e73788ab` (2026-09-02, 16 spec renames) ... Earlier renumberings predate the tracked names or were not recorded as renames, so cite the sweep rather than a count.' Re-derived: `git log --diff-filter=R --name-status -- 'spec/*.md'` returns exactly one commit, e73788ab, with 16 renames.","note":"The source deliberately writes 'That sweep, after the recorded one' precisely to avoid the ordinal; this unit converts it back into 'the sweep after the fifth renumbering'. The conclusion (47 / ~35 / resolution is not the signal) is faithful; the premise that fixes it in time is the one CLAUDE.md withdrew as not checkable. Counting rule for the figures: they are copied from CLAUDE.md, not re-derived — the sweep is historical and cannot be re-run against a tree that has since been repaired."}
{"unit_id":"u8","anchor":"§1","text_sha":"e437493ce056","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"CLAUDE.md:471 carries the quoted rule almost verbatim ('Name a pending release by its subject, never by its filename — outside the roadmap table above, which is the one place a number belongs'; the unit drops 'above'). The quoted sentence is present at `spec/0.36.0.md:239`: 'Naming a file that does not contain the question is worse than naming none'. The 'two filenames a renumbering then falsified' is confirmed at the section level: `git show 3a83be30:spec/0.36.0.md` (the state just before 7d472704, 'repair 0.36.0's stale cross-references after the renumbering') — §3.1 carries `spec/0.38.0.md` once and `spec/0.41.0.md` twice, and e73788ab renamed both (0.38.0->1.38.0, 0.41.0->1.41.0). The third filename in that section, `spec/0.35.0.md`, shipped and did not move.","note":"Counting rule: two DISTINCT renumbered filenames in §3.1, not two occurrences — 0.41.0 appears twice. Read at paragraph rather than section granularity the count would be one, since the paragraph carrying the quoted sentence held only `spec/0.41.0.md` (later `spec/0.48.0.md`, then removed entirely by 7704cede on 2026-09-02). The section reading is the one CLAUDE.md's own wording supports."}
{"unit_id":"u9","anchor":"§1","text_sha":"57c5ca644071","ordinal":1,"verdict":"PASS","kind":"corpus","tier":"query","evidence":"True at the spec's own commit and still true today. At 85a46682 (`git archive 85a46682 | tar -x` into a scratch tree, then `python3 scripts/forward-spec-ref-prototype.py spec 1.0.0`): 2 findings. At committed HEAD 020262d9, same command in a fresh archive: 11 findings. Counting rule for both: one finding per (referrer file, line, target) triple, so one line naming two targets counts twice — the 2 at 85a46682 are both `1.0.0.md:409`.","note":"The convention is broken more often now than when the sentence was written, not less."}
{"unit_id":"u10","anchor":"§1","text_sha":"d4b4a5fed448","ordinal":1,"verdict":"PARTIAL","partial_kind":"true-when-written","held_at":"85a46682 (2026-09-04 03:05)","kind":"corpus","tier":"query","evidence":"Reproduced exactly at the authoring commit: `python3 scripts/forward-spec-ref-prototype.py spec 1.0.0` in an archive of 85a46682 prints `1.0.0.md:409 -> spec/1.38.0.md` / `1.0.0.md:409 -> spec/1.53.0.md` / `--- 2 findings; 20 non-version files skipped`, and `git show 85a46682:spec/1.0.0.md | sed -n '409p'` is the quoted sentence, byte-for-byte: 'the set is `spec/0.37.1.md` plus `spec/1.38.0.md`–`spec/1.53.0.md`'. At committed HEAD 020262d9 the same command returns **11** findings and **none is in spec/1.0.0.md**: ten are in `spec/1.0.1.md` (a spec created at 09:07 the same morning) and one in `spec/1.44.0.md:167`.","note":"The sentence itself was repaired at 0c7e3cf9 (2026-09-04 07:22, 'the pending set is derived, not enumerated — it grew past its endpoints'), about four hours after this spec was written. Counting rule: findings, i.e. (file, line, target) triples; 'both in spec/1.0.0.md' is 2 findings on 1 line in 1 file. The spec's own §2.3 forbids carrying such a count ('Any implementation of this rule should expect the count to move and should not carry one in a comment'); §1 carries one anyway, pinned only to the moving word 'HEAD'."}
{"unit_id":"u11","anchor":"§1","text_sha":"80ebbfb1f31f","ordinal":1,"verdict":"PARTIAL","partial_kind":"true-when-written","held_at":"85a46682 (2026-09-04 03:05)","kind":"document","tier":"read","evidence":"At 85a46682 the same passage supplies the durable derivation the unit describes: `git show 85a46682:spec/1.0.0.md | sed -n '405,413p'` contains \"its size is whatever `ls spec/*.md | grep -E 'spec/(1\\.[0-9]+\\.0|0\\.37\\.1)\\.md' | grep -v 1\\.0\\.0` returns rather than a number written here\" on the lines immediately following the two endpoints. The present tense no longer holds: `git grep -n 'the set is' -- spec/1.0.0.md` now returns line 419, 'the set is not enumerated here, because an earlier draft named its endpoints and the set has since grown past them.'","note":"The prediction was confirmed, not refuted — 0c7e3cf9's own commit subject is 'the pending set is derived, not enumerated — it grew past its endpoints', and the diff replaces exactly the two numbers this unit names. This is the strongest piece of evidence in the spec, and it is stated in a tense that has already expired. A form pinned to the commit ('at 85a46682 that sentence carried two numbers; 0c7e3cf9 removed them four hours later') would not have needed this row."}
{"unit_id":"u16","anchor":"§2","text_sha":"9855d7399ac4","ordinal":1,"verdict":"NOT-A-CLAIM","kind":"code-structure","tier":"read","evidence":"A step of the proposed predicate — a decision about what the rule will match, not an assertion about the world. The one checkable part is that the prototype implements it: `scripts/forward-spec-ref-prototype.py:28` is `REF = re.compile(r'spec/(\\d+\\.\\d+\\.\\d+)\\.md')` and `refs()` at lines 40-48 toggles `fenced` on a line whose lstrip starts with ``` and `continue`s while fenced.","note":"Two consequences of the stated form that the spec does not name, offered as scope rather than as defects. (1) The pattern is unanchored, so `myspec/1.2.3.md` and `.tp-review/spec/1.2.3.md` match as references. (2) Only the PATH form is caught: a spec naming a pending release as `v1.53.0`, `1.53.0` or `spec/1.53.0` (no `.md`) is invisible to the rule, and the convention CLAUDE.md states — 'never by its filename' — is about the number as much as the path. Neither is an error in the unit; both bound what §1 can claim the rule enforces."}
{"unit_id":"u18","anchor":"§2","text_sha":"117cefbeb26f","ordinal":1,"verdict":"PARTIAL","partial_kind":"two-readings","kind":"code-structure","tier":"query","evidence":"The three named rules exist: `grep -rhoE 'Rule:\\s*\"[a-z-]+\"' internal/engine --include='*.go'` returns broken-cross-ref, duplicate-paragraph and numbering-gap among others, and README.md's rule table (lines 337-353) gives all three severity `warning` under `tp lint`. The figure twelve does not reproduce under any stated rule. `runLint` (internal/cli/lint.go:81-93) calls **thirteen** `Check*` functions, emitting thirteen distinct rule names (…, long-spec, orphan-list-item, structured-elements); the same function then appends `frontmatter` (line 97-99), `acceptance-quality` (checkTaskFileQuality, line 115) and `affected-files-scope` (line 118), so README documents **sixteen** identifiers `tp lint` can emit.","note":"Twelve is reachable only as thirteen-minus-`structured-elements`, which is nowhere stated — and §3 of this same spec says 'All thirteen `Check*` functions', so the document carries 13 and 12 for overlapping sets without reconciling them. The load-bearing part of the unit (severity `warning`, a hyphenated slug in the same register) is unaffected; only the count is."}
{"unit_id":"u20","anchor":"§2.1","text_sha":"4c988f5c2e90","ordinal":1,"verdict":"PASS","kind":"corpus","tier":"query","evidence":"Run in an out-of-repo archive of 85a46682: `python3 scripts/forward-spec-ref-prototype.py spec 1.0.0 --no-boundary` returns 3 findings against the boundaried run's 2. Re-run in an archive of committed HEAD 020262d9: 12 against 11. In both states `diff` of the two outputs shows exactly one added line and no removed line, so the boundary clause is subtractive only.","note":"Counting rule: findings are (file, line, target) triples. The claim's qualitative content — the boundary clause removes findings and adds none — holds at both states; only the denominators moved."}
{"unit_id":"u22","anchor":"§2.1","text_sha":"aab65d98c050","ordinal":1,"verdict":"PASS","kind":"corpus","tier":"query","evidence":"`diff <(proto spec 1.0.0) <(proto spec 1.0.0 --no-boundary)` at committed HEAD 020262d9 shows exactly one added line: `0.36.0.md:566 -> spec/0.37.0.md`; the same single addition appears at 85a46682. The reference is real and not an artifact: `sed -n '566p' spec/0.36.0.md` is '3. **Convergence arithmetic.** `spec/0.37.0.md` owns the rule that turns a row set into a verdict.', a Non-Goal deferring a question to that release. Both ends are shipped: `git tag --sort=-v:refname` lists v1.0.0, v0.37.0, v0.36.0.","note":"Checked for the 'right defect, wrong reason' shape and it is not present — this is a genuine cross-spec reference, outside any fence, and the target genuinely shipped. The spec writes the location as `spec/0.36.0.md:566` where the prototype prints `0.36.0.md:566`; same finding."}
{"unit_id":"u26","anchor":"§2.1","text_sha":"c92e371479f6","ordinal":1,"verdict":"PASS","kind":"corpus","tier":"query","evidence":"The row's three cells are each checked. 'both ends frozen': v0.36.0 and v0.37.0 are both tags (`git tag --sort=-v:refname`), so neither filename can be renumbered. 'the measured false positive': the sole finding the `--no-boundary` run adds over the boundaried run, at both 85a46682 and committed HEAD 020262d9, is `0.36.0.md:566 -> spec/0.37.0.md` and nothing else.","note":"The table's other three rows are unmeasured by construction — 'pending -> lower pending' and 'shipped -> pending' produce no distinguishing observation on today's tree, since the boundary clause and the target>referrer clause agree on them. Only this row is a measurement, and the unit says so."}
{"unit_id":"u29","anchor":"§2.1","text_sha":"8ee71904afcf","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"Both bars are recorded where the unit implies. CLAUDE.md:463-466 on the example-table rule: 'the keyword-and-shape heuristic fires on 3.9–22.6% of this repository's 1,032 spec sections **against a shipped bar of *zero* false positives**'. CLAUDE.md:389-393 on the contradictory-comparator rule: 'flags **2 groups, both false positives, with zero true positives**; the same-section narrowing flags **zero, on anything**.' The ratio was reproduced at the authoring commit: 3 findings, 1 of them the 0.36.0->0.37.0 false positive.","note":"The denominator has since moved: at committed HEAD 020262d9 the same false positive is 1 in 12, not 1 in 3. The inference is unaffected — any positive count fails a zero bar — but the sentence carries a figure that is already stale, which is what §2.3 of this spec tells implementers not to do."}
{"unit_id":"u33","anchor":"§2.2","text_sha":"50fe9ab1be60","ordinal":1,"verdict":"PARTIAL","partial_kind":"true-when-written","held_at":"85a46682 (2026-09-04 03:05)","kind":"corpus","tier":"query","evidence":"At the authoring commit the claim holds exactly: in an archive of 85a46682, `python3 scripts/forward-spec-ref-prototype.py spec 1.0.0` and the same command with `--no-fence-skip` both print `--- 2 findings; 20 non-version files skipped`, and the two finding lines are identical. At committed HEAD 020262d9 the two runs differ: 11 against 15, and `diff` of the two outputs names the four fenced references, all in the spec created that morning — `1.0.1.md:130 -> spec/1.57.0.md`, `1.0.1.md:132 -> spec/1.58.0.md`, `1.0.1.md:200 -> spec/1.56.0.md`, `1.0.1.md:327 -> spec/1.56.0.md`.","note":"The unit's own framing — 'state the limit rather than the evidence' — is the right instinct and the sentence still states an evidence claim ('at HEAD the fence-skip changes nothing'), pinned to the moving word HEAD. It survived less than eleven hours. The limit-only form ('no count on today's tree can demonstrate the fence rule') would not have rotted, and §2.2's next sentence (u34) already says exactly that."}
{"unit_id":"u34","anchor":"§2.2","text_sha":"4ad3928d304b","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"The citation is exact and current. `sed -n '40,52p' internal/engine/lint.go` puts the fence toggle at lines 43-49: `if strings.HasPrefix(strings.TrimSpace(line), \"```\") { inCodeBlock = !inCodeBlock; continue }` followed by `if inCodeBlock { continue }`. It is inside `ParseHeadingsFromScanner`, which `grep -n 'func ParseHeadingsFromScanner' internal/engine/lint.go` puts at line 32. The 'not by a count on today's tree' half is confirmed by the measurement under u33: at the authoring commit the fence-skip changed nothing.","note":"A rare non-rotted line citation in this repository; checked because the standing failure mode here is a line number that has moved. The unit's last clause ('its test is a constructed fixture, not the corpus') is a decision about the future test, not a claim — it is consistent with §5 row 4."}
{"unit_id":"u35","anchor":"§2.3","text_sha":"eab2e94f1c2a","ordinal":1,"verdict":"PASS","kind":"corpus","tier":"query","evidence":"Both re-derivations were run and neither recorded figure came back. Recorded (in `git show 85a46682:spec/candidates.md`, the table under '### `forward-spec-ref`'): '51 spec files, pre-repair | **18**' and 'the same today | **0**'. Re-derived: at `v0.37.0` with boundary 0.37.0, `python3 proto.py spec 0.37.0` returns 48; at 85a46682 with boundary 1.0.0 it returns 2. Additionally re-derived at the only two 51-file states in history (dc8213d2 and 8f7d2e92, 2026-09-02 15:12 and 18:06, `git ls-tree --name-only <c> spec/ | grep -cE '\\.md$'` = 51): both return **0**, not 18.","note":"The heading claim ('No count of violations') is sound and the section then states three. Counting rule throughout: findings are (referrer file, line, target) triples produced by `scripts/forward-spec-ref-prototype.py`."}
{"unit_id":"u36","anchor":"§2.3","text_sha":"ff13ea06c1db","ordinal":1,"verdict":"NOT-A-CLAIM","kind":"document","tier":"read","evidence":"A table header naming three columns. Its only assertion is provenance — that the figures were recorded in `spec/candidates.md` — which held at the authoring commit (`git show 85a46682:spec/candidates.md` carries both figures in a two-row table) and no longer holds at committed HEAD 020262d9, where that file is a 44-line forwarding note stating 'This file holds no content.'","note":"Same rot as u3: the column heading now names a file that does not contain the figures it attributes. `spec/undecided.md` `## Refuted` took the refuted candidates; the `forward-spec-ref` section itself was routed to this spec, so the recorded figures survive only in git history."}
{"unit_id":"u37","anchor":"§2.3","text_sha":"c9f7b732c8d9","ordinal":1,"verdict":"PARTIAL","partial_kind":"reason-not-conclusion","kind":"corpus","tier":"query","evidence":"The number and its illustration reproduce exactly. In an out-of-repo archive of `v0.37.0` with the HEAD prototype copied in: `python3 proto.py spec 0.37.0` prints `--- 48 findings; 17 non-version files skipped`, and `0.48.0.md:90 -> spec/0.49.0.md` is among them, which is the pre-renumbering pairing the unit names. The stated reason does not survive. The record DOES name its corpus state: `git show 85a46682:spec/candidates.md` gives the table row '| 51 spec files, pre-repair | **18** |'. And the v0.37.0 tree the re-derivation used is not that corpus — `ls spec/*.md | wc -l` there is **46**, of which 29 are version-named. Re-derived at the two commits where the count is actually 51 (dc8213d2 09-02 15:12, 8f7d2e92 09-02 18:06, both pre-renumbering), boundary 0.37.0: **0 findings**, and `--no-fence-skip` gives 2 — which is the two-violations-one-transcript run the same record describes.","note":"So the 18 does not reproduce at 46 files (48), at 51 files (0) or at HEAD (11) — the conclusion is right and better supported than the spec claims. What is wrong is the diagnosis: the record's failure is not that it named no corpus, but that the corpus it named cannot be located by the description alone, and the spec's own re-derivation silently substituted a differently sized tree for it. Counting rule: 'spec files' = `spec/*.md`, version-named and not, as candidates.md's own '18 non-version files skipped' line implies."}
{"unit_id":"u38","anchor":"§2.3","text_sha":"3f91d09e72c6","ordinal":1,"verdict":"PARTIAL","partial_kind":"true-when-written","held_at":"85a46682 (2026-09-04 03:05)","kind":"corpus","tier":"query","evidence":"Reproduced at the authoring commit: archive of 85a46682, `python3 scripts/forward-spec-ref-prototype.py spec 1.0.0` returns exactly 2 findings, both `1.0.0.md:409`, so 'spec/1.0.0.md ... introduced both' is right. At committed HEAD 020262d9 the same command returns 11, none of them in spec/1.0.0.md — the line was repaired at 0c7e3cf9 (07:22) and ten new findings appeared in spec/1.0.1.md (created 09:07).","note":"The sharpest instance of the pattern this section is about. §2.3's own thesis is that 'a figure over a glob has two moving parts — the corpus and the boundary — and a record that fixes neither is not checkable'; this row fixes the boundary (1.0.0) and names the corpus only as 'HEAD', which is precisely the half that moved. Within one working day the re-derivation that was offered as the durable replacement for a stale figure became a stale figure itself, 2 -> 11. Fixing it costs one commit hash."}
{"unit_id":"u42","anchor":"§3","text_sha":"d8fefd657b16","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"query","evidence":"`grep -rhn '^func Check[A-Za-z]*(' internal/engine --include='*.go' | grep -v _test.go` returns exactly 13 declarations, in lint_rules.go (7), vague.go (5) and structured.go (1). Both cited signatures are byte-exact: `func CheckBrokenCrossRefs(lines []string, headings []*Heading) []Finding` and `func CheckSectionSize(headings []*Heading, totalLines, maxSectionLines int) []Finding`. No declaration takes a string path; the parameter set across all thirteen is `[]string`, `[]*Heading` and `int`. The three files declaring them contain zero `os.` or `exec.` call sites.","note":"Two qualifications that do not touch the counted claim. `CheckSpecSize(totalLines, maxLines int)` receives no text at all, so 'pure functions of the spec's own text' is loose for it and for `CheckSectionSize`, both of which also take a config threshold — the spec cites the second itself, so this is visible rather than hidden. And `Check*` is a narrower set than 'the rules `tp lint` emits': `runLint` also produces the `frontmatter` rule from `engine.ParseFrontmatter(specPath)` (internal/engine/frontmatter.go:48), which takes a path and calls `os.ReadFile` at line 49. That does not falsify this unit, which is scoped to `Check*` functions; it bears on u47."}
{"unit_id":"u43","anchor":"§3","text_sha":"534d5c14bed6","ordinal":1,"verdict":"PASS","kind":"code-structure","tier":"read","evidence":"`.tp/config.json` at committed HEAD registers exactly one entry under `workflow.checks`: `{\"class\": \"code-citation-drift\", \"cmd\": \"python3 scripts/check-spec-code-citations.py \\\"$(tp resume 2>/dev/null | python3 -c 'import sys,json;print(json.load(sys.stdin)[\\\"spec\\\"])')\\\"\"}`. The script exists at `scripts/check-spec-code-citations.py`, and it is not a lint rule — `grep -rn 'engine.Check' internal/cli/*.go` shows `runLint` calling only the thirteen pure engine checks.","note":"This unit and u52 describe the same tree and contradict each other: this one says the repo-aware check lives as a `workflow.checks` script, u52 says `tp config --resolved` reports `checks: []`. The registration has been at the project layer since 4d155a88 (2026-09-02 15:12), two days before this spec was written, so this unit is the one that matches the tree."}
{"unit_id":"u46","anchor":"§3","text_sha":"57ebc587f0d5","ordinal":1,"verdict":"NOT-A-CLAIM","kind":"code-structure","tier":"read","evidence":"A statement of what the release will do, for a function that does not exist. Its two premises check out: `runLint` does already hold the path — `internal/cli/lint.go:58-59` is `func runLint(_ *cobra.Command, args []string) error { specPath := args[0]` — and `git tag --sort=-v:refname | head -1` returns `v1.0.0`, which is the boundary the prototype's own runs are given.","note":"Nothing in the tree derives a boundary from tags today; `grep -rn 'git tag' internal/` finds no such read in the lint path, so this is entirely new surface. That is what §3 says, and it is why the unit is a decision rather than a claim."}
{"unit_id":"u47","anchor":"§3","text_sha":"a94c3016a402","ordinal":1,"verdict":"PARTIAL","partial_kind":"two-readings","kind":"code-structure","tier":"query","evidence":"Read as 'the CLI is one place tp already does I/O', true. Read as written — the CLI layer 'is where tp already puts its I/O' — contradicted: `grep -rn 'os\\.ReadFile|os\\.Open|os\\.WriteFile|os\\.Create|exec\\.Command|os\\.Stat|filepath\\.Walk' internal/engine --include='*.go' | grep -v _test.go | wc -l` returns **61** call sites across **27** non-test files, among them `internal/engine/lint.go:24` (`data, err := os.ReadFile(path)`), `frontmatter.go:49`, `runcmd.go`, `lock.go`, `discover.go`, `projectconfig.go` and `driver.go`. The second clause survives intact: the three files holding the thirteen `Check*` functions (lint_rules.go, vague.go, structured.go) contain zero `os.` or `exec.` call sites, so the rules really are testable from a `[]string`.","note":"The distinction the design rests on is `Check*`-function purity, not engine purity, and the sentence states the second. Nothing about the decision changes — putting the git read in the CLI is still reasonable — but the reason offered for it is not a property the engine has. Counting rule for 61: call sites matched by that seven-alternative pattern, one per matching line, `_test.go` excluded."}
{"unit_id":"u48","anchor":"§3","text_sha":"1dc7bcde5755","ordinal":1,"verdict":"NOT-A-CLAIM","kind":"mechanism","tier":"probe","evidence":"A decision about behaviour that does not exist yet. Probed for whether the prototype it is derived from already has it, and it does not: `scripts/forward-spec-ref-prototype.py:54` is `specdir, boundary = sys.argv[1], ver(sys.argv[2])` and `ver('')` raises `ValueError: invalid literal for int() with base 10: ''`, so an empty boundary crashes rather than degrading to silence. The non-version-filename half IS implemented — `SELF = re.compile(r'^(\\d+\\.\\d+\\.\\d+)\\.md$')` and the `if not m: skipped.append(name); continue` branch at lines 62-65 — and shows as '22 non-version files skipped' on every run.","note":"Worth flagging for the implementing task: two of the three degradation paths §5 rows 7 and 8 assert have no prototype behind them, so those rows are the only place the empty-string contract exists. Their fixtures will be the first execution of it."}
{"unit_id":"u49","anchor":"§3","text_sha":"1be6559f959c","ordinal":1,"verdict":"PARTIAL","partial_kind":"two-readings","kind":"mechanism","tier":"probe","evidence":"True for a guess BELOW the real boundary, false for one above it. Probed at committed HEAD 020262d9 over the whole spec corpus: boundary 0.0.0 -> 12 findings, boundary 0.36.0 -> 12, boundary 1.0.0 (the true newest tag) -> 11, boundary 1.60.0 -> 0, boundary 2.0.0 -> 0. A set comparison in an out-of-repo probe confirms the low guess is exactly §2.1's refuted draft: the finding set at boundary 0.0.0 equals the `--no-boundary` set exactly (12 == 12, set-equal), at both HEAD and 85a46682 (3 == 3).","note":"A high guess produces the opposite failure — total silence, every true positive suppressed — which is the failure mode §3's own 'degradation is silence' paragraph treats as acceptable, so the two paragraphs disagree about whether silence is the safe direction. Neither failure is hypothetical once the boundary comes from `git tag`: a repo with a stray `v9.0.0` tag, or a shallow clone with no tags, reaches one or the other."}
{"unit_id":"u50","anchor":"§3.1","text_sha":"51177f474ddb","ordinal":1,"verdict":"NOT-A-CLAIM","kind":"document","tier":"read","evidence":"A framing sentence for §3.1. Its one checkable half — that the failure mode is recorded — holds: CLAUDE.md's mechanize-candidates rule states it in terms ('registering early is not a harmless head start: tp tells every reviewer to stop reporting a mechanized class, so a check registered before its subject exists suppresses that finding class for every reviewer while verifying only whatever part of its subject already exists — which may be nothing'), and names v0.34.0 §7.1 as the worked instance.","note":"The record supports a suppression hazard from registering a check EARLY. §3.1 then argues from a different hazard — a registration that dies with its release — and the two halves of the evidence for that (u51, u52) are the two units in this spec the tree contradicts outright."}
{"unit_id":"u51","anchor":"§3.1","text_sha":"a7a525eccd99","ordinal":1,"verdict":"FAIL","kind":"code-structure","tier":"read","evidence":"The first clause holds: `internal/engine/runcmd.go:64` is `func RunCommand(command, dir string, timeout time.Duration, tailLines int) RunResult` and its body passes `command` straight to `exec.CommandContext(ctx, \"sh\", \"-c\", command)` (or `cmd /C` on Windows) with no templating — nothing in the function inspects or rewrites the string. The conclusion drawn from it is contradicted by the live registration. `.tp/config.json` at committed HEAD holds `\"cmd\": \"python3 scripts/check-spec-code-citations.py \\\"$(tp resume 2>/dev/null | python3 -c 'import sys,json;print(json.load(sys.stdin)[\\\"spec\\\"])')\\\"\"` — no spec path anywhere in it; the path is resolved at run time by `tp resume`. Registered at 4d155a88, 2026-09-02 15:12, two days before this spec. `git log -S'code-citation-drift' -- .tp/config.json 'spec/*.tasks.json'` returns four commits: two that DO hardcode (8df3d156 `spec/0.33.0.md`, 22b769c9 `spec/0.34.0.md`), one unrelated, and 4d155a88, which does not.","note":"The inference is the defect, not the reading of `RunCommand`. Because the command goes to `sh -c`, 'tp performs no substitution' does not entail 'the registration must hardcode a path' — the SHELL performs the substitution, and the repository's own live registration is built on exactly that. Whatever §3.1 concludes about `workflow.checks` has to survive that counterexample first."}
{"unit_id":"u52","anchor":"§3.1","text_sha":"b75f7c3e0f46","ordinal":1,"verdict":"FAIL","kind":"behaviour","tier":"run","evidence":"`/tmp/tp-dev/tp config --resolved` (built from HEAD with `go build -o /tmp/tp-dev/tp ./cmd/tp`) reports `\"checks\": {\"source\": \"project\", \"value\": [{\"class\": \"code-citation-drift\", \"cmd\": \"python3 scripts/check-spec-code-citations.py ...\"}]}` — one entry, not `[]`, and resolved from the project layer. It has been there since 4d155a88, 'chore(tp): register code-citation-drift at the project layer', 2026-09-02 15:12 — verified with `git show 85a46682:.tp/config.json`, which already carries the identical entry at the commit that added this spec's own prototype script.","note":"Every downstream clause inherits the error: 'unmechanized for four releases' is false as of 2026-09-02; 'the registration died with the release' is not what happened, since the current registration is at the PROJECT layer and cannot die with a release; and 'nobody was told it had come back' inverts the situation — it is back, and this spec was written after it came back. The measurement that would have caught this is the command the sentence itself names."}
{"unit_id":"u55","anchor":"§4","text_sha":"5b56b7b50c65","ordinal":1,"verdict":"PARTIAL","partial_kind":"reason-not-conclusion","kind":"corpus","tier":"query","evidence":"The conclusion holds on today's tree; the universal that is given as its reason does not survive an independent construction. I built the companion rule (a `spec/X.md §Y` pair matcher plus a per-target section index, fences skipped) and ran it over committed HEAD 020262d9: 35 raw pairs in `spec/*.md`, of which 2 are broken — and **both point at a LOWER-numbered spec**, which is exactly the case the reason says does not exist: `1.50.0.md:332 -> spec/0.41.0.md §2` (target absent) and `1.50.0.md:399 -> spec/0.31.0.md §8.4`. On inspection both are disqualifiable — the first is a deliberate historical citation whose own sentence says 'That filename is not today's `spec/1.41.0.md`', the second is my instrument's false positive, since 0.31.0.md carries §8.4 as a bolded numbered paragraph at line 100 rather than as a Markdown heading — so after disqualification the genuine broken-reference set is empty and the marginal yield is indeed zero.","note":"The recorded derivation exists but is no longer in the tree: `git show 85a46682:spec/candidates.md` gives '| broken sectioned refs across both trees, snapshots included | **312** | / | of those, target <= referrer (the only case `forward-spec-ref` cannot see) | **0** |'. The spec kept the conclusion and dropped both figures, so a reader cannot check it — which is what §2.3 defines as not checkable. And the property is not structural: nothing prevents a lower-numbered broken reference, and the only two candidates the corpus produces are both of that shape."}
{"unit_id":"u56","anchor":"§4","text_sha":"c92acad6b2f5","ordinal":1,"verdict":"PASS","kind":"corpus","tier":"query","evidence":"Measured with the same companion prototype at committed HEAD 020262d9: 586 raw `spec/X.md §Y` pairs inside `spec/.tp-review/` against 35 in `spec/*.md`, so 94.4% (586/621) of raw hits are in round snapshots; 246 of the 586 are broken under the same predicate. The record it derives from agrees on the proportion though not the counts: `git show 85a46682:spec/candidates.md` says '163 of its 173 raw hits are inside round snapshots' (94.2%).","note":"Counting rule: a raw hit is one regex match of a `spec/<version>.md` token followed within 24 characters by a `§<number>` token, outside fenced blocks, counted once per match. My totals differ from the record's by about 3.6x, which is an instrument difference I cannot reconcile — the record's matcher is not in the tree — but both give the same ratio and the same conclusion, and it is the ratio the unit asserts."}
{"unit_id":"u57","anchor":"§4","text_sha":"fb7a19c16f0b","ordinal":1,"verdict":"PASS","kind":"corpus","tier":"query","evidence":"Searched for the case directly and found none. Over committed HEAD 020262d9 the companion prototype's 2 broken `spec/X.md §Y` references in `spec/*.md` are the entire candidate set, and both fail on inspection (see u55): one is an annotated citation into a git object, one is a section that exists as a numbered paragraph. Over an archive of 85a46682 the same run gives exactly 1 candidate, `candidates.md:292 -> spec/0.31.0.md §8.4`, the same false positive. No mistyped section reference into a shipped, lower-or-equal-numbered spec exists in the corpus at either state.","note":"The one case that would be the companion's alone if it were counted — a reference whose target file does not exist and is numbered BELOW the referrer — does occur once (`1.50.0.md:332 -> spec/0.41.0.md`), and neither `forward-spec-ref` nor a resolution check would help there, because the referrer deliberately cites a path that exists only inside a git object. That is an argument for the non-goal, not against it."}
{"unit_id":"u59","anchor":"§4","text_sha":"1ca9dab7a360","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"A faithful restatement of CLAUDE.md:475-476: 'None was caught by checking that the target file exists — every one resolved, and every one had moved.' Independently consistent with a probe: an out-of-repo variant of the prototype that stats each target and skips absent ones loses **zero** findings, at committed HEAD 020262d9 (11 -> 11) and at 85a46682 (2 -> 2), so a resolution check discriminates nothing on this corpus in either state.","note":"The ~35 figure itself cannot be re-derived — the sweep repaired the citations it counted, so the state it measured no longer exists in any tagged tree — and this row grades the inference, not the number. The inference is the strongest thing §4 non-goal 2 rests on and it survives an independent check."}
{"unit_id":"u61","anchor":"§4","text_sha":"a1cdb8f05454","ordinal":1,"verdict":"NOT-A-CLAIM","kind":"code-structure","tier":"read","evidence":"A budget for a mechanism that does not exist. Nothing in the tree resolves any git string during lint: `grep -rn 'git tag|describe --tags' internal/ --include='*.go' | grep -v _test.go` returns only unrelated task-`--tag` flags in graph.go, listcmd.go and stats.go.","note":"The 'once per invocation' half is not free once §3's degradation contract is added: a `git` that fails must be distinguishable from a repo with no tags only insofar as both produce the empty string, which §3 says they do. So the cost is one subprocess and no caching question — the decision is coherent."}
{"unit_id":"u63","anchor":"§4","text_sha":"8b890352cdcc","ordinal":1,"verdict":"PARTIAL","partial_kind":"two-readings","kind":"code-structure","tier":"read","evidence":"The exit-code contract is exactly as stated: `internal/cli/lint.go:141-143` is `if result.Errors > 0 { os.Exit(ExitValidation) }` and nothing else in `runLint` exits on a finding, so a `warning` changes no exit code. Confirmed by running it — `/tmp/tp-dev/tp lint CLAUDE.md` reports 0 errors, 3 warnings, 17 info and exits 0. The count does not reproduce. Deriving the rule table from README with `awk -F'|' '/\\| `tp lint` \\|/ {print $2, $4}' README.md` gives **16** rules `tp lint` can emit, of which **9** have a severity cell containing `warning` (frontmatter, section-size, vague-language, duplicate-line, duplicate-paragraph, numbering-gap, broken-cross-ref, acceptance-quality, affected-files-scope).","note":"Same figure as u18 and it is wrong under both readings available here: 16 named rules, 9 that emit warnings, and 13 `Check*` functions — never 12. The substantive claim (no new severity, no new gate) is correct and independent of the number."}
{"unit_id":"u64","anchor":"§4","text_sha":"de4642a72299","ordinal":1,"verdict":"PARTIAL","partial_kind":"reason-not-conclusion","kind":"behaviour","tier":"run","evidence":"The conclusion holds; the reason given for it is false. 'lint does not read it' is refuted by running the shipped command: `/tmp/tp-dev/tp lint CLAUDE.md` succeeds, exits 0 and returns 20 findings (0 errors, 3 warnings, 17 info) — `runLint` lints whatever path `args[0]` names, and `parseSpecFile` has no spec-directory or naming restriction. What actually keeps the roadmap table out of scope is §2 step 1: `CLAUDE.md` is not a version-named file, so it yields no self-version and is skipped. That is a construction-level exemption, which is what the unit concludes.","note":"Worth stating in the spec, because the two reasons behave differently: 'lint does not read it' would also exempt a hypothetical `spec/ROADMAP.md`, while the real mechanism exempts every non-version filename and nothing else — including `spec/undecided.md` and `spec/candidates.md`, which the prototype reports as 22 non-version files skipped."}
{"unit_id":"u68","anchor":"§5","text_sha":"d5ffefbfd34d","ordinal":1,"verdict":"NOT-A-CLAIM","kind":"corpus","tier":"query","evidence":"A test-table row specifying an assertion and its mutant. Both halves check out against the world. The assertion is the prototype's measured behaviour on exactly this pairing: in an archive of 85a46682, `python3 scripts/forward-spec-ref-prototype.py spec 1.0.0` prints `1.0.0.md:409 -> spec/1.38.0.md` — a referrer at 1.0.0, a target at 1.38.0, boundary 1.0.0, reported once, carrying the reference's line number. The mutant's premise is true: `grep -rn 'forward-spec-ref' internal/ --include='*.go'` returns nothing, so `return no finding` is today's shipped behaviour.","note":"The row is discriminating: because the rule does not exist, the mutant is the current tree, and a test written from this row fails before the implementation and passes after — which is the shape CLAUDE.md's 'write the test first and watch it fail' rule asks for."}
{"unit_id":"u69","anchor":"§5","text_sha":"4ce1fe7442d7","ordinal":1,"verdict":"NOT-A-CLAIM","kind":"corpus","tier":"query","evidence":"A test-table row whose two factual premises are measured. The pairing is real, not invented: `sed -n '566p' spec/0.36.0.md` names `spec/0.37.0.md`, and both v0.36.0 and v0.37.0 are tags. The mutant does produce it: `diff` of `proto spec 1.0.0` against `proto spec 1.0.0 --no-boundary` adds exactly `0.36.0.md:566 -> spec/0.37.0.md` and nothing else, at committed HEAD 020262d9 and at 85a46682 alike.","note":"This is the only §5 row whose mutant is demonstrated on the live corpus rather than on a fixture, which is why §2.1 can call it 'the measured false positive'. Note the boundary the row fixes (1.0.0) is the newest tag today and will not be tomorrow; a fixture that hard-codes it stays valid because the row also fixes the referrer and target, but the row is worth reading as three fixed strings rather than as 'the current boundary'."}
{"unit_id":"u70","anchor":"§5","text_sha":"07b1f0dc2d2b","ordinal":1,"verdict":"NOT-A-CLAIM","kind":"corpus","tier":"query","evidence":"A test-table row; its assertion is measured on the live corpus. `python3 scripts/forward-spec-ref-prototype.py spec 0.36.0` at committed HEAD 020262d9 prints `0.36.0.md:566 -> spec/0.37.0.md` — the same pair that boundary 1.0.0 suppresses is reported at boundary 0.36.0, so the answer really does turn on the caller's string and rows 2 and 3 together pin it from both sides.","note":"Rows 2 and 3 are a matched pair over one input with only the boundary varying, which is the shape that discriminates a hard-coded constant from a parameter. The mutant's stated harm ('makes the rule's answer depend on when it was compiled') is exact: 0.36.0 was the newest tag for part of this repository's history and 1.0.0 is now."}
{"unit_id":"u71","anchor":"§5","text_sha":"7fb97df4c485","ordinal":1,"verdict":"NOT-A-CLAIM","kind":"corpus","tier":"query","evidence":"A test-table row whose factual premise is confirmed twice, in two different tree states. At the 51-file pre-renumbering state dc8213d2, `diff` of the boundaried run against `--no-fence-skip` adds exactly two lines, `0.39.0.md:40 -> spec/1.0.0.md` and `0.43.0.md:27 -> spec/1.0.0.md`, and both are shell-transcript lines inside fences (`$ tp audit spec/1.0.0.md --status --check   ->  exit 0` and `$ tp audit spec/1.0.0.md --role no-such-role --affected-files main.go`). At committed HEAD 020262d9 the same diff adds four, all transcripts in `spec/1.0.1.md` (`$ tp ground spec/1.57.0.md | ...`, `$ tp ground spec/1.58.0.md | ...`, `$ tp ground spec/1.56.0.md --units | ...`, `$ tp ground spec/1.56.0.md --status --check; echo $?`).","note":"The origin story §2.2 tells is verified, and it is stronger than §2.2 claims — see the row filed against §2.2's own heading. The mutant is discriminating on the live corpus today, which the spec says it is not."}
{"unit_id":"u72","anchor":"§5","text_sha":"fef8e8f9839d","ordinal":1,"verdict":"NOT-A-CLAIM","kind":"mechanism","tier":"probe","evidence":"A test-table row. The behaviour it asserts is the prototype's, checked by reading and by running: `refs()` (scripts/forward-spec-ref-prototype.py:40-48) sets `fenced = not fenced` on every ```-prefixed line and `continue`s while `fenced and skip_fences`, so an unterminated fence leaves `fenced` true to end of file and every later line is skipped; there is no unbalanced-fence branch, so nothing can panic. Probed on a constructed file with one opening fence and two later references: 0 findings, exit 0.","note":"The mutant ('treat an unterminated fence as closed at the next line') would restore the transcript hits u71 measures, so its harm is demonstrable rather than argued. One caveat for the implementing task: the row asserts a NEGATIVE (nothing is reported), so it passes on any implementation that reports nothing at all — it needs a companion assertion in the same test that the same references ARE reported once the fence is closed, or it cannot fail for the reason it names."}
{"unit_id":"u73","anchor":"§5","text_sha":"c05bf6884f55","ordinal":1,"verdict":"PARTIAL","partial_kind":"reason-not-conclusion","kind":"mechanism","tier":"probe","evidence":"The assertion holds and the mutant's stated consequence does not. Assertion: `SELF = re.compile(r'^(\\d+\\.\\d+\\.\\d+)\\.md$')` rejects both names, and every prototype run reports '22 non-version files skipped' at committed HEAD, `candidates.md` and `0.35.0-candidates.md` among them. Mutant: I built it — a variant whose self-version falls back to `^(\\d+\\.\\d+\\.\\d+)` on any leading digits — and ran it over `spec/` at committed HEAD 020262d9 and at 85a46682. It adds **zero** findings at both states. The reason is checkable directly: `grep -oE 'spec/[0-9]+\\.[0-9]+\\.[0-9]+\\.md' spec/0.35.0-candidates.md | sort | uniq -c` returns exactly one reference, `spec/0.35.0.md`, which is equal to the referrer's own version and therefore excluded by `>`.","note":"So the row names a real file as the mutant's victim and that file cannot be one: a test whose fixture is `spec/0.35.0-candidates.md` as it stands passes under the mutant and under the correct rule alike. Fixing it costs a constructed fixture — a file named `0.35.0-candidates.md` containing a reference to `spec/1.38.0.md` — which is what the row should say, since §2.2 already accepts constructed fixtures for exactly this reason."}
{"unit_id":"u74","anchor":"§5","text_sha":"be2d0af692f8","ordinal":1,"verdict":"NOT-A-CLAIM","kind":"mechanism","tier":"probe","evidence":"A test-table row; the equivalence its mutant claims is measured exactly. In an out-of-repo probe over `spec/`, the finding SET at boundary 0.0.0 equals the finding set of `--no-boundary` — 12 == 12 at committed HEAD 020262d9 and 3 == 3 at 85a46682, set-equal in both, not merely equal in count. So 'treat empty as 0.0.0' is not merely similar to §2.1's refuted draft; on this corpus it is that draft.","note":"The row asserts a negative ('reports nothing') and, unlike row 5, immediately supplies the discriminator — 'on input that would otherwise report' — which is the clause that stops it passing vacuously. That clause is load-bearing and should survive into the test."}
{"unit_id":"u75","anchor":"§5","text_sha":"a8b200b6ecff","ordinal":1,"verdict":"PARTIAL","partial_kind":"reason-not-conclusion","kind":"mechanism","tier":"probe","evidence":"The assertion is sound; the mutant's stated consequence overreaches. Probed at committed HEAD 020262d9: treating an empty `selfVersion` as 0.0.0 for the 22 non-version files, with boundary 1.0.0 still applied, flags **12 of the 40** `spec/<version>.md` references those files contain — 30%, in 2 files (`candidates.md` and `trim-pass.md`) out of 22, not 'every reference in every non-version file'. The other 28 references are below the boundary and stay unreported, because the boundary clause survives the mutation.","note":"The mutant still discriminates (12 > 0), so the row is a real test; only its stated harm is wrong, and wrong in a direction that matters — 'every reference in every non-version file' is what the mutant would do if the boundary clause were ALSO dropped, which is row 7's mutant, not this one. Stating it as 'flags references in files the rule is meant to skip' would be both true and sufficient."}
{"unit_id":"u76","anchor":"§5","text_sha":"d2bbbdeb695d","ordinal":1,"verdict":"NOT-A-CLAIM","kind":"mechanism","tier":"probe","evidence":"A test-table row; its mutant's stated consequence is now demonstrable on the live corpus. A probe replacing `t > self_v` with `t >= self_v` adds **8** findings at committed HEAD 020262d9, every one a self-reference: `1.43.0.md:32`, `1.43.0.md:180`, `1.44.0.md:167`, `1.45.0.md:5`, `1.45.0.md:140`, `1.49.0.md:127`, `1.49.0.md:128`, `1.49.0.md:135`. `sed -n '5p' spec/1.45.0.md` is a blockquote line in the file's own header block reading '`git log --oneline -- spec/1.45.0.md` returns a single commit', which is the case the row names.","note":"At the authoring commit 85a46682 the same mutant added **zero** — no spec then named its own filename in the path form — so the row's stated consequence became demonstrable between the spec being written and now. It does not matter for a fixture-based test, but it is a second instance of a claim in this spec whose truth value turned over within a day."}
{"unit_id":"u77","anchor":"§5","text_sha":"9a31accb12c9","ordinal":1,"verdict":"PARTIAL","partial_kind":"reason-not-conclusion","kind":"mechanism","tier":"probe","evidence":"The assertion is a legitimate fixture requirement; the justification clause is contradicted by the corpus and by this spec's own §4. I built the mutant — the prototype plus `if not os.path.exists(os.path.join(specdir, target + '.md')): continue` — and ran it over `spec/` at committed HEAD 020262d9 and at 85a46682. It loses **zero** findings in both: 11 -> 11 and 2 -> 2. So it does not 'silently exempt exactly the references a renumbering has already broken' — it exempts none of them, because, as u59 and u6 both say, every one of the ~35 stale citations RESOLVED. A stat check can only exempt a reference whose target is absent, and the only three absent-target references anywhere in `spec/*.md` are `1.50.0.md:332 -> spec/0.41.0.md` (below the referrer), `1.53.0.md:209 -> spec/0.53.0.md` (below) and one in `undecided.md` (a non-version file, skipped).","note":"Second, structural: §3 fixes the signature as `CheckForwardSpecRefs(lines []string, selfVersion, shippedBoundary string) []Finding`, which receives no directory, so the mutant this row names cannot be written inside the engine at all except by statting relative to the process cwd. A mutant that the release's own architecture forbids is not a mutant the test can be built against. Both points push the same way: row 10 needs a constructed fixture and cannot be justified from the corpus."}
{"unit_id":"u78","anchor":"§5","text_sha":"53ed856ffb22","ordinal":1,"verdict":"PASS","kind":"corpus","tier":"query","evidence":"The distinction it draws is correct and independently confirmed. 'A reference that resolves to a file whose subject has moved is the state this rule catches': all 11 findings at committed HEAD 020262d9 and both at 85a46682 point at targets that exist on disk (`spec/1.56.0.md`, `1.41.0.md`, `1.48.0.md`, `1.55.0.md`, `1.50.0.md`, `1.45.0.md`, and at 85a46682 `1.38.0.md`, `1.53.0.md` — each checked with `[ -f spec/<t>.md ]`). 'A reference that no longer resolves is the end state of the rot': only three unresolvable `spec/<version>.md` references exist anywhere in `spec/*.md`, and none is in the finding set.","note":"The first sentence — that row 10 decides whether the rule is worth shipping — is a judgement, and the measurement above undercuts the support u79 offers for it: on this corpus a stat-skipping implementation and the correct one are indistinguishable, so row 10 decides nothing that can be observed without a constructed fixture. The distinction the second sentence draws is still the right one to test; it just cannot be tested against the tree."}
{"unit_id":"u79","anchor":"§5","text_sha":"af36ce20026a","ordinal":1,"verdict":"FAIL","kind":"mechanism","tier":"probe","evidence":"Built the implementation the sentence describes — the prototype with `if not os.path.exists(os.path.join(specdir, target + '.md')): continue` added to the reporting branch — and ran it over `spec/` in out-of-repo archives of two states. At committed HEAD 020262d9 it reports **11 findings, the identical set** the unrestricted rule reports; at 85a46682 it reports **2, the identical set**. The difference is empty in both directions: `set(baseline) - set(stat_skip)` is the empty set. It does not report nothing; it reports everything.","note":"The sentence is refuted by two other units of the same spec. u59 says 'Every one of the ~35 stale citations resolved', and u6 says 'not one was caught by checking that the target file exists, because every one of them resolved' — if every citation resolves, a stat-and-skip implementation skips none of them and its output is identical to the correct rule's. That is exactly what the probe measures. The second clause ('passes rows 1-9') is also unsupported: rows 1-9's fixtures name targets like `spec/1.38.0.md` with no directory in the signature to resolve them against, so under a cwd-relative stat the mutant would fail row 1 rather than pass it. Both halves point the same way: row 10 cannot carry the weight u78 puts on it, and the sentence that says it can is the one measurably false."}
{"unit_id":"u80","anchor":"§5","text_sha":"46d040c1c137","ordinal":1,"verdict":"PASS","kind":"document","tier":"read","evidence":"The prescription is a decision; the historical claim behind it is recorded and exact. CLAUDE.md's measurement-traps entry: 'a fixture's own *filename* can decide the result — a guard blocking a directory named `zz` proved nothing about a stopped scan, because `zz` sorts after every `*.tasks.json` and `WalkDir` had already collected them; renaming it `A-blocked` falsified the test's own doc comment. **When a fixture's incidental property could carry the verdict, assert the property**.' The precondition the row asks a test to assert is currently satisfiable: `ls spec/*.md | xargs -n1 basename` splits 40 version-named against 22 non-version-named at committed HEAD 020262d9.","note":"This is the best-constructed row in §5 and the one the rest of the section does not follow: rows 6, 8 and 10 each rest on a corpus property that was NOT asserted and that measurement shows to be absent (u73, u75, u77). Applying this row's own standard to them is the cheapest repair available to the next round."}
{"unit_id":"u4","anchor":"§0","text_sha":"c9149a96061c","ordinal":1,"verdict":"FAIL","kind":"corpus","tier":"query","evidence":"Filed against a unit the arms CUT. Its text, canonicalised, is 'This one exists because a reference to a pending spec is not wrong the day it is written — it is wrong the day that spec is renumbered, and that has now happened five times.' (snapshot lines 6-8); the `text_sha` is mine, computed the same way the index computes one — verified by reproducing u3's published `9c5874db7c50` from its text with the same method. The ordinal is refuted at its source. `CLAUDE.md:472`, the sentence that governs this convention, says: '**The ordinal is not checkable and this file used to assert one** — v1.0.0's grounding of the patch release found `CLAUDE.md` saying *fourth* while `spec/candidates.md` said *fifth*, and a repair unit refused to pick a side. What git records is **one** renumbering commit, `e73788ab` (2026-09-02, 16 spec renames) ... Earlier renumberings predate the tracked names or were not recorded as renames, so cite the sweep rather than a count.' Re-derived: `git log --diff-filter=R --name-status -- 'spec/*.md'` returns exactly one commit with 16 renames.","note":"This is the sharpest thing in the cut set, and it was cut. The number five is not merely unsupported — it is the specific figure a grounding round already adjudicated and CLAUDE.md already withdrew, and this spec is the second document to restate it after the withdrawal (`spec/candidates.md`'s `forward-spec-ref` section, from which this spec was written, carries 'that has now happened five times' verbatim at `git show 85a46682:spec/candidates.md`). The floor carried the same ordinal a second time in u6, which the arms kept; so the claim survives in the graded set, but the sentence that makes it load-bearing for the whole release — 'This one exists because ...' — owed no disposition. The rule's motivation does not need a count: 'a renumbering has happened, and this repository records one with 16 renames' is both true and sufficient."}
{"unit_id":"u53","anchor":"§3.1","text_sha":"a4d32abd094a","ordinal":1,"verdict":"PARTIAL","partial_kind":"reason-not-conclusion","kind":"code-structure","tier":"read","evidence":"Filed against a cut unit; its canonicalised text is 'A rule whose subject is **the spec's own text** does not need that machinery, and putting it there buys the suppression hazard for nothing.' (snapshot lines 133-134), `text_sha` computed by the method verified against u3. The conclusion is sound and the two sentences that support it are the two units this round found false. u51's 'every registration hardcodes a spec path — and every one has' is contradicted by `.tp/config.json`'s live `$(tp resume ...)` command, and u52's '`tp config --resolved` reports `checks: []` today' is contradicted by running that command, which returns one project-layer entry. What survives independently is the suppression hazard CLAUDE.md records ('a check registered before its subject exists suppresses that finding class for every reviewer'), and that hazard is about registering a check EARLY, not about a registration dying with a release.","note":"So §3.1 reaches the right answer — a rule over the spec's own text belongs in lint — through an argument whose two factual premises are both false at HEAD and were both false when written. That is the failure shape this repository names as reaching the right defect for the wrong reason. The decision does not need repairing; the paragraph does."}
{"unit_id":null,"anchor":"§2.2","text_sha":"78b38eec38d8","ordinal":1,"verdict":"FAIL","kind":"corpus","tier":"query","evidence":"A claim the index never carried, because it is a section HEADING and headings are not units: '### 2.2 Fenced blocks, and why the live corpus cannot demonstrate this' (snapshot line 74). `text_sha` is mine over that line. The live corpus can demonstrate it, at committed HEAD 020262d9: `diff` of `python3 scripts/forward-spec-ref-prototype.py spec 1.0.0` against the same command with `--no-fence-skip` adds exactly four findings, and all four are shell-transcript lines inside fences in `spec/1.0.1.md` — line 130 `$ tp ground spec/1.57.0.md | python3 -c ...`, line 132 `$ tp ground spec/1.58.0.md | ...`, line 200 `$ tp ground spec/1.56.0.md --units | ...`, line 327 `$ tp ground spec/1.56.0.md --status --check; echo $?`. These are precisely the case §2.2 describes — 'a fixture spec named inside a shell session, not a reference to anything'.","note":"The heading was true at the authoring commit 85a46682, where the two runs return the identical 2 findings; `spec/1.0.1.md` was created at 09:07 the same morning and brought four of them. The consequence is that §2.2's stated limit and §2.2's stated remedy have swapped: the release no longer needs a constructed fixture to justify the fence toggle, and a corpus assertion in the shape u80 prescribes ('the tree contains at least one fenced `spec/<version>.md` reference') is now satisfiable. Every graded unit in §2.2 (u33, u34) inherits the framing this heading sets, which is why it is filed separately."}
