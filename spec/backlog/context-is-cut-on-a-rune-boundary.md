# tp — Context is cut on a rune boundary — folded

Folded on 2026-09-11 into `an-unreadable-file-is-named.md`, which widened to *tp says when it could
not read*.

| what moved | where |
|---|---|
| §2, a lint finding's `Context` cut on a rune boundary, cap unchanged | `an-unreadable-file-is-named` §6 item 1 |
| §3 Non-Goal 1 (cap not raised, not a character count) | `an-unreadable-file-is-named` §7 Non-Goal 7 |
| §4 rows 1 and 1b | `an-unreadable-file-is-named` §8 rows 8 and 8b |
| the same byte-for-character cut in the review's finding identity, routed from `a-findings-exits-agree-measurements.md` | `an-unreadable-file-is-named` §6 item 2 and row 9 |

Dropped: nothing. As a standalone release it was one task, which is why it folded into the spec that
already touches `tp lint`.

Its `-measurements.md` sidecar and any `.tp-review/context-is-cut-on-a-rune-boundary/` rounds stay
as history.
