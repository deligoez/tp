# tp — Guards read what production reads — dropped

Dropped on 2026-09-11: its guards bind a frozen shipped spec (`spec/1.0.0.md`); every hole needs an
edit no release makes, and the false failures are loud.

| what moved | where |
|---|---|
| §2, the half that corrects the enum guard's doc comment | `refusals-that-name-nothing` §8, last paragraph |
| sidecar, *`engine.UnitKinds` is reachable only from tests* | `refusals-that-name-nothing` §7 — the runner map's refusal lists the unit kinds, which is the production caller the accessor's comment promises |

Dropped, for the reason above: §2's binding of §7.2's `partial_kind` cell to the code, §3's three
helper tasks, §4's Non-Goals and §5's rows 1 and 1b.

Its `-measurements.md` sidecar and any `.tp-review/guards-read-what-production-reads/` rounds stay
as history.
