# tp — Floor anchors need fixtures — folded

Folded on 2026-09-11. It was never a release: six fixtures, no decision, and no field report.

| what moved | where |
|---|---|
| §2, the six `FloorAnchorOf` fixtures | `the-floor-names-what-it-cut` §7.1, as one test task; the fixtures, as built and run, in its sidecar under *The anchor fixtures* |
| §3 Non-Goal 1 (the family's notion of a code block is not reopened) | the same sidecar section |
| §4, the fixtures as acceptance | `the-floor-names-what-it-cut` §7.1 |

**Dropped:** §3 Non-Goal 2 (no workflow field, gate or convergence effect) — the host spec's own
Non-Goal 2 says the same.

Its `floor-anchors-need-fixtures-measurements.md` sidecar stays as history; it never ran a round of
its own, so there is no `.tp-review/floor-anchors-need-fixtures/`.
