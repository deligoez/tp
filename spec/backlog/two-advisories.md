# tp — Two advisories — folded

2026-09-11. This file is a forwarding stub; it stays so that citations of it in shipped specs and
round files keep resolving.

| what moved | where |
|---|---|
| §3 the untracked-task-file advisory, Non-Goals 1–2 and 7–9 as they bear on it, rows 8–14 | `a-task-file-write-names-its-target` §6, §7 Non-Goal 7, §8 rows 21–26; derivations and testimony in its sidecar under *Carried from `two-advisories`* |
| the raw-stderr advisory sweep (sidecar, *Routed here at the 2026-09-08 re-verification*) | `an-unreadable-file-is-named` |
| the payload-plus-notice channel rule (sidecar, *Decided at the 2026-09-08 decision pass*) | `an-unreadable-file-is-named` |

**Dropped: §2, the binary advisory, with rows 1–7.** By its own Non-Goal 5 it fires only inside tp's
own repository, and there the orchestrator rule in `CLAUDE.md` and `scripts/audit-round-prep.py`
already build the binary each round runs against.

Its `two-advisories-measurements.md` sidecar and its `spec/backlog/.tp-review/two-advisories/` rounds
stay as history.
