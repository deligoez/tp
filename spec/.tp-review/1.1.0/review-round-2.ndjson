{"category":"consistency","class":"undeclared-change-to-unchanged-keys","evidence":"Read internal/cli/review_merge.go:190-198 — `missingFindingFields` loops `location, severity, finding` and tests `if s, _ := row[k].(string); s == \"\"`, with no TrimSpace. Probe at `HEAD` in the clone: `tp review --merge wsloc.ndjson` on a single row `{\"severity\":\"high\",\"category\":\"c\",\"location\":\" \",\"finding\":\"F\",\"suggestion\":\"s\"}` exits 0 and emits the row unchanged (`merged: 1 unique findings from 1 files`). Under §2's shared predicate that same row is skipped and, being alone in its file, turns the input into a dropped input — exit 1 by §5 row 5's rule. Neither outcome is stated anywhere in the spec for the three pre-existing keys.","finding":"Decision 2 says `--merge` \"skips a row missing one of them with a warning on stderr, **as it already does for the first three**\" — an explicit claim that the release changes nothing about `severity`, `finding` and `location` at `--merge`. §2's *One predicate, called from both gates* paragraph contradicts it: \"A value that is empty once `strings.TrimSpace` has run counts as empty … **for `severity`, `location` and `finding` as much as for `evidence`**\". `--merge` today tests `s == \"\"`, which §5 row 9's own mutant column states as fact. So the release does change what `--merge` accepts for three keys it says it leaves alone, across the whole existing corpus, and no §5 row pins it: row 9's fixture puts `\" \"` in `evidence` only and says its second row is \"legal on all four keys\", so a mutant that applies TrimSpace to `evidence` alone and leaves the other three on `==\"\"` is green in every row of §5.","found_by":1,"found_by_roles":["architect"],"location":"§1","resolved":{"evidence":"Fixed by c951674d and 4b5aae1c, confirmed in the current text rather than in the commit messages. Decision 2 at spec/1.1.0.md:33-36 now reads only \"--merge skips a row missing one of them with a warning on stderr; --record refuses it\" -- a regex for \"as it already does\" over the file returns no match, so the contradiction with the shared-predicate paragraph in section 2 is gone. Section 5 row 11 (spec/1.1.0.md:147) is the row this finding asked for: its fixture's only defect is a whitespace-only location, and its mutant is HEAD's missingFindingFields testing x == \"\". The mutant this finding named -- TrimSpace applied to evidence alone, green in every row of section 5 -- is now red against row 11. Residual, recorded rather than claimed closed: the predicate names four keys while only evidence (row 9) and location (row 11) are pinned by a row; severity and finding are pinned by nothing.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"architect","severity":"high","suggestion":"Decide it in one place. Either narrow §2's predicate paragraph to `evidence` and say the other three keep `== \"\"` at `--merge` (matching decision 2's \"as it already does\"), or keep the shared predicate and delete the \"as it already does for the first three\" clause from decision 2, add the widening to §2 as a stated consequence, and give §5 a row whose fixture is a row whose only defect is a whitespace-only `location`."}
{"category":"completeness","class":"injection-path-not-enumerated","evidence":"In /tmp/rev-regression-bD7Ao6/clone: `tp review s.md --round 2 --findings f.ndjson` emitted four role prompts each containing `UNRESOLVED findings from previous rounds — DO NOT re-report:\\n  [HIGH] consistency — §1: F1` with no evidence text; `tp review s.md --verify --findings f.ndjson` emitted `Unresolved findings (still open):\\n  1. [high] consistency — §1: F1`; and the round-2 regression prompt this unit was handed by `tp review spec/1.1.0.md --role regression` carries `## Previously fixed findings` with lines of the form `- \u003cfinding\u003e — \u003cresolution evidence\u003e`. Source read: internal/cli/review_regression.go:178-188 (prints f.Resolved.Evidence), internal/cli/review.go:131-139 (reviewFinding carries no evidence field) and 1105-1112. Compared spec/1.1.0.md:45-48 and :153 against spec/.tp-review/1.1.0/snapshot-round-1.md, which has neither.","finding":"Decision 5 is new this repair and says `the previous-round findings tp injects into the next round's prompt carry each finding's evidence beside its finding text`; row 10 pins it against `the role prompts for the round after it`. Measured, tp has three distinct previous-findings injection sites and neither the decision nor the row names any of them: the panel role prompt's `UNRESOLVED findings from previous rounds — DO NOT re-report:` block, the regression prompt's `## Previously fixed findings` block, and `--verify`'s `Unresolved findings (still open):` block. The second already renders `- \u003cfinding text\u003e — \u003cresolved.evidence\u003e`, which is exactly the `evidence beside its finding text` shape decision 5 describes but sourced from a different key. So an implementer can satisfy row 10 in the panel block alone and leave the write-only defect round-1 index 2 raised intact at two of the three sites, or can read decision 5 as already satisfied by the regression block and build nothing there. This is the same class round-1 index 1 was fixed for — an unenumerated set of emission paths in row 8 — reintroduced by the release's own new decision, in a different section.","found_by":0,"location":"§1","resolved":{"evidence":"Fixed by 86374665, which cut decision 5 (the previous-round injection carrying each finding's evidence) and section 5 row 10 with it; the former decision 6 renumbered to 5. Confirmed in the current text: spec/1.1.0.md holds five decisions, the fifth being release sizing, and no sentence names an injection site, a previous-round prompt or a role prompt's unresolved-findings block. The unenumerated set of injection paths this finding reported has no subject left in the spec. The read-back question the decision was carrying was routed to The evidence contract under Undecided in spec/undecided.md (d0c99d9f); the injection measurements went to spec/1.1.0-measurements.md.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"regression","severity":"high","suggestion":"Name the injection sites decision 5 covers, and say whether the regression block's existing `resolved.evidence` render satisfies it or must sit beside the finding's own `evidence`. Then give row 10 a fixture that pins more than one site, the way row 8's fixture now does, so an implementation that reaches only the panel summary goes red."}
{"category":"consistency","class":"quantifier-ignores-existing-cap","evidence":"Read internal/cli/review.go:1303-1410: `findingsCap := 50` (l.1347), `if len(text) \u003e 80 { text = text[:80] + \"...\" }` (l.1361), wontfix evidence `if len(evidence) \u003e 40` (l.1366), and the resolved channel at l.1382-1406 printing only `[RESOLVED] %s: %s`. Probe in the clone: a 57-row findings file (55 unresolved with `SENTINEL-UNRES-nn` evidence, one wontfix and one fixed-high each carrying a distinct top-level `evidence` sentinel), fed to `tp review s.md --no-state --round 2 --findings big.ndjson --role architect`. Result: `detailed lines shown: 50`, `omitted-for-brevity present: True`, `... and 6 more (omitted for brevity)`, first detailed row truncated at 80 chars, wontfix line `[WONTFIX] consistency — §W: WONTFIX-ROW (wontfix: RESOLVER-JUSTIFICATION-WHICH-IS-LONG-ENO...)`, and the fixed-high row reduced to `[RESOLVED] §R: FIXEDHIGH-ROW`. This input is not one the repair's own set reaches: row 10 fixes the round at one row.","finding":"Decision 5 says the injected previous-round findings \"carry **each** finding's `evidence` beside its finding text\". The injection it names is not a flat list and has never carried every finding: it is three channels with two caps and two truncations. Detailed rows (unresolved + wontfix) are capped at 50, with the remainder printed only as \"… and N more (omitted for brevity)\"; each finding's text is truncated at 80 characters; a wontfix row's `resolved.evidence` is truncated at 40; and rows resolved `fixed`/`duplicate` reach the prompt only as a bare count plus, for high/critical, a `[RESOLVED] \u003clocation\u003e: \u003cfinding\u003e` line with no evidence slot at all. Decision 5's universal quantifier therefore either (a) silently changes those caps — a change to the `.tp-review` injection contract no section declares and no Non-Goal fences — or (b) is false for every finding past the 50th and for every resolved finding. §5 row 10's fixture is \"a recorded round holding **one** row\", which is below every cap, in the only channel that carries full detail, and whose evidence length is unpinned — so no row in §5 can tell (a) from (b).","found_by":1,"found_by_roles":["architect"],"location":"§1","resolved":{"evidence":"Fixed by 86374665: decision 5 was cut entirely, so the universal quantifier over an injection channel that is capped at 50 detailed rows, truncated at 80/60/40 characters and split across three sub-channels no longer exists. Confirmed in the current text -- no sentence in spec/1.1.0.md names the previous-round block, a cap or a truncation, and row 10 is now the --merge -o row rather than the injection row. The caps and channels this finding measured are untouched by the release, which is now the release's stated position rather than an undeclared change to them.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"architect","severity":"high","suggestion":"Scope decision 5 to the channel it means — e.g. \"each finding in the detailed unresolved/wontfix listing carries its `evidence`\" — and state whether the evidence is truncated and at what length, or say explicitly that the 50-finding cap and the resolved-findings channel are unchanged and carry no evidence. Then make row 10's fixture pin what its SHALL asserts, as §5's own preamble now requires: a round whose detailed rows exceed the cap and whose evidence is longer than the truncation length."}
{"category":"feasibility","class":"merge-writes-output-then-refuses","evidence":"Ran in a `git clone --no-hardlinks` of HEAD (8e41f111) with a HEAD-built binary: `tp review --merge role-a.ndjson role-b.ndjson -o merged.ndjson` where role-a's single row omits `location` (the same `missingFindingFields` slice `evidence` joins). Output: `warning: skipping incomplete line (missing location) in role-a.ndjson`, the JSON summary with `merged_count: 1`, then `{\"error\":\"no line parsed in role-a.ndjson ...\",\"code\":1}`; merge exit=1. `ls -l merged.ndjson` -\u003e 149 bytes, present, containing ONLY role B's row. Re-running the REFERENCE.md guard `[ -f merged.ndjson ] || tp review --merge ...` left the file at 1 row — the merge never re-ran. REFERENCE.md:182 is the recipe quoted; REFERENCE.md:213 is the `unit-failure` retry stop reason.","finding":"`tp review --merge ... -o \u003cfile\u003e` WRITES the merged output and THEN exits 1 when an input parsed zero rows. §5 row 5 makes that exit-1 path the expected one for a role file whose row omits `evidence`, but no decision and no row says what is on disk after the refusal. The consumer is tp's own driver: REFERENCE.md's `review-record` unit command is `[ -f $TP_ROUND_DIR/merged.ndjson ] || tp review --merge $TP_ROUND_DIR/role-*.ndjson -o $TP_ROUND_DIR/merged.ndjson; tp review \u003cspec\u003e --record $TP_ROUND_DIR/merged.ndjson`. The separator is `;`, not `\u0026\u0026`, so `--record` runs on the truncated file the refused merge left behind; and the `[ -f ]` idempotence guard sees that file on the `unit-failure` retry and skips the merge, so a retry can never repair it. The round records with one role's rows silently absent. Today this is reachable only when a role omits `location`/`severity`/`finding` — three keys every emitted prompt names. §2 adds a fourth key no model has been asked for before, and §2's own last paragraph measures that every file in this corpus fails the merge under it, so the release moves this from a rare path to the expected one.","found_by":1,"found_by_roles":["implementer"],"location":"§2","resolved":{"evidence":"Fixed by 4b5aae1c and ab3430ec, both confirmed in the current text. Row 10 (spec/1.1.0.md:146) is new: WHEN tp review --merge exits non-zero, tp SHALL leave no file at the -o path; its fixture is a dropped input beside a legal file with the -o path absent beforehand, and its mutant is HEAD, which writes the surviving row and then exits 1. Section 3 (spec/1.1.0.md:105-107) now puts REFERENCE.md's review-record unit command in scope for the same task, naming the reason: row 10 changes what a refused --merge leaves on disk, and that command chains the merge into --record with a semicolon behind a [ -f ] guard.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"implementer","severity":"high","suggestion":"Add a decision and a §5 row over what `--merge -o` leaves on disk when an input is dropped: either write nothing on a refusal (build in a temp file, rename only at exit 0), or make the refusal precede the write. Whichever is chosen, name the `review-record` recipe as in scope for the same task alongside §3's REFERENCE.md work — `;` must become `\u0026\u0026`, or the `[ -f ]` guard must stop treating a partial merge as done."}
{"category":"ambiguity","class":"fixture-does-not-pin-the-mutants-discriminator","evidence":"Built the fixture from the cell alone in a git clone --no-hardlinks at /tmp/tester-r2-Xfrfnu/repo, implemented decision 5 two faithful ways (A: evidence truncated at 40, mirroring buildFindingsSummary's existing resolved.evidence rule at review.go:1366-1368; B: no truncation), recorded a one-row round and emitted the next: impl A short sentinel (21 chars) sentinel_hits=3 PASS, impl A long sentinel (110 chars) sentinel_hits=0 finding_text_hits=3 FAIL, impl B long sentinel sentinel_hits=3 PASS. Separately measured the shipped truncation at HEAD: a 60-char resolved.evidence reached the prompt as '(wontfix: WFEVIDENCE012345678901234567890123456789...)' with its 'TAILMARKER' tail occurring 0 times. Corpus evidence lengths (both .tp-review globs, 1675 resolved.evidence strings): median 463, only 5.79% are \u003c= 40 chars, so a fixture author writing a realistic sentinel lands in the red half.","finding":"Row 10's fixture cell pins the sentinel's UNIQUENESS ('occurring nowhere else in the spec or in the prompt scaffolding') but not its LENGTH, and length decides the row's verdict. The function row 10 extends, buildFindingsSummary, already truncates the only evidence it injects — a wontfix row's resolved.evidence — at 40 characters, and its finding text at 80. A test built from the fixture cell alone is green with a 21-character sentinel and RED with a 110-character one against the SAME faithful implementation. This is the class the repair's new preamble sentence was added to close, reappearing in the row the same repair added.","found_by":1,"found_by_roles":["tester"],"location":"§5","resolved":{"evidence":"Fixed by 86374665: the row this finding is about -- the former row 10, whose fixture pinned a sentinel's uniqueness but not its length -- was cut together with decision 5. Confirmed in the current text: row 10 is now the --merge -o row, and no row anywhere in section 5 names a sentinel or depends on a rendering budget. The unpinned discriminator has no row left to be unpinned in.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"tester","severity":"high","suggestion":"State the value in the fixture cell: 'the sentinel is at most 40 characters and occurs nowhere else', or state the SHALL against a value the mutant turns on rather than against observability — e.g. 'the finding struct the round file is parsed into carries the evidence text', which a unit test on findingFromRow can assert without depending on any rendering budget. Alternatively take the truncation decision in §1 decision 5 and say what the prompt renders."}
{"category":"completeness","class":"prompt-contract-incomplete-key-set","evidence":"Built the exact JSON object line from `codeAuditOutputFormat` (internal/cli/review.go:1766, read via codedbpro), added `\"evidence\":\"read AddCustomer.vue:12\"`, ran both gates on the HEAD binary. `tp review --merge ca.ndjson` -\u003e `warning: skipping incomplete line (missing location) in ca.ndjson`, `merged: 0 unique findings from 1 files`, then exit 1 `no line parsed in ca.ndjson`. `tp review spec.md --record ca.ndjson` in a fresh dir -\u003e exit 0, `\"findings\": 1` — accepted today only because `parseRecordRows` requires none of the four.","finding":"Row 8 requires the `--perspective code-audit` output format to name `evidence`, but that format names no `location` either. Its eleven keys are role, id, file, line, pattern, current_behavior, spec_coverage, finding, suggestion, severity, category (`codeAuditOutputFormat`, internal/cli/review.go:1764-1766). §2 makes the four keys `severity`, `finding`, `location`, `evidence` one required set enforced at both gates, so after this release a reviewer who copies the code-audit prompt's own JSON line verbatim — even with `evidence` added — produces a row `--merge` skips and `--record` refuses, for want of `location`. Row 8's premise sentence is that these are paths 'whose rows reach the same `--record`'; measured, they already do not survive `--merge` today, and the release makes them fail `--record` too. Adding one key to that format is not enough to satisfy the premise the row rests on. §3 fixes REFERENCE.md because it would otherwise ship a reference contradicting the release; this is the same defect inside the binary, and no row covers it.","found_by":1,"found_by_roles":["implementer"],"location":"§5","resolved":{"evidence":"→ task acceptance: prompt-output-formats-carry-the-required-set. Re-resolved to replace \"Accepted open\", which recorded an open finding as closed: tp has no status for \"accepted, not yet taken\", and a `wontfix` row is injected into the next round's prompts under DO NOT re-report and counts as resolved for convergence. `wontfix` stays, and the evidence now says the answer is code. Confirmed still open: row 8's SHALL at spec/1.1.0.md:151 requires only that each listed path's output-format object line names `evidence`, and `--perspective code-audit` was not dropped with a Non-Goal. The measurement stands — `codeAuditOutputFormat`'s eleven keys carry no `location`, so a reviewer copying that line verbatim with `evidence` added still produces a row `--merge` skips and `--record` refuses. The implementing task's acceptance is that every prompt output format row 8 lists carries the four required keys, not `evidence` alone.","resolved_at":"2026-09-08T06:23:21Z","status":"wontfix"},"role":"implementer","severity":"high","suggestion":"Either extend row 8's SHALL to the whole required set for every path it names — the format must carry all four keys, not just `evidence` — or drop `--perspective code-audit` from the enumeration and state in §4 that its rows are not review-record rows. Do not leave the row asserting a premise the measured behaviour refutes."}
{"category":"completeness","class":"unverified-exhaustiveness-quantifier","evidence":"Ran /tmp/tester-r2-Xfrfnu/tp-head (built from HEAD in the clone) as `tp review spec.md --verify --findings \u003crecorded round\u003e` in /tmp/tester-r2-Xfrfnu/qa2 after recording a 63-row round; exit 0, prompts[0].role == 'verifier', and the prompt's last three lines are the NDJSON + role-stamp contract quoted above with no key list. Also ran the other perspectives to confirm they are not additional finding-producing paths in that dir: --perspective regression exits 2 ('regression review has nothing to check'), documentation and testing exit 2 without --docs-path/--test-path.","finding":"Row 8's fixture cell asserts an exhaustive enumeration — 'every review emission path whose rows reach the same --record' — and lists three. There is a fourth: `tp review \u003cspec\u003e --verify` emits a prompt with role 'verifier' whose closing contract is 'Respond with one JSON finding per line (NDJSON)... Stamp EVERY finding with role: \"verifier\" (so the finding is attributed to this verification pass in the per-role overlap report)' — the overlap report is --merge's, so verifier rows go to --merge and --record like any other. That prompt names NO key list at all: not severity, not location, not finding, not evidence. A verifier following it produces rows --record will refuse, and no row of §5 goes red, which is exactly the defect round 1's resolved finding on `--perspective code-audit` closed for three paths and left open for the fourth.","found_by":1,"found_by_roles":["tester"],"location":"§5","resolved":{"evidence":"Fixed by a9f720b3, confirmed in the current text. Row 8's fixture cell no longer enumerates three paths: it cites the derived list under The review emission sites, derived in spec/1.1.0-measurements.md, which runs a git grep over review.go, review_regression.go and review_verify.go, resolves four call sites, and tabulates the five commands that reach them -- tp review \u003cspec\u003e, --role \u003cr\u003e, --perspective code-audit, --perspective regression and --verify. The --verify path is in that list, and row 8's mutant column now names it as HEAD's failing path, \"whose prompt carries no JSON object line at all\", so the SHALL is not vacuous there: satisfying the row means building an object line for it. The two perspectives that emit plan items rather than findings (documentation, testing) are excluded by the derivation with a stated reason.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"tester","severity":"high","suggestion":"Either add the --verify path to row 8's fixture enumeration, or drop the word 'every' and list the paths the row covers plus a Non-Goal naming --verify and saying what happens to its rows at --record. Prefer deriving the path list in the test from the command surface rather than restating it, so the enumeration cannot rot again."}
{"category":"consistency","class":"unenumerated-emission-paths","evidence":"Built `HEAD` (8e41f111) in a `git clone --no-hardlinks` at /tmp/arch-r2-eaj2hq/clone. (1) `tp review s.md --verify --findings f.ndjson --no-state` — the whole emitted prompt ends `Respond with one JSON finding per line (NDJSON)…` / `Stamp EVERY finding with role: \"verifier\"` / `For findings … set category to \"regression\"`; a python check over it reports `contains {\"severity\": -\u003e False` and `occurrences of word evidence -\u003e 1` (that one occurrence is `(evidence: EV-RESOLVED-1)`, i.e. `resolved.evidence`, so a substring check on the prompt is green with nothing built). (2) `tp review s.md --record verifier.ndjson` with a single `{\"role\":\"verifier\",…}` row exits 0, reports `\"findings\": 1`, and writes `review-round-1.ndjson` — verifier rows are recordable. (3) Read internal/cli/review_verify.go:203-271 (`buildVerifyPrompt` — no `findingFormat`, no `outputContractInstruction`) and internal/cli/review_regression.go:195-196 (`buildRegressionPrompt` DOES append `findingFormat` + `outputContractInstruction`), so the emission-path set is five, not three.","finding":"Row 8's fixture cell asserts an exhaustive set — \"every review emission path whose rows reach the same `--record`\" — and then enumerates three: `tp review \u003cspec\u003e`, `--role \u003cr\u003e` and `--perspective code-audit`. There are five. `--perspective regression` emits a fourth prompt (it reaches the shared `findingFormat`, so it is carried along), and `tp review \u003cspec\u003e --verify` emits a fifth whose prompt names NO key list at all — no JSON object line, nothing a reviewer copies. Verifier rows reach the same `--record` and count as findings. So after §2 lands, a `--verify` pass produces rows `--record` refuses, from a prompt that never told the verifier the key exists, and row 8 stays green because the verifier path is outside its enumeration. The row's own mutant column reinforces the gap: it names `HEAD`'s `code-auditor` as the one path lacking the key, so satisfying the row means fixing three paths and leaving the one that has no key list at all.","found_by":1,"found_by_roles":["architect"],"location":"§5","resolved":{"evidence":"Fixed by a9f720b3, confirmed in the current text. The \"every review emission path\" quantifier is now bound to a derived artifact rather than to a hand-written list of three: row 8's fixture cites The review emission sites, derived in spec/1.1.0-measurements.md, which derives four call sites from the tree and tabulates the five commands reaching them -- the three the row used to name plus --perspective regression (review_regression.go:195) and --verify (review_verify.go:267), exactly the two this finding said were missing. Row 8's mutant is now --verify at HEAD, so the row is red until a prompt with no output-format object line at all gains one; the SHALL is therefore not vacuous on that path, which is what made a separate --verify row necessary in the suggestion.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"architect","severity":"high","suggestion":"Either enumerate all five paths in the fixture cell (adding `--perspective regression` and `--verify`), or drop the \"every … path\" quantifier and name the three the row actually tests, and add a §5 row for `--verify`: its prompt today has no output-format line, so the SHALL as written is vacuous there and the release would ship a review mode whose output `--record` refuses by construction."}
{"category":"regression","class":"emission-path-not-enumerated","evidence":"git clone --no-hardlinks of the repo into /tmp/rev-regression-bD7Ao6/clone; ran `tp init s.md`, `tp review s.md`, `tp review s.md --record f.ndjson`, then `tp review s.md --verify --findings f.ndjson` and parsed the JSON: one prompt, role `verifier`, output_path empty, `'evidence' in prompt` False, findingFormat JSON line absent; prompt tail printed verbatim ends `Respond with one JSON finding per line (NDJSON)... Stamp EVERY finding with role: \"verifier\"`. Read internal/cli/review_verify.go:264-271, internal/cli/review.go:505-512 (reviewPerspectives) and 826-899 (doc/test plans), skills/tp/SKILL.md:608 and :849 (Tail protocol), spec/0.31.0.md:79 (§7.2). Compared spec/1.1.0.md:151 (row 8) with spec/.tp-review/1.1.0/snapshot-round-1.md:133 and with index 1's resolved.evidence in spec/.tp-review/1.1.0/review-round-1.ndjson.","finding":"Row 8's fixture claims to cover `every review emission path whose rows reach the same --record` and then names three: `tp review \u003cspec\u003e`, `tp review \u003cspec\u003e --role \u003cr\u003e` and `tp review \u003cspec\u003e --perspective code-audit`. `tp review \u003cspec\u003e --verify` is a fourth and is missing. It emits a `verifier` prompt whose closing instruction is `Respond with one JSON finding per line (NDJSON)` with no key list at all — the word `evidence` occurs zero times in the entire prompt — and its rows are review findings that reach the same `--merge`/`--record`: SKILL.md's Tail protocol writes a verifier-rejected finding into the findings file before `--record`, and spec/0.31.0.md §7.2 stamps verify findings `role: \"verifier\"` for the same per-role overlap report. Under §1 decision 1 a verifier following that prompt exactly produces rows `--record` refuses, and row 8's check — which `parses each path's JSON object line` — has no JSON object line to parse there. Round-1 index 1 is dispositioned `fixed` on exactly this enumeration, and its evidence claims completeness from reading `reviewPerspectives` (review.go:512, four perspectives) and review.go:848-899; `--verify` is a mode, not a perspective, so that check could not have reached it. The `fixed` is therefore only partly earned: the gap index 1 named is closed for `--perspective code-audit` and open for `--verify`.","found_by":0,"location":"§5","resolved":{"evidence":"Fixed by a9f720b3, confirmed in the current text. The --verify command is row 5 of the derived emission-site table in spec/1.1.0-measurements.md, whose keys cell reads \"none -- the prompt carries no JSON object line at all\" and whose names-evidence cell reads no. Row 8's fixture now cites that list instead of restating three paths, and its mutant column names --verify as HEAD's failing path. The sidecar also records the measurement behind this finding: evidence occurs 0 times in the --verify prompt and 0 times in the code-audit prompt, once in each panel prompt. The partly-earned round-1 fixed this finding reported is closed for the path it left open.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"regression","severity":"high","suggestion":"Add `tp review \u003cspec\u003e --verify` to row 8's fixture and say what its output format must become — it has no JSON object line today, so the SHALL needs a form that covers a prompt naming no keys — or add a Non-Goal naming `--verify` and stating what happens to a verifier's rows at `--record`. Either way, replace the `every ... path` quantifier with the list it actually holds, or derive the list from `reviewPerspectives` plus the modes that emit findings rather than writing it out."}
{"category":"completeness","class":"unbounded-injection-payload","evidence":"Read internal/cli/review.go:1300-1410 (mcp__codedbpro__read) — the previous-round block: finding truncated at 80 chars (1361), resolved.Evidence at 40 (1366), resolved finding at 60 (1399), detailed capped at 50 (1347), highResolved at 10 (1396), heading 'UNRESOLVED findings from previous rounds — DO NOT re-report' at 1345. Ran python over spec/.tp-review/1.1.0/review-round-1.ndjson: 33 rows, 33 with non-empty evidence, evidence mean 427 / median 424 / p90 554 / max 664 chars; dispositions {fixed 10, wontfix 15, duplicate 8}, so 15 rows reach 'detailed' and all 15 are wontfix (mean evidence 411). Re-rendered the block by the exact format strings at review.go:1369/1371: today 2,598 chars, with untruncated evidence 8,953 (+245%), with evidence truncated at 40 chars 3,438 (+32%); over all 33 rows 3,764 -\u003e 18,289 (+386%). Panel size 4 from spec/1.1.0-measurements.md line 616; 23-round budget from spec/1.1.0.md line 11. The block is emitted into every role prompt (internal/cli/review_test.go:301-304 loops all prompts asserting it).","finding":"Decision 5 opens the loop's most-repeated agent-facing payload and decides neither how much of the field it renders nor which rows carry it. The block it writes into is already budgeted end to end: its three free-text renders are truncated at 80, 60 and 40 characters and its two lists are capped at 50 and 10 rows (internal/cli/review.go:1341-1409). Decision 5 adds beside them a field whose recorded length is mean 427 / median 424 / p90 554 characters, with no bound stated anywhere in the spec, onto rows the same block heads with 'DO NOT re-report'. Rendered against this cycle's own recorded round 1, the block goes 2,598 to 8,953 characters (+245%); over all 33 recorded rows it goes 3,764 to 18,289 (+386%). That is paid once per role — four roles in this repository's panel — for each of the 23 rounds §1's Class paragraph budgets. Sharper: all 15 rows that reach that block from round 1 are dispositioned 'wontfix', so under decision 5 each renders two evidence texts — the shipped '(wontfix: ...)' truncated at 40 characters, and the new unbounded one — for a reader instructed not to act on the row. §5 row 10 cannot make either choice observable: one row, a sentinel of unstated length, green under a full render and under a 40-character truncation alike, which §5's own preamble rules out ('the fixture cell names the value the mutant turns on, and any count a SHALL asserts is stated against that value').","found_by":1,"found_by_roles":["ax-economist"],"location":"§1","resolved":{"evidence":"Fixed by 86374665: decision 5 was cut, so the release opens no injection channel and the unbounded payload has no source. Confirmed in the current text -- no sentence in spec/1.1.0.md names the previous-round block, a render length, a row cap or the wontfix line. The figures this finding produced are not discarded: the evidence-length distribution and the +245% / +386% block growth are recorded in spec/1.1.0-measurements.md, and the read-back question the decision was carrying is now The evidence contract under Undecided in spec/undecided.md (d0c99d9f), where a bound and a scope are the open design rather than an unstated consequence of a shipped decision.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"ax-economist","severity":"medium","suggestion":"One sentence in decision 5 deciding both halves at requirement altitude: the rendered length (the block's existing 40/60/80 ladder is the precedent, and a bound of 40 costs +32% on this cycle's block instead of +245%), and which rows carry it (a row already dispositioned 'wontfix' carries its disposition evidence and is one the reader is told not to re-report). Then state a length in §5 row 10's fixture cell so a mutant that truncates is distinguishable from one that does not. This is not round 1's 'unpriced-output-growth' row, which asked for a price on §2's merged NDJSON and was accepted open; this asks for a bound and a scope, on a channel that did not exist in the reviewed text then."}
{"category":"ambiguity","class":"two-evidences-one-line","evidence":"Read internal/cli/review.go:1355-1372 via codedbpro (the two branches of the detailed listing). Confirmed live in this round's own emitted prompt from `tp review spec/1.1.0.md --role implementer`: 15 lines of the form `[WONTFIX] \u003ccategory\u003e — \u003c§\u003e: \u003ctext\u003e (wontfix: \u003cevidence\u003e)`.","finding":"Decision 5 puts the top-level `evidence` 'beside its finding text' in the injected summary, but a `wontfix` row's line in that summary already ends in a parenthetical evidence: `fmt.Fprintf(\u0026b, \"  [WONTFIX] %s — %s: %s (wontfix: %s)\\n\", f.Category, f.Location, text, evidence)` where that `evidence` is `resolved.Evidence` (internal/cli/review.go:1364-1369). The two are different things — `resolved.evidence` says why the finding was rejected, `evidence` says what the reviewer ran to reach it — and after this release a wontfix row must carry both on one line. The spec gives no format, no ordering and no labelling, so the implementer invents it, and whatever it invents an agent reading the prompt sees two unlabelled parentheticals. This round's own prompt is the live instance: fifteen `[WONTFIX]` lines, each already ending in `(wontfix: ...)`.","found_by":1,"found_by_roles":["implementer"],"location":"§1","resolved":{"evidence":"Fixed by 86374665: decision 5 was cut, so no line of the injected summary must carry both a top-level evidence and a resolved.evidence, and the release invents no format for them. Confirmed in the current text -- spec/1.1.0.md names neither the wontfix branch nor the previous-round summary anywhere. The parenthetical this finding measured at review.go:1364-1369 is untouched by the release.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"implementer","severity":"medium","suggestion":"Say what a `wontfix` row's line looks like under decision 5 — which evidence appears, in what order, under what label — or state that the injected `evidence` is added only to the non-wontfix branch and say why. Add the wontfix row to row 10's fixture so the choice is pinned."}
{"category":"feasibility","class":"quantifier-over-capped-output","evidence":"Ran the HEAD binary on a temp spec with a 60-row findings file, every row carrying `evidence`: `tp review --no-state --round 2 --findings f.ndjson spec.md`. Counted `^  \\[HIGH\\]` lines in prompts[0].prompt -\u003e 50; the prompt carries `... and 10 more (omitted for brevity)`; 'finding number 55' absent. Read internal/cli/review.go:1341-1377 via codedbpro for `findingsCap := 50`, `len(text) \u003e 80`, `len(evidence) \u003e 40`.","finding":"Decision 5 says the previous-round findings tp injects 'carry each finding's `evidence`', and §5 row 10's SHALL repeats 'each previous-round finding reaches the prompt with its `evidence` text beside its finding text'. `buildFindingsSummary` (internal/cli/review.go:1341-1377) shows at most 50 detailed findings and reports the remainder only as `... and N more (omitted for brevity)`, and truncates each finding's text at 80 characters. Past 50 findings a row reaches the prompt with no text at all, so 'each' is unbuildable as written unless the cap is lifted — and the spec says nothing about the cap, which was itself added to shrink this exact block. The implementer must therefore invent both a decision about the cap and a truncation width for `evidence` (the adjacent wontfix branch already truncates at 40), and neither choice is falsifiable by row 10: its fixture cell names 'a sentinel string occurring nowhere else' and pins no length, so it is green whether the whole evidence reaches the prompt or only its first twelve characters. §5's own preamble — added this round — obliges the fixture cell to name the value the mutant turns on; row 10 does not obey it.","found_by":1,"found_by_roles":["implementer"],"location":"§1","resolved":{"evidence":"Fixed by 86374665: decision 5 and the row that repeated its \"each previous-round finding\" quantifier were both cut, so no sentence in the spec asserts anything about buildFindingsSummary's 50-row cap or its 80-character truncation, and the implementer is not left inventing a cap decision and a truncation width. Confirmed in the current text: the five decisions name no prompt-rendering behaviour, and the eleven rows of section 5 name no sentinel.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"implementer","severity":"medium","suggestion":"State in decision 5 whether the 50-row cap and the 80-character truncation apply to the injected `evidence`, and give it an explicit width or state that it is injected whole. Then pin that value in row 10's fixture: a sentinel whose length exceeds the chosen truncation, so a truncating implementation is red."}
{"category":"completeness","class":"task-boundary-not-derivable","evidence":"In the clone of HEAD (8e41f111): applied the measurements file's counting rule mechanically to every `internal/cli/*_test.go` (a single-line JSON object literal naming both `\"severity\"` and `\"finding\"` and neither `\"item_id\"` nor `\"status\"`), inserting `\"evidence\":\"ran the probe\"` — 149 rows across 29 files, `gofmt -w` on the one file it unaligned. No production change. `go test ./... -count=1` -\u003e 0 lines matching `^--- FAIL: `; `golangci-lint run` -\u003e `0 issues.`; `./scripts/check-deadcode.sh` -\u003e exit 0; `./scripts/check-complexity.sh` -\u003e exit 0. Then applied the parity change on top (`evidence` appended to `missingFindingFields`; a `strings.TrimSpace` check on the top-level `evidence` in `parseRecordRows`): `go test ./... -count=1` -\u003e 6 failing test functions — TestReviewRecord_RowRules, TestReviewRecord_Lifecycle, TestReviewRecord_CleanAndDirty, TestOutputFlagRequiresMerge, TestSpecHashFailureNamesTheRealCause, TestAuditSignal_AbsentFromPromptMergeAndReviewOutputs. So the census closes 62 of the 68 the measurements file reports and not the remaining 6, none of which is a census row by construction.","finding":"Decision 6 sizes the release by 'the hand-written finding fixtures throughout the existing suite — the measurements file counts them' and orders the work 'migration first, suite green, then the parity change'. Built and run: a migration scoped by exactly the measurements file's own counting rule leaves the parity task with a red suite it must also repair, and the spec gives no rule for which repairs belong to which task. So the migration task's boundary is not derivable from the spec — an implementer scoping it by the census the decision points at will close it green under the full project gate and hand the next unit six failures it cannot distinguish from regressions it introduced itself, at exactly the sink where decision 6 forbids `--skip-gate`.","found_by":1,"found_by_roles":["implementer"],"location":"§1","resolved":{"evidence":"→ task acceptance: fixture-migration-acceptance-is-outcome-shaped. Re-resolved to replace \"Accepted open\", which recorded an open finding as closed; the answer here is a task boundary rather than a sentence. Decision 5 at spec/1.1.0.md:44-48 is unchanged and still sizes the release by the census. The measured prediction is not disputed: a 149-row / 29-file migration closes green under the full project gate on its own, and the parity change applied on top leaves six failing test functions, none of them a census row. So the migration task's acceptance is outcome-shaped rather than census-shaped — the migration is complete when the suite is green with the parity change applied on top, verified locally and reverted — and the residual behaviour-assertion repairs belong to the parity task.","resolved_at":"2026-09-08T06:23:21Z","status":"wontfix"},"role":"implementer","severity":"medium","suggestion":"State the migration task's acceptance as an outcome rather than a census: 'the fixture migration is complete when the suite is green with the parity change applied on top' (verified locally, reverted), or explicitly assign the residual behaviour-assertion repairs to the parity task and say roughly what they are. A count in the measurements file is a size, not a boundary."}
{"category":"consistency","class":"unfalsifiable-sentence","evidence":"Read spec/1.1.0.md:49-54 (decision 6) against spec/.tp-review/1.1.0/snapshot-round-1.md:29-44, which ends at decision 4 — the sentence is new. Read index 9's resolved.status (`wontfix`) and resolved.evidence in spec/.tp-review/1.1.0/review-round-1.ndjson. Read skills/tp/SKILL.md:108-113. `git log --oneline -14` shows 622e1f35 `docs(spec): 1.1.0 §1 decides the evidence is read back, and what the release really costs` as the commit that added decision 6.","finding":"Decision 6's closing sentence — `A unit meeting a red gate does not reach for --skip-gate; that is the operator's decision, not the unit's` — states behaviour this release does not change, and no row of §5 can make it red. skills/tp/SKILL.md:110 carries the rule it breaks: `A sentence describing behaviour the release does not change is a test name, or it is not in the spec`. The same repair pass used that rule to refuse round-1 index 9 as `wontfix`, whose disposition reads `stating it in decision 2 or row 5 would add a claim no mutant of this release can make red`. So the pass applied the rule to a reviewer's request and broke it two commits later in a sentence of its own.","found_by":0,"location":"§1","resolved":{"evidence":"Fixed by d02d7b00, which deleted decision 6's closing sentence \"A unit meeting a red gate does not reach for --skip-gate; that is the operator's decision, not the unit's\". Confirmed in the current text: a regex for skip-gate over spec/1.1.0.md returns no match. The reason is recorded in spec/1.1.0-measurements.md under the heading naming --skip-gate a fence in the binary rather than a rule in a spec, with the run that establishes it: TP_UNATTENDED=1 tp done ... --skip-gate exits 2 with a hint naming tp escalate. The rule this finding cited from skills/tp/SKILL.md:110 is now applied to the repair's own sentence, which is what the finding asked for.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"regression","severity":"medium","suggestion":"Delete the sentence. Decision 6's requirement — the fixture migration is its own task and the gate change depends on it — stands without it, and the `--skip-gate` fence already lives in CLAUDE.md and in skills/tp/SKILL.md, where an operator reads it."}
{"category":"consistency","class":"misattributed-falsification","evidence":"Read skills/tp/REFERENCE.md:682 and 720-722 via codedbpro. Line 722 lists `role`, `location`, `class`, `severity` and does not list `finding`; internal/cli/review_merge.go:190-197 shows `missingFindingFields` gating on `location, severity, finding` — `finding` required, `role` and `class` not (the function's own comment says \"role is attribution metadata … not a merge gate\"). So the four-key list at line 722 already disagrees with the shipped gate in two directions, independently of §2.","finding":"§3 justifies putting `REFERENCE.md` in scope by saying it \"states the superseded required-field set and the superseded output contract as present fact … and §2 makes both false\". That is right for the first site and wrong for the second, and the difference matters because it is the only instruction the implementing task gets. REFERENCE.md line 722 reads \"Every review finding carries `role`, `location` (a `§`-anchor), `class`, and `severity` (tp's output contract)\" — a set that includes `role` and `class`, which no gate requires, and omits `finding`, which BOTH gates already require at `HEAD`. So that sentence is already inaccurate before this release; §2 does not falsify it, it merely fails to fix it, and §2's required set (`severity`, `finding`, `location`, `evidence`) is a different set serving a different sink (what a gate refuses) than the *output contract* (what a prompt tells a reviewer to stamp). A task told \"§2 makes it false\" will add `evidence` and leave `finding` missing and `role`/`class` unexplained.","found_by":1,"found_by_roles":["architect"],"location":"§3","resolved":{"evidence":"→ task acceptance: reference-output-contract-names-required-and-attribution-keys. Re-resolved to replace \"Accepted open\", which recorded an open finding as closed; the answer here is a documentation edit rather than a spec sentence. §3 at spec/1.1.0.md:99-102 still attributes both REFERENCE.md sites to §2. The measurement is not disputed: REFERENCE.md:722 lists `role`, `location`, `class` and `severity` and omits `finding`, so it disagrees with `missingFindingFields` at HEAD in two directions and §2 does not falsify it. The documentation task's acceptance therefore treats the two sites separately — the recipe sentence is superseded by §2 and gains `evidence`; the output-contract sentence is repaired on its own grounds, naming which keys the gates require and which are attribution.","resolved_at":"2026-09-08T06:23:21Z","status":"wontfix"},"role":"architect","severity":"medium","suggestion":"State the two sites separately: the recipe sentence is superseded by §2 and must gain `evidence`; the output-contract sentence is a different set that is already wrong at `HEAD` and must be repaired to say which keys the gates require and which are attribution. Or, if the second is out of scope, say so — but do not attribute its repair to §2, which does not reach it."}
{"category":"ambiguity","class":"doc-task-underdetermined","evidence":"Read spec/1.1.0.md:111-114 (absent from spec/.tp-review/1.1.0/snapshot-round-1.md:94-99). Read skills/tp/REFERENCE.md:682-685 and :720-722 via search — the only two sites in skills/tp/ that state the merge required set or the finding output contract. Read internal/cli/review_merge.go:187-198: missingFindingFields loops over `location`, `severity`, `finding` and its doc comment says role is not a merge gate. Read index 14's finding and disposition in spec/.tp-review/1.1.0/review-round-1.ndjson.","finding":"§3's new paragraph sends the doc task at two REFERENCE.md statements and asserts `§2 makes both false`. For the first — `--merge requires only location, severity and finding` at REFERENCE.md:685 — that holds. For the second — `Every review finding carries role, location (a §-anchor), class, and severity (tp's output contract)` at REFERENCE.md:722 — §2 makes it incomplete rather than false, and the task cannot write its replacement without deciding whether `role` and `class` are required keys. §2 leaves both optional (missingFindingFields checks location, severity and finding only, and its comment says role is attribution the rest of the pipeline treats as optional), and round-1 index 14 is open on exactly that asymmetry. So the release's only documentation requirement asks a task to restate a contract the spec has not settled, and the task's acceptance depends on an open finding.","found_by":0,"location":"§3","resolved":{"evidence":"→ task acceptance: reference-output-contract-names-required-and-attribution-keys. Re-resolved to replace \"Accepted open\", which recorded an open finding as closed. Deliberately still not a duplicate of index 14: that one is that the attribution is wrong, this one is that the doc task cannot write the replacement sentence at all without deciding whether `role` and `class` are required keys, which §2 does not decide. The decision is taken here as the task's acceptance rather than left to the unit — the four keys of §2 are the required set, and `role` and `class` are attribution and cluster keys no gate requires — so the replacement sentence states both and the task no longer depends on an open finding.","resolved_at":"2026-09-08T06:23:21Z","status":"wontfix"},"role":"regression","severity":"medium","suggestion":"Say in §3 what the replacement sentence must assert — the four required keys, with `role` and `class` named as optional attribution and cluster keys — or scope §3 to the `--merge` required-set sentence alone and fence the output-contract sentence out until the required-set question index 14 raises is decided."}
{"category":"ambiguity","class":"evidence-label-collision","evidence":"Probe at `HEAD` in the clone: `tp review s.md --verify --findings f.ndjson --no-state` prints `  1. [high] completeness — §1: F1 (evidence: EV-RESOLVED-1)` — the label `evidence:` is already taken, by `resolved.evidence`. Read internal/cli/review_verify.go:227 (`(evidence: %s)`), review_regression.go:187 (`fmt.Fprintf(\u0026b, \"- %s — %s\\n\", f.Finding, evidence)` where `evidence = f.Resolved.Evidence`), and review.go:1369 (`(wontfix: %s)`). The 57-row probe above shows the panel injector's wontfix line already ending `(wontfix: RESOLVER-JUSTIFICATION-…)` while both top-level `evidence` sentinels occur zero times in the emitted prompt.","finding":"Row 10 pins \"the role prompts for the round after it\" — one injector. tp has three that inject previous-round findings into an emitted prompt, and two of them already print `resolved.evidence` under the name *evidence*: `--verify` renders a fixed finding as `… F1 (evidence: EV-RESOLVED-1)`, and the regression prompt renders each previously fixed finding as `- \u003cfinding\u003e — \u003cresolved.evidence\u003e`. §2 keeps `--record`'s pre-resolved rules unchanged, so a wontfix row now carries two different texts — the reviewer's top-level `evidence` and the resolver's `resolved.evidence` — and decision 5 puts the first \"beside its finding text\" on the very line that already shows the second. Neither §1 nor §2 nor §5 says how a reader tells them apart, whether the other two injectors are in scope, or what the panel injector's wontfix line becomes.","found_by":1,"found_by_roles":["architect"],"location":"§5","resolved":{"evidence":"Fixed by 86374665: decision 5 was cut, so nothing in the release puts a top-level evidence on a line that already renders a resolved.evidence, and the question of which injectors are in scope has no decision to be in scope of. Confirmed in the current text -- spec/1.1.0.md names no injector. The three injectors this finding measured -- the panel block's wontfix parenthetical at review.go:1369, the verify listing's evidence parenthetical at review_verify.go:227 and the regression prompt's resolved-evidence render at review_regression.go:187 -- are all untouched by the release, which is now its stated position rather than an unnamed consequence.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"architect","severity":"medium","suggestion":"Say which injectors decision 5 covers (name them, or say the panel injector only and fence the other two in §4), and give the two texts distinct labels in the rendered line — e.g. `(found by: …)` for the finding's own evidence against the existing `(wontfix: …)` / `(evidence: …)` for the resolver's. Then extend row 10's fixture to a wontfix row carrying both, with distinct sentinels, so the mutant that renders only one is red."}
{"category":"ambiguity","class":"universal-shall-untestable-fixture","evidence":"Recorded a 63-row round in /tmp/tester-r2-Xfrfnu/qa2 with tp-head (1 unresolved high, 1 wontfix, 1 duplicate-critical, 60 unresolved low) and emitted round 2: the tester prompt showed 48 distinct BULK rows + 2 named rows = 50, then '... and 12 more (omitted for brevity)'; grep of the duplicate row returned exactly '[RESOLVED] §4: DUPROWMARKER'. The cap and the truncations are review.go:1347 (findingsCap := 50), 1361-1363 (finding text \u003e 80) and 1366-1368 (wontfix evidence \u003e 40). The --verify prompt on the same state listed 'Unresolved findings (still open): 1..61' with no cap.","finding":"Row 10's SHALL is a universal — 'EACH previous-round finding reaches the prompt with its evidence text beside its finding text' — over a channel that is not universal, and its one-row fixture cannot see the difference. Measured at HEAD: a recorded round of 63 findings puts 50 into the panel prompt and reports the rest as '... and 12 more (omitted for brevity)'; rows resolved fixed or duplicate never enter the detailed listing at all (a critical duplicate appeared only as '[RESOLVED] §4: DUPROWMARKER', with no evidence and no channel for one); and the --verify prompt lists all 61 unresolved rows uncapped in a third format. Two engineers write contradictory tests from this cell: one asserts the 50-cap lifts, one asserts only the shown rows carry evidence, and the fixture's single row is green under both.","found_by":1,"found_by_roles":["tester"],"location":"§5","resolved":{"evidence":"Fixed by 86374665: the universal SHALL this finding is about -- the former row 10, over a channel that is not universal -- was cut with decision 5. Confirmed in the current text: row 10 is now the --merge -o row, and no row in section 5 asserts anything about the previous-round prompt, the 50-row cap, the resolved high/critical block or the --verify listing. The two contradictory tests this finding said would be written have no cell left to be written from.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"tester","severity":"medium","suggestion":"Scope the SHALL to the rows the prompt already lists — 'every finding the previous-round block lists carries its evidence' — and say in §4 whether the 50-row cap, the resolved high/critical block and the --verify listing are in scope. Then size the fixture against the scope: a 51-row fixture is what discriminates a lifted cap from a kept one, and a one-row fixture tests neither."}
{"category":"regression","class":"method-only-in-ungraded-sidecar","evidence":"Read spec/1.1.0.md:148 (row 5) and :49-54 (decision 6) against spec/.tp-review/1.1.0/snapshot-round-1.md:130 and :29-44 — neither round-1 text references the measurements file. Read spec/1.1.0.md:5-6 for the standing-without clause. Read index 23's finding text and resolved.evidence in spec/.tp-review/1.1.0/review-round-1.ndjson.","finding":"Row 5's fixture cell now justifies its own choice of key with `the four-cell run is in the measurements file`, and decision 6 sizes the release with `the measurements file counts them`. Neither sentence was in the round-1 text. Round-1 index 23 is dispositioned `fixed` precisely because a row's construction rule was reachable only in spec/1.1.0-measurements.md — a file line 5 of this spec says grading does not read and line 6 says the spec stands without — and the repair removed that dependency from row 8 while adding two more elsewhere. From the body alone a reader of row 5 cannot tell why `evidence` rather than `location` is the key the fixture must omit, which is the whole content of that cell.","found_by":0,"location":"§5","resolved":{"evidence":"Fixed by 9869ff84 for the half that carries the finding's harm. Row 5's fixture cell at spec/1.1.0.md:141 now states its own reason inline -- \"with location omitted instead, --record accepts the row and the mutant becomes indistinguishable from the shipped rule at both gates\" -- and the pointer to the measurements file is gone. A reader of row 5 can now tell from the body alone why evidence rather than location is the key the fixture omits. Confirmed in the current text. Residual, stated rather than hidden: decision 5's \"the measurements file counts them\" survives at spec/1.1.0.md:46. The repair kept it deliberately -- spec/1.1.0-measurements.md records that the census is a size and not a task boundary, that the decision stays and that the numbers stay out of the spec -- and this finding's own suggestion concedes the sizing directive is actionable without the count.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"regression","severity":"medium","suggestion":"Put the reason in row 5's own cell in one clause — with `location` omitted instead, the mutant is indistinguishable from the shipped rule at both gates — and drop the pointer. Give decision 6 its size in its own words or drop the count; the sizing directive is actionable without it."}
{"category":"consistency","class":"mutant-rationale-refuted-by-measurement","evidence":"Ran /tmp/tester-r2-Xfrfnu/tp-head `review spec.md --perspective code-audit --affected-files code.go` in /tmp/tester-r2-Xfrfnu/qa: prompts[0].role == 'code-auditor', prompt.count('evidence') == 0, and its object line has exactly the 11 keys the mutant cell names (role,id,file,line,pattern,current_behavior,spec_coverage,finding,suggestion,severity,category) — that figure is correct. Ran `review spec.md --no-state` in the same dir: implementer/tester/architect each report prompt.count('evidence') == 1, and the occurrence is 'report it with its evidence instead of making the edit'.","finding":"Row 8's fixture cell forbids the cheap check — 'a substring search of the whole prompt is not the check' — and the mutant cell justifies the ban by asserting that under the mutant 'evidence already occurs in the surrounding prose of the panel prompts, so a substring check is green with nothing built'. Measured, that is false for the path the mutant names: HEAD's code-audit prompt contains the string 'evidence' ZERO times, so a per-path substring check over the whole prompt is RED against the named mutant, not green. The claim holds only for the three panel prompts (1 occurrence each, from the shared unit-framing sentence 'report it with its evidence instead of making the edit'). A reader who checks the mutant's reasoning finds it contradicted by the tree, and the prohibition it supports is left resting on nothing.","found_by":1,"found_by_roles":["tester"],"location":"§5","resolved":{"evidence":"Re-resolved from `fixed` to `wontfix`: the requirement is closed, but the measurement the `fixed` rested on is not one. That disposition cited the sidecar's \"`evidence` occurs 0 times in the `--verify` prompt and 0 times in the code-audit prompt, and once in each panel prompt\". Re-measured by emitting every prompt, the figure does not reproduce, because it named no input: against `spec/1.1.0.md` with `--affected-files internal/cli/review.go` the code-audit prompt carries `evidence` twice, the `--verify` prompt once and each panel prompt three times, and the 0/0/1 reading holds only against a neutral spec whose own text does not contain the word. The count is a property of the document under review and of what `--affected-files` names, not of tp, which is why no bare figure can carry the row's reason. What IS closed is the requirement: the refuted clause is gone from row 8's mutant cell (a9f720b3), and 19ebfe72 restored the justification in the form the measurement supports, inline in the graded fixture cell — a whole-prompt substring search is not the check, because the word already occurs in a panel prompt's instruction text and in whatever an `--affected-files` excerpt happens to contain. spec/1.1.0-measurements.md now carries the two-column table and the correction, including that the old conclusion was wrong in both directions.","resolved_at":"2026-09-08T06:23:21Z","status":"wontfix"},"role":"tester","severity":"medium","suggestion":"Either restate the justification against what was actually measured — the panel prompts already contain the word, so a substring check over THEM is green with nothing built, which is why the check parses the object line — or drop the justification and keep the requirement, since 'the check parses each path's JSON object line' stands on its own without an argument the tree refutes."}
{"category":"completeness","class":"rule-in-an-ungraded-arm","evidence":"Ran /tmp/tester-r2-Xfrfnu/tp-head `ground spec/1.1.0.md --units` in the clone at /tmp/tester-r2-Xfrfnu/repo: 54 floor units printed, and §5's are u68..u77, whose texts are the ten table rows verbatim ('1 — a findings file handed to...' through '10 — a round is recorded, and...'). No preamble sentence appears among them; the units between u62 (§4's last) and u68 are absent from the listing, i.e. cut. The committed floor from ground round 3 agrees: spec/.tp-review/1.1.0/floor-ground-round-3.txt shows u52/u53/u54 §5 (cut) ahead of the eight §5 floor units of that revision.","finding":"The whole of §5's preamble — the standing rule 'A row whose mutant cannot make it red is not in this table' and the sentence the round-1 repair added to it — is cut from the spec's own floor, so no ground round can grade it and no round can record that a row violates it. §5's floor units are exactly its ten table rows; the preamble sentences are all cut. That matters here rather than in general: the repair answered a round-1 finding by writing a RULE instead of fixing the rows, and round 2 has just found the same class in the row that same repair added (see this round's row-10 finding). A rule in a cut arm is a claim the loop cannot enforce or falsify — which is §1 decision 4's own standard applied to §5.","found_by":1,"found_by_roles":["tester"],"location":"§5","resolved":{"evidence":"Kept `wontfix`, and re-resolved only to replace \"Accepted open\", which recorded an open finding as closed. This one is about the text, and the text stays as it is. §5's preamble is the table's stated standard and it is deliberately in the cut arm: `tp lint` floors §5 at its table rows. The rule was enforced at the rows rather than through the preamble — rows 3 and 7 in round 2 (eb2ee542, a9f720b3), rows 1, 2, 8 and 10 in round 3 (79fde9ee, 19ebfe72, ceb13c92) — which is this finding's own second branch applied case by case, and it is what four of round 3's high findings closed on. Whether a normative sentence may live in a cut arm at all is a question about `tp lint`'s floor, not about this release.","resolved_at":"2026-09-08T06:23:21Z","status":"wontfix"},"role":"tester","severity":"medium","suggestion":"Move the obligation out of the preamble and into the thing that is graded: state it as a property of each row (the fixture cell of every row names the value its mutant turns on) and let the row cells carry it, or accept that it is guidance and stop relying on it as the table's gate — in which case the repair for a fixture defect is the fixture, not another preamble sentence."}
{"category":"redundancy","class":"forensics-in-the-graded-document","evidence":"Measured on spec/1.1.0.md and spec/.tp-review/1.1.0/snapshot-round-1.md with python: snapshot 134 lines / 13,184 chars, current 153 lines / 15,735 chars (+19.4%); §5 from the '## 5. Tests' split is 20 lines / 6,236 chars = 40% of the file. Per-row cell lengths from splitting each table row on ' | ': row 1 WHEN 101 SHALL 92 fixture 123 mutant 124; row 5 WHEN 22 SHALL 310 fixture 487 mutant 150; row 8 fixture 270 mutant 245. Counting rule stated before counting, and crude by design: within a SHALL/fixture/mutant cell, text from the FIRST em dash onward is counted as justification for the cell rather than a value a test instantiates — 1,642 of 4,870 chars (34%). Quoted clauses read directly from spec/1.1.0.md:144-152. Floor movement 47 -\u003e 54 units and cut 21 -\u003e 23 from spec/1.1.0-measurements.md:569-574 (the repair's own table). The four-reads-per-round claim is from the emitted prompt itself: 'The spec is NOT included inline to save context. Read the spec file before reviewing', with a four-role panel (measurements line 616) and the 23-round budget at spec/1.1.0.md:11.","finding":"§5's cells have absorbed the argument for each fixture as well as the fixture, and the argument is re-read by every role in every round while feeding no decision the reader takes. §5 is now 6,236 characters — 40% of the whole spec — and its SHALL/fixture/mutant cells are 4,870 of that; 1,642 characters (34%) sit after the first em dash of a cell, which is where the justifying clause lives in every row that has one: row 2's 'at HEAD this records at exit 0 ... which is the defect this row closes', row 5's 'which is --merge's existing dropped-input rule rather than a new one, and the legal second file does not lift it' plus 'the four-cell run is in the measurements file', row 7's 'not the live corpus, which stops being uniformly pre-1.1.0', row 8's 'while evidence already occurs in the surrounding prose ... so a substring check is green with nothing built'. Row 5 is the shape at its clearest: a 22-character WHEN carrying a 310-character SHALL and a 487-character fixture. The spec grew 13,184 -\u003e 15,735 characters (+19.4%) in this one repair round, and every byte is paid four times per round for the budgeted 23 rounds because each role prompt tells its reader to open the file rather than inlining it, and again by ground, which floors the table rows (47 -\u003e 54 units in the same repair). A sidecar for exactly this already exists and ground does not grade it — spec/1.1.0-measurements.md, which one §5 cell already cites. This is not a reversal of round 1's resolved 'method-only-in-ungraded-sidecar' row: that asked for the rule that determines a fixture to be in the body, and it should stay. What should leave is the reason the fixture was chosen over another.","found_by":1,"found_by_roles":["ax-economist"],"location":"§5","resolved":{"evidence":"Kept `wontfix`, and re-resolved only to replace \"Accepted open\", which recorded an open finding as closed. This one is about the text, and the text stays as it is — the trade it asks for has now been measured and settled the other way. Round 3 moved MORE justification into the graded document, not less: 19ebfe72 put row 8's five emission commands and the reason for its whole-prompt substring ban inline, and that one edit closed six round-3 findings (indices 7, 9, 11, 24, 26, 27) that existed only because the fixture had been delegated to the ungraded sidecar. `floor_size` went 51 → 52 for it. The cost measurement in this row is not disputed; the conclusion drawn from it is, because the cheaper document is the one the loop kept failing.","resolved_at":"2026-09-08T06:23:21Z","status":"wontfix"},"role":"ax-economist","severity":"medium","suggestion":"Keep in each cell only what a test instantiates — the value the mutant turns on, which is what §5's preamble asks for — and move the clause explaining why that value rather than another into spec/1.1.0-measurements.md beside the run that established it, as row 5 already does for its four-cell run. Rows 2, 5, 7 and 8 are the four that carry the clause; the rewrite is subtraction from the graded document, not new text."}
{"category":"consistency","class":"preamble-contradicted-by-own-rows","evidence":"Read spec/1.1.0.md:136-140 (preamble) and rows 3 and 7 at lines 146 and 150; compared with spec/.tp-review/1.1.0/snapshot-round-1.md:121-122 (two-sentence preamble, no such rule) and its rows 3 and 7 at lines 128 and 132. Read index 26's finding text and disposition in spec/.tp-review/1.1.0/review-round-1.ndjson. `git log --oneline -14` names b3dae3f3 `test(spec): 1.1.0 §5's preamble makes the fixture name the value its mutant turns on`.","finding":"§5's new preamble sentence — `any count a SHALL asserts is stated against that value` — is falsified by two rows in its own table. Row 7's SHALL asserts `the same consecutive_clean the previous binary reports` and its fixture cell (`a pre-1.1.0 round file copied under testdata`) states no value for it. Row 3's SHALL asserts a `findings` count and a `consecutive_clean` and its fixture states neither; it says only `a three-row file ... recorded against a spec with no prior round`, from which a reader must infer both. The preamble arrived in the same repair pass (b3dae3f3) that rewrote row 3's fixture and mutant and left the SHALL's referent untouched, so the table now publishes a rule it does not keep — and the rows it fails on are the two round-1 index 26 already flags for an unpinned referent.","found_by":0,"location":"§5","resolved":{"evidence":"Fixed by eb2ee542 and a9f720b3, confirmed in the current text. Row 3's SHALL at spec/1.1.0.md:139 now reads \"record the round with findings: 3 and consecutive_clean: 0\" and its fixture states the three severities high, medium and low. Row 7's SHALL at spec/1.1.0.md:143 now reads \"exit 0 with consecutive_clean: 0 (exit 1 under --check, as before)\"; a regex for \"previous binary\" over the file returns no match, so the floating referent round-1 index 26 flagged is closed in the same edit. The sidecar records the control run that makes row 3's severities load-bearing: the same three rows at low/medium/low give consecutive_clean 1 under the default review_converge_on=blocking, which is the value the cell now pins.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"regression","severity":"medium","suggestion":"State the values in rows 3 and 7's fixture cells — `findings: 3`, `consecutive_clean: 0`, and the pre-1.1.0 fixture's own consecutive_clean — which satisfies the preamble and closes the `previous binary` referent in the same edit; or narrow the preamble to the counts a mutant turns on, so it does not claim more than the table delivers."}
{"category":"redundancy","class":"policy-restated-as-spec","evidence":"Read internal/cli/unattended_fence_test.go:17-40 — `fenceProject` seeds a failing gate and asserts `require.Equal(t, 2, code, \"--skip-gate under TP_UNATTENDED must exit 2\")` plus `Contains(… \"user-approved decision\")` and `Contains(… \"tp escalate\")`; internal/cli/unattended.go:38-43 (`refuseUnattended`) is the sink. `tp run` exports `TP_UNATTENDED=1` to every child (internal/engine/childenv.go:28, engine/unattended.go:9-12), so under the primary self-development model the sentence describes a path the binary already refuses.","finding":"Decision 6 closes with \"A unit meeting a red gate does not reach for `--skip-gate`; that is the operator's decision, not the unit's.\" This is a fourth copy of a rule that already lives in `CLAUDE.md` and `skills/tp/SKILL.md`, and — more to the point — it is stated in the weaker, instructional form of a rule tp already enforces mechanically: under `TP_UNATTENDED` a `--skip-gate` close exits 2 and the hint names the supported route, `tp escalate --decision \u003cname\u003e`. The spec's sentence neither adds a requirement nor mentions the route, so it can only drift against the fence it restates, and it is not a decision this release takes.","found_by":1,"found_by_roles":["architect"],"location":"§1","resolved":{"evidence":"Fixed by d02d7b00, which deleted the sentence. Confirmed in the current text: a regex for skip-gate over spec/1.1.0.md returns no match, and decision 5 (formerly 6) now ends at the migration ordering. The reason is the one this finding gave, and it is recorded with its run in spec/1.1.0-measurements.md: TP_UNATTENDED=1 tp done ... --skip-gate exits 2 with a hint naming tp escalate, so the spec's instructional restatement could only drift against a fence the binary already enforces. Section 1 also lost four lines, which is what the finding predicted about section-size.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"architect","severity":"low","suggestion":"Delete the sentence, or replace it with the one thing the release actually needs from it: that the fixture-migration task's gate runs unmodified, and that a red gate at that boundary is an escalation (`tp escalate`), not a skip. §1 is also already 60 lines and over `section-size`; this is a sentence that can go without renumbering anything."}
{"category":"completeness","class":"section-with-no-test-row","evidence":"Read spec/1.1.0.md:104-114 (§3) and :142-153 (§5's ten rows) — no row names skills/tp/REFERENCE.md, skills/tp/SKILL.md or any documentation file. Read index 31's finding text and its resolved.evidence in spec/.tp-review/1.1.0/review-round-1.ndjson, and compared §3 against spec/.tp-review/1.1.0/snapshot-round-1.md:94-99.","finding":"Round-1 index 31 makes two claims — §3 is a normative section with no implementable work, AND no §5 row covers it — and is dispositioned `fixed`. The repair closed the first half: §3 now names REFERENCE.md work a decomposer can claim as source_sections. The second half stands. §5 carries ten rows and none names a documentation file, so the release's only documentation requirement ships with no fixture and no mutant in a table whose preamble says each row names both, and a task can edit SKILL.md alone without any row going red.","found_by":0,"location":"§3","resolved":{"evidence":"→ task acceptance: docs-task-acceptance-no-superseded-sentence-survives. Re-resolved to replace \"Accepted open\", which recorded an open finding as closed; the answer here is the documentation task's own acceptance rather than a twelfth §5 row. Confirmed: §5 carries eleven rows and none names a documentation file. So the release's documentation requirement carries its acceptance in the task — at the tagged tree neither superseded REFERENCE.md sentence survives (the recipe's required-field set and the *Finding `class` and Report* output contract), the `review-record` unit command chains with `\u0026\u0026`, and SKILL.md's four rules ship — and a task that edits SKILL.md alone does not satisfy it.","resolved_at":"2026-09-08T06:23:21Z","status":"wontfix"},"role":"regression","severity":"low","suggestion":"Add a row whose WHEN is the tagged tree and whose SHALL is that neither superseded REFERENCE.md sentence survives it, with the mutant being a task that edits SKILL.md only; or state in §3 that the doc change is verified by the audit phase rather than by §5, so the absence is a decision instead of a gap."}
{"category":"completeness","class":"unfenced-reader-set","evidence":"Read the resolved round-1 row 'write-only-required-field' in spec/.tp-review/1.1.0/review-round-1.ndjson (its suggestion names the either/or §4 was asked to take) and compared it against the current §4 bullets in spec/1.1.0.md:116-132 — none of the six mentions a reader. faster_search over internal/cli/review_report.go for by_category: the payload struct at 40-44 and its construction at 108-122 carry severity/category/class/mechanize breakdowns and no evidence key. faster_search over internal/cli/review_resolve.go: runReviewResolve and runReviewResolveAll set finding[\"resolved\"] on rows held as map[string]any (106-123, 206-223), so a top-level evidence key is neither read nor dropped — it is on disk for any unit that opens the file.","finding":"Round 1's resolved §2 finding asked §4 to take the decision on who reads a stored `evidence` — 'either state as a Non-Goal that stored evidence is never surfaced to a later prompt, or ...'. The repair took the other branch in §1 decision 5, for exactly one path: the previous-round injection, which is the one path the sentinel probe measured. §4 gained nothing, so the remaining readers are still undecided rather than fenced. Two exist and neither costs the release anything to name: the resolve path rewrites the round file as a map of raw rows, so an unknown top-level `evidence` survives and is already readable from disk with no new payload (internal/cli/review_resolve.go:106-123, 206-223); and `tp review --report` builds `by_severity`, `by_category`, `by_class` and `mechanize_candidates` from the same recorded rows and aggregates no evidence (internal/cli/review_report.go:40-44, 108-122). An unnamed reader is not a defect in decision 5 — it is a finding a later round can raise at no cost to itself, on a field whose whole purpose is contested, which is the recurring price this cycle has already paid on §2 four times.","found_by":1,"found_by_roles":["ax-economist"],"location":"§4","resolved":{"evidence":"Kept `wontfix`, and re-resolved only to replace \"Accepted open\", which recorded an open finding as closed. This one is about the text, and the text is right as it stands. The premise moved out from under it: 86374665 cut the old decision 5, so this release surfaces a stored `evidence` on no tp path at all. §4's bullets fence things the release could plausibly do and deliberately does not; a sentence saying that a field nothing reads is never read would fence a non-change. The measurements are not disputed — `review_report.go` aggregates no `evidence`, and `review_resolve.go` rewrites rows as maps so an unknown top-level key survives on disk — and they stay recorded with this row for whichever release does add a reader.","resolved_at":"2026-09-08T06:23:21Z","status":"wontfix"},"role":"ax-economist","severity":"low","suggestion":"One line in §4: apart from the previous-round injection §1 decision 5 names, no tp path surfaces a stored `evidence` — `--report` does not aggregate it, and the resolve path reads the round file directly rather than through a new payload. That closes the branch the resolved round-1 finding left open, and it is a fence sentence rather than new behaviour."}
{"category":"consistency","class":"unenumerated-quantifier","evidence":"Ran the HEAD binary: `tp review spec.md --perspective regression --diff-from base.md --findings prev.ndjson --no-state` -\u003e `perspective: regression`, one prompt with `ROLE: regression` whose output format line is `{\"severity\":\"critical|high|medium|low\",\"category\":\"...\",\"location\":\"section heading or line number\",\"finding\":\"what is wrong\",\"suggestion\":\"how to fix it\"}` — the same five keys the default panel emits. `reviewPerspectives` at internal/cli/review.go:512 lists documentation, testing, code-audit, regression.","finding":"Row 8's fixture cell claims 'every review emission path whose rows reach the same `--record`' and then names three: `tp review \u003cspec\u003e`, `tp review \u003cspec\u003e --role \u003cr\u003e`, `tp review \u003cspec\u003e --perspective code-audit`. There is a fourth: `--perspective regression`, which emits a `regression` role prompt carrying the standard five-key finding format, and whose rows are ordinary review findings that reach the same `--record`. A check built from the row's enumeration is sound only by accident — the regression path shares the default panel's format const today, so adding `evidence` there fixes it invisibly, and the check stays green if that ever stops being true. Row 8 exists precisely because per-path enumeration is load-bearing (its mutant is one path diverging), which is what makes an incomplete enumeration inside it worth reporting.","found_by":1,"found_by_roles":["implementer"],"location":"§5","resolved":{"evidence":"Fixed by a9f720b3, confirmed in the current text. Row 8's fixture no longer restates three paths: it cites the derived list in spec/1.1.0-measurements.md, where tp review with --perspective regression is a row of the table in its own right (call site review_regression.go:195, five panel keys, does not name evidence). The check the row asks for is therefore derived from the tree rather than sound by accident, which is the branch this finding preferred over adding one more name to a hand-written list.","resolved_at":"2026-09-08T05:24:58Z","status":"fixed"},"role":"implementer","severity":"low","suggestion":"Either add `--perspective regression` to the row's list, or derive the list in the check from the code (`reviewPerspectives` plus the panel roles) rather than restating it, the way §6.2 of an earlier release already requires for the mode list. Do not leave an 'every' over three named members of a set of four."}
{"category":"ambiguity","class":"sibling-rows-disagree-on-an-unstated-observable","evidence":"Built both fixtures from their cells alone against the minimal implementation (/tmp/tester-r2-Xfrfnu/tp-fixed, evidence added to missingFindingFields with TrimSpace + an all-lines required-key pass at --record). Row 9 fixture (evidence ' ' plus a legal row in one file): merge exit=0, stderr 'warning: skipping incomplete line (missing evidence) in f.ndjson' then 'merged: 1 unique findings from 1 files'. Row 5 fixture (offender alone in a.ndjson, legal row in b.ndjson): merge exit=1 with 'no line parsed in a.ndjson: every content line was skipped'.","finding":"Rows 5 and 9 describe the same offending row — one that both gates must judge missing on `evidence` — but only row 5 states --merge's exit code, and the two fixtures make that code differ. Row 5's offender sits alone in its input file, so the input is dropped and --merge exits 1; row 9's offender sits beside a legal row in one file, so the file parses one row and --merge exits 0. A tester who reads row 5's '**exits 1**' as the rule for a skipped-evidence row and asserts it on row 9's fixture is red against a correct build. Row 9's cell explains the file shape ('the file still parses one row and the skip shows as a warning rather than as a dropped input') without ever stating the code that follows from it.","found_by":1,"found_by_roles":["tester"],"location":"§5","resolved":{"evidence":"→ task acceptance: rows-9-and-11-assert-merge-exit-0. Re-resolved to replace \"Accepted open\", which recorded an open finding as closed; the answer here is what the tests assert. Confirmed unchanged: row 9 at spec/1.1.0.md:152 and row 11 at :154 both state the skip and the stderr warning and neither states `--merge`'s exit code, while row 5 at :148 states its 1. The tests for rows 9 and 11 therefore assert `--merge` exit 0 — the file still parses its second row, so the skip surfaces as a warning and not as a dropped input — which is exactly what distinguishes them from row 5, whose offender sits alone in its input file and drops it at exit 1.","resolved_at":"2026-09-08T06:23:21Z","status":"wontfix"},"role":"tester","severity":"low","suggestion":"State row 9's --merge exit code in its SHALL — 'exit 0, with the stderr warning naming evidence' — the way row 5 states its 1. One clause, and the two rows stop looking like a contradiction."}
