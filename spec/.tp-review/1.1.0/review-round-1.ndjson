{"category":"completeness","class":"fixture-migration-unscoped","evidence":"Built a mutant in a `git clone --no-hardlinks` at f8d9795d outside the repository: added \"evidence\" to missingFindingFields and a TrimSpace check in parseRecordRows, nothing else. Baseline `go test ./internal/cli/ -count=1` = ok 15.689s; merge-gate change alone = 18 failing test functions; both gates = 68 failing test functions, package FAIL. Fixture count by regex over internal/cli/*_test.go for JSON objects carrying both \"finding\" and \"severity\": 164 rows in 33 files (27 in review_merge_test.go, 22 in review_report_test.go, 19 in review_test.go).","finding":"Adding evidence to both gates makes 68 existing test functions in internal/cli fail, and neither §5 nor §4 acknowledges the migration. 164 review-finding fixture rows are spelled out across 33 _test.go files; every one of them would have to gain an evidence key, and tests that assert merge counts, dedup, cluster attribution and report breakdowns change behaviour rather than just failing. That work cannot ride along in the one atomic commit that edits the two predicates, and a decomposer reading §5 would size the release at nine test rows.","found_by":1,"found_by_roles":["implementer"],"location":"§1","role":"implementer","severity":"high","suggestion":"Name the fixture migration as its own decision or as an explicit consequence in §1, so it decomposes as a task of its own with the parity change depending on it; and say whether existing fixtures gain a real evidence string or a placeholder, since §2 forbids tp from judging the text but the repository's own recording rules do not."}
{"category":"completeness","class":"emission-path-not-enumerated","evidence":"Ran `tp review spec/1.1.0.md --perspective code-audit --affected-files internal/cli/review_merge.go` in a clone: exit 0, prompts[0].role='code-auditor', output format line is {\"role\":\"code-audit\",\"id\":\"ca-001\",\"file\":...,\"category\":\"gap\"}, and the string 'evidence' occurs once in the whole prompt (in unrelated prose). Also read internal/cli/review_regression.go:195, a second writer of the same findingFormat constant.","finding":"Decision 1 says 'The review role prompt tp emits names the key', and §5 row 8's fixture pins one path (`--role implementer`). But `tp review` emits at least one other prompt whose rows go to the same `--record`: `--perspective code-audit` emits role `code-auditor` with an eleven-key output format (role,id,file,line,pattern,current_behavior,spec_coverage,finding,suggestion,severity,category) that names no evidence key at all, and the regression role prompt is generated from a second call site. Under §1's rule a reviewer following the code-audit prompt exactly produces rows `--record` refuses, and no §5 row would go red. The implementer must guess whether these paths are in scope.","found_by":1,"found_by_roles":["implementer"],"location":"§1","role":"implementer","severity":"high","suggestion":"Enumerate the emission paths the rule covers, or state a non-goal naming the ones it does not (`--perspective code-audit`, `--perspective documentation`, `--perspective testing`) and say what happens to their rows at `--record`."}
{"category":"completeness","class":"write-only-required-field","evidence":"Probe in a `git clone --no-hardlinks` outside the repo (tp review writes a snapshot). Recorded a 2-row round carrying `\"evidence\":\"ZZSENTINELZZ ...\"` on both rows; `tp review probe-spec.md --record r1.ndjson` exited 0 and .tp-review/probe-spec/review-round-1.ndjson holds keys ['category','class','evidence','finding','location','role','severity','suggestion'] with the sentinel intact. `tp review probe-spec.md --round 2` then emitted 4 prompts, 15,938 chars total: the prior finding text 'section one under-specifies' occurs 4 times (once per role prompt) and 'ZZSENTINELZZ' occurs 0 times. Cause read afterwards, not before: reviewFinding has no evidence field.","finding":"`evidence` is write-only inside tp: every reviewer must produce it on every row, `--record` stores it, and no tp path ever shows it to another agent. `reviewFinding` (internal/cli/review.go:131-139) has no evidence field, so the prior-round findings that tp injects into round N+1's prompts drop it. The spec's cited justification is a study where the loop improved because each finding had to cite an artifact that confirms or refutes it — a citation that is read. Here it is neither checked (§2, deliberately) nor surfaced (unstated). The release therefore charges every finding for a string whose only readers are the merged file and a human, which is exactly the AX case of an output byte no downstream agent parses.","found_by":1,"found_by_roles":["ax-economist"],"location":"§2","role":"ax-economist","severity":"high","suggestion":"§4 must take the decision rather than leave it unmade: either state as a Non-Goal that stored `evidence` is never surfaced to a later prompt (and say the cost is accepted as write-time discipline), or add a §5 row requiring the prior-round injection to carry it. Price both: at the measured 440 chars/row the surfacing option costs roughly rows x 440 x panel size per round (~39k chars for a 22-row round at 4 roles), because prior findings render once per role prompt."}
{"category":"completeness","class":"required-set-vs-pre-resolved-row","evidence":"Ran the row through both binaries in the clone. HEAD: exit 0, `clean: true`, `consecutive_clean: 1`, `findings: 1` — the round records clean. The four-key mutant: exit 1, `line 1: missing or empty evidence`. The same input, two opposite verdicts, and nothing in §2 or §5 chooses between them.","finding":"§2 says the four keys are 'one required set' enforced at both gates, and that `--record`'s pre-resolved rules are unchanged. It never says whether a pre-resolved `wontfix` row — which already carries `resolved.evidence` — must ALSO carry a top-level `evidence`. Both readings are defensible and they disagree on whether a clean round can still be recorded from a pre-resolved file, which is what convergence rests on. §5 row 6 covers only the pre-resolved `fixed` case, which aborts for an unrelated reason.","found_by":1,"found_by_roles":["tester"],"location":"§2","role":"tester","severity":"high","suggestion":"State in §2 whether the required set applies to pre-resolved rows, and add a §5 row whose fixture is one `wontfix` row carrying `resolved.evidence` and no top-level `evidence`, with the exit code and the resulting `consecutive_clean` named."}
{"category":"completeness","class":"doc-contract-not-in-scope","evidence":"Read skills/tp/REFERENCE.md at lines 182, 685 and 722. 685: `role is free-form attribution metadata — --merge requires only location, severity and finding, and never checks role against the corpus.` 722: `Every review finding carries role, location (a §-anchor), class, and severity (tp's output contract).` 182 is the review-record run unit, merge-then-record.","finding":"§3 is the release's only statement of what documentation ships, and it names only SKILL.md's four writing rules. skills/tp/REFERENCE.md states the rule §2 changes as fact in two places, and §4 fences neither out: line 685 says `--merge requires only location, severity and finding`, and line 722 says `Every review finding carries role, location (a §-anchor), class, and severity (tp's output contract)`. After this release both sentences are false — the second is the very key-set §5 row 8 is about — and line 685 sits inside a documented recipe whose inputs are exactly the pre-release per-role files §2 says now exit 1.","found_by":1,"found_by_roles":["architect"],"location":"§3","role":"architect","severity":"high","suggestion":"Widen §3 to name the REFERENCE.md sentences the release must change, or add a Non-Goal saying the doc set is the implementing task's acceptance and naming the two sites, so the task cannot miss them."}
{"category":"ambiguity","class":"fixture-does-not-pin-the-mutants-discriminator","evidence":"Wrote row 3's fixture from its cell alone — three rows with realistic evidence strings of 70, 65 and 32 characters — then built the stated mutant (the four-key check plus `len(evidence) \u003c 20` rejection) in the clone. `tp review spec/probe3.md --record` returned exit 0, `findings 3`, `consecutive_clean 0` under BOTH the HEAD binary and the mutant binary: byte-identical verdicts, so the assertion cannot separate them.","finding":"Row 3's mutant is 'the check also rejects an `evidence` under 20 characters, so the shortest of the three rows is refused', but its fixture cell — 'a three-row file, legal on all four keys' — pins no evidence length. A fixture written from that cell alone has no row under 20 characters, because §2 defines `evidence` as what was run or read and any faithful instance of that is longer. The mutant then cannot make the row red.","found_by":1,"found_by_roles":["tester"],"location":"§5","role":"tester","severity":"high","suggestion":"Pin the length in the fixture: 'three rows legal on all four keys, one of whose `evidence` is a single word'. The general repair for §5 is a rule that each fixture cell must name the value its mutant flips."}
{"category":"consistency","class":"non-goal-crossed-by-test-row","evidence":"Built the binary from a `git clone --no-hardlinks` outside the repo and ran `tp audit spec/1.1.0.md` there: all 4 emitted auditor prompts contain the literal `Output contract — stamp EVERY finding` and the `- class: a kebab-case slug` line. Read internal/cli/review.go:1128-1152 — `outputContractInstruction(role, phase)` is one body branching on `phase == engine.PhaseAuditors`.","finding":"Row 8 requires that EVERY site in an emitted review prompt that names finding keys name the same set. One of those sites — the `Output contract — stamp EVERY finding` block — is emitted by a single function shared with the audit phase, so satisfying row 8 by deriving the key-naming sites from one source necessarily edits the audit prompt as well. §4 declares `No change to the audit phase`. The spec therefore requires, in §5, a change §4 forbids.","found_by":1,"found_by_roles":["architect"],"location":"§5","role":"architect","severity":"high","suggestion":"Either scope row 8 to the review-only key list (the JSON object line a reviewer copies) and drop the `every site` quantifier, or amend §4's Non-Goal to say the shared output-contract block is in scope and state what the audit prompt is permitted to become."}
{"category":"feasibility","class":"unsatisfiable-shall","evidence":"Emitted the prompt in a clone and parsed it: `tp review spec/1.1.0.md --role implementer` -\u003e prompts[0].prompt, 4264 chars; quoted-key sites at offsets 2195 (severity/category/location/finding/suggestion), 2425 (Optional \"class\"), and the '- key:' contract lines 41-45 (role/location/class/severity). Then ran `tp review spec/1.1.0.md --report`, whose JSON keys are ['by_category','by_class','by_severity','convergence','mechanize_candidates','overlap_report','rounds'] — by_category is a shipped output fed by the `category` key the example names and the contract does not.","finding":"Row 8's second SHALL — 'every site in the prompt that names keys names the same set' — cannot be satisfied by any implementation, and the spec never says which set is canonical. The three sites name genuinely different things by design: the JSON example names severity/category/location/finding/suggestion, the sentence beside it names class and calls it 'Optional', and the output contract names role/location/class/severity. Making them equal forces either (a) class to become mandatory, contradicting the word 'Optional' in the same prompt and the merge gate's deliberate exclusion of class and role, or (b) category and suggestion to be dropped from the example, which removes the input of a shipped output key. §4 authorises neither change. As written the row is a requirement no task can close.","found_by":2,"found_by_roles":["implementer","tester"],"location":"§5","role":"implementer","severity":"high","suggestion":"Replace the second SHALL with the only thing §2 can justify: the prompt's output-format key line names the four required keys (severity, finding, location, evidence). If cross-site agreement is wanted, name the canonical set in §2 and add the schema change to §4 as an explicit goal, distinguishing required keys from optional ones (class) and from attribution keys (role) so 'the same set' has a referent."}
{"category":"consistency","class":"merge-discards-evidence-it-attributes","evidence":"In the clone: two one-row files sharing location §1 and class c, roles implementer and tester, evidence EV-ONE and EV-TWO. `tp review --merge m1.ndjson m2.ndjson` exits 0 and prints {\"class\":\"c\",\"evidence\":\"EV-ONE\",\"finding\":\"a\",\"found_by\":2,\"found_by_roles\":[\"implementer\",\"tester\"],\"location\":\"§1\",\"role\":\"implementer\",\"severity\":\"high\"} — EV-TWO is absent at HEAD, before any implementation. skills/tp/REFERENCE.md:182 defines the review-record run unit as `tp review --merge $TP_ROUND_DIR/role-*.ndjson -o merged.ndjson; tp review \u003cspec\u003e --record merged.ndjson`.","finding":"§1's Consequences says a finding that does not name what was done to reach it does not enter the record at all. Row 4 settles the merge semantics as `the representative row carries its own evidence`, so when two roles reach one (location, class) by different work the recorded round keeps one role's evidence while `found_by_roles` still names both — the record asserts two roles found it and preserves the work of one. Under `tp run` this is the normal path, not an edge case: the `review-record` unit is defined as merge-then-record, so the merged file IS the record input. No section decides whether the discarded evidence is acceptable and no Non-Goal fences it.","found_by":1,"found_by_roles":["architect"],"location":"§5","role":"architect","severity":"high","suggestion":"Take the decision in §1 or fence it in §4: either the representative carries every contributing row's evidence (keyed by role, mirroring found_by_roles), or state as a Non-Goal that a merged record keeps only the representative's evidence, and say plainly what is lost."}
{"category":"completeness","class":"exit-code-not-actionable","evidence":"Ran in the clone: `tp review --merge drop-a.ndjson drop-c.ndjson -o merged-drop.ndjson` where drop-c.ndjson holds one row missing location and severity. Exit 1, stderr 'no line parsed in drop-c.ndjson: every content line was skipped...', and merged-drop.ndjson is 130 bytes holding 1 row — the payload was written despite the non-zero exit. The corpus sweep the spec relies on is the same shape: 163/163 canonical round files and 59/59 per-role files carry zero rows with a non-empty `evidence`, re-derived here under both globs.","finding":"Decision 1 buys `--record` a one-round-trip recovery (it names every offending row rather than stopping at the first). Decision 2 gives `--merge` no equivalent, and the cost on that side is larger: in the reset-native model each per-role findings file comes from a separate subagent, so a role that omits `evidence` on all its rows makes its input parse zero rows, which trips the existing dropped-input rule and exits 1. Measured, `--merge` still writes a usable merged payload on that exit, so exit 1 does not mean 'no output' — an orchestrator branching on the exit code alone either re-spawns a whole role subagent or discards a payload it could have used. §5 row 5 pins the exit code but says nothing about the payload.","found_by":1,"found_by_roles":["ax-economist"],"location":"§1","role":"ax-economist","severity":"medium","suggestion":"Add to §1 decision 2 or §5 row 5 what an operator can conclude from the exit: that `--merge` exits 1 when an input contributes no row AND still writes the merged payload for the inputs that did parse, so recovery is re-emitting the named role rather than the whole panel. This costs no new flag and no new byte."}
{"category":"ambiguity","class":"check-order-unspecified","evidence":"Ran that exact two-row file through the unmodified HEAD binary in the clone: line 1 legal but with no evidence key, line 2 legal on all four keys and carrying resolved.status=fixed. `tp review spec/1.1.0.md --record` exited 1 with {\"error\":\"line 2: row arrives pre-resolved fixed — ...\"} — line 1 is never mentioned, so HEAD's behaviour does not settle the question and the new rule inverts what a reader would expect.","finding":"Decision 1 requires --record to name every offending row 'rather than stopping at the first', while the pre-resolved `fixed` rule §2 preserves aborts on the first offending line. The spec never orders the two, and the interleaved case is not in §5: row 1 uses a file where only evidence is wrong, row 6 uses a single-row file where only `resolved.status` is wrong. So for a file whose line 1 lacks evidence and whose line 2 is pre-resolved fixed, both 'collect all evidence offences, then abort' and 'abort at the first line that fails any rule' satisfy every named test while giving different exit messages.","found_by":1,"found_by_roles":["implementer"],"location":"§1","role":"implementer","severity":"medium","suggestion":"State the order in §2 — e.g. the required-field scan completes over the whole file and its report wins, or the pre-resolved abort still short-circuits — and add a §5 row whose fixture carries both defects on different lines, since that is the only fixture that can make the wrong order red."}
{"category":"completeness","class":"unspecified-prompt-guidance","evidence":"Read spec/1.1.0.md lines 29-32 (decision 1) and line 133 (§5 row 8); read §2's rule cell at line 71 and §2's `tp does not look at the content of evidence` at lines 86-88. Ran `tp review probe-spec.md --round 2` in the clone: the word 'evidence' occurs 4 times across the emitted prompts and every occurrence is the unit-framing sentence 'report it with its evidence instead of making the edit', i.e. no key list and no rule.","finding":"Decision 1 says only that \"the review role prompt tp emits names the key\", and §5 row 8 tests only that the prompt's key-naming sites agree with each other. Nothing in the spec requires the prompt to state what the text should contain. Since tp explicitly does not look at the content, the cheapest compliant reviewer writes `\"evidence\":\"read the spec\"` and passes both gates. The release's entire yield is therefore decided by a prompt sentence the spec neither writes nor constrains, while its cost is paid unconditionally — the spread between the cheapest and a real compliant string is roughly 16 chars against the measured 440.","found_by":1,"found_by_roles":["ax-economist"],"location":"§1","role":"ax-economist","severity":"medium","suggestion":"State in §1 decision 1 what the emitted prompt must say, at the altitude of a requirement rather than a wording: that it names the key AND states the rule §2's table gives (what was run or read — the command and its output, the query, the probe, or the artifact and line). Then §5 row 8 can assert the rule reaches the prompt, not only that three sites agree on a key name."}
{"category":"redundancy","class":"unstripped-payload","evidence":"Ran in the clone on one legal row: `tp review --merge drop-a.ndjson -o m-full.ndjson` and the same with `--compact -o m-comp.ndjson` — both files 130 bytes and `diff` reports them identical, so `--compact` changes no byte of the row payload. Read internal/cli/review_merge.go:82,88 — the two `if !IsCompact()` sites wrap the stderr summary and the attribution-exclusion notice, not the rows.","finding":"The merged findings NDJSON is the largest agent-facing artifact the review loop produces, and this release grows every row of it by a field tp never reads. `--compact` — documented as 'minimal JSON: omit descriptions, source_lines, metadata' — does not touch merged row payloads at all: `IsCompact()` in review_merge.go gates only the summary and attribution notice lines. So there is no path by which an agent can read a merged round without paying for every evidence string, and the spec does not mention `--compact` or any strip once.","found_by":1,"found_by_roles":["ax-economist"],"location":"§2","role":"ax-economist","severity":"medium","suggestion":"Decide it in §2 or fence it in §4: either `--compact` strips `evidence` from `--merge` output (the field feeds a resolve decision, not a re-review decision, so an agent reading the merge to pick what to fix does not need it), or state as a Non-Goal that no output path strips it and the full text is always carried."}
{"category":"completeness","class":"unpriced-output-growth","evidence":"Ran over both globs in the clone: ground rows 2,932, with a non-empty `evidence` 2,692; evidence length mean 440, median 411, p90 692, max 1,833. Review corpus: 3,969 rows, 6,296,973 bytes, median row 1,436 bytes -\u003e +1,801,823 bytes (+28.6%) at ground's mean; rows per non-empty round mean 24.3 / median 22 -\u003e ~9,987 chars/round. Disposition counts on the same set: 1,482 fixed + 95 wontfix = 1,577 (39.7%), 2,392 rows (60.3%) carry no `resolved` at all.","finding":"The release's only cost is output bytes, and the spec prices none of them. §1's Consequences and the measurements file price exactly one thing — the 3,969 rows the exemption spares — which is a one-time compatibility cost. The recurring cost is what each round now writes. An empirical basis for it already exists in this repository and this cycle did not use it: ground rows carry an `evidence` key under the same free-text semantics, and 60.3% of recorded review rows are never dispositioned, so the majority of the added bytes are written for rows that no resolve decision ever reads.","found_by":1,"found_by_roles":["ax-economist"],"location":"§2","role":"ax-economist","severity":"medium","suggestion":"Add the per-round figure to spec/1.1.0-measurements.md (derivation, not the number, in §1's Consequences): ground's evidence is mean 440 / median 411 / p90 692 chars over 2,692 rows; a median 22-row review round therefore grows by ~10,000 chars (~2,500 tokens), and the recorded corpus would grow 28.6%. Then state whether that is accepted, because the AX case for the release rests on it."}
{"category":"consistency","class":"required-set-not-aligned-with-readers","evidence":"Read internal/cli/review_merge.go:187-198 (`missingFindingFields` gates location, severity, finding; the doc comment says role 'is not a merge gate') and 240-248 (`clusterFindingFromRow` reads Location, Class, Role, Severity, Finding). Counted over the recorded review corpus under both globs, canonical round files: 3,969 rows, missing `role` 300 (7.6%), missing `class` 205 (5.2%), missing both 202 — so the silent-mis-cluster case is not hypothetical.","finding":"The required set is chosen by what a reader wants to see, not by what tp's own machinery needs. `evidence` — which §2 says tp never looks at — becomes mandatory at both gates, while `class` and `role`, the two fields `clusterFindingFromRow` actually reads to cluster by (location, class) and to build `found_by_roles`, stay optional at both. A row missing `class` silently forms its own cluster and a row missing `role` silently drops out of attribution; neither is warned about, and both defects cost the agent a wrong dedup rather than a refusal. The release makes the field with no consumer the strict one.","found_by":1,"found_by_roles":["ax-economist"],"location":"§2","role":"ax-economist","severity":"medium","suggestion":"Either add `class` and `role` to the required set (they are what the merge output's own correctness depends on), or state in §4 why a field tp reads stays optional while a field tp ignores becomes mandatory. As it stands §2's 'those four keys are one required set' reads as a principle the set does not follow."}
{"category":"completeness","class":"non-string-value-unjudged","evidence":"Ran five one-row fixtures through the HEAD binary in the clone, evidence set to 123, null, [\"ran x\"], {\"cmd\":\"go test\"} and true: `tp review --merge` exited 0 for all five with '1 unique findings' and no warning, and `tp review spec/1.1.0.md --record` exited 0 for all five. Read internal/cli/review_merge.go:193 (`if s, _ := row[k].(string); s == \"\"`) and review_record.go:271 for the two predicates that would inherit the case.","finding":"The table types evidence as `string` and rules it 'non-empty', and §1 says --record refuses a row whose evidence is 'missing or empty'. A row carrying `\"evidence\": 123` (or null, an array, an object, a bool) is neither missing nor empty, and the spec gives no verdict. The two natural implementations differ observably: a `.(string)` assertion silently classes a number as missing and prints the missing-or-empty message, while a `_, ok := row[\"evidence\"].(string)` check can report a type error. No §5 row contains a non-string value, so both pass every named test and the choice is left to the implementer.","found_by":1,"found_by_roles":["implementer"],"location":"§2","role":"implementer","severity":"medium","suggestion":"Add one sentence to §2 saying a value that is not a JSON string is judged missing (with the same message), and add the non-string case to §5 row 9's fixture so the choice is pinned by a test rather than by the implementer."}
{"category":"completeness","class":"error-precedence-unpinned","evidence":"Built a three-row file: legal row, pre-resolved `fixed` row at line 2, row with no `evidence` key at line 3. The per-line implementation (my four-key mutant) returned exit 1 with `line 2: row arrives pre-resolved fixed — a fix means the spec changed`, identical to HEAD, and never named line 3. A whole-file-first implementation returns `line 3: missing or empty evidence` on the same input; both are consistent with §2 as written.","finding":"§2 requires `--record` to name 'every offending line in one exit rather than stopping at the first', while the pre-resolved rules it leaves unchanged abort at the first offending line. When one file holds both defects the precedence is unspecified: a per-line implementation reports whichever comes first in the file, a whole-file required-field pass reports the missing-field lines and never reaches the abort. No §5 row holds a file with both, so two engineers write contradictory assertions on the same input.","found_by":1,"found_by_roles":["tester"],"location":"§2","role":"tester","severity":"medium","suggestion":"State the order in §2 — e.g. 'the required-field scan runs over the whole file first; the pre-resolved rules are applied only to a file that passes it' — and add a §5 row whose fixture holds a pre-resolved `fixed` row before a row missing `evidence`."}
{"category":"completeness","class":"conflicting-refusal-rules-unordered","evidence":"In the clone at HEAD, two files: A = [pre-resolved fixed row at line 1, row missing evidence at line 2]; B = the same two rows reversed. `tp review spec/1.1.0.md --record` exits 1 on both and reports exactly one line each — A: `line 1: row arrives pre-resolved fixed`, B: `line 2: row arrives pre-resolved fixed`. The report is positional and single-line, so the two rules already compete for one message and the spec names no winner. Round file: spec/.tp-review/1.1.0/ still held no review-round-1.ndjson after both runs.","finding":"§2 says `--record` names every offending line in one exit rather than stopping at the first, while §5 row 6 requires the existing pre-resolved `fixed` abort — which stops at the first — to be preserved unchanged. The spec gives no rule for a file that holds both kinds of offender, and neither row's mutant can discriminate that input, because rows 1, 2 and 6 each build a fixture holding only their own violation.","found_by":1,"found_by_roles":["architect"],"location":"§2","role":"architect","severity":"medium","suggestion":"Add one sentence to §2 fixing the precedence — e.g. required-field offenders are collected and reported first, and the pre-resolved aborts run only on a file that passes the field gate, or the reverse — and add a §5 row whose fixture holds both kinds, in both orders."}
{"category":"completeness","class":"field-type-mismatch-unspecified","evidence":"In the clone at HEAD, three one-row files with `location` set to 42, [\"§1\"] and null. Each `tp review --merge` exits 1 with `warning: skipping incomplete line (missing location)`. internal/cli/review_merge.go:193 is `if s, _ := row[k].(string); s == \"\"` — the type assertion silently yields \"\" for every non-string, so `missing` is what a wrong type reports.","finding":"§2's table types `evidence` as `string` with the rule `non-empty`, but neither §2 nor any §5 row says what either gate does when the key is present and is not a string. This is sharper for `evidence` than for the other three keys: it is explicitly free text describing `the command and the output it produced, the query, the probe that was built` — a shape a reviewer plausibly emits as an array or an object — while the existing gate reports any non-string as *missing*, a diagnosis the reviewer cannot act on because the key is right there.","found_by":1,"found_by_roles":["architect"],"location":"§2","role":"architect","severity":"medium","suggestion":"State in §2 whether a non-string `evidence` is refused as a type error with its own message, or folded into `missing or empty`; if the latter, say the message says the value was not a string."}
{"category":"completeness","class":"missing-clean-round-row","evidence":"Read §5 rows 1-9 of spec/1.1.0.md (lines 126-134): rows 1, 2, 5, 9 record nothing, row 3 records a 3-finding dirty round, rows 4 and 6 are merge-side, rows 7 and 8 are load-side and prompt-side. Confirmed against a run that a clean round is reachable and is the thing at risk: the pre-resolved `wontfix` fixture recorded `clean: true, consecutive_clean: 1` at HEAD and exit 1 under a four-key gate.","finding":"No row pins that a findings file with zero rows still records a CLEAN round and advances `consecutive_clean` under the new gate. §1 decision 3 promises rounds 'clean and converge as they did', but row 7 tests only a round being LOADED, and row 3 records a dirty round. The one behaviour convergence actually depends on — a clean round recorded after this release — has no fixture.","found_by":1,"found_by_roles":["tester"],"location":"§5","role":"tester","severity":"medium","suggestion":"Add a row: WHEN the findings file holds no rows (or only blank lines), tp SHALL record the round, report `clean: true` and `consecutive_clean` incremented; mutant: the required-field check treats an empty file as an offending input and exits 1."}
{"category":"feasibility","class":"row-green-before-the-change","evidence":"Ran row 4's fixture through the unmodified HEAD binary in the clone: two files with identical location §2 and class c1, roles implementer and tester, different finding and evidence text. `tp review --merge` exited 0 and printed {...,\"evidence\":\"ran X\",...,\"found_by\":2,\"found_by_roles\":[\"implementer\",\"tester\"],...}; feeding that stdout to `tp review spec/1.1.0.md --record` exited 0 with findings 1. Both halves of the SHALL hold with nothing implemented.","finding":"Row 4 is already green against the unmodified binary, so it names no work. `--merge` returns the representative row itself, so every unknown top-level key including evidence survives untouched, and `--record` accepts merged stdout today. Its mutant ('the merge emits the representative with evidence dropped') would require someone to write deletion code that no implementation of §1 or §2 calls for, so the row certifies a property nothing in the release can break.","found_by":1,"found_by_roles":["implementer"],"location":"§5","role":"implementer","severity":"medium","suggestion":"Either drop row 4, or replace its SHALL with the part the change can break: that a merged row whose inputs all carried evidence still passes the NEW record gate, and that a cluster whose representative came from an input the new merge gate would skip does not silently lose its evidence."}
{"category":"consistency","class":"green-at-head-row","evidence":"Ran row 4's fixture verbatim at HEAD in the clone: two files, identical location/class/severity, different finding and evidence, roles implementer and tester. `--merge` exit 0 emitted `{\"class\":\"parity-gap\",\"evidence\":\"ran tp review --merge on two files and read the merged output\",...,\"found_by\":2,\"found_by_roles\":[\"implementer\",\"tester\"],\"role\":\"implementer\"}` — the implementer row's own evidence, carried through. `tp review spec/probe.md --record` on that merged output: exit 0, findings 1. Both halves green with nothing built. Read `internal/cli/review_merge.go:203-236` for why: `row := allFindings[c.Representative(cfs)]` is the input map itself.","finding":"Row 4 passes at HEAD in both halves with nothing implemented, so it cannot distinguish the built release from the current tree. `clusterMergeFindings` returns the original row map, so any key a reviewer wrote — `evidence` included — survives the merge by construction, and the merged file records at exit 0 today. Its mutant is a deletion of a key that no code enumerates, i.e. a regression nobody would introduce, which is the shape §5's standing rule was written against even though the row satisfies its letter.","found_by":1,"found_by_roles":["tester"],"location":"§5","role":"tester","severity":"medium","suggestion":"Either drop row 4, or give it a half that only the release can satisfy — e.g. require that the merged output is recordable UNDER THE NEW GATE and that a cluster whose representative lacks `evidence` is skipped before clustering rather than emitted."}
{"category":"completeness","class":"underspecified-fixture-artifacts","evidence":"Measured in the clone: after `tp review spec/probe4.md` (emit only, no record) the round directory already holds `snapshot-round-1.md` and `state.json`. So a recorded round is at minimum three artifacts, not the single 'round file' the fixture names.","finding":"Row 7's fixture is 'a pre-`1.1.0` round file copied under `testdata`', but `tp review --status` reads the round through `state.json`, and the round's snapshot participates in the staleness/hash reporting the same command emits. A fixture that is one file does not produce a readable round at all, and two engineers building the missing pieces differently get different `stale`/`spec_hash` output from the same SHALL.","found_by":1,"found_by_roles":["tester"],"location":"§5","role":"tester","severity":"medium","suggestion":"Name the artifact set in the fixture cell: the round NDJSON, the `state.json` entry that references it, and the round snapshot — and say whether the snapshot is expected to match the spec text."}
{"category":"ambiguity","class":"method-only-in-ungraded-sidecar","evidence":"Compared spec/1.1.0.md line 133 against spec/1.1.0-measurements.md lines 391-402, which hold the three-site table (review.go:804, :806, :1128-1152) and the sentence 'Making the three derive from one source is acceptance for the task'; neither appears in the body. Counted bare key mentions in the emitted round-1 prompt with a regex over prompts[0].prompt: class 5, finding 6, location 3, role 3, severity 2, category 1, suggestion 1, evidence 3 — most outside any list.","finding":"Row 8 tells the implementer that 'the check parses each key-naming site and compares the sets', but the spec body never defines what a key-naming site is, how many there are, or where. A decomposer working from the body alone must invent the enumeration: the emitted prompt mentions `class` five times, `finding` six and `location` three, most of them in prose that names no key list. The only enumeration — three sites with file and line — and the only statement of the intended fix ('Making the three derive from one source is acceptance for the task') live in `spec/1.1.0-measurements.md`, which line 5 of that file says `tp ground` does not grade and which spec line 6 says the spec stands without. So the one row that requires a construction rule has that rule reachable only in a file no round reads.","found_by":1,"found_by_roles":["implementer"],"location":"§5","role":"implementer","severity":"medium","suggestion":"Put the site-identification rule in the body — e.g. 'a key-naming site is a literal enumeration of finding keys in the emitted prompt text; there are three' — or drop the cross-site half (see the finding on §5 row 8's unsatisfiable SHALL) so the body no longer depends on the sidecar to be buildable."}
{"category":"ambiguity","class":"unsatisfiable-set-equality","evidence":"Read internal/cli/review.go lines 802-808 and 1120-1152 via codedbpro; the three sites and their key sets are as quoted. spec/1.1.0-measurements.md lines 399-402 says `Making the three derive from one source is acceptance for the task that adds evidence to the emission — it is not a claim in the spec body`.","finding":"Row 8's SHALL — `every site in the prompt that names keys names the same set` — cannot be satisfied by the three existing sites without a design decision the spec never takes. review.go:804's JSON example names severity/category/location/finding/suggestion; review.go:806 names only the optional `class`; review.go:1131-1149 names role/location/class/severity. Set equality requires either deleting the optional-class sentence or pushing finding/category/suggestion into the contract block. §1 decision 4 says exactly this class of code shape is `filled in by the implementing task, not predicted by the author`, so §5 row 8 contradicts §1 decision 4 as well as being undecided.","found_by":1,"found_by_roles":["architect"],"location":"§5","role":"architect","severity":"medium","suggestion":"State the property at the altitude §1 decision 4 sets — e.g. `no key-naming site in the emitted prompt omits a key another site requires` — or keep row 8 to `the JSON object line a reviewer copies names evidence` and move the one-source unification into the implementing task's acceptance, where spec/1.1.0-measurements.md already says it belongs."}
{"category":"feasibility","class":"mutant-is-the-baseline","evidence":"Read §5's mutant column across all nine rows in spec/1.1.0.md lines 126-133; rows 1,2,3,4,5,6,7,9 each name a change to the fixed implementation, row 8 alone names HEAD. Cross-checked against row 2, which also cites HEAD but in its FIXTURE column ('at HEAD this records at exit 0') while its mutant column names a separate post-fix mutation.","finding":"Row 8's mutant column names HEAD ('which is `HEAD`, where three sites name three different sets'), i.e. the pre-implementation tree, not a mutation of the code the task will write. Every other row mutates the post-fix implementation (row 1 'a check that names the first offender', row 2 'a check that reads only the keys a row actually has', row 5 'evidence added to --record's check only'). Because row 8 names no post-fix mutant, a check written as a tautology — parse one site, compare the set to itself, or hardcode the four-key literal on both sides — is red at HEAD and green after the task, and no mutant the table names distinguishes it from a real one. The table's own preamble says a row whose mutant cannot make it red is not in the table; row 8 satisfies that only against the state the task removes.","found_by":1,"found_by_roles":["implementer"],"location":"§5","role":"implementer","severity":"medium","suggestion":"Give row 8 a mutant of the implemented check, e.g. 'one of the sites is built from its own literal list rather than from the shared source, so the emitted sets differ by one key while every site still lists evidence' — a mutation that is red only if the check actually compares two independently derived sets."}
{"category":"ambiguity","class":"floating-referent-in-acceptance","evidence":"Ran row 3's fixture at HEAD and got `findings 3`, `consecutive_clean 0`; the values are fully determined by the fixture, so the cross-binary comparison adds a dependency without adding discrimination. Read §5 rows 3 and 7 of spec/1.1.0.md (lines 128 and 132) — no other row references a second binary.","finding":"Rows 3 and 7 both assert 'the same `findings` count and `consecutive_clean` the previous binary reports'. 'The previous binary' names no tag, commit or build, so the comparison the SHALL rests on is not reproducible — and it obliges the test to build and run a second binary, which no other row in the table does and which the repository's test harness has no mechanism for.","found_by":1,"found_by_roles":["tester"],"location":"§5","role":"tester","severity":"medium","suggestion":"Replace the comparison with the literal values the fixture produces (e.g. 'exit 0, `findings: 3`, `consecutive_clean: 0`'), which is what the fixture determines anyway, or anchor the referent to a tag."}
{"category":"feasibility","class":"unrunnable-oracle","evidence":"Read spec/1.1.0.md lines 128 and 132. Compared against how §5's other rows state their SHALL — rows 1, 2, 5, 6 all name an exit code and an observable, none names a second binary. Confirmed the practical route by running the current binary in the clone: `tp review spec/1.1.0.md --record \u003clegal file\u003e` returns a JSON object carrying findings, clean, consecutive_clean and converged directly, so literal expectations are available and the second-binary phrasing buys nothing a test can use.","finding":"Rows 3 and 7 both state their SHALL as agreement with 'the previous binary' — 'the same findings count and consecutive_clean the previous binary reports for the same file' (row 3) and 'the same consecutive_clean the previous binary reports' (row 7). A Go test in internal/cli has exactly one build of the package under test; comparing against the pre-change binary would mean building an earlier revision from inside the test. The implementer will silently substitute literal expected values, which is a different and weaker assertion than the row states — and neither row's mutant detects the substitution.","found_by":1,"found_by_roles":["implementer"],"location":"§5","role":"implementer","severity":"medium","suggestion":"State the expected values directly (e.g. 'findings 3, consecutive_clean 0') or name the fixture's expected round JSON, so the assertion is one a test can make; if a real before/after comparison is wanted, say which artifact holds the previous values."}
{"category":"ambiguity","class":"undefined-comparison-referent","evidence":"Read spec/1.1.0.md lines 119-133 via codedbpro. The phrase `the previous binary` occurs in row 3 and row 7 and nowhere else in the file, and no §, tag, commit or testdata path is cited beside it in either row.","finding":"Rows 3 and 7 both state their expected value as `the same … the previous binary reports for the same file`. `The previous binary` appears nowhere else in the spec and is anchored to no tag, commit or checked-in artifact, so the expected value of two of the nine rows is not a value. §5's own preamble promises each row names its fixture and its mutant; these two name a fixture and a comparison nothing pins.","found_by":1,"found_by_roles":["architect"],"location":"§5","role":"architect","severity":"medium","suggestion":"Replace the referent with something a test can hold: name the tag (`v1.0.1`), or state the invariant directly — `findings equals the number of non-blank lines, and consecutive_clean is unchanged from the value the round before it recorded`."}
{"category":"consistency","class":"citation-locus-drift","evidence":"spec/.tp-review/1.1.0/ground-round-3.ndjson u4 is PARTIAL with the note `Under the table, false: the table names the expensive class only as the loop reviews itself`, naming the same repair. Line 8 of spec/1.1.0.md and line 8 of spec/.tp-review/1.1.0/snapshot-ground-round-3.md are identical, and `git log --oneline -- spec/1.1.0.md` shows eight commits after round 3's record (1ca54e25), none touching lines 8-11 — so the PARTIAL entered review unrepaired.","finding":"The Class paragraph (lines 8-11, immediately above §1) attributes to CLAUDE.md's *What a cycle costs* TABLE a definition the table does not carry. The table's only class labels are `the loop reviews itself` and `everything else`; `its signals, its convergence, its prompts` is in the prose above the table. The substance is right and only the locus is wrong, but a reader sent to the table for the definition does not find it there.","found_by":1,"found_by_roles":["architect"],"location":"§1","role":"architect","severity":"low","suggestion":"Change `table` to `section` in the first sentence and keep the second reference as `that table's loop reviews itself median of 23 rounds`, which is where the median actually is."}
{"category":"completeness","class":"type-rule-without-a-verdict","evidence":"Read `missingFindingFields` at internal/cli/review_merge.go:190-198 — `if s, _ := row[k].(string); s == \"\"` silently maps any non-string to missing — and §2 of spec/1.1.0.md lines 69-84, which names only `strings.TrimSpace` and the zero-width-space fence and never mentions the type.","finding":"§2's table gives `evidence` the type `string` and the rule 'non-empty', but no rule says what a present-and-wrongly-typed value does — `\"evidence\": 42` or `\"evidence\": {\"cmd\": \"...\"}`. The 'one predicate' paragraph is written entirely in terms of trimming a string, so it does not reach the case; an implementation that reports a distinct type error and one that folds it into 'missing' are both faithful readings.","found_by":1,"found_by_roles":["tester"],"location":"§2","role":"tester","severity":"low","suggestion":"Add one sentence to §2: a value that is not a JSON string is treated as missing at both gates (which is what `missingFindingFields`'s type assertion already yields), so the predicate has a verdict for every input."}
{"category":"completeness","class":"section-with-no-task","evidence":"Read spec/1.1.0.md lines 94-99 and confirmed the referenced text exists at skills/tp/SKILL.md:123 ('#### What a spec is about (v1.1.0)'), so the section describes committed state. Checked §5's nine rows (lines 126-133): rows 1-7 and 9 are about --record/--merge, row 8 about the emitted prompt; none names SKILL.md or §3.","finding":"§3 is a normative section that contains no implementable work. Its content is that the four skill rules are already committed and that this release 'distributes' them, which happens at the tag rather than in a task. No §5 row covers §3. A decomposer required to give every task source_sections has a section nothing can claim, and the alternative — inventing a task that re-edits SKILL.md — would rewrite text the section says is already correct.","found_by":1,"found_by_roles":["implementer"],"location":"§3","role":"implementer","severity":"low","suggestion":"Either fold §3 into §4 as a non-goal ('the skill rules ship as already committed; no task edits them'), or give it an acceptance row naming what a task must verify at the tag — e.g. that the SKILL.md section is present in the tagged tree and that the plugin manifest version matches."}
{"category":"completeness","class":"evidence-discarded-at-merge","evidence":"Ran `tp review --merge` over the 12 real per-role groups in the clone (spec/.tp-review/0.37.0, rounds 1-12, 4-5 role files each) after synthesising an `evidence` string on every row: 669 input rows -\u003e 630 merged representatives, so 39 evidence strings (5.8%) are discarded. Round 1's merged rows carry keys ['category','class','evidence','finding','found_by','found_by_roles','location','role','severity','suggestion'], confirming the representative is the original map and unknown keys pass through unchanged at HEAD.","finding":"Row 4 requires the representative to carry its own `evidence`, which leaves the non-representative rows' evidence paid for and thrown away. The merged row still asserts `found_by_roles` with N roles beside a single evidence string, so an agent reading the merge sees that four roles found a finding and what one of them ran. Measured the discard rate on this repository's real per-role groups it is small — 5.8% — so this is a sizing note rather than a blocking defect, but the spec asserts the representative's evidence without saying the others are dropped.","found_by":1,"found_by_roles":["ax-economist"],"location":"§5","role":"ax-economist","severity":"low","suggestion":"One sentence in §2 or §5 row 4 saying the cluster keeps the representative's evidence only, so nobody later reads `found_by_roles: 4` as four corroborating runs. No new key: `found_by` already carries the count."}
