{"category":"completeness","class":"compatibility-scope-omits-in-flight-round","evidence":"Ran in a `git clone --no-hardlinks` outside the repository: patched `missingFindingFields` to include `evidence` (and a TrimSpace `evidence` check in `parseRecordRows`), `go build`, then `tp review --merge old.ndjson -o out.ndjson` on a copy of spec/.tp-review/0.37.0/review-round-1.ndjson — exit 1, `no line parsed in old.ndjson`, `ls -l out.ndjson` = 0 bytes; HEAD binary on the same input = exit 0, 98,585 bytes. Then on the HEAD binary in a scratch dir: `tp review s2.md` + `tp review s2.md --record empty.ndjson` twice on a zero-byte file → `{clean: true, consecutive_clean: 2, converged: true}`, `--status --check` exit 0. Read spec/1.1.0.md:37-38, skills/tp/SKILL.md's step-3 `-o` bullet, skills/tp/REFERENCE.md:182.","finding":"Decision 3 states the release's whole compatibility position as \"Rounds recorded before this release load, clean and converge as they did … enforced when a round is recorded, never when one is loaded\". That covers rounds already recorded and rounds being read. It does not cover the case that actually breaks: a round IN FLIGHT at the upgrade — per-role findings files already written by reviewers under the pre-1.1.0 prompt, not yet merged. Those rows are neither recorded nor loaded, and §4 fences them nowhere. Measured: a real pre-1.1.0 per-role file (spec/.tp-review/0.37.0/review-round-1.ndjson, 65 rows) through a clone-built binary with `evidence` added to `missingFindingFields` — `--merge old.ndjson -o out.ndjson` skips all 65 rows, exits 1, and writes `out.ndjson` ZERO-BYTE; the same file through the HEAD binary exits 0 with 98,585 bytes. A zero-byte merged file is what `skills/tp/SKILL.md` documents as how a converged round reports itself, and tp's own shipped `review-record` unit command (REFERENCE.md:182) chains merge into record with `;`, so `--record` then runs on it. Verified on the HEAD binary against a throwaway spec: two `--record` calls on a zero-byte file give `clean: true`, `consecutive_clean: 2`, `converged: true`, and `--status --check` exit 0. The release's only answer to any of this is §5 row 10, a test row.","found_by":1,"found_by_roles":["architect"],"location":"§1","role":"architect","severity":"high","suggestion":"Take the decision in §1 beside decision 3: say what happens to a round whose per-role files predate the release — the operator re-runs the reviewers, or `--merge` distinguishes an input that predates `evidence` from one that is mis-emitted — or add it to §4 as a named, priced cost. Decision 3's sentence should say which of {recorded, in flight, being loaded} it covers rather than leaving the reader to infer that 'recorded before this release' includes rows that were never recorded."}
{"category":"completeness","class":"requirement-only-in-a-test-row","evidence":"Read spec/1.1.0.md lines 29-48 (§1's five decisions) and 77-92 (§2) in full through codedbpro: no sentence mentions `-o`, output files, or what a refused merge leaves on disk; the only occurrences of `-o` in the spec are in §5 row 10 (line 146) and §3's line 105-107. Read internal/cli/review_merge.go:100-119 — `finishMerge(output.JSON(summary), dropped)` runs after the `-o` write. Read skills/tp/SKILL.md:259-260 via faster_search. Same clone build as the §5 finding: run A left a 91-byte out.ndjson behind after exit 1, refuting SKILL.md:260 at HEAD.","finding":"The `-o` write-ordering change is a production behaviour change that no decision states, and decision 5 then tells the decomposer not to look where it IS stated. §1's five decisions cover `evidence` at `--record` (1), gate parity over the four keys (2), backward-readability (3), the skill rules (4) and sizing (5); §2 covers the field, the shared predicate and the pre-resolved carve-out. Read line by line, none of them says a refused `--merge` must leave no `-o` file — the requirement exists only in §5 row 10 and in §3's justification for a doc edit. Decision 5 then says the release \"is sized by the fixtures it breaks, not by §5's rows\" and names exactly two tasks, the fixture migration and the parity change; neither touches `runReviewMerge`'s write ordering, which is a different function from `missingFindingFields`/`parseRecordRows` and shares no fixture with them. A decomposer executing decision 5 as written produces a task set with no task for row 10, and §3's REFERENCE.md recipe work then has nothing to depend on. That this is a real change and not already-shipped behaviour is confirmed from the other side: `skills/tp/SKILL.md:260` already asserts \"`-o` writes only after the merge succeeds (the write is the last step), so a failed merge leaves nothing and `--record` exits 3 on the missing file\" — false at HEAD, as the run below shows.","found_by":1,"found_by_roles":["implementer"],"location":"§1","role":"implementer","severity":"high","suggestion":"Add the `-o` write ordering as a numbered decision in §1 (it is the same shape as decision 2: a gate that today reports failure while leaving a usable artifact behind), and extend decision 5's ordering sentence so the task list it prescribes names every production change the release makes — the migration, the parity change, the `-o` ordering, and the prompt key lists of §5 row 8 — rather than two of the four."}
{"category":"regression","class":"disposition-marks-open-findings-closed","evidence":"Parsed spec/.tp-review/1.1.0/review-round-2.ndjson: Counter({'fixed': 19, 'wontfix': 9}); all nine wontfix `resolved.evidence` strings start with 'Accepted open'. Ran `/tmp/tp-dev/tp review spec/1.1.0.md --role implementer` in a `git clone --no-hardlinks` at 7c465ed9 and read the emitted prompt: the block headed 'UNRESOLVED findings from previous rounds -- DO NOT re-report:' lists them as `[WONTFIX] \u003ccategory\u003e -- \u003clocation\u003e: \u003ctext\u003e (wontfix: Accepted open...)`, including round-2 index 5, the one high the repair itself reports as standing. `tp review spec/1.1.0.md --status` reports consecutive_clean 0, required_clean_rounds 2 -- round 2's clean flag was fixed at record time, before the dispositions, so the nine do not show there either.","finding":"§1's Context opens with 'A review finding today leaves a round in exactly two ways: the spec changes, or the finding is dispositioned wontfix/duplicate with evidence', and the whole release is derived from that premise. The corpus this release is being written against refutes it, in the round immediately before this one. Nine of round 2's 28 rows are dispositioned `wontfix`, and the evidence of every one of the nine begins 'Accepted open' — six say in so many words 'the finding stands for round 3'. Measured, that disposition has two effects the repair did not choose: it puts each of those findings into round 3's panel prompts under the heading 'UNRESOLVED findings from previous rounds -- DO NOT re-report', so the round told to act on them is told not to raise them; and `wontfix` is a resolution for convergence, so a round 3 and a round 4 with no blocking rows converge with nine findings their own record says are open. The release adds a mandatory key to the *finding* and leaves the *exit* vocabulary exactly as it was, so the field it introduces will be carried by rows that are, in the same pass, marked closed while open. §4 routes 'the audit side of the acceptance channel' to a backlog file and takes no position on the review side, which is this release.","found_by":0,"location":"§1","role":"regression","severity":"high","suggestion":"Either correct the Context sentence to say what the record shows -- a third exit is in use, `wontfix` standing in for 'accepted, not yet taken', and it both suppresses the finding and counts as resolved -- or add a Non-Goal saying this release adds no disposition and naming the file that will. Do not leave §1's premise asserting a two-exit model that this cycle's own round 2 breaks nine times, because that premise is the argument for the release."}
{"category":"consistency","class":"undeclared-merge-failure-contract","evidence":"Read internal/cli/merge_inputs.go:32-61 (`droppedInputs`, `finishMerge` and its doc comment) and internal/cli/review_merge.go:92-102 (the `-o` write at line 95, before `finishMerge` at line 101). Ran the HEAD binary on a two-file fixture whose first input's only row omits `location`: exit 1, and `out.ndjson` exists at 96 bytes holding the surviving row — row 10's mutant column reproduced. Read spec/1.1.0.md §1 decisions 1-5, §2's closing 'parity is over this set only', §3:105-107 and §4 for any statement of the change: none.","finding":"Row 10 (`tp review --merge` exits non-zero → leave no file at the `-o` path) reverses an existing tp rule that is documented WITH ITS REASON in the tree, and no decision section of this spec takes that decision. `finishMerge` (internal/cli/merge_inputs.go:42-47) reads: \"finishMerge applies §8a.4's exit rule after the merge has already emitted its payload: the surviving roles still merge and `-o` is still written, because the accounting an operator reads is in that payload — only the exit code changes, since an unattended driver reads nothing else.\" Row 10 requires the opposite of that sentence's second clause and removes the operator's read. §1's five decisions say nothing about `--merge`'s output on refusal; §2's only statement about `--merge` is the field-set parity, and it closes with \"The parity is over this set only\", which reads as a containment promise; §4 fences it nowhere. §3 then reasons FROM row 10 outwards — \"§5 row 10 changes what a refused `--merge` leaves on disk\" — so a Tests row is the release's normative source for a documentation scope decision. That is the inverse of decision 4's own account of what the reviewed document carries.","found_by":1,"found_by_roles":["architect"],"location":"§5","role":"architect","severity":"high","suggestion":"Promote it. Either add a sixth decision (or a paragraph in §2) stating the failure contract — a refused `--merge` leaves no `-o` file, and the payload's `inputs` accounting is therefore read from stdout/stderr rather than from the file — and say explicitly that this supersedes v0.35.0 §8a.4's stated rationale; or, if the release does not intend to change `--merge`'s failure contract, cut row 10 and let §3's REFERENCE.md scope rest on the parity change alone."}
{"category":"completeness","class":"shall-unconditional-fixture-conditional","evidence":"git clone --no-hardlinks of HEAD outside the repo, `go build -o tp ./cmd/tp`, then two runs of `tp review --merge bad.ndjson legal.ndjson -o out.ndjson` in a private temp dir (bad.ndjson: one row omitting `location`; legal.ndjson: one row legal on all four keys). Run A, out.ndjson absent beforehand: exit 1, `no line parsed in bad.ndjson`, out.ndjson present afterwards, 91 bytes. Run B, out.ndjson pre-seeded with `{\"severity\":\"low\",\"finding\":\"PRIOR MERGE RESULT\",\"location\":\"§9\",\"evidence\":\"prev\"}`: exit 1, out.ndjson present afterwards, `cmp -s out.ndjson prior-copy.ndjson` → NO, content replaced by the surviving row. Run B is the input §5 row 10's fixture does not build.","finding":"Row 10's SHALL is unconditional — `tp review --merge` exits non-zero, therefore \"leave no file at the `-o` path\" — but its fixture pins only the case where \"the `-o` path does not exist before the run\". The case the fixture excludes is the one the release's own consumer hits, and the two natural implementations disagree on it observably. Measured at HEAD in a clone: with `out.ndjson` pre-seeded with a previous merge's row, the refusing merge REPLACED it with the partial result (exit 1, file present, `cmp` against the seed: not identical). An implementer who reads row 10 as \"write only on success\" leaves the operator's pre-existing file untouched — which violates the SHALL as written, and hands REFERENCE.md:182's `[ -f $TP_ROUND_DIR/merged.ndjson ]` guard a STALE file to chain into `--record`, strictly worse than the truncated file row 10 exists to close. An implementer who reads it literally must unlink a path the run did not create, which no decision in §1–§4 authorizes. Nothing in the spec chooses, so the branch is invented at implementation time.","found_by":1,"found_by_roles":["implementer"],"location":"§5","role":"implementer","severity":"high","suggestion":"State the precondition inside the SHALL — e.g. \"leave no file the run itself created at the `-o` path; a file present before the run is not written and not removed\" — and add a second fixture whose `-o` path exists before the run, so whichever branch is chosen is red under the other. If the intended rule really is unlink-on-refusal, say so in §1 or §2, because deleting an operator-supplied file is a decision, not a detail."}
{"category":"ambiguity","class":"universal-shall-single-case-fixture","evidence":"In the sandbox against the unmodified HEAD binary: `echo stale-from-a-previous-run \u003e fx/pre.ndjson` then three runs, checking `cat fx/pre.ndjson` after each. Exit 1 (dropped input, fx/r10-a.ndjson missing `location` + a legal fx/r10-b.ndjson): file overwritten with the surviving row. Exit 3 (`--merge fx/nope.ndjson`): content unchanged, still `stale-from-a-previous-run`. Exit 2 (`--merge s.md fx/r10-b.ndjson`): content unchanged. Also built row 10's own fixture with an absent `-o` path and confirmed the mutant cell is accurate: HEAD writes the 84-byte file and then exits 1.","finding":"Row 10's WHEN is '`tp review --merge` exits non-zero' — every non-zero exit — while its fixture pins exactly one of them (the exit-1 dropped-input refusal) and adds 'the `-o` path does not exist before the run'. Measured at HEAD with `-o` pointing at a file that already exists: `--merge fx/nope.ndjson -o pre.ndjson` exits 3 and leaves `pre.ndjson`'s old contents untouched, and `--merge s.md b.ndjson -o pre.ndjson` exits 2 (spec-looking positional) and does the same. So the SHALL as written is already false for two of the three reachable non-zero exits, and the fixture cannot see either. Two engineers write contradictory tests from this row: one moves the `-o` write to after the dropped-input check (green on the fixture, still leaves a stale file on exits 2 and 3); the other reads the SHALL literally and makes `--merge` DELETE whatever sits at the `-o` path on every non-zero exit, including a usage error rejected before a single input file is opened. The second is destructive to a caller-owned file and nothing in §5, §2 or §4 rules it out.","found_by":1,"found_by_roles":["tester"],"location":"§5","role":"tester","severity":"high","suggestion":"Narrow the WHEN to the exit the row is about ('`tp review --merge` refuses because an input contributed no row') and say what happens to a path that already exists — either 'the `-o` path is left exactly as the run found it' or 'the refusal removes it' — so the two readings cannot both be built."}
{"category":"regression","class":"universal-shall-untestable-fixture","evidence":"Probe in /tmp (outside the repository) against /tmp/tp-dev/tp at 7c465ed9: `printf PREEXISTING \u003e out.ndjson; tp review --merge a.ndjson nosuch.ndjson -o out.ndjson` -\u003e `{\"error\":\"file not found: nosuch.ndjson\",\"code\":3}`, exit=3, and `ls -l out.ndjson` still shows 12 bytes containing PREEXISTING. Control run of the fixture's own case: `tp review --merge noloc.ndjson legal.ndjson -o o2.ndjson` exits 1 and writes the surviving row (140 bytes), confirming the mutant cell; and `tp review --merge noloc.ndjson -o o3.ndjson` exits 1 leaving a 0-byte file, a second shape the fixture does not reach. Read internal/cli/review_merge.go:93-101 (WriteFile then finishMerge) and :134-140. Round-2 index 17 read from spec/.tp-review/1.1.0/review-round-2.ndjson, resolved.status fixed by 86374665.","finding":"Row 10, added by this repair, is false as written on a reachable path, and its fixture cannot see that path — which is the class round-2 index 17 (`universal-shall-untestable-fixture`, medium) was dispositioned `fixed` for when the old row 10 was cut. The new row 10's WHEN is the universal `tp review --merge exits non-zero`; its SHALL is `leave no file at the -o path`. `--merge` also exits **3** on a missing or unreadable input, from `loadMergeFindings` (internal/cli/review_merge.go:136-138, 146-148, 172-179), before any write happens — so a file already at the `-o` path survives untouched. Built and run: with `out.ndjson` holding the text PREEXISTING, `tp review --merge a.ndjson nosuch.ndjson -o out.ndjson` exits 3 and `out.ndjson` is still 12 bytes of PREEXISTING. Under the row as written that is a violation, and the only implementation that satisfies it deletes a file the run never wrote, on a file-not-found error — a destructive behaviour no decision in §1 and no bullet in §4 authorises. The fixture reaches exit 1 with the `-o` path absent beforehand, so it is green against a build that does nothing at all on exit 3. This matters at exactly the sink §3 cites: the driver's `[ -f $TP_ROUND_DIR/merged.ndjson ] ||` guard sees the surviving file on the retry, which is the stale-file hazard row 10 exists to close.","found_by":0,"location":"§5","role":"regression","severity":"high","suggestion":"Narrow the WHEN to what the release decides and the fixture reaches — 'tp review --merge refuses because an input parsed no row' — and say in the same cell what happens to a file that was already at the `-o` path, since that is the case the driver's `[ -f ]` guard actually hits. If the universal is intended, add the exit-3 case to the fixture and take the decision to delete a pre-existing file, in §1 rather than in a test row."}
{"category":"regression","class":"fixture-lives-outside-the-graded-document","evidence":"Read spec/1.1.0.md:144 (row 8) and lines 3-6 (the header paragraph) via codedbpro; read the cited section of spec/1.1.0-measurements.md via codedbpro `read mode=section`. Read the round-1 and round-2 recorded rows in spec/.tp-review/1.1.0/review-round-{1,2}.ndjson and confirmed index 23's status is `fixed` with the quoted justification, and that index 18's disposition names the re-added dependencies.","finding":"Row 8 is the only row in §5 whose fixture is not in this file: it delegates the path set to *The review emission sites, derived* in `spec/1.1.0-measurements.md`. Line 5 of this spec says that file holds 'its measurements and the survey's sources' and that 'this file states the decision and stands without it'. Row 8's fixture is neither a measurement nor a survey source, and the spec does not stand without it — from the body alone no path set exists, so the row cannot be built. This defect was reported in round 1 (index 23) and is dispositioned `fixed` on the explicit ground that 'the body no longer depends on the three-site enumeration that lives only in spec/1.1.0-measurements.md, a file no round grades'. Round 2's repair `a9f720b3` put the dependency back; round 2 index 18's own disposition already recorded that the repair 'removed that dependency from row 8 while adding two more elsewhere'. It is now the last one standing, and it is in the row that most needs a buildable fixture.","found_by":1,"found_by_roles":["tester"],"location":"§5","role":"tester","severity":"high","suggestion":"State the path set in row 8's fixture cell — it is four call sites reached by five commands, which fits one cell — and keep the measurements file as the derivation's provenance rather than its only home; or, if the delegation is deliberate, amend line 5 so it stops claiming the spec stands without that file."}
{"category":"regression","class":"vacuous-shall-on-the-mutant-path","evidence":"Ran `/tmp/tp-dev/tp review spec/1.1.0.md --verify --findings \u003cone-row file\u003e` in a `git clone --no-hardlinks` at 7c465ed9 and parsed the emitted prompt: 0 lines whose trimmed text starts with '{'; the prompt's closing contract is 'Respond with one JSON finding per line (NDJSON)... Stamp EVERY finding with role: \"verifier\"' with no key list. Read spec/1.1.0.md:129-133 (preamble) and :144 (row 8), and spec/1.1.0-measurements.md:684 ('none -- the prompt carries no JSON object line at all') and :698-706 ('--verify is a mode, not a perspective'). Round-2 indices 6, 7 and 8 read from spec/.tp-review/1.1.0/review-round-2.ndjson, all severity high, all resolved.status fixed by a9f720b3.","finding":"Row 8's SHALL is vacuous on the only path its mutant names, so the row cannot go red and the vacuity three round-2 high findings (indices 6, 7, 8) were dispositioned `fixed` for is relocated rather than closed. The SHALL is `the key list in the prompt's output format — the JSON object line a reviewer copies — names evidence`. The mutant is `HEAD`'s `--verify`, `whose prompt carries no JSON object line at all`. Emitted and measured: the `--verify` prompt contains zero lines beginning with `{`, so there is no key list for the check to parse. A check that parses each listed path's object line and finds none for `--verify` may reasonably skip it — the SHALL says what a key list must name, not that a prompt must have one — and is then GREEN against the row's own mutant, which §5's preamble forbids ('A row whose mutant cannot make it red is not in this table'). The row's WHEN makes it worse rather than better: it scopes on 'a review role prompt is emitted', while the sidecar section the fixture cites argues in its own heading that `--verify` is a mode, not a perspective, and `review_verify.go` builds that prompt outside the role-prompt builder. So whether the row's only failing path is inside its own WHEN is a question the row does not settle. The three round-2 dispositions all claim 'the SHALL is therefore not vacuous there'; that claim rests on the mutant cell, and a mutant is what makes a row red, not what a task must build.","found_by":0,"location":"§5","role":"regression","severity":"high","suggestion":"State the missing half in the SHALL, not in the mutant cell: 'every listed emission path emits an output-format JSON object line, and that line names evidence' — a path with no object line then fails the row rather than passing it vacuously. If adding an object line to the `--verify` prompt is not this release's work, drop `--verify` from the fixture's list and take the decision in §4 that verifier rows are not review-record rows, which is the branch round-2 index 5 is still open on."}
{"category":"regression","class":"method-only-in-ungraded-sidecar","evidence":"Read spec/1.1.0.md:5-6 and :144 through codedbpro; `grep -n measurements spec/1.1.0.md` returns 5, 46, 58, 99, 144 and row 8 (line 144) is the only §5 cell citing the file. `git log --oneline 86374665^..HEAD` shows 9869ff84 immediately followed by a9f720b3. Round-1 index 23 and round-2 index 18 read from spec/.tp-review/1.1.0/review-round-{1,2}.ndjson, both resolved.status fixed, both class method-only-in-ungraded-sidecar. Confirmed the sidecar is outside what a round reads: `/tmp/tp-dev/tp lint spec/1.1.0.md` in the clone reports floor_size 51 / cut 23 over spec/1.1.0.md alone, and the emitted `--role implementer` prompt inlines the spec's own content while naming `1.1.0-measurements` exactly once, as the pointer in row 8's cell rather than as content. spec/1.1.0-measurements.md:659-696 is the cited section and is anchored to 2833f81e.","finding":"Row 8's enumeration is once again reachable only in `spec/1.1.0-measurements.md`, reversing two findings dispositioned `fixed` on exactly that rule — round-1 index 23 (`method-only-in-ungraded-sidecar`, whose `fixed` evidence reads 'the body no longer depends on the three-site enumeration that lives only in spec/1.1.0-measurements.md, a file no round grades') and round-2 index 18 (same slug, fixed by removing row 5's sidecar pointer). Row 8's fixture cell now reads `every path in the emission-site list under The review emission sites, derived in spec/1.1.0-measurements.md`. Line 6 of this spec still says 'this file states the decision and stands without it', and it no longer does: the set row 8 quantifies over cannot be recovered from the body at all, and the sidecar is cut from the graded floor, so no ground round can grade the list or record that it is wrong. The two edits are adjacent commits in opposite directions on the same rule — 9869ff84 'row 5 keeps its reason inline and drops the sidecar pointer', then a9f720b3 'row 8 binds its quantifier to a derived list'. Deriving beats restating is right, and it is not what this trade bought: the sidecar restates too. It publishes a hand-copied table of five commands, anchored at 2833f81e, whose own accounting does not close (see the sibling finding), so what row 8 now points at is a second hand-written list one indirection further from grading.","found_by":0,"location":"§5","role":"regression","severity":"high","suggestion":"Put the derivation command in row 8's fixture cell instead of the sidecar's prose table — `git grep -nE 'findingFormat|codeAuditOutputFormat|JSON finding per line' -- internal/cli/review*.go`, every call site it returns — which is a rule a decomposer can run from the body alone and which cannot go stale the way a copied table does. Keep the sidecar for the run and its output, which is what a sidecar is for."}
{"category":"feasibility","class":"fixture-demands-unspecified-surface","evidence":"Read spec/1.1.0-measurements.md section \"The review emission sites, derived\" via codedbpro read mode=section: five commands over four call sites, the `--verify` row reading \"none — the prompt carries no JSON object line at all\". Read internal/cli/review_verify.go:240-272: line 267 is `b.WriteString(\"Respond with one JSON finding per line (NDJSON). ...\")` with no key list anywhere in the builder, and line 268 stamps `role: \"verifier\"`. Read spec/1.1.0.md:29-48 and 109-125: neither §1 nor §4 mentions `--verify`.","finding":"Row 8's fixture is \"every path in the emission-site list under *The review emission sites, derived*\" in the measurements file, and that list's fifth entry is `tp review \u003cspec\u003e --verify`, whose prompt carries NO JSON object line at all — `review_verify.go:267-269` writes \"Respond with one JSON finding per line (NDJSON)\" and then names zero keys. Row 8's SHALL is about \"the key list in the prompt's output format — the JSON object line a reviewer copies\", and the row names `--verify` as its mutant precisely BECAUSE it has none. So the row can go green only if the implementation CREATES an output format for `--verify`: which keys, in which order, the five-key panel form or something else, and whether `role: \"verifier\"` (which the prompt sets separately on the next line) is part of it. No decision names that work — decision 1 promises only that \"the review role prompt tp emits names the key\", and `--verify` emits a verification prompt, not a role prompt; §3 scopes documentation and §4 fences audit and ground, so the new contract is invented by whoever picks up the task. It is not cosmetic: a verifier's rows reach `--merge`/`--record` like any other findings file, so once §2 lands, a verifier file whose rows lack `evidence` parses zero rows and trips the existing dropped-input exit 1 — the release breaks the verify pass unless that prompt gains a key list the spec never specifies.","found_by":1,"found_by_roles":["implementer"],"location":"§5","role":"implementer","severity":"high","suggestion":"Either bring `--verify` (and, on the same reasoning, `--perspective code-audit`) into §1 explicitly with the key list each prompt must emit — one sentence per site, since the two formats differ — or narrow row 8's fixture to the sites decision 1 actually covers and give the remaining sites their own row and their own decision. As it stands the fixture is broader than every decision behind it."}
{"category":"consistency","class":"derivation-not-derived","evidence":"Read spec/1.1.0.md row 8 (line 144) and spec/1.1.0-measurements.md lines 659-697 via codedbpro. Re-ran the derivation's three alternatives with codedbpro faster_search over internal/: findingFormat -\u003e review.go:802,1108,1110,1412 and review_regression.go:195 (5); codeAuditOutputFormat -\u003e review.go:1178,1764 (2); `per line (NDJSON)` -\u003e review.go:803,1667,1715,1765 and review_verify.go:267. Eight lines total in the three named files, of which review.go:1110 (b.WriteString(findingFormatRound2)) is neither a const nor in the sidecar's four-call-site list. Read internal/cli/review.go:1121-1152 (outputContractInstruction) and its three call sites from faster_search: review.go:1112, review_regression.go:196, audit_roles.go:279 — none matches any of the three grep alternatives. Read round-1 index 23's resolution text out of spec/.tp-review/1.1.0/review-round-1.ndjson with python3/json.","finding":"Row 8's fixture now delegates its enumeration to spec/1.1.0-measurements.md's `The review emission sites, derived`, and asserts the file `derives the list from the tree rather than restating it`. It does not. The derivation restates the file set and derives only the line set: the command is a git grep for three hand-named alternatives (findingFormat|codeAuditOutputFormat|JSON finding per line) over three hand-named paths (internal/cli/review.go, review_regression.go, review_verify.go). By the sidecar's OWN definition of the set — `call sites in the review command that write an output contract into an emitted prompt` — the list is already under-inclusive: outputContractInstruction (internal/cli/review.go:1128, whose doc comment reads `returns the §7.3 output-contract block for a phase`) writes the block headed `Output contract — stamp EVERY finding with the full contract:` plus a four-key list into every panel prompt at review.go:1112 and into the regression prompt at review_regression.go:196, and matches none of the three alternatives, so it appears neither in the eight matched lines nor in the five-command table. The same section's arithmetic is also short: `Eight lines, of which three are the const definitions and four are call sites` is 3+4=7, and the eighth (review.go:1110) is unaccounted for. This is not a request to widen row 8's SHALL back over that block — round-1 index 6 and 7 routed that out for §4 reasons and it should stay out. The cost is that round-1 index 23 was dispositioned `fixed` precisely by removing row 8's dependence on this ungraded file, and this round put the dependence back: the one row binding the release to the emitted prompts now rests on a list no round reads, bounded by a hand-picked file set — the exact mechanism the sidecar itself blames for `the same enumeration has now been wrong twice`. A third wrong enumeration costs a round nobody spends catching it.","found_by":1,"found_by_roles":["ax-economist"],"location":"§5","role":"ax-economist","severity":"high","suggestion":"Bound the claim in the row instead of asserting derivation: state the fixture as `the call sites matched by \u003cpattern\u003e in \u003cthe three named files\u003e`, so a reader of the graded document can see the enumeration is file-bounded and judge it — or make the fixture a rule the check computes at test time (every prompt-terminal contract const reachable from the review command) so the list cannot go stale unread. Either way, do not leave row 8's only statement of its fixture in a file the loop never grades."}
{"category":"ambiguity","class":"row-unsatisfiable-or-mutant-unkillable","evidence":"Emitted the prompt in a private sandbox with the HEAD binary: `tp review s.md --verify --findings fx/r3.ndjson`, parsed the JSON with python and counted lines whose strip() starts with `{` (0) and occurrences of the substring `evidence` (0) in the one `verifier` prompt. For contrast the default panel prompt carries one object line, `{\"severity\":\"critical|high|medium|low\",\"category\":...,\"location\":...,\"finding\":...,\"suggestion\":...}`. Read §1, §2, §3 and §4 of spec/1.1.0.md for any sentence authorising a change to the verify prompt's output format; there is none.","finding":"Row 8's fixture says 'the check parses each path's JSON object line', and its mutant is HEAD's `--verify`, described in the same cell as 'whose prompt carries no JSON object line at all'. I emitted that prompt: `tp review s.md --verify --findings \u003cfile\u003e` returns one prompt, role `verifier`, with zero lines beginning `{` and the string `evidence` occurring 0 times. The row therefore admits two implementations that cannot both be tested, and §5 does not say which. (a) A listed path with no object line is a failure: then the release must give the `--verify` prompt a JSON object line naming `evidence`, changing what a verifier is asked to produce — a behaviour change no decision in §1 states, §2 does not reach, §3 does not ship and §4 does not fence out. (b) A listed path with no object line is skipped: then the check is green for the exact path the mutant column names, and row 8 is unkillable there, leaving the row with no mutant at all — which §5's preamble says disqualifies a row from the table.","found_by":1,"found_by_roles":["tester"],"location":"§5","role":"tester","severity":"high","suggestion":"Say what the check does with a listed path that carries no JSON object line. If it fails, add the `--verify` output-format change to §1 or §2 so the implementing task has a decision to build from; if it is skipped, drop `--verify` from row 8's list and name a mutant that a shipped implementation can actually be mutated into."}
{"category":"completeness","class":"unpinned-discriminator","evidence":"git clone --no-hardlinks of the repo at 7c465ed9 into /tmp; implemented §1/§2's rule (evidence added to missingFindingFields with TrimSpace; parseRecordRows collecting every offender), then applied the mutant with codedbpro edit and rebuilt. Ran `tp review s.md --record fx/r1.ndjson` and `--record fx/r2.ndjson` in a private sandbox spec: exit 1 both times, `diff` of `.tp-review/s/state.json` against the pre-run copy reported no difference. Repeated row 1's fixture against a state carrying one round recorded by the HEAD binary: review_rounds 1 -\u003e 0, same exit 1, same message.","finding":"Rows 1 and 2 both SHALL 'leave `review_rounds` unchanged', but neither fixture cell names a prior round, and against an empty `review_rounds` that clause discriminates nothing. I built the clause's mutant — the refusal path in runReviewRecord assigns `stPre.ReviewRounds = []engine.ReviewRound{}` and saves before exiting — and ran both rows' fixtures exactly as their cells state them (a spec with no recorded round): both still exit 1, still write no round file, and `state.json` came back BYTE-IDENTICAL to the pre-run file, so both rows are fully green against a mutant that destroys the recorded history. The same binary on the same row-1 fixture, against a state holding one recorded round, takes `review_rounds` from 1 to 0. Row 3 shows the value is known to be load-bearing — it alone says 'recorded against a spec with no prior round' — and it is stated in the one row whose SHALL does not need it. This is the class §5's preamble was added to close ('any count a SHALL asserts is stated against that value'), now on its third instance in this table.","found_by":1,"found_by_roles":["tester"],"location":"§5","role":"tester","severity":"high","suggestion":"Add the prior-round count to rows 1 and 2's fixture cells — e.g. 'recorded against a spec with one round already recorded, so the unchanged-`review_rounds` assertion has a non-zero value to be unchanged from' — or delete the clause from both rows and let row 7 carry the record-time/load-time separation alone."}
{"category":"completeness","class":"mutant-survives-every-row","evidence":"In the clone, replaced parseRecordRows' `missingFindingFields(row)` call with a loop over `[]string{\"evidence\"}` only (codedbpro edit), rebuilt, and re-ran every fixture I had built for rows 1-11 in the sandbox, comparing exit codes and payloads against the same fixtures under the full four-key implementation. All eight record-side rows matched byte-for-byte; then `printf '{\"evidence\":\"ran something\"}' \u003e fx/x3.ndjson; tp review s.md --record fx/x3.ndjson` returned exit 0 with `\"findings\": 1` and advanced review_rounds to 1.","finding":"§2 says the four keys are 'one required set, and both gates enforce it', and §1 decision 2 notes that today `--record` requires none of the four. So for three of the four — `severity`, `location`, `finding` — this release creates a brand-new refusal at `--record`, and no row in §5 can see it. I built the mutant: `--record`'s check covers `evidence` only, leaving the other three unchecked there, `--merge` untouched. Every one of §5's eleven rows stays green — rows 1 (exit 1 naming lines 2 and 4), 2 (exit 1), 3 (exit 0, `findings: 3`, `consecutive_clean: 0`), 4 (merge recordable), 5 (merge exit 1 + warning, record exit 1), 6 (merge exit 0, record exit 1 on the fixed abort), 7 (status exit 0 / --check exit 1), 9 (merge exit 0 + warning, record exit 1), and rows 10 and 11 are merge-only and untouched. The same binary records `{\"evidence\":\"ran something\"}` — no `severity`, no `location`, no `finding` — at exit 0 as one finding that advances the round, which is the same shape as the `{}` defect row 2 exists to close.","found_by":1,"found_by_roles":["tester"],"location":"§5","role":"tester","severity":"high","suggestion":"Add a row whose fixture is a single row carrying a non-empty `evidence` and missing one of `severity`, `location` or `finding` (and a legal row beside it so the file still parses), SHALL `--record` exit 1 naming its line, mutant `--record`'s check covering `evidence` only — which is the shape the release's cheapest implementation actually takes, since `evidence` is the only key being added."}
{"category":"consistency","class":"documented-recipe-broken-by-the-change","evidence":"Read skills/tp/REFERENCE.md:682-690 and spec/1.1.0.md:37-38 and :101-107. Ran the `evidence`-in-`missingFindingFields` mutant built in a clone outside the repository against a copy of spec/.tp-review/0.37.0/review-round-1.ndjson (65 rows, a real recorded pre-1.1.0 round): exit 1, `no line parsed`, zero rows merged, no `overlap_report` emitted; the HEAD binary on the same file exits 0 and merges it.","finding":"Decision 3's \"Rounds recorded before this release load, clean and converge as they did\" is true of `--status` and `--record`, and false of the one documented procedure that feeds recorded rounds back through `--merge`. REFERENCE.md:682-690 (*Measuring whether a second model earns its cost*) is a three-step recipe whose steps are: keep each round's raw per-role NDJSON, relabel one run's `role`, then `tp review --merge \u003cround-a\u003e.ndjson \u003cround-b\u003e.ndjson` and read `overlap_report`. §3 already puts that section in scope, but only for one sentence inside it (\"`--merge` requires only `location`, `severity` and `finding`\"). The recipe's PROCEDURE breaks too: on any pre-1.1.0 pair every row is skipped, the merge exits 1 and produces no `overlap_report` at all — measured on a real recorded round file. So a reader repairing only the sentence §3 names ships a recipe that cannot be run on the corpus it was written for, and decision 3's compatibility claim is broader than the behaviour it describes.","found_by":1,"found_by_roles":["architect"],"location":"§1","role":"architect","severity":"medium","suggestion":"Either narrow decision 3 to the sinks it means (`--status`, `--record`, `--resolve`) and say `--merge` over pre-1.1.0 rows is a break the release accepts, or extend §3's REFERENCE.md scope from the one sentence to the whole recipe, which needs a caveat that it applies to rounds recorded at or after v1.1.0."}
{"category":"completeness","class":"behaviour-only-in-tests","evidence":"Read all 148 lines of spec/1.1.0.md via codedbpro; searched it for -o and for merge: §1 (lines 27-66) and §2 (68-92) contain neither the -o path nor any statement about files left on disk; the only occurrences outside §5 row 10 are §3 lines 105-107. Section byte shares measured with python3 over the round snapshots: r1 total 13,184 / §5 5,011 (38%); r2 15,735 / §5 6,247 (40%); current 15,387 with §1 4,262, §2 1,550, §5 6,583 (43%). Diffed spec/.tp-review/1.1.0/snapshot-round-2.md against snapshot-round-3.md to confirm row 10 is new this round and replaced the cut prompt-injection row.","finding":"The release has two behaviour changes and §1 states one. Row 10 requires that `tp review --merge` leave no file at the -o path whenever it exits non-zero — a change to a shipped gate whose WHEN is universally quantified over EVERY non-zero exit, reaching exits this release otherwise never touches (a missing input file, an over-long NDJSON line, a dropped input). No decision bullet in §1 and no rule in §2 mentions it; the only prose trace is a subordinate clause in §3 explaining why REFERENCE.md is in scope. That is expensive in exactly the way §1's own decision 5 cares about: decision 5 says `The release is sized by the fixtures it breaks`, so a decomposer sizing from §1 budgets the fixture migration and the two field gates and misses both the -o change and the REFERENCE.md brief_command edit it forces. It is also the cheap/expensive split the wrong way round — §1 plus §2 are 5,812 of the file's 15,387 bytes while §5 is 6,583 (43%), so the reader who takes the cheap half away learns one behaviour change where there are two, and every role pays the expensive half to find the other.","found_by":1,"found_by_roles":["ax-economist"],"location":"§1","role":"ax-economist","severity":"medium","suggestion":"Promote it to a decision bullet at requirement altitude — a refused `--merge` leaves no output at -o — with the reason already written in §3: the review-record unit command guards its merge with `[ -f $TP_ROUND_DIR/merged.ndjson ]`, so a leftover partial merge is what a RETRIED record unit records. Row 10 then tests a decision rather than carrying one, and §1 sizes the release it actually is."}
{"category":"consistency","class":"row-without-a-decision","evidence":"Read spec/1.1.0.md:8-11 (header class paragraph), :27-48 (all five decisions -- `grep -nE '^[0-9]+\\. \\*\\*'` returns exactly lines 29, 33, 37, 39, 44, none about `-o` or an output file), :80-82 ('The parity is over this set only'), :109-125 (six Non-Goals, none naming `--merge`'s output file), :105-107 (§3's dependency on row 10) and :146 (row 10). The diff against spec/.tp-review/1.1.0/snapshot-round-2.md shows row 10 arriving in 4b5aae1c and §3's paragraph in ab3430ec, with no decision added in either.","finding":"The repair moved a behaviour change out of §1 and into §5 alone, so the release's normative content is now partly in its test table. New row 10 requires `tp review --merge` to leave no file at the `-o` path when it exits non-zero. That is a change to `--merge`'s output contract, not to the required-field set: no decision in §1 states it, no bullet in §4 fences it, and §2's own sentence 'The parity is over this set only' explicitly scopes the section away from it. §3 then makes a documentation task depend on it -- 'Its review-record unit command is in scope for the same reason: §5 row 10 changes what a refused --merge leaves on disk' -- so a §3 obligation is derived from a row whose authority is nothing but the row. The release header compounds it: lines 8-11 justify the `loop` class and its 23-round budget by naming exactly two changes, '§2 changes what tp review --record accepts, and §5 row 8 changes the review role prompt tp emits'. Row 10 is a third, and it is the one that changes what an unattended driver finds on disk after a refusal. §1 decision 4's own standard applies here: the reviewed document carries the decisions, and a test row is not where a decision is taken.","found_by":0,"location":"§1","role":"regression","severity":"medium","suggestion":"Add one decision to §1 -- '§5 row 10: a refused `--merge` leaves nothing at the `-o` path, because the driver chains merge into record with `;` behind an `[ -f ]` guard' -- and add it to the header's list of what this release changes, so the class and the budget rest on the real count. That also gives row 10 something to be acceptance FOR, which is what §5 rows are."}
{"category":"regression","class":"undeclared-change-to-unchanged-keys","evidence":"Read spec/1.1.0.md:77-88 and :137-147 (all eleven rows) through codedbpro; no row's fixture cell places a whitespace-only value in `severity` or `finding`. Read internal/cli/review_merge.go:190-198 in a clone at 7c465ed9: `for _, k := range []string{\"location\", \"severity\", \"finding\"} { if s, _ := row[k].(string); s == \"\" {...} }` -- confirms the `== \"\"` test the mutant column of row 11 asserts. `git -c diff.external= diff --no-ext-diff --no-index snapshot-round-2.md spec/1.1.0.md` shows decision 2 losing exactly 'as it already does for the first three' (c951674d) and row 11 being added (4b5aae1c). Round-2 index 0's own resolved.evidence states the residual: 'the predicate names four keys while only evidence (row 9) and location (row 11) are pinned by a row; severity and finding are pinned by nothing.'","finding":"Round-2 index 0 (high) was dispositioned `fixed` for the release changing what `--merge` accepts on three keys it claimed to leave alone; the repair deleted the offending clause from decision 2 and pinned one of the three, and its own evidence records the residual. Two thirds of the finding survives, and §2 still carries the reading the deleted clause carried. §2:77-78 reads '`--merge` **already** skips a row missing `severity`, `finding` or `location` ... `evidence` **joins that list**', which reads as: the mechanism for those three is as shipped, and a fourth key is added to it. The next paragraph contradicts that -- 'A value that is empty once strings.TrimSpace has run counts as empty ... for severity, location and finding as much as for evidence' -- and `missingFindingFields` at HEAD tests `s == \"\"`, so the release does change what `--merge` accepts on all three. §5 now pins two of the four: row 9 pins `evidence` and new row 11 pins `location`. No row anywhere in the table puts a whitespace-only value in `severity` or in `finding`, so a build that applies TrimSpace to `evidence` and `location` and leaves the other two on `== \"\"` is green in all eleven rows, and the shipped predicate then contradicts the paragraph that defines it.","found_by":0,"location":"§2","role":"regression","severity":"medium","suggestion":"Two edits, both subtraction or near it. Drop 'already' and 'joins that list' from §2:77-78 so the sentence stops implying the first three are untouched -- 'all four keys are judged by the same predicate at `--merge`, which today judges three of them by `== \"\"`'. And widen row 11's fixture from one whitespace-only key to all four, one per row: the mutant it already names (`missingFindingFields` testing `x == \"\"`) makes every one of them red, so this costs a cell rather than a row."}
{"category":"completeness","class":"shared-predicate-half-unpinned","evidence":"Built the mutant in a `git clone --no-hardlinks` outside the repository (missingFindingFields: TrimSpace over {location, evidence}, `== \"\"` over {severity, finding}; parseRecordRows: TrimSpace check on top-level `evidence`), `go build`. Row 9 fixture → `merged_count 1`, `inputs [{parsed:1, skipped:1}]`, stderr `skipping incomplete line (missing evidence)`; row 11 fixture → same with `(missing location)`; `{\"severity\":\" \", …}` → `merged_count 2`, `skipped 0`, no stderr, exit 0, and `tp review s.md --record sev.ndjson` → recorded, `clean:false`, exit 0; `{\"finding\":\" \", …}` → identical. Read spec/1.1.0.md:84-88 and rows 9 and 11 at :145 and :147.","finding":"§2's *One predicate, called from both gates* paragraph asserts the TrimSpace rule for four keys — \"for `severity`, `location` and `finding` as much as for `evidence`\". §5 pins two of the four: row 9 (`evidence`) and row 11 (`location`). `severity` and `finding` are asserted by §2 and pinned by nothing, and §5's own preamble says a row whose mutant cannot make it red is not in the table, i.e. that every asserted rule has a red-making row. I built the discriminating mutant and ran it: a binary whose `missingFindingFields` trims only `location` and `evidence` and keeps `s == \"\"` for `severity` and `finding`, with the record-side `evidence` check added, satisfies rows 9 and 11 exactly as written and is green across all eleven rows — while a row carrying `\"severity\": \" \"` and a row carrying `\"finding\": \" \"` are each PARSED by `--merge` (2/0, no warning, exit 0) and RECORDED by `--record` at exit 0 as findings. So the release can ship with half of §2's stated predicate absent and every acceptance row green. (This is the half round 2's `location` repair did not reach, not that repair restated: row 11 exists and is red against its own mutant.)","found_by":1,"found_by_roles":["architect"],"location":"§2","role":"architect","severity":"medium","suggestion":"Either add one row whose fixture's only defect is a whitespace-only `severity` (and a second value for `finding`, or one fixture carrying both), or narrow §2's paragraph to the keys §5 pins and say the other two keep `== \"\"` at `--merge`. Rows 9 and 11 are already near-identical in shape, so a single row parameterised over the four keys would collapse three rows into one and close the gap."}
{"category":"feasibility","class":"doc-task-depends-on-unnamed-code","evidence":"Read spec/1.1.0.md:94-107 in full. Read skills/tp/REFERENCE.md:179-187 via faster_search — line 182's command is `[ -f $TP_ROUND_DIR/merged.ndjson ] || tp review --merge $TP_ROUND_DIR/role-*.ndjson -o $TP_ROUND_DIR/merged.ndjson; tp review \u003cspec\u003e --record $TP_ROUND_DIR/merged.ndjson`. Read skills/tp/SKILL.md:259-260, which states the opposite rule (`\u0026\u0026`, never `;`) as already-shipped behaviour.","finding":"§3 bundles into one task three pieces of work with different readiness. The SKILL.md half is zero edits — §3 itself says the four rules \"are already in the tree ... landed by commit `4914215f`\", so distribution happens at the tag and the task changes no bytes there. REFERENCE.md's two superseded-fact sites can be edited the moment §2 is decided; they depend on no code. But the `review-record` unit command at REFERENCE.md:182 can only be rewritten truthfully AFTER row 10's `-o` change lands — before it, any sentence the doc task writes about what the chained `;` walks into is false. So \"in scope for the same task\" produces one task carrying a `depends_on` edge to an implementation task the spec never names (see the §1 finding), and a decomposer has no id to point that edge at. Decomposed as written it is a single task that cannot start until unnamed work finishes, which is the shape decision 5 was added to prevent for the fixtures.","found_by":1,"found_by_roles":["implementer"],"location":"§3","role":"implementer","severity":"medium","suggestion":"Split §3's scope explicitly into the unblocked half (SKILL.md distribution plus REFERENCE.md's two superseded statements) and the blocked half (the `review-record` unit command), and name the implementation task the second depends on — so `source_sections` and `depends_on` can both be written at decomposition instead of discovered when the doc task opens the file."}
{"category":"completeness","class":"agent-facing-command-undecided","evidence":"Read skills/tp/REFERENCE.md:176-198 via codedbpro faster_search on review-record: line 182 carries the guarded chain verbatim. Read spec/1.1.0.md §3 (lines 94-107): the REFERENCE.md sentences are named by their section, the unit command is not. Confirmed the missing-file behaviour is a file error rather than a merge diagnosis: internal/cli/review.go:557-558 and the sibling guards in review_resolve.go:78-82 / review_verify.go:30-31 all exit ExitFile with `findings file not found`, and internal/cli/review_test.go:377-379 asserts exit code 3 for it. §3's own sentence (lines 105-107) already states the causal link to row 10, so the decision is one clause away.","finding":"§3 names the exact superseded sentences for its other two items (the *Measuring whether a second model earns its cost* recipe and *Finding class and Report*) but for the third it says only that the review-record unit command `is in scope`, deciding nothing. That command is not prose a human reads: it is a brief_command a `tp run` child EXECUTES (skills/tp/REFERENCE.md:182), and it reads `[ -f $TP_ROUND_DIR/merged.ndjson ] || tp review --merge $TP_ROUND_DIR/role-*.ndjson -o $TP_ROUND_DIR/merged.ndjson; tp review \u003cspec\u003e --record $TP_ROUND_DIR/merged.ndjson`. Under row 10 a refused merge leaves nothing, so the `;` still runs --record against a path that does not exist and the unit dies with tp's file error on a missing findings file rather than with the merge's own stderr warning naming the offending input. The diagnosis the operator needs is produced and then discarded, costing one round-trip to recover per occurrence — under an unattended run, one stopped run. Whether the `;` becomes `\u0026\u0026` is a decision about an agent-facing artifact tp owns (P4), not the mechanism decision 4 assigns to the implementing task.","found_by":1,"found_by_roles":["ax-economist"],"location":"§3","role":"ax-economist","severity":"medium","suggestion":"One clause in §3 saying what the command becomes — `\u0026\u0026`, so a refused merge stops the chain and the merge's diagnosis is the unit's last output — or a Non-Goal in §4 stating that the chain operator is unchanged and why a missing-file error is an acceptable substitute for the merge's warning."}
{"category":"consistency","class":"skill-and-reference-disagree-on-the-changed-behaviour","evidence":"Read skills/tp/SKILL.md's Workflow Step 2 item 3 sub-bullet (found by literal search for 'Always `-o`, never `\u003e`') and skills/tp/REFERENCE.md:182 and :189-192. Ran the HEAD binary: `tp review --merge a.ndjson b.ndjson -o out.ndjson` with a.ndjson's only row missing `location` → exit 1, stderr `no line parsed in a.ndjson`, `ls -l out.ndjson` = 96 bytes containing the surviving row. Read internal/cli/review_merge.go:92-102 for the write/exit order.","finding":"§3's criterion is that the tag must not ship \"a reference that contradicts the release\", and it names two REFERENCE.md statements plus the `review-record` unit command. It misses the document that already states row 10's behaviour as present fact and is measurably wrong today. `skills/tp/SKILL.md`'s Step 2 bullet reads: \"**Always `-o`, never `\u003e`, and `\u0026\u0026` rather than `;` between merge and record.** … `-o` writes only after the merge succeeds (the write is the last step), so a failed merge leaves nothing and `--record` exits 3 on the missing file.\" At HEAD the write is NOT the last step (review_merge.go:95 precedes finishMerge at :101), and a failed merge leaves the file — measured, 96 bytes after exit 1. So SKILL.md documents behaviour tp does not have, and row 10 is what would make it true. Worse, the same bullet's rule (`\u0026\u0026`, never `;`) is broken by REFERENCE.md:182's shipped `review-record` brief command, which uses `;` — the two documents this release ships together already disagree, and §3 puts the command in scope for a different reason without naming the rule it violates or saying what the command becomes. (Distinct from the two open §3 findings, which are about REFERENCE.md:685/722 and about §3 carrying no §5 row.)","found_by":1,"found_by_roles":["architect"],"location":"§3","role":"architect","severity":"medium","suggestion":"Name the third site in §3: SKILL.md's `-o`/`\u0026\u0026` bullet is a claim row 10 makes true, so it should be listed as text the release repairs-by-landing rather than left as an accidental truth. And say what REFERENCE.md:182's command becomes — if `\u0026\u0026` is the rule, the command should use it; if `;` is deliberate because `--record` exiting 3 on the missing file is the wanted signal, say so, because that reasoning only holds once row 10 has landed."}
{"category":"ambiguity","class":"shall-quantifies-over-unenumerated-exit-paths","evidence":"Enumerated the exit paths by reading internal/cli/review_merge.go (a regex for `os.Exit|ExitFile|ExitUsage|ExitValidation` over that file returns lines 17,18,27,31,96,97,137,138,147,148,177,178) plus `finishMerge` at internal/cli/merge_inputs.go:55-59; the `-o` write is line 95, i.e. after every load-time exit and before the `finishMerge` exit. Read spec/1.1.0.md:146.","finding":"Row 10's WHEN is `tp review --merge` exits non-zero — a universal quantifier over every non-zero exit of the command, while its fixture pins exactly one. `--merge` has four non-zero exit paths: ExitUsage at review_merge.go:17-18 (no input files) and :27-31 (a spec-looking positional); ExitFile at :137-138, :147-148 and :177-178 (a missing, unopenable or unreadable input); ExitFile at :95-98 (the `-o` write itself fails); and ExitValidation via `finishMerge` (a dropped input). The first three occur before any write, so the SHALL is vacuously true there and no fixture can make it red. The fourth IS the write failing, where a partial file can remain — so on that path the SHALL is unachievable rather than merely untested. Only the fifth is the case the fixture builds. This is the same shape the round-3 repair was run to remove (a quantifier over a set nobody enumerated), reintroduced by one of the repair's two additions.","found_by":1,"found_by_roles":["architect"],"location":"§5","role":"architect","severity":"medium","suggestion":"Bind the WHEN to the refusal it means: \"`tp review --merge` refuses because an input parsed no row\". If the wider rule is intended, enumerate the paths and say what happens when the write itself is what failed."}
{"category":"consistency","class":"derivation-restated-not-derived","evidence":"Ran the section's command verbatim in the clone at HEAD: `git grep -nE 'findingFormat|codeAuditOutputFormat|JSON finding per line' -- internal/cli/review.go internal/cli/review_regression.go internal/cli/review_verify.go` -\u003e 8 lines, listed above. Re-ran it as `git grep -nE ... 2833f81e -- ...` and `git grep -cE ... 2833f81e` -\u003e review.go:6, review_regression.go:1, review_verify.go:1, the same 8, so the count did not drift after the section was written. Read `findingFormatRound2`'s body at review.go:1412-1413 and its call site at review.go:1109-1111 via sed/codedbpro.","finding":"Row 8's fixture credits the cited list with deriving 'the list from the tree rather than restating it'. It restates it, and the restatement does not close. I ran the section's own command at HEAD (7c465ed9) and at the `2833f81e` it names; both return the same eight lines — six in `review.go` (802, 1108, 1110, 1178, 1412, 1764) plus `review_regression.go:195` and `review_verify.go:267`. The section partitions those eight as 'three are the `const` definitions (802, 1412, 1764) and four are call sites (1108, 1178, regression:195, verify:267)': 3 + 4 = 7. `review.go:1110`, `b.WriteString(findingFormatRound2)`, is a call site, is in neither group, and appears in no row of the table — while `docOutputFormat` and `testOutputFormat`, which the command does not return at all, get an explicit exclusion paragraph. A test author who re-runs the command gets eight lines and no rule for classifying them; one who follows the prose gets a set whose own count contradicts it. This sits in the section whose first paragraph says the enumeration 'has now been wrong twice'.","found_by":1,"found_by_roles":["tester"],"location":"§5","role":"tester","severity":"medium","suggestion":"State the classification rule the partition uses (a call site that writes a finding output contract, which `findingFormatRound2` — a one-line 'only report NEW issues' reminder — is not), and account for `review.go:1110` in the exclusion paragraph beside `docOutputFormat` and `testOutputFormat`, so the eight lines the command returns and the four the prose keeps are reconciled in the text rather than by the reader."}
{"category":"completeness","class":"undecided-output-contract","evidence":"python3 over both globs (spec/.tp-review/*/*.ndjson and spec/backlog/.tp-review/*/*.ndjson), counting rule: canonical review-round-N.ndjson basenames only, one JSON object per content line — 176 files, 4,030 rows, rows with a top-level role field equal to the string verifier: 0; role distribution tester 1030, implementer 859, architect 842, regression 523, ax-economist 475, absent 300, medium 1. Read internal/cli/review_verify.go in full: buildVerifyPrompt ends at line 267-269 with `Respond with one JSON finding per line (NDJSON)` and the role stamp, and writes no key list. Read skills/tp/SKILL.md:849 via faster_search. Read spec/1.1.0-measurements.md:678-696, whose own exclusion criterion for the documentation and testing perspectives is that `they never reach --record as findings`.","finding":"Row 8's list now includes `tp review \u003cspec\u003e --verify`, whose format the cited sidecar table records as `none — the prompt carries no JSON object line at all`. Satisfying row 8 there is therefore not adding a key: it is AUTHORING a whole output contract for that path — every key of it, not only evidence — and no section of this spec decides those keys. An output contract is the agent-facing surface tp owns (P4), not the mechanism decision 4 hands to the task, so the release would ship a new JSON contract that no round reviewed. I am not asking to reverse a9f720b3's inclusion of the path; I am asking what including it costs, which nothing prices. Measured, the benefit side is thin: across BOTH round globs, in the 176 canonical review-round-N.ndjson files holding 4,030 rows, rows carrying role verifier number ZERO — while the --verify prompt itself instructs `Stamp EVERY finding with role: \"verifier\"` (internal/cli/review_verify.go:268), so an unattributed row is not a hidden verifier row. No recorded row in this repository has ever come from that path. And the documented use of the path routes around findings entirely: skills/tp/SKILL.md:849's Tail protocol turns a verifier's judgement into a wontfix disposition WRITTEN INTO the findings file before --record, a channel that already carries its own evidence through `tp review --resolve \u003cfile\u003e \u003cidx\u003e wontfix \"evidence\"`. So the release invents a contract for the one listed path whose rows have never reached the gate, to carry evidence in a place where evidence is already required.","found_by":1,"found_by_roles":["ax-economist"],"location":"§5","role":"ax-economist","severity":"medium","suggestion":"Decide it in the spec rather than in the task, either way: exclude --verify from row 8 with one line saying its rows reach --record only through a disposition that already carries evidence (the same shape of exclusion the sidecar already applies to the documentation and testing perspectives), or keep it and name in §2 the keys the --verify format gains — the four required ones — so what ships is a contract the release reviewed."}
{"category":"regression","class":"unenumerated-quantifier","evidence":"Ran the sidecar's own command verbatim in a `git clone --no-hardlinks` at 7c465ed9: `git grep -nE 'findingFormat|codeAuditOutputFormat|JSON finding per line' -- 'internal/cli/review.go' 'internal/cli/review_regression.go' 'internal/cli/review_verify.go'` -\u003e 8 lines: review.go:802 (const), 1108 (call), **1110 (call, findingFormatRound2)**, 1178 (call), 1412 (const), 1764 (const), review_regression.go:195 (call), review_verify.go:267 (call). `| wc -l` = 8. Read internal/cli/review.go:1104-1112 (the call is guarded by `if summary != \"\"`, so it fires on every round after the first) and :1412-1413 (the const is 'Remember: only report NEW issues...', no keys). Read spec/1.1.0-measurements.md:665-696.","finding":"The derivation row 8's fixture cites does not account for its own output, which is the class -- a quantifier over a set the author did not enumerate -- that row 8 was rewritten twice to escape. spec/1.1.0-measurements.md:673-675 reads 'Eight lines, of which three are the const definitions (review.go:802, review.go:1412, review.go:1764) and **four are call sites**: review.go:1108, review.go:1178, review_regression.go:195, review_verify.go:267.' Three plus four is seven. Re-run at HEAD the grep returns eight lines, and the eighth is `review.go:1110: b.WriteString(findingFormatRound2)` -- a fifth call site, silently absent from both categories and from the five-row table. It happens to be harmless (findingFormatRound2 is a one-line reminder naming no keys, review.go:1412-1413), but nothing in the sidecar says so, while the two `--perspective` prompts that ARE excluded get an explicit reason at :693-696. A reader re-deriving the list -- which is the entire point of citing a derivation rather than a list -- gets eight lines, can reconstruct seven from the prose, and has no way to tell whether the eighth was examined and dismissed or missed. The same enumeration has now been wrong at round 1 (three paths, a fourth found) and round 2 (a fifth found); this is the third statement of it and it still does not close.","found_by":0,"location":"§5","role":"regression","severity":"medium","suggestion":"Account for all eight lines: name review.go:1110 as a call site of `findingFormatRound2`, and state the one-line reason it carries no key set, the way :693-696 does for docOutputFormat and testOutputFormat. Then the arithmetic in the sentence closes and the table's completeness is checkable from the command's own output."}
{"category":"regression","class":"refuted-measurement-in-cited-derivation","evidence":"Four runs of /tmp/tp-dev/tp at 7c465ed9 in a `git clone --no-hardlinks`, counting the substring `evidence` in the emitted prompt: `review spec/1.1.0.md --verify --findings \u003cone-row file with no such word\u003e` -\u003e 1 (the single occurrence is the injected outline line '- 2. The evidence field on a review finding'); `review spec/1.1.0.md --perspective code-audit --affected-files internal/cli/review_merge.go` -\u003e 1, same source; `review spec/1.1.0.md --role implementer` -\u003e 24; `review spec/1.1.0.md --perspective regression` -\u003e 91. Control against the round-2 text: copied spec/.tp-review/1.1.0/snapshot-round-2.md to a scratch dir as r2.md and ran the same code-audit command -\u003e 1, so the figure did not rot, it was never 0. Read spec/1.1.0-measurements.md:686-691 and round-2 index 19 in spec/.tp-review/1.1.0/review-round-2.ndjson (resolved.status fixed by a9f720b3).","finding":"The sidecar section row 8's fixture now cites states a measurement that does not reproduce, and round-2 index 19's `fixed` rests on it. spec/1.1.0-measurements.md:687-691 reads: 'Measured on the emitted prompt, the string evidence occurs **0** times in the --verify prompt and **0** times in the code-audit prompt, and once in each panel prompt -- which is why the row's ban on a whole-prompt substring search stays and the justification that used to accompany it was deleted: it asserted the word occurs in *the* prompts generally, and it does not occur in two of the five.' Emitted and counted at HEAD: `--verify` 1, `--perspective code-audit` 1, `--role implementer` 24, `--perspective regression` 91. The source in the two paths said to be zero is this spec's own section heading, '2. The evidence field on a review finding', which every prompt injects as an outline. Reproduced against the round-2 snapshot text as well, so the number was already wrong when round-2 index 19 was raised and when it was dispositioned. Two consequences. The deleted justification was substantively right -- a whole-prompt substring search for `evidence` is green on every one of the five paths with nothing built, because the spec's own heading supplies the word -- so the ban now stands with its only reason removed and will be re-litigated. And the artifact row 8 binds its exhaustiveness quantifier to publishes a figure a reader who re-runs the command gets a different answer to, which is precisely what binding to a derivation was supposed to prevent.","found_by":0,"location":"§5","role":"regression","severity":"medium","suggestion":"Correct the sidecar's counts to what the command returns and restore the justification in the form the measurement supports: a whole-prompt substring check is green on all five paths because the spec's own §2 heading is injected into every prompt, which is why the check must parse the output-format object line. Anchor the counts to a commit, or state the command and let the reader run it, rather than publishing three bare numbers."}
{"category":"completeness","class":"partial-predicate-coverage","evidence":"Read internal/cli/review_merge.go:164-198 via codedbpro faster_search on missingFindingFields: the loop is `for _, k := range []string{\"location\", \"severity\", \"finding\"}` with `if s, _ := row[k].(string); s == \"\"`. Read spec/1.1.0.md §2 lines 84-88 (the one-predicate paragraph) and §5 rows 9 and 11 (lines 145-147). Counting rule for the corpus: every content line of every *.ndjson under both globs that parses as a JSON object; a field counts as whitespace-only when it is a string, is not equal to the empty string, and strips to the empty string. python3 result: 427 files, 21,936 rows, whitespace-only counts {} (empty) for all four keys; restricted to the 176 canonical review-round-N.ndjson files, 4,030 rows, absent counts {evidence: 3969} only — severity, location and finding are present and non-blank on every row. Section share measured on the current file: §5 6,583 of 15,387 bytes.","finding":"§2's one-predicate paragraph extends TrimSpace emptiness to severity, location and finding at --merge, and §5 buys a row for exactly one of the three. internal/cli/review_merge.go:190-197 shows why that is arbitrary: missingFindingFields loops over the literal slice {location, severity, finding} applying one `s == \"\"` test, so the divergence row 11 closes is byte-for-byte identical on all three fields. An implementation that trims only location is GREEN under row 11 while severity and finding still diverge from --record's TrimSpace — the two gates the release exists to make agree stay disagreeing on two of four keys, and no row in the table is red. §5's preamble bans a row whose mutant cannot make it red but says nothing about a mutant no row can catch, which is exactly what a two-field partial fix is. The economical repair is not two more rows: measured, across both globs (427 round files, 21,936 rows) the count of rows carrying a present-but-whitespace-only value for severity, location, finding or evidence is ZERO, and in the 176 canonical review round files all 4,030 rows carry non-blank severity, location and finding. The case has never occurred here, so it does not deserve to grow the section that is already 43% of the file.","found_by":1,"found_by_roles":["ax-economist"],"location":"§5","role":"ax-economist","severity":"medium","suggestion":"Widen row 11's fixture instead of adding rows: one row carrying `\" \"` in EACH of severity, location and finding (the file's second row staying legal on all four keys, as now), with the SHALL stating the stderr warning names all three. One row, three fields, no growth — and a trim applied to one field alone goes red."}
