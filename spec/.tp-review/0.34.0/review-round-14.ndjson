{"category":"completeness","class":"guard-region-boundary-escape","finding":"The guard-test list region is bounded at the next `## ` heading, but `spec/0.34.0-guard-tests.md` states the rule as \"above the next heading\" and \"the entries end where the following heading starts\" — no heading level named. Reproduced against the current tree: a written list of 90 entries under `## Guard tests` followed by `### Excluded from the sweep` carrying the remaining 10 exits 0 (probe J), and the same file with `# Appendix` instead of `###` also exits 0 (probe K), while the `## Excluded` spelling round 13 repaired fails as intended (probe L, exit 1, 10 derived-but-not-listed). So §7.1's acceptance — run `scripts/check-measured-claims.py` to exit 0 with no PENDING — is satisfiable by a list whose operative extent, under the document's own stated region rule, is short of the derivation by any number of entries, on an artifact produced after review closes and read by no reviewer. The sweep is capped at that list, so the dropped entries leave the sweep silently. This is the same escape as round 13 with the heading spelled one level deeper, and a sub-heading inside the section is a natural way to write triage ordering, which §7.1 assigns to that same task.","found_by":1,"found_by_roles":["tester"],"location":"§7.1","resolved":{"evidence":"The region now ends at any heading level, matching the document's own wording, and the check is no longer a registered workflow.checks entry -- it runs where the artifact does not exist. Mutation-tested: entries under a following heading at any level now fail, a correct list passes.","resolved_at":"2026-08-12T15:19:10Z","status":"fixed"},"role":"tester","severity":"high","suggestion":"Per the standing commitment, this is the eighth break: remove the mechanical registration of `measured-claim-not-reproducible` and return the class to reviewers, rather than repairing the boundary an eighth time. If instead it is repaired, the two sides must be made to say the same thing — bound the region at any ATX heading (`^#{1,6} `) and state exactly that in the guard document, or state `## ` literally in the document and have the check reject any deeper heading inside the region rather than reading through it."}
{"category":"consistency","class":"fixed-repair-half-applied","finding":"Round 13 idx 4 (retired-guard-residue, low) was recorded fixed with the evidence \"The guard document no longer advertises the repository test-function count ... Its coverage paragraph now states what the check actually guards.\" That finding asked for two edits and its suggestion named both: the guard document's coverage paragraph, and docstring property 5 in `scripts/check-measured-claims.py` (\"delete or reword docstring property 5 ... now that no `git ls-files` walk remains\"). Verified against the shipped script rather than the evidence line: only the first landed. `scripts/check-measured-claims.py`:22-24 still reads \"5. The tree is read through `git ls-files`. Walking the working tree let an untracked scratch file fail the check ...\", and grepping the tree, that line is the sole surviving occurrence of `ls-files` anywhere in `scripts/` — `REPO_CLAIM`, `tracked_test_functions` and `check_repo_claim` were all deleted in 589729d, so the file performs no tree read at all. The docstring's own stated purpose is that \"the escapes it found are named here so the next author does not reopen them\", so a numbered property describing a defence the file no longer has is the one part of that list a next author cannot act on. Harm is contained — the reviewer-facing COVERAGE string and the guard document now agree and are accurate, nothing fails, and the first task never reads the docstring — which is why this is low and not medium. Its value is as the residue of a repair the resolution log records as complete. Separately verified this round, by mutation in a clone of 278b540 with the true 100-entry derivation: round 13's region bound disabled no property rounds 7-13 built. Twenty mutations, all as intended — correct list + declaration deleted exits 0 with no PENDING; 99 entries, a fabricated entry, duplicate entries, entries alongside the placeholder, an empty list with the declaration deleted, a missing `## Guard tests` heading, 80 entries hidden above the heading (round 12's escape), 10 entries under a following `## Excluded from the sweep` heading (round 13's escape), a doc window disagreeing with §7.1, a dropped `--no-ext-diff`, a reworded match rule, a stale count, a count reworded past its pattern, a deleted document, a spec window reworded past its pattern, a window moved in both files with a stale list, and an empty derivation all exit 1; and a correct list followed by an unrelated trailing `## Notes` section still exits 0, so the new bound does not fail a legitimate document.","found_by":0,"location":"§7.1","resolved":{"evidence":"The stale docstring property is replaced by the one that is now true: the check is deliberately unregistered.","resolved_at":"2026-08-12T15:19:10Z","status":"fixed"},"role":"regression","severity":"low","suggestion":"Delete docstring property 5 from `scripts/check-measured-claims.py` (lines 22-24), or reword it to record the lesson without claiming a derivation the file no longer performs — e.g. fold it into the note explaining why the repository test-function count was retired, which is the failure that lesson now belongs to."}
