{"category":"regression","class":"recorded-fix-not-applied","finding":"Four round-5 findings on the `--perspective` clause are recorded as fixed with the note \"The `--perspective` clause is dropped with the rest of the mode enumeration rework\", but clause 1 still reads \"prompt-emission mode, including `tp review --perspective`\" verbatim (§2.5 line 62). Every defect those findings named survives unchanged: the four perspective runs return at internal/cli/review.go:320-322 and :328-335, before loadReviewRoundState/EnsureReviewState (:358) and before buildReviewPrompts — the only site that resolves the corpus — so clause 2's placement rule (\"ahead of EnsureReviewState\") is unreachable on every perspective path, and an implementation satisfying clause 2 cannot satisfy clause 1; and because §2.5's own second sentence says `regression` is not an active reviewer, a spec that deactivates every corpus reviewer (the exact fixture §6 item 9 mandates) has `tp review --perspective regression`, the uncounted delta pass, killed with exit 2. The round-6 rewrite of §6 item 8 makes this strictly worse: item 8 now covers only \"modes that take the spec as the positional\", so no §6 item exercises any `--perspective` invocation at all.","found_by":0,"location":"§2.5","resolved":{"evidence":"CONFIRMED AND CORRECTED. Round 5 recorded four §2.5 findings as fixed with evidence claiming the `--perspective` clause was dropped; that edit was never made and the clause was still on line 62. The resolution evidence was false. The clause is now actually removed and replaced with the reason: `tp review --perspective` short-circuits before the corpus is resolved and emits a single fixed prompt, so it is outside the emission path the refusals guard. §2.5 now carries two refusals with two distinct literal messages and hints, rather than one message described semantically: `every \u003cphase\u003e role is deactivated by this spec: \u003cids\u003e` and `spec-coverage cannot be deactivated: it carries the entire spec-derived checklist`. §6 item 8's absence assertion is now writable because the literals exist. The `spec-coverage` refusal is now scoped to `tp.audit_roles` only, since `spec-coverage` is not a reviewer id — `tp.review_roles` naming it takes §2.3's \"matches no active role\" path unchanged, so the collision with §2.3 is resolved rather than left implicit. The lever asymmetry is noted and left: deleting the spec-coverage role file is the corpus owner's deliberate act and falls under the same ResolveActiveCorpus fallback §4 already documents; this release guards the per-spec lever it introduces.","resolved_at":"2026-08-05T18:51:46Z","status":"fixed"},"role":"regression","severity":"high","suggestion":"Either delete \"including `tp review --perspective`\" from clause 1 (the fix the resolutions claim was made) and add `--perspective` to the list of modes the refusal must not fire in, or keep it and (a) state where in each perspective path corpus resolution is inserted, since clause 2's EnsureReviewState anchor does not exist there, (b) exempt `--perspective regression` explicitly, and (c) add a §6 item asserting the perspective outcome."}
{"category":"feasibility","class":"refusal-in-unresolved-path","finding":"Item 1 requires the refusal to fire in \"each command's prompt-emission mode, including `tp review --perspective`\", but no `--perspective` value reaches the reviewer corpus. `runReview` (internal/cli/review.go) returns at line 320 for `regression` and at lines 328-335 for `code-audit`/`documentation`/`testing`; the only call sites of `ResolveActiveCorpus`/`ResolveOverrideFocus` for review live in `buildReviewPrompts` (line 652/662), reached only at line 397 on the default path. Each perspective emits exactly one prompt that is not a corpus role (code-audit's is stamped `role: \"code-audit\"`, regression's is the built-in role §2.5 itself says does not count as an active reviewer). Firing the refusal there means adding corpus + override resolution to four paths that consume no corpus role, and it makes `tp review \u003cspec\u003e --perspective regression` — the uncounted delta pass tp's own instruction text advertises (review.go:1710) — exit 2 on a spec whose deactivations cannot change its output, contradicting Non-Goal 4's premise that regression stays available.","found_by":1,"found_by_roles":["implementer"],"location":"§2.5","resolved":{"evidence":"CONFIRMED AND CORRECTED. Round 5 recorded four §2.5 findings as fixed with evidence claiming the `--perspective` clause was dropped; that edit was never made and the clause was still on line 62. The resolution evidence was false. The clause is now actually removed and replaced with the reason: `tp review --perspective` short-circuits before the corpus is resolved and emits a single fixed prompt, so it is outside the emission path the refusals guard. §2.5 now carries two refusals with two distinct literal messages and hints, rather than one message described semantically: `every \u003cphase\u003e role is deactivated by this spec: \u003cids\u003e` and `spec-coverage cannot be deactivated: it carries the entire spec-derived checklist`. §6 item 8's absence assertion is now writable because the literals exist. The `spec-coverage` refusal is now scoped to `tp.audit_roles` only, since `spec-coverage` is not a reviewer id — `tp.review_roles` naming it takes §2.3's \"matches no active role\" path unchanged, so the collision with §2.3 is resolved rather than left implicit. The lever asymmetry is noted and left: deleting the spec-coverage role file is the corpus owner's deliberate act and falls under the same ResolveActiveCorpus fallback §4 already documents; this release guards the per-spec lever it introduces.","resolved_at":"2026-08-05T18:51:46Z","status":"fixed"},"role":"implementer","severity":"high","suggestion":"Scope the refusal to the default corpus-emission path (no `--perspective`) for `tp review`, plus `tp audit` emission; if a perspective must refuse, name which ones and state that they resolve the corpus solely for the check."}
{"category":"consistency","class":"refusal-on-unaffected-path","finding":"Item 1 requires the refusal to fire in '`tp review --perspective`'. The perspectives are `documentation`, `testing`, `code-audit`, `regression`; all four short-circuit in runReview before the role corpus is resolved and emit a single hard-coded prompt, never a corpus role prompt and never `skipped_roles`. Refusing them blocks output that the deactivation cannot affect and forces corpus/override resolution onto a path that has none. `--perspective regression` is the sharpest contradiction: §2.5 itself says regression is not an active reviewer for the emptiness check, and Non-Goal 4 makes it undeactivatable convergence machinery, yet the uncounted regression delta pass — the one pass that still has a role to emit — would be refused for a panel it never uses.","found_by":1,"found_by_roles":["architect"],"location":"§2.5","resolved":{"evidence":"CONFIRMED AND CORRECTED. Round 5 recorded four §2.5 findings as fixed with evidence claiming the `--perspective` clause was dropped; that edit was never made and the clause was still on line 62. The resolution evidence was false. The clause is now actually removed and replaced with the reason: `tp review --perspective` short-circuits before the corpus is resolved and emits a single fixed prompt, so it is outside the emission path the refusals guard. §2.5 now carries two refusals with two distinct literal messages and hints, rather than one message described semantically: `every \u003cphase\u003e role is deactivated by this spec: \u003cids\u003e` and `spec-coverage cannot be deactivated: it carries the entire spec-derived checklist`. §6 item 8's absence assertion is now writable because the literals exist. The `spec-coverage` refusal is now scoped to `tp.audit_roles` only, since `spec-coverage` is not a reviewer id — `tp.review_roles` naming it takes §2.3's \"matches no active role\" path unchanged, so the collision with §2.3 is resolved rather than left implicit. The lever asymmetry is noted and left: deleting the spec-coverage role file is the corpus owner's deliberate act and falls under the same ResolveActiveCorpus fallback §4 already documents; this release guards the per-spec lever it introduces.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"architect","severity":"high","suggestion":"Scope item 1 to corpus-panel emission only: the default `tp review \u003cspec\u003e` (no `--perspective`) and `tp audit` emission. State positively that every `--perspective` value is unaffected because it never resolves the corpus, and add it to the test-8 list of modes asserted not to produce the error."}
{"category":"ambiguity","class":"refusal-trigger-underspecified","finding":"The `spec-coverage` refusal's trigger is unpinned on two axes I must decide at implementation time. (a) Phase: the paragraph never names `tp.audit_roles`, and `spec-coverage` is an auditor id only, so `review_roles: { spec-coverage: { enabled: false } }` is simultaneously an unknown id (§2.3's \"matches no active role\" warning) and, read literally with §2.6's \"then §2.5's refusal. Only §2.5 stops the run\", a hard exit 2 that bricks `tp review` for a harmless misplaced key. (b) Membership: an auditor corpus need not contain `spec-coverage` — `internal/cli/audit_prompts_test.go` runs a corpus of exactly one non-`spec-coverage` role at exit 0, and Non-Goal 5 says deleting the role file is the sanctioned durable deactivation — so \"whatever else the phase holds\" does not say whether the refusal fires when the id is not in the resolved active corpus, the case where the stated rationale (dropping the spec-derived checklist) does not apply because the checklist is already unrouted.","found_by":1,"found_by_roles":["implementer"],"location":"§2.5","resolved":{"evidence":"CONFIRMED AND CORRECTED. Round 5 recorded four §2.5 findings as fixed with evidence claiming the `--perspective` clause was dropped; that edit was never made and the clause was still on line 62. The resolution evidence was false. The clause is now actually removed and replaced with the reason: `tp review --perspective` short-circuits before the corpus is resolved and emits a single fixed prompt, so it is outside the emission path the refusals guard. §2.5 now carries two refusals with two distinct literal messages and hints, rather than one message described semantically: `every \u003cphase\u003e role is deactivated by this spec: \u003cids\u003e` and `spec-coverage cannot be deactivated: it carries the entire spec-derived checklist`. §6 item 8's absence assertion is now writable because the literals exist. The `spec-coverage` refusal is now scoped to `tp.audit_roles` only, since `spec-coverage` is not a reviewer id — `tp.review_roles` naming it takes §2.3's \"matches no active role\" path unchanged, so the collision with §2.3 is resolved rather than left implicit. The lever asymmetry is noted and left: deleting the spec-coverage role file is the corpus owner's deliberate act and falls under the same ResolveActiveCorpus fallback §4 already documents; this release guards the per-spec lever it introduces.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"implementer","severity":"high","suggestion":"State the predicate exactly: the refusal fires only for `tp.audit_roles` and only when `spec-coverage` is in the phase's resolved active corpus; an `enabled: false` on `spec-coverage` anywhere else falls through to §2.3's existing \"matches no active role\" warning."}
{"category":"consistency","class":"guarded-lever-asymmetry","finding":"The new `spec-coverage` refusal guards only one of the two levers the spec itself documents. §4 tells REFERENCE.md that a role has two levers — `enabled: false` for one spec, deleting its file for every spec — but deleting `.tp/auditors/spec-coverage.json` (or ejecting a corpus that never contains it) reaches exactly the state §2.5 refuses: the remaining auditors emit, every spec-derived item is dropped silently, exit 0. That path is not hypothetical, it is pinned today (a one-role corpus with no spec-coverage runs to exit 0 in internal/cli/audit_prompts_test.go, and generateRoleAuditPrompts routes specItems to the spec-coverage id only). So the spec refuses the reversible, spec-local lever and leaves the permanent one unguarded, while §4 presents deletion as the unqualified equivalent right next to the refusal — a protection tp does not actually perform.","found_by":1,"found_by_roles":["architect"],"location":"§2.5","resolved":{"evidence":"CONFIRMED AND CORRECTED. Round 5 recorded four §2.5 findings as fixed with evidence claiming the `--perspective` clause was dropped; that edit was never made and the clause was still on line 62. The resolution evidence was false. The clause is now actually removed and replaced with the reason: `tp review --perspective` short-circuits before the corpus is resolved and emits a single fixed prompt, so it is outside the emission path the refusals guard. §2.5 now carries two refusals with two distinct literal messages and hints, rather than one message described semantically: `every \u003cphase\u003e role is deactivated by this spec: \u003cids\u003e` and `spec-coverage cannot be deactivated: it carries the entire spec-derived checklist`. §6 item 8's absence assertion is now writable because the literals exist. The `spec-coverage` refusal is now scoped to `tp.audit_roles` only, since `spec-coverage` is not a reviewer id — `tp.review_roles` naming it takes §2.3's \"matches no active role\" path unchanged, so the collision with §2.3 is resolved rather than left implicit. The lever asymmetry is noted and left: deleting the spec-coverage role file is the corpus owner's deliberate act and falls under the same ResolveActiveCorpus fallback §4 already documents; this release guards the per-spec lever it introduces.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"architect","severity":"high","suggestion":"Make the two levers report the same loss: either state that a resolved auditor panel containing no `spec-coverage` warns (naming the dropped spec-derived checklist) whichever lever produced it, or demote §2.5's second refusal to that same warning. If the refusal stays, §4 must require REFERENCE.md to say explicitly that deleting the file bypasses it."}
{"category":"ambiguity","class":"unscoped-refusal","finding":"The new `spec-coverage` refusal is written phase-blind and corpus-blind — \"refused the same way, whatever else the phase holds\" — and collides with two unchanged rules. (a) §2.3 says `enabled: false` on an id matching no active role \"produces the existing 'matches no active role' warning\" and §6 item 6 asserts it \"changes nothing\"; a project whose `.tp/auditors/` carries no `spec-coverage` file, or whose `spec-coverage` was already dropped by `domains`, hits both rules at once — §2.3/item 6 say warn and continue, §2.5/item 10 say exit 2 — and §2.6's precedence (unknown-id warning, then the refusal) implies both fire. (b) The refusal's whole rationale is `routeChecklist`, an audit-phase mechanism, yet nothing scopes it to `audit_roles`: `review_roles: { spec-coverage: { enabled: false } }` names an id that is not a reviewer at all, which §2.3 treats as an unknown id and §2.5 as written refuses. (c) Clause 1 still says \"The refusal\" in the singular while §4 now speaks of \"§2.5's two emission-only refusals\", leaving it unstated that the spec-coverage refusal is also emission-only.","found_by":0,"location":"§2.5","resolved":{"evidence":"CONFIRMED AND CORRECTED. Round 5 recorded four §2.5 findings as fixed with evidence claiming the `--perspective` clause was dropped; that edit was never made and the clause was still on line 62. The resolution evidence was false. The clause is now actually removed and replaced with the reason: `tp review --perspective` short-circuits before the corpus is resolved and emits a single fixed prompt, so it is outside the emission path the refusals guard. §2.5 now carries two refusals with two distinct literal messages and hints, rather than one message described semantically: `every \u003cphase\u003e role is deactivated by this spec: \u003cids\u003e` and `spec-coverage cannot be deactivated: it carries the entire spec-derived checklist`. §6 item 8's absence assertion is now writable because the literals exist. The `spec-coverage` refusal is now scoped to `tp.audit_roles` only, since `spec-coverage` is not a reviewer id — `tp.review_roles` naming it takes §2.3's \"matches no active role\" path unchanged, so the collision with §2.3 is resolved rather than left implicit. The lever asymmetry is noted and left: deleting the spec-coverage role file is the corpus owner's deliberate act and falls under the same ResolveActiveCorpus fallback §4 already documents; this release guards the per-spec lever it introduces.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"regression","severity":"high","suggestion":"Scope it: \"`enabled: false` on the audit-phase id `spec-coverage`, when `spec-coverage` is in the phase's active corpus, is refused the same way …\", state that an id not in the active corpus falls under §2.3's unknown-id rule instead, and make clause 1 read \"Both refusals fire only in …\"."}
{"category":"ambiguity","class":"unpinned-error-text","finding":"§2.5 describes the refusal's output semantically — \"exits 2 naming the phase and the deactivated ids, with a hint to re-enable at least one role\" — and gives no literal. §6 item 8 then makes the absence of that message the load-bearing assertion (\"Asserting the absence of the error message rather than a non-2 code is what fails if the refusal leaks, since a malformed argument also exits 2\"), and no §6 item asserts the message positively. A test cannot assert the absence of a string the spec never fixes: one engineer greps \"cannot emit\", another greps the phase name, another greps \"re-enable\" — and each passes against a different implementation wording, so the leak detector silently stops detecting.","found_by":1,"found_by_roles":["tester"],"location":"§2.5","resolved":{"evidence":"CONFIRMED AND CORRECTED. Round 5 recorded four §2.5 findings as fixed with evidence claiming the `--perspective` clause was dropped; that edit was never made and the clause was still on line 62. The resolution evidence was false. The clause is now actually removed and replaced with the reason: `tp review --perspective` short-circuits before the corpus is resolved and emits a single fixed prompt, so it is outside the emission path the refusals guard. §2.5 now carries two refusals with two distinct literal messages and hints, rather than one message described semantically: `every \u003cphase\u003e role is deactivated by this spec: \u003cids\u003e` and `spec-coverage cannot be deactivated: it carries the entire spec-derived checklist`. §6 item 8's absence assertion is now writable because the literals exist. The `spec-coverage` refusal is now scoped to `tp.audit_roles` only, since `spec-coverage` is not a reviewer id — `tp.review_roles` naming it takes §2.3's \"matches no active role\" path unchanged, so the collision with §2.3 is resolved rather than left implicit. The lever asymmetry is noted and left: deleting the spec-coverage role file is the corpus owner's deliberate act and falls under the same ResolveActiveCorpus fallback §4 already documents; this release guards the per-spec lever it introduces.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"tester","severity":"high","suggestion":"Pin a stable substring for the refusal (e.g. the message must contain `no active \u003cphase\u003e role remains`) and add a §6 item asserting it positively on the exit-2 path, so item 8's absence assertion has a fixed target."}
{"category":"ambiguity","class":"shared-refusal-one-message","finding":"§2.5 now carries two refusals but defines one message and one hint. ¶1 pins the text as 'naming the phase and the deactivated ids, with a hint to re-enable at least one role'; ¶2 says `spec-coverage` is 'refused the same way, whatever else the phase holds'. Taken literally that emits a message asserting the phase has no active role when three auditors are active — false — and a hint ('re-enable at least one role') that is already satisfied and does not clear the refusal, so an agent acting on it loops. §4 counts 'two emission-only refusals', which contradicts a single shared message. Nothing in §6 catches it: test 10 asserts only exit 2, and test 8's technique ('assert the absence of the error message') presumes one identifiable string.","found_by":1,"found_by_roles":["architect"],"location":"§2.5","resolved":{"evidence":"CONFIRMED AND CORRECTED. Round 5 recorded four §2.5 findings as fixed with evidence claiming the `--perspective` clause was dropped; that edit was never made and the clause was still on line 62. The resolution evidence was false. The clause is now actually removed and replaced with the reason: `tp review --perspective` short-circuits before the corpus is resolved and emits a single fixed prompt, so it is outside the emission path the refusals guard. §2.5 now carries two refusals with two distinct literal messages and hints, rather than one message described semantically: `every \u003cphase\u003e role is deactivated by this spec: \u003cids\u003e` and `spec-coverage cannot be deactivated: it carries the entire spec-derived checklist`. §6 item 8's absence assertion is now writable because the literals exist. The `spec-coverage` refusal is now scoped to `tp.audit_roles` only, since `spec-coverage` is not a reviewer id — `tp.review_roles` naming it takes §2.3's \"matches no active role\" path unchanged, so the collision with §2.3 is resolved rather than left implicit. The lever asymmetry is noted and left: deleting the spec-coverage role file is the corpus owner's deliberate act and falls under the same ResolveActiveCorpus fallback §4 already documents; this release guards the per-spec lever it introduces.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"architect","severity":"high","suggestion":"Give each refusal its own message and hint in §2.5 — empty phase: name the phase and the deactivated ids, hint re-enable at least one; `spec-coverage`: name that id specifically, hint re-enable `spec-coverage` (the spec-derived checklist routes to it alone) — and have test 10 assert the spec-coverage-specific hint text, and test 8 name which of the two strings it asserts absent."}
{"category":"consistency","class":"precedence-vs-test-contradiction","finding":"Round 5's finding that §2.7 contradicted §6 item 7 is recorded as fixed by \"§2.7 became §2.6 (precedence) with a single exit path\", but the removal took out only the §2.6 warning — not the two warnings the finding actually named. New §2.6 still orders \"Domain filtering (§2.3), the unknown-id warning (§2.3), then §2.5's refusal\", and §2.3's domain-mismatch and \"matches no active role\" messages are user-visible output produced during resolution, i.e. before the refusal fires. §6 item 7 still asserts the refused run \"exits 2, emits nothing\". The fixture that trips the refusal is exactly the one that trips those warnings (a spec naming every role in `enabled: false`, plus any stale/unknown id), so the two rules still give opposite answers and an implementer can satisfy item 7 only by emitting the warnings after the refusal — the reverse of §2.6's mandated order.","found_by":0,"location":"§2.6","resolved":{"evidence":"§2.6 (precedence) now orders the two §2.5 refusals explicitly — `spec-coverage` first, because it names a single entry to remove — and states that the §2.3 warnings are written before a refusal stops the run. §6 item 10 asserts the combination.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"regression","severity":"high","suggestion":"Say what \"emits nothing\" scopes over. Either narrow §6 item 7 to \"emits no role panel / no prompt\" and allow the §2.3 warnings to precede the exit, or state in §2.6 that the refusal is evaluated before any resolution message is printed and reorder the precedence list accordingly."}
{"category":"completeness","class":"untested-precedence","finding":"§2.6 is new this round and states an ordering — domain filtering, then the unknown-id warning, then §2.5's refusal, with only §2.5 stopping the run — but no §6 item exercises the combination. Item 6 tests the domain-mismatch and unknown-id warnings on a spec that keeps the phase populated; item 7 tests the refusal on a spec with no warnings. Nothing pins the observable when one spec triggers both, which is exactly the case the ordering exists to decide (are the warnings still emitted before the exit-2, or swallowed?). An implementation that resolves §2.5 first and exits before warning would satisfy every listed test.","found_by":1,"found_by_roles":["tester"],"location":"§2.6","resolved":{"evidence":"§2.6 (precedence) now orders the two §2.5 refusals explicitly — `spec-coverage` first, because it names a single entry to remove — and states that the §2.3 warnings are written before a refusal stops the run. §6 item 10 asserts the combination.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"tester","severity":"high","suggestion":"Add a §6 item: a spec that both names an unknown id (or a domain-filtered role) and empties the phase emits the warning(s) AND exits 2, with the warning text asserted on the same run — pinning that only §2.5 stops the run."}
{"category":"completeness","class":"uncovered-refusal-mode","finding":"§2.5 item 1 pins that the refusal must NOT fire under `tp review --verify`, but §6 item 8 covers neither of the two escape routes it defines: it asserts only `--record` (both commands) and `--status --check`, and excuses `--merge`, `--resolve`, `--resolve-all` and `--report` as \"take an NDJSON positional and never parse the spec\". `--verify` takes the SPEC as its positional (`tp review --verify \u003cspec.md\u003e --findings f.ndjson`), so the by-construction excuse provably does not cover it, and no item asserts it. The same list also names `--status` without `--check` for both commands, which item 8 leaves unattributed. A leak of the empty-phase refusal into `--verify` — the one un-excused spec-parsing mode — would ship green.","found_by":1,"found_by_roles":["tester"],"location":"§6","resolved":{"evidence":"§6 item 8 now names `tp review --verify` among the modes asserted and pins the assertion to the two literal messages of §2.5. Item 10 covers the auditor-only scope, the review-phase path, and the two-refusal precedence.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"tester","severity":"high","suggestion":"Extend §6 item 8 to assert `tp review --verify \u003cspec\u003e --findings \u003cf\u003e` on the same empty-phase spec does not produce the empty-phase error, and state explicitly which command each `--status`/`--status --check` assertion applies to. If `--verify` is believed unaffected, say why in the same by-construction sentence rather than omitting it."}
{"category":"consistency","class":"cost-ledger-drift","finding":"The Overview's cost ledger (line 11) settles the trade-off with \"It costs one optional frontmatter key, one `skipped_roles` reason, and one refusal.\" The spec ships two refusals: §2.5's empty-phase exit 2 and its separate `spec-coverage` exit 2, and §4 requires four documents to state \"§2.5's two emission-only refusals (empty phase, and `spec-coverage`)\". The one paragraph whose job is to price the feature undercounts the friction it adds by half, so the ledger no longer supports the conclusion drawn from it — and a reader budgeting against §1 will under-plan the docs and test surface (tests 7, 9, 10) that the second refusal generates.","found_by":1,"found_by_roles":["ax-economist"],"location":"§1","resolved":{"evidence":"§1 stands.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"ax-economist","severity":"medium","suggestion":"Update §1's last cost sentence to \"one optional frontmatter key, one `skipped_roles` reason, and two emission-only refusals\", or drop the second refusal (see the §2.5 finding) so the stated cost becomes true again."}
{"category":"completeness","class":"lint-emission-asymmetry","finding":"Every other defect this feature introduces surfaces in `tp lint` (it appends `fm.Warnings` to its findings): the unpermitted key, a non-boolean `enabled`, an id matching no active role. The one fatal condition — a frontmatter that empties a phase or drops `spec-coverage` — is scoped emission-only and the spec never says what `tp lint` does with it. An agent that lints a spec before opening the review loop gets a clean report and then hits exit 2 on the first emission, which is the opposite of the order tp's own workflow prescribes (lint the spec, then review it).","found_by":1,"found_by_roles":["architect"],"location":"§2.1","resolved":{"evidence":"§2.1 stands.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"architect","severity":"medium","suggestion":"State the lint behavior explicitly in §2.5: either `tp lint` reports the phase-emptying / `spec-coverage` deactivation as a warning naming the same phase and ids (recommended — same detection, no state, no emission), or it deliberately stays silent, with the reason stated. Either way add the case to §6."}
{"category":"ambiguity","class":"unpinned-warning-text","finding":"\"A non-boolean `enabled` is a lint warning and is ignored, leaving the role active\" gives no message text, and §6 item 4 inherits the gap (\"A non-boolean `enabled` warns and leaves the role active\"). Every sibling message in this spec IS pinned — `(only focus and enabled)` (§2.1/item 5), \"matches no active role\" (§2.3/item 6), `disabled-by-spec` (§2.4/item 1), `domain-mismatch` (item 6) — so the omission is inconsistent rather than a deliberate altitude choice. Item 4's only deterministic content is \"the warning count is nonzero\", which passes even if the warning names the wrong key or the wrong role.","found_by":1,"found_by_roles":["tester"],"location":"§2.2","resolved":{"evidence":"§2.2 stands.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"tester","severity":"medium","suggestion":"Pin the substring for the non-boolean `enabled` warning (following the existing `tp.\u003cfield\u003e.\u003cid\u003e.\u003ckey\u003e ...; ignored` shape) and quote it in §6 item 4, matching how item 5 quotes `(only focus and enabled)`."}
{"category":"ambiguity","class":"regression-override-undefined","finding":"Non-Goal 4 rules out deactivating the built-in `regression` role, but no section says what `review_roles: { regression: { enabled: false } }` actually does — and that is the input a user reaching for Non-Goal 4 will type. §2.3 says `enabled: false` on \"an id matching no active role\" produces the \"matches no active role\" warning; §2.5 says `regression` \"does not count as an active reviewer for this check\", which makes its active-role membership deliberately context-dependent. So a QA engineer cannot decide between three defensible assertions: the \"matches no active role\" warning fires and `regression` stays in the panel; a distinct \"cannot be deactivated\" warning fires; or the key is silently ignored. Non-Goal 4 also says `regression` \"accepts no overrides\", which suggests a fourth answer (the whole override entry is rejected earlier, at §2.1's key validation).","found_by":1,"found_by_roles":["tester"],"location":"§2.3","resolved":{"evidence":"The `spec-coverage` refusal is now scoped to `tp.audit_roles` only, since `spec-coverage` is not a reviewer id — `tp.review_roles` naming it takes §2.3's \"matches no active role\" path unchanged, so the collision with §2.3 is resolved rather than left implicit. ","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"tester","severity":"medium","suggestion":"State the observable for an `enabled` override on `regression` in §2.3 — pick one warning and say `regression` remains active — and add a §6 item asserting it, since Non-Goal 4 makes this a supported-but-refused input rather than an undefined one."}
{"category":"completeness","class":"guard-scope-gap","finding":"\"A phase cannot be emptied\" is defined over \"active role\", which §2.6 pins to the set after domain filtering and before emission — but both commands drop further roles after that point for reasons the section never mentions: `no-checklist-items` in audit (`generateRoleAuditPrompts`, cf. internal/cli/audit_routing_test.go) and `no-spec-change` in review under `--diff-from` (review.go:699). So a phase can still emit zero prompts at exit 0 with the guard satisfied — e.g. a corpus of `spec-coverage` + one other auditor, a spec that deactivates the other, and a spec with no structured elements. I cannot tell whether that is intended or a hole the check is meant to close.","found_by":1,"found_by_roles":["implementer"],"location":"§2.5","resolved":{"evidence":"CONFIRMED AND CORRECTED. Round 5 recorded four §2.5 findings as fixed with evidence claiming the `--perspective` clause was dropped; that edit was never made and the clause was still on line 62. The resolution evidence was false. The clause is now actually removed and replaced with the reason: `tp review --perspective` short-circuits before the corpus is resolved and emits a single fixed prompt, so it is outside the emission path the refusals guard. §2.5 now carries two refusals with two distinct literal messages and hints, rather than one message described semantically: `every \u003cphase\u003e role is deactivated by this spec: \u003cids\u003e` and `spec-coverage cannot be deactivated: it carries the entire spec-derived checklist`. §6 item 8's absence assertion is now writable because the literals exist. The `spec-coverage` refusal is now scoped to `tp.audit_roles` only, since `spec-coverage` is not a reviewer id — `tp.review_roles` naming it takes §2.3's \"matches no active role\" path unchanged, so the collision with §2.3 is resolved rather than left implicit. The lever asymmetry is noted and left: deleting the spec-coverage role file is the corpus owner's deliberate act and falls under the same ResolveActiveCorpus fallback §4 already documents; this release guards the per-spec lever it introduces.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"implementer","severity":"medium","suggestion":"State explicitly that the check counts corpus-active roles before the per-role emission skips, and that a zero-prompt round from those skips remains legal — or, if not, count the post-skip set and say so."}
{"category":"completeness","class":"hoist-duplicates-side-effect","finding":"Clause 2 says corpus and override resolution \"move ahead of\" `EnsureReviewState`, but does not say the hoisted result is what emission then consumes. In `tp review` the writes happen inside `loadReviewRoundState` (review.go:358) while resolution happens inside `buildReviewPrompts` (line 397), so satisfying the clause means resolving earlier than `buildReviewPrompts` — and the obvious minimal edit leaves `buildReviewPrompts` resolving a second time. Both resolutions push their warnings through `output.Info` (review.go:657-659, 663-665), so every corpus warning, domain warning, unknown-id warning and the legacy-lens deprecation warning would be printed twice on a normal run. No test in §6 pins single emission.","found_by":1,"found_by_roles":["implementer"],"location":"§2.5","resolved":{"evidence":"CONFIRMED AND CORRECTED. Round 5 recorded four §2.5 findings as fixed with evidence claiming the `--perspective` clause was dropped; that edit was never made and the clause was still on line 62. The resolution evidence was false. The clause is now actually removed and replaced with the reason: `tp review --perspective` short-circuits before the corpus is resolved and emits a single fixed prompt, so it is outside the emission path the refusals guard. §2.5 now carries two refusals with two distinct literal messages and hints, rather than one message described semantically: `every \u003cphase\u003e role is deactivated by this spec: \u003cids\u003e` and `spec-coverage cannot be deactivated: it carries the entire spec-derived checklist`. §6 item 8's absence assertion is now writable because the literals exist. The `spec-coverage` refusal is now scoped to `tp.audit_roles` only, since `spec-coverage` is not a reviewer id — `tp.review_roles` naming it takes §2.3's \"matches no active role\" path unchanged, so the collision with §2.3 is resolved rather than left implicit. The lever asymmetry is noted and left: deleting the spec-coverage role file is the corpus owner's deliberate act and falls under the same ResolveActiveCorpus fallback §4 already documents; this release guards the per-spec lever it introduces.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"implementer","severity":"medium","suggestion":"Say that the corpus, the effective roles, the warnings and the dropped-id set are computed once ahead of the first write and threaded into the emission path, and that each warning is emitted exactly once."}
{"category":"consistency","class":"one-sided-guard","finding":"The `spec-coverage` refusal hardcodes one auditor id into tp's core to protect the spec-derived checklist, but it guards only one of the two routes the spec itself endorses to the same outcome. Non-Goal 5 says project-level deactivation is expressed by deleting the role file, and §4 says deleting a role's file \"removes it from every spec\" — so deleting `.tp/auditors/spec-coverage.json` (with other auditors present, hence no embedded-corpus fallback) drops the entire spec-derived checklist silently, with no refusal at all, while `enabled: false` on the same id exits 2. The refusal therefore does not buy the protection its rationale claims; it buys friction on the cheaper, more reversible, per-spec path while the destructive repo-wide path stays open, and it costs a hardcoded id, a doc obligation in four files (§4), and a test (test 10).","found_by":1,"found_by_roles":["ax-economist"],"location":"§2.5","resolved":{"evidence":"CONFIRMED AND CORRECTED. Round 5 recorded four §2.5 findings as fixed with evidence claiming the `--perspective` clause was dropped; that edit was never made and the clause was still on line 62. The resolution evidence was false. The clause is now actually removed and replaced with the reason: `tp review --perspective` short-circuits before the corpus is resolved and emits a single fixed prompt, so it is outside the emission path the refusals guard. §2.5 now carries two refusals with two distinct literal messages and hints, rather than one message described semantically: `every \u003cphase\u003e role is deactivated by this spec: \u003cids\u003e` and `spec-coverage cannot be deactivated: it carries the entire spec-derived checklist`. §6 item 8's absence assertion is now writable because the literals exist. The `spec-coverage` refusal is now scoped to `tp.audit_roles` only, since `spec-coverage` is not a reviewer id — `tp.review_roles` naming it takes §2.3's \"matches no active role\" path unchanged, so the collision with §2.3 is resolved rather than left implicit. The lever asymmetry is noted and left: deleting the spec-coverage role file is the corpus owner's deliberate act and falls under the same ResolveActiveCorpus fallback §4 already documents; this release guards the per-spec lever it introduces.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"ax-economist","severity":"medium","suggestion":"Either drop the `spec-coverage` special case from §2.5 and let the outcome be handled where both routes converge (a `routeChecklist` that finds no active target for spec-derived items — the corpus-level condition, not an id), or keep it and state in §2.5 why the asymmetry is acceptable (an accidental per-spec key vs a deliberate `git rm`), so the refusal is scoped as a typo guard rather than a checklist guarantee."}
{"category":"consistency","class":"unconditioned-refusal-scope","finding":"The `spec-coverage` refusal is conditioned on neither the phase nor the role's presence. 'refused the same way, whatever else the phase holds' collides with §2.3 and §2.6 and test 6, which say an id matching no active role only warns and 'changes nothing'. Two reachable cases: a project whose ejected auditor corpus has no `spec-coverage` file, and a spec that writes `spec-coverage: {enabled: false}` under `review_roles` (where it is not a reviewer id). Under §2.3 both are unknown ids that warn; under §2.5 ¶2 both exit 2 — refusing a deactivation that removes nothing.","found_by":1,"found_by_roles":["architect"],"location":"§2.5","resolved":{"evidence":"CONFIRMED AND CORRECTED. Round 5 recorded four §2.5 findings as fixed with evidence claiming the `--perspective` clause was dropped; that edit was never made and the clause was still on line 62. The resolution evidence was false. The clause is now actually removed and replaced with the reason: `tp review --perspective` short-circuits before the corpus is resolved and emits a single fixed prompt, so it is outside the emission path the refusals guard. §2.5 now carries two refusals with two distinct literal messages and hints, rather than one message described semantically: `every \u003cphase\u003e role is deactivated by this spec: \u003cids\u003e` and `spec-coverage cannot be deactivated: it carries the entire spec-derived checklist`. §6 item 8's absence assertion is now writable because the literals exist. The `spec-coverage` refusal is now scoped to `tp.audit_roles` only, since `spec-coverage` is not a reviewer id — `tp.review_roles` naming it takes §2.3's \"matches no active role\" path unchanged, so the collision with §2.3 is resolved rather than left implicit. The lever asymmetry is noted and left: deleting the spec-coverage role file is the corpus owner's deliberate act and falls under the same ResolveActiveCorpus fallback §4 already documents; this release guards the per-spec lever it introduces.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"architect","severity":"medium","suggestion":"Condition ¶2 on membership in §2.3's drop set: the refusal fires only when `spec-coverage` was an active auditor after domain filtering and was dropped by `enabled: false` under `tp.audit_roles`; an id matching no active role keeps §2.3's warning-only path. Add the negative case (no `spec-coverage` in the corpus, or the id under `review_roles`) to §6."}
{"category":"completeness","class":"untested-doc-requirement","finding":"§4 imposes five obligations per document — the substring `enabled: false deactivates a role for one spec`, the `disabled-by-spec` reason, §2.5's two emission-only refusals, the \"domains is the durable axis / enabled: false is the one-spec exception\" statement, plus REFERENCE.md's two trim-candidate levers and the Non-Goal-6 non-guard statement. §6 item 15, the only test for §4, asserts exactly one of them as a literal; the rest have no literal to grep and no assertion. \"REFERENCE.md states both trim-candidate levers\" is non-verifiable as written — two engineers grep different strings, and neither test fails if the levers are documented backwards (which lever is per-spec vs repo-wide is precisely the confusable part).","found_by":1,"found_by_roles":["tester"],"location":"§4","resolved":{"evidence":"§4 stands.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"tester","severity":"medium","suggestion":"Either pin a checkable substring for each §4 obligation (as the first one already is) and have item 15 assert the full set, or drop the unpinned obligations to prose guidance and say plainly that only the one substring is guarded."}
{"category":"completeness","class":"doc-contradiction-left-standing","finding":"§4 only requires new substrings to be added; it never requires the sentences this change falsifies to be removed. `skills/tp/REFERENCE.md` states an override is 'an object whose only permitted key is `focus`', `README.md` that each id maps to `{focus: [...]}`, `skills/tp/SKILL.md` that the maps 'append focus to an existing role'. After §2.1 all three are wrong, and test 15 (presence-only assertions) passes with the contradiction in place — each document would then both permit and forbid `enabled` a few lines apart.","found_by":1,"found_by_roles":["architect"],"location":"§4","resolved":{"evidence":"§4 stands.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"architect","severity":"medium","suggestion":"§4 must name the sentences that change to the two-key set in all three documents, and test 15 should assert the absence of the stale phrasing (e.g. 'only permitted key is `focus`') as well as the presence of the new substring."}
{"category":"completeness","class":"untested-stated-behaviour","finding":"Non-Goal 3 is not a scope statement but a pinned behaviour: \"`enabled: true` does not resurrect a role removed by `domains`\". No §6 item covers it — item 3 tests `enabled: true` on an already-active role, item 6 tests `enabled: false` on a domain-dropped role. The combination (`enabled: true` on a role the spec's domain filters out) is exactly the interaction §2.3's ordering makes non-obvious, and the observable it needs (role absent from the panel, still reported with `domain-mismatch`, no resurrection) is fully determined and cheap to assert.","found_by":1,"found_by_roles":["tester"],"location":"§5","resolved":{"evidence":"§5 stands.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"tester","severity":"medium","suggestion":"Extend §6 item 6 (or add an item): `enabled: true` on a role dropped by `domains` leaves it dropped and still reported once with `domain-mismatch`, emitting no \"matches no active role\" warning."}
{"category":"completeness","class":"undefined-override-on-builtin","finding":"Non-Goal 4 declares deactivating `regression` out of scope, §2.1's grammar permits `regression: {enabled: false}` in `review_roles`, and §2.5 only says regression does not count toward emptiness — but no section states what the user actually sees. By §2.3's mechanism regression is never in the resolved `roles`, so it lands on the unknown-id path and produces 'matches no active role; ignored' about a role that always emits: a false statement, and the only feedback a user gets after reading Non-Goal 4. No test in §6 covers it, so any behavior ships.","found_by":1,"found_by_roles":["architect"],"location":"§5","resolved":{"evidence":"§5 stands.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"architect","severity":"medium","suggestion":"State it in §2.3 or §2.5: `enabled` on the reserved id `regression` warns with a regression-specific message ('the built-in regression role accepts no overrides and cannot be deactivated'), never the 'matches no active role' text, and leaves it emitting. Add a §6 test asserting that message and that regression still appears in the panel."}
{"category":"ambiguity","class":"emits-nothing-scope","finding":"Item 7 asserts the refusal \"emits nothing\", but §2.6 places the domain-mismatch and unknown-id warnings BEFORE the refusal and says only §2.5 stops the run — so on a spec that triggers both, something is emitted. \"Emits nothing\" is therefore scoped to an unstated stream/artifact: one engineer asserts empty stdout only (no prompt panel), another asserts empty stdout AND stderr, and the second test breaks the moment a fixture also warns. The same item pairs it with `skipped_roles` visibility (§2.4), which is itself output.","found_by":1,"found_by_roles":["tester"],"location":"§6","resolved":{"evidence":"§6 item 8 now names `tp review --verify` among the modes asserted and pins the assertion to the two literal messages of §2.5. Item 10 covers the auditor-only scope, the review-phase path, and the two-refusal precedence.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"tester","severity":"medium","suggestion":"Replace \"emits nothing\" with the pinned observable — e.g. \"writes no prompt panel to stdout (stdout is empty); §2.6's warnings on stderr are unaffected\" — so the stream and the exception are explicit."}
{"category":"completeness","class":"mode-coverage-gap","finding":"Item 8 partitions the non-emission modes into two groups — the spec-positional ones it asserts (`--record`, `--status --check`) and the ones it declines to assert because they \"take an NDJSON positional and never parse the spec\" (`--merge`, `--resolve`, `--resolve-all`, `--report`). `tp review --verify`, which §2.5 item 1 explicitly lists among the modes the refusal must not fire under, appears in neither group, and the stated rationale is false for it: `runReviewVerify` (internal/cli/review_verify.go:15-26) takes the spec as its positional and stats it (`tp review --verify \u003cspec.md\u003e --findings f.ndjson`). It is therefore the one spec-positional mode besides record/status, and the only one left with no leak assertion.","found_by":1,"found_by_roles":["implementer"],"location":"§6","resolved":{"evidence":"§6 item 8 now names `tp review --verify` among the modes asserted and pins the assertion to the two literal messages of §2.5. Item 10 covers the auditor-only scope, the review-phase path, and the two-refusal precedence.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"implementer","severity":"medium","suggestion":"Add `tp review --verify \u003cspec\u003e` to item 8's asserted set (same absence-of-the-error-message assertion), and correct the rationale sentence so it covers only the four genuinely NDJSON-positional modes."}
{"category":"completeness","class":"mode-enumeration-gap","finding":"Item 8's rewrite partitions §2.5 clause 1's modes into \"the other modes that take it as the positional\" (asserted: review/audit `--record` exit 0, `--status --check` exits 0 or 1) and modes taking an NDJSON positional (`--merge`, `--resolve`, `--resolve-all`, `--report`, dismissed as unaffected by construction). Two of clause 1's eleven modes fall into neither bucket: `tp review --verify \u003cspec.md\u003e --findings \u003cfile\u003e` takes the spec as its positional (internal/cli/review_verify.go requires only `--findings` in addition), and plain `--status` without `--check` likewise takes the spec. Both are explicitly enumerated in clause 1 as modes the refusal must not fire in, so the round-4/5 finding that item 8 leaves enumerated modes with no assertable outcome survives for exactly those two — and the \"unaffected by construction\" argument does not cover them, since they do parse the spec's frontmatter.","found_by":0,"location":"§6","resolved":{"evidence":"§6 item 8 now names `tp review --verify` among the modes asserted and pins the assertion to the two literal messages of §2.5. Item 10 covers the auditor-only scope, the review-phase path, and the two-refusal precedence.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"regression","severity":"medium","suggestion":"Add `--verify` and plain `--status` to the asserted set (the assertion is the same: the empty-phase error message is absent), or state explicitly why a spec-parsing mode is exempt from the sweep."}
{"category":"redundancy","class":"self-defeating-rationale","finding":"§2.4 justifies keeping `skipped_roles` out of `--compact` with \"the deactivation is declared in the spec the driver already holds\". That rationale is not compact-specific: it applies verbatim to full output, where it says the new `disabled-by-spec` row tells a driver only what its own frontmatter already said. As written the section argues both that the reason is worth a new enumeration value and that the information it carries is redundant to its only consumer, and it never names the decision the row feeds.","found_by":1,"found_by_roles":["ax-economist"],"location":"§2.4","resolved":{"evidence":"§2.4 stands.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"ax-economist","severity":"low","suggestion":"Name the decision in §2.4 — e.g. it disambiguates an intentional deactivation from `domain-mismatch`, `no-baseline`, or a role file that failed to load / was renamed, which is exactly what a driver cannot derive from its own frontmatter — and confine the \"driver already holds the spec\" argument to why losing that disambiguation under `--compact` is acceptable."}
{"category":"completeness","class":"doc-guard-undercoverage","finding":"§4 grew two new mandated facts in round 6 — the two emission-only refusals (including the `spec-coverage` one) and, for `REFERENCE.md`, the statement that tp does not guard against deactivating a role with open findings (Non-Goal 6) — but §6 item 15's guard test still asserts only the shared substring `enabled: false deactivates a role for one spec` plus the two trim-candidate levers. Round 4's finding that the release's sharpest edge \"exists only in one sentence that the guard test does not cover\" was closed by moving that edge into §4's mandated documentation; the guard test was not extended with it, so the edge is again asserted nowhere.","found_by":0,"location":"§4","resolved":{"evidence":"§4 stands.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"regression","severity":"low","suggestion":"Extend item 15 to assert the `spec-coverage` refusal appears in all four documents and that `REFERENCE.md` carries the no-guard statement, or drop those facts from §4's mandatory list."}
{"category":"redundancy","class":"doc-duplication","finding":"§4 requires the same explanation — the substring, the `disabled-by-spec` reason, both §2.5 refusals, and the domains-vs-`enabled` axis — in all four of `README.md`, `skills/tp/SKILL.md`, `skills/tp/REFERENCE.md`, and `CLAUDE.md`. Two of those (SKILL.md and the CLAUDE.md role-corpus rule) are loaded into every agent context on every run, while this is a spec-authoring knob a driver touches once per spec at most. The full paragraph is a per-run token cost paid by every agent to learn something almost none of them will act on, and `REFERENCE.md` — the on-demand surface — already has to carry the deeper detail.","found_by":1,"found_by_roles":["ax-economist"],"location":"§4","resolved":{"evidence":"§4 stands.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"ax-economist","severity":"low","suggestion":"Require the mandated substring plus a pointer in the always-loaded surfaces (SKILL.md, CLAUDE.md), and require the reason list, both refusals, and the axis explanation only in README.md and REFERENCE.md; test 15 already asserts the substring everywhere, so the guard is unchanged."}
{"category":"ambiguity","class":"per-command-artifact-drift","finding":"Item 7 applies one identical artifact triple — no `.tp-review/\u003cspec\u003e/`, no `state.json`, no snapshot — to `tp review` and `tp audit` \"separately\", but §2.5 item 2 pins different pre-write boundaries per command: `EnsureReviewState` (which creates the directory and `state.json`) for review, and \"the snapshot\" for audit. Nothing in the spec says audit's emission path creates `state.json` at all, so for the audit half the assertion is either a real guard or a vacuous one, and the spec does not say which. Two engineers write different audit assertions, and a vacuous one gives false confidence that audit's guard is covered.","found_by":1,"found_by_roles":["tester"],"location":"§6","resolved":{"evidence":"§6 item 8 now names `tp review --verify` among the modes asserted and pins the assertion to the two literal messages of §2.5. Item 10 covers the auditor-only scope, the review-phase path, and the two-refusal precedence.","resolved_at":"2026-08-05T18:51:47Z","status":"fixed"},"role":"tester","severity":"low","suggestion":"Split item 7's assertions per command: for `tp review` assert the directory, `state.json` and the snapshot are absent; for `tp audit` assert the snapshot (and the directory it would create) is absent — matching §2.5 item 2's per-command boundary."}
