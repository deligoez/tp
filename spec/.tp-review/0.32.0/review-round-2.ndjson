{"category":"completeness","class":"reader-list-drift","finding":"Changing the field type breaks the legacy shim's signature, which §2.2 never mentions. In `ResolveOverrideFocus` (internal/engine/overrides.go) the one `overrides` variable is assigned both from `fm.ReviewRoles` (line 28) and from `TranslateLegacyLens(fm, ids)` (line 37), whose declared return type is `map[string][]string` (internal/engine/lens_shim.go:29); the focus layering then treats the value as a slice (`if extra := overrides[roles[i].ID]; len(extra) \u003e 0`, line 63). Making the frontmatter fields `map[string]RoleOverride` therefore forces a decision the spec does not state: change `TranslateLegacyLens`'s return type, or adapt it at the call site, and rework the layering to read `.Focus`. §2.2 instead describes this reader as \"TranslateLegacyLens's caller, which compares against the new map only for emptiness\", but the emptiness compare is inside `TranslateLegacyLens` itself (lens_shim.go:37), and its caller is precisely the code that assigns the incompatible type.","found_by":1,"found_by_roles":["implementer"],"location":"§2.2","resolved":{"evidence":"§2.2 rewritten on the evidence. The earlier reader list was wrong at two of three entries: there is no separate lint pass for the unknown-id warning (ResolveOverrideFocus emits it itself), and the third site is TranslateLegacyLens, whose declared return type is map[string][]string and whose result is assigned to the same `overrides` variable as fm.ReviewRoles — so it must change type or be adapted or the package will not compile. parseRoleOverrides is named as the writer.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"implementer","severity":"high","suggestion":"State the shim's new contract: either `TranslateLegacyLens` keeps returning focus-only overrides and `ResolveOverrideFocus` wraps them into `RoleOverride{Focus: ...}`, or the shim returns `map[string]RoleOverride` with `Enabled` nil. Also state that the layering reads `.Focus` and that a `RoleOverride` with an empty `Focus` layers nothing."}
{"category":"regression","class":"code-citation-drift","finding":"The reader enumeration added to fix the round-1 seam finding is wrong at two of its three entries and omits a third site. (a) There is no separate \"lint pass that reports unknown override ids\": that warning is emitted inside ResolveOverrideFocus itself (internal/engine/overrides.go:49-58, `tp.%s override for %q matches no active %s role; ignored`), which §2.2 already names separately — so one of the three named readers does not exist as a distinct site. (b) `TranslateLegacyLens`'s caller does NOT \"compare against the new map only for emptiness\": the emptiness comparison is inside TranslateLegacyLens (internal/engine/lens_shim.go:37, `if len(fm.ReviewRoles) \u003e 0 || len(fm.AuditRoles) \u003e 0`), while the caller assigns the shim's `map[string][]string` return straight into the same `overrides` variable that receives `fm.ReviewRoles` (overrides.go:28 and overrides.go:37). So TranslateLegacyLens's own return type must change to `map[string]RoleOverride` in lockstep — which is exactly what the round-1 finding said and what §2.2 now denies. (c) The enumeration omits `parseRoleOverrides` and the zero-value constructor in internal/engine/frontmatter.go (lines 31-32, 120, 123, 249-250), which produce the maps and whose signatures change too. An implementer following §2.2 literally will not touch the shim's return type and will hunt for a lint pass that is not there.","found_by":1,"found_by_roles":["architect"],"location":"§2.2","resolved":{"evidence":"§2.2 rewritten on the evidence. The earlier reader list was wrong at two of three entries: there is no separate lint pass for the unknown-id warning (ResolveOverrideFocus emits it itself), and the third site is TranslateLegacyLens, whose declared return type is map[string][]string and whose result is assigned to the same `overrides` variable as fm.ReviewRoles — so it must change type or be adapted or the package will not compile. parseRoleOverrides is named as the writer.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"regression","severity":"high","suggestion":"Replace the three-item list with the sites that exist: ResolveOverrideFocus (overrides.go — override selection, the unknown-id warning, and focus layering), parseRoleOverrides plus the Frontmatter zero-value constructor (frontmatter.go), and TranslateLegacyLens itself (lens_shim.go), whose return type becomes map[string]RoleOverride with Enabled left nil because the shim cannot express deactivation. State that the value-shape assertions in internal/engine/frontmatter_roleoverrides_test.go and lens_shim_test.go are rewritten with it."}
{"category":"consistency","class":"mode-list-drift","finding":"Clause 1's mode list is not per-command, and test 8 inherits the error. `tp audit` registers only --affected-files, --affected-from-tasks, --base, --findings, --record, --status, --check, --harness-note, --merge and -o (internal/cli/audit.go:158-167): it has no --resolve, --resolve-all, --verify or --report. Test 8 (\"still exits normally under --record, --status, --status --check, --merge, --resolve, --verify and --report, for both tp review and tp audit\") therefore cannot be written — those invocations are unknown-flag usage errors on tp audit — and it also omits --resolve-all, which clause 1 does list. Clause 1's rationale is inaccurate too: --merge/--resolve/--resolve-all explicitly reject a spec-looking positional (internal/cli/mode_positionals.go, with exit-2 tests), so it is not true that \"every mode takes the same spec argument and parses the same frontmatter\".","found_by":1,"found_by_roles":["implementer"],"location":"§2.5","resolved":{"evidence":"§2.5 rewritten twice over on this round's evidence. (1) The mode list is now PER COMMAND, because tp audit registers no --resolve, --resolve-all, --verify or --report — an earlier list implied flags that do not exist and §6 item 8 inherited the error. (2) The ordering clause now covers every write the emission path performs, not just the snapshot: tp review's loadReviewRoundState calls EnsureReviewState, which creates .tp-review/\u003cspec\u003e/ and saves a fresh state.json BEFORE WriteSnapshotAtomic runs, so resolution moves ahead of that too and §6 item 7 asserts all three absences.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"implementer","severity":"high","suggestion":"Split the list per command (review: --record/--status[--check]/--merge/--resolve/--resolve-all/--verify/--report; audit: --record/--status[--check]/--merge), correct the rationale to \"the modes that do take the spec positional parse the same frontmatter, and the NDJSON-positional modes never see it\", and rewrite test 8 with the per-command mode sets."}
{"category":"feasibility","class":"partial-state-write-ordering","finding":"Clause 2 moves resolution ahead of the snapshot write only, but the snapshot is not the first write. In review, `loadReviewRoundState` calls `engine.EnsureReviewState(specPath)` at internal/cli/review.go:1616 — which `os.MkdirAll`s `.tp-review/\u003cspec\u003e/` and `SaveReviewState`s a fresh state.json (internal/engine/reviewstate.go:141-157) — before `WriteSnapshotAtomic` at line 1629. A refusal decided after that ordering change still exits 2 with a state directory and a state.json newly created on disk, so \"both refusals are decidable while nothing has been written\" remains false for a first-ever review round, and test 7's snapshot-only assertion would not catch it.","found_by":1,"found_by_roles":["implementer"],"location":"§2.5","resolved":{"evidence":"§2.5 rewritten twice over on this round's evidence. (1) The mode list is now PER COMMAND, because tp audit registers no --resolve, --resolve-all, --verify or --report — an earlier list implied flags that do not exist and §6 item 8 inherited the error. (2) The ordering clause now covers every write the emission path performs, not just the snapshot: tp review's loadReviewRoundState calls EnsureReviewState, which creates .tp-review/\u003cspec\u003e/ and saves a fresh state.json BEFORE WriteSnapshotAtomic runs, so resolution moves ahead of that too and §6 item 7 asserts all three absences.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"implementer","severity":"high","suggestion":"Name every pre-emission write the refusals must precede — for review, `EnsureReviewState` as well as `WriteSnapshotAtomic` — or weaken the claim to \"no round snapshot and no state index entry is written\" and extend test 7 to assert the `.tp-review/\u003cspec\u003e/` directory is absent after a refusal on a spec with no prior state."}
{"category":"completeness","class":"role-attribution-lost-in-dedup","finding":"\"A recorded finding attributed to that role\" and \"deduped by the same key the round-carry uses\" cannot both hold for review. The review round-carry is `loadReviewRoundState` (internal/cli/review.go:1651-1663), which converts rows via `findingFromRow` — that function copies severity, category, class, location, finding, suggestion and resolved, and never reads `role` — then dedups with `dedupFindings`, whose key is `findingIdentityKey(category, location, finding-prefix)` (review.go:946, 973-985). Role is dropped both from the row and from the key, so two roles reporting the same (category, location, finding) collapse into one entry and the surviving entry carries no role at all. Reusing that key means a role's open finding can be invisible to the guard exactly when another role reported the same thing — the bypass §2.6 exists to close.","found_by":1,"found_by_roles":["implementer"],"location":"§2.6","resolved":{"evidence":"§2.6 rewritten as two phase-specific arms, because the attempt to unify them was the source of the errors. Review: the predicate is reviewFindingResolvedAway AND the severity filter ReviewRoundClean applies under the effective review_converge_on — the earlier 'exactly the set that keeps a round from being clean' was false since v0.31.0, where a medium finding does not block; the scan covers every recorded round (matching loadReviewRoundState); role attribution comes from the recorded row's own `role` field, not the (location, class) dedup key which carries no role; the escape hatch is tp review --resolve against the recorded round file. Audit: the scan is the LATEST round only (matching loadAuditPriorRound), the predicate is non-PASS, and there is no escape command because tp audit has no --resolve — a row stops blocking when a later recorded round reports PASS, so the refusal points at the audit loop. The exit code moves from 2 to 4: the trigger is recorded state, and tp reserves 4 for state. --no-state is addressed: the guard needs state, so under it the guard is skipped WITH a warning rather than silently permitting the deactivation.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"implementer","severity":"high","suggestion":"State the guard's key literally rather than by reference, e.g. \"dedup within each role's rows by (role, category, location, finding)\" for review and \"(role, item_id)\" for audit, and note that the guard reads `role` straight from the recorded rows (which carry it — the record path warns on rows missing `role`) rather than through `findingFromRow`."}
{"category":"ambiguity","class":"undefined-dedup-key","finding":"\"deduped by the same key the round-carry uses\" names no key, and the two phases do not use the same one: audit's carry (`loadAuditPriorRound`) keys on (role, item_id) over the latest round only, while review rows dedup on (location, class) — a key that carries no role at all. Because the guard is per-role and now scans every recorded round, three things are left undecidable: whether dedup runs before or after filtering by role attribution; which round's resolution state wins when one key appears in several rounds; and, for review, what happens when two roles reported the same (location, class). The ordinary workflow case — a finding unresolved in round 1 and resolved in round 2 — has no defined answer and no §6 item, so two engineers write contradictory tests for it.","found_by":1,"found_by_roles":["tester"],"location":"§2.6","resolved":{"evidence":"§2.6 rewritten as two phase-specific arms, because the attempt to unify them was the source of the errors. Review: the predicate is reviewFindingResolvedAway AND the severity filter ReviewRoundClean applies under the effective review_converge_on — the earlier 'exactly the set that keeps a round from being clean' was false since v0.31.0, where a medium finding does not block; the scan covers every recorded round (matching loadReviewRoundState); role attribution comes from the recorded row's own `role` field, not the (location, class) dedup key which carries no role; the escape hatch is tp review --resolve against the recorded round file. Audit: the scan is the LATEST round only (matching loadAuditPriorRound), the predicate is non-PASS, and there is no escape command because tp audit has no --resolve — a row stops blocking when a later recorded round reports PASS, so the refusal points at the audit loop. The exit code moves from 2 to 4: the trigger is recorded state, and tp reserves 4 for state. --no-state is addressed: the guard needs state, so under it the guard is skipped WITH a warning rather than silently permitting the deactivation.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"high","suggestion":"Name the guard's key explicitly per phase (e.g. review (role, location, class); audit (role, item_id)), state that the latest recorded occurrence of a key determines its resolution state, and add a test item for \"unresolved in round 1, resolved in round 2 → deactivation proceeds\"."}
{"category":"consistency","class":"predicate-scope-composition","finding":"For review, the per-round convergence predicate composed over every recorded round is not the same predicate and permanently blocks deactivation. `engine.reviewFindingResolvedAway` (internal/engine/reviewclean.go:32) returns true only for `wontfix` or `duplicate` with non-empty evidence — test 10 confirms a finding resolved `fixed` still blocks. Convergence tolerates that because `ReviewConsecutiveClean` only inspects trailing rounds, so a round-1 finding fixed in round 2 stops mattering. Scanning every recorded round removes that escape: any reviewer that ever reported a genuine defect that was then fixed carries an \"unresolved\" row forever and can never be deactivated. That defeats §1's motivation (declining a role that is noise on this spec) and pushes users toward marking genuinely fixed findings `wontfix` to falsify the record.","found_by":1,"found_by_roles":["implementer"],"location":"§2.6","resolved":{"evidence":"§2.6 rewritten as two phase-specific arms, because the attempt to unify them was the source of the errors. Review: the predicate is reviewFindingResolvedAway AND the severity filter ReviewRoundClean applies under the effective review_converge_on — the earlier 'exactly the set that keeps a round from being clean' was false since v0.31.0, where a medium finding does not block; the scan covers every recorded round (matching loadReviewRoundState); role attribution comes from the recorded row's own `role` field, not the (location, class) dedup key which carries no role; the escape hatch is tp review --resolve against the recorded round file. Audit: the scan is the LATEST round only (matching loadAuditPriorRound), the predicate is non-PASS, and there is no escape command because tp audit has no --resolve — a row stops blocking when a later recorded round reports PASS, so the refusal points at the audit loop. The exit code moves from 2 to 4: the trigger is recorded state, and tp reserves 4 for state. --no-state is addressed: the guard needs state, so under it the guard is skipped WITH a warning rather than silently permitting the deactivation.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"implementer","severity":"high","suggestion":"Say what makes a cross-round review row stop counting. The natural rule is that a row is cleared when it is resolved with any status plus evidence (fixed included), or when the same (role, key) row does not reappear in the most recent recorded round; either way, state it and align test 10 with it instead of asserting the round-local convergence predicate unchanged across all rounds."}
{"category":"feasibility","class":"unsatisfiable-escape-hatch","finding":"The audit half of the guard has no escape hatch that exists in the tree. §2.6 defines the audit predicate as \"a recorded row attributed to that role whose status is not PASS\" scanned over every recorded round, and test 9 requires that \"resolving that row in the recorded file lets the same deactivation succeed\" — but `tp audit` registers no `--resolve`/`--resolve-all` flag (its flags are --affected-files, --affected-from-tasks, --base, --findings, --record, --status, --check, --harness-note, --merge, -o), and `tp review --resolve` only stamps a `resolved` object, which does not change a row's `status`. Under the stated predicate a row stays non-PASS forever: the fix shows up as a PASS row in a later round while the old round's FAIL row remains, so any auditor that ever reported one FAIL can never be deactivated. The only way to satisfy the spec as written is hand-rewriting a recorded round file, which the project forbids and which tp never does elsewhere.","found_by":1,"found_by_roles":["implementer"],"location":"§2.6","resolved":{"evidence":"§2.6 rewritten as two phase-specific arms, because the attempt to unify them was the source of the errors. Review: the predicate is reviewFindingResolvedAway AND the severity filter ReviewRoundClean applies under the effective review_converge_on — the earlier 'exactly the set that keeps a round from being clean' was false since v0.31.0, where a medium finding does not block; the scan covers every recorded round (matching loadReviewRoundState); role attribution comes from the recorded row's own `role` field, not the (location, class) dedup key which carries no role; the escape hatch is tp review --resolve against the recorded round file. Audit: the scan is the LATEST round only (matching loadAuditPriorRound), the predicate is non-PASS, and there is no escape command because tp audit has no --resolve — a row stops blocking when a later recorded round reports PASS, so the refusal points at the audit loop. The exit code moves from 2 to 4: the trigger is recorded state, and tp reserves 4 for state. --no-state is addressed: the guard needs state, so under it the guard is skipped WITH a warning rather than silently permitting the deactivation.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"implementer","severity":"high","suggestion":"Name the audit clearing mechanism explicitly. Either scope the audit predicate to rows whose item is still non-PASS in the most recent round that mentions it (a later PASS for the same (role,item_id) supersedes an earlier FAIL), or specify a resolution surface for audit rows (a `tp audit --resolve` mode, or accepting a `resolved` object on audit rows and treating resolved-with-evidence rows as cleared). Restate test 9 against whichever mechanism is chosen."}
{"category":"completeness","class":"undefined-escape-operation","finding":"The escape-hatch paragraph is written entirely for review (\"tp review --resolve takes an arbitrary findings NDJSON as its positional\"), but the audit arm of the same refusal has no equivalent: `tp audit` implements no --resolve/--resolve-all, and an audit row stops blocking only when its `status` becomes PASS. §6 item 9 nonetheless asserts the round trip — \"resolving that row in the recorded file lets the same deactivation succeed\" — without naming the operation that does it (hand-edit `audit-round-N.ndjson`? re-record the round from a corrected results file? --merge then --record?). The item cannot be written without inventing an interface, and naming the round file, which §2.6 calls \"what makes the escape hatch reachable\", is not sufficient when no command acts on it.","found_by":1,"found_by_roles":["tester"],"location":"§2.6","resolved":{"evidence":"§2.6 rewritten as two phase-specific arms, because the attempt to unify them was the source of the errors. Review: the predicate is reviewFindingResolvedAway AND the severity filter ReviewRoundClean applies under the effective review_converge_on — the earlier 'exactly the set that keeps a round from being clean' was false since v0.31.0, where a medium finding does not block; the scan covers every recorded round (matching loadReviewRoundState); role attribution comes from the recorded row's own `role` field, not the (location, class) dedup key which carries no role; the escape hatch is tp review --resolve against the recorded round file. Audit: the scan is the LATEST round only (matching loadAuditPriorRound), the predicate is non-PASS, and there is no escape command because tp audit has no --resolve — a row stops blocking when a later recorded round reports PASS, so the refusal points at the audit loop. The exit code moves from 2 to 4: the trigger is recorded state, and tp reserves 4 for state. --no-state is addressed: the guard needs state, so under it the guard is skipped WITH a warning rather than silently permitting the deactivation.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"high","suggestion":"State for audit exactly how a recorded non-PASS row is cleared (most plausibly: re-record the round from a results file in which the row is PASS), and rewrite item 9's second clause to assert that operation by name."}
{"category":"feasibility","class":"escape-hatch-unreachable","finding":"The rewrite fixed the \"which file\" half of the round-1 escape-hatch finding and left the \"which command\" half open for audit. §2.6 names `tp review --resolve` and the recorded round file, but audit rows carry a `status` (PASS/FAIL/PARTIAL), not a `resolved` block, and there is no `tp audit --resolve` or `--resolve-all` (audit.go:158-167). Item 9's \"resolving that row in the recorded file lets the same deactivation succeed\" therefore has no command behind it: the only way to clear a recorded audit FAIL is to hand-edit .tp-review/\u003cspec\u003e/audit-round-N.ndjson, which CLAUDE.md's unit scope fence explicitly forbids. The alternative — re-running `tp audit --record` so the role reports PASS — is undecided, because §2.6 says the scan covers every recorded round \"deduped by the same key the round-carry uses\" without saying which row wins when round N holds a FAIL and round N+1 a PASS for the same (role,item_id). Under a first-wins or any-round-non-PASS reading the block is permanent and the audit half of the feature is unreachable once a role has ever reported a non-PASS.","found_by":0,"location":"§2.6","resolved":{"evidence":"§2.6 rewritten as two phase-specific arms, because the attempt to unify them was the source of the errors. Review: the predicate is reviewFindingResolvedAway AND the severity filter ReviewRoundClean applies under the effective review_converge_on — the earlier 'exactly the set that keeps a round from being clean' was false since v0.31.0, where a medium finding does not block; the scan covers every recorded round (matching loadReviewRoundState); role attribution comes from the recorded row's own `role` field, not the (location, class) dedup key which carries no role; the escape hatch is tp review --resolve against the recorded round file. Audit: the scan is the LATEST round only (matching loadAuditPriorRound), the predicate is non-PASS, and there is no escape command because tp audit has no --resolve — a row stops blocking when a later recorded round reports PASS, so the refusal points at the audit loop. The exit code moves from 2 to 4: the trigger is recorded state, and tp reserves 4 for state. --no-state is addressed: the guard needs state, so under it the guard is skipped WITH a warning rather than silently permitting the deactivation.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"regression","severity":"high","suggestion":"State the audit clearing path explicitly: either the dedup keeps the latest recorded row per (role,item_id) so a subsequent PASS clears the block (and item 9 asserts that sequence rather than editing a file), or the release adds the missing audit resolve surface. Say which, and make item 9 exercise a command tp actually has."}
{"category":"consistency","class":"exit-code-contract-drift","finding":"The unresolved-findings refusal is specified as exit 2 (usage), but its trigger is recorded state in .tp-review/ (rows in review-round-N.ndjson / audit-round-N.ndjson), not a malformed invocation. tp's exit-code table reserves 2 for usage and 4 for state, and the closest existing precedent in the very same emission path — refuseIfBudgetExhausted (internal/cli/budget.go:34, reached from refuseAuditIfBudgetExhausted and its review twin), which also refuses prompt emission because recorded rounds say so — exits ExitState (4). A loop driver branches on the exit code to decide whether to fix its command line or to go edit recorded state; §2.6 as written tells it the wrong thing. §2.5 (spec frontmatter asked for something impossible) is genuinely usage and can stay at 2, so the spec would then use two different codes for two refusals it otherwise treats as a pair — that has to be stated deliberately, not left implicit.","found_by":1,"found_by_roles":["architect"],"location":"§2.6","resolved":{"evidence":"§2.6 rewritten as two phase-specific arms, because the attempt to unify them was the source of the errors. Review: the predicate is reviewFindingResolvedAway AND the severity filter ReviewRoundClean applies under the effective review_converge_on — the earlier 'exactly the set that keeps a round from being clean' was false since v0.31.0, where a medium finding does not block; the scan covers every recorded round (matching loadReviewRoundState); role attribution comes from the recorded row's own `role` field, not the (location, class) dedup key which carries no role; the escape hatch is tp review --resolve against the recorded round file. Audit: the scan is the LATEST round only (matching loadAuditPriorRound), the predicate is non-PASS, and there is no escape command because tp audit has no --resolve — a row stops blocking when a later recorded round reports PASS, so the refusal points at the audit loop. The exit code moves from 2 to 4: the trigger is recorded state, and tp reserves 4 for state. --no-state is addressed: the guard needs state, so under it the guard is skipped WITH a warning rather than silently permitting the deactivation.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"architect","severity":"high","suggestion":"Make §2.6's refusal exit 4 (state) and say why it differs from §2.5's exit 2: §2.5 is a bad declaration, §2.6 is a state precondition. State the pairing explicitly in §2.7 and add the code to tests 9 and 10. If exit 2 is kept for both, justify it against the exit-code table and against refuseIfBudgetExhausted's exit 4, since an agent cannot distinguish 'fix your flags' from 'go resolve findings' otherwise."}
{"category":"consistency","class":"convergence-predicate-mismatch","finding":"§2.6 defines the review blocking set as every recorded finding that `reviewFindingResolvedAway` does not treat as resolved away, and asserts \"This is exactly the set that keeps a round from being clean\". It is not. Since v0.31.0 `ReviewRoundClean` filters the surviving set through `reviewSeverityBlocking` under the default `review_converge_on=blocking` policy, so a surviving low/medium finding does NOT keep a round from being clean; only `review_converge_on=all` makes the two sets coincide. The guard as specified would therefore block deactivation on findings the convergence gate ignores, and §6 item 10's \"matching the convergence predicate exactly\" is unassertable: a test with one open `low` finding has two contradictory correct answers, and no §6 item pins `review_converge_on` at all.","found_by":1,"found_by_roles":["tester"],"location":"§2.6","resolved":{"evidence":"§2.6 rewritten as two phase-specific arms, because the attempt to unify them was the source of the errors. Review: the predicate is reviewFindingResolvedAway AND the severity filter ReviewRoundClean applies under the effective review_converge_on — the earlier 'exactly the set that keeps a round from being clean' was false since v0.31.0, where a medium finding does not block; the scan covers every recorded round (matching loadReviewRoundState); role attribution comes from the recorded row's own `role` field, not the (location, class) dedup key which carries no role; the escape hatch is tp review --resolve against the recorded round file. Audit: the scan is the LATEST round only (matching loadAuditPriorRound), the predicate is non-PASS, and there is no escape command because tp audit has no --resolve — a row stops blocking when a later recorded round reports PASS, so the refusal points at the audit loop. The exit code moves from 2 to 4: the trigger is recorded state, and tp reserves 4 for state. --no-state is addressed: the guard needs state, so under it the guard is skipped WITH a warning rather than silently permitting the deactivation.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"high","suggestion":"Decide and state whether the guard is severity-aware. Either (a) define the blocking set as the phase's effective convergence predicate including `review_converge_on` (surviving AND blocking under `blocking`; surviving under `all`) and add a test item pinning both policies with a surviving `low` finding, or (b) keep the any-surviving-finding set, drop the \"exactly the set that keeps a round from being clean\" claim, and say the guard is deliberately stricter than convergence."}
{"category":"completeness","class":"flag-contract-conflict","finding":"§2.6's guard requires reading every recorded round of the phase, but `tp review --no-state` is documented as 'Disable all review-state reads and writes (pre-0.23.0 manual behavior)' and, in the default emission path, skips loadReviewRoundState entirely (internal/cli/review.go:357-362) — no snapshot, no state read — while still resolving the corpus and applying overrides in buildReviewPrompts, which receives noState. The spec never mentions --no-state, so both resolutions are open and both are bad: skip the guard under --no-state and a role with open recorded findings is silently deactivated with no guard at all — exactly the hole §2.5's emission-only scoping was rewritten to avoid; run the guard and --no-state reads .tp-review/ in violation of its own contract. §2.5 rule 2's 'nothing has been written' rationale is also vacuous under --no-state, since no snapshot is written there in the first place.","found_by":1,"found_by_roles":["architect"],"location":"§2.6","resolved":{"evidence":"§2.6 rewritten as two phase-specific arms, because the attempt to unify them was the source of the errors. Review: the predicate is reviewFindingResolvedAway AND the severity filter ReviewRoundClean applies under the effective review_converge_on — the earlier 'exactly the set that keeps a round from being clean' was false since v0.31.0, where a medium finding does not block; the scan covers every recorded round (matching loadReviewRoundState); role attribution comes from the recorded row's own `role` field, not the (location, class) dedup key which carries no role; the escape hatch is tp review --resolve against the recorded round file. Audit: the scan is the LATEST round only (matching loadAuditPriorRound), the predicate is non-PASS, and there is no escape command because tp audit has no --resolve — a row stops blocking when a later recorded round reports PASS, so the refusal points at the audit loop. The exit code moves from 2 to 4: the trigger is recorded state, and tp reserves 4 for state. --no-state is addressed: the guard needs state, so under it the guard is skipped WITH a warning rather than silently permitting the deactivation.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"architect","severity":"high","suggestion":"Add a sentence to §2.6 settling --no-state. The defensible choice is to run the guard anyway (it is a read of recorded state that the refusal exists to protect) and to narrow --no-state's documented contract to 'disables state-derived round numbering, snapshots and finding carry', or alternatively to refuse `--no-state` together with an `enabled: false` in the frontmatter. Pin whichever is chosen with a test alongside tests 9-11."}
{"category":"consistency","class":"unverified-fallback-claim","finding":"The rewrite promotes a claim round 1 disproved into required documentation text and a guard test. §4 requires REFERENCE.md to state that \"deleting the role file removes it from every spec\", and item 16 asserts REFERENCE.md \"states both trim-candidate levers\". That is false at the boundary: ResolveActiveCorpus (internal/engine/corpusdomain.go:31-59) falls back to the domain-selected embedded corpus whenever LoadRoleCorpus reports the phase unpopulated, so deleting the LAST file in .tp/reviewers/ or .tp/auditors/ does not empty the panel — it restores the full embedded default panel. For an ejected default role (tp's own repo runs tuned implementer/tester/architect files) the deleted role comes straight back; for a custom role the deletion silently re-adds three embedded roles the project had replaced, i.e. the advertised trimming lever makes the panel larger. Round 1 raised precisely this against the old Non-Goal 4, and the recorded resolution answered only the allowlist half of that entry, so §5 item 5 still carries the unqualified \"delete or stop committing its file\" — and the rewrite has now copied the same wrong claim into a doc contract with a test behind it.","found_by":0,"location":"§4","resolved":{"evidence":"§4 corrected on the evidence: the claim that deleting a role file removes the role from every spec is false at the boundary, because ResolveActiveCorpus falls back to the embedded default corpus when the deletion empties the phase. The required REFERENCE.md text now states that exception, so the documentation and §6's guard no longer promote a claim round 1 had already disproved.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"regression","severity":"high","suggestion":"Qualify both places: deleting a role file removes it from every spec only while at least one file remains in that phase directory; emptying the directory restores the embedded default panel (ResolveActiveCorpus's unpopulated-phase fallback). Either state the qualification in §4's required REFERENCE.md text and in §5 item 5, or drop the delete-the-file lever from the documented guidance so item 16 does not guard a false sentence."}
{"category":"feasibility","class":"nonexistent-mode-cited","finding":"New item 8 is unwritable as stated and reintroduces a premise round 1 disproved. It requires the deactivating spec to \"still exit normally under `--record`, `--status`, `--status --check`, `--merge`, `--resolve`, `--verify` and `--report`, for both `tp review` and `tp audit`\". `tp audit` registers only --affected-files, --affected-from-tasks, --base, --findings, --record, --status, --check, --harness-note, --merge and -o (internal/cli/audit.go:158-167): it has no --resolve, no --resolve-all, no --verify and no --report. A test asserting `tp audit \u003cspec\u003e --resolve` \"exits normally\" asserts cobra's unknown-flag error. Round 1 established this fact against the old §2.6 hint (\"there is no `tp audit --resolve` at all\"); the rewrite carried it back in through the very item written to fix the scoping problem. Item 8 also drops `--resolve-all`, which §2.5's own list of unaffected modes includes, so the test does not cover the mode list the section states.","found_by":0,"location":"§6","resolved":{"evidence":"§6 (Tests) corrected on this round's evidence. Item 7 asserts the absence of the directory, the state file AND the snapshot, since the snapshot is not the first write. Item 8 enumerates modes per command, dropping the audit flags that do not exist. Item 9 is the audit arm and its escape is recording a later PASS round, not a resolve call. Item 10 is the review arm and covers all five decisive branches of reviewFindingResolvedAway plus the severity boundary. Item 11 states both scan ranges and their opposite expectations. A new item covers --no-state skipping the guard with a warning.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"regression","severity":"high","suggestion":"Split item 8 by command: for `tp review`, assert --record, --status, --status --check, --merge, --resolve, --resolve-all, --verify and --report; for `tp audit`, assert only --record, --status, --status --check and --merge. Align §2.5's point 1 the same way, naming the modes per command rather than as one list applied to both."}
{"category":"completeness","class":"predicate-boundary-untested","finding":"Item 10 exercises three points — not-resolved-away blocks, `wontfix` with evidence does not block, `fixed` blocks — but `reviewFindingResolvedAway` has two more decisive branches and neither is covered: `duplicate` with evidence (must not block) and `wontfix`/`duplicate` with empty or whitespace-only evidence (must block). An implementation that tested only `status == \"wontfix\"`, or that ignored the evidence clause entirely, passes all three of item 10's cases. The item claims to pin the predicate but cannot fail on a wrong one, which is the failure mode it was written to prevent.","found_by":1,"found_by_roles":["tester"],"location":"§6 item 10","resolved":{"evidence":"§6 (Tests) corrected on this round's evidence. Item 7 asserts the absence of the directory, the state file AND the snapshot, since the snapshot is not the first write. Item 8 enumerates modes per command, dropping the audit flags that do not exist. Item 9 is the audit arm and its escape is recording a later PASS round, not a resolve call. Item 10 is the review arm and covers all five decisive branches of reviewFindingResolvedAway plus the severity boundary. Item 11 states both scan ranges and their opposite expectations. A new item covers --no-state skipping the guard with a warning.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"high","suggestion":"Turn item 10 into a table over the predicate's branches — no `resolved` block; `fixed`+evidence; `wontfix`+evidence; `duplicate`+evidence; `wontfix`+empty evidence; `wontfix`+whitespace-only evidence — naming block/no-block for each."}
{"category":"feasibility","class":"unwritable-test-item","finding":"Item 8 requires the same spec to \"still exit normally under --record, --status, --status --check, --merge, --resolve, --verify and --report, for both tp review and tp audit\". Most of those combinations cannot be written. `tp audit` defines no --resolve, --verify or --report flag (its flag set is --affected-files/--affected-from-tasks/--base/--findings/--record/--status/--check/--harness-note/--merge/-o), so cobra rejects each with exit 2 — the exact code the item exists to prove absent. And `tp review --merge`, `--resolve` and `--report` take the findings/round NDJSON as their positional, never the spec, so \"the same spec\" is never passed or parsed and those assertions are vacuous: they would pass identically on a build that wired the refusal into every mode. Item 8 also omits `--resolve-all`, which §2.5's own enumeration includes.","found_by":1,"found_by_roles":["tester"],"location":"§6 item 8","resolved":{"evidence":"§6 (Tests) corrected on this round's evidence. Item 7 asserts the absence of the directory, the state file AND the snapshot, since the snapshot is not the first write. Item 8 enumerates modes per command, dropping the audit flags that do not exist. Item 9 is the audit arm and its escape is recording a later PASS round, not a resolve call. Item 10 is the review arm and covers all five decisive branches of reviewFindingResolvedAway plus the severity boundary. Item 11 states both scan ranges and their opposite expectations. A new item covers --no-state skipping the guard with a warning.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"high","suggestion":"Split item 8 by command and by mode class: for spec-taking modes (review --record/--status/--status --check/--verify; audit --record/--status/--status --check) assert the exact non-refusal outcome; for the NDJSON-positional modes (--merge/--resolve/--resolve-all/--report) assert instead that no corpus resolution happens when invoked as documented (no override warning, no snapshot written); and drop the modes `tp audit` does not implement, aligning §2.5's enumeration with the same split."}
{"category":"consistency","class":"savings-arithmetic-overstated","finding":"The closing sentence multiplies the per-round saving twice: 'Multiplied by the required clean rounds, and again by each round the loop does not converge on.' Clean rounds and non-converging rounds are disjoint subsets of the same round sequence, so the correct factor is their sum (total rounds the phase runs), not their product. Read literally, the headline saving in the one section whose job is honest arithmetic is inflated by a factor of roughly audit_clean_rounds (2 by default).","found_by":1,"found_by_roles":["ax-economist"],"location":"§1.1","resolved":{"evidence":"§6 (Tests) corrected on this round's evidence. Item 7 asserts the absence of the directory, the state file AND the snapshot, since the snapshot is not the first write. Item 8 enumerates modes per command, dropping the audit flags that do not exist. Item 9 is the audit arm and its escape is recording a later PASS round, not a resolve call. Item 10 is the review arm and covers all five decisive branches of reviewFindingResolvedAway plus the severity boundary. Item 11 states both scan ranges and their opposite expectations. A new item covers --no-state skipping the guard with a warning.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"ax-economist","severity":"medium","suggestion":"State the factor once: saving = per-round saving x total rounds the phase runs (rounds that surface findings plus the required clean rounds, audit_clean_rounds = 2 by default). If a worked number is wanted, give one concrete example instead of two chained multipliers."}
{"category":"consistency","class":"deleted-not-resolved","finding":"The round-1 timing collision was resolved by deleting the section that surfaced it, not by resolving it, and §1.1's saving claim is now stated as if the collision did not exist. §1.1 says \"The saving is one sub-agent round per declined role per phase round\" unconditionally. But a role is only observed to be noise after it has emitted findings in a round, and at that moment those findings are freshly recorded and unresolved — so §2.6 refuses the deactivation until they are resolved, and per the §2.6 finding above the audit phase has no command for that. The old §3 stated the same collision from the advice side and was deleted as Non-Goal 2; §4 even concedes it (\"a role carrying unresolved findings must be resolved before either\"). So the constraint survives, is documented in §4, and is contradicted by the release's headline saving claim in §1.1.","found_by":0,"location":"§1.1","resolved":{"evidence":"§6 (Tests) corrected on this round's evidence. Item 7 asserts the absence of the directory, the state file AND the snapshot, since the snapshot is not the first write. Item 8 enumerates modes per command, dropping the audit flags that do not exist. Item 9 is the audit arm and its escape is recording a later PASS round, not a resolve call. Item 10 is the review arm and covers all five decisive branches of reviewFindingResolvedAway plus the severity boundary. Item 11 states both scan ranges and their opposite expectations. A new item covers --no-state skipping the guard with a warning.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"regression","severity":"medium","suggestion":"Add one sentence to §1.1: the saving is realized from the next round after the role's outstanding findings are resolved, or immediately for a role that has recorded nothing on this spec. That makes §1.1, §2.6 and §4 agree and prices the feature honestly."}
{"category":"consistency","class":"cost-accounting-gap","finding":"§1.1's cost side was written against the pre-rewrite scope and understates the release the rest of the spec now describes. It enumerates \"one optional frontmatter key, one new `skipped_roles` reason, and two refusals that only fire on a mistake\", but the same round added §2.5 point 2 — moving corpus and override resolution ahead of the round-snapshot write in BOTH `tp review` and `tp audit` — which is a control-flow change to the state-writing order of two commands and by far the riskiest item in the release, plus §2.2's map-to-struct migration across the frontmatter parser, the shim and their tests, plus §2.3's signature change to ResolveOverrideFocus, plus §2.6's new scan over every recorded round file on every emission. A cost section whose stated purpose is to make the trade judgeable omits the four costliest changes in it.","found_by":0,"location":"§1.1","resolved":{"evidence":"§6 (Tests) corrected on this round's evidence. Item 7 asserts the absence of the directory, the state file AND the snapshot, since the snapshot is not the first write. Item 8 enumerates modes per command, dropping the audit flags that do not exist. Item 9 is the audit arm and its escape is recording a later PASS round, not a resolve call. Item 10 is the review arm and covers all five decisive branches of reviewFindingResolvedAway plus the severity boundary. Item 11 states both scan ranges and their opposite expectations. A new item covers --no-state skipping the guard with a warning.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"regression","severity":"medium","suggestion":"Extend the cost sentence to name the ordering change in both commands, the frontmatter type migration and its test rewrites, and the per-emission scan over recorded rounds. Keeping the conclusion (\"still small relative to a saved sub-agent round per declined role per round\") is fine; omitting the items is what makes it unverifiable."}
{"category":"consistency","class":"stale-warning-text","finding":"§2.1 makes `enabled` a permitted key, while §6 item 5 requires \"the existing 'not a permitted override key' warning\". The existing message literally reads `tp.review_roles.\u003cid\u003e.\u003ckey\u003e is not a permitted override key (only focus); ignored` and is asserted verbatim in internal/engine/frontmatter_roleoverrides_test.go:74-75. Keeping it makes the user-facing message false the day this ships; changing it breaks a committed assertion. The spec pins neither, so item 5 has two contradictory correct implementations.","found_by":1,"found_by_roles":["tester"],"location":"§2.1","resolved":{"evidence":"The override contract is unchanged; the sections that consume it were corrected. §2.2 now names the real readers, §2.5 the per-command mode lists, §2.6 the two phase-specific arms.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"medium","suggestion":"State that the parenthetical becomes \"(only focus and enabled)\", give the new literal in §2.2, and have item 5 assert the updated string — noting the committed assertions that move with it."}
{"category":"consistency","class":"phantom-component-citation","finding":"Two of the three named readers are wrong about the tree, and the component that must actually change is missing. There is no lint pass that reports unknown override ids: the \"matches no active \u003cphase\u003e role\" warning is produced inside `ResolveOverrideFocus` itself (internal/engine/overrides.go:49-58), and searching the tree finds `Frontmatter.ReviewRoles`/`AuditRoles` read in only three non-test places — overrides.go, lens_shim.go:37, and frontmatter.go where they are assigned. The list also omits `parseRoleOverrides` (internal/engine/frontmatter.go:134-...), which is the permitted-key gate that today rejects any key other than `focus`, defaults a focus-less override to `[]string{}`, and is the only place that can accept `enabled` and emit the new non-boolean warning.","found_by":1,"found_by_roles":["implementer"],"location":"§2.2","resolved":{"evidence":"§2.2 rewritten on the evidence. The earlier reader list was wrong at two of three entries: there is no separate lint pass for the unknown-id warning (ResolveOverrideFocus emits it itself), and the third site is TranslateLegacyLens, whose declared return type is map[string][]string and whose result is assigned to the same `overrides` variable as fm.ReviewRoles — so it must change type or be adapted or the package will not compile. parseRoleOverrides is named as the writer.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"implementer","severity":"medium","suggestion":"Replace the reader list with the three real sites — `parseRoleOverrides` in internal/engine/frontmatter.go (accept `enabled`, warn on a non-boolean, build the struct), `ResolveOverrideFocus` in internal/engine/overrides.go (source selection, unknown-id warning, focus layering), and `TranslateLegacyLens` in internal/engine/lens_shim.go (presence check and return type) — and drop the reference to a lint pass."}
{"category":"completeness","class":"untested-clause","finding":"§2.3 devotes a paragraph to the interaction this release most risks breaking — the drop is applied outside `ResolveActiveCorpus` so that domain filtering emptying the user panel still falls back to the embedded corpus instead of hitting §2.5's refusal — and calls it what \"keeps it from colliding\". §6 has no item for it, while the far simpler no-change claim of §3 does get one (item 13). An implementation that applied the drop inside `ResolveActiveCorpus`, or that let the refusal fire on the fallback path, passes every item in §6.","found_by":1,"found_by_roles":["tester"],"location":"§2.3","resolved":{"evidence":"§2.3's placement outside ResolveActiveCorpus stands and §2.7 now states the full precedence including the exit codes each refusal uses. Where this finding touched the dropped-set seam, §2.6 no longer relies on the round-carry's dedup key — it reads the `role` field the output contract stamps on each recorded row.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"medium","suggestion":"Add a test item: a spec whose domain matches no user role still emits the embedded corpus (existing fallback), `enabled: false` on one of those embedded roles drops just that role, and the refusal fires only when the drop empties the fallback panel itself."}
{"category":"consistency","class":"guard-coverage-gap","finding":"§2.6 states the invariant that a spec must not reach a clean round by removing the lens that was failing, yet §2.3 and §2.7 put domain filtering first and unguarded: a role removed by `domains` is reported as domain-mismatch and never reaches the enabled:false drop set, so §2.6's scan never sees it. §1 itself says the domains axis 'does deactivate a role per spec', which makes it the same bypass on a different lever — editing a spec's `domain:` (or a role file's `domains`) drops a finding-bearing role from the panel with no refusal at all. The spec neither extends the guard to domain-dropped roles nor records the omission as a deliberate boundary, so the stated invariant is only half enforced.","found_by":1,"found_by_roles":["architect"],"location":"§2.3","resolved":{"evidence":"§2.3's placement outside ResolveActiveCorpus stands and §2.7 now states the full precedence including the exit codes each refusal uses. Where this finding touched the dropped-set seam, §2.6 no longer relies on the round-carry's dedup key — it reads the `role` field the output contract stamps on each recorded row.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"architect","severity":"medium","suggestion":"Either extend the §2.6 scan to every role dropped this invocation regardless of reason (enabled:false plus domain-mismatch), or add a Non-Goal stating explicitly that domain-driven drops are out of scope and why — e.g. that changing `domain:` changes spec_hash and so is visible to the staleness check, if that is the argument. Whichever is chosen, §2.7's 'domain filtering first' needs a sentence saying what that means for the guard."}
{"category":"completeness","class":"ordering-underspecified","finding":"Rule 2 moves only 'corpus resolution and override resolution' ahead of the snapshot write, and concludes 'both refusals of §2.5 and §2.6 are decidable while nothing has been written'. That does not follow for §2.6, whose predicate needs a second input the rule never mentions: the rows of every recorded round. In review those rows are loaded inside loadReviewRoundState itself, after WriteSnapshotAtomic (internal/cli/review.go:1629 writes, L1650-1663 then reads the round files); in audit the prior-round read (loadAuditPriorRound) runs after loadAuditSpec has already snapshotted. So a literal implementation of rule 2 still exits 2 on the §2.6 refusal with a round snapshot on disk. Test 7 pins the no-snapshot property only for the §2.5 case; tests 9 and 10 assert nothing about the snapshot, leaving the claim unpinned exactly where it does not hold.","found_by":1,"found_by_roles":["architect"],"location":"§2.5","resolved":{"evidence":"§2.5 rewritten twice over on this round's evidence. (1) The mode list is now PER COMMAND, because tp audit registers no --resolve, --resolve-all, --verify or --report — an earlier list implied flags that do not exist and §6 item 8 inherited the error. (2) The ordering clause now covers every write the emission path performs, not just the snapshot: tp review's loadReviewRoundState calls EnsureReviewState, which creates .tp-review/\u003cspec\u003e/ and saves a fresh state.json BEFORE WriteSnapshotAtomic runs, so resolution moves ahead of that too and §6 item 7 asserts all three absences.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"architect","severity":"medium","suggestion":"Extend rule 2 to name the recorded-round scan: corpus resolution, override resolution, and the recorded-round read that feeds §2.6 all move ahead of the snapshot write in both commands. Add the 'leaves no round snapshot on disk' assertion to tests 9 and 10 so the guarantee is pinned for §2.6, not only §2.5."}
{"category":"ambiguity","class":"justification-mismatch","finding":"The refusal is justified by its outcome — 'a review or audit round that emits nothing and records nothing while still counting as a round' — but tp already permits that exact outcome from another cause and does not refuse: an audit whose checklist is empty logs 'no structured elements found in spec — checklist is empty' (internal/cli/audit.go:202-204) and every auditor is skipped with no-checklist-items, producing zero prompts and exit 0. Since the harm named is already tolerated, the stated ground does not distinguish the two cases, and an implementer reading the justification rather than the rule may key the check on the emitted-prompt count instead of the active-role set, which would newly refuse today's legitimate empty-checklist audit.","found_by":1,"found_by_roles":["architect"],"location":"§2.5","resolved":{"evidence":"§2.5 rewritten twice over on this round's evidence. (1) The mode list is now PER COMMAND, because tp audit registers no --resolve, --resolve-all, --verify or --report — an earlier list implied flags that do not exist and §6 item 8 inherited the error. (2) The ordering clause now covers every write the emission path performs, not just the snapshot: tp review's loadReviewRoundState calls EnsureReviewState, which creates .tp-review/\u003cspec\u003e/ and saves a fresh state.json BEFORE WriteSnapshotAtomic runs, so resolution moves ahead of that too and §6 item 7 asserts all three absences.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"architect","severity":"medium","suggestion":"Restate the ground as the declaration rather than the outcome: the phase is refused because the spec explicitly asked for a panel of zero, which is always a mistake, whereas an empty emission arising from routing (no-checklist-items, no-spec-change) stays permitted and is reported through skipped_roles. Say explicitly that the predicate is 'no active role after the enabled:false drop', never 'zero emitted prompts'."}
{"category":"completeness","class":"uncovered-carve-out","finding":"The rewrite grew §6 from 12 items to 16 without adding the two `regression`-role items round 1 asked for, while the resolution notes present that finding as addressed. §2.5's carve-out (\"The built-in `regression` role does not count as an active reviewer for this check\") is still untested: item 7 only tests \"deactivating every role of a phase\", which does not distinguish the rule — the case that does is every corpus reviewer disabled while `regression` would still emit at round 2, and the expectation is still exit 2. Separately, Non-Goal 4 forbids deactivating `regression` but the spec still never states the observable behavior of trying; because `regression` is appended to emission separately and never passed to ResolveOverrideFocus, `review_roles: { regression: { enabled: false } }` falls into the active-role check and emits \"matches no active reviewers role; ignored\" — a message that reads as \"this role does not exist\" for a role the same output emits. Round 1 raised both; the resolution notes point at §2.3 and §2.7, neither of which mentions `regression`.","found_by":0,"location":"§2.5","resolved":{"evidence":"§2.5 rewritten twice over on this round's evidence. (1) The mode list is now PER COMMAND, because tp audit registers no --resolve, --resolve-all, --verify or --report — an earlier list implied flags that do not exist and §6 item 8 inherited the error. (2) The ordering clause now covers every write the emission path performs, not just the snapshot: tp review's loadReviewRoundState calls EnsureReviewState, which creates .tp-review/\u003cspec\u003e/ and saves a fresh state.json BEFORE WriteSnapshotAtomic runs, so resolution moves ahead of that too and §6 item 7 asserts all three absences.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"regression","severity":"medium","suggestion":"Add a test item for the carve-out (all corpus reviewers disabled, regression still eligible, still exit 2) and one sentence to §2.3 or Non-Goal 4 stating what `enabled: false` on `regression` emits — either the existing unknown-id warning, acknowledged as slightly misleading, or a dedicated \"the regression role cannot be deactivated\" warning."}
{"category":"ambiguity","class":"mode-scope-underspecified","finding":"The scope is drawn as 'fires only in the prompt-emission mode' plus an exclusion list containing only the state/merge/resolve modes, which leaves `tp review --perspective documentation|testing|code-audit|regression` on the refusing side of the line. Those are prompt-emission invocations, but none of them resolves the reviewer corpus: runReview returns through runReviewRegression (internal/cli/review.go:320-322) or runReviewCodeAudit/runReviewDocPlan/runReviewTestPlan (L328-335) before buildReviewPrompts — the only caller of ResolveActiveCorpus for reviewers — and before any snapshot is written. A refusal there would block a doc plan or a code-audit prompt on a spec that deactivated its reviewer panel, for no benefit; not refusing there needs to be stated, since the spec's own boundary is 'emission vs not'.","found_by":1,"found_by_roles":["architect"],"location":"§2.5","resolved":{"evidence":"§2.5 rewritten twice over on this round's evidence. (1) The mode list is now PER COMMAND, because tp audit registers no --resolve, --resolve-all, --verify or --report — an earlier list implied flags that do not exist and §6 item 8 inherited the error. (2) The ordering clause now covers every write the emission path performs, not just the snapshot: tp review's loadReviewRoundState calls EnsureReviewState, which creates .tp-review/\u003cspec\u003e/ and saves a fresh state.json BEFORE WriteSnapshotAtomic runs, so resolution moves ahead of that too and §6 item 7 asserts all three absences.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"architect","severity":"medium","suggestion":"Restate the scope positively: the refusals fire only on the corpus-driven emission path — default `tp review` (no --perspective) and default `tp audit` — and name --perspective documentation/testing/code-audit/regression alongside the state modes as unaffected, because they emit a single non-corpus prompt. Add one test covering a --perspective run on a spec that deactivates every reviewer."}
{"category":"consistency","class":"mode-list-command-mismatch","finding":"§2.5 rule 1 enumerates '--record, --status (with or without --check), --merge, --resolve, --resolve-all, --verify, and --report' as modes of 'each command', and test 8 requires that same set to 'still exit normally ... for both `tp review` and `tp audit`'. `tp audit` has no --resolve, --resolve-all, --verify or --report: its flag set is --affected-files, --affected-from-tasks, --base, --findings, --record, --status, --check, --harness-note, --merge, -o (internal/cli/audit.go:158-167). Test 8 as written is unsatisfiable for audit — those invocations fail cobra flag parsing with a non-zero exit, so the test either cannot be written or passes for the wrong reason. §2.5's list also includes --resolve-all while test 8 drops it, so the normative list and its pinning test already disagree.","found_by":1,"found_by_roles":["architect"],"location":"§2.5","resolved":{"evidence":"§2.5 rewritten twice over on this round's evidence. (1) The mode list is now PER COMMAND, because tp audit registers no --resolve, --resolve-all, --verify or --report — an earlier list implied flags that do not exist and §6 item 8 inherited the error. (2) The ordering clause now covers every write the emission path performs, not just the snapshot: tp review's loadReviewRoundState calls EnsureReviewState, which creates .tp-review/\u003cspec\u003e/ and saves a fresh state.json BEFORE WriteSnapshotAtomic runs, so resolution moves ahead of that too and §6 item 7 asserts all three absences.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"architect","severity":"medium","suggestion":"Split the mode list per command: review = --record / --status[--check] / --merge / --resolve / --resolve-all / --verify / --report; audit = --record / --status[--check] / --merge. Restate test 8 as two tests over the modes each command actually has, and keep --resolve-all in both the list and the test."}
{"category":"completeness","class":"untested-clause","finding":"§2.5 states that the built-in `regression` role does not count as an active reviewer for the empty-phase check — the difference between refusing and emitting a regression-only round. No §6 item covers it, and item 7 will not reach it: regression is skipped at round 1 with reason `no-baseline` (there is no snapshot-round-0.md), so the natural round-1 setup never has regression active and an implementation that wrongly counts it still passes.","found_by":1,"found_by_roles":["tester"],"location":"§2.5","resolved":{"evidence":"§2.5 rewritten twice over on this round's evidence. (1) The mode list is now PER COMMAND, because tp audit registers no --resolve, --resolve-all, --verify or --report — an earlier list implied flags that do not exist and §6 item 8 inherited the error. (2) The ordering clause now covers every write the emission path performs, not just the snapshot: tp review's loadReviewRoundState calls EnsureReviewState, which creates .tp-review/\u003cspec\u003e/ and saves a fresh state.json BEFORE WriteSnapshotAtomic runs, so resolution moves ahead of that too and §6 item 7 asserts all three absences.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"medium","suggestion":"Add a test item: at round 2 (a snapshot-round-1.md exists, so regression is active), deactivating every corpus reviewer still exits 2 rather than emitting a regression-only round."}
{"category":"ambiguity","class":"ambiguous-dedup-key","finding":"\"The same key the round-carry uses\" names no single key: the tree holds three. Review's round-carry dedups on (category, location, finding-prefix) (`dedupFindings`); `tp review --merge` dedups on (location, class) (documented in internal/engine/reviewstate.go:34); audit's carry `loadAuditPriorRound` (internal/cli/audit.go:393-435) keys per (role, item_id) and reads only `st.AuditRounds[len-1]`, so for audit there is no existing cross-round key at all to borrow. Worse for audit, `IDScheme`/`IsLegacyRound` (reviewstate.go:28-46) record that rounds written before v0.30.0 use positional `file-\u003crole\u003e-\u003cn\u003e` ids while newer rounds use stable slugs, so the same item has different item_ids across rounds and a cross-round dedup by item_id silently double-counts or mis-merges on any project that upgraded mid-loop.","found_by":1,"found_by_roles":["implementer"],"location":"§2.6","resolved":{"evidence":"§2.6 rewritten as two phase-specific arms, because the attempt to unify them was the source of the errors. Review: the predicate is reviewFindingResolvedAway AND the severity filter ReviewRoundClean applies under the effective review_converge_on — the earlier 'exactly the set that keeps a round from being clean' was false since v0.31.0, where a medium finding does not block; the scan covers every recorded round (matching loadReviewRoundState); role attribution comes from the recorded row's own `role` field, not the (location, class) dedup key which carries no role; the escape hatch is tp review --resolve against the recorded round file. Audit: the scan is the LATEST round only (matching loadAuditPriorRound), the predicate is non-PASS, and there is no escape command because tp audit has no --resolve — a row stops blocking when a later recorded round reports PASS, so the refusal points at the audit loop. The exit code moves from 2 to 4: the trigger is recorded state, and tp reserves 4 for state. --no-state is addressed: the guard needs state, so under it the guard is skipped WITH a warning rather than silently permitting the deactivation.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"implementer","severity":"medium","suggestion":"Write the key out per phase (see the previous finding) and say what the audit scan does with a legacy (marker-less) round: skip it, treat its rows as un-dedupable, or refuse with a hint pointing at the legacy round."}
{"category":"redundancy","class":"emission-guard-for-convergence-invariant","finding":"The guard runs on every prompt emission, but the invariant it protects - 'a spec could reach a clean round by removing the lens that was failing' - only binds at convergence. A hard exit 2 on emission halts every other role's round until the agent detours into tp review --resolve against recorded round state; since the message names the round file each unresolved item lives in, that detour is one --resolve call per recorded round file, for findings that could legitimately be resolved at any point before the phase is declared converged. The spec never costs that detour against the alternative of enforcing the same rule once, where convergence is actually decided.","found_by":1,"found_by_roles":["ax-economist"],"location":"§2.6","resolved":{"evidence":"§2.6 rewritten as two phase-specific arms, because the attempt to unify them was the source of the errors. Review: the predicate is reviewFindingResolvedAway AND the severity filter ReviewRoundClean applies under the effective review_converge_on — the earlier 'exactly the set that keeps a round from being clean' was false since v0.31.0, where a medium finding does not block; the scan covers every recorded round (matching loadReviewRoundState); role attribution comes from the recorded row's own `role` field, not the (location, class) dedup key which carries no role; the escape hatch is tp review --resolve against the recorded round file. Audit: the scan is the LATEST round only (matching loadAuditPriorRound), the predicate is non-PASS, and there is no escape command because tp audit has no --resolve — a row stops blocking when a later recorded round reports PASS, so the refusal points at the audit loop. The exit code moves from 2 to 4: the trigger is recorded state, and tp reserves 4 for state. --no-state is addressed: the guard needs state, so under it the guard is skipped WITH a warning rather than silently permitting the deactivation.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"ax-economist","severity":"medium","suggestion":"Cost the alternative: have the phase's convergence check count a deactivated role's unresolved recorded rows as blocking (a not-converged result, not a usage error, so §2.5's 'refusals never fire on --status --check' scoping is untouched) and downgrade the emission-time check to a warning naming the role and the round files. If the emission-time refusal is kept, say why blocking unrelated rounds is worth it, and state that the escape hatch costs one --resolve call per recorded round file."}
{"category":"consistency","class":"stale-rationale","finding":"The opening justification survived the rewrite unchanged and is false for the review phase. \"The prior-round carry mechanism re-presents a role's unresolved rows in its next-round prompt\" holds for audit (loadAuditPriorRound -\u003e priorByRole -\u003e buildRolePrompt) but not for review, where every reviewer prompt receives the same aggregate findings summary built from all prior rounds (buildFindingsSummary, passed identically to each generateCorpusReviewPrompt). Deactivating a review role therefore does not drop its findings out of the loop — they stay in the summary the remaining roles read. Round 1 raised exactly this and the resolution note claims §2.6 was rewritten, but only the predicate was; the premise the review half of the refusal rests on is still asserted, and §2.6's own later paragraph half-concedes it by noting that loadReviewRoundState carries findings from every recorded round rather than per role.","found_by":0,"location":"§2.6","resolved":{"evidence":"§2.6 rewritten as two phase-specific arms, because the attempt to unify them was the source of the errors. Review: the predicate is reviewFindingResolvedAway AND the severity filter ReviewRoundClean applies under the effective review_converge_on — the earlier 'exactly the set that keeps a round from being clean' was false since v0.31.0, where a medium finding does not block; the scan covers every recorded round (matching loadReviewRoundState); role attribution comes from the recorded row's own `role` field, not the (location, class) dedup key which carries no role; the escape hatch is tp review --resolve against the recorded round file. Audit: the scan is the LATEST round only (matching loadAuditPriorRound), the predicate is non-PASS, and there is no escape command because tp audit has no --resolve — a row stops blocking when a later recorded round reports PASS, so the refusal points at the audit loop. The exit code moves from 2 to 4: the trigger is recorded state, and tp reserves 4 for state. --no-state is addressed: the guard needs state, so under it the guard is skipped WITH a warning rather than silently permitting the deactivation.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"regression","severity":"medium","suggestion":"Rewrite the first sentence to state the true asymmetry: audit carries a role's own unresolved rows into its next-round prompt, review carries an aggregate summary to every role. Then justify the review half on the honesty ground that actually applies — recorded state still holds the open finding and the convergence predicate counts it — rather than on a per-role carry review does not have."}
{"category":"completeness","class":"precedence-gap","finding":"The fixed order covers only the three new outcomes and says nothing about the pre-emission exits that already run first in audit. `runAudit` calls `refuseAuditIfBudgetExhausted` (internal/cli/audit.go:190) and `determineAuditFiles` (line 194, which exits 4 via `exitAuditNoFiles` when the diff holds no audit-able file) before it ever reaches `loadAuditSpec` and role resolution. Once §2.5 clause 2 hoists resolution earlier, an implementer must choose whether the new exit-2 refusals fire before or after those exit-4/budget refusals, and the spec gives no rule — so the exit code an agent sees for a spec that both disables every auditor and has no changed files is undefined.","found_by":1,"found_by_roles":["implementer"],"location":"§2.7","resolved":{"evidence":"§2.7 now carries the exit code each refusal uses (4 for §2.6, 2 for §2.5) so the precedence and the contract are stated together.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"implementer","severity":"medium","suggestion":"Extend §2.7 to place the two new refusals relative to audit's existing pre-emission exits (budget exhaustion and no-audit-able-files), and state the resulting exit code for an invocation that trips both."}
{"category":"redundancy","class":"sequential-refusal-round-trips","finding":"The section fixes an order between two exit-2 refusals, so a spec tripping both is diagnosed over two runs instead of one - and it surfaces the more expensive remedy first. §2.6 sends the agent to resolve findings inside recorded round files, while re-enabling that one role (the §2.5 fix) clears both conditions in a single frontmatter edit; §2.7's own text concedes the second run ('re-running after resolving it then reports the empty phase if one remains'). Separately, the first two steps it orders (domain filtering, then the unknown-id warning) are already fixed by §2.3, so the section's only new content is the §2.6-before-§2.5 choice.","found_by":1,"found_by_roles":["ax-economist"],"location":"§2.7","resolved":{"evidence":"§2.7 now carries the exit code each refusal uses (4 for §2.6, 2 for §2.5) so the precedence and the contract are stated together.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"ax-economist","severity":"medium","suggestion":"Emit every applicable refusal in one exit-2 payload (a list of blocking reasons) so the agent sees the whole fix in one round-trip. If a single ordered message is kept, order §2.5 first, because re-enabling a role is the cheaper fix that also clears §2.6. Either way fold the §2.3-derived steps back into §2.3 rather than restating them as a section."}
{"category":"consistency","class":"resolution-reverses-finding","finding":"A round-1 finding is recorded as addressed by a change that moves in the opposite direction, with no rationale. The finding argued that requiring skills/tp/SKILL.md to carry the full semantics of both refusals is waste — SKILL.md bytes load into every agent session unconditionally, and both refusals are self-announcing at the moment they matter through tp's own hint mechanism. The rewrite's answer expands the requirement instead: from three documents to four (REFERENCE.md added), from prose \"must state\" to an exact substring plus the trim-candidate guidance displaced from the old §3, all guarded by item 16. If the expansion is right, the finding deserves a stated rejection (the doc contract is the lookup path an agent consults before writing frontmatter, so drift costs a round); as it stands the finding is marked fixed by a change that increases exactly the cost it named.","found_by":0,"location":"§4","resolved":{"evidence":"§4 corrected on the evidence: the claim that deleting a role file removes the role from every spec is false at the boundary, because ResolveActiveCorpus falls back to the embedded default corpus when the deletion empties the phase. The required REFERENCE.md text now states that exception, so the documentation and §6's guard no longer promote a claim round 1 had already disproved.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"regression","severity":"medium","suggestion":"State in §4 why the per-session doc cost is worth paying — or trim SKILL.md's share to the one-line substring plus a REFERENCE.md pointer, and keep the full semantics (both refusals, their emission-only scope, the domains relationship) in REFERENCE.md, which is fetched on demand."}
{"category":"redundancy","class":"doc-duplication-four-way","finding":"All four of README.md, skills/tp/SKILL.md, skills/tp/REFERENCE.md and CLAUDE.md must carry the same full semantics - the disabled-by-spec reason, both refusals with their emission-only scope, and the domains-vs-enabled distinction - and test 16 pins the duplication with an exact-substring guard. Two of those files (CLAUDE.md and skills/tp/SKILL.md) are loaded into agent context on essentially every session, so a one-spec exception mechanism buys permanent per-session context in four hand-synced copies. The release's own economy argument ('the cost side is deliberately small') does not account for it.","found_by":1,"found_by_roles":["ax-economist"],"location":"§4","resolved":{"evidence":"§4 corrected on the evidence: the claim that deleting a role file removes the role from every spec is false at the boundary, because ResolveActiveCorpus falls back to the embedded default corpus when the deletion empties the phase. The required REFERENCE.md text now states that exception, so the documentation and §6's guard no longer promote a claim round 1 had already disproved.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"ax-economist","severity":"medium","suggestion":"Split by audience: CLAUDE.md and SKILL.md carry the one-line rule ('enabled: false deactivates a role for one spec') plus a pointer; REFERENCE.md (and README.md at overview depth) carry the refusals, their emission-only scope, and the domains comparison. Keep the guard test on the one-line substring in all four files, and assert the full semantics only in REFERENCE.md."}
{"category":"consistency","class":"doc-asserts-unenforced-rule","finding":"§4's second paragraph requires REFERENCE.md to state that \"a role carrying unresolved findings must be resolved before either\" lever. tp enforces that for only one of the two: §2.6's refusal keys on \"a role targeted by `enabled: false`\", so deleting the role's file is not guarded at all — the role vanishes from the corpus with its unresolved rows still in recorded state and no refusal fires. The documentation would therefore state a rule the release deliberately does not implement, and the delete lever is a documented bypass of the exact honesty guard §2.6 exists to provide. This is the same escape §2.6 closes for the enabled:false path (\"a spec could reach a clean round by removing the lens that was failing\"), left open on the path §4 advertises alongside it.","found_by":0,"location":"§4","resolved":{"evidence":"§4 corrected on the evidence: the claim that deleting a role file removes the role from every spec is false at the boundary, because ResolveActiveCorpus falls back to the embedded default corpus when the deletion empties the phase. The required REFERENCE.md text now states that exception, so the documentation and §6's guard no longer promote a claim round 1 had already disproved.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"regression","severity":"medium","suggestion":"Either scope the sentence to the lever tp actually guards (\"`enabled: false` on a role carrying unresolved findings is refused; deleting the role file is not checked\") and say so plainly as an accepted gap, or extend §2.6's scan to any role with unresolved recorded rows that is no longer in the active corpus — and add a test item either way."}
{"category":"consistency","class":"audience-claim-drift","finding":"Non-Goal 2's factual premise is too broad and contradicts §4. It asserts 'there is no rendered advice, only the literal [trim candidate] flag appended to a per-role line in `tp review --report`'s TTY block ... so the one surface the change would touch is the one no agent ever reads'. trim_candidate is a JSON payload key, not a TTY artifact: RoleOverlap carries `json:\"trim_candidate\"` (internal/engine/overlap.go:17) and the overlap_report ships in `tp review --merge/--status/--report` and, since v0.29.0, `tp audit --merge/--status` (internal/cli/audit_overlap.go) — which is the surface agents actually consume, and is documented as such in REFERENCE.md. §4 in the same spec calls that flag advice that 'advises trimming a role repository-wide' and requires REFERENCE.md to teach it two levers, so the release ships a machine-read flag whose documented meaning stays repo-wide-only while the correction lives in a human doc.","found_by":1,"found_by_roles":["architect"],"location":"§5","resolved":{"evidence":"§5 (Non-Goals) stands; Non-Goal 2's reasoning for deleting the trim_candidate rendering change is unchanged and the guidance it displaces now lives in §4 with the fallback exception stated.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"architect","severity":"medium","suggestion":"Keep the deletion but narrow the justification to the two claims that survive: the *rendering* the earlier draft would have changed is TTY-only, and adding an advice field would be a new payload key for a decision the driver can derive from unique/shared. Drop 'the one surface ... no agent ever reads' as stated about trim_candidate itself, and say instead that trim_candidate's value is unchanged and only its documented interpretation gains the second lever."}
{"category":"completeness","class":"untested-clause","finding":"Non-goal 3 states that `enabled: true` does not resurrect a role removed by `domains` or absent from the corpus. That is a behavioural requirement, not merely a scope exclusion, and it is a plausible implementation slip (treating `Enabled == true` as force-include when assembling the panel). §6 item 3 covers only `enabled: true` on an already-active role, and item 6 covers only `enabled: false` on a domain-dropped id, so the stated non-goal has no test.","found_by":1,"found_by_roles":["tester"],"location":"§5","resolved":{"evidence":"§5 (Non-Goals) stands; Non-Goal 2's reasoning for deleting the trim_candidate rendering change is unchanged and the guidance it displaces now lives in §4 with the fallback exception stated.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"medium","suggestion":"Add a test item: `enabled: true` on a role dropped by `domains`, and on an id absent from the corpus, leaves it out of the panel and produces the existing \"matches no active role\" warning."}
{"category":"completeness","class":"unspecified-observable-behavior","finding":"Non-goal 4 states that the built-in `regression` role cannot be deactivated but never says what `review_roles: { regression: { enabled: false } }` actually does. `regression` is never passed to `ResolveOverrideFocus` (it is appended to emission separately), so the existing path warns \"tp.review_roles override for \\\"regression\\\" matches no active reviewers role; ignored\" — a message that is misleading from round 2 on, where the regression prompt IS emitted. No test in §6 pins the case either.","found_by":1,"found_by_roles":["implementer"],"location":"§5","resolved":{"evidence":"§5 (Non-Goals) stands; Non-Goal 2's reasoning for deleting the trim_candidate rendering change is unchanged and the guidance it displaces now lives in §4 with the fallback exception stated.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"implementer","severity":"medium","suggestion":"Specify the observable outcome: a dedicated warning such as \"regression cannot be deactivated; it is tp's convergence machinery\" (and the role stays emitted), plus a test asserting it — or state explicitly that the generic unknown-id warning is the intended behavior."}
{"category":"completeness","class":"existing-test-migration-unnamed","finding":"§2.2 retypes `Frontmatter.ReviewRoles`/`AuditRoles` to `map[string]RoleOverride` and §2.3 gives `ResolveOverrideFocus` a third return value, but no §6 item requires the committed tests that read them to be migrated rather than weakened. Concretely: internal/engine/overrides_test.go's four tests stop compiling (two-value assignment); internal/engine/frontmatter_roleoverrides_test.go asserts `[]string{...}` against `fm.ReviewRoles[id]` in four places, including the deliberate invariant \"an empty focus list is retained as empty, not dropped\" (line 41), which the struct form makes indistinguishable from an absent key unless a comma-ok lookup is kept; internal/engine/lens_shim_test.go asserts `[]string` values from `TranslateLegacyLens`, whose result is assigned straight into the override map at overrides.go:37 (and which itself, not merely its caller, reads `fm.ReviewRoles`/`fm.AuditRoles` at lens_shim.go:37 — contrary to §2.2's reader inventory). The cheap migration is to delete or loosen these assertions and nothing forbids it.","found_by":1,"found_by_roles":["tester"],"location":"§6","resolved":{"evidence":"§6 (Tests) corrected on this round's evidence. Item 7 asserts the absence of the directory, the state file AND the snapshot, since the snapshot is not the first write. Item 8 enumerates modes per command, dropping the audit flags that do not exist. Item 9 is the audit arm and its escape is recording a later PASS round, not a resolve call. Item 10 is the review arm and covers all five decisive branches of reviewFindingResolvedAway plus the severity boundary. Item 11 states both scan ranges and their opposite expectations. A new item covers --no-state skipping the guard with a warning.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"medium","suggestion":"Add a test item requiring the existing override/frontmatter/lens-shim assertions to be preserved in the new shape — specifically the empty-focus-retained-as-empty case and the \"regression accepts no overrides\" case — and state in §2.2 whether an override entry carrying neither `focus` nor `enabled` is retained as a map key, and what `TranslateLegacyLens` now returns."}
{"category":"completeness","class":"single-phase-test-for-two-phase-rule","finding":"Items 7 and 11 are written in the singular (\"a phase\", \"a row\") for rules whose mechanics differ per phase, and item 8 is the only item that says \"for both tp review and tp audit\". §2.5's ordering change touches two distinct call sites (`loadReviewRoundState` and `loadAuditSpec`), so item 7 satisfied on `tp review` leaves the audit snapshot-before-refusal path — explicitly called load-bearing — untested. Item 11's all-rounds scan is the larger behavioural change on the audit side, whose carry reads only the latest recorded round today, yet \"row\" reads as audit and leaves the review side of the same rule to item 10's single-round setup.","found_by":1,"found_by_roles":["tester"],"location":"§6 item 11","resolved":{"evidence":"§6 (Tests) corrected on this round's evidence. Item 7 asserts the absence of the directory, the state file AND the snapshot, since the snapshot is not the first write. Item 8 enumerates modes per command, dropping the audit flags that do not exist. Item 9 is the audit arm and its escape is recording a later PASS round, not a resolve call. Item 10 is the review arm and covers all five decisive branches of reviewFindingResolvedAway plus the severity boundary. Item 11 states both scan ranges and their opposite expectations. A new item covers --no-state skipping the guard with a warning.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"medium","suggestion":"Say \"for both tp review and tp audit\" in items 7 and 11, or split each into two items, so both call sites of the ordering change and both phases of the all-rounds scan are pinned."}
{"category":"ambiguity","class":"impossible-as-phrased","finding":"\"Toggling `enabled` changes the recorded round's `spec_hash`\" cannot be asserted literally: `SpecHash` is stamped into a `ReviewRound` at --record time, and editing the spec afterwards never mutates an already-recorded entry — it makes that entry stale. The intended observable is either (a) round N+1's recorded spec_hash differs from round N's while both roles_hash values match, or (b) `StateStale` reports the recorded round stale after the toggle. The two produce different tests from the same sentence.","found_by":1,"found_by_roles":["tester"],"location":"§6 item 13","resolved":{"evidence":"§6 (Tests) corrected on this round's evidence. Item 7 asserts the absence of the directory, the state file AND the snapshot, since the snapshot is not the first write. Item 8 enumerates modes per command, dropping the audit flags that do not exist. Item 9 is the audit arm and its escape is recording a later PASS round, not a resolve call. Item 10 is the review arm and covers all five decisive branches of reviewFindingResolvedAway plus the severity boundary. Item 11 states both scan ranges and their opposite expectations. A new item covers --no-state skipping the guard with a warning.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"medium","suggestion":"Reword to the record-toggle-record form — \"recording a round, toggling `enabled`, and recording again yields a different spec_hash and an identical roles_hash across the two rounds\" — or state the staleness form explicitly."}
{"category":"ambiguity","class":"vague-pass-criterion","finding":"\"reports the §2.6 refusal first\" does not say whether the §2.5 error is emitted as well. §2.7's own model is sequential — \"re-running after resolving it then reports the empty phase if one remains\" — which implies exactly one error per invocation, but the word \"first\" reads as an ordering assertion over two emitted messages. One engineer asserts the §2.6 text appears and the §2.5 text does not; another asserts both appear in order. Both satisfy the item as written.","found_by":1,"found_by_roles":["tester"],"location":"§6 item 15","resolved":{"evidence":"§6 (Tests) corrected on this round's evidence. Item 7 asserts the absence of the directory, the state file AND the snapshot, since the snapshot is not the first write. Item 8 enumerates modes per command, dropping the audit flags that do not exist. Item 9 is the audit arm and its escape is recording a later PASS round, not a resolve call. Item 10 is the review arm and covers all five decisive branches of reviewFindingResolvedAway plus the severity boundary. Item 11 states both scan ranges and their opposite expectations. A new item covers --no-state skipping the guard with a warning.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"medium","suggestion":"State that such an invocation emits the §2.6 error only and exits 2, with the §2.5 error appearing on the subsequent run, and phrase item 15 as \"emits the §2.6 error and not the §2.5 error\"."}
{"category":"completeness","class":"test-passes-without-change","finding":"\"A non-boolean `enabled` produces a lint warning and leaves the role active\" is already true of today's tp: `enabled` is not a permitted key, so any value emits \"...enabled is not a permitted override key (only focus); ignored\" and the role stays active. The item therefore passes unchanged against a build in which §2.2's new non-boolean warning was never implemented — it cannot distinguish new behaviour from old, because no warning text is pinned, while every other frontmatter warning in the codebase is asserted verbatim.","found_by":1,"found_by_roles":["tester"],"location":"§6 item 4","resolved":{"evidence":"§6 (Tests) corrected on this round's evidence. Item 7 asserts the absence of the directory, the state file AND the snapshot, since the snapshot is not the first write. Item 8 enumerates modes per command, dropping the audit flags that do not exist. Item 9 is the audit arm and its escape is recording a later PASS round, not a resolve call. Item 10 is the review arm and covers all five decisive branches of reviewFindingResolvedAway plus the severity boundary. Item 11 states both scan ranges and their opposite expectations. A new item covers --no-state skipping the guard with a warning.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"medium","suggestion":"Pin the new warning's exact message in §2.2 (e.g. `tp.review_roles.\u003cid\u003e.enabled is not a boolean; ignored`) and have item 4 assert that literal plus the absence of the \"not a permitted override key\" warning."}
{"category":"ambiguity","class":"unpinned-disk-assertion","finding":"\"leaves no round snapshot on disk\" names neither the artifact nor the precondition. The artifact is `.tp-review/\u003cspec\u003e/snapshot-round-N.md` for review and the audit-namespaced snapshot for audit, and the assertion is only meaningful from a stated baseline: earlier rounds' snapshots legitimately exist, and re-running emission for the same round overwrites an existing snapshot-round-N.md — so a test that emits successfully once, then adds `enabled: false` and re-runs, finds the file present and fails for the wrong reason. Two engineers pick different baselines (fresh state dir vs mid-loop) and get different verdicts on the same code.","found_by":1,"found_by_roles":["tester"],"location":"§6 item 7","resolved":{"evidence":"§6 (Tests) corrected on this round's evidence. Item 7 asserts the absence of the directory, the state file AND the snapshot, since the snapshot is not the first write. Item 8 enumerates modes per command, dropping the audit flags that do not exist. Item 9 is the audit arm and its escape is recording a later PASS round, not a resolve call. Item 10 is the review arm and covers all five decisive branches of reviewFindingResolvedAway plus the severity boundary. Item 11 states both scan ranges and their opposite expectations. A new item covers --no-state skipping the guard with a warning.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"medium","suggestion":"Rewrite as: from a state directory with R recorded rounds and no in-flight snapshot, the refused invocation leaves the file set under `.tp-review/\u003cspec\u003e/` byte-identical to before the call — naming `snapshot-round-\u003cR+1\u003e.md` as the file that must not appear."}
{"category":"completeness","class":"one-phase-cost-model","finding":"The section presents itself as the release's cost/benefit arithmetic, but every quantified term is audit-specific: 'per audit round', the shared-arm audit prompt, CodeFileCap affected-file lines, one result row per affected file. The same mechanism applies to tp review, which is the phase with the larger round counts (tp's own v0.31.0 review ran 22 rounds) and with prior-round carry re-presenting rows, so the review-side saving is both the larger term and the unquantified one.","found_by":1,"found_by_roles":["ax-economist"],"location":"§1.1","resolved":{"evidence":"§6 (Tests) corrected on this round's evidence. Item 7 asserts the absence of the directory, the state file AND the snapshot, since the snapshot is not the first write. Item 8 enumerates modes per command, dropping the audit flags that do not exist. Item 9 is the audit arm and its escape is recording a later PASS round, not a resolve call. Item 10 is the review arm and covers all five decisive branches of reviewFindingResolvedAway plus the severity boundary. Item 11 states both scan ranges and their opposite expectations. A new item covers --no-state skipping the guard with a warning.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"ax-economist","severity":"low","suggestion":"Add one sentence quantifying the review side (one reviewer sub-agent invocation, its prompt, and its findings rows through merge/dedup/read, per review round), or retitle the section so it is explicit that it costs the audit phase only and that review is analogous."}
{"category":"completeness","class":"arithmetic-not-stated","finding":"§1.1 answers the round-1 request for the release's arithmetic by naming the components but still states almost no numbers: it gives 50 for claudeMDExcerptLineCap and then leaves CodeFileCap unvalued (it is 10, internal/engine/auditfiles.go:18), states no preamble total, and does not multiply by the required clean rounds (review_clean_rounds/audit_clean_rounds default 2). The in-repo precedent §1.1 is answering — spec/0.31.2.md §6 — carries a per-role numeric table with signed deltas and explicit +~55 / +~65 best and worst cases. A section titled \"What a declined role costs\" that a reader cannot add up is a weaker fix than the precedent it cites.","found_by":0,"location":"§1.1","resolved":{"evidence":"§6 (Tests) corrected on this round's evidence. Item 7 asserts the absence of the directory, the state file AND the snapshot, since the snapshot is not the first write. Item 8 enumerates modes per command, dropping the audit flags that do not exist. Item 9 is the audit arm and its escape is recording a later PASS round, not a resolve call. Item 10 is the review arm and covers all five decisive branches of reviewFindingResolvedAway plus the severity boundary. Item 11 states both scan ranges and their opposite expectations. A new item covers --no-state skipping the guard with a warning.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"regression","severity":"low","suggestion":"Give the totals the way 0.31.2 §6 does: ~55 preamble lines (50-line Project Context cap + the Disposition block) plus up to 10 affected-file lines per shared-arm audit prompt, one sub-agent invocation and up to 10 result rows, times the clean-round count."}
{"category":"consistency","class":"stale-message-text","finding":"§2.1 says an unknown key 'remains a lint warning and is ignored, as today', and test 5 pins 'the existing \"not a permitted override key\" warning'. The existing message enumerates the permitted set inside itself — 'tp.%s.%s.%s is not a permitted override key (only focus); ignored' (internal/engine/frontmatter.go:163) — so leaving it unchanged makes it state something this release falsifies, while changing it silently contradicts 'as today' and the existing assertions in internal/engine/frontmatter_roleoverrides_test.go:74-75.","found_by":1,"found_by_roles":["architect"],"location":"§2.1","resolved":{"evidence":"The override contract is unchanged; the sections that consume it were corrected. §2.2 now names the real readers, §2.5 the per-command mode lists, §2.6 the two phase-specific arms.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"architect","severity":"low","suggestion":"Say in §2.1 that the warning's parenthetical becomes '(only focus, enabled)' while its identity and behavior are unchanged, and word test 5 against a stable substring ('is not a permitted override key') plus an assertion that the message names both permitted keys."}
{"category":"redundancy","class":"self-declared-state-echo","finding":"The --compact justification argues the entry carries nothing the driver lacks ('deactivation is declared in the spec the driver already has, unlike the silent skips skipped_roles exists to surface'). That argument applies just as well to the non-compact emission: the disabled-by-spec entry is a per-round echo of state the same driver authored, and the one mistake it might catch - a mistyped id - is already reported by the existing 'matches no active role' warning (§2.3). As specified it is output an agent reads every run to learn nothing it did not write.","found_by":1,"found_by_roles":["ax-economist"],"location":"§2.4","resolved":{"evidence":"§2.4's accepted --compact limitation stands as written; it is stated as a limitation with its mitigation rather than as a transparency claim the same paragraph contradicts.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"ax-economist","severity":"low","suggestion":"Name the decision the entry feeds - e.g. confirming tp honored the deactivation for an orchestrator that has been context-reset since writing the frontmatter - or drop the entry and the new disabled-by-spec reason, which would let §1.1 claim the strictly stronger 'this release adds no output bytes at all'."}
{"category":"completeness","class":"reorder-side-effect","finding":"Hoisting corpus resolution ahead of the snapshot write also hoists an existing abort. `ResolveActiveCorpus` returns an error on a malformed role file, and both commands turn that into exit 3 (internal/cli/review.go:652-656, internal/cli/audit.go:223-228). Today that abort happens after the round snapshot (and, in review, after the state index) exists, so an interrupted round is visible to --status and `tp resume`; after the move it happens with no artifacts on disk. The spec presents the reorder as affecting only the two new refusals and never acknowledges that the exit-3 malformed-role path changes its on-disk aftermath.","found_by":1,"found_by_roles":["implementer"],"location":"§2.5","resolved":{"evidence":"§2.5 rewritten twice over on this round's evidence. (1) The mode list is now PER COMMAND, because tp audit registers no --resolve, --resolve-all, --verify or --report — an earlier list implied flags that do not exist and §6 item 8 inherited the error. (2) The ordering clause now covers every write the emission path performs, not just the snapshot: tp review's loadReviewRoundState calls EnsureReviewState, which creates .tp-review/\u003cspec\u003e/ and saves a fresh state.json BEFORE WriteSnapshotAtomic runs, so resolution moves ahead of that too and §6 item 7 asserts all three absences.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"implementer","severity":"low","suggestion":"State the intended post-move behavior for the malformed-role abort (no snapshot, no in-flight round) as a deliberate consequence, so an implementer does not treat an existing test that asserts the current artifacts as a regression."}
{"category":"completeness","class":"untested-clause","finding":"\"A phase with no recorded round has nothing unresolved and deactivation proceeds\" is the base case guarding a nil-state path (`LoadReviewState` returning nil, or `IsMissingStateIndex`), and it is the state every first round is in — the most common invocation of the new feature. No §6 item covers it.","found_by":1,"found_by_roles":["tester"],"location":"§2.6","resolved":{"evidence":"§2.6 rewritten as two phase-specific arms, because the attempt to unify them was the source of the errors. Review: the predicate is reviewFindingResolvedAway AND the severity filter ReviewRoundClean applies under the effective review_converge_on — the earlier 'exactly the set that keeps a round from being clean' was false since v0.31.0, where a medium finding does not block; the scan covers every recorded round (matching loadReviewRoundState); role attribution comes from the recorded row's own `role` field, not the (location, class) dedup key which carries no role; the escape hatch is tp review --resolve against the recorded round file. Audit: the scan is the LATEST round only (matching loadAuditPriorRound), the predicate is non-PASS, and there is no escape command because tp audit has no --resolve — a row stops blocking when a later recorded round reports PASS, so the refusal points at the audit loop. The exit code moves from 2 to 4: the trigger is recorded state, and tp reserves 4 for state. --no-state is addressed: the guard needs state, so under it the guard is skipped WITH a warning rather than silently permitting the deactivation.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"low","suggestion":"Add a test item: with no `.tp-review/\u003cspec\u003e/` state at all, `enabled: false` on a role emits normally and refuses nothing."}
{"category":"consistency","class":"exit-code-convention","finding":"Both refusals use exit 2, tp's documented usage-error code. §2.5's is an invocation/config mistake and fits, but §2.6's verdict is decided by recorded state under `.tp-review/` — the situation tp's convention assigns exit 4 (state). Items 9, 10 and 15 bake exit 2 into assertions, so if 4 was intended the tests lock in the wrong code and a later correction is a breaking change to the agent-facing contract.","found_by":1,"found_by_roles":["tester"],"location":"§2.6","resolved":{"evidence":"§2.6 rewritten as two phase-specific arms, because the attempt to unify them was the source of the errors. Review: the predicate is reviewFindingResolvedAway AND the severity filter ReviewRoundClean applies under the effective review_converge_on — the earlier 'exactly the set that keeps a round from being clean' was false since v0.31.0, where a medium finding does not block; the scan covers every recorded round (matching loadReviewRoundState); role attribution comes from the recorded row's own `role` field, not the (location, class) dedup key which carries no role; the escape hatch is tp review --resolve against the recorded round file. Audit: the scan is the LATEST round only (matching loadAuditPriorRound), the predicate is non-PASS, and there is no escape command because tp audit has no --resolve — a row stops blocking when a later recorded round reports PASS, so the refusal points at the audit loop. The exit code moves from 2 to 4: the trigger is recorded state, and tp reserves 4 for state. --no-state is addressed: the guard needs state, so under it the guard is skipped WITH a warning rather than silently permitting the deactivation.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"low","suggestion":"Say why §2.6 uses 2 rather than 4 (e.g. \"both refusals reject the invocation as written, so both use 2\"), or switch §2.6 to 4 and update items 9, 10 and 15."}
{"category":"completeness","class":"vacuous-assertion","finding":"Only half of the round-1 finding on this item was fixed. The field name was corrected (review_roles_hash/audit_roles_hash -\u003e roles_hash, now item 13), but the other half stands: ComputeRolesHash reads only .tp/\u003cphase\u003e/*.json and never opens the spec, so \"toggling `enabled` ... leaves its `roles_hash` unchanged\" is true by construction and green for any implementation. The two readings the finding named are still both available — a unit test on the two hash functions, or an integration test that records rounds and diffs .tp-review/\u003cbase\u003e/state.json — and only the second one pins anything.","found_by":0,"location":"§3","resolved":{"evidence":"§3 (corpus staleness) is unchanged in substance and continues to assert the observable field name roles_hash.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"regression","severity":"low","suggestion":"Pin the observable form: record a round, toggle `enabled` in the frontmatter, record another, and assert the two rounds in state.json differ in spec_hash and match in roles_hash. Or state plainly in §3 that the assertion is a regression guard against a future implementation folding overrides into the roles hash."}
{"category":"redundancy","class":"drafting-history-leak","finding":"Non-Goal 2 narrates the spec's own review history rather than stating a decision: \"An earlier draft made the trim-candidate rendering name both levers. That was wrong about the tree and about the audience...\". No other Non-Goal is written against a draft that no reader of the shipped spec has seen, and the entry is the longest of the six because most of it is the rebuttal rather than the boundary. The same paragraph is also the only place that carries the trim-candidate facts §4 depends on, so the release's rationale for a documentation requirement now lives inside a retraction.","found_by":0,"location":"§5","resolved":{"evidence":"§5 (Non-Goals) stands; Non-Goal 2's reasoning for deleting the trim_candidate rendering change is unchanged and the guidance it displaces now lives in §4 with the fallback exception stated.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"regression","severity":"low","suggestion":"State it as a boundary: the trim_candidate field, its computation, its TTY flag and the JSON payload are unchanged; the two-lever guidance is documentation only (§4), because the only rendered surface is `tp review --report`'s TTY block, audit has none, and agents read JSON. Drop the draft narration."}
{"category":"completeness","class":"dropped-assertion","finding":"The test list dropped an assertion the round-1 list carried. Old item 9 ended \"...the same deactivation succeeds against a spec with no recorded round\", covering the rule §2.6 still states in its last sentence (\"A phase with no recorded round has nothing unresolved and deactivation proceeds\"). New items 9, 10 and 11 all exercise the blocking direction; none exercises the no-recorded-round pass. The rewrite grew the list from 12 to 16 items, so this is an unintended loss rather than a budget decision.","found_by":0,"location":"§6","resolved":{"evidence":"§6 (Tests) corrected on this round's evidence. Item 7 asserts the absence of the directory, the state file AND the snapshot, since the snapshot is not the first write. Item 8 enumerates modes per command, dropping the audit flags that do not exist. Item 9 is the audit arm and its escape is recording a later PASS round, not a resolve call. Item 10 is the review arm and covers all five decisive branches of reviewFindingResolvedAway plus the severity boundary. Item 11 states both scan ranges and their opposite expectations. A new item covers --no-state skipping the guard with a warning.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"regression","severity":"low","suggestion":"Restore the clause on item 9 or add a short item: on a spec with no recorded round of that phase, `enabled: false` on any role proceeds and emits normally."}
{"category":"ambiguity","class":"vacuous-assertion","finding":"\"--compact still omits skipped_roles, including entries carrying the disabled-by-spec reason\" asserts a property of an absent field: under --compact there is nothing to inspect, so the \"including...\" clause is unobservable and the item collapses to \"the key is absent\" — which passes on a build that never records `disabled-by-spec` at all.","found_by":1,"found_by_roles":["tester"],"location":"§6 item 14","resolved":{"evidence":"§6 (Tests) corrected on this round's evidence. Item 7 asserts the absence of the directory, the state file AND the snapshot, since the snapshot is not the first write. Item 8 enumerates modes per command, dropping the audit flags that do not exist. Item 9 is the audit arm and its escape is recording a later PASS round, not a resolve call. Item 10 is the review arm and covers all five decisive branches of reviewFindingResolvedAway plus the severity boundary. Item 11 states both scan ranges and their opposite expectations. A new item covers --no-state skipping the guard with a warning.","resolved_at":"2026-08-05T14:00:46Z","status":"fixed"},"role":"tester","severity":"low","suggestion":"Phrase it as a paired run on one fixture: without --compact the role is listed with reason `disabled-by-spec`; with --compact `skipped_roles` is absent entirely."}
