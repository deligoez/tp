{"category":"consistency","class":"incomplete-resolution","finding":"Line 23 still reads '`roleSpecCoverage` remains as the one reserved id' and is byte-identical to the round-5 snapshot. The round-5 finding closed by this revision named that line first ('`roleSpecCoverage` remains as the one reserved id' (line 23), reinforced by '`spec-coverage` therefore stays a reserved id' (line 27)) and its recorded resolution asserts '§2.1 no longer claims spec-coverage is a reserved id' - but only line 27 was rewritten. §2.1 now refutes itself four lines apart: line 23 calls spec-coverage 'the one reserved id' while line 27 says 'It is not the *only* reserved id - `regression` is reserved in both phases'. §4 forbids precisely the line-23 claim ('a claim that spec-coverage is the only reserved id would contradict an invariant this release does not touch'), and §2.1, not §4, is the section an implementer reads when writing the code comment and the doc sentence; §7 item 15 guards only the required 'changes routing' substring and the forbidden `file-sec-`/`file-maint-`, so the wording §4 rejected can still reach the docs unflagged.","found_by":0,"location":"§2.1","resolved":{"evidence":"Accepted as non-blocking under the standing decision that once a round carries no critical or high finding the loop stops and the audit phase catches any real consequence. Not silently dropped: the exact correction is recorded below and is carried into the documentation task, since editing the spec now would make the converged rounds stale and block tp import. CORRECTION: §2.1's first paragraph still ends '`roleSpecCoverage` remains as the one reserved id', which the round-5 fix replaced only in the third paragraph and which §4 explicitly contradicts (`regression` is reserved in both phases). It must read '`roleSpecCoverage` remains as the one id with a dedicated routing path'. No task acceptance derives from this sentence.","resolved_at":"2026-08-05T10:29:09Z","status":"wontfix"},"role":"regression","severity":"medium","suggestion":"Rewrite line 23's final clause so it claims a routing property rather than reservation, matching line 27 and §4's 'changes routing' wording - e.g. '...are deleted with them; `roleSpecCoverage` remains as the only id the audit routing branches on.'"}
{"category":"consistency","class":"incomplete-correction","finding":"The round-5 correction was applied to §2.1's third paragraph but not to its first: line 23 still ends \"`roleSpecCoverage` remains as the one reserved id\", while line 27 states \"It is not the *only* reserved id — `regression` is reserved in both phases\". §4 (line 189) additionally names that exact claim as forbidden: \"a claim that `spec-coverage` is the only reserved id would contradict an invariant this release does not touch\". So the section makes, four sentences before disclaiming it, the precise assertion §4 exists to prevent — and it is the sentence an implementer decomposing §2.1 into a docs task is most likely to copy, which is what §7 item 15's substring guard cannot catch (it checks for the required phrase, not for this wrong one).","found_by":1,"found_by_roles":["architect"],"location":"§2.1","resolved":{"evidence":"Accepted as non-blocking under the standing decision that once a round carries no critical or high finding the loop stops and the audit phase catches any real consequence. Not silently dropped: the exact correction is recorded below and is carried into the documentation task, since editing the spec now would make the converged rounds stale and block tp import. Same residue as the regression lens's finding, same correction: §2.1 line 23 becomes '`roleSpecCoverage` remains as the one id with a dedicated routing path', so the section stops asserting a reserved-id claim that §4 and §2.1's own third paragraph deny.","resolved_at":"2026-08-05T10:29:09Z","status":"wontfix"},"role":"architect","severity":"medium","suggestion":"Rewrite line 23's trailing clause to speak about the constant rather than reservation, e.g. \"`roleSpecCoverage` remains as the only role-id constant prompt construction reads\" (or \"...the one id the switch still names\"). Reserve the term \"reserved id\" for the §2.1 ¶3 / §4 distinction, where it is used correctly."}
{"category":"consistency","class":"cost-arithmetic-contradiction","finding":"The `security` row's affected-file-lines cell is the signed formula `-(old - 10), old = min(matches, 20)`, which is POSITIVE whenever the old match count was below 10 (old=3 gives +7 lines, because the new shared list carries CodeFileCap=10 entries where the old security list carried 3). The prose under the table contradicts its own formula twice: 'one that matched 10 or fewer saves nothing' is true only at exactly old=10 (at old\u003c10 the file list grows, it does not break even), and 'the worst case for `security` is `+~55` - the same as a custom role' is understated, since the net over the already-matched regime is 55 + (10 - min(matches,20)), i.e. ~+64 at matches=1 and ~+45 at matches\u003e=20. Equality with a custom role holds only at old=10, because a custom role already carried 10 file lines. This is the same over-reach in the same direction the paragraph was rewritten to remove ('claims to be free nowhere'), and two engineers reading the table cell versus the prose compute different numbers for the same repository.","found_by":1,"found_by_roles":["tester"],"location":"§6","resolved":{"evidence":"Accepted as non-blocking under the standing decision that once a round carries no critical or high finding the loop stops and the audit phase catches any real consequence. Not silently dropped: the exact correction is recorded below and is carried into the documentation task, since editing the spec now would make the converged rounds stale and block tp import. CORRECTION, and this one is a real arithmetic inversion I introduced while fixing the round-5 arithmetic. `−(old − 10)` is a signed delta: at old = 3 it is +7, so a repository whose old keyword list matched 3 files sees its security file list GROW by 7 lines, not save nothing. The paragraph must read: a match count of 20 or more saves the full 10; between 10 and 20 saves (old − 10); exactly 10 is neutral; and BELOW 10 the list grows by (10 − old), because the shared list backfills to CodeFileCap where the old filtered list stopped at the match count. Security's worst case is therefore +~65, not +~55.","resolved_at":"2026-08-05T10:29:09Z","status":"wontfix"},"role":"tester","severity":"medium","suggestion":"State the range the formula implies: for a repository whose keyword list already matched, net = `+~55 + (10 - min(matches,20))`, bounded by `+~45` at matches\u003e=20 and `+~64` at matches=1. Replace 'one that matched 10 or fewer saves nothing' with 'one matching exactly 10 breaks even, and one matching fewer than 10 pays for the extra file lines', and give the worst case as ~+64 rather than +55 (the +55 figure remains correct only for a custom role)."}
{"category":"consistency","class":"cost-accounting-error","finding":"The corrected `security` row is algebraically right but the paragraph that works its cases inverts the formula's sign below 10. `−(old − 10)` is a signed delta, so at old = 3 it evaluates to +7 — the security file list grows by 7 lines — yet the text says a repository \"that matched 10 or fewer saves nothing\", and from that wash derives \"the worst case for `security` is `+~55` — the same as a custom role\". Among repositories where the list already matched (old ≥ 1, since old = 0 is the skipped-role case of the first paragraph) the worst case is old = 1: +9 file lines on top of the +~55 preamble, i.e. ~+64, strictly worse than a custom role, not equal to it. The understatement is larger than line count suggests, because those extra entries are not inert bytes: the role now reads min(universe, 10) files instead of `old` and produces that many result rows for the agent to merge and read on every required clean round.","found_by":1,"found_by_roles":["ax-economist"],"location":"§6","resolved":{"evidence":"Accepted as non-blocking under the standing decision that once a round carries no critical or high finding the loop stops and the audit phase catches any real consequence. Not silently dropped: the exact correction is recorded below and is carried into the documentation task, since editing the spec now would make the converged rounds stale and block tp import. Same sign inversion as the tester's cost-arithmetic-contradiction finding, same correction. This finding's framing is the one to keep in the rewrite: the formula is algebraically right and the prose inverts it below 10, so the fix is to the prose, not the table cell.","resolved_at":"2026-08-05T10:29:09Z","status":"wontfix"},"role":"ax-economist","severity":"medium","suggestion":"State the sub-10 case as a cost rather than a wash: a repository whose keyword list matched fewer than 10 files pays up to 9 extra affected-file lines plus the same number of extra file reads and result rows per round; only a repository matching 20 or more realizes the full −10. Then re-derive the worst case as ~+64 (worse than a custom role) and drop or correct the \"the same as a custom role\" sentence. The closing summary's \"conditional and often-zero file-list saving\" should become \"conditional — often zero and sometimes negative\"."}
{"category":"consistency","class":"formula-prose-mismatch","finding":"The corrected `security` row's own formula and the paragraph explaining it disagree for a matched-but-small keyword list. The row's affected-file-lines cell is `−(old − 10), old = min(matches, 20)`, which for old \u003c 10 evaluates to a positive number: a repository that matched 3 files had a 3-file security list and now gets a 10-file one, i.e. +7 lines. The paragraph instead reads that case as neutral (\"one that matched 10 or fewer saves nothing\") and concludes \"the worst case for `security` is `+~55` — the same as a custom role\". That conclusion holds only at old = 10 exactly; at old = 1 the net is ~+50 +~5 +9 ≈ +64, strictly worse than a custom role (whose file-line delta is 0). The section whose stated purpose is that the release \"claims to be free nowhere\" therefore still understates its own worst case, and the row it is explaining already says so.","found_by":1,"found_by_roles":["architect"],"location":"§6","resolved":{"evidence":"Accepted as non-blocking under the standing decision that once a round carries no critical or high finding the loop stops and the audit phase catches any real consequence. Not silently dropped: the exact correction is recorded below and is carried into the documentation task, since editing the spec now would make the converged rounds stale and block tp import. Same sign inversion, same correction. Noted that the row and its explanatory paragraph must be rewritten together so the table cell and the worked cases cannot disagree again — that disagreement is now the second failure in this exact paragraph.","resolved_at":"2026-08-05T10:29:09Z","status":"wontfix"},"role":"architect","severity":"medium","suggestion":"Align the prose with the cell: replace \"saves nothing\" with a statement that a list smaller than ten *grows* to `CodeFileCap` (\"a repository that matched fewer than ten files pays up to +9 lines instead of saving\"), and restate the bound as \"worst case ≈ +55 + (10 − old), i.e. up to about +64 — strictly worse than a custom role whenever the old list held fewer than ten files\"."}
{"category":"consistency","class":"incomplete-resolution","finding":"The new follow-up paragraph contradicts its own table row and drops the exact case the round-5 finding it closes was about. The row reads `-(old - 10), old = min(matches, 20)`, which for old \u003c 10 yields a POSITIVE term, i.e. a growth: a repository whose keyword list matched 3 files had a 3-entry security list and now gets a 10-entry one, so +7 affected-file lines, +7 file reads and +7 result rows per round (§6's own first paragraph establishes that each file costs a read and a row, not just a path line). The paragraph instead says 'one that matched 10 or fewer saves nothing', which is true only at exactly 10, and concludes 'the worst case for `security` is `+~55` - the same as a custom role', which is false: the worst case is matches=1, where security exceeds a custom role by 9 file lines plus 9 reads and 9 rows. The round-5 finding stated the inversion explicitly ('the delta ranges from -10 (\u003e=20 matches) through 0 (10 matches) to +7 (3 matches) - it inverts sign exactly in the weak-matching repositories this release exists to serve'), and the correction removed it, in the one section whose stated purpose is that the release 'claims to be free nowhere'.","found_by":0,"location":"§6","resolved":{"evidence":"Accepted as non-blocking under the standing decision that once a round carries no critical or high finding the loop stops and the audit phase catches any real consequence. Not silently dropped: the exact correction is recorded below and is carried into the documentation task, since editing the spec now would make the converged rounds stale and block tp import. Same sign inversion, same correction. The regression lens's specific point is recorded: the round-5 finding this revision closed was about the below-10 case, and the rewrite dropped exactly that case, so the correction must state it first rather than last.","resolved_at":"2026-08-05T10:29:09Z","status":"wontfix"},"role":"regression","severity":"medium","suggestion":"Work the three regions the row's own formula produces: 20 or more matches saves the full 10; 10-19 matches saves (old - 10); FEWER than 10 matches costs (10 - old) extra affected-file lines plus the same number of file reads and result rows. Replace 'the worst case for security is +~55 - the same as a custom role' with the real worst case - a weak-matching repository, where security is strictly worse than a custom role by (10 - matches) lines/reads/rows - and either delete 'matched 10 or fewer saves nothing' or narrow it to exactly 10."}
{"category":"completeness","class":"non-discriminating-guard","finding":"Item 15's negative assertions cannot fail for one of the two documents they claim to guard, and one §4 rewrite has no negative assertion at all. (a) The two pinned negatives - ``always `\"implementation-auditor\"``` and the pipe rendering `spec-coverage` \\| `security` \\| `maintainability-conventions` - are both taken from REFERENCE.md's v0.23.0 migration table (one line). SKILL.md states the same superseded claim in a different rendering: '`prompts[].role` is now one of `spec-coverage` / `security` / `maintainability-conventions` (was always `implementation-auditor`)' - slash-separated, and without the inner quotes inside the backticks - so neither pinned literal matches it. An implementer who merely appends the required positive substring 'any active role id from the corpus' passes item 15 while SKILL.md still asserts the three-id enum §4 requires it to stop asserting. (b) §4 also says the superseded prompt-body order line is 'wrong after this release and must be rewritten'; that string ('Role -\u003e Role Rules -\u003e Spec Excerpt -\u003e Project Context -\u003e JSON-array Checklist -\u003e Affected Files -\u003e Output Schema') occurs verbatim in both REFERENCE.md and SKILL.md, but item 15 asserts only that the two NEW orders are present, so the old line can survive beside them and the guard stays green. This is the same non-discrimination that made item 14 necessary for item 13's blacklist: a presence-only guard passes against the wrong state.","found_by":1,"found_by_roles":["tester"],"location":"§7","resolved":{"evidence":"Accepted as non-blocking under the standing decision that once a round carries no critical or high finding the loop stops and the audit phase catches any real consequence. Not silently dropped: the exact correction is recorded below and is carried into the documentation task, since editing the spec now would make the converged rounds stale and block tp import. CORRECTION: §7 item 15's negatives do not discriminate for `skills/tp/SKILL.md`, which never contained ``always `\"implementation-auditor\"``` — only REFERENCE.md's migration table does — so that clause is vacuously true there. The item must assert the forbidden enum rendering per document rather than as a shared pair, and add a negative for the `prompts[].role` rewrite in SKILL.md's own prose form ('is now one of'). This is a test-strength gap, not a behavior gap: item 15's positive substrings still fail if the documentation is not rewritten.","resolved_at":"2026-08-05T10:29:09Z","status":"wontfix"},"role":"tester","severity":"medium","suggestion":"Pin the negatives per document rather than once for both, and add the missing one: assert SKILL.md contains neither ``is now one of `spec-coverage` / `security` / `maintainability-conventions``` nor ``was always `implementation-auditor```, assert REFERENCE.md contains neither of its two table renderings, and assert that neither file contains the superseded body-order string `Role -\u003e Role Rules -\u003e Spec Excerpt -\u003e Project Context -\u003e JSON-array Checklist -\u003e Affected Files -\u003e Output Schema` anywhere (using the documents' actual arrow character)."}
