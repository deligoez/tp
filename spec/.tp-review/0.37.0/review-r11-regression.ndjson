{"role":"regression","location":"§3","class":"escalation-ground-falsified","severity":"critical","category":"feasibility","finding":"§3's new escalation paragraph rests on a ground the tree falsifies: \"This is what separates it from the two deadlocks in the table, where *every* import was refused and no document the unit could write would pass.\" For the uncovering import, two documents the unit CAN write pass, and neither needs a human. (1) Omit the top-level `workflow` key: internal/cli/importcmd.go:158 preserves the existing task-level block whenever `importedHasWorkflowKey(trimmed)` is false (internal/cli/import_workflow.go:11-22 keys on the raw top-level key), so the task-level `all` survives, resolved-before equals resolved-after, and the change rule passes. (2) Carry `\"audit_converge_on\": \"all\"` in the document's workflow block: §3 says \"a write of `all` never trips it\". So the refusal is a correctable authoring error the unit can fix in one edit, not a decision reserved for the operator — and escalating it stops the entire run (stop_reason: escalation) for something the unit already had the means to fix. §3 tells the unit neither remedy, and §7 holds no row asserting a passing document exists.","suggestion":"Replace the escalation claim with the true one: the refusal is answerable by the unit — re-emit the document without a top-level `workflow` key, or with `audit_converge_on` set to the value that already resolves — and reserve `tp escalate --decision audit-converge-on` for a unit that intends the relax. Add a §7 row asserting that both of those documents import cleanly under TP_UNATTENDED=1 over a task-level `all` and a project-level `blocking`, with the mutant 'refuse any import while the two layers disagree'."}
{"role":"regression","location":"§3","class":"escalation-does-not-resolve","severity":"high","category":"feasibility","finding":"The escalation route §3 names does not terminate. §3: \"the operator's resolution is to write the field explicitly into the imported document.\" Traced under `tp run`: the imported document is authored by the `decompose` unit itself (engine.UnitDecompose, internal/engine/unitkind.go:25; resumepayload.go:205 'decompose the converged spec into tasks and tp import'), and the decompose predicate is `taskFileHasTasks` (internal/engine/unitkind.go:206) — an import that wrote nothing leaves the task file at zero tasks, so DetectPhase returns `decompose` again. `tp escalate` writes a record and exits 2 and nothing else (internal/cli/escalate.go:91-119: engine.WriteEscalation, then os.Exit(ExitUsage)); there is no answer channel back, and SKILL.md states 'Nothing is replayed'. So after the operator answers and restarts, a fresh decompose unit writes a fresh document and the same import is refused again — the escalation loops. The two resolutions that DO survive a restart are ones §3 does not name: an attended `tp set --workflow audit_converge_on=blocking` at the layer the import will replace, or changing the project layer.","suggestion":"Name a resolution that survives the unit re-running: an attended write of the field at the task layer (which makes resolved-before equal resolved-after for every subsequent import), or a change to the project layer. Delete 'write the field explicitly into the imported document', which the next decompose unit overwrites."}
{"role":"regression","location":"§3","class":"incomplete-write-path-enumeration","severity":"high","category":"completeness","finding":"Round 10 raised this at medium (implementer, `incomplete-write-path-enumeration`); the rewrite did not fix it and made it load-bearing. §3 still closes the set — \"The three write paths are `tp set --workflow`, its `--project` form, and `tp import`\" — while `tp config --extract` writes BOTH layers for this field: it merges the hoisted value into .tp/config.json (internal/cli/config_extract.go:297-302, mergeCommon + engine.WriteProjectConfig) and then DELETES the field from every task file (line 303-309, engine.StripTaskWorkflowFields(f, fields)). The strip is keyed on the field NAME, so --extract is a second sink that removes the task layer — directly contradicting §3's new sentence \"`tp set --project` can only ever write the lower layer, so it cannot uncover anything; **the sink that uncovers is `tp import`**\". And §7 row 4 newly REQUIRES the field to be in `hoistedFields` (internal/cli/config_extract.go:108), so this release deliberately wires `audit_converge_on` into the fourth writer in §7 while §3 says there are three. (--extract is in fact resolution-preserving, because it writes the value one layer down before stripping and hoists only fields unanimous across every task file — but §3 makes no such argument, no §7 row asserts it, and `tp config --extract --force` is unfenced under TP_UNATTENDED.)","suggestion":"Either enumerate four write paths and state why --extract needs no fence — with a §7 row asserting the resolved value is unchanged across `tp config --extract --force` under TP_UNATTENDED=1, mutant 'hoist without stripping, or strip without hoisting' — or drop the field from §7 row 4's `hoistedFields` requirement and say so."}
{"role":"regression","location":"§2","class":"false-no-code-change-claim","severity":"high","category":"consistency","finding":"Both new paragraphs assert something the release's own §6a falsifies. §2: \"No line of `engine/divergence.go` changes; what changes is the doc comment calling that condition redundant\". §6a.5: \"the release changes no line of `divergence.go`. What it changes is a comment\". But `engine.DivergenceHint` — the shipped constant §6a.3 and §7 row 19 require to be rewritten — is DEFINED IN THAT FILE, at internal/engine/divergence.go:16-19, and the doc comment §6a.5 reverses is at lines 101-109 of the same file. So the release changes engine/divergence.go in two places, one of them executable code. A decomposer reading §2 would exclude the file from the release's edit set and then find §6a.3 demanding an edit to it.","suggestion":"Say what is true: divergence.go's LOGIC is unchanged — `ComputeAuditDivergence` keeps all five conditions — while the file changes twice, at `DivergenceHint` (§6a.3) and at the doc comment (§6a.5)."}
{"role":"regression","location":"§2","class":"clean-readers-uncounted","severity":"high","category":"completeness","finding":"§2's new paragraph opens \"**One shipped signal reads `clean` and therefore does change: `divergence`.**\" and justifies itself as the place \"where a reader looks for what `clean` touches\", but it is an uncounted uniqueness claim and it misses the most consequential reader: the resume/run phase oracle. internal/engine/resume.go:87 computes `auditConverged := Converged(auditRounds, wf.AuditCleanRounds, specHash)` from each round's stored `Clean`, and internal/engine/phase.go's DetectPhase turns that into `PhaseRelease` (rule 3, 'tasks exist, all done, audit converged → release'); `tp run` then exits 0 with stop_reason converged. So under `blocking` the driver declares a cycle releasable while `warning`/`info` rows are open — the release's actual behavioural consequence, and it is stated nowhere. Same shape as the claims this file has already had falsified twice (a uniqueness assertion over a set nobody enumerated).","suggestion":"Enumerate the readers of `clean` rather than asserting a count: `converged`/`consecutive_clean`, `next_action` (already named), `divergence`, and the resume/run oracle's phase and `tp run`'s converged exit. State explicitly that under `blocking` `tp resume`/`tp run` reach `release`/exit 0 over open advisory rows, since that is what an operator setting the knob is buying."}
{"role":"regression","location":"§3","class":"sink-reads-wrong-layer","severity":"medium","category":"ambiguity","finding":"Round 10 raised this (regression, `sink-reads-wrong-layer`); the rewrite dropped the phrase 'at every sink' but kept the rule and left the gap. §3 says the change rule \"compares resolved-before against resolved-after\" and names `tp import` as the uncovering sink, without saying which task file the import sink resolves against. The one reusable helper resolves the wrong one: `resolvedWorkflowForFence()` (internal/cli/unattended.go:90-98) resolves whatever `engine.DiscoverTaskFile(\".\", flagFile)` returns — the --file/TP_FILE/.tp/local.json active pointer — while `tp import` writes `targetPath` derived from the imported document's own `tf.Spec` (internal/cli/importcmd.go:141-143). Whenever the active pointer is not the import's target, the fence compares a file the write does not touch, and both false-refuse and false-pass are reachable.","suggestion":"State that each sink resolves the file it is about to write: `tp set --workflow` the discovered task file, `tp set --project` the project config under that same file, `tp import` the target derived from the document's `spec` field. Add it to §7 row 12 as an assertion with an active pointer deliberately pointed at a different spec."}
{"role":"regression","location":"§3","class":"fence-model-shape-mismatch","severity":"medium","category":"feasibility","finding":"§3 tells the implementer what to copy and the named model cannot carry the rule. \"The string-shaped fence tp already runs — placed before the type dispatch — is what to copy.\" The only string-shaped fence tp runs is `engine.FencedCommandField` (internal/engine/unattended.go:66-68), called at internal/cli/set.go:361, set_project.go:46 and set_local.go:41. It is value- and layer-INDEPENDENT: it refuses `runner`/`notify_cmd` at any value and any layer, reads no resolved value, and so supplies no comparison at all. The only code that compares a requested value against a resolved one is `fenceWorkflowWrite` (unattended.go:56-76) — the numeric fence §3 explicitly rules out two sentences earlier. Copied as instructed, the model yields a FIELD-scoped fence, which is the first rule §3's own table rejects as deadlocking Workflow A step 6.","suggestion":"Say that the fence borrows the PLACEMENT from the command-field check (before the type dispatch, so a string field is reached) and the RESOLVED COMPARISON from `fenceWorkflowWrite`, rather than naming either as the thing to copy."}
{"role":"regression","location":"§4","class":"compact-retention-unjustified","severity":"medium","category":"completeness","finding":"The revert to '≥1 non-`PASS` row' is correct and well argued, but it deleted the only stated ground for the `--compact` half and did not replace it. Round 10's text justified survival as 'it survives `--compact` *because* its mere presence then signals the accepted-open state' — true only while the key was gated on cleanliness. Under the new condition the key is present on every round that holds any finding, so its presence signals nothing, and `--compact`'s whole job is stripping what is not decision-bearing. §4 now asserts 'and it survives `--compact`' with no reason, and §7 row 16 still pins it with the mutant 'strip it as explanatory' — which is exactly the reading the section no longer refutes.","suggestion":"Give the retention its own ground under the new condition — e.g. that the breakdown is the only surface separating the round's blocking rows from its advisory ones, so a compacted payload without it cannot answer why a round was clean — or drop the --compact clause and row 16."}
{"role":"regression","location":"§6a","class":"uncounted-exhaustive-claim","severity":"medium","category":"consistency","finding":"§6a's new framing asserts two things it does not tie to an artifact. (1) \"The list is exhaustive over what `git grep` finds today; a sixth would be a defect in this section, not a surprise.\" No grep is named, no pattern is given, and no §7 row asserts the set is complete — rows 18-21 each check one member. This is the count that has already been wrong three times (one, two, three, now five), stated again as prose rather than as a runnable derivation. (2) \"Both additions were found by walking the call graph out of `clean` rather than by searching for the sentence\" is false for item 4: `REFERENCE.md`'s 'Honest Convergence Signals' paragraph is markdown prose, reachable by no call graph, and round 10's architect finding cites it as a document reference alongside the divergence comment. Item 1's counting rule, by contrast, verifies exactly: `TestAuditGate_NoEscapeHatchFromTheGate` (internal/cli/audit_signal_test.go:469-503) holds eleven assert./require. call sites, two naming `audit_converge_on` (lines 473-474), nine surviving including two `require.True(t, ok)` decode checks (493, 497) — 7 without them, as stated.","suggestion":"Replace the exhaustiveness sentence with the derivation: give the literal grep whose hit set is the five items, and add a §7 row asserting that grep returns nothing outside them after the edits. Fix the provenance sentence — one addition came from the call graph out of `clean` (item 5), the other from reading the document the guard pins (item 4)."}
