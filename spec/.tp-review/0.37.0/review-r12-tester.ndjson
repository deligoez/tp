{"role":"tester","severity":"critical","category":"consistency","location":"§7","class":"mutant-passes-assertion","finding":"Row 11's rewritten mutant provably cannot fail row 11, and the sentence justifying it is arithmetically false. The cell reads \"count the streak as a total rather than a trailing run — v0.35.0 has three clean-under-`blocking` rounds before round 3, so a total reaches 2 one round early\". Measured over the committed corpus (per-round non-PASS severity counted from spec/.tp-review/0.35.0/audit-round-*.ndjson): v0.35.0's clean-under-`blocking` map is `. C C . C C C C C` — round 1 carries 2 `error` rows, so exactly ONE clean round precedes round 3, not three. And the mutant changes no answer anywhere in the row: computing both a trailing run and a running total over the same cleanmaps gives v0.35.0 blocking 3/3 at audit_clean_rounds 2 and 2/2 at 1, all 9/9 and 8/8; v0.36.0 blocking 3/3 and 2/2, all never/never. All eight of row 11's number pairs hold identically under the named mutant, so a correct replay and the mutant are indistinguishable at this row's observable. Round 11 reported this cell as a fixture variation rather than a mutant; the rewrite replaced it with a mutant that cannot fail, which is the same defect in the form §7's new column header forbids.","suggestion":"Name a mutant that moves one of the eight numbers. Two that do, checked against the same cleanmaps: \"grade `error` as advisory\" makes every v0.35.0 round clean under `blocking`, so the blocking column reads 2 instead of 3 and round 4's halt disappears; \"treat a round with only `warning`/`info` rows as unclean under `blocking`\" collapses the blocking column onto the `all` column (9 and never). Drop the three-clean-rounds sentence, which no cleanmap supports."}
{"role":"tester","severity":"critical","category":"completeness","location":"§6","class":"sweep-scope-misses-named-instance","finding":"§6's sweep predicate is scoped to \"doc comments\" and therefore does not reach the two shipped comments §2 names as false under `blocking`. §2 states: \"Both audit sinks compute `next_action` from the round's `clean`, each carrying a comment asserting the equivalence this release breaks — that a round's non-`PASS` rows are exactly `!clean`.\" Verified in the tree: internal/cli/audit_record.go:124 (\"The just-recorded round is the latest, so its non-PASS rows are exactly !clean.\") and internal/cli/audit_record.go:389 (\"The latest recorded round holding open non-PASS rows is exactly !Clean.\"). Both sit inside function bodies — neither is a doc comment, so neither is picked up by the acceptance sweep as written (\"across `skills/`, `README.md`, `internal/engine/` and `internal/cli/` doc comments\"), and no §7 row covers them. §6's whole design is that the count is untrustworthy and \"the deliverable is the sweep, and the task's acceptance runs it rather than restating its result\" — a sweep whose stated scope provably misses the two instances the release itself names in §2 cannot carry that weight, and both survive as shipped false assertions with the gate green.","suggestion":"Widen the predicate's Go scope from \"doc comments\" to \"comments\" in `internal/engine/` and `internal/cli/`, and add a §7 row asserting that neither audit_record.go comment states the non-PASS/`!clean` equivalence, with the mutant \"restrict the sweep to declaration doc comments, which leaves both call sites' comments false\"."}
{"role":"tester","severity":"critical","category":"consistency","location":"§3","class":"fence-hole","finding":"§3's \"resolution-preserving by construction\" is false for a spec that has no task file, and that clause is the whole reason `tp config --extract` is exempted from the fence. Proven by running, not reading (tp built from HEAD at /tmp/tp-dev/tp): in a fresh git repo with a.md/b.md carrying `a.tasks.json`/`b.tasks.json` that both set a workflow field, plus lonely.md with NO task file, `TP_UNATTENDED=1 tp config --extract` exits 0 with no refusal and hoists the field into .tp/config.json; `tp config --resolved --file lonely.md` reports the field as `{source: default, value: 2}` before and `{source: project, value: 5}` after. Substituting `audit_converge_on`, the identical command changes lonely.md's resolved value from `all` to `blocking` — a relax of the gate, under the environment the fence exists for, with the change rule never firing because §3 asserts it cannot. The hole is not theoretical: 21 of this repository's 46 `spec/*.md` files have no `<base>.tasks.json` (counted). It also survives the import fence: once the project layer holds `blocking`, a later `tp import` for that spec compares resolved-before against resolved-after and sees no change, so row 13b's rule passes it.","suggestion":"Either fence `tp config --extract` by the same resolved-change rule applied over every spec the repository holds (not only those with task files), or narrow the claim: \"resolution-preserving for every spec that has a task file; a spec with none moves from the built-in to the hoisted value, so --extract is fenced for `audit_converge_on`\". Add a §7 row with the mutant \"exempt --extract because it hoists a unanimous value, which is resolution-preserving only over the specs it read\"."}
{"role":"tester","severity":"high","category":"completeness","location":"§2","class":"decided-branch-without-row","finding":"§2 decides two `next_action` branches this round and §7 tests one. The new table has two rows and the paragraph above it says \"**Both affected branches are decided here rather than left to an implementer**, because this is the only place an operator learns that a round closed over open rows.\" Row 10 asserts only the converged arm: \"a converged round holding non-`PASS` rows yields a `next_action` containing the count of accepted rows as a numeral\". The other decided branch — clean, not yet converged — is `AuditNextAction`'s default arm (internal/engine/nextaction.go:82-83, \"run the next audit round: tp audit …\"), and it is the arm that fires on EVERY non-final round under `blocking`, where the converged arm fires once per cycle. No row covers it, and §7's closing \"not tested here, named so the absence is deliberate\" paragraph names only the three streak surfaces. An implementation that appends the count on the converged arm alone passes all 26 rows while leaving the common case as it is today.","suggestion":"Extend row 10 to both arms — \"on both audit sinks, both a converged round and a clean-but-not-yet-converged round holding non-`PASS` rows carry the count\" — with the added mutant \"append the count on the converged arm only, which leaves the arm an operator meets every round unchanged\"."}
{"role":"tester","severity":"high","category":"ambiguity","location":"§2","class":"policy-conditioned-observable","finding":"§2's `next_action` table and row 10 give different answers on the scenario §2 itself constructs two paragraphs earlier. The table's third column is headed \"under `blocking`, when the round holds accepted rows\", conditioning the count on the resolved policy. Row 10 states the trigger with no policy term: \"a converged round holding non-`PASS` rows\". §2's \"Stamping is symmetric\" paragraph makes the two disagree on a reachable state — a round recorded under `blocking` keeps `clean: true` after the knob returns to `all`, so under `all` there exists a converged round holding non-`PASS` rows. §2's header says no count; row 10 says a count. The sinks are policy-free at that point (both compute the branch input from the stored `clean`: audit_record.go:125 passes `!clean`, audit_record.go:390 computes `!rounds[last].Clean`), so an implementer following row 10 writes rows-only code and one following §2's header threads the resolved policy into `AuditNextAction` — the same policy input §4 argues is unavailable and unstable at the emitting sink. Two engineers write contradictory tests over the same fixture.","suggestion":"State which it is in §2, and make row 10 say so. If the count is a property of the rows (consistent with §4 and with record-time stamping), retitle the table column \"when the round holds non-`PASS` rows\" and add to row 10 \"including under `all`, over a round stamped clean under `blocking`\" with the mutant \"gate the count on the resolved policy, which drops it for exactly the rounds §2's symmetric-stamping paragraph creates\"."}
{"role":"tester","severity":"high","category":"consistency","location":"§7","class":"mutant-passes-assertion","finding":"Row 19's second mutant cannot fail, because the guard it names compares each document against a literal the same author writes. Verified in internal/cli/docs_contract_test.go: line 96 defines `divergenceCountingSubstring` as a TEST-LOCAL literal and line 132 asserts `skills/tp/SKILL.md` contains it; line 145 asserts `skills/tp/REFERENCE.md` contains `engine.DivergenceHint`, i.e. the document against the constant whatever text the constant holds. Neither check pins any wording. So row 19's second mutant — \"Phrasing it as 'whose severity is blocking' is a second mutant\" — reddens nothing when applied to the deliverable as a whole: the implementer writes that phrasing into `engine.DivergenceHint`, into REFERENCE.md:879 (which must match the constant), into SKILL.md:149 and into the guard's literal, and both checks are green. The row's claim that the new sentence is \"asserted by the docs-contract guard's two independent checks\" is therefore false for the wording; the two checks assert only that four artifacts agree with each other. Row 19's FIRST mutant does fail (editing the constant alone leaves SKILL.md's sentence unchanged), so the row is half-discriminating.","suggestion":"Pin the replacement sentence in §6 as a verbatim literal, the way §6.2/§6.3 pin the sentence being replaced, and have row 19 assert \"the guard's `divergenceCountingSubstring` is exactly «…»\" — a spec-side literal is the only form a test can hold against an author who writes both sides of a Contains assertion."}
{"role":"tester","severity":"high","category":"completeness","location":"§7","class":"fixture-not-pinned","finding":"Rows 21 and 22 leave their fixture under-specified in exactly the way that lets both named mutants survive. `ComputeAuditDivergence` (internal/engine/divergence.go:114-127) returns nil at condition 1 when `SpecCoverageCleanRounds` is nil or below `RequiredCleanRounds`, and at condition 5 when the latest round's stored `RolesHash` differs from `CurrentRolesHash` — both BEFORE the row walk and before condition 4. Row 21 pins only \"clean and converged under `blocking` while holding non-spec-coverage rows\"; row 22 says \"the same fixture\" with the rows carrying no `role`. A fixture satisfying those and failing condition 1 emits no object under correct code AND under \"delete condition 4\" AND under \"exempt roleless rows from condition 4\", so both rows pass on their own mutants. The engine's fixtures are hand-assembled (`newDivergenceFixture`, internal/engine/divergence_test.go:32) and `DivergenceInputs.SpecCoverageCleanRounds` is a `*int` that defaults to nil, so the passing-for-the-wrong-reason fixture is the easy one to write. §7's own standard this round is that a row states any counting rule it depends on; these two depend on two conditions they never name, and §6.5 says row 22 exists precisely \"so the cost §6.5 accepts cannot change without a failing test\".","suggestion":"Add to both rows: \"with `spec_coverage_clean_rounds` at or above the resolved `audit_clean_rounds` and the latest round's stored `roles_hash` equal to the current corpus hash — without both, conditions 1 and 5 suppress the object before condition 4 is reached and either mutant passes.\""}
{"role":"tester","severity":"high","category":"feasibility","location":"§7","class":"unwritable-assertion","finding":"Two of row 18's three clauses cannot be written as a deterministic assertion. The row asserts that `TestAuditGate_NoEscapeHatchFromTheGate` \"still holds **eleven** `assert.`/`require.` call sites; the two naming `audit_converge_on` now assert it **is** a known workflow field, and the other nine are byte-identical\". The count is writable as a source-scanning guard (verified against the tree: internal/cli/audit_signal_test.go lines 473, 474, 481, 484, 485, 490, 493, 494, 497, 498, 500 — eleven, of which 473 and 474 name the field). \"Byte-identical\" names no baseline: a test written after the edit holds no pre-release copy of the nine, so the property is checkable only by reading the diff, which is review rather than a test. \"Now assert it **is** a known workflow field\" is a claim about what an assertion means, not about a value any assertion can compare. Three engineers write three different tests — one counts, one embeds nine literals, one drops the clauses — and only the first can fail.","suggestion":"Keep the count clause and replace the other two with what a test can hold: \"and the nine surviving sites still assert the same three subjects — a `wontfix` disposition does not clear `go-safety`'s open finding, `--status --check` still exits 1, and a streak entry carries exactly `role`, `consecutive_clean` and `open`\". For the reversal itself, assert the behaviour (`tp set --workflow --project audit_converge_on=blocking` exits 0 attended) rather than the assertion's wording."}
{"role":"tester","severity":"medium","category":"ambiguity","location":"§7","class":"unpinned-message-assertion","finding":"\"The refusal message names both\" is asserted twice and anchored nowhere, and one of the two rows asserting it produces no refusal. §3 says \"**The refusal message must name both**\" of the two remedies (omit the top-level `workflow` key; carry `\"audit_converge_on\": \"all\"`). Row 13d ends \"and the refusal message names both\" — but row 13d's own scenario is two imports that SUCCEED under `TP_UNATTENDED=1`, so no refusal is produced by the input the row describes; the refusal belongs to row 13b's input. Row 14 asserts the same thing again (\"the refusal's message names the two documents that pass (§3)\"). Neither row nor §3 pins a substring, so two engineers write incompatible assertions — one greps for `workflow`, one for `\"audit_converge_on\": \"all\"`, one for both — and a message naming only one remedy passes one of them, which is the failure §3 says stops a run (\"a unit that reads only 'refused' stops a run over something one edit fixes\"). Row 14's only pinned message anchor is the negative one.","suggestion":"Pin two positive substrings in §3 and cite them from ONE row (delete the duplicate clause from the other): the message must contain both `omit the top-level \"workflow\" key` and `\"audit_converge_on\": \"all\"`, and row 13d should assert only that its two documents exit 0."}
{"role":"tester","severity":"medium","category":"completeness","location":"§6","class":"coordinated-edit-undercount","finding":"§6 says \"**Items 1, 2 and 3 are three coordinated edits, not one**\" and counts the artifacts as the gate guard, SKILL.md's literal and the constant — but a fourth artifact is made mandatory by an existing gate assertion and is never stated as work. internal/cli/docs_contract_test.go:145 asserts `skills/tp/REFERENCE.md` contains `engine.DivergenceHint` verbatim, and skills/tp/REFERENCE.md:879 is that copy. Rewriting the constant (item 3) without rewriting REFERENCE.md:879 reddens `TestDocsCarryTheConvergenceSignalWording`, so a decomposer who builds the three edits §6 counts leaves the suite red. §6.3 states the fact (\"The guard asserts it against `skills/tp/REFERENCE.md`, which quotes it verbatim\") but never turns it into an edit, and no §7 row covers it — row 20 covers a DIFFERENT REFERENCE.md paragraph (\"Honest Convergence Signals\"), so REFERENCE.md is edited twice in this release and only one edit is named. §6 already added the analogous note for divergence.go (\"edited twice, not zero times\").","suggestion":"Say \"four coordinated edits\" and name REFERENCE.md:879's verbatim copy of the constant, exactly as §6's closing note names divergence.go's second edit; the existing guard already fails on the omission, so the fix is a sentence, not a test."}
{"role":"tester","severity":"medium","category":"consistency","location":"§7","class":"cross-ref-not-resolvable","finding":"Rows 18-22's \"derives from\" cells cite §6.1, §6.2-3, §6.4 and §6.5, and §6 has no numbered subsections — it is a single section holding a numbered list of five items that the section itself declares non-normative: \"**The list below is illustrative, not exhaustive, and the release owes the derivation instead of the count.**\" So five of the 26 rows derive from list items in a list the spec says not to trust, while the thing §6 calls the deliverable — the blockquoted sweep predicate — is cited by no row and named nowhere in §7's closing \"not tested here\" paragraph. A reader cannot tell whether the five rows are the coverage or regression pins for the instances known at authoring time, and §6's own history (\"The count has been one, two, three and five, and each number was falsified by the next round\") says a sixth instance is the expected case, with no row that would notice it. Note also that §7's opening rule is \"Every row derives from a numbered decision above\", and §6's items are numbered but explicitly not decisions.","suggestion":"Add a sentence to §7: \"Rows 18-22 pin the five instances known at authoring time; the sweep itself is task acceptance, because a test cannot enumerate a set the spec declines to enumerate.\" And cite the items as \"§6 item 1\" rather than \"§6.1\", so the reference resolves to what exists."}
{"role":"tester","severity":"low","category":"completeness","location":"§7","class":"unpinned-observable","finding":"Row 8's rewrite closes its mutant by conceding that the fail-closed rule has no discriminating observable at the grading sink: \"A type assertion alone does not discriminate: it yields `\\\"\\\"`, which is outside the enum and so blocking by the same rule — the fold is observable only in §4's buckets, which is row 17's job\". That is correct and is the right repair, but it leaves row 8 asserting exactly what rows 5-7 already assert (advisory vs blocking over one round), differing only in the fixture's severity shape — and, like rows 5, 6 and 7, row 8 names no sink, so an implementer may satisfy it by calling an unexported grading function rather than by recording a round. The consequence is that §2's fail-closed rule is verified end-to-end nowhere: the four shapes are graded in one place and bucketed in another, and no row asserts that the same four shapes make a RECORDED round's stored `clean` false.","suggestion":"Name the sink in row 8 — \"…is unclean under `blocking`, as `tp audit --record` stores it and `--status` reports it\" — so the four fixtures exercise the record-time stamp §2 decides rather than a grading helper."}
