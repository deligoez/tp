{"role":"ax-economist","severity":"critical","category":"feasibility","location":"§2","class":"unvalidated-agent-label-gates-release","finding":"The paragraph that makes this release safe to ship is false as written. It claims severity is \"already validated\" and that \"audit_schema.go rejects a row that omits it\", so \"nothing new is asserted by the agent being measured\". internal/cli/audit_schema.go is a prompt generator: it writes the sentence \"severity: field MUST exist; null for PASS, one of error|warning|info for PARTIAL or FAIL\" into the emitted prompt and carries severity nowhere else (one occurrence, in the example row on line 17). Nothing parses or rejects it. No audit-side code reads severity at all: the only audit row predicate is engine.AuditRowIsPass, which reads status alone, and internal/engine's severity references are all review-side (reviewconvergeon.go, reviewclean.go, cluster.go, locationcluster.go, vague.go, configresolve.go) with zero occurrences in auditrounds.go, rolestreaks.go, divergence.go or speccoverage.go. So audit_converge_on turns an unvalidated string written by the agent under audit into the thing that opens the release gate, which is verbatim the objection §6 Non-Goal 1 uses to defer scope for a fourth time. The AX cost the spec has not budgeted is the validator this argument assumes: a record-path refusal, its exit code, its hint and its test are new surface, not free.","suggestion":"Either add row-level severity validation on the --record path as its own budgeted task and restate §2 in terms of what actually rejects, or drop the \"nothing new is asserted\" argument and answer the self-declared-label objection for severity on its own merits."}
{"role":"ax-economist","severity":"critical","category":"regression","location":"§3","class":"self-declared-label-opens-the-gate","finding":"§3 never says who may set a disposition, and under the mechanism tp already ships the answer is: the agent being audited. tp audit --resolve is an ordinary CLI call with no operator fence — it is not among CLAUDE.md's four user-approved decisions, and engine/nextunits.go:188 plus engine/unitkind.go:264 define an audit-fix unit whose entire durable output can be a disposition with no code change (internal/cli/audit_resolve_test.go:104-118 asserts exactly that). So under §3 a unit can converge its own phase by writing wontfix on its own findings, and under tp run nothing is watching. This is the same hazard §2 cites to defer scope a fourth time, arriving through a different door, and it is strictly worse than scope because it needs no new field to exploit.","suggestion":"State who may write a disposition that stops counting. Either fence wontfix/duplicate behind the operator (an escalation or an explicit user-approved decision like --skip-gate and import --force), or restrict the exclusion to dispositions recorded outside the audited unit, and say which in §3 rather than leaving it to the implementer."}
{"role":"ax-economist","severity":"high","category":"regression","location":"§3","class":"shipped-guard-not-retired","finding":"§3 reverses a behaviour tp deliberately shipped and guards. internal/cli/audit_signal_test.go carries the comment \"a disposition is not a way to park a finding past the gate\" (:464-466) and asserts at :487 that \"a wontfix disposition does not clear go-safety's open finding\" and at :490 that --status --check stays shut over the disposed finding. That test, TestAuditGate_NoEscapeHatchFromTheGate, is on the protected guard list at spec/0.34.0-guard-tests.md:56. §7 test 5 states the new counting rule but never says the shipped guard is retired or which assertions move with it, so decomposition will meet a red gate with no instruction.","suggestion":"Name TestAuditGate_NoEscapeHatchFromTheGate in §7 (or in the section) with the exact assertions this release changes and what replaces each, so the reversal is a decision on the record rather than a test edit an implementer improvises."}
{"role":"ax-economist","severity":"high","category":"regression","location":"§2","class":"shipped-guard-not-retired","finding":"The same protected guard pins the absence of this release's central field: internal/cli/audit_signal_test.go:472-474 asserts that tp set --workflow --project audit_converge_on=blocking fails with \"unknown workflow field: audit_converge_on\", and spec/0.34.0.tasks.json recorded the handover verbatim — \"should it ever arrive, that assertion is the test that has to move with it\". §2 and §7 test 1 add the field without mentioning the guard, so the release opens with a known-failing protected test and no owner for it.","suggestion":"Add the guard's retirement to §7 test 1 or to §2, naming the file, the test and the assertion that is replaced."}
{"role":"ax-economist","severity":"high","category":"consistency","location":"§2","class":"contested-default","finding":"The release ships blocking as the default while its own §2.1 says \"the two cycles disagree, and one sample each way is not a policy\" and records a defect blocking would have shipped over (the WriteSnapshotAtomic fixed-.tmp collision, found at v0.36.0 round 7, four rounds after blocking would have closed the phase). A default is the one setting every project pays without reading this spec, so choosing the contested side spends the argument on everybody. §2.1 also states the cheaper alternative in the same breath: all \"reproduces the recorded history exactly, which is what makes the change opt-in rather than a reinterpretation\" — that property belongs to whichever value is the default, and it is discarded by defaulting to blocking.","suggestion":"Default to all, which changes no existing project's behaviour and leaves blocking as the opt-in the measurement supports; or, if blocking must be the default, state the decision rule that settles a split sample rather than resting on symmetry with review_converge_on."}
{"role":"ax-economist","severity":"medium","category":"consistency","location":"§2.1","class":"stale-measured-count","finding":"The table's v0.36.0 row says \"7 and open\". spec/.tp-review/0.36.0/ holds audit-round-1.ndjson through audit-round-13.ndjson — thirteen recorded rounds — and §5 of this same spec tabulates rounds 10 through 13 with spec_coverage_clean_rounds running 1 to 4. The row is six rounds stale and is contradicted sixty lines later in its own document. It is the row carrying the release's one decision with a recorded argument against it, and the parenthetical \"(r2, r3 carry zero error)\" is asserted over a cycle whose extent the row gets wrong.","suggestion":"Re-derive both cells from the thirteen recorded rounds, and bind the count to the artifact (the audit-round-N.ndjson file count under spec/.tp-review/0.36.0/) rather than restating a number that goes stale every round."}
{"role":"ax-economist","severity":"high","category":"completeness","location":"§5","class":"undecided-mechanism-in-spec","finding":"§5 is headed \"What this release must decide\" and decides nothing — \"the candidate shapes, neither chosen here\". The two candidates have different surfaces and different costs: converge_roles is a new list-valued workflow knob, which in this codebase means a case in each of engine/projectconfig.go, model/projectconfig.go, model/workflow.go, cli/config.go, cli/config_extract.go, cli/set.go, cli/set_project.go and cli/validate_project.go plus resolution through five layers; the alternative changes what --check means and demotes the existing all-roles count to advisory, which is a contract change for every consumer including tp run's exit-code branch. Decomposition cannot size or order the release while the choice is open, and §7 carries no numbered test for either.","suggestion":"Choose one shape here, or move §5 out of this release entirely and let it ship audit_converge_on alone — the spec-length data in CLAUDE.md argues for the second."}
{"role":"ax-economist","severity":"medium","category":"feasibility","location":"§5","class":"corpus-specific-default","finding":"converge_roles is proposed \"defaulting to the conformance role\", but tp has no general notion of one. engine.RoleSpecCoverage is the literal string spec-coverage, and role corpora have been fully user-defined since v0.26.0, so the default names a role that exists only in corpora that happen to use tp's own name for it. Every other project either sets the knob or gets a default matching nothing, and the spec does not say what --check does when the named role is absent from the corpus — which is the case that decides whether the gate fails open or closed.","suggestion":"State the default as the literal spec-coverage and specify the absent-role behaviour explicitly, or derive the convergence set from the corpus (roles whose checklist covers spec sections) so no per-project knob is needed."}
{"role":"ax-economist","severity":"high","category":"consistency","location":"§5a","class":"blocking-class-with-no-severity","finding":"§5a's change (2) makes a PASS row carrying a prose disclosure \"an unresolved item for convergence, not a clean one\", but a PASS row has no severity to classify: internal/cli/audit_schema.go:24 requires \"severity: field MUST exist; null for PASS\", and §2 itself records that \"every PASS row carries severity: null\" across v0.35.0 and v0.36.0. audit_converge_on — this release's headline mechanism and its default — is a predicate over the severity of non-PASS rows. So a disclosing PASS row is unclassifiable by the very gate shipping beside it: under blocking it can never block, under all it always does, and §5a says which in neither case. Two mechanisms in one release, and their interaction is undefined at exactly the point where both fire.","suggestion":"State what severity a disclosing PASS row carries, or give the disclosure its own counter outside the severity predicate, and add the interaction (a PASS-with-disclosure round under blocking and under all) to §7."}
{"role":"ax-economist","severity":"high","category":"feasibility","location":"§5a","class":"disclosure-penalises-the-discloser","finding":"§5a's two changes are in tension: (1) asks every role to volunteer what its prose knows that its row cannot carry, while (2) makes volunteering it the thing that keeps the round unclean. A role that discloses lengthens its own loop; a role that stays silent converges. Nothing can detect the omission, because the only evidence the disclosure was owed is the disclosure that was not written — so the mechanism is unfalsifiable in the direction it fails. §2.0a's own caveat depends on this working: it says a severity gate must not end rounds \"which would have changed their own method\", and §5a is the instrument nominated to notice that, running on an incentive that suppresses it.","suggestion":"Decouple the ask from the penalty: let a disclosure be recorded without failing the round it appears in (route it to the operator or to the next round's brief), or bound (2) so only a disclosure the role itself marks as actionable blocks — and say what makes the disclosure verifiable at all."}
{"role":"ax-economist","severity":"medium","category":"redundancy","location":"§5a","class":"unearned-prompt-bytes","finding":"§5a(1) adds a standing clause to every role's brief in every round, paid roles x rounds, for an event measured five times across two repositories. tp's own v0.36.0 is an entire release about what the emitted prompt costs, and this release adds to it without a byte budget. The spec also does not say whether the clause is emitted to every role or only to roles whose rows can be PASS, nor whether --role emission carries it, nor whether it survives --compact — each of which is a per-field decision this repo asserts in tests elsewhere.","suggestion":"Bound the ask: emit the clause only where it can fire (a role whose rows are all PASS, or a round that would otherwise be clean), and state its emission and --compact disposition the way §2.4 of spec/0.33.0.md states the signal fields."}
{"role":"ax-economist","severity":"medium","category":"redundancy","location":"§5b","class":"store-what-could-be-derived","finding":"§5b's \"cheap answer\" stores a not-run marker per absent role on every recorded round. tp already has everything needed to derive that set at read time: each recorded audit round stores a roles_hash, and the audit outputs already read the live corpus and compare against it (engine.ComputeRolesHash, divergence condition 5, roles_stale). The roles a round did not receive is the corpus role set minus the roles present in that round's rows — computable from data already on disk, with no new stored bytes per round, and it stays correct when the corpus changes, which a frozen stored marker will not.","suggestion":"Derive the not-run set at read time from the corpus and the round's rows; if a stored marker is still wanted, state what it buys over the derivation and what happens to it when the corpus changes underneath it."}
{"role":"ax-economist","severity":"medium","category":"completeness","location":"§5b","class":"undecided-mechanism-in-spec","finding":"§5b diagnoses a defect precisely and then commits to nothing: \"the cheap answer is that a recorded round should carry forward the roles it did not receive\" is a suggestion, not a clause. It does not say whether a carried-forward role's streak freezes or resets, what spec_coverage_clean_rounds reports when spec-coverage was not run (today it is a null the section calls a bug), or whether tp run's exit-code branch changes. §7 has no test for any of it, so the section as written adds review surface without adding shippable surface.","suggestion":"Turn §5b into a clause with the three answers above and one numbered test, or move it to spec/0.46.0.md, which §5b already names as where the finding came from."}
{"role":"ax-economist","severity":"high","category":"completeness","location":"§7","class":"section-without-acceptance","finding":"The eight tests cover §2 (1-4), §3 (5) and §4 (6-8). §5, §5a and §5b — three of the six substantive sections, including both mechanisms that change what counts as a clean round — have no numbered test. §5's acceptance is prose inside §5 (\"the test is the one v0.36.0 would have failed\"), which is where an implementer will not look, and §5a's two changes and §5b's carry-forward have no acceptance anywhere. A section with no acceptance is prose that costs review rounds and produces no surface, which is the exact non-converging shape §2.0a describes.","suggestion":"Give §5, §5a and §5b a numbered test each — promoting §5's inline fixture description into the list is the cheapest of the three — or drop the sections from this release."}
{"role":"ax-economist","severity":"low","category":"completeness","location":"§4","class":"unnamed-payload-key","finding":"§4 says the reset \"resets consecutive_clean to zero and says so in the payload\" without naming the key, its type, its value when no reset happened (absent key versus false), or its --compact disposition. Every comparable audit signal field in this codebase is specified to that precision — spec/0.33.0.md §2.4 pins emitted-null versus absent key, and internal/cli/audit_record.go:138 implements it — and --compact survival is a per-field decision this repo asserts in tests. As written the emitted byte cost of §4 cannot be sized, and §7 tests 6-8 say \"reports the reset on the machine-readable surface\" without pinning it either.","suggestion":"Name the key, give its shape in both the reset and no-reset cases, and state whether --compact keeps it; then have test 6 assert that shape rather than the phrase \"machine-readable surface\"."}
{"role":"ax-economist","severity":"low","category":"redundancy","location":"§6","class":"knob-count","finding":"§6 Non-Goal 4 fences off review convergence, which also fences off the one question an economist would ask before adding the knob: whether one converge_on could govern both phases instead of two. The second knob is not one field — in this codebase a settable workflow field costs a case in engine/projectconfig.go, model/projectconfig.go, model/workflow.go, cli/config.go, cli/config_extract.go, cli/set.go, cli/set_project.go and cli/validate_project.go, plus a row in REFERENCE.md and SKILL.md and resolution through five layers. The answer is probably that two knobs are right, since the phases can legitimately differ and a rename would be breaking; the spec should say so in a line rather than let the Non-Goal foreclose it silently.","suggestion":"Add one sentence to §2 saying why the policy is per-phase rather than shared, so the second knob is a decision rather than an inherited shape."}
