{"role":"tester","severity":"critical","category":"consistency","location":"§7","class":"mutant-passes-assertion","finding":"Row 14's column-4 cell states in its own words that the named mutant satisfies the row's assertion. The assertion is \"a fenced refusal records `--decision audit-converge-on` and its message names this field\"; the mutant is \"reuse the command-field message, which interpolates the field name and so satisfies a substring check\". Verified against the tree: internal/cli/unattended.go's refuseUnattendedCommandField formats \"%s names a command the driver executes and cannot be set under TP_UNATTENDED, at any layer\" with the field interpolated, so reusing it produces a message containing `audit_converge_on` and passes any check the row as written can support. The column was renamed to \"the mutant that must fail it\", and this cell holds a mutant the row provably cannot fail.","suggestion":"State the discriminating observable rather than the trap: pin what the message must contain that the command-field message cannot — e.g. the message must name the field AND the value that was refused AND must not contain the command-field clause 'names a command the driver executes'. Alternatively pin the new message's literal shape the way §3 pins the enum name."}
{"role":"tester","severity":"critical","category":"consistency","location":"§7","class":"mutant-passes-assertion","finding":"Row 9's observable cannot be moved by row 9's mutant. The assertion reads the stored `clean` back from `state.json`; the mutant is \"re-grade at read time in `engine.ConsecutiveClean`\". Verified: engine.ConsecutiveClean (internal/engine/reviewstate.go:429) takes `[]ReviewRound` and reads only `ReviewRound.Clean` — the struct carries round, findings, clean, recorded_at, file, spec_hash, roles_hash, id_scheme, harness_note and no severity — so a read-time re-grade there is not even constructible from its inputs, and if it were, it changes a derived value while leaving every stored byte in `state.json` identical. The row therefore passes under its own named mutant. This is separate from round 10's finding that the row pins no property of its fixture: the defect here is the observable/mutant pairing, not the fixture.","suggestion":"Either pin the observable the mutant moves (the `consecutive_clean` and `converged` values emitted by `tp audit --status` after the knob is switched, asserted equal to their pre-switch values) or keep `state.json` and name a mutant that writes it (a `--status` path that rewrites the stored round entry). One row cannot do both halves with one assertion."}
{"role":"tester","severity":"critical","category":"consistency","location":"§7","class":"mutant-passes-assertion","finding":"Row 8's mutant is invisible at row 8's observable. The assertion is that a non-`PASS` row is blocking for all four severity shapes (outside the enum, `null`, absent, not a string); the mutant is `row[\"severity\"].(string)`, \"which folds the last three into `\\\"\\\"`\". Under §2's own fail-closed rule an unrecognised severity is blocking, and `\"\"` is unrecognised — so the naive type assertion grades all four fixtures blocking exactly as a correct implementation does, and the row passes. The `\"\"` fold is observable only in §4's bucketing, which is row 17's subject; §2's grading cannot see it. So the release's marquee fail-closed row is non-discriminating against the exact defect §4 says it exists to prevent.","suggestion":"Give row 8 a mutant it can fail — e.g. \"treat an absent `severity` key as advisory\" or \"grade only on a string value, skipping non-string rows\" — and move the `\\\"\\\"`-fold mutant to row 17, where the bucket count is the observable that separates it."}
{"role":"tester","severity":"critical","category":"consistency","location":"§6a","class":"reversed-or-removed","finding":"§6a's title and item 1 say the gate guard's assertion is *reversed*; row 18 says the call sites \"become nine\". These predict different test files and cannot both be written. Verified by reading internal/cli/audit_signal_test.go:469-503: TestAuditGate_NoEscapeHatchFromTheGate holds exactly eleven `assert.`/`require.` call sites (473, 474, 481, 484, 485, 490, 493, 494, 497, 498, 500), of which exactly two (473, 474) name `audit_converge_on`, and two of the remaining nine are `require.True(t, ok)` decode checks — so §6a.1's counting rule verifies. But a *reversed* assertion still occupies its call site (it now asserts the write succeeds), which leaves eleven, not nine; only a *deleted* assertion leaves nine. Row 18 additionally cannot fail its own mutant: if the two are deleted, the nine survivors are precisely \"the ones that do not name `audit_converge_on`\", so \"delete the surrounding block\" satisfies the assertion verbatim.","suggestion":"Decide and state it once: either the two sites are replaced in place (then the row asserts eleven call sites of which two assert that `audit_converge_on=blocking` is accepted at exit 0) or they are removed (then the row must additionally assert where the positive assertion now lives, which is what makes deletion distinguishable from repair)."}
{"role":"tester","severity":"high","category":"ambiguity","location":"§3","class":"unpinned-escape-path","finding":"§3's escalation paragraph names no value, and one of the two values it could mean is still refused. It says such an import \"is an escalation rather than a deadlock\" because \"the operator's resolution is to write the field explicitly into the imported document\". In row 13b's scenario (task-level `all`, project-level `blocking`, imported document carrying a `workflow` key without the field) writing `audit_converge_on: all` preserves the resolved value and passes, while writing `audit_converge_on: blocking` changes the resolved value and is refused by the very change rule §3 states two paragraphs earlier. The paragraph's load-bearing claim — that this refusal differs from the two table deadlocks because a document the unit could write would pass — is therefore true for exactly one literal and the spec does not say which. No §7 row asserts the resolution path at all.","suggestion":"Name the literal: state that the resolving document is one carrying `audit_converge_on: all` (or whatever the resolved-before value was), and add a §7 row asserting that this import passes under `TP_UNATTENDED` while the row-13b document is refused. Without a row, the difference between an escalation and a deadlock is untested prose."}
{"role":"tester","severity":"high","category":"ambiguity","location":"§3","class":"env-activation-vocabulary","finding":"The fence's activation vocabulary is pinned at two points and the shipped predicate covers a wider set. §3 says the fence \"refuses under `TP_UNATTENDED=1`\" and row 12 repeats the literal `=1`; row 13c pins the negative case as \"`TP_UNATTENDED` unset\". engine.UnattendedActive (internal/engine/unattended.go:27) is on for any present, non-empty value other than `\"0\"` — `true`, `yes`, `00`, `\" \"` all activate, and `\"\"` and `\"0\"` do not, with TestUnattendedActive_ActivationVocabulary pinning that set. A test written from §3 and rows 12/13c alone passes an implementation that compares against the literal `\"1\"`, which would silently un-fence every harness that writes `true` — the precise case that predicate's comment says it exists for. The spec never says the new fence reuses that predicate.","suggestion":"State the fence's activation as \"under unattended mode as tp already defines it (`engine.Unattended`)\" rather than as the literal `TP_UNATTENDED=1`, and have row 12 or 13c assert one non-`1` activating value and `TP_UNATTENDED=0` as non-activating, so the boundary is asserted rather than assumed."}
{"role":"tester","severity":"high","category":"completeness","location":"§7","class":"unpinned-observable","finding":"Row 9b names no surface, and the one it would inherit cannot see its mutant. Row 9 pins `state.json` explicitly (\"the only surface carrying more than the latest round\"); row 9b opens with \"and the relaxing direction\" and names no read path, so an implementer either inherits `state.json` — where the named mutant, \"add a read-side floor that re-blocks it\", changes nothing, since a read-side floor by definition does not rewrite the store — or picks a payload the spec never named. Two engineers write two different tests, and one of them cannot fail. §7's own new rule for this round is that a row depending on a file or a function states it.","suggestion":"Name 9b's surface, and make it a read surface: `tp audit --status` after the knob returns to `all` reports `consecutive_clean` counting the round recorded under `blocking`. That is the value a read-side floor moves."}
{"role":"tester","severity":"high","category":"completeness","location":"§7","class":"unpinned-observable","finding":"Rows 5, 6 and 7 name no sink, so the release's central record-time decision is asserted nowhere end-to-end. Each reads as a predicate over one row (\"`info` is advisory: not clean under `all`, clean under `blocking`\"), which an implementer can satisfy by calling an unexported grading function directly. §2's \"`clean` is stamped at record time, not recomputed live\" is a claim about what `tp audit --record` writes, and the only row that touches a stored stamp is row 9, whose assertion is that stamps do NOT change. Nothing in §7 asserts that a round recorded *after* the knob is set is stamped under the new value — the prospective half of the decision, which is the half §2.1's whole offer rests on.","suggestion":"State the sink in rows 5-7 (\"`tp audit --record` stores `clean: true` for a round whose only non-`PASS` row is `info`, read back from `state.json`\"), or add one row that closes the prospective half explicitly."}
{"role":"tester","severity":"high","category":"completeness","location":"§2.1","class":"normative-clause-without-row","finding":"§2.1 places a shipped artifact in `skills/tp/SKILL.md` — \"`skills/tp/SKILL.md` carries the one-line command that derives it, next to the field's documentation, so the decision has a stated basis\" — and §7 has no row for it. It is not covered by row 19, which edits a different sentence in the same file (§6a.2's convergence-counting claim). Two independent edits to one file, one of them guarded and one not, is exactly the coordination defect §6a warns about for items 1-3, in the same release. Nothing in §7 fails if the command is never added, or is added and does not run.","suggestion":"Add a row asserting the command's presence in SKILL.md next to the field's documentation, and — since the section's claim is that the operator gets a number — that running it against the committed projection reproduces §2.1's own per-cycle severity mix."}
{"role":"tester","severity":"medium","category":"completeness","location":"§7","class":"mutant-column-not-a-mutant","finding":"Row 11's column-4 cell is a fixture variation, not a mutant. It reads \"replay with the field set at round *k*, which §2.1 says converges no earlier than *k* + `audit_clean_rounds`\" — that is a different input to the same correct implementation, so it describes a second test case rather than wrong code that the row's assertion must fail. Under the column's own header (\"the mutant that must fail it\") the cell is empty: no implementation defect is named, so nothing establishes that row 11's four number pairs discriminate a correct replay from a broken one.","suggestion":"Name a code defect — e.g. \"count `consecutive_clean` as a total rather than a trailing run, which makes `all` converge for v0.36.0\" — and promote the round-*k* case to its own assertion inside row 11, where §2.1 already states the expected result."}
{"role":"tester","severity":"medium","category":"completeness","location":"§7","class":"mutant-column-not-a-mutant","finding":"Row 20's column-4 cell is \"leave it, which the guard does not currently catch\" — the absence of the repair, not a mutant. Every assertion in the table fails when its subject is left unedited, so the cell measures nothing about this row's strength, and its second clause is a statement about existing coverage rather than about wrong code. Row 20 is the one row of the five §6a items whose column 4 carries no independent information.","suggestion":"Name what a wrong repair looks like: e.g. \"delete the whole 'Honest Convergence Signals' paragraph\", which also removes the stored-flag mention and would pass an assertion phrased only as absence — so the row must assert that the paragraph survives and no longer lists the flag."}
{"role":"tester","severity":"medium","category":"completeness","location":"§2","class":"normative-clause-without-row","finding":"§2 requires that invalid values are refused \"on the same terms and with the same hint as `review_converge_on`\", and no row asserts the hint. Row 3 asserts exit codes only (`ExitUsage` at the write sink, `ExitValidation` at the consuming sink). Hint parity is the half an agent actually consumes — it is what names the two legal literals — and it is the half that silently drifts, since nothing compares the two fields' messages.","suggestion":"Extend row 3 or add a row: the refusal message for an invalid `audit_converge_on` names both legal values and matches `review_converge_on`'s hint shape, asserted by comparing the two commands' stderr rather than by a literal copied into the test."}
{"role":"tester","severity":"medium","category":"completeness","location":"§2","class":"normative-clause-without-row","finding":"§2 states a new co-occurrence normatively and §7 neither tests it nor lists it as a deliberate absence: \"Under `blocking` a round can record `clean: true` beside a non-zero `open`, and that is correct\". §7's closing paragraph declares `role_streaks[].open`, `consecutive_clean` and `spec_coverage_clean_rounds` \"not tested here\" because they stay severity-blind — but that covers *not changing* them, whereas the sentence above is a positive claim about a state this release newly makes reachable. Row 10 comes closest and asserts `next_action`'s text, not `open`. An implementation that zeroes `open` on a clean round, or that refuses to stamp `clean` while `open` is non-zero, passes every row in the table.","suggestion":"Add one assertion on the same payload: a round recorded under `blocking` whose only non-`PASS` rows are advisory reports `clean: true` and `role_streaks[].open` greater than zero together."}
{"role":"tester","severity":"medium","category":"completeness","location":"§6","class":"normative-clause-without-row","finding":"Non-Goal 6 — \"`AuditRowIsPass` stays byte-exact on `status == \\\"PASS\\\"`\" — is a preservation claim about this release and appears neither as a §7 row nor in §7's deliberate-absence paragraph, which names only the three streak surfaces. Every other numbered Non-Goal either defers to `spec/0.46.0.md` (2, 3, 4), restates a role-scoping deferral (1) or is about review (5); 6 is the only one asserting that this release's own predicate is unchanged, and this release adds a second grading step immediately beside it. §7's opening rule is that every row derives from a numbered decision; the converse — a numbered decision with no row and no stated absence — is unhandled here.","suggestion":"Either add a row (a padded `\" PASS\"` row is non-`PASS` under both values of the field, so the severity grading runs strictly after the status predicate) or name Non-Goal 6 in the deliberate-absence paragraph with the existing guard that covers it."}
{"role":"tester","severity":"medium","category":"ambiguity","location":"§7","class":"unscoped-fence-row","finding":"Rows 13b and 14 do not state the environment, in a section (§3) whose first paragraph says \"Every clause below is scoped to the unattended path\". Row 12 says \"under `TP_UNATTENDED=1`\", row 13 says \"under the same environment\", row 13c states the attended case — but 13b's base case is recoverable only by reading row 13c, which happens to name 13b in its own scope list, and row 14's \"a fenced refusal\" presumes it. A reader writing rows in order reaches 13b before the sentence that scopes it.","suggestion":"Repeat the scope in 13b and 14 the way row 13 does (\"under the same environment\"). One clause each, and the three fence rows then stand alone."}
{"role":"tester","severity":"medium","category":"ambiguity","location":"§4","class":"emission-condition-not-iff","finding":"§4 states the emission condition as \"Emitted when the round holds at least one non-`PASS` row\" and row 15 asserts the biconditional, including \"**absent** on an all-`PASS` round\". \"Emitted when X\" does not by itself forbid emission when not-X, and the section's other governing sentence — \"Every row lands in a named bucket, because the point is that nothing hides\" — reads naturally as an argument for emitting `{}` rather than omitting the key. An implementer reading §4 and an implementer reading row 15 write different code, and only one of them is caught.","suggestion":"Write §4's condition as an iff — \"present exactly when the round holds at least one non-`PASS` row; absent, not empty, otherwise\" — since row 15 already depends on the exclusive reading."}
{"role":"tester","severity":"medium","category":"feasibility","location":"§7","class":"half-tautological-assertion","finding":"Half of row 2 is a tautology no implementation can fail. The row asserts that \"a `TP_AUDIT_CONVERGE_ON` in the environment and an `--audit-converge-on` flag both change nothing\", with the mutant \"add either layer to `engine.ResolveWorkflowLayers`\". Verified: that function (internal/engine/configresolve.go:233) takes exactly two `*model.WorkflowOverride` arguments, so adding an env layer to it does make the environment half fail — but no change to it can create a CLI flag, and a command invoked with an unregistered `--audit-converge-on` fails at flag parsing, which also \"changes nothing\". The flag half therefore passes both on correct code and on the named mutant, for two different reasons.","suggestion":"Split the flag half into an assertion that discriminates — the flag is rejected as unknown with the parser's exit code, so a future silently-accepted flag is caught — or drop it and let §2's prose carry the no-CLI-layer decision."}
{"role":"tester","severity":"low","category":"ambiguity","location":"§7","class":"unpinned-artifact","finding":"§7's own new rule for this round is that \"a row that depends on a file, a function or a counting rule now states it\", and rows 19 and 20 depend on a guard they do not name. Row 19 says \"asserted by the docs-contract guard's two independent checks\" and row 20 says \"which the guard does not currently catch\"; the guard is a specific test in `internal/cli/docs_contract_test.go`, which row 18 would have named under the rule the section just adopted. The inconsistency matters because §6a.2 distinguishes a test-local literal from a shipped constant and an implementer needs the file to tell which check is which.","suggestion":"Name the file and the test in rows 19 and 20 as row 18 names `internal/cli/audit_signal_test.go`."}
