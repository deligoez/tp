{"role":"architect","location":"§6a","class":"reversal-set-not-exhaustive","severity":"critical","finding":"§6a declares its five-item list exhaustive -- \"The list is exhaustive over what `git grep` finds today; a sixth would be a defect in this section\" -- and §2 of this same file names the sixth and seventh four paragraphs earlier. §2: \"Both audit sinks compute `next_action` from the round's `clean`, each carrying a comment asserting the equivalence this release breaks -- that a round's non-`PASS` rows are exactly `!clean`.\" Those two comments are shipped and I read them: internal/cli/audit_record.go:124 (\"The just-recorded round is the latest, so its non-PASS rows are exactly !clean.\") and internal/cli/audit_record.go:389 (\"The latest recorded round holding open non-PASS rows is exactly !Clean.\"). Neither is in §6a's five, and no §7 row reverses either. Two more, both in artifacts §6a already names and so not reachable by a fresh `git grep` for a new file: (a) skills/tp/SKILL.md:149 is ONE sentence, and §6a.2 names only its \"counts every non-PASS row\" clause -- the same sentence continues \"the stored per-round `clean` flag, `next_action` and the `--status --check` exit code are unchanged\" and ends \"so `--check` still exits 1 and `next_action` still reads fix-and-re-audit\", every clause of which §2 falsifies; (b) skills/tp/REFERENCE.md:883-885 (\"Whenever `divergence` is emitted over state tp itself recorded, `next_action` reads the fix-and-re-audit directive\") is a DIFFERENT paragraph from the \"Honest Convergence Signals\" paragraph §6a.4 names at 753-756, and is falsified by the case §6a.5's own repair does not cover: under `blocking` a round that is clean but not yet converged satisfies all five divergence conditions while both sinks pass `latestRoundHasFindings = !clean = false`, so the object is emitted beside branch 3, not branch 2. The count is therefore at least seven by the spec's own text, and §6a's stated standard is that a sixth is a defect in the section.","suggestion":"Re-derive the list from the call graph out of the stored `clean` flag rather than from a grep, and state the count as whatever that walk returns. Add at minimum: internal/cli/audit_record.go:124 and :389 (the two comments §2 already names); the remainder of skills/tp/SKILL.md:149, edited as a whole sentence rather than as the \"counts every non-PASS row\" clause; and skills/tp/REFERENCE.md:883-885. Give each a §7 row. Then either drop the exhaustiveness sentence or bind it to a named artifact -- \"the list is exactly the sites the walk in <task> enumerates\" -- so it cannot rot the way this one did in the round that wrote it."}
{"role":"architect","location":"§6a.5","class":"no-line-changes-claim-false","severity":"critical","finding":"§6a.5 and §2 both assert that no line of engine/divergence.go changes, and §6a.3 -- three items above it in the same list -- requires editing a line of that exact file. §6a.5: \"suppression is correct and the release changes no line of `divergence.go`.\" §2 line 100: \"No line of `engine/divergence.go` changes; what changes is the doc comment calling that condition redundant, which §6a.5 takes.\" But `engine.DivergenceHint` is declared at internal/engine/divergence.go:16-19 -- a `const`, executable code emitted verbatim as `divergence.hint`, not a comment -- and §6a.3 requires its final clause to be rewritten. The claim is false a second time on its own terms: the `ComputeAuditDivergence` doc comment §6a.5 itself edits is internal/engine/divergence.go:101-109, so the sentence \"the release changes no line of `divergence.go`. What it changes is a comment\" contradicts itself, since that comment IS a line of divergence.go. The sentence matters because it is the sizing signal a decomposer reads: taken literally it says divergence.go is out of scope, while the release must edit two separate places in it, one of them shipped code whose change propagates to internal/engine/divergence_test.go:166 and :176 and to internal/cli/docs_contract_test.go:145 (which asserts REFERENCE.md contains the constant verbatim, so REFERENCE.md:879 must be edited in the same commit or the guard reddens).","suggestion":"Replace both sentences with what is actually meant and make the scope explicit: \"No line of `ComputeAuditDivergence`'s BODY changes -- the suppression is already correct. Two other places in `engine/divergence.go` do change: the `DivergenceHint` constant (§6a.3) and the condition-4 doc comment (§6a.5), and because `internal/cli/docs_contract_test.go` asserts `REFERENCE.md` contains the constant verbatim, `skills/tp/REFERENCE.md`:879 changes in the same commit.\" Say \"no behaviour changes\" wherever \"no line changes\" is meant."}
{"role":"architect","location":"§6a.5","class":"suppression-hides-unattributed-open","severity":"critical","finding":"§6a.5 asserts \"the behaviour is already right\" on an argument that covers severity and not attribution, and the population it misses is measured, non-empty, and 100% inside the class `blocking` makes invisible. The argument: divergence \"hands the driver a decision about whether open findings should gate a release\" and under `blocking` \"that is exactly the state an operator has chosen, so suppression is correct.\" But divergence's third message form asks a different question, and it is one no operator answered by setting a severity policy: engine/divergence.go:130-132 and :187-190 count rows carrying NO role into `unattributed_open` and render \"none attributed to a role (possibly spec-coverage's)\" -- i.e. the warning that the spec-coverage streak this repository ships on may be understated because some open rows could be spec-coverage's. That is a caveat about the RELIABILITY of the conformance measurement, not about whether advisory findings gate. Nothing else in the payload carries it: engine/rolestreaks.go:132-134 (`auditRoundOpenByRole`) skips roleless rows outright, so `role_streaks[].open` cannot express them; §4's `by_severity` keys on severity; §2's new `next_action` carries only an aggregate count. Measured this session over all 90 recorded audit-round files (10,435 rows, 360 non-PASS): 20 non-PASS rows carry no role, and their severities are `warning` 11 / `info` 9 -- ZERO `error`. So in tp's entire recorded audit history every roleless open row is non-blocking under `blocking`, makes its round clean, and after `audit_clean_rounds` such rounds sets `converged`, which is condition 4 -- suppressing the only field that counts them. §2's own framing (\"divergence exists to ask whether open findings should gate a release and the operator has already answered\") is what hides this: the operator answered the severity question, and the attribution question is silently answered for them at the moment of the ship decision. §6a.5 also generalises from the wrong side of the fixture -- the case it reasons about is a round whose open rows are attributed, which is the only case a reader of the section constructs.","suggestion":"Either (a) state in §2 and §6a.5 that `unattributed_open` disappears from the payload once a `blocking` round converges, name that as an accepted loss, and say where the operator reads it instead -- the honest answer today is nowhere, so this needs a surface; or (b) make condition 4 severity-aware in one direction only: suppress on a converged round whose open non-PASS rows all carry a role, and keep emitting when `unattributed_open > 0`, which is exactly the state where a decision does remain. Whichever is chosen, add a §7 row with the mutant \"suppress on every converged round, which drops the 20-row roleless population measured in spec/.tp-review\", and drop \"the behaviour is already right\" unless (a) is taken."}
{"role":"architect","location":"§2","class":"unenumerated-uniqueness-claim","severity":"high","finding":"§2's \"**One shipped signal reads `clean` and therefore does change: `divergence`.**\" is a uniqueness claim over a set §2 itself enumerates differently two paragraphs earlier, and it is false on at least four counts. §2 line 84: \"`blocking` governs `clean`, and therefore also `next_action`, which is derived from it\" -- so `next_action` is a second shipped signal reading `clean`, named by §2 before the uniqueness sentence denies it. Read this session: engine/reviewstate.go:429-438 `ConsecutiveClean` walks `rounds[i].Clean`, and :509-511 `Converged` is `ConsecutiveClean >= required && !stale`, so the reported top-level `consecutive_clean`, the reported `converged`, and the exit code of `tp audit --status --check` all read the stored flag and all change value under `blocking`. `divergence` does not even read `clean` directly -- it reads `Converged` (divergence.go:120). The claim is exactly the shape CLAUDE.md names as this repository's recurring defect (\"never assert a quantifier you did not count\"), and here it is load-bearing rather than decorative: §6a.4's repair strikes ONE of the five items REFERENCE.md:753-756 lists as unchanged (`engine.Converged`, `engine.ConsecutiveClean`, the stored per-round `clean` flag, the `--status --check` exit code, `next_action`'s three-state precedence), leaving four standing and false, and §7 test 20 pins only that one strike. The undercount in §2 is what sized the repair in §6a.","suggestion":"Delete the count. Write instead: \"Everything downstream of the stored `clean` flag changes value under `blocking`: `consecutive_clean`, `converged`, the exit code of `tp audit --status --check`, `next_action` (above) and `divergence` (below). The first four are the release's intent; `divergence` is the one whose direction is not obvious, so it is stated here.\" Then extend §6a.4 and §7 test 20 to strike all five items from REFERENCE.md:753-756, not the `clean` flag alone."}
{"role":"architect","location":"§7","class":"repair-scoped-below-its-guard","severity":"high","finding":"§7 rows 19 and 20 are the release's two doc reversals, and neither can be verified by the mechanism the rows name. Row 19 says the two sentences are \"asserted by the docs-contract guard's two independent checks\". Read this session: internal/cli/docs_contract_test.go:96 defines `divergenceCountingSubstring = \"audit convergence still counts every non-PASS row\"` and :132 asserts `Contains(skill, ...)`. That is a 46-character SUBSTRING of skills/tp/SKILL.md:149, and the sentence it sits in continues \"...the stored per-round `clean` flag, `next_action` and the `--status --check` exit code are unchanged: audit convergence still counts every non-PASS row, so `--check` still exits 1 and `next_action` still reads fix-and-re-audit.\" Perform row 19's edit exactly as written -- qualify the counting clause -- and the guard goes green while the same sentence still promises three unchanged things this release changes and one `next_action` branch it replaces. The guard cannot catch that, by construction: it pins a substring, so it is blind to every clause around it. Row 20 states the same problem in its own mutant column: \"leave it, which the guard does not currently catch\". A column whose contract is \"the mutant that must fail it\" cannot be satisfied by a mutant nothing fails, and §7's own new rule this round -- \"A test whose subject the spec cannot name is a decision that has not been made yet\" -- is the rule row 20 breaks. Row 21 has the third variant: \"its comment no longer calls condition 4 redundant\" is an assertion over a Go doc comment, and no guard in this repository asserts a Go doc comment (the docs-contract guard reads markdown via `readRepoDoc` plus one exported constant).","suggestion":"For row 19, change the unit of repair from the clause to the sentence, and change the guard's literal with it: replace `divergenceCountingSubstring` with the whole rewritten SKILL.md sentence, so the guard fails until every clause is true under both values. For row 20, name the guard extension the row requires -- a new pinned sentence for REFERENCE.md's Honest Convergence Signals paragraph in `referenceSignalSentences` -- and rewrite the mutant as \"leave the paragraph, which the extended guard fails\"; a row whose mutant nothing catches is not a test. For row 21, drop the comment half of the assertion or state the mechanism (there is none today); the behavioural half stands on its own."}
{"role":"architect","location":"§2","class":"next-action-lacks-its-input","severity":"high","finding":"§2 decides a `next_action` recommendation neither audit sink can compute from what it passes, and §7 test 10's own mutant column shows why. §2: on a converged round holding non-`PASS` rows the recommendation \"states that the phase may proceed and names how many non-`PASS` rows were accepted, so the sentence differs observably from the one an empty round produces.\" Read this session: `engine.AuditNextAction(specPath string, converged, latestRoundHasFindings bool)` (internal/engine/nextaction.go:76) takes two booleans and no count, and BOTH sinks pass the negation of the stored flag -- internal/cli/audit_record.go:125 passes `!clean`, and :390 passes `len(rounds) > 0 && !rounds[len(rounds)-1].Clean`. Under `blocking` a converged round holding advisory rows has `clean == true`, so both sinks pass `(converged=true, latestRoundHasFindings=false)`: the function is handed exactly the inputs an EMPTY converged round produces and cannot render a count or differ by one byte. §7 test 10's mutant is \"branch on `clean` alone, which makes the two strings equal\" -- but branching on `clean` alone is the shipped code, so the mutant is the current state and the row asserts a behaviour no stated change reaches. The release therefore needs a third input to `AuditNextAction` (the round's non-PASS row count), which is a signature change to a shipped exported function that §2, §3, §4 and §6a all leave unnamed -- and which reverses the enumerated three-branch doc comment at internal/engine/nextaction.go:63-75 that §6a's exhaustive list also omits.","suggestion":"State the input in §2: \"`AuditNextAction` gains the round's non-`PASS` row count as a third argument; both sinks already hold it (`--record` from the round it just stored, `--status` from the latest round's rows) and both currently pass only `!clean`.\" Add `internal/engine/nextaction.go`'s three-branch doc comment to §6a as a reversed assertion, since a converged round now has two recommendations rather than one. Rewrite test 10's mutant as \"keep the two-argument signature, which hands both sinks the inputs an empty converged round produces\"."}
{"role":"architect","location":"§1","class":"opt-in-promise-broken-by-deliverable-3","severity":"high","finding":"This round's revert of §4's emission condition makes deliverable 3 unconditional, which falsifies deliverable 1's promise in the same list. §1 deliverable 1: \"Default `all`, which is the behaviour tp ships today, so a project that does not set it sees no change.\" §4 as reverted: \"Emitted when the round holds at least one non-`PASS` row, and it survives `--compact`. The condition is a property of the rows and nothing else.\" So `by_severity` appears on `tp audit --merge` for EVERY project -- including every project that never sets the field, which by deliverable 1 is every project -- and it appears in `--compact`, the mode §2.1 of spec/0.33.0.md says an agent driver runs in. Measured this session with the dev binary on a real round (spec/.tp-review/0.35.0/audit-round-4.ndjson, 200 rows, 4 non-PASS): today's `--merge` payload carries `by_role`, `by_status`, `duplicates_removed`, `findings`, `input_files`, `inputs`, `merged_count`, `output_path` and (non-compact only) `overlap_report`; `--compact` strips `overlap_report` and nothing else. `by_severity` would be a new key on both, for all projects, on a command §1 frames as buying an opt-in feature. §5 and §6 do not mention it, and §6a does not list REFERENCE.md's `tp audit --merge` output documentation among what changes -- a further instance of §6a's exhaustiveness gap. This is not an argument for reverting the revert: round 10's finding that the clean-under-policy condition emptied the buckets stands. It is that §1's \"sees no change\" and §4's \"a property of the rows and nothing else\" are now the two halves of a contradiction, and §1 is what a decomposer reads to size the release.","suggestion":"Fix §1 rather than §4. Rewrite deliverable 1 to scope its promise: \"Default `all`, which is the behaviour tp ships today, so a project that does not set it sees no change in what the audit gate counts.\" And rewrite deliverable 3 to say what §4 now decides: \"the breakdown of every non-`PASS` row on `tp audit --merge`, emitted whenever there is one -- under either value of the field, so it is the one part of this release every project sees.\" Then name `skills/tp/REFERENCE.md`'s `tp audit --merge` output section in §6a and give it a §7 row, since a new unconditional key in an agent-parsed payload is a documented-contract change."}
