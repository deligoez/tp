{"role":"ax-economist","location":"§6a","class":"exhaustiveness-claim-falsified","severity":"critical","category":"consistency","finding":"§6a stakes exhaustiveness and is falsified by the cheapest possible grep, which is the method §6a itself declares insufficient. It says the list 'is exhaustive over what `git grep` finds today; a sixth would be a defect in this section' and explains that the two additions 'were found by walking the call graph out of `clean` rather than by searching for the sentence, which is why searching had missed them'. Run this session: `git grep -n review_converge_on -- skills/ README.md` returns 6 hits, and three are shipped assertions this release falsifies or invalidates that are in none of the five. (a) `skills/tp/SKILL.md:153` — 'tp has no audit-side equivalent of `review_converge_on` yet.' This release is that equivalent; the sentence is not §6a item 2, which is the different sentence at SKILL.md:149 in the `divergence` block. (b) `skills/tp/SKILL.md:269` — the `tp set --workflow` field table enumerates every settable workflow field and omits `audit_converge_on`. (c) `skills/tp/REFERENCE.md:478` — the workflow-field table has a `review_converge_on` row and no `audit_converge_on` row. A fourth, adjacent instance: §6a item 2 characterises SKILL.md:149 as 'saying audit convergence counts every non-`PASS` row', but that sentence asserts three things unchanged — 'the stored per-round `clean` flag, `next_action` and the `--status --check` exit code' — which is exactly the over-promise §6a item 4 flags in REFERENCE.md's parallel paragraph. §7 row 19 pins only the counting clause, so the SKILL.md over-promise ships false with the guard green. The AX cost is the count itself: L266-269 is 571 bytes of self-defence ('The count has been one, two, three and is now five, and each rise came from someone counting rather than reasoning'), the number has now been wrong at 1, 2, 3 and 5, it has consumed a finding in four separate rounds, and it is wrong again.","suggestion":"Delete L266-269 entirely. Replace the count with the derivation a check can run: 'every sentence in `skills/`, `README.md` and `CLAUDE.md` that a `git grep -n \"review_converge_on\\|audit convergence\\|non-PASS\" -- skills/ README.md CLAUDE.md` reaches, plus every doc comment on the call graph out of `clean`.' Then §7 gets one row asserting that grep returns no sentence stating a rule false under either value of the field, instead of rows 19 and 20 naming two sentences by hand. A rule cannot be short by one; a number has been four times."}
{"role":"ax-economist","location":"§3","class":"enum-enumeration-drift","severity":"high","category":"completeness","finding":"§3 adds a fifth user-approved decision and a new `--decision` enum member, and the spec budgets for neither the enumerations that go stale nor the exhaustive test loop that will stay green. Measured this session: `internal/cli/escalate.go:41` ships the help string '--decision is one of skip-gate, raise-review-cap, raise-audit-cap, import-force'; `escalate.go:32` ships a parallel prose enumeration; `internal/cli/escalate_test.go:158` loops `[]string{\"skip-gate\",\"raise-review-cap\",\"raise-audit-cap\",\"import-force\",\"other\"}`, so adding a member without touching that loop leaves the suite green with the new member untested; `skills/tp/SKILL.md:372` says 'the four decisions above stop being available to it' and lists them; and `CLAUDE.md` carries the same 'four user-approved decisions' enumeration twice. §7 row 14 pins only that a fenced refusal *records* `--decision audit-converge-on` and that its message names this field — nothing pins that the closed enum's own enumerations were widened, and nothing pins that the new member is in the validated set. This is the same class as §6a's miss, one section earlier: the release changes a closed set and counts only the sites it thought of. Confirmed by count: the spec names `CLAUDE.md` 0 times and `README.md` 0 times, where the twin release spec/0.31.0.md — shipping the identical knob on the review side — names `CLAUDE.md` 5 times and `README.md` 2 times in its §9 Documentation section. (README itself is genuinely fine here: README.md:206 delegates the workflow field table to REFERENCE.md, so it needs no field row. CLAUDE.md is not fine.)","suggestion":"Add to §3: the `--decision` enum's shipped enumerations widen with it — `escalate.go`'s help and prose, `escalate_test.go`'s exhaustive loop, `skills/tp/SKILL.md`'s 'four decisions' paragraph and `CLAUDE.md`'s two. Then either fold them into §6a's rule (preferred, since they are the same class) or give §7 one row: the enum's validated set, its help text and its test loop agree, asserted by deriving the set from one place rather than restating it."}
{"role":"ax-economist","location":"§2.1","class":"recipe-obsoleted-by-own-repair","severity":"high","category":"redundancy","finding":"The SKILL.md severity-mix recipe was obsoleted by the very next repair commit and left standing, so the release now ships two routes to one number and the untested one is the one §2.1 names. Commit 32765c8 ('give 0.37.0's operator the number the default decision needs') added L130-134: 'the previous cycle's mix is recoverable only by counting `severity` across its `audit-round-*.ndjson`. No flag is added for this. `skills/tp/SKILL.md` carries the one-line command that derives it.' Commit 071bc52, the next commit on this file, reverted §4's emission predicate to a property of the rows. Measured this session over the whole tracked corpus (90 `audit-round-*.ndjson` files, 10,435 rows): 70 of 90 round files (78%) carry at least one non-`PASS` row, so under the reverted predicate `by_severity` is emitted on 78% of rounds under BOTH policy values, including the default `all` the previous cycle ran under. That breakdown is exactly the previous cycle's mix. The word 'only' in L132 is now false, and the release pays for a shell one-liner that no §7 row runs to produce a number `tp audit --merge` already computes over the same files.","suggestion":"Cut L130-134. Replace with one sentence in §2.1: the previous cycle's mix is read by running `tp audit --merge` over that cycle's `audit-round-*.ndjson` and reading `by_severity` — an existing command over files already in the tree, no new doc surface and nothing untested. If a SKILL.md sentence is still wanted, it is a cross-reference to `--merge`, not a recipe."}
{"role":"ax-economist","location":"§1","class":"forensics-share-of-spec","severity":"high","category":"redundancy","finding":"A quarter of the spec is the review loop's own drafting history, and it is not the knob that grew. Counted this session: 7,044 of 29,155 bytes (24.2%) and 81 of 353 lines sit in nine paragraph spans whose subject is a rejected draft, a superseded count, or a round postmortem — L3-16 (1,203 B, the round-7 postmortem blockquote), L53-58 (598 B, 'An earlier draft wrote `all` as...' including 'It was written while deleting the word *surviving* for imprecision'), L64-66 (235 B, 'because two earlier drafts left it to be inferred and both inferred it wrong'), L100-103 (375 B, 'the call graph out of `clean` is what found it'), L156-170 (1,289 B, §3's three rejected rules), L210-222 (1,170 B, §4's reverted draft and the dependency it no longer has), L236-244 (726 B, §5), L263-269 (571 B, §6a's count history), L308-317 (877 B, §7's round-9/10 postmortem). None of it is a decision to build; all of it is re-read by every role of every future round and by the decomposer. The load-bearing half of this finding is the control: the knob itself is NOT bloated. §2+§2.1 is 7,578 bytes; the same ground in the shipped twin — spec/0.31.0.md §3.3-3.5 (field, resolution, validation, exit-code split, recompute policy) plus §4 (blocking set, unknown severity is blocking) — is 8,248 bytes, so the audit knob's own sections are 8% SMALLER than the review knob's. Every byte of growth is in sections the loop wrote about itself: §7 is now 7,206 bytes (24.7% of the file) and has overtaken §2+§2.1, the decisions it derives from. Round 10 to round 11 alone added 13,674 bytes and removed 7,204 (snapshots 22,685 to 29,155).","suggestion":"Cut, in order of bytes returned: L3-16 (the postmortem belongs in the commit that rewrote the file — the git log already carries it); L308-317 (§7's preamble: keep one sentence, 'each row names the decision it derives from and the mutant that must fail it', and drop the round-9/round-10 measurements); L210-222 (§4: keep 'Emitted when the round holds at least one non-`PASS` row; the condition is a property of the rows and nothing else' and drop the reverted-draft narrative); L236-244 (§5, see the separate finding); L263-269 (§6a's count, see the separate finding); the second half of L53-58 and all of L64-66 and L100-103 (keep the 2,913-row measurement, which is load-bearing for the wording; drop 'It was written while deleting...', 'two earlier drafts... inferred it wrong', and 'the call graph out of `clean` is what found it'). That is roughly 5,500 bytes, 19% of the file, and nothing an implementer builds is lost."}
{"role":"ax-economist","location":"§4","class":"emission-not-stripped-by-compact","severity":"medium","category":"redundancy","finding":"This prices the cost of the round-10 revert, which I asked for and did not price. The reverted predicate is right — a breakdown of the rows in front of it needs no policy — but it makes an opt-in feature's output unconditional. Measured this session: 70 of 90 tracked `audit-round-*.ndjson` files (78%) carry at least one non-`PASS` row, and the resolved default is `all`, which no project in this repository's corpus has ever overridden. Under `all` the map feeds no decision the payload does not already carry: a round is unclean iff any non-`PASS` row exists, which `findings` and `by_status` state, and the severity mix changes nothing about the verdict. So `by_severity` is emitted on ~78% of `tp audit --merge` invocations to serve the operators who have set `blocking` plus the once-a-cycle default decision §2.1 describes. §4 then adds 'and it survives `--compact`', which is where a byte an agent does not need is supposed to be stripped — and `overlap_report`, the only other map on this payload, IS stripped under `--compact` as explanatory. The map is worth emitting; it is not worth emitting in the payload whose entire purpose is that an agent asked for less.","suggestion":"Reverse the `--compact` clause: `by_severity` is emitted whenever the round holds a non-`PASS` row and is stripped under `--compact`, like `overlap_report`. Change §7 row 16 to assert the strip, with the mutant 'keep it under `--compact`, which bills every default-policy round for a breakdown its verdict does not read'. An operator making §2.1's decision is not running `--compact`."}
{"role":"ax-economist","location":"§6a","class":"over-specification","severity":"medium","category":"feasibility","finding":"§6a item 1 and §7 row 18 pin a count of test-internal call sites and then pin the counting rule that produces it: 'Two of the eleven `assert.`/`require.` call sites in `TestAuditGate_NoEscapeHatchFromTheGate` (`internal/cli/audit_signal_test.go`); the other nine stand — nine under that counting rule, which includes two `require.True(t, ok)` decode checks, and the rule is stated because without it the number is 7.' Row 18 then asserts eleven becomes nine. Nothing here is establishable except against code, and the numbers move on any unrelated edit to that test — adding one decode check to an assertion the release does not touch turns 11 into 12 and reddens a row that is measuring nothing about this release. This is the canonical `over-specification` smell: the spec is prescribing a mechanism whose correctness only the tree can establish, in the section a decomposer reads to size the work. The behaviour that matters is stateable without a number.","suggestion":"§6a item 1: 'the gate guard's assertion that `audit_converge_on` is an unknown workflow field is removed; every other assertion in that test stands.' §7 row 18: 'after the change, `TestAuditGate_NoEscapeHatchFromTheGate` contains no assertion naming `audit_converge_on`, and the assertions that do not name it are unchanged — mutant: delete the surrounding block, which also removes assertions the release does not reverse.' That is checkable by running, needs no counting rule, and does not rot."}
{"role":"ax-economist","location":"§3","class":"rejected-alternatives-duplicated-in-tests","severity":"medium","category":"redundancy","finding":"§3's three-row rejected-rules table (L156-170, 1,289 bytes counted this session) is already carried by §7's mutant column, so the release states the same three failures twice and a decomposer reads both. Row 13's mutant is 'a field-scoped or value-scoped fence, both of which refuse the third and deadlock Workflow A step 6' — that is table rows 1 and 2. Row 13b's mutant is 'build the fence from the three shapes in §3's table, which do not reach this input' — that is table row 3's shape plus the uncovering case. The mutant column is the better home for the reasoning, because a mutant is checked by running while a table of rejected designs is checked by reading, which is the substitution the file's own header paragraph says this rewrite was about.","suggestion":"Cut the table and its lead-in (L158-165), keep the normative sentence 'The fence refuses a write that CHANGES the resolved value to `blocking` — not the field, not the value, not a transition between literals', and keep the paragraph after it that says why the change rule answers all three. Reword row 13b's mutant so it no longer references the deleted table: 'a fence scoped to the field, to the value, or to an `all` to `blocking` transition — none of which reaches an import naming neither literal'. Returns ~1,100 bytes and removes a drift pair."}
{"role":"ax-economist","location":"§5","class":"pointer-stated-twice","severity":"low","category":"redundancy","finding":"§5 is a whole section (781 bytes) whose payload is a pointer §1 already gives in two lines. §1 L37-38: 'A fourth, a spec-hash reset for `consecutive_clean`, was in this release through five rounds and is now `spec/0.46.0.md` §8.4. §5 records why, so the next reader does not re-propose it.' §5 then ends 'It is `spec/0.46.0.md` §8.4 with both options priced.' Everything between is the drafting history of a mechanism this release does not contain — three rounds, three falsified drafts, an inert marker, and a 0-of-15 measurement. The measurement is the only durable content, and it is an argument that belongs in spec/0.46.0.md §8.4 where the decision is actually priced. The spec already carries eight pointers to spec/0.46.0.md (counted); this is the only one that costs a section.","suggestion":"Delete §5. Extend §1's existing sentence to carry the one durable fact: '...is now `spec/0.46.0.md` §8.4, where both options are priced — `IsLegacyRound`, the marker three drafts proposed, changes the streak in 0 of 15 recorded audit histories.' Renumber nothing else; §6 and §6a are already the sections after it."}
