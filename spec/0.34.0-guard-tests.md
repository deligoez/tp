# v0.34.0 guard-test list

Populated by the first task of v0.34.0 (see `spec/0.34.0.md` §7.1). One `file:function` per line.

Derivation: every test function added or modified by a commit falling between the
`chore(<version>): record audit round N` commits of v0.31.2, v0.32.0 and v0.33.0.

The §7 sweep is capped at this list plus any test a removal decision depends on.
